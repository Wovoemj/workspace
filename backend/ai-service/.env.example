# AI 服务环境变量配置

## Kimi / Moonshot AI 配置

```bash
# Moonshot API Key（必需）- 从 platform.moonshot.cn 获取
MOONSHOT_API_KEY=sk-JcUSqOZ2FUdxICD3Plubl72fUJhY4j8cuWRpnvkjMlavNOI5

# 可选：指定模型（默认 kimi-k2.5）
# kimi-k2.5: Kimi 2.5 最新模型（推荐）✅
# kimi-k2.6: Kimi 2.6 最新模型
# kimi-k2: Kimi 2 基础模型
# kimi-k2-thinking: Kimi 2 思考模型
# moonshot-v1-8k: 8K上下文
# moonshot-v1-32k: 32K上下文
# moonshot-v1-128k: 128K上下文
MOONSHOT_MODEL=kimi-k2.5
```

## Redis 配置

```bash
# Redis 地址（默认 localhost:6379）
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# 可选：Redis 数据库编号
REDIS_DB=0
```

## 服务端口

```bash
# 服务监听端口（默认 8080）
PORT=8080
```

## 启动方式

### 方式1：直接运行
```bash
cd backend/ai-service
set MOONSHOT_API_KEY=sk-你的密钥
go run main.go moonshot_service.go
```

### 方式2：使用 .env 文件
```bash
# 复制配置文件
copy .env.example .env

# 编辑 .env 填入你的 API Key
# 使用 godotenv 加载
go run main.go moonshot_service.go
```

## 启动方式

### 直接运行
```bash
cd backend/ai-service
go run main.go moonshot_service.go
```

### 测试 API

```bash
# 健康检查
curl http://localhost:8084/health

# 发送消息
curl -X POST http://localhost:8084/api/ai/chat ^
  -H "Content-Type: application/json" ^
  -d "{\"message\": \"推荐一些适合冬天去的旅游目的地\"}"
```

## 常见问题

### 1. API 调用失败
- 检查 `MOONSHOT_API_KEY` 是否正确
- 检查网络是否能访问 `api.moonshot.cn`

### 2. 回答不够智能
- 调整 `temperature` 参数（0.0-1.0，越高越有创意）
- 优化 `systemPrompt` 系统提示词

### 3. 响应太慢
- 启用流式响应 `/api/ai/chat/stream`
- 使用更小的模型 `moonshot-v1-8k`
