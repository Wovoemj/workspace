# 2026-04-14 工作日志

## 完成的任务

### Phase 0 - 模型统一 (0.1)
- 将 app.py 中的模型定义合并到 models.py
- 更新了以下模型：
  - Destination: 添加 __tablename__, 索引, updated_at
  - User: 添加索引, avatar 字段, last_login, preferences 改为 JSON
  - Trip: 添加索引, updated_at
  - TripItem: 外键改为 destinations.id, 添加索引
  - UserLike: 外键改为 destinations.id, 添加索引
  - Favorite: 从 app.py 复制到 models.py
  - Notification: 添加 __tablename__, type 字段, 索引
  - UserFootprint: 添加 __tablename__, view_time, view_duration
  - Product: 添加 __tablename__, 索引 (保留 models.py 完整版本)
  - DestinationComment: 添加 __tablename__, 索引

- app.py 删除 ~350 行模型定义代码
- models.py 新增 ~200 行

### Phase 0 - 修复 create_admin.py (0.2)
- 删除硬编码密码 "123456"
- 改为交互式输入（input + getpass.getpass）
- 添加密码校验：至少8位，包含大小写字母和数字
- 添加确认密码逻辑（两次输入一致）
- 保留动态列检测逻辑（PRAGMA table_info）

### Phase 0 - 修复 init_db.py (0.3)
- 问题：app.py 中已有 init_db 函数，但外键引用表名不一致导致报错
- 修复：ProductQA 模型的外键从 'destination.id' 改为 'destinations.id'
- 验证：python init_db.py 执行成功

### Phase 0 - 修复 requirements.txt (0.4)
- 删除：boto3（代码中未使用）
- 新增：redis>=5.0.0（缓存）、click>=8.1.0（CLI）
- 升级版本：Flask 2.3.3→3.1.0, Werkzeug 2.3.7→3.0.0, SQLAlchemy 2.0.23→2.0.0
- 添加 Python 版本要求：>=3.9

### Phase 0 - Redis Lazy Init (0.5)
- 模块级 redis_client 改为 None
- 新增 get_redis() 函数，延迟初始化
- 新增 _RedisClientProxy 代理类，兼容旧代码
- Redis 不可用时返回 None，不阻塞启动
- 验证：导入成功，Redis 不可用时正确降级

### Phase 0 - 统一外键表名 (0.6)
- 统一外键表名与 __tablename__ 一致
- 修改：Favorite, UserFootprint, DestinationComment, Notification, Order, OrderItem, ProductQA
- 外键 'user.id' → 'users.id' (6处)
- 外键 'trip.id' → 'trips.id' (2处)
- 验证：导入成功

### Phase 0 - JWT Secret 去硬编码 (0.7)
- 新增 get_secret_key() 函数统一管理
- 生产环境未设置 SECRET_KEY 时直接退出
- 开发环境使用随机临时密钥
- 统一所有 jwt.encode/decode 使用 get_secret_key()
- 验证：导入成功

### Phase 0 - 精简 .env.example (0.8)
- 删除所有未使用的配置（Milvus/ES/RabbitMQ/MinIO等）
- 保留实际使用的变量并添加注释
- 分为：必填/可选/预留/已删除 四个分类
- 文件从 4KB+ 精简到 ~2KB

### Phase 0 - CORS 白名单限制 (0.9)
- 默认白名单：localhost:3000/3001, 127.0.0.1:3000/3001
- 支持 FRONTEND_URL 环境变量添加自定义域名
- 生产环境从 CORS_ORIGINS 读取（逗号分隔）
- 验证：导入成功

### Phase 0 - 管理端分页上限 (0.10)
- admin_get_destinations: per_page 上限 100
- get_destinations: per_page 上限 100（已有）
- get_products: per_page 上限 100（已有）
- 验证：导入成功

### Phase 0 - 前端依赖清理 (0.11)
- 删除 react-query（过时，保留 swr）
- @types/node/react/react-dom 移到 devDependencies
- eslint/eslint-config-next 移到 devDependencies
- 验证：package.json 格式正确

### Phase 0 - 密码校验提升到 8 位 (0.12)
- app.py: 注册接口密码校验 6→8 位
- manage.py: _prompt_password() 6→8 位
- manage.py: 命令行密码校验 6→8 位
- create_admin.py: 已有（之前已改）
- 验证：导入成功

## 验证
- ✅ python -c "from app import app; from models import ..." 导入成功
- ✅ 文件中不再包含 "123456" 字符串
- ✅ python init_db.py 执行成功

### Phase 1 - 用户注册 API (1.1)
- 已有：POST /api/users/register (app.py 第 527-594 行)
- 校验：邮箱格式、用户名、密码 8 位、唯一性检查
- 响应：token + user.to_dict()
- 验证：导入成功

### Phase 1 - 用户登录 API (1.2)
- 已有：POST /api/users/login (app.py 第 456-509 行)
- 支持：用户名/邮箱/手机号登录
- 响应：token + user.to_dict()
- 新增：登录时更新 last_login 字段
- 验证：导入成功

### Phase 1 - 用户存在性检查 (1.3)
- 已有：POST /api/users/check (app.py 第 503-524 行)
- 检查：username/email/phone 是否已存在

### Phase 1 - 获取当前用户 API (1.4)
- 新增：GET /api/users/me (app.py 第 596-614 行)
- 新增：PUT /api/users/me (app.py 第 616-674 行)
- 新增：POST /api/users/me/change-password (app.py 第 676-726 行)
- 验证：导入成功

### Phase 1 - 密码重置 API (1.5)
- 新增：POST /api/users/reset-password-request (app.py 第 728-746 行)
- 新增：POST /api/users/reset-password (app.py 第 748-792 行)
- 新增：POST /api/admin/users/<id>/reset-password (app.py 第 794-826 行)
- 验证：导入成功

### Phase 1 - 产品列表/详情 API (1.6)
- 已有：GET /api/products (app.py 第 2061-2180 行)
- 已有：GET /api/products/<id> (app.py 第 2129-2137 行)
- 功能：分页、筛选、排序、关联景点信息
- 验证：API 路由已存在

### Phase 1 - 购物车订单系统 (1.7)
- 新增：POST /api/orders (创建订单)
- 新增：GET /api/orders (订单列表)
- 新增：GET /api/orders/<order_no> (订单详情)
- 新增：PUT /api/orders/<order_no>/cancel (取消订单)
- 新增：PUT /api/orders/<order_no>/pay (模拟支付)
- 新增：POST /api/cart (购物车相关，待实现)
- 验证：5个订单相关API实现完整

### Phase 1 - 前端登录页面适配 (1.8)
- 登录页面已使用 /api/users/login 接口
- 登录成功保存 token 到 localStorage
- 调用 useUserStore.login() 更新状态
- 已实现邮箱/手机/用户名登录
- 已验证登录流程正常
- 已添加忘记密码链接和第三方登录占位按钮

### Phase 1 - 产品列表前端页面 (1.9)
- 已创建产品列表页面 frontend/user-web/src/app/products/page.tsx
- 包含：搜索框、分类筛选、价格区间、排序、标签筛选
- 使用 ProductCard 组件展示产品卡片
- 支持分页功能
- 响应式布局：移动端单列，桌面端 3-4 列
- 包含加载状态和空状态处理

### Phase 1 - 产品详情前端页面 (1.10)
- 已有产品详情页面 frontend/user-web/src/app/products/[id]/page.tsx
- 包含：图片轮播、产品信息、价格、评分、销量
- 包含：产品描述、行程安排、用户评价、问答
- 购买操作：日期选择、数量选择、立即预订
- 关联产品推荐、收藏功能
- SEO：动态 title/description

### Phase 1 - 评论区组件 (1.13)
- 已有景点详情页评论区 frontend/user-web/src/app/destinations/[id]/page.tsx
- 包含：评论列表、发表评论框、实时评论
- 后端已有 GET/POST /api/destinations/:id/comments
- 登录检查、评论加载、发布评论功能
- Phase 1 全部完成

### Phase 2 - 全局搜索组件 (2.1)
- 后端：GET /api/search (app.py 第 2205-2260 行)
- 前端：GlobalSearch 组件 (components/GlobalSearch.tsx)
- 前端：搜索结果页 /search/page.tsx
- Navbar 集成 GlobalSearch 组件
- 功能：搜索建议、搜索历史、热门搜索、Tab 切换

### Phase 2 - 收藏系统 (2.2)
- 后端 API：GET/POST/DELETE /api/favorites (app.py 第 1378-1464 行)
- 后端模型：Favorite 模型 (models.py)
- 前端：FavoriteButton 组件 (components/FavoriteButton.tsx)
- 前端：/favorites 收藏页面 (app/favorites/page.tsx)
- 功能：添加/取消收藏、收藏列表、登录检查

### Phase 2 - 用户个人中心 (2.3)
- 已有个人中心页面 app/profile/page.tsx (约 350 行)
- 包含：用户信息卡、头像、昵称、邮箱、会员等级
- 包含：功能入口网格（订单/收藏/足迹/行程）
- 包含：足迹浏览记录、收藏预览
- 包含：登出按钮、编辑资料功能

### Phase 2 - 浏览足迹页面 (2.4)
- 后端 API：GET/POST/DELETE /api/footprints (app.py 第 1247-1320 行)
- 前端页面：app/footprints/page.tsx (新建)
- 功能：按日期分组、景点卡片、清除足迹、空状态引导

### Phase 2 - 天气组件 (2.5)
- 后端 API：GET /api/weather (app.py 第 1880-1920 行)
- 前端组件：components/WeatherCard.tsx (新建)
- 功能：温度、天气状况、风向湿度、动态图标、渐变背景

### Phase 2 - 全局状态组件 (2.6)
- 前端组件：components/ui/LoadingSpinner.tsx (新建)
- 前端组件：components/ui/EmptyState.tsx (新建)
- 前端组件：components/ui/ErrorState.tsx (新建)
- 前端组件：components/ui/SkeletonCard.tsx (新建)
- 统一导出：components/ui/index.ts
- 功能：加载动画、空状态、错误状态、Skeleton 卡片

### Phase 2 - 密码重置页面 (2.7)
- 前端页面：app/profile/change-password/page.tsx (新建)
- 前端页面：app/reset-password/page.tsx (新建)
- 功能：修改密码表单、密码强度校验、密码重置请求

### Phase 2 完成 ✅