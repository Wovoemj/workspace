# `frontend/user-web/src/i18n.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `// Simplified i18n config - using static locale for now` -> 注释行，用于解释设计意图或使用说明。
- 2: `export const locale = 'zh-CN'` -> 导出当前声明，供其他模块复用。
- 3: `export const locales = ['zh-CN', 'en', 'ja']` -> 导出当前声明，供其他模块复用。
- 4: `export const defaultLocale = 'zh-CN'` -> 导出当前声明，供其他模块复用。
- 5: `(空行)` -> 空行，用于提升代码结构可读性。
- 6: `export function getLocale() {` -> 导出当前声明，供其他模块复用。
- 7: `return locale` -> 返回结果或提前结束当前流程。
- 8: `}` -> 结束当前语句或代码块。
