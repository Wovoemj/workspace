# `frontend/user-web/src/components/TripCalendar.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `/**` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 行程日历组件 (TripCalendar)` -> 注释行，用于解释设计意图或使用说明。
- 6: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 7: `*` -> 注释行，用于解释设计意图或使用说明。
- 8: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 行程日历可视化展示` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 显示行程期间每天的行程安排` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 支持月份切换浏览` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 显示每天的项目数量指示器` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 快速跳转到指定天数` -> 注释行，用于解释设计意图或使用说明。
- 14: `*` -> 注释行，用于解释设计意图或使用说明。
- 15: `* 日历特点：` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 行程内的日期高亮显示` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 当天日期有特殊标识` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - 支持项目数量气泡显示` -> 注释行，用于解释设计意图或使用说明。
- 19: `*/` -> 注释行，用于解释设计意图或使用说明。
- 20: `(空行)` -> 空行，用于提升代码结构可读性。
- 21: `import { useMemo, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 22: `import { ChevronLeft, ChevronRight, MapPin } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `/** 行程中的单个项目类型 */` -> 注释行，用于解释设计意图或使用说明。
- 25: `type TripItem = {` -> 定义类型或类结构，约束数据与行为。
- 26: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 27: `day_number: number` -> 执行当前语句，参与该文件整体逻辑。
- 28: `title?: string` -> 执行当前语句，参与该文件整体逻辑。
- 29: `location?: string` -> 执行当前语句，参与该文件整体逻辑。
- 30: `destination?: {` -> 执行当前语句，参与该文件整体逻辑。
- 31: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 32: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 33: `cover_image?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 34: `}` -> 结束当前语句或代码块。
- 35: `}` -> 结束当前语句或代码块。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `type Trip = {` -> 定义类型或类结构，约束数据与行为。
- 38: `start_date?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 39: `end_date?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 40: `}` -> 结束当前语句或代码块。
- 41: `(空行)` -> 空行，用于提升代码结构可读性。
- 42: `interface TripCalendarProps {` -> 定义类型或类结构，约束数据与行为。
- 43: `trip: Trip` -> 执行当前语句，参与该文件整体逻辑。
- 44: `items: TripItem[]` -> 执行当前语句，参与该文件整体逻辑。
- 45: `onDayClick: (day: number) => void` -> 执行当前语句，参与该文件整体逻辑。
- 46: `selectedDay: number` -> 执行当前语句，参与该文件整体逻辑。
- 47: `}` -> 结束当前语句或代码块。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `/**` -> 注释行，用于解释设计意图或使用说明。
- 50: `* 行程日历组件` -> 注释行，用于解释设计意图或使用说明。
- 51: `* @description 行程日期可视化日历组件` -> 注释行，用于解释设计意图或使用说明。
- 52: `* @param props - 组件属性` -> 注释行，用于解释设计意图或使用说明。
- 53: `* @param props.trip - 行程信息（起止日期）` -> 注释行，用于解释设计意图或使用说明。
- 54: `* @param props.items - 行程项目列表` -> 注释行，用于解释设计意图或使用说明。
- 55: `* @param props.onDayClick - 日期点击回调` -> 注释行，用于解释设计意图或使用说明。
- 56: `* @param props.selectedDay - 当前选中的天数` -> 注释行，用于解释设计意图或使用说明。
- 57: `*/` -> 注释行，用于解释设计意图或使用说明。
- 58: `export function TripCalendar({ trip, items, onDayClick, selectedDay }: TripCalendarProps) {` -> 导出当前声明，供其他模块复用。
- 59: `const [currentMonth, setCurrentMonth] = useState(() => {` -> 声明变量或常量，保存运行时数据。
- 60: `if (trip.start_date) {` -> 条件判断分支，根据场景执行不同逻辑。
- 61: `return new Date(trip.start_date)` -> 返回结果或提前结束当前流程。
- 62: `}` -> 结束当前语句或代码块。
- 63: `return new Date()` -> 返回结果或提前结束当前流程。
- 64: `})` -> 执行当前语句，参与该文件整体逻辑。
- 65: `(空行)` -> 空行，用于提升代码结构可读性。
- 66: `// 按天统计每天的项目数` -> 注释行，用于解释设计意图或使用说明。
- 67: `const dayItemCounts = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 68: `const counts = new Map<number, number>()` -> 声明变量或常量，保存运行时数据。
- 69: `for (const item of items) {` -> 循环处理集合或重复执行逻辑。
- 70: `const day = Number(item.day_number || 1)` -> 声明变量或常量，保存运行时数据。
- 71: `counts.set(day, (counts.get(day) || 0) + 1)` -> 执行当前语句，参与该文件整体逻辑。
- 72: `}` -> 结束当前语句或代码块。
- 73: `return counts` -> 返回结果或提前结束当前流程。
- 74: `}, [items])` -> 执行当前语句，参与该文件整体逻辑。
- 75: `(空行)` -> 空行，用于提升代码结构可读性。
- 76: `// 获取总天数` -> 注释行，用于解释设计意图或使用说明。
- 77: `const totalDays = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 78: `if (!trip.start_date || !trip.end_date) {` -> 条件判断分支，根据场景执行不同逻辑。
- 79: `return items.length > 0 ? Math.max(...items.map(i => i.day_number)) : 0` -> 返回结果或提前结束当前流程。
- 80: `}` -> 结束当前语句或代码块。
- 81: `const start = new Date(trip.start_date)` -> 声明变量或常量，保存运行时数据。
- 82: `const end = new Date(trip.end_date)` -> 声明变量或常量，保存运行时数据。
- 83: `return Math.ceil((end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24)) + 1` -> 返回结果或提前结束当前流程。
- 84: `}, [trip, items])` -> 执行当前语句，参与该文件整体逻辑。
- 85: `(空行)` -> 空行，用于提升代码结构可读性。
- 86: `const year = currentMonth.getFullYear()` -> 声明变量或常量，保存运行时数据。
- 87: `const month = currentMonth.getMonth()` -> 声明变量或常量，保存运行时数据。
- 88: `(空行)` -> 空行，用于提升代码结构可读性。
- 89: `const firstDayOfMonth = new Date(year, month, 1).getDay()` -> 声明变量或常量，保存运行时数据。
- 90: `const daysInMonth = new Date(year, month + 1, 0).getDate()` -> 声明变量或常量，保存运行时数据。
- 91: `(空行)` -> 空行，用于提升代码结构可读性。
- 92: `const monthNames = ['一月', '二月', '三月', '四月', '五月', '六月', '七月', '八月', '九月', '十月', '十一月', '十二月']` -> 声明变量或常量，保存运行时数据。
- 93: `const weekNames = ['日', '一', '二', '三', '四', '五', '六']` -> 声明变量或常量，保存运行时数据。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `// 计算行程开始日期对应的日历偏移` -> 注释行，用于解释设计意图或使用说明。
- 96: `const tripStartDate = trip.start_date ? new Date(trip.start_date) : new Date()` -> 声明变量或常量，保存运行时数据。
- 97: `const calendarStartDate = new Date(year, month, 1)` -> 声明变量或常量，保存运行时数据。
- 98: `(空行)` -> 空行，用于提升代码结构可读性。
- 99: `function prevMonth() {` -> 定义函数或方法，实现具体业务逻辑。
- 100: `setCurrentMonth(new Date(year, month - 1, 1))` -> 执行当前语句，参与该文件整体逻辑。
- 101: `}` -> 结束当前语句或代码块。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `function nextMonth() {` -> 定义函数或方法，实现具体业务逻辑。
- 104: `setCurrentMonth(new Date(year, month + 1, 1))` -> 执行当前语句，参与该文件整体逻辑。
- 105: `}` -> 结束当前语句或代码块。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `// 判断某一天是否在行程范围内` -> 注释行，用于解释设计意图或使用说明。
- 108: `function getDayInfo(day: number) {` -> 定义函数或方法，实现具体业务逻辑。
- 109: `const tripDay = day - Math.floor((calendarStartDate.getTime() - tripStartDate.getTime()) / (1000 * 60 * 60 * 24))` -> 声明变量或常量，保存运行时数据。
- 110: `if (tripDay >= 1 && tripDay <= totalDays) {` -> 条件判断分支，根据场景执行不同逻辑。
- 111: `const itemCount = dayItemCounts.get(tripDay) || 0` -> 声明变量或常量，保存运行时数据。
- 112: `return { isInTrip: true, tripDay, itemCount }` -> 返回结果或提前结束当前流程。
- 113: `}` -> 结束当前语句或代码块。
- 114: `return { isInTrip: false, tripDay: 0, itemCount: 0 }` -> 返回结果或提前结束当前流程。
- 115: `}` -> 结束当前语句或代码块。
- 116: `(空行)` -> 空行，用于提升代码结构可读性。
- 117: `const days = []` -> 声明变量或常量，保存运行时数据。
- 118: `for (let i = 0; i < firstDayOfMonth; i++) {` -> 循环处理集合或重复执行逻辑。
- 119: `days.push(<div key={\`empty-${i}\`} className="h-10" />)` -> 执行当前语句，参与该文件整体逻辑。
- 120: `}` -> 结束当前语句或代码块。
- 121: `for (let day = 1; day <= daysInMonth; day++) {` -> 循环处理集合或重复执行逻辑。
- 122: `const { isInTrip, tripDay, itemCount } = getDayInfo(day)` -> 声明变量或常量，保存运行时数据。
- 123: `const isSelected = isInTrip && tripDay === selectedDay` -> 声明变量或常量，保存运行时数据。
- 124: `const isToday = new Date().toDateString() === new Date(year, month, day).toDateString()` -> 声明变量或常量，保存运行时数据。
- 125: `(空行)` -> 空行，用于提升代码结构可读性。
- 126: `days.push(` -> 执行当前语句，参与该文件整体逻辑。
- 127: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 128: `key={day}` -> 执行当前语句，参与该文件整体逻辑。
- 129: `onClick={() => isInTrip && onDayClick(tripDay)}` -> 执行当前语句，参与该文件整体逻辑。
- 130: `disabled={!isInTrip}` -> 执行当前语句，参与该文件整体逻辑。
- 131: `className={\`` -> 执行当前语句，参与该文件整体逻辑。
- 132: `relative h-10 w-10 rounded-lg text-sm font-medium transition-all` -> 执行当前语句，参与该文件整体逻辑。
- 133: `${isInTrip` -> 执行当前语句，参与该文件整体逻辑。
- 134: `? 'cursor-pointer hover:bg-sky-100'` -> 执行当前语句，参与该文件整体逻辑。
- 135: `: 'cursor-default text-gray-300'` -> 执行当前语句，参与该文件整体逻辑。
- 136: `}` -> 结束当前语句或代码块。
- 137: `${isSelected` -> 执行当前语句，参与该文件整体逻辑。
- 138: `? 'bg-gradient-to-r from-sky-500 to-indigo-500 text-white shadow-md'` -> 执行当前语句，参与该文件整体逻辑。
- 139: `: isInTrip` -> 执行当前语句，参与该文件整体逻辑。
- 140: `? 'bg-sky-50 text-sky-700'` -> 执行当前语句，参与该文件整体逻辑。
- 141: `: ''` -> 执行当前语句，参与该文件整体逻辑。
- 142: `}` -> 结束当前语句或代码块。
- 143: `${isToday && !isSelected ? 'ring-2 ring-sky-300' : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 144: `\`}` -> 执行当前语句，参与该文件整体逻辑。
- 145: `>` -> 执行当前语句，参与该文件整体逻辑。
- 146: `{day}` -> 执行当前语句，参与该文件整体逻辑。
- 147: `{isInTrip && itemCount > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 148: `<span className={\`absolute -bottom-1 left-1/2 h-1.5 w-1.5 -translate-x-1/2 rounded-full ${` -> 执行当前语句，参与该文件整体逻辑。
- 149: `isSelected ? 'bg-white' : 'bg-sky-500'` -> 执行当前语句，参与该文件整体逻辑。
- 150: `}\`} />` -> 执行当前语句，参与该文件整体逻辑。
- 151: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 152: `{isInTrip && (` -> 执行当前语句，参与该文件整体逻辑。
- 153: `<span className={\`absolute -right-0.5 -top-0.5 flex h-4 w-4 items-center justify-center rounded-full text-[8px] ${` -> 执行当前语句，参与该文件整体逻辑。
- 154: `isSelected ? 'bg-white text-sky-600' : 'bg-indigo-500 text-white'` -> 执行当前语句，参与该文件整体逻辑。
- 155: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 156: `{tripDay}` -> 执行当前语句，参与该文件整体逻辑。
- 157: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 158: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 159: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `)` -> 结束当前语句或代码块。
- 161: `}` -> 结束当前语句或代码块。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `return (` -> 返回结果或提前结束当前流程。
- 164: `<div className="rounded-2xl border border-gray-100 bg-white p-4 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 165: `{/* 月份切换 */}` -> 执行当前语句，参与该文件整体逻辑。
- 166: `<div className="mb-4 flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 168: `onClick={prevMonth}` -> 执行当前语句，参与该文件整体逻辑。
- 169: `className="rounded-lg p-2 hover:bg-gray-100"` -> 执行当前语句，参与该文件整体逻辑。
- 170: `>` -> 执行当前语句，参与该文件整体逻辑。
- 171: `<ChevronLeft className="h-5 w-5 text-gray-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 173: `<h3 className="text-lg font-semibold text-gray-800">` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `{year}年 {monthNames[month]}` -> 执行当前语句，参与该文件整体逻辑。
- 175: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 177: `onClick={nextMonth}` -> 执行当前语句，参与该文件整体逻辑。
- 178: `className="rounded-lg p-2 hover:bg-gray-100"` -> 执行当前语句，参与该文件整体逻辑。
- 179: `>` -> 执行当前语句，参与该文件整体逻辑。
- 180: `<ChevronRight className="h-5 w-5 text-gray-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `(空行)` -> 空行，用于提升代码结构可读性。
- 184: `{/* 星期标题 */}` -> 执行当前语句，参与该文件整体逻辑。
- 185: `<div className="mb-2 grid grid-cols-7 gap-1 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 186: `{weekNames.map(week => (` -> 执行当前语句，参与该文件整体逻辑。
- 187: `<div key={week} className="h-8 text-xs font-medium text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 188: `{week}` -> 执行当前语句，参与该文件整体逻辑。
- 189: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 190: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 191: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 192: `(空行)` -> 空行，用于提升代码结构可读性。
- 193: `{/* 日历格子 */}` -> 执行当前语句，参与该文件整体逻辑。
- 194: `<div className="grid grid-cols-7 gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 195: `{days}` -> 执行当前语句，参与该文件整体逻辑。
- 196: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 197: `(空行)` -> 空行，用于提升代码结构可读性。
- 198: `{/* 图例 */}` -> 执行当前语句，参与该文件整体逻辑。
- 199: `<div className="mt-4 flex items-center justify-center gap-4 text-xs text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 200: `<div className="flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 201: `<span className="h-2 w-2 rounded-full bg-sky-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 202: `有行程` -> 执行当前语句，参与该文件整体逻辑。
- 203: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 204: `<div className="flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 205: `<span className="h-2 w-2 rounded-full bg-gray-300" />` -> JSX/HTML 结构行，用于描述页面元素。
- 206: `无行程` -> 执行当前语句，参与该文件整体逻辑。
- 207: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 208: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 209: `(空行)` -> 空行，用于提升代码结构可读性。
- 210: `{/* 快速跳转 */}` -> 执行当前语句，参与该文件整体逻辑。
- 211: `{totalDays > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 212: `<div className="mt-4 border-t pt-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 213: `<p className="mb-2 text-xs font-medium text-gray-500">快速跳转</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 214: `<div className="flex flex-wrap gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 215: `{Array.from({ length: Math.min(totalDays, 14) }, (_, i) => i + 1).map(day => (` -> 执行当前语句，参与该文件整体逻辑。
- 216: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 217: `key={day}` -> 执行当前语句，参与该文件整体逻辑。
- 218: `onClick={() => onDayClick(day)}` -> 执行当前语句，参与该文件整体逻辑。
- 219: `className={\`rounded-full px-3 py-1 text-xs font-medium transition-colors ${` -> 执行当前语句，参与该文件整体逻辑。
- 220: `selectedDay === day` -> 执行当前语句，参与该文件整体逻辑。
- 221: `? 'bg-gradient-to-r from-sky-500 to-indigo-500 text-white'` -> 执行当前语句，参与该文件整体逻辑。
- 222: `: 'bg-gray-100 text-gray-600 hover:bg-gray-200'` -> 执行当前语句，参与该文件整体逻辑。
- 223: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 224: `>` -> 执行当前语句，参与该文件整体逻辑。
- 225: `第{day}天` -> 执行当前语句，参与该文件整体逻辑。
- 226: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 228: `{totalDays > 14 && (` -> 执行当前语句，参与该文件整体逻辑。
- 229: `<span className="flex items-center text-xs text-gray-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 230: `...共{totalDays}天` -> 执行当前语句，参与该文件整体逻辑。
- 231: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 232: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 233: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 236: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `)` -> 结束当前语句或代码块。
- 238: `}` -> 结束当前语句或代码块。
