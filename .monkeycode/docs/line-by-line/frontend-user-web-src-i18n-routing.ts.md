# `frontend/user-web/src/i18n/routing.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `// Simplified routing config` -> 注释行，用于解释设计意图或使用说明。
- 2: `export const routing = {` -> 导出当前声明，供其他模块复用。
- 3: `locales: ['zh-CN', 'en', 'ja'],` -> 执行当前语句，参与该文件整体逻辑。
- 4: `defaultLocale: 'zh-CN'` -> 执行当前语句，参与该文件整体逻辑。
- 5: `}` -> 结束当前语句或代码块。
