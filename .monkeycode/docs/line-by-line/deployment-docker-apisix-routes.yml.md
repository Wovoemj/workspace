# `deployment/docker/apisix/routes.yml` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# 路由配置 - 将请求转发到后端服务` -> 注释行，用于解释设计意图或使用说明。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `# Flask 后端 (端口 5000)` -> 注释行，用于解释设计意图或使用说明。
- 4: `routes:` -> 执行当前语句，参与该文件整体逻辑。
- 5: `- id: flask-api` -> 执行当前语句，参与该文件整体逻辑。
- 6: `uri: /api/*` -> 执行当前语句，参与该文件整体逻辑。
- 7: `upstream:` -> 执行当前语句，参与该文件整体逻辑。
- 8: `type: roundrobin` -> 执行当前语句，参与该文件整体逻辑。
- 9: `nodes:` -> 执行当前语句，参与该文件整体逻辑。
- 10: `- host: host.docker.internal` -> 执行当前语句，参与该文件整体逻辑。
- 11: `port: 5000` -> 执行当前语句，参与该文件整体逻辑。
- 12: `weight: 1` -> 执行当前语句，参与该文件整体逻辑。
- 13: `timeout:` -> 执行当前语句，参与该文件整体逻辑。
- 14: `connect: 60` -> 执行当前语句，参与该文件整体逻辑。
- 15: `send: 60` -> 执行当前语句，参与该文件整体逻辑。
- 16: `read: 120  # 较长的超时时间用于 AI 请求` -> 执行当前语句，参与该文件整体逻辑。
- 17: `(空行)` -> 空行，用于提升代码结构可读性。
- 18: `# AI 对话接口` -> 注释行，用于解释设计意图或使用说明。
- 19: `- id: ai-chat` -> 执行当前语句，参与该文件整体逻辑。
- 20: `uri: /api/ai/*` -> 执行当前语句，参与该文件整体逻辑。
- 21: `upstream:` -> 执行当前语句，参与该文件整体逻辑。
- 22: `type: roundrobin` -> 执行当前语句，参与该文件整体逻辑。
- 23: `nodes:` -> 执行当前语句，参与该文件整体逻辑。
- 24: `- host: host.docker.internal` -> 执行当前语句，参与该文件整体逻辑。
- 25: `port: 5000` -> 执行当前语句，参与该文件整体逻辑。
- 26: `weight: 1` -> 执行当前语句，参与该文件整体逻辑。
- 27: `timeout:` -> 执行当前语句，参与该文件整体逻辑。
- 28: `connect: 60` -> 执行当前语句，参与该文件整体逻辑。
- 29: `send: 180` -> 执行当前语句，参与该文件整体逻辑。
- 30: `read: 300  # AI 生成可能需要更长时间` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `# 静态资源` -> 注释行，用于解释设计意图或使用说明。
- 33: `- id: static-files` -> 执行当前语句，参与该文件整体逻辑。
- 34: `uri: /*` -> 执行当前语句，参与该文件整体逻辑。
- 35: `upstream:` -> 执行当前语句，参与该文件整体逻辑。
- 36: `type: roundrobin` -> 执行当前语句，参与该文件整体逻辑。
- 37: `nodes:` -> 执行当前语句，参与该文件整体逻辑。
- 38: `- host: host.docker.internal` -> 执行当前语句，参与该文件整体逻辑。
- 39: `port: 3000` -> 执行当前语句，参与该文件整体逻辑。
- 40: `weight: 1` -> 执行当前语句，参与该文件整体逻辑。
