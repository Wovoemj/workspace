# `database/schema/schema.sql` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `-- 智能旅游助手数据库架构设计` -> 执行当前语句，参与该文件整体逻辑。
- 2: `-- 用户表` -> 执行当前语句，参与该文件整体逻辑。
- 3: `CREATE TABLE users (` -> 执行当前语句，参与该文件整体逻辑。
- 4: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 5: `phone VARCHAR(20) UNIQUE NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 6: `email VARCHAR(100),` -> 执行当前语句，参与该文件整体逻辑。
- 7: `nickname VARCHAR(50),` -> 执行当前语句，参与该文件整体逻辑。
- 8: `avatar_url VARCHAR(500),` -> 执行当前语句，参与该文件整体逻辑。
- 9: `password_hash VARCHAR(255) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 10: `membership_level TINYINT DEFAULT 1 COMMENT '会员等级：1-普通，2-银卡，3-金卡，4-钻石',` -> 执行当前语句，参与该文件整体逻辑。
- 11: `preferences JSON COMMENT '用户偏好设置',` -> 执行当前语句，参与该文件整体逻辑。
- 12: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 13: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 14: `INDEX idx_phone (phone),` -> 执行当前语句，参与该文件整体逻辑。
- 15: `INDEX idx_email (email),` -> 执行当前语句，参与该文件整体逻辑。
- 16: `INDEX idx_membership_level (membership_level),` -> 执行当前语句，参与该文件整体逻辑。
- 17: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 18: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 19: `-- 产品表（多态设计）` -> 执行当前语句，参与该文件整体逻辑。
- 20: `CREATE TABLE products (` -> 执行当前语句，参与该文件整体逻辑。
- 21: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 22: `type ENUM('flight', 'hotel', 'ticket', 'experience') NOT NULL COMMENT '产品类型',` -> 定义类型或类结构，约束数据与行为。
- 23: `name VARCHAR(200) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 24: `description TEXT,` -> 执行当前语句，参与该文件整体逻辑。
- 25: `price DECIMAL(10,2) NOT NULL COMMENT '现价',` -> 执行当前语句，参与该文件整体逻辑。
- 26: `original_price DECIMAL(10,2) COMMENT '原价',` -> 执行当前语句，参与该文件整体逻辑。
- 27: `inventory INT DEFAULT 0 COMMENT '库存数量',` -> 执行当前语句，参与该文件整体逻辑。
- 28: `tags JSON COMMENT '标签数组',` -> 执行当前语句，参与该文件整体逻辑。
- 29: `metadata JSON COMMENT '类型特定字段',` -> 执行当前语句，参与该文件整体逻辑。
- 30: `status ENUM('active', 'inactive', 'sold_out', 'deleted') DEFAULT 'active',` -> 执行当前语句，参与该文件整体逻辑。
- 31: `images JSON COMMENT '图片URL数组',` -> 执行当前语句，参与该文件整体逻辑。
- 32: `location JSON COMMENT '位置信息',` -> 执行当前语句，参与该文件整体逻辑。
- 33: `rating DECIMAL(3,2) DEFAULT 0.00 COMMENT '评分',` -> 执行当前语句，参与该文件整体逻辑。
- 34: `review_count INT DEFAULT 0 COMMENT '评论数量',` -> 执行当前语句，参与该文件整体逻辑。
- 35: `view_count INT DEFAULT 0 COMMENT '浏览次数',` -> 执行当前语句，参与该文件整体逻辑。
- 36: `sales_count INT DEFAULT 0 COMMENT '销售数量',` -> 执行当前语句，参与该文件整体逻辑。
- 37: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 38: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 39: `INDEX idx_type (type),` -> 执行当前语句，参与该文件整体逻辑。
- 40: `INDEX idx_status (status),` -> 执行当前语句，参与该文件整体逻辑。
- 41: `INDEX idx_price (price),` -> 执行当前语句，参与该文件整体逻辑。
- 42: `INDEX idx_rating (rating),` -> 执行当前语句，参与该文件整体逻辑。
- 43: `INDEX idx_review_count (review_count),` -> 执行当前语句，参与该文件整体逻辑。
- 44: `INDEX idx_location_city ((location->>'city')),` -> 执行当前语句，参与该文件整体逻辑。
- 45: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 46: `INDEX idx_tags ((tags))` -> 执行当前语句，参与该文件整体逻辑。
- 47: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 48: `-- 行程表` -> 执行当前语句，参与该文件整体逻辑。
- 49: `CREATE TABLE itineraries (` -> 执行当前语句，参与该文件整体逻辑。
- 50: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 51: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 52: `title VARCHAR(100) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 53: `start_date DATE NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 54: `end_date DATE NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 55: `days JSON NOT NULL COMMENT '每日行程安排',` -> 执行当前语句，参与该文件整体逻辑。
- 56: `budget DECIMAL(12,2) COMMENT '预算',` -> 执行当前语句，参与该文件整体逻辑。
- 57: `tags JSON COMMENT '标签数组',` -> 执行当前语句，参与该文件整体逻辑。
- 58: `is_public BOOLEAN DEFAULT FALSE COMMENT '是否公开',` -> 执行当前语句，参与该文件整体逻辑。
- 59: `is_template BOOLEAN DEFAULT FALSE COMMENT '是否为模板',` -> 执行当前语句，参与该文件整体逻辑。
- 60: `template_category VARCHAR(50) COMMENT '模板分类',` -> 执行当前语句，参与该文件整体逻辑。
- 61: `view_count INT DEFAULT 0,` -> 执行当前语句，参与该文件整体逻辑。
- 62: `like_count INT DEFAULT 0,` -> 执行当前语句，参与该文件整体逻辑。
- 63: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 64: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 65: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 66: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 67: `INDEX idx_dates (start_date, end_date),` -> 执行当前语句，参与该文件整体逻辑。
- 68: `INDEX idx_is_public (is_public),` -> 执行当前语句，参与该文件整体逻辑。
- 69: `INDEX idx_is_template (is_template),` -> 执行当前语句，参与该文件整体逻辑。
- 70: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 71: `INDEX idx_tags ((tags))` -> 执行当前语句，参与该文件整体逻辑。
- 72: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 73: `-- 订单表` -> 执行当前语句，参与该文件整体逻辑。
- 74: `CREATE TABLE orders (` -> 执行当前语句，参与该文件整体逻辑。
- 75: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 76: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 77: `order_no VARCHAR(32) UNIQUE NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 78: `total_amount DECIMAL(12,2) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 79: `status ENUM('pending', 'paid', 'cancelled', 'completed', 'refunded') DEFAULT 'pending',` -> 执行当前语句，参与该文件整体逻辑。
- 80: `payment_method ENUM('alipay', 'wechat', 'credit_card', 'bank_transfer') COMMENT '支付方式',` -> 执行当前语句，参与该文件整体逻辑。
- 81: `payment_time TIMESTAMP NULL COMMENT '支付时间',` -> 执行当前语句，参与该文件整体逻辑。
- 82: `payment_no VARCHAR(100) COMMENT '支付流水号',` -> 执行当前语句，参与该文件整体逻辑。
- 83: `cancelled_reason TEXT COMMENT '取消原因',` -> 执行当前语句，参与该文件整体逻辑。
- 84: `refund_amount DECIMAL(12,2) COMMENT '退款金额',` -> 执行当前语句，参与该文件整体逻辑。
- 85: `refund_time TIMESTAMP NULL COMMENT '退款时间',` -> 执行当前语句，参与该文件整体逻辑。
- 86: `refund_reason TEXT COMMENT '退款原因',` -> 执行当前语句，参与该文件整体逻辑。
- 87: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 88: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 89: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 90: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 91: `INDEX idx_order_no (order_no),` -> 执行当前语句，参与该文件整体逻辑。
- 92: `INDEX idx_status (status),` -> 执行当前语句，参与该文件整体逻辑。
- 93: `INDEX idx_payment_time (payment_time),` -> 执行当前语句，参与该文件整体逻辑。
- 94: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 95: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 96: `-- 订单项表` -> 执行当前语句，参与该文件整体逻辑。
- 97: `CREATE TABLE order_items (` -> 执行当前语句，参与该文件整体逻辑。
- 98: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 99: `order_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 100: `product_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 101: `product_name VARCHAR(200) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 102: `product_type ENUM('flight', 'hotel', 'ticket', 'experience') NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 103: `quantity INT NOT NULL DEFAULT 1,` -> 执行当前语句，参与该文件整体逻辑。
- 104: `unit_price DECIMAL(10,2) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 105: `total_price DECIMAL(12,2) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 106: `booking_details JSON COMMENT '预订详情',` -> 执行当前语句，参与该文件整体逻辑。
- 107: `status ENUM('pending', 'confirmed', 'cancelled', 'completed') DEFAULT 'pending',` -> 执行当前语句，参与该文件整体逻辑。
- 108: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 109: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 110: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 111: `INDEX idx_order_id (order_id),` -> 执行当前语句，参与该文件整体逻辑。
- 112: `INDEX idx_product_id (product_id),` -> 执行当前语句，参与该文件整体逻辑。
- 113: `INDEX idx_status (status),` -> 执行当前语句，参与该文件整体逻辑。
- 114: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 115: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 116: `-- AI对话历史表` -> 执行当前语句，参与该文件整体逻辑。
- 117: `CREATE TABLE ai_conversations (` -> 执行当前语句，参与该文件整体逻辑。
- 118: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 119: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 120: `session_id VARCHAR(64) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 121: `role ENUM('user', 'assistant') NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 122: `content TEXT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 123: `intent VARCHAR(50) COMMENT '意图识别结果',` -> 执行当前语句，参与该文件整体逻辑。
- 124: `metadata JSON COMMENT '额外元数据',` -> 执行当前语句，参与该文件整体逻辑。
- 125: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 126: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 127: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 128: `INDEX idx_session_id (session_id),` -> 执行当前语句，参与该文件整体逻辑。
- 129: `INDEX idx_role (role),` -> 执行当前语句，参与该文件整体逻辑。
- 130: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 131: `INDEX idx_intent (intent)` -> 执行当前语句，参与该文件整体逻辑。
- 132: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 133: `-- 用户收藏表` -> 执行当前语句，参与该文件整体逻辑。
- 134: `CREATE TABLE user_favorites (` -> 执行当前语句，参与该文件整体逻辑。
- 135: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 136: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 137: `product_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 138: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 139: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 140: `FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 141: `UNIQUE KEY unique_user_product (user_id, product_id),` -> 执行当前语句，参与该文件整体逻辑。
- 142: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 143: `INDEX idx_product_id (product_id),` -> 执行当前语句，参与该文件整体逻辑。
- 144: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 145: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 146: `-- 用户足迹表` -> 执行当前语句，参与该文件整体逻辑。
- 147: `CREATE TABLE user_footprints (` -> 执行当前语句，参与该文件整体逻辑。
- 148: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 149: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 150: `product_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 151: `view_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 152: `view_duration INT DEFAULT 0 COMMENT '浏览时长（秒）',` -> 执行当前语句，参与该文件整体逻辑。
- 153: `action_type ENUM('view', 'click', 'search', 'book') DEFAULT 'view',` -> 执行当前语句，参与该文件整体逻辑。
- 154: `metadata JSON COMMENT '额外信息',` -> 执行当前语句，参与该文件整体逻辑。
- 155: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 156: `FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 157: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 158: `INDEX idx_product_id (product_id),` -> 执行当前语句，参与该文件整体逻辑。
- 159: `INDEX idx_action_type (action_type),` -> 执行当前语句，参与该文件整体逻辑。
- 160: `INDEX idx_view_time (view_time)` -> 执行当前语句，参与该文件整体逻辑。
- 161: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 162: `-- 产品评价表` -> 执行当前语句，参与该文件整体逻辑。
- 163: `CREATE TABLE product_reviews (` -> 执行当前语句，参与该文件整体逻辑。
- 164: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 165: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 166: `product_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 167: `rating TINYINT NOT NULL COMMENT '评分1-5',` -> 执行当前语句，参与该文件整体逻辑。
- 168: `content TEXT COMMENT '评价内容',` -> 执行当前语句，参与该文件整体逻辑。
- 169: `images JSON COMMENT '图片URL数组',` -> 执行当前语句，参与该文件整体逻辑。
- 170: `is_verified BOOLEAN DEFAULT FALSE COMMENT '是否已验证',` -> 执行当前语句，参与该文件整体逻辑。
- 171: `is_anonymous BOOLEAN DEFAULT FALSE COMMENT '是否匿名',` -> 执行当前语句，参与该文件整体逻辑。
- 172: `helpful_count INT DEFAULT 0 COMMENT '有用数量',` -> 执行当前语句，参与该文件整体逻辑。
- 173: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 174: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 175: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 176: `FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 177: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 178: `INDEX idx_product_id (product_id),` -> 执行当前语句，参与该文件整体逻辑。
- 179: `INDEX idx_rating (rating),` -> 执行当前语句，参与该文件整体逻辑。
- 180: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 181: `INDEX idx_is_verified (is_verified)` -> 执行当前语句，参与该文件整体逻辑。
- 182: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 183: `-- 推荐记录表` -> 执行当前语句，参与该文件整体逻辑。
- 184: `CREATE TABLE recommendations (` -> 执行当前语句，参与该文件整体逻辑。
- 185: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 186: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 187: `type ENUM('product', 'destination', 'itinerary') NOT NULL,` -> 定义类型或类结构，约束数据与行为。
- 188: `target_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 189: `score DECIMAL(5,4) NOT NULL COMMENT '推荐分数',` -> 执行当前语句，参与该文件整体逻辑。
- 190: `reason VARCHAR(500) COMMENT '推荐原因',` -> 执行当前语句，参与该文件整体逻辑。
- 191: `is_clicked BOOLEAN DEFAULT FALSE COMMENT '是否被点击',` -> 执行当前语句，参与该文件整体逻辑。
- 192: `is_booked BOOLEAN DEFAULT FALSE COMMENT '是否被预订',` -> 执行当前语句，参与该文件整体逻辑。
- 193: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 194: `expires_at TIMESTAMP NULL COMMENT '过期时间',` -> 执行当前语句，参与该文件整体逻辑。
- 195: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 196: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 197: `INDEX idx_type (type),` -> 执行当前语句，参与该文件整体逻辑。
- 198: `INDEX idx_target_id (target_id),` -> 执行当前语句，参与该文件整体逻辑。
- 199: `INDEX idx_score (score),` -> 执行当前语句，参与该文件整体逻辑。
- 200: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 201: `INDEX idx_expires_at (expires_at)` -> 执行当前语句，参与该文件整体逻辑。
- 202: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 203: `-- 搜索日志表` -> 执行当前语句，参与该文件整体逻辑。
- 204: `CREATE TABLE search_logs (` -> 执行当前语句，参与该文件整体逻辑。
- 205: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 206: `user_id BIGINT,` -> 执行当前语句，参与该文件整体逻辑。
- 207: `keywords VARCHAR(500) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 208: `filters JSON COMMENT '搜索条件',` -> 执行当前语句，参与该文件整体逻辑。
- 209: `result_count INT DEFAULT 0,` -> 执行当前语句，参与该文件整体逻辑。
- 210: `execution_time INT COMMENT '执行时间（毫秒）',` -> 执行当前语句，参与该文件整体逻辑。
- 211: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 212: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 213: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 214: `INDEX idx_keywords (keywords),` -> 执行当前语句，参与该文件整体逻辑。
- 215: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 216: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 217: `-- 系统配置表` -> 执行当前语句，参与该文件整体逻辑。
- 218: `CREATE TABLE system_configs (` -> 执行当前语句，参与该文件整体逻辑。
- 219: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 220: `config_key VARCHAR(100) UNIQUE NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 221: `config_value TEXT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 222: `description VARCHAR(500),` -> 执行当前语句，参与该文件整体逻辑。
- 223: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 224: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 225: `INDEX idx_config_key (config_key)` -> 执行当前语句，参与该文件整体逻辑。
- 226: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 227: `-- 知识库表` -> 执行当前语句，参与该文件整体逻辑。
- 228: `CREATE TABLE knowledge_base (` -> 执行当前语句，参与该文件整体逻辑。
- 229: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 230: `title VARCHAR(200) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 231: `content TEXT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 232: `category VARCHAR(50) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 233: `tags JSON,` -> 执行当前语句，参与该文件整体逻辑。
- 234: `source VARCHAR(100) COMMENT '来源',` -> 执行当前语句，参与该文件整体逻辑。
- 235: `author VARCHAR(100) COMMENT '作者',` -> 执行当前语句，参与该文件整体逻辑。
- 236: `view_count INT DEFAULT 0,` -> 执行当前语句，参与该文件整体逻辑。
- 237: `is_active BOOLEAN DEFAULT TRUE,` -> 执行当前语句，参与该文件整体逻辑。
- 238: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 239: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 240: `INDEX idx_category (category),` -> 执行当前语句，参与该文件整体逻辑。
- 241: `INDEX idx_is_active (is_active),` -> 执行当前语句，参与该文件整体逻辑。
- 242: `INDEX idx_created_at (created_at),` -> 执行当前语句，参与该文件整体逻辑。
- 243: `INDEX idx_tags ((tags))` -> 执行当前语句，参与该文件整体逻辑。
- 244: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 245: `-- 通知记录表` -> 执行当前语句，参与该文件整体逻辑。
- 246: `CREATE TABLE notifications (` -> 执行当前语句，参与该文件整体逻辑。
- 247: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 248: `user_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 249: `type ENUM('system', 'booking', 'payment', 'promotion', 'service') NOT NULL,` -> 定义类型或类结构，约束数据与行为。
- 250: `title VARCHAR(200) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 251: `content TEXT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 252: `channel ENUM('app', 'sms', 'email', 'wechat') DEFAULT 'app',` -> 执行当前语句，参与该文件整体逻辑。
- 253: `status ENUM('pending', 'sent', 'failed') DEFAULT 'pending',` -> 执行当前语句，参与该文件整体逻辑。
- 254: `send_time TIMESTAMP NULL COMMENT '发送时间',` -> 执行当前语句，参与该文件整体逻辑。
- 255: `read_time TIMESTAMP NULL COMMENT '阅读时间',` -> 执行当前语句，参与该文件整体逻辑。
- 256: `metadata JSON COMMENT '额外信息',` -> 执行当前语句，参与该文件整体逻辑。
- 257: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 258: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 259: `INDEX idx_user_id (user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 260: `INDEX idx_type (type),` -> 执行当前语句，参与该文件整体逻辑。
- 261: `INDEX idx_status (status),` -> 执行当前语句，参与该文件整体逻辑。
- 262: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 263: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 264: `-- 支付记录表` -> 执行当前语句，参与该文件整体逻辑。
- 265: `CREATE TABLE payment_records (` -> 执行当前语句，参与该文件整体逻辑。
- 266: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 267: `order_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 268: `payment_method ENUM('alipay', 'wechat', 'credit_card', 'bank_transfer') NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 269: `amount DECIMAL(12,2) NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 270: `status ENUM('pending', 'success', 'failed', 'refunded') DEFAULT 'pending',` -> 执行当前语句，参与该文件整体逻辑。
- 271: `transaction_id VARCHAR(100) COMMENT '交易流水号',` -> 执行当前语句，参与该文件整体逻辑。
- 272: `callback_data JSON COMMENT '回调数据',` -> 执行当前语句，参与该文件整体逻辑。
- 273: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 274: `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 275: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,` -> 执行当前语句，参与该文件整体逻辑。
- 276: `INDEX idx_order_id (order_id),` -> 执行当前语句，参与该文件整体逻辑。
- 277: `INDEX idx_payment_method (payment_method),` -> 执行当前语句，参与该文件整体逻辑。
- 278: `INDEX idx_status (status),` -> 执行当前语句，参与该文件整体逻辑。
- 279: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 280: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 281: `-- 创建一些索引优化查询性能` -> 执行当前语句，参与该文件整体逻辑。
- 282: `CREATE INDEX idx_products_search ON products(` -> 执行当前语句，参与该文件整体逻辑。
- 283: `type,` -> 执行当前语句，参与该文件整体逻辑。
- 284: `status,` -> 执行当前语句，参与该文件整体逻辑。
- 285: `(location->>'city'),` -> 执行当前语句，参与该文件整体逻辑。
- 286: `price,` -> 执行当前语句，参与该文件整体逻辑。
- 287: `rating,` -> 执行当前语句，参与该文件整体逻辑。
- 288: `review_count` -> 执行当前语句，参与该文件整体逻辑。
- 289: `);` -> 结束当前语句或代码块。
- 290: `CREATE INDEX idx_orders_user_status ON orders(` -> 执行当前语句，参与该文件整体逻辑。
- 291: `user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 292: `status,` -> 执行当前语句，参与该文件整体逻辑。
- 293: `created_at` -> 执行当前语句，参与该文件整体逻辑。
- 294: `);` -> 结束当前语句或代码块。
- 295: `CREATE INDEX idx_itineraries_user_dates ON itineraries(` -> 执行当前语句，参与该文件整体逻辑。
- 296: `user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 297: `start_date,` -> 执行当前语句，参与该文件整体逻辑。
- 298: `end_date` -> 执行当前语句，参与该文件整体逻辑。
- 299: `);` -> 结束当前语句或代码块。
- 300: `-- 创建全文搜索索引（如果使用MySQL 5.7+）` -> 执行当前语句，参与该文件整体逻辑。
- 301: `-- ALTER TABLE products ADD FULLTEXT INDEX ft_search(name, description, tags);` -> 执行当前语句，参与该文件整体逻辑。
- 302: `-- 创建向量搜索表（如果使用Milvus等向量数据库）` -> 执行当前语句，参与该文件整体逻辑。
- 303: `/*` -> 注释行，用于解释设计意图或使用说明。
- 304: `CREATE TABLE vector_embeddings (` -> 执行当前语句，参与该文件整体逻辑。
- 305: `id BIGINT PRIMARY KEY AUTO_INCREMENT,` -> 执行当前语句，参与该文件整体逻辑。
- 306: `entity_type ENUM('product', 'knowledge', 'user') NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 307: `entity_id BIGINT NOT NULL,` -> 执行当前语句，参与该文件整体逻辑。
- 308: `vector_data JSON NOT NULL COMMENT '向量数据',` -> 执行当前语句，参与该文件整体逻辑。
- 309: `metadata JSON COMMENT '元数据',` -> 执行当前语句，参与该文件整体逻辑。
- 310: `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,` -> 执行当前语句，参与该文件整体逻辑。
- 311: `INDEX idx_entity_type (entity_type),` -> 执行当前语句，参与该文件整体逻辑。
- 312: `INDEX idx_entity_id (entity_id),` -> 执行当前语句，参与该文件整体逻辑。
- 313: `INDEX idx_created_at (created_at)` -> 执行当前语句，参与该文件整体逻辑。
- 314: `) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` -> 执行当前语句，参与该文件整体逻辑。
- 315: `*/` -> 注释行，用于解释设计意图或使用说明。
