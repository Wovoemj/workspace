# `frontend/user-web/src/app/assistant/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* AI 旅行助手页模块 - 小游对话系统` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 6: `*   - AI 驱动的旅行规划对话界面` -> 注释行，用于解释设计意图或使用说明。
- 7: `*   - 支持行程规划、景点推荐、问答咨询` -> 注释行，用于解释设计意图或使用说明。
- 8: `*   - 语音输入、附件上传（模拟）` -> 注释行，用于解释设计意图或使用说明。
- 9: `*   - 历史对话本地持久化存储` -> 注释行，用于解释设计意图或使用说明。
- 10: `*   - 流式响应显示` -> 注释行，用于解释设计意图或使用说明。
- 11: `*` -> 注释行，用于解释设计意图或使用说明。
- 12: `* 依赖项：` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - components/MarkdownRenderer：Markdown 渲染` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - lucide-react：图标库` -> 注释行，用于解释设计意图或使用说明。
- 15: `*   - react-hot-toast：消息提示` -> 注释行，用于解释设计意图或使用说明。
- 16: `*   - store/index：Zustand 用户状态` -> 注释行，用于解释设计意图或使用说明。
- 17: `*` -> 注释行，用于解释设计意图或使用说明。
- 18: `* 数据来源：` -> 注释行，用于解释设计意图或使用说明。
- 19: `*   - POST /api/agent/chat：AI 对话接口（流式响应）` -> 注释行，用于解释设计意图或使用说明。
- 20: `*   - GET /api/recommendations：目的地推荐` -> 注释行，用于解释设计意图或使用说明。
- 21: `*   - GET /api/health：后端健康检查` -> 注释行，用于解释设计意图或使用说明。
- 22: `*   - POST /api/conversations/save：保存对话` -> 注释行，用于解释设计意图或使用说明。
- 23: `*   - POST /api/travel-plans：保存行程` -> 注释行，用于解释设计意图或使用说明。
- 24: `*` -> 注释行，用于解释设计意图或使用说明。
- 25: `* 核心功能：` -> 注释行，用于解释设计意图或使用说明。
- 26: `*   - parsePlannerInput：从文本提取目的地和天数` -> 注释行，用于解释设计意图或使用说明。
- 27: `*   - parseTripContent：解析行程内容为结构化数据` -> 注释行，用于解释设计意图或使用说明。
- 28: `*   - TripCard：行程卡片展示组件` -> 注释行，用于解释设计意图或使用说明。
- 29: `*   - 语音识别：Web Speech API` -> 注释行，用于解释设计意图或使用说明。
- 30: `*/` -> 注释行，用于解释设计意图或使用说明。
- 31: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `import { useEffect, useMemo, useRef, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 34: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 35: `import { useRouter, useSearchParams } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `import { Loader2, Send, Sparkles, Mic, Paperclip, MapPin, Calendar, Plane, Lightbulb, Square } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 38: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 39: `import { onImgErrorUseFallback, resolveCoverSrc } from '@/lib/media'` -> 导入依赖模块，供当前文件使用。
- 40: `import MarkdownRenderer from '@/components/MarkdownRenderer'` -> 导入依赖模块，供当前文件使用。
- 41: `(空行)` -> 空行，用于提升代码结构可读性。
- 42: `type ChatMsg = {` -> 定义类型或类结构，约束数据与行为。
- 43: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 44: `role: 'user' | 'assistant'` -> 执行当前语句，参与该文件整体逻辑。
- 45: `content: string` -> 执行当前语句，参与该文件整体逻辑。
- 46: `createdAt: number` -> 执行当前语句，参与该文件整体逻辑。
- 47: `planTrip?: any` -> 执行当前语句，参与该文件整体逻辑。
- 48: `planItems?: any[]` -> 执行当前语句，参与该文件整体逻辑。
- 49: `}` -> 结束当前语句或代码块。
- 50: `(空行)` -> 空行，用于提升代码结构可读性。
- 51: `type TripActivity = {` -> 定义类型或类结构，约束数据与行为。
- 52: `place?: string` -> 执行当前语句，参与该文件整体逻辑。
- 53: `activity?: string` -> 执行当前语句，参与该文件整体逻辑。
- 54: `duration?: string` -> 执行当前语句，参与该文件整体逻辑。
- 55: `cost?: string` -> 执行当前语句，参与该文件整体逻辑。
- 56: `transport?: string` -> 执行当前语句，参与该文件整体逻辑。
- 57: `}` -> 结束当前语句或代码块。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `type TripDay = {` -> 定义类型或类结构，约束数据与行为。
- 60: `day: number` -> 执行当前语句，参与该文件整体逻辑。
- 61: `morning?: TripActivity` -> 执行当前语句，参与该文件整体逻辑。
- 62: `afternoon?: TripActivity` -> 执行当前语句，参与该文件整体逻辑。
- 63: `evening?: TripActivity` -> 执行当前语句，参与该文件整体逻辑。
- 64: `hotel?: string` -> 执行当前语句，参与该文件整体逻辑。
- 65: `tips?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 66: `}` -> 结束当前语句或代码块。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `function parsePlannerInput(text: string) {` -> 定义函数或方法，实现具体业务逻辑。
- 69: `const normalized = text.trim()` -> 声明变量或常量，保存运行时数据。
- 70: `let destination = ''` -> 声明变量或常量，保存运行时数据。
- 71: `let days = 3` -> 声明变量或常量，保存运行时数据。
- 72: `const cityLike = normalized.match(/去\s*([\u4e00-\u9fa5A-Za-z]{2,12})/) ||` -> 声明变量或常量，保存运行时数据。
- 73: `normalized.match(/([\u4e00-\u9fa5A-Za-z]{2,12})\s*(?:旅行|旅游|行程|攻略)/)` -> 执行当前语句，参与该文件整体逻辑。
- 74: `if (cityLike?.[1]) destination = cityLike[1].trim()` -> 条件判断分支，根据场景执行不同逻辑。
- 75: `const dayLike = normalized.match(/(\d{1,2})\s*(?:天|日)/)` -> 声明变量或常量，保存运行时数据。
- 76: `if (dayLike?.[1]) {` -> 条件判断分支，根据场景执行不同逻辑。
- 77: `const n = parseInt(dayLike[1], 10)` -> 声明变量或常量，保存运行时数据。
- 78: `if (!Number.isNaN(n)) days = Math.max(1, Math.min(14, n))` -> 条件判断分支，根据场景执行不同逻辑。
- 79: `}` -> 结束当前语句或代码块。
- 80: `return { destination, days }` -> 返回结果或提前结束当前流程。
- 81: `}` -> 结束当前语句或代码块。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `function parseActivity(text: string): TripActivity {` -> 定义函数或方法，实现具体业务逻辑。
- 84: `const raw = text.trim()` -> 声明变量或常量，保存运行时数据。
- 85: `if (!raw) return {}` -> 条件判断分支，根据场景执行不同逻辑。
- 86: `const place = raw.split(/[，,。；;：:\-]/)[0]?.trim() || raw` -> 声明变量或常量，保存运行时数据。
- 87: `const durationMatch = raw.match(/(\d+\s*(?:小时|分钟|h|min))/i)` -> 声明变量或常量，保存运行时数据。
- 88: `const costMatch = raw.match(/([¥￥$]?\s*\d+\s*(?:元|RMB|USD)?)/i)` -> 声明变量或常量，保存运行时数据。
- 89: `return {` -> 返回结果或提前结束当前流程。
- 90: `place,` -> 执行当前语句，参与该文件整体逻辑。
- 91: `activity: raw,` -> 执行当前语句，参与该文件整体逻辑。
- 92: `duration: durationMatch?.[1],` -> 执行当前语句，参与该文件整体逻辑。
- 93: `cost: costMatch?.[1],` -> 执行当前语句，参与该文件整体逻辑。
- 94: `}` -> 结束当前语句或代码块。
- 95: `}` -> 结束当前语句或代码块。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `function parseTripContent(content: string): TripDay[] {` -> 定义函数或方法，实现具体业务逻辑。
- 98: `const text = content.replace(/\r\n/g, '\n')` -> 声明变量或常量，保存运行时数据。
- 99: `const dayStart = /(?:^|\n)(?:第\s*([一二三四五六七八九十\d]+)\s*天|Day\s*(\d+))/gi` -> 声明变量或常量，保存运行时数据。
- 100: `const matches = [...text.matchAll(dayStart)]` -> 声明变量或常量，保存运行时数据。
- 101: `if (matches.length === 0) return []` -> 条件判断分支，根据场景执行不同逻辑。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `const toNum = (s?: string) => {` -> 声明变量或常量，保存运行时数据。
- 104: `if (!s) return 0` -> 条件判断分支，根据场景执行不同逻辑。
- 105: `const m = s.match(/\d+/)` -> 声明变量或常量，保存运行时数据。
- 106: `if (m) return parseInt(m[0], 10)` -> 条件判断分支，根据场景执行不同逻辑。
- 107: `const map: Record<string, number> = { 一: 1, 二: 2, 三: 3, 四: 4, 五: 5, 六: 6, 七: 7, 八: 8, 九: 9, 十: 10 }` -> 声明变量或常量，保存运行时数据。
- 108: `return map[s] || 0` -> 返回结果或提前结束当前流程。
- 109: `}` -> 结束当前语句或代码块。
- 110: `(空行)` -> 空行，用于提升代码结构可读性。
- 111: `const days: TripDay[] = []` -> 声明变量或常量，保存运行时数据。
- 112: `for (let i = 0; i < matches.length; i++) {` -> 循环处理集合或重复执行逻辑。
- 113: `const start = matches[i].index ?? 0` -> 声明变量或常量，保存运行时数据。
- 114: `const end = i + 1 < matches.length ? (matches[i + 1].index ?? text.length) : text.length` -> 声明变量或常量，保存运行时数据。
- 115: `const section = text.slice(start, end)` -> 声明变量或常量，保存运行时数据。
- 116: `const dayNum = toNum(matches[i][1]) || toNum(matches[i][2]) || i + 1` -> 声明变量或常量，保存运行时数据。
- 117: `const morning = section.match(/(?:上午|早上|Morning)[:：]?\s*([^\n]+)/i)` -> 声明变量或常量，保存运行时数据。
- 118: `const afternoon = section.match(/(?:下午|午后|Afternoon)[:：]?\s*([^\n]+)/i)` -> 声明变量或常量，保存运行时数据。
- 119: `const evening = section.match(/(?:晚上|傍晚|Evening|Night)[:：]?\s*([^\n]+)/i)` -> 声明变量或常量，保存运行时数据。
- 120: `const hotel = section.match(/(?:住宿|酒店|民宿|Hotel|Accommodation)[:：]?\s*([^\n]+)/i)` -> 声明变量或常量，保存运行时数据。
- 121: `const tips = section.split('\n').map((l) => l.trim()).filter((l) => /^[-•*]/.test(l) || /(?:提示|注意|tips)/i.test(l)).map((l) => l.replace(/^[-•*]\s*/, '')).slice(0, 5)` -> 声明变量或常量，保存运行时数据。
- 122: `const day: TripDay = {` -> 声明变量或常量，保存运行时数据。
- 123: `day: dayNum,` -> 执行当前语句，参与该文件整体逻辑。
- 124: `morning: morning?.[1] ? parseActivity(morning[1]) : undefined,` -> 执行当前语句，参与该文件整体逻辑。
- 125: `afternoon: afternoon?.[1] ? parseActivity(afternoon[1]) : undefined,` -> 执行当前语句，参与该文件整体逻辑。
- 126: `evening: evening?.[1] ? parseActivity(evening[1]) : undefined,` -> 执行当前语句，参与该文件整体逻辑。
- 127: `hotel: hotel?.[1]?.trim(),` -> 执行当前语句，参与该文件整体逻辑。
- 128: `tips: tips.length ? tips : undefined,` -> 执行当前语句，参与该文件整体逻辑。
- 129: `}` -> 结束当前语句或代码块。
- 130: `if (day.morning || day.afternoon || day.evening) days.push(day)` -> 条件判断分支，根据场景执行不同逻辑。
- 131: `}` -> 结束当前语句或代码块。
- 132: `return days` -> 返回结果或提前结束当前流程。
- 133: `}` -> 结束当前语句或代码块。
- 134: `(空行)` -> 空行，用于提升代码结构可读性。
- 135: `function isTripContent(content: string) {` -> 定义函数或方法，实现具体业务逻辑。
- 136: `return /(?:第\s*[一二三四五六七八九十\d]+\s*天|Day\s*\d+)/i.test(content) &&` -> 返回结果或提前结束当前流程。
- 137: `/(?:上午|下午|晚上|Morning|Afternoon|Evening|行程|景点|住宿|酒店)/i.test(content)` -> 执行当前语句，参与该文件整体逻辑。
- 138: `}` -> 结束当前语句或代码块。
- 139: `(空行)` -> 空行，用于提升代码结构可读性。
- 140: `function ActivityDetails({ activity, timeSlot }: { activity: TripActivity; timeSlot: string }) {` -> 定义函数或方法，实现具体业务逻辑。
- 141: `const [showDetails, setShowDetails] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 142: `const icon = timeSlot === 'morning' ? '🌅' : timeSlot === 'afternoon' ? '🌇' : '🌙'` -> 声明变量或常量，保存运行时数据。
- 143: `(空行)` -> 空行，用于提升代码结构可读性。
- 144: `return (` -> 返回结果或提前结束当前流程。
- 145: `<div className="mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 146: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 147: `onClick={() => setShowDetails((v) => !v)}` -> 执行当前语句，参与该文件整体逻辑。
- 148: `className="w-full text-left flex items-center justify-between p-3 rounded-xl bg-white/50 border border-white/30"` -> 执行当前语句，参与该文件整体逻辑。
- 149: `>` -> 执行当前语句，参与该文件整体逻辑。
- 150: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 151: `<div className="h-8 w-8 rounded-full bg-white/60 flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 152: `<span className="font-bold">{icon}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 153: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 154: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 155: `<div className="font-semibold text-gray-800">{activity.place || '自由活动'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 156: `{activity.activity && activity.activity !== activity.place ? (` -> 执行当前语句，参与该文件整体逻辑。
- 157: `<div className="text-xs text-gray-500 truncate">{activity.activity}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 158: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 159: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 161: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 162: `{activity.duration ? (` -> 执行当前语句，参与该文件整体逻辑。
- 163: `<span className="text-xs px-2 py-1 bg-white/50 text-gray-600 rounded-full font-medium">⏱ {activity.duration}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 164: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 165: `<span className="text-gray-400 text-sm">{showDetails ? '▲' : '▼'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 166: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 168: `(空行)` -> 空行，用于提升代码结构可读性。
- 169: `{showDetails ? (` -> 执行当前语句，参与该文件整体逻辑。
- 170: `<div className="mt-3 pl-11 pr-4 pb-3 bg-white/60 border border-white/30 rounded-xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 171: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `{activity.activity && activity.activity !== activity.place ? (` -> 执行当前语句，参与该文件整体逻辑。
- 173: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `<div className="text-xs font-medium text-gray-500 mb-1">活动内容</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 175: `<div className="text-sm text-gray-700">{activity.activity}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 178: `{activity.cost ? (` -> 执行当前语句，参与该文件整体逻辑。
- 179: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `<div className="text-xs font-medium text-gray-500 mb-1">费用</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `<div className="text-sm font-medium text-green-600">{activity.cost}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 184: `{activity.transport ? (` -> 执行当前语句，参与该文件整体逻辑。
- 185: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 186: `<div className="text-xs font-medium text-gray-500 mb-1">交通方式</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 187: `<div className="text-sm text-gray-700">{activity.transport}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 188: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 189: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 190: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 191: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 192: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 193: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 194: `)` -> 结束当前语句或代码块。
- 195: `}` -> 结束当前语句或代码块。
- 196: `(空行)` -> 空行，用于提升代码结构可读性。
- 197: `function processMessageContent(content: string, isUserMessage: boolean = false): React.ReactNode {` -> 定义函数或方法，实现具体业务逻辑。
- 198: `const imageRegex = /@image:([^\s]+)/g` -> 声明变量或常量，保存运行时数据。
- 199: `const parts: React.ReactNode[] = []` -> 声明变量或常量，保存运行时数据。
- 200: `let lastIndex = 0` -> 声明变量或常量，保存运行时数据。
- 201: `let match: RegExpExecArray | null` -> 声明变量或常量，保存运行时数据。
- 202: `(空行)` -> 空行，用于提升代码结构可读性。
- 203: `while ((match = imageRegex.exec(content)) !== null) {` -> 循环处理集合或重复执行逻辑。
- 204: `if (match.index > lastIndex) {` -> 条件判断分支，根据场景执行不同逻辑。
- 205: `parts.push(` -> 执行当前语句，参与该文件整体逻辑。
- 206: `<MarkdownRenderer key={\`text-${lastIndex}\`} content={content.slice(lastIndex, match.index)} darkMode={isUserMessage} />` -> JSX/HTML 结构行，用于描述页面元素。
- 207: `)` -> 结束当前语句或代码块。
- 208: `}` -> 结束当前语句或代码块。
- 209: `const src = resolveCoverSrc(match[1])` -> 声明变量或常量，保存运行时数据。
- 210: `parts.push(` -> 执行当前语句，参与该文件整体逻辑。
- 211: `<img key={\`img-${match.index}\`} src={src} alt="行程图片" loading="lazy" onError={onImgErrorUseFallback}` -> 执行当前语句，参与该文件整体逻辑。
- 212: `className="mt-2 max-h-64 rounded-lg border border-gray-200 object-cover" />,` -> 执行当前语句，参与该文件整体逻辑。
- 213: `)` -> 结束当前语句或代码块。
- 214: `lastIndex = match.index + match[0].length` -> 执行当前语句，参与该文件整体逻辑。
- 215: `}` -> 结束当前语句或代码块。
- 216: `(空行)` -> 空行，用于提升代码结构可读性。
- 217: `if (lastIndex < content.length) {` -> 条件判断分支，根据场景执行不同逻辑。
- 218: `parts.push(` -> 执行当前语句，参与该文件整体逻辑。
- 219: `<MarkdownRenderer key={\`text-tail-${lastIndex}\`} content={content.slice(lastIndex)} darkMode={isUserMessage} />` -> JSX/HTML 结构行，用于描述页面元素。
- 220: `)` -> 结束当前语句或代码块。
- 221: `}` -> 结束当前语句或代码块。
- 222: `(空行)` -> 空行，用于提升代码结构可读性。
- 223: `return <>{parts.length ? parts : <MarkdownRenderer content={content} darkMode={isUserMessage} />}</>` -> 返回结果或提前结束当前流程。
- 224: `}` -> 结束当前语句或代码块。
- 225: `(空行)` -> 空行，用于提升代码结构可读性。
- 226: `function TripDayCard({ day }: { day: TripDay }) {` -> 定义函数或方法，实现具体业务逻辑。
- 227: `const [isExpanded, setIsExpanded] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 228: `(空行)` -> 空行，用于提升代码结构可读性。
- 229: `return (` -> 返回结果或提前结束当前流程。
- 230: `<div className="rounded-2xl border border-white/40 bg-white/70">` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 232: `className="px-5 py-3 bg-gradient-to-r from-blue-500 to-cyan-500 rounded-t-2xl cursor-pointer"` -> 执行当前语句，参与该文件整体逻辑。
- 233: `onClick={() => setIsExpanded((v) => !v)}` -> 执行当前语句，参与该文件整体逻辑。
- 234: `>` -> 执行当前语句，参与该文件整体逻辑。
- 235: `<div className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `<div className="text-white font-semibold">第 {day.day} 天</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `<div className="text-white/90">{isExpanded ? '▲' : '▼'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 238: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 239: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 240: `(空行)` -> 空行，用于提升代码结构可读性。
- 241: `{isExpanded ? (` -> 执行当前语句，参与该文件整体逻辑。
- 242: `<div className="px-5 py-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 243: `{day.morning ? <ActivityDetails activity={day.morning} timeSlot="morning" /> : null}` -> 执行当前语句，参与该文件整体逻辑。
- 244: `{day.afternoon ? <ActivityDetails activity={day.afternoon} timeSlot="afternoon" /> : null}` -> 执行当前语句，参与该文件整体逻辑。
- 245: `{day.evening ? <ActivityDetails activity={day.evening} timeSlot="evening" /> : null}` -> 执行当前语句，参与该文件整体逻辑。
- 246: `{day.hotel ? (` -> 执行当前语句，参与该文件整体逻辑。
- 247: `<div className="mt-2 text-sm text-gray-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 248: `<span className="font-medium">住宿：</span><span>{day.hotel}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 249: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 250: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 251: `{day.tips?.length ? (` -> 执行当前语句，参与该文件整体逻辑。
- 252: `<div className="mt-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 253: `<div className="text-sm font-medium text-gray-700">小贴士</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 254: `<ul className="list-disc list-inside text-sm text-gray-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 255: `{day.tips.map((tip, idx) => (<li key={idx}>{tip}</li>))}` -> 执行当前语句，参与该文件整体逻辑。
- 256: `</ul>` -> JSX/HTML 结构行，用于描述页面元素。
- 257: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 258: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 259: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 260: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 261: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 262: `)` -> 结束当前语句或代码块。
- 263: `}` -> 结束当前语句或代码块。
- 264: `(空行)` -> 空行，用于提升代码结构可读性。
- 265: `function TripOverview({ days, destination }: { days: TripDay[]; destination?: string }) {` -> 定义函数或方法，实现具体业务逻辑。
- 266: `const totalActivities = days.reduce((acc, day) => acc + (day.morning ? 1 : 0) + (day.afternoon ? 1 : 0) + (day.evening ? 1 : 0), 0)` -> 声明变量或常量，保存运行时数据。
- 267: `return (` -> 返回结果或提前结束当前流程。
- 268: `<div className="bg-white/50 rounded-xl p-4 mb-4 border border-white/40">` -> JSX/HTML 结构行，用于描述页面元素。
- 269: `<div className="font-semibold text-gray-800">{destination || '行程概览'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 270: `<div className="text-xs text-gray-600 mt-1">{days.length} 天 · {totalActivities} 个活动</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 271: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 272: `)` -> 结束当前语句或代码块。
- 273: `}` -> 结束当前语句或代码块。
- 274: `(空行)` -> 空行，用于提升代码结构可读性。
- 275: `function TripCard({ content }: { content: string }) {` -> 定义函数或方法，实现具体业务逻辑。
- 276: `const days = parseTripContent(content)` -> 声明变量或常量，保存运行时数据。
- 277: `if (days.length === 0) {` -> 条件判断分支，根据场景执行不同逻辑。
- 278: `return <div className="mt-3 text-sm text-gray-700">{processMessageContent(content)}</div>` -> 返回结果或提前结束当前流程。
- 279: `}` -> 结束当前语句或代码块。
- 280: `const destination = parsePlannerInput(content).destination` -> 声明变量或常量，保存运行时数据。
- 281: `return (` -> 返回结果或提前结束当前流程。
- 282: `<div className="mt-4 space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 283: `<TripOverview days={days} destination={destination} />` -> JSX/HTML 结构行，用于描述页面元素。
- 284: `{days.map((day, idx) => (<TripDayCard key={idx} day={day} />))}` -> 执行当前语句，参与该文件整体逻辑。
- 285: `{content.includes('@image:') ? <div>{processMessageContent(content)}</div> : null}` -> 执行当前语句，参与该文件整体逻辑。
- 286: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 287: `)` -> 结束当前语句或代码块。
- 288: `}` -> 结束当前语句或代码块。
- 289: `(空行)` -> 空行，用于提升代码结构可读性。
- 290: `function uid() {` -> 定义函数或方法，实现具体业务逻辑。
- 291: `return \`${Date.now()}-${Math.random().toString(16).slice(2)}\`` -> 返回结果或提前结束当前流程。
- 292: `}` -> 结束当前语句或代码块。
- 293: `(空行)` -> 空行，用于提升代码结构可读性。
- 294: `type DestinationPreview = {` -> 定义类型或类结构，约束数据与行为。
- 295: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 296: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 297: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 298: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 299: `cover_image?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 300: `rating?: number | null` -> 执行当前语句，参与该文件整体逻辑。
- 301: `ticket_price?: number | null` -> 执行当前语句，参与该文件整体逻辑。
- 302: `}` -> 结束当前语句或代码块。
- 303: `(空行)` -> 空行，用于提升代码结构可读性。
- 304: `export default function AssistantPage() {` -> 导出当前声明，供其他模块复用。
- 305: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 306: `const searchParams = useSearchParams()` -> 声明变量或常量，保存运行时数据。
- 307: `(空行)` -> 空行，用于提升代码结构可读性。
- 308: `// 处理 URL 参数中的目的地` -> 注释行，用于解释设计意图或使用说明。
- 309: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 310: `const destination = searchParams.get('destination')` -> 声明变量或常量，保存运行时数据。
- 311: `if (destination) {` -> 条件判断分支，根据场景执行不同逻辑。
- 312: `setInput(\`我想去${destination}旅行，帮我规划一份3天的行程\`)` -> 执行当前语句，参与该文件整体逻辑。
- 313: `}` -> 结束当前语句或代码块。
- 314: `}, [searchParams])` -> 执行当前语句，参与该文件整体逻辑。
- 315: `(空行)` -> 空行，用于提升代码结构可读性。
- 316: `const [input, setInput] = useState('')` -> 声明变量或常量，保存运行时数据。
- 317: `const [sending, setSending] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 318: `const [abortController, setAbortController] = useState<AbortController | null>(null)` -> 声明变量或常量，保存运行时数据。
- 319: `const [backendOk, setBackendOk] = useState<boolean | null>(null)` -> 声明变量或常量，保存运行时数据。
- 320: `// 默认使用智谱GLM-4.5 Air模型（隐藏选择器）` -> 注释行，用于解释设计意图或使用说明。
- 321: `const [showHistory, setShowHistory] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 322: `const [sessions, setSessions] = useState<Array<{ id: string; title: string; createdAt: number; msgs: ChatMsg[] }>>([])` -> 声明变量或常量，保存运行时数据。
- 323: `const [sessionId, setSessionId] = useState<string>(() => uid())` -> 声明变量或常量，保存运行时数据。
- 324: `const [msgs, setMsgs] = useState<ChatMsg[]>([` -> 声明变量或常量，保存运行时数据。
- 325: `{` -> 执行当前语句，参与该文件整体逻辑。
- 326: `id: uid(),` -> 执行当前语句，参与该文件整体逻辑。
- 327: `role: 'assistant',` -> 执行当前语句，参与该文件整体逻辑。
- 328: `content: '您好！我是小游，您的专属旅行规划师。有什么旅行计划我可以帮您规划吗？',` -> 执行当前语句，参与该文件整体逻辑。
- 329: `createdAt: Date.now(),` -> 执行当前语句，参与该文件整体逻辑。
- 330: `},` -> 执行当前语句，参与该文件整体逻辑。
- 331: `])` -> 执行当前语句，参与该文件整体逻辑。
- 332: `(空行)` -> 空行，用于提升代码结构可读性。
- 333: `const bottomRef = useRef<HTMLDivElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 334: `const chatContainerRef = useRef<HTMLDivElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 335: `const [autoScrollEnabled, setAutoScrollEnabled] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 336: `const [destPreview, setDestPreview] = useState<DestinationPreview[]>([])` -> 声明变量或常量，保存运行时数据。
- 337: `const [destPreviewLoading, setDestPreviewLoading] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 338: `const [recognizing, setRecognizing] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 339: `const fileInputRef = useRef<HTMLInputElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 340: `const recognitionRef = useRef<any>(null)` -> 声明变量或常量，保存运行时数据。
- 341: `(空行)` -> 空行，用于提升代码结构可读性。
- 342: `const [streamMsgId, setStreamMsgId] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 343: `const [streamText, setStreamText] = useState('')` -> 声明变量或常量，保存运行时数据。
- 344: `const [agentTools, setAgentTools] = useState<Array<{ tool: string; label: string; status: 'thinking' | 'done' }>>([])` -> 声明变量或常量，保存运行时数据。
- 345: `(空行)` -> 空行，用于提升代码结构可读性。
- 346: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 347: `window.scrollTo({ top: 0, behavior: 'instant' as any })` -> 执行当前语句，参与该文件整体逻辑。
- 348: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 349: `(空行)` -> 空行，用于提升代码结构可读性。
- 350: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 351: `if (!autoScrollEnabled) return` -> 条件判断分支，根据场景执行不同逻辑。
- 352: `const container = chatContainerRef.current` -> 声明变量或常量，保存运行时数据。
- 353: `if (container) {` -> 条件判断分支，根据场景执行不同逻辑。
- 354: `container.scrollTo({ top: container.scrollHeight })` -> 执行当前语句，参与该文件整体逻辑。
- 355: `}` -> 结束当前语句或代码块。
- 356: `}, [autoScrollEnabled, msgs.length, streamText.length, sending])` -> 执行当前语句，参与该文件整体逻辑。
- 357: `(空行)` -> 空行，用于提升代码结构可读性。
- 358: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 359: `try {` -> 异常处理逻辑，提升程序健壮性。
- 360: `const raw = localStorage.getItem('assistant_sessions_v1')` -> 声明变量或常量，保存运行时数据。
- 361: `if (!raw) return` -> 条件判断分支，根据场景执行不同逻辑。
- 362: `const parsed = JSON.parse(raw)` -> 声明变量或常量，保存运行时数据。
- 363: `if (!Array.isArray(parsed)) return` -> 条件判断分支，根据场景执行不同逻辑。
- 364: `const next = parsed.filter((s) => s && Array.isArray(s.msgs)).slice(0, 10).map((s) => ({` -> 声明变量或常量，保存运行时数据。
- 365: `id: String(s.id || uid()),` -> 执行当前语句，参与该文件整体逻辑。
- 366: `title: String(s.title || '对话'),` -> 执行当前语句，参与该文件整体逻辑。
- 367: `createdAt: Number(s.createdAt || Date.now()),` -> 执行当前语句，参与该文件整体逻辑。
- 368: `msgs: s.msgs as ChatMsg[],` -> 执行当前语句，参与该文件整体逻辑。
- 369: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 370: `if (!next.length) return` -> 条件判断分支，根据场景执行不同逻辑。
- 371: `setSessions(next)` -> 执行当前语句，参与该文件整体逻辑。
- 372: `const latest = next[0]` -> 声明变量或常量，保存运行时数据。
- 373: `setSessionId(latest.id)` -> 执行当前语句，参与该文件整体逻辑。
- 374: `setMsgs(latest.msgs)` -> 执行当前语句，参与该文件整体逻辑。
- 375: `setStreamMsgId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 376: `setStreamText('')` -> 执行当前语句，参与该文件整体逻辑。
- 377: `} catch { /* ignore */ }` -> 执行当前语句，参与该文件整体逻辑。
- 378: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 379: `(空行)` -> 空行，用于提升代码结构可读性。
- 380: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 381: `if (!streamMsgId) return` -> 条件判断分支，根据场景执行不同逻辑。
- 382: `const full = msgs.find((m) => m.id === streamMsgId)?.content ?? ''` -> 声明变量或常量，保存运行时数据。
- 383: `if (!full) return` -> 条件判断分支，根据场景执行不同逻辑。
- 384: `if (streamText.length >= full.length) { setStreamMsgId(null); return }` -> 条件判断分支，根据场景执行不同逻辑。
- 385: `const step = full.length > 600 ? 12 : full.length > 200 ? 4 : 2` -> 声明变量或常量，保存运行时数据。
- 386: `const t = window.setTimeout(() => {` -> 声明变量或常量，保存运行时数据。
- 387: `setStreamText(full.slice(0, Math.min(full.length, streamText.length + step)))` -> 执行当前语句，参与该文件整体逻辑。
- 388: `}, 14)` -> 执行当前语句，参与该文件整体逻辑。
- 389: `return () => window.clearTimeout(t)` -> 返回结果或提前结束当前流程。
- 390: `}, [streamMsgId, streamText, msgs])` -> 执行当前语句，参与该文件整体逻辑。
- 391: `(空行)` -> 空行，用于提升代码结构可读性。
- 392: `function formatClock(ts: number) {` -> 定义函数或方法，实现具体业务逻辑。
- 393: `try { return new Date(ts).toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' }) }` -> 异常处理逻辑，提升程序健壮性。
- 394: `catch { return '' }` -> 异常处理逻辑，提升程序健壮性。
- 395: `}` -> 结束当前语句或代码块。
- 396: `(空行)` -> 空行，用于提升代码结构可读性。
- 397: `const coverSrc = (d: { cover_image?: string | null }) => resolveCoverSrc(d?.cover_image)` -> 声明变量或常量，保存运行时数据。
- 398: `(空行)` -> 空行，用于提升代码结构可读性。
- 399: `function buildSessionTitle(messages: ChatMsg[]) {` -> 定义函数或方法，实现具体业务逻辑。
- 400: `const firstUser = messages.find((m) => m.role === 'user')` -> 声明变量或常量，保存运行时数据。
- 401: `const t = firstUser?.content?.trim() || '新对话'` -> 声明变量或常量，保存运行时数据。
- 402: `return t.length > 20 ? t.slice(0, 20) + '…' : t` -> 返回结果或提前结束当前流程。
- 403: `}` -> 结束当前语句或代码块。
- 404: `(空行)` -> 空行，用于提升代码结构可读性。
- 405: `function persistSession(nextMsgs: ChatMsg[], title?: string) {` -> 定义函数或方法，实现具体业务逻辑。
- 406: `const now = Date.now()` -> 声明变量或常量，保存运行时数据。
- 407: `const id = sessionId || uid()` -> 声明变量或常量，保存运行时数据。
- 408: `const nextTitle = title || buildSessionTitle(nextMsgs)` -> 声明变量或常量，保存运行时数据。
- 409: `const nextSession = { id, title: nextTitle, createdAt: now, msgs: nextMsgs }` -> 声明变量或常量，保存运行时数据。
- 410: `setSessionId(id)` -> 执行当前语句，参与该文件整体逻辑。
- 411: `setSessions((prev) => {` -> 执行当前语句，参与该文件整体逻辑。
- 412: `const merged = [nextSession, ...prev.filter((s) => s.id !== id)].sort((a, b) => b.createdAt - a.createdAt).slice(0, 10)` -> 声明变量或常量，保存运行时数据。
- 413: `try { localStorage.setItem('assistant_sessions_v1', JSON.stringify(merged)) } catch { /* ignore */ }` -> 异常处理逻辑，提升程序健壮性。
- 414: `return merged` -> 返回结果或提前结束当前流程。
- 415: `})` -> 执行当前语句，参与该文件整体逻辑。
- 416: `}` -> 结束当前语句或代码块。
- 417: `(空行)` -> 空行，用于提升代码结构可读性。
- 418: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 419: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 420: `;(async () => {` -> 执行当前语句，参与该文件整体逻辑。
- 421: `try {` -> 异常处理逻辑，提升程序健壮性。
- 422: `const res = await fetch('/api/health', { cache: 'no-store' })` -> 声明变量或常量，保存运行时数据。
- 423: `if (!cancelled) setBackendOk(res.ok)` -> 条件判断分支，根据场景执行不同逻辑。
- 424: `} catch { if (!cancelled) setBackendOk(false) }` -> 执行当前语句，参与该文件整体逻辑。
- 425: `})()` -> 执行当前语句，参与该文件整体逻辑。
- 426: `return () => { cancelled = true }` -> 返回结果或提前结束当前流程。
- 427: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 428: `(空行)` -> 空行，用于提升代码结构可读性。
- 429: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 430: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 431: `;(async () => {` -> 执行当前语句，参与该文件整体逻辑。
- 432: `setDestPreviewLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 433: `try {` -> 异常处理逻辑，提升程序健壮性。
- 434: `const res = await fetch('/api/recommendations?limit=8', { cache: 'no-store' })` -> 声明变量或常量，保存运行时数据。
- 435: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 436: `if (!cancelled && Array.isArray(data?.recommendations)) setDestPreview(data.recommendations as DestinationPreview[])` -> 条件判断分支，根据场景执行不同逻辑。
- 437: `} catch { /* ignore */ }` -> 执行当前语句，参与该文件整体逻辑。
- 438: `finally { if (!cancelled) setDestPreviewLoading(false) }` -> 执行当前语句，参与该文件整体逻辑。
- 439: `})()` -> 执行当前语句，参与该文件整体逻辑。
- 440: `return () => { cancelled = true }` -> 返回结果或提前结束当前流程。
- 441: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 442: `(空行)` -> 空行，用于提升代码结构可读性。
- 443: `const canSend = useMemo(() => input.trim().length > 0 && !sending, [input, sending])` -> 声明变量或常量，保存运行时数据。
- 444: `(空行)` -> 空行，用于提升代码结构可读性。
- 445: `function onPickAttachment() { fileInputRef.current?.click() }` -> 定义函数或方法，实现具体业务逻辑。
- 446: `(空行)` -> 空行，用于提升代码结构可读性。
- 447: `function onAttachmentChange(e: any) {` -> 定义函数或方法，实现具体业务逻辑。
- 448: `const file = e?.target?.files?.[0]` -> 声明变量或常量，保存运行时数据。
- 449: `if (!file) return` -> 条件判断分支，根据场景执行不同逻辑。
- 450: `const next = input.trim() ? \`${input}\n[附件] ${file.name}\` : \`[附件] ${file.name}\`` -> 声明变量或常量，保存运行时数据。
- 451: `setInput(next)` -> 执行当前语句，参与该文件整体逻辑。
- 452: `toast.success('已添加附件到对话（模拟）')` -> 执行当前语句，参与该文件整体逻辑。
- 453: `if (fileInputRef.current) fileInputRef.current.value = ''` -> 条件判断分支，根据场景执行不同逻辑。
- 454: `}` -> 结束当前语句或代码块。
- 455: `(空行)` -> 空行，用于提升代码结构可读性。
- 456: `function onToggleVoice() {` -> 定义函数或方法，实现具体业务逻辑。
- 457: `if (recognizing) {` -> 条件判断分支，根据场景执行不同逻辑。
- 458: `try { recognitionRef.current?.stop?.() } catch { /* ignore */ }` -> 异常处理逻辑，提升程序健壮性。
- 459: `setRecognizing(false)` -> 执行当前语句，参与该文件整体逻辑。
- 460: `return` -> 返回结果或提前结束当前流程。
- 461: `}` -> 结束当前语句或代码块。
- 462: `const SpeechRecognition = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition` -> 声明变量或常量，保存运行时数据。
- 463: `if (!SpeechRecognition) { toast.error('当前浏览器不支持语音输入（建议使用 Chrome）'); return }` -> 条件判断分支，根据场景执行不同逻辑。
- 464: `const recog = new SpeechRecognition()` -> 声明变量或常量，保存运行时数据。
- 465: `recognitionRef.current = recog` -> 执行当前语句，参与该文件整体逻辑。
- 466: `recog.lang = 'zh-CN'` -> 执行当前语句，参与该文件整体逻辑。
- 467: `recog.interimResults = true` -> 执行当前语句，参与该文件整体逻辑。
- 468: `recog.maxAlternatives = 1` -> 执行当前语句，参与该文件整体逻辑。
- 469: `recog.onstart = () => setRecognizing(true)` -> 执行当前语句，参与该文件整体逻辑。
- 470: `recog.onend = () => setRecognizing(false)` -> 执行当前语句，参与该文件整体逻辑。
- 471: `recog.onerror = () => setRecognizing(false)` -> 执行当前语句，参与该文件整体逻辑。
- 472: `recog.onresult = (event: any) => {` -> 执行当前语句，参与该文件整体逻辑。
- 473: `let transcript = ''` -> 声明变量或常量，保存运行时数据。
- 474: `for (let i = event.resultIndex; i < event.results.length; i += 1) transcript += event.results[i][0]?.transcript || ''` -> 循环处理集合或重复执行逻辑。
- 475: `transcript = transcript.trim()` -> 执行当前语句，参与该文件整体逻辑。
- 476: `if (transcript) setInput(transcript)` -> 条件判断分支，根据场景执行不同逻辑。
- 477: `}` -> 结束当前语句或代码块。
- 478: `try { recog.start() } catch { toast.error('语音启动失败'); setRecognizing(false) }` -> 异常处理逻辑，提升程序健壮性。
- 479: `}` -> 结束当前语句或代码块。
- 480: `(空行)` -> 空行，用于提升代码结构可读性。
- 481: `// 保存对话到数据库` -> 注释行，用于解释设计意图或使用说明。
- 482: `async function saveConversationToDB(userMsg: ChatMsg, assistantMsg: ChatMsg) {` -> 定义函数或方法，实现具体业务逻辑。
- 483: `try {` -> 异常处理逻辑，提升程序健壮性。
- 484: `const backendBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 485: `(空行)` -> 空行，用于提升代码结构可读性。
- 486: `// 保存用户消息` -> 注释行，用于解释设计意图或使用说明。
- 487: `await fetch(\`${backendBase}/api/conversations/save\`, {` -> 执行当前语句，参与该文件整体逻辑。
- 488: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 489: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 490: `body: JSON.stringify({` -> 执行当前语句，参与该文件整体逻辑。
- 491: `user_id: null, // 未登录用户` -> 执行当前语句，参与该文件整体逻辑。
- 492: `session_id: sessionId,` -> 执行当前语句，参与该文件整体逻辑。
- 493: `role: 'user',` -> 执行当前语句，参与该文件整体逻辑。
- 494: `content: userMsg.content,` -> 执行当前语句，参与该文件整体逻辑。
- 495: `intent: detectIntent(userMsg.content)` -> 执行当前语句，参与该文件整体逻辑。
- 496: `})` -> 执行当前语句，参与该文件整体逻辑。
- 497: `})` -> 执行当前语句，参与该文件整体逻辑。
- 498: `(空行)` -> 空行，用于提升代码结构可读性。
- 499: `// 保存AI回复` -> 注释行，用于解释设计意图或使用说明。
- 500: `await fetch(\`${backendBase}/api/conversations/save\`, {` -> 执行当前语句，参与该文件整体逻辑。
- 501: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 502: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 503: `body: JSON.stringify({` -> 执行当前语句，参与该文件整体逻辑。
- 504: `user_id: null,` -> 执行当前语句，参与该文件整体逻辑。
- 505: `session_id: sessionId,` -> 执行当前语句，参与该文件整体逻辑。
- 506: `role: 'assistant',` -> 执行当前语句，参与该文件整体逻辑。
- 507: `content: assistantMsg.content,` -> 执行当前语句，参与该文件整体逻辑。
- 508: `intent: detectIntent(assistantMsg.content)` -> 执行当前语句，参与该文件整体逻辑。
- 509: `})` -> 执行当前语句，参与该文件整体逻辑。
- 510: `})` -> 执行当前语句，参与该文件整体逻辑。
- 511: `(空行)` -> 空行，用于提升代码结构可读性。
- 512: `// 如果是行程规划，尝试保存为行程` -> 注释行，用于解释设计意图或使用说明。
- 513: `if (isTripContent(assistantMsg.content)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 514: `await saveTravelPlan(assistantMsg)` -> 执行当前语句，参与该文件整体逻辑。
- 515: `}` -> 结束当前语句或代码块。
- 516: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 517: `console.error('保存对话到数据库失败:', e)` -> 执行当前语句，参与该文件整体逻辑。
- 518: `}` -> 结束当前语句或代码块。
- 519: `}` -> 结束当前语句或代码块。
- 520: `(空行)` -> 空行，用于提升代码结构可读性。
- 521: `// 检测意图` -> 注释行，用于解释设计意图或使用说明。
- 522: `function detectIntent(text: string): string {` -> 定义函数或方法，实现具体业务逻辑。
- 523: `const lower = text.toLowerCase()` -> 声明变量或常量，保存运行时数据。
- 524: `if (lower.includes('行程') || lower.includes('规划') || lower.includes('攻略')) return '行程规划'` -> 条件判断分支，根据场景执行不同逻辑。
- 525: `if (lower.includes('推荐') || lower.includes('建议')) return '推荐咨询'` -> 条件判断分支，根据场景执行不同逻辑。
- 526: `if (lower.includes('酒店') || lower.includes('住宿')) return '酒店咨询'` -> 条件判断分支，根据场景执行不同逻辑。
- 527: `if (lower.includes('天气')) return '天气咨询'` -> 条件判断分支，根据场景执行不同逻辑。
- 528: `if (lower.includes('价格') || lower.includes('费用')) return '价格咨询'` -> 条件判断分支，根据场景执行不同逻辑。
- 529: `if (lower.includes('预订') || lower.includes('订票')) return '预订咨询'` -> 条件判断分支，根据场景执行不同逻辑。
- 530: `return '通用咨询'` -> 返回结果或提前结束当前流程。
- 531: `}` -> 结束当前语句或代码块。
- 532: `(空行)` -> 空行，用于提升代码结构可读性。
- 533: `// 保存行程到数据库` -> 注释行，用于解释设计意图或使用说明。
- 534: `async function saveTravelPlan(assistantMsg: ChatMsg) {` -> 定义函数或方法，实现具体业务逻辑。
- 535: `try {` -> 异常处理逻辑，提升程序健壮性。
- 536: `const backendBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 537: `const parsed = parseTripContent(assistantMsg.content)` -> 声明变量或常量，保存运行时数据。
- 538: `if (parsed.length === 0) return` -> 条件判断分支，根据场景执行不同逻辑。
- 539: `(空行)` -> 空行，用于提升代码结构可读性。
- 540: `const { destination, days } = parsePlannerInput(assistantMsg.content)` -> 声明变量或常量，保存运行时数据。
- 541: `(空行)` -> 空行，用于提升代码结构可读性。
- 542: `const planData = {` -> 声明变量或常量，保存运行时数据。
- 543: `session_id: sessionId,` -> 执行当前语句，参与该文件整体逻辑。
- 544: `plan_name: \`${destination || '行程'}规划\`,` -> 执行当前语句，参与该文件整体逻辑。
- 545: `destination: destination,` -> 执行当前语句，参与该文件整体逻辑。
- 546: `start_date: new Date().toISOString().split('T')[0],` -> 执行当前语句，参与该文件整体逻辑。
- 547: `end_date: new Date(Date.now() + days * 86400000).toISOString().split('T')[0],` -> 执行当前语句，参与该文件整体逻辑。
- 548: `status: 'draft',` -> 执行当前语句，参与该文件整体逻辑。
- 549: `items: parsed.flatMap(day => {` -> 执行当前语句，参与该文件整体逻辑。
- 550: `const items = []` -> 声明变量或常量，保存运行时数据。
- 551: `if (day.morning) {` -> 条件判断分支，根据场景执行不同逻辑。
- 552: `items.push({` -> 执行当前语句，参与该文件整体逻辑。
- 553: `day_number: day.day,` -> 执行当前语句，参与该文件整体逻辑。
- 554: `time_slot: 'morning',` -> 执行当前语句，参与该文件整体逻辑。
- 555: `title: day.morning.place || '上午活动',` -> 执行当前语句，参与该文件整体逻辑。
- 556: `description: day.morning.activity,` -> 执行当前语句，参与该文件整体逻辑。
- 557: `activity_type: '景点'` -> 执行当前语句，参与该文件整体逻辑。
- 558: `})` -> 执行当前语句，参与该文件整体逻辑。
- 559: `}` -> 结束当前语句或代码块。
- 560: `if (day.afternoon) {` -> 条件判断分支，根据场景执行不同逻辑。
- 561: `items.push({` -> 执行当前语句，参与该文件整体逻辑。
- 562: `day_number: day.day,` -> 执行当前语句，参与该文件整体逻辑。
- 563: `time_slot: 'afternoon',` -> 执行当前语句，参与该文件整体逻辑。
- 564: `title: day.afternoon.place || '下午活动',` -> 执行当前语句，参与该文件整体逻辑。
- 565: `description: day.afternoon.activity,` -> 执行当前语句，参与该文件整体逻辑。
- 566: `activity_type: '景点'` -> 执行当前语句，参与该文件整体逻辑。
- 567: `})` -> 执行当前语句，参与该文件整体逻辑。
- 568: `}` -> 结束当前语句或代码块。
- 569: `if (day.evening) {` -> 条件判断分支，根据场景执行不同逻辑。
- 570: `items.push({` -> 执行当前语句，参与该文件整体逻辑。
- 571: `day_number: day.day,` -> 执行当前语句，参与该文件整体逻辑。
- 572: `time_slot: 'evening',` -> 执行当前语句，参与该文件整体逻辑。
- 573: `title: day.evening.place || '晚上活动',` -> 执行当前语句，参与该文件整体逻辑。
- 574: `description: day.evening.activity,` -> 执行当前语句，参与该文件整体逻辑。
- 575: `activity_type: '景点'` -> 执行当前语句，参与该文件整体逻辑。
- 576: `})` -> 执行当前语句，参与该文件整体逻辑。
- 577: `}` -> 结束当前语句或代码块。
- 578: `return items` -> 返回结果或提前结束当前流程。
- 579: `})` -> 执行当前语句，参与该文件整体逻辑。
- 580: `}` -> 结束当前语句或代码块。
- 581: `(空行)` -> 空行，用于提升代码结构可读性。
- 582: `await fetch(\`${backendBase}/api/travel-plans\`, {` -> 执行当前语句，参与该文件整体逻辑。
- 583: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 584: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 585: `body: JSON.stringify(planData)` -> 执行当前语句，参与该文件整体逻辑。
- 586: `})` -> 执行当前语句，参与该文件整体逻辑。
- 587: `(空行)` -> 空行，用于提升代码结构可读性。
- 588: `console.log('行程已保存到数据库')` -> 执行当前语句，参与该文件整体逻辑。
- 589: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 590: `console.error('保存行程失败:', e)` -> 执行当前语句，参与该文件整体逻辑。
- 591: `}` -> 结束当前语句或代码块。
- 592: `}` -> 结束当前语句或代码块。
- 593: `(空行)` -> 空行，用于提升代码结构可读性。
- 594: `// 从数据库加载历史对话` -> 注释行，用于解释设计意图或使用说明。
- 595: `async function loadHistoryFromDB() {` -> 定义函数或方法，实现具体业务逻辑。
- 596: `try {` -> 异常处理逻辑，提升程序健壮性。
- 597: `const backendBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 598: `const res = await fetch(\`${backendBase}/api/conversations/sessions?user_id=${null}\`, {` -> 声明变量或常量，保存运行时数据。
- 599: `cache: 'no-store'` -> 执行当前语句，参与该文件整体逻辑。
- 600: `})` -> 执行当前语句，参与该文件整体逻辑。
- 601: `if (!res.ok) return` -> 条件判断分支，根据场景执行不同逻辑。
- 602: `(空行)` -> 空行，用于提升代码结构可读性。
- 603: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 604: `if (data.sessions?.length > 0) {` -> 条件判断分支，根据场景执行不同逻辑。
- 605: `// 可以选择加载某个历史会话` -> 注释行，用于解释设计意图或使用说明。
- 606: `console.log('找到历史会话:', data.sessions.length)` -> 执行当前语句，参与该文件整体逻辑。
- 607: `}` -> 结束当前语句或代码块。
- 608: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 609: `console.error('加载历史失败:', e)` -> 执行当前语句，参与该文件整体逻辑。
- 610: `}` -> 结束当前语句或代码块。
- 611: `}` -> 结束当前语句或代码块。
- 612: `(空行)` -> 空行，用于提升代码结构可读性。
- 613: `async function onSend() {` -> 定义函数或方法，实现具体业务逻辑。
- 614: `const text = input.trim()` -> 声明变量或常量，保存运行时数据。
- 615: `if (!text || sending) return` -> 条件判断分支，根据场景执行不同逻辑。
- 616: `setAutoScrollEnabled(true)` -> 执行当前语句，参与该文件整体逻辑。
- 617: `setSending(true)` -> 执行当前语句，参与该文件整体逻辑。
- 618: `const controller = new AbortController()` -> 声明变量或常量，保存运行时数据。
- 619: `setAbortController(controller)` -> 执行当前语句，参与该文件整体逻辑。
- 620: `setInput('')` -> 执行当前语句，参与该文件整体逻辑。
- 621: `setStreamMsgId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 622: `setStreamText('')` -> 执行当前语句，参与该文件整体逻辑。
- 623: `setAgentTools([])` -> 执行当前语句，参与该文件整体逻辑。
- 624: `(空行)` -> 空行，用于提升代码结构可读性。
- 625: `const userMsg: ChatMsg = { id: uid(), role: 'user', content: text, createdAt: Date.now() }` -> 声明变量或常量，保存运行时数据。
- 626: `const nextMsgs = [...msgs, userMsg]` -> 声明变量或常量，保存运行时数据。
- 627: `setMsgs(nextMsgs)` -> 执行当前语句，参与该文件整体逻辑。
- 628: `(空行)` -> 空行，用于提升代码结构可读性。
- 629: `const assistantId = uid()` -> 声明变量或常量，保存运行时数据。
- 630: `setMsgs(prev => [...prev, { id: assistantId, role: 'assistant', content: '', createdAt: Date.now() }])` -> 执行当前语句，参与该文件整体逻辑。
- 631: `setStreamMsgId(assistantId)  // 设置流式消息ID，用于显示"正在思考"状态` -> 执行当前语句，参与该文件整体逻辑。
- 632: `(空行)` -> 空行，用于提升代码结构可读性。
- 633: `let accContent = ''` -> 声明变量或常量，保存运行时数据。
- 634: `(空行)` -> 空行，用于提升代码结构可读性。
- 635: `try {` -> 异常处理逻辑，提升程序健壮性。
- 636: `const backendBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 637: `const res = await fetch(\`${backendBase}/api/agent/chat\`, {` -> 声明变量或常量，保存运行时数据。
- 638: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 639: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 640: `signal: controller.signal,` -> 执行当前语句，参与该文件整体逻辑。
- 641: `body: JSON.stringify({ messages: nextMsgs.map(({ role, content }) => ({ role, content })) }),` -> 执行当前语句，参与该文件整体逻辑。
- 642: `})` -> 执行当前语句，参与该文件整体逻辑。
- 643: `(空行)` -> 空行，用于提升代码结构可读性。
- 644: `if (!res.ok) throw new Error(\`HTTP ${res.status}\`)` -> 条件判断分支，根据场景执行不同逻辑。
- 645: `(空行)` -> 空行，用于提升代码结构可读性。
- 646: `const reader = res.body?.getReader()` -> 声明变量或常量，保存运行时数据。
- 647: `if (!reader) throw new Error('无法读取响应')` -> 条件判断分支，根据场景执行不同逻辑。
- 648: `(空行)` -> 空行，用于提升代码结构可读性。
- 649: `const decoder = new TextDecoder()` -> 声明变量或常量，保存运行时数据。
- 650: `let buffer = ''` -> 声明变量或常量，保存运行时数据。
- 651: `(空行)` -> 空行，用于提升代码结构可读性。
- 652: `while (true) {` -> 循环处理集合或重复执行逻辑。
- 653: `const { done, value } = await reader.read()` -> 声明变量或常量，保存运行时数据。
- 654: `if (done) break` -> 条件判断分支，根据场景执行不同逻辑。
- 655: `(空行)` -> 空行，用于提升代码结构可读性。
- 656: `buffer += decoder.decode(value, { stream: true })` -> 执行当前语句，参与该文件整体逻辑。
- 657: `const lines = buffer.split('\n')` -> 声明变量或常量，保存运行时数据。
- 658: `buffer = lines.pop() || ''` -> 执行当前语句，参与该文件整体逻辑。
- 659: `(空行)` -> 空行，用于提升代码结构可读性。
- 660: `for (const line of lines) {` -> 循环处理集合或重复执行逻辑。
- 661: `if (!line.startsWith('data: ')) continue` -> 条件判断分支，根据场景执行不同逻辑。
- 662: `const rawData = line.slice(6).trim()` -> 声明变量或常量，保存运行时数据。
- 663: `if (!rawData) continue` -> 条件判断分支，根据场景执行不同逻辑。
- 664: `let event: any` -> 声明变量或常量，保存运行时数据。
- 665: `try { event = JSON.parse(rawData) } catch { continue }` -> 异常处理逻辑，提升程序健壮性。
- 666: `(空行)` -> 空行，用于提升代码结构可读性。
- 667: `if (event.type === 'content') {` -> 条件判断分支，根据场景执行不同逻辑。
- 668: `accContent += event.data` -> 执行当前语句，参与该文件整体逻辑。
- 669: `setMsgs(prev => prev.map(m => m.id === assistantId ? { ...m, content: accContent } : m))` -> 执行当前语句，参与该文件整体逻辑。
- 670: `} else if (event.type === 'thinking') {` -> 执行当前语句，参与该文件整体逻辑。
- 671: `setAgentTools(prev => [...prev, { tool: event.tool, label: event.label, status: 'thinking' }])` -> 执行当前语句，参与该文件整体逻辑。
- 672: `} else if (event.type === 'tool_result') {` -> 执行当前语句，参与该文件整体逻辑。
- 673: `setAgentTools(prev => prev.map(t =>` -> 执行当前语句，参与该文件整体逻辑。
- 674: `t.tool === event.tool && t.status === 'thinking' ? { ...t, status: 'done' } : t` -> 执行当前语句，参与该文件整体逻辑。
- 675: `))` -> 执行当前语句，参与该文件整体逻辑。
- 676: `} else if (event.type === 'done') { break }` -> 执行当前语句，参与该文件整体逻辑。
- 677: `else if (event.type === 'error') { throw new Error(event.message || 'Agent 执行出错') }` -> 条件判断分支，根据场景执行不同逻辑。
- 678: `}` -> 结束当前语句或代码块。
- 679: `}` -> 结束当前语句或代码块。
- 680: `(空行)` -> 空行，用于提升代码结构可读性。
- 681: `const finalContent = accContent || '(未收到回复)'` -> 声明变量或常量，保存运行时数据。
- 682: `const assistantMsg: ChatMsg = { id: assistantId, role: 'assistant', content: finalContent, createdAt: Date.now() }` -> 声明变量或常量，保存运行时数据。
- 683: `setMsgs(prev => prev.map(m => m.id === assistantId ? { ...m, content: finalContent } : m))` -> 执行当前语句，参与该文件整体逻辑。
- 684: `setBackendOk(true)` -> 执行当前语句，参与该文件整体逻辑。
- 685: `persistSession([...nextMsgs, assistantMsg])` -> 执行当前语句，参与该文件整体逻辑。
- 686: `(空行)` -> 空行，用于提升代码结构可读性。
- 687: `// 保存对话到数据库` -> 注释行，用于解释设计意图或使用说明。
- 688: `saveConversationToDB(userMsg, assistantMsg)` -> 执行当前语句，参与该文件整体逻辑。
- 689: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 690: `if (e?.name === 'AbortError') return` -> 条件判断分支，根据场景执行不同逻辑。
- 691: `const rawMsg = e?.message || '未知错误'` -> 声明变量或常量，保存运行时数据。
- 692: `// 改进错误显示：显示原始错误信息便于调试` -> 注释行，用于解释设计意图或使用说明。
- 693: `const errContent = '😅 ' + rawMsg` -> 声明变量或常量，保存运行时数据。
- 694: `setMsgs(prev => prev.map(m => m.id === assistantId ? { ...m, content: errContent } : m))` -> 执行当前语句，参与该文件整体逻辑。
- 695: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 696: `setSending(false)` -> 执行当前语句，参与该文件整体逻辑。
- 697: `setAbortController(null)` -> 执行当前语句，参与该文件整体逻辑。
- 698: `setTimeout(() => setAgentTools([]), 3000)` -> 执行当前语句，参与该文件整体逻辑。
- 699: `}` -> 结束当前语句或代码块。
- 700: `}` -> 结束当前语句或代码块。
- 701: `(空行)` -> 空行，用于提升代码结构可读性。
- 702: `function resetConversation() {` -> 定义函数或方法，实现具体业务逻辑。
- 703: `setMsgs([{` -> 执行当前语句，参与该文件整体逻辑。
- 704: `id: uid(),` -> 执行当前语句，参与该文件整体逻辑。
- 705: `role: 'assistant',` -> 执行当前语句，参与该文件整体逻辑。
- 706: `content: '您好！我是小游，您的专属旅行规划师。有什么旅行计划我可以帮您规划吗？',` -> 执行当前语句，参与该文件整体逻辑。
- 707: `createdAt: Date.now(),` -> 执行当前语句，参与该文件整体逻辑。
- 708: `}])` -> 执行当前语句，参与该文件整体逻辑。
- 709: `setStreamMsgId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 710: `setStreamText('')` -> 执行当前语句，参与该文件整体逻辑。
- 711: `setInput('')` -> 执行当前语句，参与该文件整体逻辑。
- 712: `}` -> 结束当前语句或代码块。
- 713: `(空行)` -> 空行，用于提升代码结构可读性。
- 714: `const groupedByDay = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 715: `const map = new Map<number, any[]>()` -> 声明变量或常量，保存运行时数据。
- 716: `for (const it of msgs[msgs.length - 1]?.planItems || []) {` -> 循环处理集合或重复执行逻辑。
- 717: `const day = Number(it.day_number || 1)` -> 声明变量或常量，保存运行时数据。
- 718: `if (!map.has(day)) map.set(day, [])` -> 条件判断分支，根据场景执行不同逻辑。
- 719: `map.get(day)!.push(it)` -> 执行当前语句，参与该文件整体逻辑。
- 720: `}` -> 结束当前语句或代码块。
- 721: `const days = Array.from(map.keys()).sort((a, b) => a - b)` -> 声明变量或常量，保存运行时数据。
- 722: `return days.map((d) => ({` -> 返回结果或提前结束当前流程。
- 723: `day: d,` -> 执行当前语句，参与该文件整体逻辑。
- 724: `items: (map.get(d) || []).sort((a, b) => (a.sort_order ?? 0) - (b.sort_order ?? 0)),` -> 执行当前语句，参与该文件整体逻辑。
- 725: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 726: `}, [msgs])` -> 执行当前语句，参与该文件整体逻辑。
- 727: `(空行)` -> 空行，用于提升代码结构可读性。
- 728: `function formatTime(v: any) {` -> 定义函数或方法，实现具体业务逻辑。
- 729: `if (!v) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 730: `const s = String(v)` -> 声明变量或常量，保存运行时数据。
- 731: `return s.length >= 5 ? s.slice(0, 5) : s` -> 返回结果或提前结束当前流程。
- 732: `}` -> 结束当前语句或代码块。
- 733: `(空行)` -> 空行，用于提升代码结构可读性。
- 734: `const chatInputPlaceholder = '跟小游说说你想去哪里玩，例如：杭州3天，预算2000，喜欢拍照和美食'` -> 声明变量或常量，保存运行时数据。
- 735: `(空行)` -> 空行，用于提升代码结构可读性。
- 736: `return (` -> 返回结果或提前结束当前流程。
- 737: `<div className="fixed inset-0 flex flex-col overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 738: `{/* 渐变背景 - 纯 CSS，无图片，无模糊 */}` -> 执行当前语句，参与该文件整体逻辑。
- 739: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 740: `className="fixed inset-0"` -> 执行当前语句，参与该文件整体逻辑。
- 741: `style={{` -> 执行当前语句，参与该文件整体逻辑。
- 742: `background: 'linear-gradient(135deg, #e0f2fe 0%, #ede9fe 30%, #fce7f3 60%, #fef3c7 100%)',` -> 执行当前语句，参与该文件整体逻辑。
- 743: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 744: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 745: `(空行)` -> 空行，用于提升代码结构可读性。
- 746: `{/* 主内容 */}` -> 执行当前语句，参与该文件整体逻辑。
- 747: `<div className="relative z-10 flex flex-col h-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 748: `(空行)` -> 空行，用于提升代码结构可读性。
- 749: `{/* 顶部栏 */}` -> 执行当前语句，参与该文件整体逻辑。
- 750: `<header className="flex-shrink-0 bg-white/60 border-b border-white/40">` -> JSX/HTML 结构行，用于描述页面元素。
- 751: `<div className="flex items-center justify-between px-4 py-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 752: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 753: `{/* 返回首页按钮 */}` -> 执行当前语句，参与该文件整体逻辑。
- 754: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 755: `onClick={() => router.push('/')}` -> 执行当前语句，参与该文件整体逻辑。
- 756: `className="flex items-center justify-center w-9 h-9 rounded-full bg-white/80 hover:bg-white shadow-sm border border-gray-200 transition-all duration-200 hover:shadow-md"` -> 执行当前语句，参与该文件整体逻辑。
- 757: `title="返回首页"` -> 执行当前语句，参与该文件整体逻辑。
- 758: `>` -> 执行当前语句，参与该文件整体逻辑。
- 759: `<svg className="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">` -> JSX/HTML 结构行，用于描述页面元素。
- 760: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />` -> JSX/HTML 结构行，用于描述页面元素。
- 761: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 762: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 763: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 764: `<div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 via-cyan-400 to-teal-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 765: `<Sparkles className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 766: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 767: `<div className="absolute -top-0.5 -right-0.5 h-3 w-3 rounded-full bg-green-500 border-2 border-white animate-pulse"></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 768: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 769: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 770: `<h1 className="text-lg font-semibold text-gray-800">小游 · 旅行助手</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 771: `<p className="text-xs text-green-600">在线</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 772: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 773: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 774: `{/* 隐藏AI提供商选择器，默认使用智谱GLM-4.5 Air */}` -> 执行当前语句，参与该文件整体逻辑。
- 775: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 776: `<button type="button" className="text-sm px-4 py-2 rounded-full bg-white/80 text-gray-700 border border-white/50" onClick={() => setShowHistory(true)}>` -> JSX/HTML 结构行，用于描述页面元素。
- 777: `历史对话` -> 执行当前语句，参与该文件整体逻辑。
- 778: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 779: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 780: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 781: `</header>` -> JSX/HTML 结构行，用于描述页面元素。
- 782: `(空行)` -> 空行，用于提升代码结构可读性。
- 783: `{/* 快捷入口 */}` -> 执行当前语句，参与该文件整体逻辑。
- 784: `{msgs.length <= 1 && (` -> 执行当前语句，参与该文件整体逻辑。
- 785: `<div className="flex-shrink-0 px-4 pt-4 pb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 786: `<div className="grid grid-cols-2 md:grid-cols-4 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 787: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 788: `{ label: '智能问答', desc: '解答旅行相关问题', gradient: 'from-blue-500/90 to-blue-600/90', icon: Sparkles, prompt: '推荐几个适合拍照的国内城市' },` -> 执行当前语句，参与该文件整体逻辑。
- 789: `{ label: '行程规划', desc: '智能生成旅行路线', gradient: 'from-emerald-500/90 to-teal-600/90', icon: MapPin, prompt: '想去杭州3天，预算2000，喜欢自然风光和美食' },` -> 执行当前语句，参与该文件整体逻辑。
- 790: `{ label: '日程安排', desc: '2天短途旅行计划', gradient: 'from-violet-500/90 to-purple-600/90', icon: Calendar, prompt: '帮我规划一个周末南京行程' },` -> 执行当前语句，参与该文件整体逻辑。
- 791: `{ label: '旅行建议', desc: '专业旅行咨询', gradient: 'from-amber-500/90 to-orange-600/90', icon: Plane, prompt: '云南5天游大概要花多少钱？' },` -> 执行当前语句，参与该文件整体逻辑。
- 792: `].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 793: `<button key={item.label} type="button" onClick={() => setInput(item.prompt)}` -> 执行当前语句，参与该文件整体逻辑。
- 794: `className={\`group relative overflow-hidden rounded-2xl bg-gradient-to-br ${item.gradient} p-5 text-left border border-white/20\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 795: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 796: `<div className="flex h-12 w-12 items-center justify-center rounded-xl bg-white/20 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 797: `<item.icon className="h-6 w-6 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 798: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 799: `<h3 className="text-lg font-bold text-white mb-1">{item.label}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 800: `<p className="text-white/70 text-sm">{item.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 801: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 802: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 803: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 804: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 805: `(空行)` -> 空行，用于提升代码结构可读性。
- 806: `<div className="mt-5 flex flex-wrap gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 807: `{['去厦门3天预算3000', '杭州3天亲子游', '周末北京文化之旅', '云南拍照打卡路线', '日本7天自由行攻略', '泰国美食之旅', '海边度假推荐', '冬天滑雪去哪'].map((hint) => (` -> 执行当前语句，参与该文件整体逻辑。
- 808: `<button key={hint} type="button" onClick={() => setInput(hint)}` -> 执行当前语句，参与该文件整体逻辑。
- 809: `className="text-xs rounded-full bg-white/70 border border-white/50 px-3 py-1.5 text-gray-600">` -> 执行当前语句，参与该文件整体逻辑。
- 810: `{hint}` -> 执行当前语句，参与该文件整体逻辑。
- 811: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 812: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 813: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 814: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 815: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 816: `(空行)` -> 空行，用于提升代码结构可读性。
- 817: `{/* 聊天区域 */}` -> 执行当前语句，参与该文件整体逻辑。
- 818: `<div className="flex-1 flex flex-col min-h-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 819: `<div className="flex-1 min-h-0 flex flex-col">` -> JSX/HTML 结构行，用于描述页面元素。
- 820: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 821: `ref={chatContainerRef}` -> 执行当前语句，参与该文件整体逻辑。
- 822: `className="flex-1 overflow-auto px-4 py-6 space-y-6"` -> 执行当前语句，参与该文件整体逻辑。
- 823: `onScroll={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 824: `const target = e.target as HTMLDivElement` -> 声明变量或常量，保存运行时数据。
- 825: `if (target.scrollHeight - target.scrollTop - target.clientHeight < 50) setAutoScrollEnabled(true)` -> 条件判断分支，根据场景执行不同逻辑。
- 826: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 827: `>` -> 执行当前语句，参与该文件整体逻辑。
- 828: `{msgs.map((m) => {` -> 执行当前语句，参与该文件整体逻辑。
- 829: `const displayContent = m.role === 'assistant' && streamMsgId === m.id ? streamText || '\u00a0' : m.content` -> 声明变量或常量，保存运行时数据。
- 830: `const showStreamCaret = m.role === 'assistant' && streamMsgId === m.id && streamText.length < m.content.length` -> 声明变量或常量，保存运行时数据。
- 831: `const matched = m.role === 'assistant'` -> 声明变量或常量，保存运行时数据。
- 832: `? destPreview.filter((d) => (m.content || '').includes(d.name) || (m.content || '').includes(d.city)).slice(0, 2)` -> 执行当前语句，参与该文件整体逻辑。
- 833: `: []` -> 执行当前语句，参与该文件整体逻辑。
- 834: `(空行)` -> 空行，用于提升代码结构可读性。
- 835: `return (` -> 返回结果或提前结束当前流程。
- 836: `<div key={m.id}>` -> JSX/HTML 结构行，用于描述页面元素。
- 837: `<div className={\`flex items-start gap-4 ${m.role === 'user' ? 'justify-end' : 'justify-start'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 838: `{m.role === 'assistant' && (` -> 执行当前语句，参与该文件整体逻辑。
- 839: `<div className="shrink-0 flex flex-col items-center pt-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 840: `<div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 via-cyan-400 to-teal-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 841: `<Sparkles className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 842: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 843: `<span className="text-xs text-gray-500 mt-1.5 font-medium">小游</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 844: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 845: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 846: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 847: `className={\`max-w-[85%] px-5 py-3 leading-relaxed whitespace-pre-wrap ${` -> 执行当前语句，参与该文件整体逻辑。
- 848: `m.role === 'user'` -> 执行当前语句，参与该文件整体逻辑。
- 849: `? 'rounded-3xl rounded-br-md bg-gradient-to-r from-blue-500 to-blue-600 text-white'` -> 执行当前语句，参与该文件整体逻辑。
- 850: `: 'rounded-3xl rounded-bl-md bg-white/70 text-gray-800 border border-white/40'` -> 执行当前语句，参与该文件整体逻辑。
- 851: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 852: `>` -> 执行当前语句，参与该文件整体逻辑。
- 853: `{/* 小游正在思考/处理时的加载状态 */}` -> 执行当前语句，参与该文件整体逻辑。
- 854: `{m.role === 'assistant' && m.id === streamMsgId && (` -> 执行当前语句，参与该文件整体逻辑。
- 855: `<div className="flex items-center gap-2 text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 856: `<div className="h-4 w-4 border-2 border-blue-400 border-t-transparent rounded-full animate-spin" />` -> JSX/HTML 结构行，用于描述页面元素。
- 857: `<span>{agentTools.length > 0 ? agentTools[agentTools.length - 1]?.label : '小游正在思考...'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 858: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 859: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 860: `{m.role === 'assistant' && isTripContent(displayContent) && !m.planTrip ? (` -> 执行当前语句，参与该文件整体逻辑。
- 861: `<TripCard content={displayContent} />` -> JSX/HTML 结构行，用于描述页面元素。
- 862: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 863: `<div className="text-sm leading-relaxed tracking-wide">` -> JSX/HTML 结构行，用于描述页面元素。
- 864: `{processMessageContent(displayContent, m.role === 'user')}` -> 执行当前语句，参与该文件整体逻辑。
- 865: `{showStreamCaret && <span className="inline-block w-2 h-4 ml-0.5 align-[-2px] bg-blue-500/80 animate-pulse" />}` -> 执行当前语句，参与该文件整体逻辑。
- 866: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 867: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 868: `(空行)` -> 空行，用于提升代码结构可读性。
- 869: `{m.role === 'assistant' && m.planTrip && m.planItems?.length ? (` -> 执行当前语句，参与该文件整体逻辑。
- 870: `<div className="mt-4 space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 871: `{groupedByDay.map(({ day, items }) => (` -> 执行当前语句，参与该文件整体逻辑。
- 872: `<div key={\`day-${day}\`} className="rounded-xl border border-white/40 bg-white/50 p-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 873: `<div className="flex items-center gap-2 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 874: `<div className="h-7 w-7 rounded-lg bg-gradient-to-br from-blue-500 to-cyan-500 text-white flex items-center justify-center text-xs font-bold">{day}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 875: `<span className="font-semibold text-gray-900 text-sm">第{day}天</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 876: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 877: `<div className="space-y-1.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 878: `{items.map((it: any) => (` -> 执行当前语句，参与该文件整体逻辑。
- 879: `<div key={it.id ?? \`${it.title}-${it.sort_order}\`} className="flex items-start gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 880: `<div className="min-w-0 flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 881: `<div className="text-sm font-semibold text-gray-800 truncate">{it.title}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 882: `{(it.start_time || it.location) && (` -> 执行当前语句，参与该文件整体逻辑。
- 883: `<div className="text-xs text-gray-500 mt-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 884: `{it.start_time ? formatTime(it.start_time) : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 885: `{it.location ? \` · ${it.location}\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 886: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 887: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 888: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 889: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 890: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 891: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 892: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 893: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 894: `<div className="flex gap-2 pt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 895: `<Link href="/itineraries" className="btn btn-outline text-xs py-1.5 flex-1 justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 896: `<Calendar className="h-3.5 w-3.5 mr-1" />查看完整行程` -> 执行当前语句，参与该文件整体逻辑。
- 897: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 898: `<button type="button" className="btn btn-outline text-xs py-1.5 flex-1 justify-center"` -> 执行当前语句，参与该文件整体逻辑。
- 899: `onClick={() => { const last = msgs.filter((x) => x.role === 'user').pop(); if (last) setInput(last.content) }}>` -> 执行当前语句，参与该文件整体逻辑。
- 900: `<Plane className="h-3.5 w-3.5 mr-1" />重新生成` -> 执行当前语句，参与该文件整体逻辑。
- 901: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 902: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 903: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 904: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 905: `<div className={\`mt-1 text-[11px] opacity-60 ${m.role === 'user' ? 'text-right' : 'text-left'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 906: `{formatClock(m.createdAt)}` -> 执行当前语句，参与该文件整体逻辑。
- 907: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 908: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 909: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 910: `(空行)` -> 空行，用于提升代码结构可读性。
- 911: `{m.role === 'assistant' && matched.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 912: `<div className="mt-2 ml-14">` -> JSX/HTML 结构行，用于描述页面元素。
- 913: `<div className="flex flex-wrap gap-2 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 914: `{matched.map((d) => (` -> 执行当前语句，参与该文件整体逻辑。
- 915: `<Link key={\`cta-${d.id}\`} href={\`/destinations/${d.id}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 916: `className="inline-flex items-center rounded-full bg-gradient-to-r from-blue-500 to-cyan-500 text-white text-xs font-medium px-3 py-1">` -> 执行当前语句，参与该文件整体逻辑。
- 917: `查看{d.name}详情` -> 执行当前语句，参与该文件整体逻辑。
- 918: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 919: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 920: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 921: `<div className="text-xs text-gray-400 mb-2">{destPreviewLoading ? '识别中...' : '相关目的地'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 922: `<div className="grid grid-cols-1 sm:grid-cols-2 gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 923: `{matched.map((d) => (` -> 执行当前语句，参与该文件整体逻辑。
- 924: `<Link key={d.id} href={\`/destinations/${d.id}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 925: `className="group flex gap-3 items-center rounded-xl border border-white/40 bg-white/50 px-2 py-2">` -> 执行当前语句，参与该文件整体逻辑。
- 926: `<img src={coverSrc(d)} alt={d.name} loading="lazy" onError={onImgErrorUseFallback}` -> 执行当前语句，参与该文件整体逻辑。
- 927: `className="w-16 h-12 rounded-lg object-cover flex-shrink-0" />` -> 执行当前语句，参与该文件整体逻辑。
- 928: `<div className="min-w-0 flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 929: `<div className="text-sm font-semibold text-gray-900 truncate">{d.name}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 930: `<div className="text-xs text-gray-500 truncate">{d.city} · {d.province}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 931: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 932: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 933: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 934: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 935: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 936: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 937: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 938: `)` -> 结束当前语句或代码块。
- 939: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 940: `(空行)` -> 空行，用于提升代码结构可读性。
- 941: `{/* 加载指示器 - 只在没有工具调用时显示 */}` -> 执行当前语句，参与该文件整体逻辑。
- 942: `{sending && agentTools.length === 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 943: `<div className="flex items-start gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 944: `<div className="flex flex-col items-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 945: `<div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-cyan-400 animate-pulse">` -> JSX/HTML 结构行，用于描述页面元素。
- 946: `<Sparkles className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 947: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 948: `<span className="text-xs text-gray-400 mt-1">小游</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 949: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 950: `<div className="max-w-[85%] rounded-3xl rounded-bl-md bg-white/70 border border-white/40 px-5 py-4 text-sm text-gray-800">` -> JSX/HTML 结构行，用于描述页面元素。
- 951: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 952: `<span className="font-medium text-gray-600">思考中</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 953: `<div className="flex gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 954: `<span className="h-2 w-2 rounded-full bg-cyan-400 animate-bounce" style={{ animationDelay: '0ms' }}></span>` -> JSX/HTML 结构行，用于描述页面元素。
- 955: `<span className="h-2 w-2 rounded-full bg-cyan-400 animate-bounce" style={{ animationDelay: '150ms' }}></span>` -> JSX/HTML 结构行，用于描述页面元素。
- 956: `<span className="h-2 w-2 rounded-full bg-cyan-400 animate-bounce" style={{ animationDelay: '300ms' }}></span>` -> JSX/HTML 结构行，用于描述页面元素。
- 957: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 958: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 959: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 960: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 961: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 962: `(空行)` -> 空行，用于提升代码结构可读性。
- 963: `<div ref={bottomRef} />` -> JSX/HTML 结构行，用于描述页面元素。
- 964: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 965: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 966: `(空行)` -> 空行，用于提升代码结构可读性。
- 967: `{/* 底部输入栏 */}` -> 执行当前语句，参与该文件整体逻辑。
- 968: `<div className="flex-shrink-0 px-4 py-3 bg-white/60 border-t border-white/40">` -> JSX/HTML 结构行，用于描述页面元素。
- 969: `<div className="flex items-center gap-3 bg-white/80 rounded-2xl border border-white/50">` -> JSX/HTML 结构行，用于描述页面元素。
- 970: `<div className="flex items-center gap-2 pl-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 971: `<button type="button" className="w-10 h-10 rounded-full flex items-center justify-center hover:bg-white/60" onClick={onPickAttachment} aria-label="上传图片">` -> JSX/HTML 结构行，用于描述页面元素。
- 972: `<Paperclip className="h-5 w-5 text-gray-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 973: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 974: `<input ref={fileInputRef} type="file" className="hidden" accept="image/*" onChange={onAttachmentChange} />` -> JSX/HTML 结构行，用于描述页面元素。
- 975: `(空行)` -> 空行，用于提升代码结构可读性。
- 976: `<button type="button" className={\`w-10 h-10 rounded-full flex items-center justify-center ${recognizing ? 'text-blue-600 bg-blue-50/50' : 'hover:bg-white/60'}\`} onClick={onToggleVoice} aria-label="语音输入">` -> JSX/HTML 结构行，用于描述页面元素。
- 977: `<Mic className={\`h-5 w-5 ${recognizing ? 'animate-pulse text-blue-600' : 'text-gray-500'}\`} />` -> JSX/HTML 结构行，用于描述页面元素。
- 978: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 979: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 980: `(空行)` -> 空行，用于提升代码结构可读性。
- 981: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 982: `className="flex-1 bg-transparent text-sm py-4 pr-2 outline-none placeholder-gray-400 text-gray-800"` -> 执行当前语句，参与该文件整体逻辑。
- 983: `value={input}` -> 执行当前语句，参与该文件整体逻辑。
- 984: `onChange={(e) => setInput(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 985: `placeholder={chatInputPlaceholder}` -> 执行当前语句，参与该文件整体逻辑。
- 986: `onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); onSend() } }}` -> 执行当前语句，参与该文件整体逻辑。
- 987: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 988: `(空行)` -> 空行，用于提升代码结构可读性。
- 989: `{sending && abortController && (` -> 执行当前语句，参与该文件整体逻辑。
- 990: `<button className="w-10 h-10 rounded-xl flex items-center justify-center text-white bg-gradient-to-r from-rose-500 to-pink-500 mr-1"` -> 执行当前语句，参与该文件整体逻辑。
- 991: `onClick={() => abortController.abort()} title="停止生成">` -> 执行当前语句，参与该文件整体逻辑。
- 992: `<Square className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 993: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 994: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 995: `(空行)` -> 空行，用于提升代码结构可读性。
- 996: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 997: `className="w-10 h-10 rounded-xl flex items-center justify-center text-white bg-gradient-to-r from-blue-600 to-cyan-500 disabled:opacity-40 disabled:cursor-not-allowed mr-1"` -> 执行当前语句，参与该文件整体逻辑。
- 998: `disabled={!canSend} onClick={onSend}>` -> 执行当前语句，参与该文件整体逻辑。
- 999: `{sending ? <Loader2 className="h-5 w-5 animate-spin" /> : <Send className="h-5 w-5" />}` -> 执行当前语句，参与该文件整体逻辑。
- 1000: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 1001: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1002: `<div className="mt-2 text-center text-xs text-gray-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 1003: `按 Enter 发送，Shift + Enter 换行 · 小游会自动调用工具为你规划行程` -> 执行当前语句，参与该文件整体逻辑。
- 1004: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1005: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1006: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1007: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1008: `(空行)` -> 空行，用于提升代码结构可读性。
- 1009: `{/* 历史对话弹窗 */}` -> 执行当前语句，参与该文件整体逻辑。
- 1010: `{showHistory && (` -> 执行当前语句，参与该文件整体逻辑。
- 1011: `<div className="fixed inset-0 z-[80] flex items-center justify-center px-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 1012: `<div className="absolute inset-0 bg-black/40" onClick={() => setShowHistory(false)} />` -> JSX/HTML 结构行，用于描述页面元素。
- 1013: `<div className="relative w-full max-w-2xl rounded-2xl border border-white/20 bg-white/90 p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 1014: `<div className="flex items-center justify-between gap-3 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 1015: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 1016: `<div className="flex h-10 w-10 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-cyan-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 1017: `<Calendar className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 1018: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1019: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1020: `<h2 className="text-2xl font-bold text-gray-900">对话历史</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 1021: `<div className="text-sm text-gray-500 mt-1">本地保存，刷新不丢失</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1022: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1023: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1024: `<button className="btn btn-outline rounded-full px-4 py-2" type="button" onClick={() => setShowHistory(false)}>关闭</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 1025: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1026: `<div className="mt-4 space-y-3 max-h-[50vh] overflow-auto pr-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 1027: `{sessions.length ? sessions.map((s) => (` -> 执行当前语句，参与该文件整体逻辑。
- 1028: `<div key={s.id} className="rounded-xl border border-gray-200 bg-white/80 p-4 hover:border-blue-400 cursor-pointer"` -> 执行当前语句，参与该文件整体逻辑。
- 1029: `onClick={() => { setMsgs(s.msgs); setSessionId(s.id); setStreamMsgId(null); setStreamText(''); setShowHistory(false) }}` -> 执行当前语句，参与该文件整体逻辑。
- 1030: `role="button">` -> 执行当前语句，参与该文件整体逻辑。
- 1031: `<div className="flex items-center justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 1032: `<div className="min-w-0 flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 1033: `<div className="font-semibold text-gray-900 truncate text-lg">{s.title}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1034: `<div className="text-sm text-gray-400 mt-1 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 1035: `<Calendar className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 1036: `{new Date(s.createdAt).toLocaleString('zh-CN')}` -> 执行当前语句，参与该文件整体逻辑。
- 1037: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1038: `<div className="text-xs text-blue-600 mt-2 bg-blue-50 px-2 py-1 rounded-full inline-block">{s.msgs.length} 条消息</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1039: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1040: `<button type="button" className="btn btn-outline rounded-full px-3 py-1.5 text-sm"` -> 执行当前语句，参与该文件整体逻辑。
- 1041: `onClick={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 1042: `e.stopPropagation()` -> 执行当前语句，参与该文件整体逻辑。
- 1043: `const next = sessions.filter((x) => x.id !== s.id)` -> 声明变量或常量，保存运行时数据。
- 1044: `setSessions(next)` -> 执行当前语句，参与该文件整体逻辑。
- 1045: `try { localStorage.setItem('assistant_sessions_v1', JSON.stringify(next)) } catch { /* ignore */ }` -> 异常处理逻辑，提升程序健壮性。
- 1046: `}}>删除</button>` -> 执行当前语句，参与该文件整体逻辑。
- 1047: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1048: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1049: `)) : (` -> 执行当前语句，参与该文件整体逻辑。
- 1050: `<div className="rounded-xl border border-dashed border-gray-300 bg-gray-50/50 p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 1051: `<div className="mx-auto w-fit rounded-full bg-blue-100 p-3 text-blue-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 1052: `<Calendar className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 1053: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1054: `<div className="mt-4 text-gray-700 font-medium">暂无历史对话</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1055: `<div className="text-sm text-gray-500 mt-1">开始对话后，历史记录将自动保存</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1056: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1057: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 1058: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1059: `<div className="mt-6 flex gap-3 flex-wrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 1060: `<button className="btn bg-gradient-to-r from-blue-600 to-indigo-500 text-white rounded-full px-6 py-3" type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 1061: `onClick={() => { resetConversation(); setShowHistory(false) }}>开始新对话</button>` -> 执行当前语句，参与该文件整体逻辑。
- 1062: `<button className="btn btn-outline rounded-full px-6 py-3" type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 1063: `onClick={() => { if (sessions.length > 0) { setSessions([]); try { localStorage.removeItem('assistant_sessions_v1') } catch { /* ignore */ } } }}>` -> 执行当前语句，参与该文件整体逻辑。
- 1064: `清除所有历史` -> 执行当前语句，参与该文件整体逻辑。
- 1065: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 1066: `<HistoryPlansButton onSelectPlan={(plan) => {` -> 执行当前语句，参与该文件整体逻辑。
- 1067: `setShowHistory(false)` -> 执行当前语句，参与该文件整体逻辑。
- 1068: `// 基于历史行程构建提示词` -> 注释行，用于解释设计意图或使用说明。
- 1069: `const prompt = \`参考我之前的${plan.destination}行程（${plan.start_date}至${plan.end_date}），帮我规划一个新的行程。预算约${plan.budget || '未知'}元。\`` -> 声明变量或常量，保存运行时数据。
- 1070: `setInput(prompt)` -> 执行当前语句，参与该文件整体逻辑。
- 1071: `}} />` -> 执行当前语句，参与该文件整体逻辑。
- 1072: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1073: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1074: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1075: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 1076: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1077: `)` -> 结束当前语句或代码块。
- 1078: `}` -> 结束当前语句或代码块。
- 1079: `(空行)` -> 空行，用于提升代码结构可读性。
- 1080: `// 历史行程选择组件` -> 注释行，用于解释设计意图或使用说明。
- 1081: `function HistoryPlansButton({ onSelectPlan }: { onSelectPlan: (plan: any) => void }) {` -> 定义函数或方法，实现具体业务逻辑。
- 1082: `const [showPlans, setShowPlans] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 1083: `const [plans, setPlans] = useState<any[]>([])` -> 声明变量或常量，保存运行时数据。
- 1084: `const [loading, setLoading] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 1085: `(空行)` -> 空行，用于提升代码结构可读性。
- 1086: `const loadPlans = async () => {` -> 声明变量或常量，保存运行时数据。
- 1087: `if (plans.length > 0) return` -> 条件判断分支，根据场景执行不同逻辑。
- 1088: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 1089: `try {` -> 异常处理逻辑，提升程序健壮性。
- 1090: `const backendBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 1091: `const res = await fetch(\`${backendBase}/api/travel-plans?status=completed&limit=10\`)` -> 声明变量或常量，保存运行时数据。
- 1092: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 1093: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 1094: `setPlans(data.plans || [])` -> 执行当前语句，参与该文件整体逻辑。
- 1095: `}` -> 结束当前语句或代码块。
- 1096: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 1097: `console.error('加载历史行程失败', e)` -> 执行当前语句，参与该文件整体逻辑。
- 1098: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 1099: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 1100: `}` -> 结束当前语句或代码块。
- 1101: `}` -> 结束当前语句或代码块。
- 1102: `(空行)` -> 空行，用于提升代码结构可读性。
- 1103: `return (` -> 返回结果或提前结束当前流程。
- 1104: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 1105: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 1106: `className="btn btn-outline rounded-full px-6 py-3"` -> 执行当前语句，参与该文件整体逻辑。
- 1107: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 1108: `onClick={() => { setShowPlans(true); loadPlans() }}` -> 执行当前语句，参与该文件整体逻辑。
- 1109: `>` -> 执行当前语句，参与该文件整体逻辑。
- 1110: `<MapPin className="h-4 w-4 mr-1" />` -> JSX/HTML 结构行，用于描述页面元素。
- 1111: `历史行程` -> 执行当前语句，参与该文件整体逻辑。
- 1112: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 1113: `(空行)` -> 空行，用于提升代码结构可读性。
- 1114: `{showPlans && (` -> 执行当前语句，参与该文件整体逻辑。
- 1115: `<div className="fixed inset-0 z-[90] flex items-center justify-center px-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 1116: `<div className="absolute inset-0 bg-black/40" onClick={() => setShowPlans(false)} />` -> JSX/HTML 结构行，用于描述页面元素。
- 1117: `<div className="relative w-full max-w-md rounded-2xl border border-white/20 bg-white/95 p-6 max-h-[80vh] overflow-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 1118: `<div className="flex items-center justify-between mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 1119: `<h3 className="text-lg font-bold text-gray-900">历史行程</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 1120: `<button className="btn btn-outline rounded-full px-3 py-1" onClick={() => setShowPlans(false)}>关闭</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 1121: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1122: `(空行)` -> 空行，用于提升代码结构可读性。
- 1123: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 1124: `<div className="text-center py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 1125: `<Loader2 className="h-8 w-8 animate-spin mx-auto text-blue-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 1126: `<p className="mt-2 text-gray-500">加载中...</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 1127: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1128: `) : plans.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 1129: `<div className="text-center py-8 text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 1130: `暂无历史行程` -> 执行当前语句，参与该文件整体逻辑。
- 1131: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1132: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 1133: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 1134: `{plans.map(plan => (` -> 执行当前语句，参与该文件整体逻辑。
- 1135: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 1136: `key={plan.id}` -> 执行当前语句，参与该文件整体逻辑。
- 1137: `className="rounded-xl border border-gray-200 bg-white p-4 hover:border-blue-400 cursor-pointer"` -> 执行当前语句，参与该文件整体逻辑。
- 1138: `onClick={() => { onSelectPlan(plan); setShowPlans(false) }}` -> 执行当前语句，参与该文件整体逻辑。
- 1139: `>` -> 执行当前语句，参与该文件整体逻辑。
- 1140: `<div className="font-semibold text-gray-900">{plan.plan_name || plan.destination}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1141: `<div className="text-sm text-gray-500 mt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 1142: `{plan.start_date} 至 {plan.end_date}` -> 执行当前语句，参与该文件整体逻辑。
- 1143: `{plan.budget && \` · ¥${plan.budget}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 1144: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1145: `<div className="text-xs text-blue-600 mt-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 1146: `点击以此为参考规划新行程` -> 执行当前语句，参与该文件整体逻辑。
- 1147: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1148: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1149: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 1150: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1151: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 1152: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1153: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 1154: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 1155: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 1156: `)` -> 结束当前语句或代码块。
- 1157: `}` -> 结束当前语句或代码块。
