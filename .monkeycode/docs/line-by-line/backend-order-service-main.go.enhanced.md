# `backend/order-service/main.go` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `package order`
  - 作用：声明包名。
  - 原因：通过包边界组织可见性与编译单元。
- 2: `import (`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 3: `"context"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `"database/sql"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"encoding/json"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `"fmt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `"net/http"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `"time"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `"github.com/gin-gonic/gin"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `"github.com/sirupsen/logrus"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `"golang.org/x/crypto/bcrypt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `"gorm.io/gorm"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 14: `type Service struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 15: `db     *gorm.DB`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `logger *logrus.Logger`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 17: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 18: `type Order struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 19: `ID           uint      \`json:"id" gorm:"primaryKey"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `UserID       uint      \`json:"user_id" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `OrderNo      string    \`json:"order_no" gorm:"unique;not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 22: `TotalAmount  float64   \`json:"total_amount" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `Status       string    \`json:"status" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `PaymentMethod string   \`json:"payment_method"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `PaymentTime  *time.Time \`json:"payment_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `CancelledReason *string \`json:"cancelled_reason"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 27: `RefundAmount *float64  \`json:"refund_amount"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `RefundTime   *time.Time \`json:"refund_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `RefundReason *string   \`json:"refund_reason"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `CreatedAt    time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `UpdatedAt    time.Time \`json:"updated_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 33: `// 关联`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 34: `User         User       \`json:"user" gorm:"foreignKey:UserID"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `Items        []OrderItem \`json:"items" gorm:"foreignKey:OrderID"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 37: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 38: `type User struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 39: `gorm.Model`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `Phone         string \`json:"phone" gorm:"unique;not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `Email         string \`json:"email"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `Nickname      string \`json:"nickname"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `AvatarURL     string \`json:"avatar_url"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `MembershipLevel int  \`json:"membership_level"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 46: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 47: `type OrderItem struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 48: `ID           uint    \`json:"id" gorm:"primaryKey"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `OrderID      uint    \`json:"order_id" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `ProductID    uint    \`json:"product_id" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `ProductName  string  \`json:"product_name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `ProductType  string  \`json:"product_type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `Quantity     int     \`json:"quantity"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `UnitPrice    float64 \`json:"unit_price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `TotalPrice   float64 \`json:"total_price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `BookingDetails json.RawMessage \`json:"booking_details" gorm:"type:json"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `Status       string  \`json:"status"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 58: `CreatedAt    time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 59: `UpdatedAt    time.Time \`json:"updated_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 61: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 62: `type CreateOrderRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 63: `UserID       uint                 \`json:"user_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `Items        []CreateOrderItem    \`json:"items" binding:"required,min=1"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `PaymentMethod string              \`json:"payment_method"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 67: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 68: `type CreateOrderItem struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 69: `ProductID    uint                 \`json:"product_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `Quantity     int                  \`json:"quantity" binding:"required,min=1"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `BookingDetails json.RawMessage     \`json:"booking_details"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 73: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 74: `type PaymentRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 75: `OrderID      uint    \`json:"order_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `PaymentMethod string \`json:"payment_method" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 78: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 79: `type CancelRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 80: `OrderID        uint   \`json:"order_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `CancelledReason string \`json:"cancelled_reason" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 83: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 84: `type RefundRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 85: `OrderID        uint    \`json:"order_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `RefundAmount   float64 \`json:"refund_amount"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `RefundReason   string  \`json:"refund_reason" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 89: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 90: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 91: `return &Service{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 92: `db:     db,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `logger: logger,`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 94: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 95: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 96: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 97: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 98: `orders := router.Group("/orders")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 99: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `orders.POST("", s.CreateOrder)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 101: `orders.GET("/user/:user_id", s.GetUserOrders)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 102: `orders.GET("/:id", s.GetOrder)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 103: `orders.PUT("/:id/pay", s.PayOrder)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 104: `orders.PUT("/:id/cancel", s.CancelOrder)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 105: `orders.PUT("/:id/refund", s.RefundOrder)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 106: `orders.GET("/:id/status", s.GetOrderStatus)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 107: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 108: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 109: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 110: `func (s *Service) CreateOrder(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 111: `var req CreateOrderRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 112: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 113: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 114: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 115: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 116: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 117: `// 生成订单号`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 118: `orderNo := fmt.Sprintf("TR%s%d", time.Now().Format("20060102"), time.Now().UnixNano()%10000)`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 119: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 120: `// 计算总金额`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 121: `var totalAmount float64`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 122: `orderItems := make([]OrderItem, len(req.Items))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 124: `for i, item := range req.Items {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 125: `// 获取产品信息`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 126: `var product struct {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 127: `ID       uint`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 128: `Name     string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `Type     string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `Price    float64`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `Inventory int`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `if err := s.db.First(&product, item.ProductID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 135: `c.JSON(http.StatusNotFound, gin.H{"error": fmt.Sprintf("Product %d not found", item.ProductID)})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 136: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 137: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 138: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 139: `// 检查库存`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 140: `if product.Inventory < item.Quantity {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 141: `c.JSON(http.StatusBadRequest, gin.H{"error": fmt.Sprintf("Product %d inventory not enough", item.ProductID)})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 142: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 143: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 144: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 145: `// 创建订单项`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 146: `totalPrice := float64(item.Quantity) * product.Price`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 147: `orderItems[i] = OrderItem{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `ProductID:    item.ProductID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `ProductName:  product.Name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `ProductType:  product.Type,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 151: `Quantity:     item.Quantity,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `UnitPrice:    product.Price,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `TotalPrice:   totalPrice,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `BookingDetails: item.BookingDetails,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 155: `Status:       "pending",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 156: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 157: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 158: `totalAmount += totalPrice`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 160: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 161: `// 创建订单`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 162: `order := Order{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 163: `UserID:       req.UserID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `OrderNo:      orderNo,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 165: `TotalAmount:  totalAmount,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `Status:       "pending",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 167: `PaymentMethod: req.PaymentMethod,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `CreatedAt:    time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 169: `UpdatedAt:    time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 170: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 171: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 172: `// 开始事务`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 173: `tx := s.db.Begin()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 174: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 175: `if err := tx.Create(&order).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 176: `tx.Rollback()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `s.logger.WithError(err).Error("Failed to create order")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 178: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 179: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 180: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 181: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 182: `// 创建订单项`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 183: `for i := range orderItems {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 184: `orderItems[i].OrderID = order.ID`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 186: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 187: `if err := tx.Create(&orderItems).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 188: `tx.Rollback()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `s.logger.WithError(err).Error("Failed to create order items")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 190: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order items"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 191: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 192: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 193: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 194: `// 减少库存`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 195: `for _, item := range orderItems {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 196: `var product struct {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 197: `Inventory int`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 199: `if err := tx.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory - ?", item.Quantity)).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 200: `tx.Rollback()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `s.logger.WithError(err).Error("Failed to update inventory")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 202: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update inventory"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 203: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 204: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 205: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 206: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 207: `// 提交事务`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 208: `if err := tx.Commit().Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 209: `s.logger.WithError(err).Error("Failed to commit transaction")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 210: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 211: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 213: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 214: `s.logger.WithFields(logrus.Fields{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 215: `"order_id":   order.ID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `"order_no":   orderNo,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 217: `"total_amount": totalAmount,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `}).Info("Order created successfully")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 219: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 220: `c.JSON(http.StatusCreated, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 221: `"message": "Order created successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `"order":   order,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 223: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 225: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 226: `func (s *Service) GetUserOrders(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 227: `userID := c.Param("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 229: `var orders []Order`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 230: `if err := s.db.Where("user_id = ?", userID).Preload("Items").Order("created_at DESC").Find(&orders).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 231: `s.logger.WithError(err).Error("Failed to get user orders")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 232: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get user orders"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 233: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 235: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 236: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 237: `"orders": orders,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `"total":  len(orders),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 240: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 241: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 242: `func (s *Service) GetOrder(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 243: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 244: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 245: `var order Order`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 246: `if err := s.db.Preload("Items").First(&order, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 247: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 248: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 249: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 250: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 251: `c.JSON(http.StatusOK, order)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 252: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 253: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 254: `func (s *Service) PayOrder(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 255: `var req PaymentRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 256: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 257: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 258: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 259: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 260: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 261: `// 查找订单`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 262: `var order Order`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 263: `if err := s.db.First(&order, req.OrderID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 264: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 265: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 266: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 267: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 268: `// 检查订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 269: `if order.Status != "pending" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 270: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order is not in pending status"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 271: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 272: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 273: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 274: `// 更新订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 275: `now := time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 276: `order.Status = "paid"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 277: `order.PaymentMethod = req.PaymentMethod`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 278: `order.PaymentTime = &now`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 279: `order.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 280: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 281: `if err := s.db.Save(&order).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 282: `s.logger.WithError(err).Error("Failed to update order")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 283: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update order"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 284: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 285: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 286: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 287: `// 更新订单项状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 288: `if err := s.db.Model(&OrderItem{}).Where("order_id = ?", req.OrderID).Update("status", "confirmed").Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 289: `s.logger.WithError(err).Error("Failed to update order items")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 290: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 291: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 292: `s.logger.WithFields(logrus.Fields{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 293: `"order_id": req.OrderID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 294: `"status":   "paid",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 295: `}).Info("Order paid successfully")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 296: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 297: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 298: `"message": "Order paid successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 299: `"order":   order,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 300: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 301: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 302: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 303: `func (s *Service) CancelOrder(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 304: `var req CancelRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 305: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 306: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 307: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 308: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 309: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 310: `// 查找订单`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 311: `var order Order`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 312: `if err := s.db.First(&order, req.OrderID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 313: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 314: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 315: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 316: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 317: `// 检查订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 318: `if order.Status != "pending" && order.Status != "paid" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 319: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order cannot be cancelled"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 320: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 321: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 322: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 323: `// 更新订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 324: `order.Status = "cancelled"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 325: `order.CancelledReason = &req.CancelledReason`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 326: `order.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 327: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 328: `// 如果已支付，处理退款逻辑`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 329: `if order.Status == "paid" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 330: `order.RefundAmount = &order.TotalAmount`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 331: `order.RefundTime = &time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 332: `order.RefundReason = &req.CancelledReason`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 333: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 334: `// 恢复库存`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 335: `var items []OrderItem`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 336: `if err := s.db.Where("order_id = ?", req.OrderID).Find(&items).Error; err == nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 337: `for _, item := range items {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 338: `s.db.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory + ?", item.Quantity))`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 339: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 340: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 341: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 342: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 343: `if err := s.db.Save(&order).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 344: `s.logger.WithError(err).Error("Failed to cancel order")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 345: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to cancel order"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 346: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 347: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 348: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 349: `s.logger.WithFields(logrus.Fields{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 350: `"order_id": req.OrderID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 351: `"status":   "cancelled",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 352: `}).Info("Order cancelled successfully")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 353: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 354: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 355: `"message": "Order cancelled successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 356: `"order":   order,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 357: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 358: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 359: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 360: `func (s *Service) RefundOrder(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 361: `var req RefundRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 362: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 363: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 364: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 365: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 366: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 367: `// 查找订单`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 368: `var order Order`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 369: `if err := s.db.First(&order, req.OrderID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 370: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 371: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 372: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 373: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 374: `// 检查订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 375: `if order.Status != "paid" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 376: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order is not in paid status"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 377: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 378: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 379: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 380: `// 更新订单状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 381: `order.Status = "refunded"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 382: `order.RefundAmount = &req.RefundAmount`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 383: `order.RefundReason = &req.RefundReason`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `order.RefundTime = &time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 385: `order.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 386: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 387: `if err := s.db.Save(&order).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 388: `s.logger.WithError(err).Error("Failed to refund order")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 389: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to refund order"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 390: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 391: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 392: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 393: `// 恢复库存`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 394: `var items []OrderItem`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 395: `if err := s.db.Where("order_id = ?", req.OrderID).Find(&items).Error; err == nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 396: `for _, item := range items {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 397: `s.db.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory + ?", item.Quantity))`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 398: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 399: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 400: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 401: `s.logger.WithFields(logrus.Fields{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 402: `"order_id": req.OrderID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 403: `"status":   "refunded",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 404: `}).Info("Order refunded successfully")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 405: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 406: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 407: `"message": "Order refunded successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 408: `"order":   order,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 409: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 410: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 411: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 412: `func (s *Service) GetOrderStatus(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 413: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 414: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 415: `var order struct {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 416: `ID     string    \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 417: `Status string    \`json:"status"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 418: `UpdatedAt time.Time \`json:"updated_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 419: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 420: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 421: `if err := s.db.Model(&Order{}).Select("id, status, updated_at").First(&order, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 422: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 423: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 424: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 425: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 426: `c.JSON(http.StatusOK, order)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 427: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
