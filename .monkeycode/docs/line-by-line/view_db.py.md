# `view_db.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `#!/usr/bin/env python3` -> 注释行，用于解释设计意图或使用说明。
- 2: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 3: `SQLite 数据库查看工具` -> 执行当前语句，参与该文件整体逻辑。
- 4: `用法: python view_db.py [表名]` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `import sqlite3` -> 导入依赖模块，供当前文件使用。
- 8: `import sys` -> 导入依赖模块，供当前文件使用。
- 9: `import os` -> 导入依赖模块，供当前文件使用。
- 10: `from tabulate import tabulate` -> 执行当前语句，参与该文件整体逻辑。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `DB_PATH = os.path.join(os.path.dirname(__file__), "instance", "travel.db")` -> 执行当前语句，参与该文件整体逻辑。
- 13: `(空行)` -> 空行，用于提升代码结构可读性。
- 14: `(空行)` -> 空行，用于提升代码结构可读性。
- 15: `def list_tables(conn):` -> 定义 Python 函数，封装可复用逻辑。
- 16: `"""列出所有表"""` -> 执行当前语句，参与该文件整体逻辑。
- 17: `cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table';")` -> 执行当前语句，参与该文件整体逻辑。
- 18: `tables = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 19: `print("\n数据库中的表:")` -> 执行当前语句，参与该文件整体逻辑。
- 20: `print("-" * 40)` -> 执行当前语句，参与该文件整体逻辑。
- 21: `for i, (table,) in enumerate(tables, 1):` -> 循环处理集合或重复执行逻辑。
- 22: `# 获取行数（表名加引号避免SQL保留字冲突）` -> 注释行，用于解释设计意图或使用说明。
- 23: `count = conn.execute(f'SELECT COUNT(*) FROM "{table}"').fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 24: `print(f"{i}. {table} ({count} 条记录)")` -> 执行当前语句，参与该文件整体逻辑。
- 25: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 26: `return [t[0] for t in tables]` -> 返回结果或提前结束当前流程。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `(空行)` -> 空行，用于提升代码结构可读性。
- 29: `def show_table_schema(conn, table_name):` -> 定义 Python 函数，封装可复用逻辑。
- 30: `"""显示表结构"""` -> 执行当前语句，参与该文件整体逻辑。
- 31: `cursor = conn.execute(f'PRAGMA table_info("{table_name}");')` -> 执行当前语句，参与该文件整体逻辑。
- 32: `columns = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `print(f"\n表结构: {table_name}")` -> 执行当前语句，参与该文件整体逻辑。
- 35: `print("-" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `schema_data = []` -> 执行当前语句，参与该文件整体逻辑。
- 38: `for col in columns:` -> 循环处理集合或重复执行逻辑。
- 39: `cid, name, dtype, notnull, default, pk = col` -> 执行当前语句，参与该文件整体逻辑。
- 40: `schema_data.append([` -> 执行当前语句，参与该文件整体逻辑。
- 41: `cid,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `name,` -> 执行当前语句，参与该文件整体逻辑。
- 43: `dtype,` -> 执行当前语句，参与该文件整体逻辑。
- 44: `"NOT NULL" if notnull else "",` -> 执行当前语句，参与该文件整体逻辑。
- 45: `"PRIMARY KEY" if pk else "",` -> 执行当前语句，参与该文件整体逻辑。
- 46: `default if default else ""` -> 执行当前语句，参与该文件整体逻辑。
- 47: `])` -> 执行当前语句，参与该文件整体逻辑。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `print(tabulate(schema_data,` -> 执行当前语句，参与该文件整体逻辑。
- 50: `headers=["#", "列名", "类型", "约束", "主键", "默认值"],` -> 执行当前语句，参与该文件整体逻辑。
- 51: `tablefmt="grid"))` -> 执行当前语句，参与该文件整体逻辑。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `(空行)` -> 空行，用于提升代码结构可读性。
- 54: `def show_table_data(conn, table_name, limit=20):` -> 定义 Python 函数，封装可复用逻辑。
- 55: `"""显示表数据"""` -> 执行当前语句，参与该文件整体逻辑。
- 56: `try:` -> 异常处理逻辑，提升程序健壮性。
- 57: `# 获取列名` -> 注释行，用于解释设计意图或使用说明。
- 58: `cursor = conn.execute(f'PRAGMA table_info("{table_name}");')` -> 执行当前语句，参与该文件整体逻辑。
- 59: `columns = [col[1] for col in cursor.fetchall()]` -> 执行当前语句，参与该文件整体逻辑。
- 60: `(空行)` -> 空行，用于提升代码结构可读性。
- 61: `# 获取数据` -> 注释行，用于解释设计意图或使用说明。
- 62: `cursor = conn.execute(f'SELECT * FROM "{table_name}" LIMIT {limit};')` -> 执行当前语句，参与该文件整体逻辑。
- 63: `rows = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `if not rows:` -> 条件判断分支，根据场景执行不同逻辑。
- 66: `print(f"\n表 {table_name} 为空")` -> 执行当前语句，参与该文件整体逻辑。
- 67: `return` -> 返回结果或提前结束当前流程。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `print(f"\n表数据: {table_name} (前 {len(rows)} 条)")` -> 执行当前语句，参与该文件整体逻辑。
- 70: `print("-" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 71: `(空行)` -> 空行，用于提升代码结构可读性。
- 72: `# 格式化数据（截断长文本）` -> 注释行，用于解释设计意图或使用说明。
- 73: `formatted_rows = []` -> 执行当前语句，参与该文件整体逻辑。
- 74: `for row in rows:` -> 循环处理集合或重复执行逻辑。
- 75: `formatted_row = []` -> 执行当前语句，参与该文件整体逻辑。
- 76: `for cell in row:` -> 循环处理集合或重复执行逻辑。
- 77: `if cell is None:` -> 条件判断分支，根据场景执行不同逻辑。
- 78: `formatted_row.append("NULL")` -> 执行当前语句，参与该文件整体逻辑。
- 79: `elif isinstance(cell, str) and len(cell) > 50:` -> 条件判断分支，根据场景执行不同逻辑。
- 80: `formatted_row.append(cell[:50] + "...")` -> 执行当前语句，参与该文件整体逻辑。
- 81: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 82: `formatted_row.append(cell)` -> 执行当前语句，参与该文件整体逻辑。
- 83: `formatted_rows.append(formatted_row)` -> 执行当前语句，参与该文件整体逻辑。
- 84: `(空行)` -> 空行，用于提升代码结构可读性。
- 85: `print(tabulate(formatted_rows, headers=columns, tablefmt="grid"))` -> 执行当前语句，参与该文件整体逻辑。
- 86: `(空行)` -> 空行，用于提升代码结构可读性。
- 87: `# 显示总行数` -> 注释行，用于解释设计意图或使用说明。
- 88: `count = conn.execute(f'SELECT COUNT(*) FROM "{table_name}"').fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 89: `if count > limit:` -> 条件判断分支，根据场景执行不同逻辑。
- 90: `print(f"\n... 还有 {count - limit} 条记录")` -> 执行当前语句，参与该文件整体逻辑。
- 91: `(空行)` -> 空行，用于提升代码结构可读性。
- 92: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 93: `print(f"查询失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `(空行)` -> 空行，用于提升代码结构可读性。
- 96: `def search_data(conn, keyword):` -> 定义 Python 函数，封装可复用逻辑。
- 97: `"""搜索数据"""` -> 执行当前语句，参与该文件整体逻辑。
- 98: `tables = list_tables(conn)` -> 执行当前语句，参与该文件整体逻辑。
- 99: `print(f"\n搜索关键字: '{keyword}'")` -> 执行当前语句，参与该文件整体逻辑。
- 100: `print("-" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 101: `(空行)` -> 空行，用于提升代码结构可读性。
- 102: `found = False` -> 执行当前语句，参与该文件整体逻辑。
- 103: `for table in tables:` -> 循环处理集合或重复执行逻辑。
- 104: `cursor = conn.execute(f'PRAGMA table_info("{table}");')` -> 执行当前语句，参与该文件整体逻辑。
- 105: `columns = [col[1] for col in cursor.fetchall()]` -> 执行当前语句，参与该文件整体逻辑。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `# 构建搜索条件` -> 注释行，用于解释设计意图或使用说明。
- 108: `conditions = [f"{col} LIKE ?" for col in columns]` -> 执行当前语句，参与该文件整体逻辑。
- 109: `sql = f'SELECT * FROM "{table}" WHERE {" OR ".join(conditions)} LIMIT 5'` -> 执行当前语句，参与该文件整体逻辑。
- 110: `(空行)` -> 空行，用于提升代码结构可读性。
- 111: `try:` -> 异常处理逻辑，提升程序健壮性。
- 112: `cursor = conn.execute(sql, [f"%{keyword}%"] * len(columns))` -> 执行当前语句，参与该文件整体逻辑。
- 113: `rows = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 114: `(空行)` -> 空行，用于提升代码结构可读性。
- 115: `if rows:` -> 条件判断分支，根据场景执行不同逻辑。
- 116: `found = True` -> 执行当前语句，参与该文件整体逻辑。
- 117: `print(f"\n在表 '{table}' 中找到 {len(rows)} 条记录:")` -> 执行当前语句，参与该文件整体逻辑。
- 118: `print(tabulate(rows, headers=columns, tablefmt="simple"))` -> 执行当前语句，参与该文件整体逻辑。
- 119: `except:` -> 异常处理逻辑，提升程序健壮性。
- 120: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 121: `(空行)` -> 空行，用于提升代码结构可读性。
- 122: `if not found:` -> 条件判断分支，根据场景执行不同逻辑。
- 123: `print("未找到匹配的记录")` -> 执行当前语句，参与该文件整体逻辑。
- 124: `(空行)` -> 空行，用于提升代码结构可读性。
- 125: `(空行)` -> 空行，用于提升代码结构可读性。
- 126: `def export_to_sql(conn, output_file="export.sql"):` -> 定义 Python 函数，封装可复用逻辑。
- 127: `"""导出为 SQL 文件"""` -> 执行当前语句，参与该文件整体逻辑。
- 128: `with open(output_file, 'w', encoding='utf-8') as f:` -> 执行当前语句，参与该文件整体逻辑。
- 129: `for line in conn.iterdump():` -> 循环处理集合或重复执行逻辑。
- 130: `f.write(f"{line}\n")` -> 执行当前语句，参与该文件整体逻辑。
- 131: `print(f"\n已导出到: {output_file}")` -> 执行当前语句，参与该文件整体逻辑。
- 132: `(空行)` -> 空行，用于提升代码结构可读性。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `def delete_record(conn, table_name, record_id):` -> 定义 Python 函数，封装可复用逻辑。
- 135: `"""删除记录，自动处理外键约束"""` -> 执行当前语句，参与该文件整体逻辑。
- 136: `try:` -> 异常处理逻辑，提升程序健壮性。
- 137: `# 获取所有外键引用` -> 注释行，用于解释设计意图或使用说明。
- 138: `cursor = conn.execute(f'PRAGMA foreign_key_list("{table_name}");')` -> 执行当前语句，参与该文件整体逻辑。
- 139: `fks = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 140: `(空行)` -> 空行，用于提升代码结构可读性。
- 141: `deleted_count = 0` -> 执行当前语句，参与该文件整体逻辑。
- 142: `(空行)` -> 空行，用于提升代码结构可读性。
- 143: `# 查询所有其他表，查找外键引用` -> 注释行，用于解释设计意图或使用说明。
- 144: `cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table';")` -> 执行当前语句，参与该文件整体逻辑。
- 145: `all_tables = [t[0] for t in cursor.fetchall()]` -> 执行当前语句，参与该文件整体逻辑。
- 146: `(空行)` -> 空行，用于提升代码结构可读性。
- 147: `for other_table in all_tables:` -> 循环处理集合或重复执行逻辑。
- 148: `if other_table == table_name:` -> 条件判断分支，根据场景执行不同逻辑。
- 149: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 150: `try:` -> 异常处理逻辑，提升程序健壮性。
- 151: `# 获取其他表的列` -> 注释行，用于解释设计意图或使用说明。
- 152: `cursor = conn.execute(f'PRAGMA table_info("{other_table}");')` -> 执行当前语句，参与该文件整体逻辑。
- 153: `columns = [col[1] for col in cursor.fetchall()]` -> 执行当前语句，参与该文件整体逻辑。
- 154: `(空行)` -> 空行，用于提升代码结构可读性。
- 155: `# 查找可能的外键列` -> 注释行，用于解释设计意图或使用说明。
- 156: `for col in columns:` -> 循环处理集合或重复执行逻辑。
- 157: `if col.endswith('_id') or col == 'user_id':` -> 条件判断分支，根据场景执行不同逻辑。
- 158: `try:` -> 异常处理逻辑，提升程序健壮性。
- 159: `cursor.execute(f'SELECT COUNT(*) FROM "{other_table}" WHERE "{col}" = ?', (record_id,))` -> 执行当前语句，参与该文件整体逻辑。
- 160: `count = cursor.fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 161: `if count > 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 162: `cursor.execute(f'DELETE FROM "{other_table}" WHERE "{col}" = ?', (record_id,))` -> 执行当前语句，参与该文件整体逻辑。
- 163: `deleted_count += cursor.rowcount` -> 执行当前语句，参与该文件整体逻辑。
- 164: `print(f'  ✓ 已删除 {other_table}.{col} 中 {count} 条')` -> 执行当前语句，参与该文件整体逻辑。
- 165: `except:` -> 异常处理逻辑，提升程序健壮性。
- 166: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 167: `# 也检查 id 字段` -> 注释行，用于解释设计意图或使用说明。
- 168: `try:` -> 异常处理逻辑，提升程序健壮性。
- 169: `cursor.execute(f'SELECT COUNT(*) FROM "{other_table}" WHERE "id" = ?', (record_id,))` -> 执行当前语句，参与该文件整体逻辑。
- 170: `count = cursor.fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 171: `if count > 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 172: `cursor.execute(f'DELETE FROM "{other_table}" WHERE "id" = ?', (record_id,))` -> 执行当前语句，参与该文件整体逻辑。
- 173: `deleted_count += cursor.rowcount` -> 执行当前语句，参与该文件整体逻辑。
- 174: `print(f'  ✓ 已删除 {other_table}.id 中 {count} 条')` -> 执行当前语句，参与该文件整体逻辑。
- 175: `except:` -> 异常处理逻辑，提升程序健壮性。
- 176: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 177: `except:` -> 异常处理逻辑，提升程序健壮性。
- 178: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `# 删除主记录` -> 注释行，用于解释设计意图或使用说明。
- 181: `cursor.execute(f'DELETE FROM "{table_name}" WHERE id = ?', (record_id,))` -> 执行当前语句，参与该文件整体逻辑。
- 182: `deleted_count += cursor.rowcount` -> 执行当前语句，参与该文件整体逻辑。
- 183: `(空行)` -> 空行，用于提升代码结构可读性。
- 184: `conn.commit()` -> 执行当前语句，参与该文件整体逻辑。
- 185: `print(f'✓ 已删除 {table_name} id={record_id}，共 {deleted_count} 条')` -> 执行当前语句，参与该文件整体逻辑。
- 186: `(空行)` -> 空行，用于提升代码结构可读性。
- 187: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 188: `conn.rollback()` -> 执行当前语句，参与该文件整体逻辑。
- 189: `print(f'✗ 删除失败: {e}')` -> 执行当前语句，参与该文件整体逻辑。
- 190: `(空行)` -> 空行，用于提升代码结构可读性。
- 191: `(空行)` -> 空行，用于提升代码结构可读性。
- 192: `def main():` -> 定义 Python 函数，封装可复用逻辑。
- 193: `if not os.path.exists(DB_PATH):` -> 条件判断分支，根据场景执行不同逻辑。
- 194: `print(f"数据库文件不存在: {DB_PATH}")` -> 执行当前语句，参与该文件整体逻辑。
- 195: `sys.exit(1)` -> 执行当前语句，参与该文件整体逻辑。
- 196: `(空行)` -> 空行，用于提升代码结构可读性。
- 197: `conn = sqlite3.connect(DB_PATH)` -> 执行当前语句，参与该文件整体逻辑。
- 198: `(空行)` -> 空行，用于提升代码结构可读性。
- 199: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 200: `print("  SQLite 数据库查看工具")` -> 执行当前语句，参与该文件整体逻辑。
- 201: `print(f"  数据库: {DB_PATH}")` -> 执行当前语句，参与该文件整体逻辑。
- 202: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 203: `(空行)` -> 空行，用于提升代码结构可读性。
- 204: `# 如果没有参数，显示帮助` -> 注释行，用于解释设计意图或使用说明。
- 205: `if len(sys.argv) == 1:` -> 条件判断分支，根据场景执行不同逻辑。
- 206: `print("\n用法:")` -> 执行当前语句，参与该文件整体逻辑。
- 207: `print("  python view_db.py              - 列出所有表")` -> 执行当前语句，参与该文件整体逻辑。
- 208: `print("  python view_db.py 表名         - 查看表结构和数据")` -> 执行当前语句，参与该文件整体逻辑。
- 209: `print("  python view_db.py -s 表名      - 只查看表结构")` -> 执行当前语句，参与该文件整体逻辑。
- 210: `print("  python view_db.py -a 表名      - 查看所有数据")` -> 执行当前语句，参与该文件整体逻辑。
- 211: `print("  python view_db.py -f 关键字    - 搜索数据")` -> 执行当前语句，参与该文件整体逻辑。
- 212: `print("  python view_db.py -e           - 导出为 SQL 文件")` -> 执行当前语句，参与该文件整体逻辑。
- 213: `print("  python view_db.py -d 表名 id   - 删除指定记录(自动处理外键)")` -> 执行当前语句，参与该文件整体逻辑。
- 214: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 215: `list_tables(conn)` -> 执行当前语句，参与该文件整体逻辑。
- 216: `(空行)` -> 空行，用于提升代码结构可读性。
- 217: `# 查看指定表` -> 注释行，用于解释设计意图或使用说明。
- 218: `elif len(sys.argv) == 2 and not sys.argv[1].startswith('-'):` -> 条件判断分支，根据场景执行不同逻辑。
- 219: `table_name = sys.argv[1]` -> 执行当前语句，参与该文件整体逻辑。
- 220: `show_table_schema(conn, table_name)` -> 执行当前语句，参与该文件整体逻辑。
- 221: `show_table_data(conn, table_name)` -> 执行当前语句，参与该文件整体逻辑。
- 222: `(空行)` -> 空行，用于提升代码结构可读性。
- 223: `# 只查看结构` -> 注释行，用于解释设计意图或使用说明。
- 224: `elif sys.argv[1] == '-s' and len(sys.argv) >= 3:` -> 条件判断分支，根据场景执行不同逻辑。
- 225: `show_table_schema(conn, sys.argv[2])` -> 执行当前语句，参与该文件整体逻辑。
- 226: `(空行)` -> 空行，用于提升代码结构可读性。
- 227: `# 查看所有数据` -> 注释行，用于解释设计意图或使用说明。
- 228: `elif sys.argv[1] == '-a' and len(sys.argv) >= 3:` -> 条件判断分支，根据场景执行不同逻辑。
- 229: `show_table_data(conn, sys.argv[2], limit=10000)` -> 执行当前语句，参与该文件整体逻辑。
- 230: `(空行)` -> 空行，用于提升代码结构可读性。
- 231: `# 搜索` -> 注释行，用于解释设计意图或使用说明。
- 232: `elif sys.argv[1] == '-f' and len(sys.argv) >= 3:` -> 条件判断分支，根据场景执行不同逻辑。
- 233: `search_data(conn, sys.argv[2])` -> 执行当前语句，参与该文件整体逻辑。
- 234: `(空行)` -> 空行，用于提升代码结构可读性。
- 235: `# 导出` -> 注释行，用于解释设计意图或使用说明。
- 236: `elif sys.argv[1] == '-e':` -> 条件判断分支，根据场景执行不同逻辑。
- 237: `export_to_sql(conn)` -> 执行当前语句，参与该文件整体逻辑。
- 238: `(空行)` -> 空行，用于提升代码结构可读性。
- 239: `# 删除记录` -> 注释行，用于解释设计意图或使用说明。
- 240: `elif sys.argv[1] == '-d' and len(sys.argv) >= 4:` -> 条件判断分支，根据场景执行不同逻辑。
- 241: `delete_record(conn, sys.argv[2], int(sys.argv[3]))` -> 执行当前语句，参与该文件整体逻辑。
- 242: `(空行)` -> 空行，用于提升代码结构可读性。
- 243: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 244: `print("未知命令，使用: python view_db.py")` -> 执行当前语句，参与该文件整体逻辑。
- 245: `(空行)` -> 空行，用于提升代码结构可读性。
- 246: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 247: `(空行)` -> 空行，用于提升代码结构可读性。
- 248: `(空行)` -> 空行，用于提升代码结构可读性。
- 249: `if __name__ == "__main__":` -> 条件判断分支，根据场景执行不同逻辑。
- 250: `main()` -> 执行当前语句，参与该文件整体逻辑。
