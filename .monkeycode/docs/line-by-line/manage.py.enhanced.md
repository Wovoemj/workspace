# `manage.py` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `#!/usr/bin/env python3`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 3: `manage.py — 智旅助手数据库管理 CLI`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 5: `用法：`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `python manage.py list-users                        列出所有用户`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `python manage.py create-user <email>              创建普通用户（交互式输入密码）`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `python manage.py create-user <email> --admin      创建管理员用户`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `python manage.py set-admin <email>                将用户设为管理员`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `python manage.py revoke-admin <email>             撤销管理员权限`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `python manage.py set-level <email> <level>        设置会员等级`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `python manage.py reset-password <email>           重置密码（交互式）`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `python manage.py delete-user <email>              删除用户`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `python manage.py shell                             进入 Flask shell 交互环境`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 15: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 16: `示例：`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 17: `python manage.py create-user admin@example.com --admin`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `python manage.py list-users`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `python manage.py set-level super@example.com 10`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `import os`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 22: `import sys`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 23: `import click`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 24: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 25: `# ── 确保项目根目录在 Python 路径 ────────────────────────────────────────────`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 26: `_ROOT = os.path.dirname(os.path.abspath(__file__))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 27: `if _ROOT not in sys.path:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 28: `sys.path.insert(0, _ROOT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 30: `from extensions import db`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `from models import User`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `from werkzeug.security import generate_password_hash, check_password_hash`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `from datetime import datetime`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 35: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 36: `def _app():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 37: `"""延迟导入，避免循环依赖时过早创建表"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `# 注意：app.py 中已完成 db.init_app(app)，直接导入 app 即可`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 39: `from app import app`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `return app`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 41: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 42: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 43: `def _ctx():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 44: `app = _app()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `return app.app_context()`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 46: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `# ── 命令组 ────────────────────────────────────────────────────────────────────`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 49: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 50: `@click.group()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `def cli():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 52: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `智旅助手数据库管理工具`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 57: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 58: `@cli.command("list-users")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 59: `def list_users():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 60: `"""列出所有用户"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `users = User.query.order_by(User.created_at.desc()).all()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `if not users:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 64: `click.echo("暂无用户记录。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `click.echo(f"\n{'ID':>4}  {'邮箱':<30}  {'昵称':<15}  {'管理员':<6}  {'等级':<5}  {'注册时间'}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `click.echo("-" * 90)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `for u in users:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 69: `is_admin = "✅" if _is_admin(u) else "  "`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `created = u.created_at.strftime("%Y-%m-%d %H:%M") if u.created_at else "—"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `email = (u.email or "—")[:28]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `nickname = (u.nickname or "—")[:13]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `click.echo(f"{u.id:>4}  {email:<30}  {nickname:<15}  {is_admin:<6}  {u.membership_level:<5}  {created}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `click.echo(f"\n共 {len(users)} 位用户。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 76: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 77: `def _is_admin(user: User) -> bool:`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 78: `if getattr(user, 'is_admin', False):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 79: `return True`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 80: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 81: `threshold = int(os.environ.get("ADMIN_MEMBERSHIP_LEVEL", "9").strip() or "9")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 83: `threshold = 9`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `return int(user.membership_level or 1) >= threshold`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 85: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 86: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 87: `def _prompt_password() -> str:`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 88: `while True:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 89: `p1 = click.prompt("密码（不少于 8 位，需包含大小写字母和数字）", hide_input=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 90: `default="", show_default=False)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `if len(p1) < 8:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 92: `click.echo("⚠  密码长度不能少于 8 位，请重新输入。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `continue`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `p2 = click.prompt("确认密码", hide_input=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 95: `if p1 != p2:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 96: `click.echo("⚠  两次密码不一致，请重新输入。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `continue`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `return p1`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 99: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 100: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 101: `@cli.command("create-user")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `@click.option("--admin", is_flag=True, help="创建为管理员账号")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 104: `@click.option("--username", default=None, help="用户名（username 字段，默认取邮箱前缀）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `@click.option("--nickname", default=None, help="昵称（默认取邮箱前缀）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 106: `@click.option("--password", default=None, help="直接指定密码（建议仅测试用，交互式更安全）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 107: `def create_user(email: str, admin: bool, username: str, nickname: str, password: str):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 108: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `创建新用户。`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 110: `若不指定密码，将交互式输入。`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 111: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 112: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `# 检查邮箱是否已存在`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 114: `existing = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 115: `if existing:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 116: `click.echo(f"❌  邮箱 {email} 已存在（用户 ID={existing.id}）。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 119: `if not password:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 120: `password = _prompt_password()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `elif len(password) < 8:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 122: `click.echo("❌  密码长度不能少于 8 位。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 125: `if not username:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 126: `username = email.split("@")[0][:20]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 127: `if not nickname:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 128: `nickname = username`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 130: `user = User(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `username=username,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `nickname=nickname,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 133: `email=email.lower(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 134: `password_hash=generate_password_hash(password),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 135: `membership_level=10 if admin else 1,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `is_admin=admin,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 137: `created_at=datetime.now(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 138: `updated_at=datetime.now(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 140: `db.session.add(user)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 141: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 142: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 143: `role = "管理员" if admin else "普通用户"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `click.echo(f"✅  用户创建成功！")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `click.echo(f"    邮箱：{email}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 146: `click.echo(f"    角色：{role}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 147: `click.echo(f"    会员等级：{user.membership_level}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `click.echo(f"    is_admin：{user.is_admin}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `click.echo(f"\n提示：管理员请使用 /api/admin/login 接口登录")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 151: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 152: `@cli.command("set-admin")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `def set_admin(email: str):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 155: `"""将用户设为管理员"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 156: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 157: `user = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 158: `if not user:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 159: `click.echo(f"❌  用户 {email} 不存在。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 160: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 161: `# 将用户设为管理员`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 162: `user.is_admin = True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 163: `# 确保 membership_level 至少为 10，兼容 None 或非整数情况`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 164: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 165: `current_level = int(user.membership_level or 1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 167: `current_level = 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `user.membership_level = max(current_level, 10)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `# 更新更新时间并提交`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 170: `user.updated_at = datetime.now()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 172: `click.echo(f"✅  {email} 现已是管理员（is_admin=True, membership_level={user.membership_level}）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 174: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 175: `@cli.command("revoke-admin")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 176: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `def revoke_admin(email: str):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 178: `"""撤销用户的管理员权限"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 179: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 180: `user = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 181: `if not user:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 182: `click.echo(f"❌  用户 {email} 不存在。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 183: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 184: `user.is_admin = False`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 186: `click.echo(f"✅  已撤销 {email} 的管理员权限（is_admin=False）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 187: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 188: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 189: `@cli.command("set-level")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 190: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 191: `@click.argument("level", type=int)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 192: `def set_level(email: str, level: int):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 193: `"""设置用户会员等级"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 194: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `user = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 196: `if not user:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 197: `click.echo(f"❌  用户 {email} 不存在。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 199: `user.membership_level = max(1, level)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 200: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `click.echo(f"✅  {email} 的会员等级已更新为 {user.membership_level}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 203: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 204: `@cli.command("reset-password")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 205: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 206: `def reset_password(email: str):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 207: `"""交互式重置用户密码"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 208: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 209: `user = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 210: `if not user:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 211: `click.echo(f"❌  用户 {email} 不存在。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 213: `password = _prompt_password()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `user.password_hash = generate_password_hash(password)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 215: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `click.echo(f"✅  {email} 的密码已重置。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 217: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 218: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 219: `@cli.command("delete-user")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 220: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `@click.option("--force", is_flag=True, help="跳过确认直接删除")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `def delete_user(email: str, force: bool):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 223: `"""删除用户（会级联删除其评论）"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `with _ctx():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 225: `user = User.query.filter_by(email=email.lower()).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `if not user:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 227: `click.echo(f"❌  用户 {email} 不存在。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 229: `if not force:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 230: `click.confirm(f"确认删除用户 {email}（ID={user.id}）及其所有评论？", abort=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 231: `from models import DestinationComment`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 232: `DestinationComment.query.filter_by(user_id=user.id).delete(synchronize_session=False)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 233: `db.session.delete(user)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 235: `click.echo(f"✅  用户 {email} 已删除。")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 236: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 237: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 238: `@cli.command("shell")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `def shell():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 240: `"""进入 Flask 应用上下文交互环境（等同于 flask shell）"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 241: `app = _app()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 242: `import code`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 243: `ctx = app.test_request_context()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 244: `ctx.push()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 245: `code.interact(local={"app": app, "db": db, "User": User})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 247: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 248: `# ── 快捷别名 ──────────────────────────────────────────────────────────────────`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 249: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 250: `@cli.command("promote")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 251: `@click.argument("email")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 252: `def promote(email: str):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 253: `"""快捷命令：将用户提升为管理员（等同于 set-admin）"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 254: `ctx = click.get_current_context()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 255: `ctx.invoke(set_admin, email=email)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 256: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 257: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 258: `if __name__ == "__main__":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 259: `cli()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
