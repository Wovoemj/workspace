# `scripts/import_destinations_from_json.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `import json` -> 导入依赖模块，供当前文件使用。
- 2: `import os` -> 导入依赖模块，供当前文件使用。
- 3: `import re` -> 导入依赖模块，供当前文件使用。
- 4: `import sys` -> 导入依赖模块，供当前文件使用。
- 5: `from pathlib import Path` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `(空行)` -> 空行，用于提升代码结构可读性。
- 8: `def _parse_ticket_price(price_range: str) -> float:` -> 定义 Python 函数，封装可复用逻辑。
- 9: `if not price_range:` -> 条件判断分支，根据场景执行不同逻辑。
- 10: `return 0.0` -> 返回结果或提前结束当前流程。
- 11: `# Examples: "60-100元", "免费-景点单独收费", "40-45元"` -> 注释行，用于解释设计意图或使用说明。
- 12: `if "免费" in price_range:  # 检查是否包含  # 检查是否包含  # 检查是否包含` -> 条件判断分支，根据场景执行不同逻辑。
- 13: `return 0.0` -> 返回结果或提前结束当前流程。
- 14: `m = re.search(r"(\d+(\.\d+)?)", price_range)` -> 执行当前语句，参与该文件整体逻辑。
- 15: `if not m:` -> 条件判断分支，根据场景执行不同逻辑。
- 16: `return 0.0` -> 返回结果或提前结束当前流程。
- 17: `try:` -> 异常处理逻辑，提升程序健壮性。
- 18: `return float(m.group(1))` -> 返回结果或提前结束当前流程。
- 19: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 20: `return 0.0` -> 返回结果或提前结束当前流程。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `def main():  # 主函数：程序入口点  # 主函数：程序入口点  # 主函数：程序入口点` -> 定义 Python 函数，封装可复用逻辑。
- 24: `# Ensure project root is on sys.path so we can import app.py as module "app"` -> 注释行，用于解释设计意图或使用说明。
- 25: `project_root = Path(__file__).resolve().parents[1]` -> 执行当前语句，参与该文件整体逻辑。
- 26: `if str(project_root) not in sys.path:  # 检查是否包含  # 检查是否包含  # 检查是否包含` -> 条件判断分支，根据场景执行不同逻辑。
- 27: `sys.path.insert(0, str(project_root))` -> 执行当前语句，参与该文件整体逻辑。
- 28: `(空行)` -> 空行，用于提升代码结构可读性。
- 29: `json_path = Path(os.environ.get("DESTINATIONS_JSON_PATH", r"D:\travel-assisent-master\destinations.json"))  # 路径配置json_path  # 路径配置json_path  # 路径配置json_path` -> 执行当前语句，参与该文件整体逻辑。
- 30: `if not json_path.exists():` -> 条件判断分支，根据场景执行不同逻辑。
- 31: `raise SystemExit(f"destinations.json not found: {json_path}")  # 抛出异常  # 抛出异常  # 抛出异常` -> 执行当前语句，参与该文件整体逻辑。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `raw = json.loads(json_path.read_text(encoding="utf-8"))` -> 执行当前语句，参与该文件整体逻辑。
- 34: `if not isinstance(raw, list):` -> 条件判断分支，根据场景执行不同逻辑。
- 35: `raise SystemExit("destinations.json must be a list")  # 抛出异常  # 抛出异常  # 抛出异常` -> 执行当前语句，参与该文件整体逻辑。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `from app import app, db, Destination` -> 执行当前语句，参与该文件整体逻辑。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `upserted = 0` -> 执行当前语句，参与该文件整体逻辑。
- 40: `created = 0` -> 执行当前语句，参与该文件整体逻辑。
- 41: `updated = 0` -> 执行当前语句，参与该文件整体逻辑。
- 42: `skipped = 0` -> 执行当前语句，参与该文件整体逻辑。
- 43: `(空行)` -> 空行，用于提升代码结构可读性。
- 44: `with app.app_context():` -> 执行当前语句，参与该文件整体逻辑。
- 45: `db.create_all()` -> 执行当前语句，参与该文件整体逻辑。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `for item in raw:  # 遍历item  # 遍历item  # 遍历item` -> 循环处理集合或重复执行逻辑。
- 48: `if not isinstance(item, dict):` -> 条件判断分支，根据场景执行不同逻辑。
- 49: `skipped += 1` -> 执行当前语句，参与该文件整体逻辑。
- 50: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `name = (item.get("name") or "").strip()` -> 执行当前语句，参与该文件整体逻辑。
- 53: `city = (item.get("city") or "").strip()` -> 执行当前语句，参与该文件整体逻辑。
- 54: `province = (item.get("province") or "").strip()` -> 执行当前语句，参与该文件整体逻辑。
- 55: `if not name or not city or not province:` -> 条件判断分支，根据场景执行不同逻辑。
- 56: `skipped += 1` -> 执行当前语句，参与该文件整体逻辑。
- 57: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `description = item.get("description") or ""` -> 执行当前语句，参与该文件整体逻辑。
- 60: `rating = float(item.get("rating") or 0) if item.get("rating") is not None else 0.0` -> 执行当前语句，参与该文件整体逻辑。
- 61: `opening_hours = item.get("opening_hours") or ""` -> 执行当前语句，参与该文件整体逻辑。
- 62: `cover_image = item.get("cover_image") or ""` -> 执行当前语句，参与该文件整体逻辑。
- 63: `ticket_price = _parse_ticket_price(item.get("price_range") or "")` -> 执行当前语句，参与该文件整体逻辑。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `obj = Destination.query.filter_by(name=name, city=city, province=province).first()` -> 执行当前语句，参与该文件整体逻辑。
- 66: `if obj is None:  # 检查是否为空  # 检查是否为空  # 检查是否为空` -> 条件判断分支，根据场景执行不同逻辑。
- 67: `obj = Destination(` -> 执行当前语句，参与该文件整体逻辑。
- 68: `name=name,` -> 执行当前语句，参与该文件整体逻辑。
- 69: `city=city,` -> 执行当前语句，参与该文件整体逻辑。
- 70: `province=province,` -> 执行当前语句，参与该文件整体逻辑。
- 71: `description=description,` -> 执行当前语句，参与该文件整体逻辑。
- 72: `cover_image=cover_image,` -> 执行当前语句，参与该文件整体逻辑。
- 73: `rating=rating or 5.0,` -> 执行当前语句，参与该文件整体逻辑。
- 74: `ticket_price=ticket_price,` -> 执行当前语句，参与该文件整体逻辑。
- 75: `open_time=opening_hours,` -> 执行当前语句，参与该文件整体逻辑。
- 76: `)` -> 结束当前语句或代码块。
- 77: `db.session.add(obj)` -> 执行当前语句，参与该文件整体逻辑。
- 78: `created += 1` -> 执行当前语句，参与该文件整体逻辑。
- 79: `else:  # 否则执行  # 否则执行  # 否则执行` -> 条件判断分支，根据场景执行不同逻辑。
- 80: `# Only overwrite fields when new value is present` -> 注释行，用于解释设计意图或使用说明。
- 81: `obj.description = description or obj.description` -> 执行当前语句，参与该文件整体逻辑。
- 82: `obj.cover_image = cover_image or obj.cover_image` -> 执行当前语句，参与该文件整体逻辑。
- 83: `obj.rating = rating or obj.rating` -> 执行当前语句，参与该文件整体逻辑。
- 84: `obj.ticket_price = ticket_price if ticket_price is not None else obj.ticket_price` -> 执行当前语句，参与该文件整体逻辑。
- 85: `obj.open_time = opening_hours or obj.open_time` -> 执行当前语句，参与该文件整体逻辑。
- 86: `updated += 1` -> 执行当前语句，参与该文件整体逻辑。
- 87: `(空行)` -> 空行，用于提升代码结构可读性。
- 88: `upserted += 1` -> 执行当前语句，参与该文件整体逻辑。
- 89: `(空行)` -> 空行，用于提升代码结构可读性。
- 90: `db.session.commit()` -> 执行当前语句，参与该文件整体逻辑。
- 91: `(空行)` -> 空行，用于提升代码结构可读性。
- 92: `print(f"import ok: total={len(raw)} upserted={upserted} created={created} updated={updated} skipped={skipped}")  # 总数变量print(f"import ok: total  # 总数变量print(f"import ok: total  # 总数变量print(f"import ok: total` -> 执行当前语句，参与该文件整体逻辑。
- 93: `(空行)` -> 空行，用于提升代码结构可读性。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `if __name__ == '__main__':` -> 条件判断分支，根据场景执行不同逻辑。
- 96: `main()` -> 执行当前语句，参与该文件整体逻辑。
- 97: `(空行)` -> 空行，用于提升代码结构可读性。
