# `frontend/user-web/src/app/profile/invite/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 邀请好友模块 - 邀请码与邀请统计` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 邀请码展示与复制` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 邀请链接展示与复制` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 邀请奖励积分展示` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 已邀请人数统计` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 总奖励积分统计` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 已邀请用户列表（用户名、注册时间）` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 登录状态校验` -> 注释行，用于解释设计意图或使用说明。
- 14: `*` -> 注释行，用于解释设计意图或使用说明。
- 15: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 18: `*` -> 注释行，用于解释设计意图或使用说明。
- 19: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - GET /api/users/invite: 获取邀请码和链接` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - GET /api/users/invite/stats: 获取邀请统计数据` -> 注释行，用于解释设计意图或使用说明。
- 22: `*` -> 注释行，用于解释设计意图或使用说明。
- 23: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 24: `* - useState: inviteData, stats, loading` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - useCallback: loadData` -> 注释行，用于解释设计意图或使用说明。
- 26: `*/` -> 注释行，用于解释设计意图或使用说明。
- 27: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 28: `(空行)` -> 空行，用于提升代码结构可读性。
- 29: `import { useCallback, useEffect, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 30: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 31: `import { Copy, Users, Gift, Link as LinkIcon, Loader2 } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 32: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 33: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 34: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 35: `(空行)` -> 空行，用于提升代码结构可读性。
- 36: `export default function InvitePage() {` -> 导出当前声明，供其他模块复用。
- 37: `const { isAuthenticated, user } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 38: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 39: `const [inviteData, setInviteData] = useState<{` -> 声明变量或常量，保存运行时数据。
- 40: `invite_code: string` -> 执行当前语句，参与该文件整体逻辑。
- 41: `invite_link: string` -> 执行当前语句，参与该文件整体逻辑。
- 42: `invited_count: number` -> 执行当前语句，参与该文件整体逻辑。
- 43: `reward_points: number` -> 执行当前语句，参与该文件整体逻辑。
- 44: `} | null>(null)` -> 执行当前语句，参与该文件整体逻辑。
- 45: `const [stats, setStats] = useState<{` -> 声明变量或常量，保存运行时数据。
- 46: `total_invited: number` -> 执行当前语句，参与该文件整体逻辑。
- 47: `total_reward_points: number` -> 执行当前语句，参与该文件整体逻辑。
- 48: `invited_users: Array<{ id: number; username: string; nickname: string; created_at: string }>` -> 执行当前语句，参与该文件整体逻辑。
- 49: `} | null>(null)` -> 执行当前语句，参与该文件整体逻辑。
- 50: `(空行)` -> 空行，用于提升代码结构可读性。
- 51: `const loadData = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 52: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 53: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 54: `return` -> 返回结果或提前结束当前流程。
- 55: `}` -> 结束当前语句或代码块。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `try {` -> 异常处理逻辑，提升程序健壮性。
- 58: `const token = localStorage.getItem('auth_token')` -> 声明变量或常量，保存运行时数据。
- 59: `const [infoRes, statsRes] = await Promise.all([` -> 声明变量或常量，保存运行时数据。
- 60: `fetch('/api/users/invite', {` -> 执行当前语句，参与该文件整体逻辑。
- 61: `headers: { Authorization: \`Bearer ${token}\` }` -> 执行当前语句，参与该文件整体逻辑。
- 62: `}),` -> 执行当前语句，参与该文件整体逻辑。
- 63: `fetch('/api/users/invite/stats', {` -> 执行当前语句，参与该文件整体逻辑。
- 64: `headers: { Authorization: \`Bearer ${token}\` }` -> 执行当前语句，参与该文件整体逻辑。
- 65: `})` -> 执行当前语句，参与该文件整体逻辑。
- 66: `])` -> 执行当前语句，参与该文件整体逻辑。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `const infoData = await infoRes.json()` -> 声明变量或常量，保存运行时数据。
- 69: `const statsData = await statsRes.json()` -> 声明变量或常量，保存运行时数据。
- 70: `(空行)` -> 空行，用于提升代码结构可读性。
- 71: `if (infoData.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 72: `setInviteData(infoData)` -> 执行当前语句，参与该文件整体逻辑。
- 73: `}` -> 结束当前语句或代码块。
- 74: `if (statsData.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 75: `setStats(statsData)` -> 执行当前语句，参与该文件整体逻辑。
- 76: `}` -> 结束当前语句或代码块。
- 77: `} catch (error) {` -> 执行当前语句，参与该文件整体逻辑。
- 78: `console.error('加载邀请数据失败:', error)` -> 执行当前语句，参与该文件整体逻辑。
- 79: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 80: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 81: `}` -> 结束当前语句或代码块。
- 82: `}, [isAuthenticated])` -> 执行当前语句，参与该文件整体逻辑。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 85: `loadData()` -> 执行当前语句，参与该文件整体逻辑。
- 86: `}, [loadData])` -> 执行当前语句，参与该文件整体逻辑。
- 87: `(空行)` -> 空行，用于提升代码结构可读性。
- 88: `const copyToClipboard = async (text: string) => {` -> 声明变量或常量，保存运行时数据。
- 89: `try {` -> 异常处理逻辑，提升程序健壮性。
- 90: `await navigator.clipboard.writeText(text)` -> 执行当前语句，参与该文件整体逻辑。
- 91: `toast.success('复制成功')` -> 执行当前语句，参与该文件整体逻辑。
- 92: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 93: `toast.error('复制失败')` -> 执行当前语句，参与该文件整体逻辑。
- 94: `}` -> 结束当前语句或代码块。
- 95: `}` -> 结束当前语句或代码块。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `if (loading) {` -> 条件判断分支，根据场景执行不同逻辑。
- 98: `return (` -> 返回结果或提前结束当前流程。
- 99: `<div className="min-h-screen">` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 101: `<div className="flex items-center justify-center pt-40">` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `<Loader2 className="h-8 w-8 animate-spin text-sky-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 104: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 105: `)` -> 结束当前语句或代码块。
- 106: `}` -> 结束当前语句或代码块。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 109: `return (` -> 返回结果或提前结束当前流程。
- 110: `<div className="min-h-screen">` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 112: `<div className="pt-24 pb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 113: `<div className="mx-auto max-w-md px-4 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 114: `<div className="rounded-3xl border border-white/60 bg-white/85 p-12 shadow-2xl backdrop-blur-md">` -> JSX/HTML 结构行，用于描述页面元素。
- 115: `<Users className="mx-auto h-16 w-16 text-sky-400 mb-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 116: `<h2 className="text-2xl font-bold text-gray-800 mb-4">请先登录</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `<p className="text-gray-600 mb-6">登录后即可邀请好友</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 121: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `)` -> 结束当前语句或代码块。
- 124: `}` -> 结束当前语句或代码块。
- 125: `(空行)` -> 空行，用于提升代码结构可读性。
- 126: `return (` -> 返回结果或提前结束当前流程。
- 127: `<div className="min-h-screen">` -> JSX/HTML 结构行，用于描述页面元素。
- 128: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `<main className="relative z-10 pt-24 pb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 130: `<div className="mx-auto max-w-2xl px-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `<div className="mb-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 132: `<h1 className="bg-gradient-to-r from-gray-900 via-sky-800 to-indigo-900 bg-clip-text text-4xl font-black tracking-tight text-transparent">` -> JSX/HTML 结构行，用于描述页面元素。
- 133: `邀请好友` -> 执行当前语句，参与该文件整体逻辑。
- 134: `</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 135: `<p className="mt-2 text-gray-600">邀请好友注册，双方各得 50 积分</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 136: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 137: `(空行)` -> 空行，用于提升代码结构可读性。
- 138: `{/* 邀请码卡片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 139: `<div className="rounded-3xl border border-white/60 bg-white/85 p-6 shadow-xl backdrop-blur-md mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 140: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 141: `<div className="rounded-full bg-sky-100 p-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 142: `<LinkIcon className="h-5 w-5 text-sky-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 143: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 144: `<h2 className="text-lg font-semibold text-gray-800">我的邀请码</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 145: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 146: `(空行)` -> 空行，用于提升代码结构可读性。
- 147: `{inviteData && (` -> 执行当前语句，参与该文件整体逻辑。
- 148: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 149: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 150: `<div className="flex-1 rounded-xl bg-gray-100 px-4 py-3 font-mono text-xl font-bold text-center tracking-wider">` -> JSX/HTML 结构行，用于描述页面元素。
- 151: `{inviteData.invite_code}` -> 执行当前语句，参与该文件整体逻辑。
- 152: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 153: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 154: `onClick={() => copyToClipboard(inviteData.invite_code)}` -> 执行当前语句，参与该文件整体逻辑。
- 155: `className="rounded-xl bg-sky-500 px-4 py-3 text-white hover:bg-sky-600"` -> 执行当前语句，参与该文件整体逻辑。
- 156: `>` -> 执行当前语句，参与该文件整体逻辑。
- 157: `<Copy className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 158: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 159: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `(空行)` -> 空行，用于提升代码结构可读性。
- 161: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 162: `<div className="text-sm text-gray-500 mb-1">邀请链接</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 163: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 164: `<div className="flex-1 rounded-xl bg-gray-100 px-3 py-2 text-sm text-gray-600 truncate">` -> JSX/HTML 结构行，用于描述页面元素。
- 165: `{inviteData.invite_link}` -> 执行当前语句，参与该文件整体逻辑。
- 166: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 168: `onClick={() => copyToClipboard(inviteData.invite_link)}` -> 执行当前语句，参与该文件整体逻辑。
- 169: `className="rounded-xl bg-sky-500 px-4 py-2 text-white hover:bg-sky-600 whitespace-nowrap"` -> 执行当前语句，参与该文件整体逻辑。
- 170: `>` -> 执行当前语句，参与该文件整体逻辑。
- 171: `复制链接` -> 执行当前语句，参与该文件整体逻辑。
- 172: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 173: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 175: `(空行)` -> 空行，用于提升代码结构可读性。
- 176: `<div className="flex items-center gap-2 text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `<Gift className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `<span>已邀请 <strong className="text-sky-600">{inviteData.invited_count}</strong> 人</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 179: `<span className="mx-2">|</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `<span>每次邀请得 <strong className="text-amber-600">{inviteData.reward_points}</strong> 积分</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 184: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 185: `(空行)` -> 空行，用于提升代码结构可读性。
- 186: `{/* 邀请统计 */}` -> 执行当前语句，参与该文件整体逻辑。
- 187: `<div className="rounded-3xl border border-white/60 bg-white/85 p-6 shadow-xl backdrop-blur-md">` -> JSX/HTML 结构行，用于描述页面元素。
- 188: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 189: `<div className="rounded-full bg-indigo-100 p-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 190: `<Users className="h-5 w-5 text-indigo-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 191: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 192: `<h2 className="text-lg font-semibold text-gray-800">邀请统计</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 193: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 194: `(空行)` -> 空行，用于提升代码结构可读性。
- 195: `{stats && (` -> 执行当前语句，参与该文件整体逻辑。
- 196: `<div className="grid grid-cols-2 gap-4 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 197: `<div className="rounded-xl bg-sky-50 p-4 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 198: `<div className="text-3xl font-bold text-sky-600">{stats.total_invited}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 199: `<div className="text-sm text-gray-600">已邀请人数</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 200: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 201: `<div className="rounded-xl bg-amber-50 p-4 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 202: `<div className="text-3xl font-bold text-amber-600">{stats.total_reward_points}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 203: `<div className="text-sm text-gray-600">累计获得积分</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 204: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 205: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 206: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 207: `(空行)` -> 空行，用于提升代码结构可读性。
- 208: `{/* 最近邀请的用户 */}` -> 执行当前语句，参与该文件整体逻辑。
- 209: `{stats && stats.invited_users && stats.invited_users.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 210: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 211: `<h3 className="text-sm font-semibold text-gray-700 mb-3">最近邀请</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 212: `<div className="space-y-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 213: `{stats.invited_users.map((u) => (` -> 执行当前语句，参与该文件整体逻辑。
- 214: `<div key={u.id} className="flex items-center justify-between rounded-lg bg-gray-50 px-4 py-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 215: `<span className="font-medium text-gray-800">{u.nickname || u.username}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 216: `<span className="text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 217: `{u.created_at?.split('T')[0]}` -> 执行当前语句，参与该文件整体逻辑。
- 218: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 219: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 220: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 221: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 222: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 223: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 224: `(空行)` -> 空行，用于提升代码结构可读性。
- 225: `{(!stats || stats.total_invited === 0) && (` -> 执行当前语句，参与该文件整体逻辑。
- 226: `<div className="text-center py-8 text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `还没有邀请过好友，快去邀请吧！` -> 执行当前语句，参与该文件整体逻辑。
- 228: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 229: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 230: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 232: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 233: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `)` -> 结束当前语句或代码块。
- 236: `}` -> 结束当前语句或代码块。
