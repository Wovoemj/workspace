# `backend/user-service/main.go` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `package user`
  - 作用：声明包名。
  - 原因：通过包边界组织可见性与编译单元。
- 2: `import (`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 3: `"context"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `"database/sql"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"encoding/json"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `"fmt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `"net/http"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `"time"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `"github.com/gin-gonic/gin"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `"github.com/sirupsen/logrus"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `"golang.org/x/crypto/bcrypt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `"gorm.io/gorm"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 14: `type Service struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 15: `db     *gorm.DB`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `logger *logrus.Logger`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 17: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 18: `type User struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 19: `ID            uint      \`json:"id" gorm:"primaryKey"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `Username      string    \`json:"username" gorm:"unique"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `Phone         string    \`json:"phone" gorm:"unique"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 22: `Email         string    \`json:"email"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `Nickname      string    \`json:"nickname"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `Password      string    \`json:"-"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `AvatarURL     string    \`json:"avatar_url"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `MembershipLevel int      \`json:"membership_level"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 27: `IsAdmin       bool      \`json:"is_admin" gorm:"column:is_admin"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `Preferences   UserPreferences \`json:"preferences" gorm:"type:jsonb"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `CreatedAt     time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `UpdatedAt     time.Time \`json:"updated_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 32: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 33: `type UserPreferences struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 34: `Destinations []string \`json:"destinations"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `BudgetRange  BudgetRange \`json:"budget_range"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `TravelStyle  string   \`json:"travel_style"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `GroupSize    int      \`json:"group_size"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `Interests    []string \`json:"interests"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 40: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 41: `type BudgetRange struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 42: `Min float64 \`json:"min"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `Max float64 \`json:"max"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 45: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 46: `type RegisterRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 47: `Phone    string \`json:"phone" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `Password string \`json:"password" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `Nickname string \`json:"nickname"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 51: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 52: `type LoginRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 53: `Phone    string \`json:"phone"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `Username string \`json:"username"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `Password string \`json:"password" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 57: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 58: `type UpdateProfileRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 59: `Nickname  string  \`json:"nickname"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `AvatarURL string  \`json:"avatar_url"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `Level     *int    \`json:"membership_level"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 63: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 64: `type UpdatePreferencesRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 65: `Destinations []string      \`json:"destinations"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `BudgetRange  *BudgetRange \`json:"budget_range"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `TravelStyle  *string      \`json:"travel_style"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `GroupSize    *int         \`json:"group_size"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `Interests    []string     \`json:"interests"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 71: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 72: `type Favorite struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 73: `ID             uint      \`json:"id" gorm:"primaryKey"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `UserID         uint      \`json:"user_id" gorm:"not null;index"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `DestinationID  uint      \`json:"destination_id" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `DestinationName string   \`json:"destination_name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `City           string    \`json:"city"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 78: `Province       string    \`json:"province"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `CoverImage     string    \`json:"cover_image"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 80: `CreatedAt      time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 82: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 83: `type AddFavoriteRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 84: `DestinationID uint   \`json:"destination_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `Name          string \`json:"name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `City          string \`json:"city"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `Province      string \`json:"province"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `CoverImage    string \`json:"cover_image"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 90: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 91: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 92: `return &Service{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 93: `db:     db,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `logger: logger,`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 95: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 96: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 97: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 98: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 99: `users := router.Group("/users")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 100: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `users.POST("/register", s.Register)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 102: `users.POST("/login", s.Login)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 103: `users.GET("/profile", s.GetProfile)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 104: `users.PUT("/profile", s.UpdateProfile)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 105: `users.GET("/preferences", s.GetPreferences)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 106: `users.PUT("/preferences", s.UpdatePreferences)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 107: `users.GET("/:id", s.GetUserByID)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 108: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 109: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 110: `// Favorites routes`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 111: `favorites := router.Group("/favorites")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 112: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `favorites.GET("", s.GetFavorites)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 114: `favorites.POST("", s.AddFavorite)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 115: `favorites.DELETE("/:id", s.RemoveFavorite)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 116: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 117: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 118: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 119: `func (s *Service) Register(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 120: `var req RegisterRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 121: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 122: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 123: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 125: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 126: `// Check if user already exists`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 127: `var existingUser User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 128: `if err := s.db.Where("phone = ?", req.Phone).First(&existingUser).Error; err == nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 129: `c.JSON(http.StatusConflict, gin.H{"error": "Phone already registered"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 130: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 132: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 133: `// Hash password`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 134: `hashedPassword, err := bcrypt.GenerateFromPassword([]byte(req.Password), bcrypt.DefaultCost)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 135: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 136: `s.logger.WithError(err).Error("Failed to hash password")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 137: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to register user"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 138: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 140: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 141: `// Create user`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 142: `user := User{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 143: `Phone:         req.Phone,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `Password:      string(hashedPassword),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `Nickname:      req.Nickname,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 146: `MembershipLevel: 1, // Default membership level`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 147: `Preferences: UserPreferences{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `TravelStyle: "relaxation",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `GroupSize:   2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 151: `CreatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 152: `UpdatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 153: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 154: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 155: `if err := s.db.Create(&user).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 156: `s.logger.WithError(err).Error("Failed to create user")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 157: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to register user"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 158: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 160: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 161: `s.logger.WithField("user_id", user.ID).Info("User registered successfully")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 162: `c.JSON(http.StatusCreated, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 163: `"message": "User registered successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `"user":    user,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 165: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 167: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 168: `func (s *Service) Login(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 169: `var req LoginRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 170: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 171: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 172: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 174: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 175: `// Find user by phone or username`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 176: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 177: `query := s.db.Where("1=1")`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 178: `if req.Phone != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 179: `query = query.Where("phone = ?", req.Phone)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 180: `} else if req.Username != "" {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 181: `query = query.Where("username = ?", req.Username)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 182: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 183: `c.JSON(http.StatusBadRequest, gin.H{"error": "Phone or username is required"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 184: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 186: `if err := query.First(&user).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 187: `c.JSON(http.StatusUnauthorized, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 188: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 190: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 191: `// Check password`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 192: `if err := bcrypt.CompareHashAndPassword([]byte(user.Password), []byte(req.Password)); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 193: `c.JSON(http.StatusUnauthorized, gin.H{"error": "Invalid password"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 194: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 196: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 197: `// Generate token (simplified - in production use JWT)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 198: `token := fmt.Sprintf("token_%d_%d", user.ID, time.Now().Unix())`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 199: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 200: `s.logger.WithField("user_id", user.ID).Info("User logged in successfully")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 201: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 202: `"success": true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 203: `"message": "Login successful",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 204: `"token":   token,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 205: `"user":    user,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 206: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 207: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 208: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 209: `func (s *Service) GetProfile(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 210: `// Get user ID from token (simplified)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 211: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 213: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 214: `if err := s.db.First(&user, userID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 215: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 216: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 217: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 218: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 219: `c.JSON(http.StatusOK, user)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 220: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 221: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 222: `func (s *Service) UpdateProfile(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 223: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 225: `var req UpdateProfileRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 226: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 227: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 228: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 229: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 230: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 231: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 232: `if err := s.db.First(&user, userID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 233: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 234: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 235: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 236: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 237: `// Update fields`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 238: `if req.Nickname != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 239: `user.Nickname = req.Nickname`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 240: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 241: `if req.AvatarURL != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 242: `user.AvatarURL = req.AvatarURL`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 243: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 244: `if req.Level != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 245: `user.MembershipLevel = *req.Level`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 247: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 248: `user.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 249: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 250: `if err := s.db.Save(&user).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 251: `s.logger.WithError(err).Error("Failed to update user profile")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 252: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update profile"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 253: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 254: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 255: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 256: `s.logger.WithField("user_id", user.ID).Info("User profile updated")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 257: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 258: `"message": "Profile updated successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 259: `"user":    user,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 260: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 261: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 262: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 263: `func (s *Service) GetPreferences(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 264: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 265: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 266: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 267: `if err := s.db.First(&user, userID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 268: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 269: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 270: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 271: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 272: `c.JSON(http.StatusOK, user.Preferences)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 273: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 274: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 275: `func (s *Service) UpdatePreferences(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 276: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 277: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 278: `var req UpdatePreferencesRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 279: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 280: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 281: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 282: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 283: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 284: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 285: `if err := s.db.First(&user, userID).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 286: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 287: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 288: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 289: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 290: `// Update preferences`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 291: `if req.Destinations != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 292: `user.Preferences.Destinations = req.Destinations`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 293: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 294: `if req.BudgetRange != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 295: `user.Preferences.BudgetRange = *req.BudgetRange`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 296: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 297: `if req.TravelStyle != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 298: `user.Preferences.TravelStyle = *req.TravelStyle`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 299: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 300: `if req.GroupSize != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 301: `user.Preferences.GroupSize = *req.GroupSize`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 302: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 303: `if req.Interests != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 304: `user.Preferences.Interests = req.Interests`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 305: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 306: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 307: `user.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 308: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 309: `if err := s.db.Save(&user).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 310: `s.logger.WithError(err).Error("Failed to update user preferences")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 311: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update preferences"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 312: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 313: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 314: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 315: `s.logger.WithField("user_id", user.ID).Info("User preferences updated")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 316: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 317: `"message": "Preferences updated successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 318: `"preferences": user.Preferences,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 319: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 320: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 321: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 322: `func (s *Service) GetUserByID(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 323: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 324: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 325: `var user User`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 326: `if err := s.db.First(&user, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 327: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 328: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 329: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 330: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 331: `c.JSON(http.StatusOK, user)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 332: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 333: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 334: `func (s *Service) GetFavorites(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 335: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 336: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 337: `var favorites []Favorite`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 338: `if err := s.db.Where("user_id = ?", userID).Order("created_at DESC").Find(&favorites).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 339: `s.logger.WithError(err).Error("Failed to get favorites")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 340: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get favorites"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 341: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 342: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 343: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 344: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 345: `"success":      true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 346: `"destinations": favorites,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 347: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 348: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 349: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 350: `func (s *Service) AddFavorite(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 351: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 352: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 353: `var req AddFavoriteRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 354: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 355: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 356: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 357: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 358: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 359: `// Check if already favorited`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 360: `var existing Favorite`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 361: `if err := s.db.Where("user_id = ? AND destination_id = ?", userID, req.DestinationID).First(&existing).Error; err == nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 362: `c.JSON(http.StatusOK, gin.H{"success": true, "message": "Already favorited"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 363: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 364: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 365: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 366: `favorite := Favorite{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 367: `UserID:          userID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 368: `DestinationID:   req.DestinationID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 369: `DestinationName: req.Name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 370: `City:            req.City,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 371: `Province:        req.Province,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 372: `CoverImage:      req.CoverImage,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 373: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 374: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 375: `if err := s.db.Create(&favorite).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 376: `s.logger.WithError(err).Error("Failed to add favorite")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 377: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to add favorite"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 378: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 379: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 380: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 381: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 382: `"success": true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 383: `"message": "Added to favorites",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 385: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 386: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 387: `func (s *Service) RemoveFavorite(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 388: `userID := c.GetUint("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 389: `favoriteID := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 390: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 391: `var favorite Favorite`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 392: `if err := s.db.Where("id = ? AND user_id = ?", favoriteID, userID).First(&favorite).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 393: `c.JSON(http.StatusNotFound, gin.H{"error": "Favorite not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 394: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 395: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 396: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 397: `if err := s.db.Delete(&favorite).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 398: `s.logger.WithError(err).Error("Failed to remove favorite")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 399: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to remove favorite"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 400: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 401: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 402: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 403: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 404: `"success": true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 405: `"message": "Removed from favorites",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 406: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 407: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
