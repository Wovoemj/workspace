# `frontend/user-web/tailwind.config.js` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/** @type {import('tailwindcss').Config} */` -> 注释行，用于解释设计意图或使用说明。
- 2: `module.exports = {` -> 执行当前语句，参与该文件整体逻辑。
- 3: `darkMode: 'class',` -> 执行当前语句，参与该文件整体逻辑。
- 4: `content: [` -> 执行当前语句，参与该文件整体逻辑。
- 5: `'./src/pages/**/*.{js,ts,jsx,tsx,mdx}',` -> 执行当前语句，参与该文件整体逻辑。
- 6: `'./src/components/**/*.{js,ts,jsx,tsx,mdx}',` -> 执行当前语句，参与该文件整体逻辑。
- 7: `'./src/app/**/*.{js,ts,jsx,tsx,mdx}',` -> 执行当前语句，参与该文件整体逻辑。
- 8: `],` -> 执行当前语句，参与该文件整体逻辑。
- 9: `theme: {` -> 执行当前语句，参与该文件整体逻辑。
- 10: `extend: {` -> 执行当前语句，参与该文件整体逻辑。
- 11: `colors: {` -> 执行当前语句，参与该文件整体逻辑。
- 12: `// 设计规范 v1.0 - 色彩系统` -> 注释行，用于解释设计意图或使用说明。
- 13: `border: 'hsl(var(--border))',` -> 执行当前语句，参与该文件整体逻辑。
- 14: `input: 'hsl(var(--input))',` -> 执行当前语句，参与该文件整体逻辑。
- 15: `ring: 'hsl(var(--ring))',` -> 执行当前语句，参与该文件整体逻辑。
- 16: `background: 'hsl(var(--background))',` -> 执行当前语句，参与该文件整体逻辑。
- 17: `foreground: 'hsl(var(--foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 18: `(空行)` -> 空行，用于提升代码结构可读性。
- 19: `// 主色板` -> 注释行，用于解释设计意图或使用说明。
- 20: `primary: {` -> 执行当前语句，参与该文件整体逻辑。
- 21: `DEFAULT: '#1E40AF', // 深蓝 - 按钮/强调` -> 执行当前语句，参与该文件整体逻辑。
- 22: `foreground: '#FFFFFF',` -> 执行当前语句，参与该文件整体逻辑。
- 23: `50: '#EFF6FF',` -> 执行当前语句，参与该文件整体逻辑。
- 24: `100: '#DBEAFE',` -> 执行当前语句，参与该文件整体逻辑。
- 25: `200: '#BFDBFE',` -> 执行当前语句，参与该文件整体逻辑。
- 26: `300: '#93C5FD',` -> 执行当前语句，参与该文件整体逻辑。
- 27: `400: '#60A5FA',` -> 执行当前语句，参与该文件整体逻辑。
- 28: `500: '#3B82F6',` -> 执行当前语句，参与该文件整体逻辑。
- 29: `600: '#2563EB',` -> 执行当前语句，参与该文件整体逻辑。
- 30: `700: '#1D4ED8',` -> 执行当前语句，参与该文件整体逻辑。
- 31: `800: '#1E40AF',` -> 执行当前语句，参与该文件整体逻辑。
- 32: `900: '#1E3A8A',` -> 执行当前语句，参与该文件整体逻辑。
- 33: `},` -> 执行当前语句，参与该文件整体逻辑。
- 34: `secondary: {` -> 执行当前语句，参与该文件整体逻辑。
- 35: `DEFAULT: '#0EA5E9', // 天蓝 - 链接/标签` -> 执行当前语句，参与该文件整体逻辑。
- 36: `foreground: '#FFFFFF',` -> 执行当前语句，参与该文件整体逻辑。
- 37: `50: '#F0F9FF',` -> 执行当前语句，参与该文件整体逻辑。
- 38: `100: '#E0F2FE',` -> 执行当前语句，参与该文件整体逻辑。
- 39: `200: '#BAE6FD',` -> 执行当前语句，参与该文件整体逻辑。
- 40: `300: '#7DD3FC',` -> 执行当前语句，参与该文件整体逻辑。
- 41: `400: '#38BDF8',` -> 执行当前语句，参与该文件整体逻辑。
- 42: `500: '#0EA5E9',` -> 执行当前语句，参与该文件整体逻辑。
- 43: `600: '#0284C7',` -> 执行当前语句，参与该文件整体逻辑。
- 44: `700: '#0369A1',` -> 执行当前语句，参与该文件整体逻辑。
- 45: `800: '#075985',` -> 执行当前语句，参与该文件整体逻辑。
- 46: `900: '#0C4A6E',` -> 执行当前语句，参与该文件整体逻辑。
- 47: `},` -> 执行当前语句，参与该文件整体逻辑。
- 48: `success: {` -> 执行当前语句，参与该文件整体逻辑。
- 49: `DEFAULT: '#10B981', // 翠绿 - 可用/完成` -> 执行当前语句，参与该文件整体逻辑。
- 50: `50: '#ECFDF5',` -> 执行当前语句，参与该文件整体逻辑。
- 51: `100: '#D1FAE5',` -> 执行当前语句，参与该文件整体逻辑。
- 52: `500: '#10B981',` -> 执行当前语句，参与该文件整体逻辑。
- 53: `600: '#059669',` -> 执行当前语句，参与该文件整体逻辑。
- 54: `},` -> 执行当前语句，参与该文件整体逻辑。
- 55: `warning: {` -> 执行当前语句，参与该文件整体逻辑。
- 56: `DEFAULT: '#F59E0B', // 琥珀 - 提示/限量` -> 执行当前语句，参与该文件整体逻辑。
- 57: `50: '#FFFBEB',` -> 执行当前语句，参与该文件整体逻辑。
- 58: `100: '#FEF3C7',` -> 执行当前语句，参与该文件整体逻辑。
- 59: `500: '#F59E0B',` -> 执行当前语句，参与该文件整体逻辑。
- 60: `600: '#D97706',` -> 执行当前语句，参与该文件整体逻辑。
- 61: `},` -> 执行当前语句，参与该文件整体逻辑。
- 62: `error: {` -> 执行当前语句，参与该文件整体逻辑。
- 63: `DEFAULT: '#EF4444', // 正红 - 错误/售罄` -> 执行当前语句，参与该文件整体逻辑。
- 64: `50: '#FEF2F2',` -> 执行当前语句，参与该文件整体逻辑。
- 65: `100: '#FEE2E2',` -> 执行当前语句，参与该文件整体逻辑。
- 66: `500: '#EF4444',` -> 执行当前语句，参与该文件整体逻辑。
- 67: `600: '#DC2626',` -> 执行当前语句，参与该文件整体逻辑。
- 68: `},` -> 执行当前语句，参与该文件整体逻辑。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `// 灰度色阶` -> 注释行，用于解释设计意图或使用说明。
- 71: `gray: {` -> 执行当前语句，参与该文件整体逻辑。
- 72: `50: '#F8FAFC',` -> 执行当前语句，参与该文件整体逻辑。
- 73: `100: '#F1F5F9',` -> 执行当前语句，参与该文件整体逻辑。
- 74: `200: '#E2E8F0',` -> 执行当前语句，参与该文件整体逻辑。
- 75: `300: '#CBD5E1',` -> 执行当前语句，参与该文件整体逻辑。
- 76: `400: '#94A3B8',` -> 执行当前语句，参与该文件整体逻辑。
- 77: `500: '#64748B', // 中性色 - 正文/图标` -> 执行当前语句，参与该文件整体逻辑。
- 78: `600: '#475569',` -> 执行当前语句，参与该文件整体逻辑。
- 79: `700: '#334155',` -> 执行当前语句，参与该文件整体逻辑。
- 80: `800: '#1E293B',` -> 执行当前语句，参与该文件整体逻辑。
- 81: `900: '#0F172A',` -> 执行当前语句，参与该文件整体逻辑。
- 82: `},` -> 执行当前语句，参与该文件整体逻辑。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `// 保留原有语义化颜色` -> 注释行，用于解释设计意图或使用说明。
- 85: `destructive: {` -> 执行当前语句，参与该文件整体逻辑。
- 86: `DEFAULT: 'hsl(var(--destructive))',` -> 执行当前语句，参与该文件整体逻辑。
- 87: `foreground: 'hsl(var(--destructive-foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 88: `},` -> 执行当前语句，参与该文件整体逻辑。
- 89: `muted: {` -> 执行当前语句，参与该文件整体逻辑。
- 90: `DEFAULT: 'hsl(var(--muted))',` -> 执行当前语句，参与该文件整体逻辑。
- 91: `foreground: 'hsl(var(--muted-foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 92: `},` -> 执行当前语句，参与该文件整体逻辑。
- 93: `accent: {` -> 执行当前语句，参与该文件整体逻辑。
- 94: `DEFAULT: 'hsl(var(--accent))',` -> 执行当前语句，参与该文件整体逻辑。
- 95: `foreground: 'hsl(var(--accent-foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 96: `},` -> 执行当前语句，参与该文件整体逻辑。
- 97: `popover: {` -> 执行当前语句，参与该文件整体逻辑。
- 98: `DEFAULT: 'hsl(var(--popover))',` -> 执行当前语句，参与该文件整体逻辑。
- 99: `foreground: 'hsl(var(--popover-foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 100: `},` -> 执行当前语句，参与该文件整体逻辑。
- 101: `card: {` -> 执行当前语句，参与该文件整体逻辑。
- 102: `DEFAULT: 'hsl(var(--card))',` -> 执行当前语句，参与该文件整体逻辑。
- 103: `foreground: 'hsl(var(--card-foreground))',` -> 执行当前语句，参与该文件整体逻辑。
- 104: `},` -> 执行当前语句，参与该文件整体逻辑。
- 105: `},` -> 执行当前语句，参与该文件整体逻辑。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `// 字体系统` -> 注释行，用于解释设计意图或使用说明。
- 108: `fontFamily: {` -> 执行当前语句，参与该文件整体逻辑。
- 109: `sans: [` -> 执行当前语句，参与该文件整体逻辑。
- 110: `'-apple-system',` -> 执行当前语句，参与该文件整体逻辑。
- 111: `'BlinkMacSystemFont',` -> 执行当前语句，参与该文件整体逻辑。
- 112: `'"Segoe UI"',` -> 执行当前语句，参与该文件整体逻辑。
- 113: `'Roboto',` -> 执行当前语句，参与该文件整体逻辑。
- 114: `'"PingFang SC"',` -> 执行当前语句，参与该文件整体逻辑。
- 115: `'"Microsoft YaHei"',` -> 执行当前语句，参与该文件整体逻辑。
- 116: `'sans-serif',` -> 执行当前语句，参与该文件整体逻辑。
- 117: `],` -> 执行当前语句，参与该文件整体逻辑。
- 118: `},` -> 执行当前语句，参与该文件整体逻辑。
- 119: `fontSize: {` -> 执行当前语句，参与该文件整体逻辑。
- 120: `// 设计规范字号` -> 注释行，用于解释设计意图或使用说明。
- 121: `'display': ['48px', { lineHeight: '1.1', fontWeight: '700' }],` -> 执行当前语句，参与该文件整体逻辑。
- 122: `'h1': ['32px', { lineHeight: '1.2', fontWeight: '700' }],` -> 执行当前语句，参与该文件整体逻辑。
- 123: `'h2': ['24px', { lineHeight: '1.3', fontWeight: '600' }],` -> 执行当前语句，参与该文件整体逻辑。
- 124: `'h3': ['18px', { lineHeight: '1.4', fontWeight: '600' }],` -> 执行当前语句，参与该文件整体逻辑。
- 125: `'body': ['16px', { lineHeight: '1.5', fontWeight: '400' }],` -> 执行当前语句，参与该文件整体逻辑。
- 126: `'small': ['14px', { lineHeight: '1.5', fontWeight: '400' }],` -> 执行当前语句，参与该文件整体逻辑。
- 127: `'tiny': ['12px', { lineHeight: '1.4', fontWeight: '400' }],` -> 执行当前语句，参与该文件整体逻辑。
- 128: `},` -> 执行当前语句，参与该文件整体逻辑。
- 129: `(空行)` -> 空行，用于提升代码结构可读性。
- 130: `// 间距系统（8px基准）` -> 注释行，用于解释设计意图或使用说明。
- 131: `spacing: {` -> 执行当前语句，参与该文件整体逻辑。
- 132: `'1': '4px',` -> 执行当前语句，参与该文件整体逻辑。
- 133: `'2': '8px',` -> 执行当前语句，参与该文件整体逻辑。
- 134: `'3': '12px',` -> 执行当前语句，参与该文件整体逻辑。
- 135: `'4': '16px',` -> 执行当前语句，参与该文件整体逻辑。
- 136: `'5': '20px',` -> 执行当前语句，参与该文件整体逻辑。
- 137: `'6': '24px',` -> 执行当前语句，参与该文件整体逻辑。
- 138: `'8': '32px',` -> 执行当前语句，参与该文件整体逻辑。
- 139: `'10': '40px',` -> 执行当前语句，参与该文件整体逻辑。
- 140: `'12': '48px',` -> 执行当前语句，参与该文件整体逻辑。
- 141: `'16': '64px',` -> 执行当前语句，参与该文件整体逻辑。
- 142: `},` -> 执行当前语句，参与该文件整体逻辑。
- 143: `(空行)` -> 空行，用于提升代码结构可读性。
- 144: `// 圆角系统` -> 注释行，用于解释设计意图或使用说明。
- 145: `borderRadius: {` -> 执行当前语句，参与该文件整体逻辑。
- 146: `'sm': '4px',` -> 执行当前语句，参与该文件整体逻辑。
- 147: `'md': '8px',` -> 执行当前语句，参与该文件整体逻辑。
- 148: `'lg': '12px',` -> 执行当前语句，参与该文件整体逻辑。
- 149: `'xl': '16px',` -> 执行当前语句，参与该文件整体逻辑。
- 150: `'2xl': '16px',` -> 执行当前语句，参与该文件整体逻辑。
- 151: `'full': '9999px',` -> 执行当前语句，参与该文件整体逻辑。
- 152: `},` -> 执行当前语句，参与该文件整体逻辑。
- 153: `(空行)` -> 空行，用于提升代码结构可读性。
- 154: `// 阴影系统` -> 注释行，用于解释设计意图或使用说明。
- 155: `boxShadow: {` -> 执行当前语句，参与该文件整体逻辑。
- 156: `'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',` -> 执行当前语句，参与该文件整体逻辑。
- 157: `'DEFAULT': '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)',` -> 执行当前语句，参与该文件整体逻辑。
- 158: `'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',` -> 执行当前语句，参与该文件整体逻辑。
- 159: `'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',` -> 执行当前语句，参与该文件整体逻辑。
- 160: `'xl': '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)',` -> 执行当前语句，参与该文件整体逻辑。
- 161: `'card': '0 2px 8px rgba(0, 0, 0, 0.06)',` -> 执行当前语句，参与该文件整体逻辑。
- 162: `'card-hover': '0 12px 24px rgba(0, 0, 0, 0.12)',` -> 执行当前语句，参与该文件整体逻辑。
- 163: `'search': '0 4px 20px rgba(0, 0, 0, 0.08)',` -> 执行当前语句，参与该文件整体逻辑。
- 164: `},` -> 执行当前语句，参与该文件整体逻辑。
- 165: `(空行)` -> 空行，用于提升代码结构可读性。
- 166: `// 动效系统` -> 注释行，用于解释设计意图或使用说明。
- 167: `transitionTimingFunction: {` -> 执行当前语句，参与该文件整体逻辑。
- 168: `'ease-out': 'cubic-bezier(0, 0, 0.2, 1)',` -> 执行当前语句，参与该文件整体逻辑。
- 169: `'ease-in-out': 'cubic-bezier(0.4, 0, 0.2, 1)',` -> 执行当前语句，参与该文件整体逻辑。
- 170: `'spring': 'cubic-bezier(0.34, 1.56, 0.64, 1)',` -> 执行当前语句，参与该文件整体逻辑。
- 171: `},` -> 执行当前语句，参与该文件整体逻辑。
- 172: `transitionDuration: {` -> 执行当前语句，参与该文件整体逻辑。
- 173: `'150': '150ms',` -> 执行当前语句，参与该文件整体逻辑。
- 174: `'200': '200ms',` -> 执行当前语句，参与该文件整体逻辑。
- 175: `'300': '300ms',` -> 执行当前语句，参与该文件整体逻辑。
- 176: `'400': '400ms',` -> 执行当前语句，参与该文件整体逻辑。
- 177: `'500': '500ms',` -> 执行当前语句，参与该文件整体逻辑。
- 178: `},` -> 执行当前语句，参与该文件整体逻辑。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `animation: {` -> 执行当前语句，参与该文件整体逻辑。
- 181: `'fade-in': 'fadeIn 0.5s ease-in-out',` -> 执行当前语句，参与该文件整体逻辑。
- 182: `'slide-up': 'slideUp 0.3s ease-out',` -> 执行当前语句，参与该文件整体逻辑。
- 183: `'slide-down': 'slideDown 0.2s ease-out',` -> 执行当前语句，参与该文件整体逻辑。
- 184: `'shimmer': 'shimmer 1.5s infinite',` -> 执行当前语句，参与该文件整体逻辑。
- 185: `},` -> 执行当前语句，参与该文件整体逻辑。
- 186: `keyframes: {` -> 执行当前语句，参与该文件整体逻辑。
- 187: `fadeIn: {` -> 执行当前语句，参与该文件整体逻辑。
- 188: `'0%': { opacity: '0' },` -> 执行当前语句，参与该文件整体逻辑。
- 189: `'100%': { opacity: '1' },` -> 执行当前语句，参与该文件整体逻辑。
- 190: `},` -> 执行当前语句，参与该文件整体逻辑。
- 191: `slideUp: {` -> 执行当前语句，参与该文件整体逻辑。
- 192: `'0%': { transform: 'translateY(10px)', opacity: '0' },` -> 执行当前语句，参与该文件整体逻辑。
- 193: `'100%': { transform: 'translateY(0)', opacity: '1' },` -> 执行当前语句，参与该文件整体逻辑。
- 194: `},` -> 执行当前语句，参与该文件整体逻辑。
- 195: `slideDown: {` -> 执行当前语句，参与该文件整体逻辑。
- 196: `'0%': { opacity: '0', transform: 'translateY(-8px)' },` -> 执行当前语句，参与该文件整体逻辑。
- 197: `'100%': { opacity: '1', transform: 'translateY(0)' },` -> 执行当前语句，参与该文件整体逻辑。
- 198: `},` -> 执行当前语句，参与该文件整体逻辑。
- 199: `shimmer: {` -> 执行当前语句，参与该文件整体逻辑。
- 200: `'0%': { backgroundPosition: '-200% 0' },` -> 执行当前语句，参与该文件整体逻辑。
- 201: `'100%': { backgroundPosition: '200% 0' },` -> 执行当前语句，参与该文件整体逻辑。
- 202: `},` -> 执行当前语句，参与该文件整体逻辑。
- 203: `},` -> 执行当前语句，参与该文件整体逻辑。
- 204: `(空行)` -> 空行，用于提升代码结构可读性。
- 205: `// 响应式断点` -> 注释行，用于解释设计意图或使用说明。
- 206: `screens: {` -> 执行当前语句，参与该文件整体逻辑。
- 207: `'sm': '640px',` -> 执行当前语句，参与该文件整体逻辑。
- 208: `'md': '768px',` -> 执行当前语句，参与该文件整体逻辑。
- 209: `'lg': '1024px',` -> 执行当前语句，参与该文件整体逻辑。
- 210: `'xl': '1280px',` -> 执行当前语句，参与该文件整体逻辑。
- 211: `'2xl': '1536px',` -> 执行当前语句，参与该文件整体逻辑。
- 212: `},` -> 执行当前语句，参与该文件整体逻辑。
- 213: `},` -> 执行当前语句，参与该文件整体逻辑。
- 214: `},` -> 执行当前语句，参与该文件整体逻辑。
- 215: `plugins: [` -> 执行当前语句，参与该文件整体逻辑。
- 216: `require('@tailwindcss/typography'),` -> 执行当前语句，参与该文件整体逻辑。
- 217: `],` -> 执行当前语句，参与该文件整体逻辑。
- 218: `}` -> 结束当前语句或代码块。
