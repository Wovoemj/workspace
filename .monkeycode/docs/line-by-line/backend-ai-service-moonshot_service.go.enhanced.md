# `backend/ai-service/moonshot_service.go` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `package main`
  - 作用：声明包名。
  - 原因：通过包边界组织可见性与编译单元。
- 2: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 3: `import (`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 4: `"bytes"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"context"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `"encoding/json"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `"fmt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `"io"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `"net/http"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `"strings"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `"time"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 13: `"github.com/gin-gonic/gin"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `"github.com/redis/go-redis/v9"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 15: `"github.com/sirupsen/logrus"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 17: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 18: `// ============== AI 模型配置 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 19: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 20: `const (`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 21: `// 默认使用 MiMo`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 22: `DefaultAIProvider = "mimo" // "mimo" 或 "kimi"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 24: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 25: `var AIProviders = map[string]struct {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 26: `BaseURL string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 27: `Model   string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `}{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `"mimo": {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `BaseURL: "https://token-plan-cn.xiaomimimo.com/v1",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `Model:   "MiMo-V2.5",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `"kimi": {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `BaseURL: "https://api.moonshot.cn/v1",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `Model:   "kimi-k2.5",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 38: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 39: `// AIModelOverride 环境变量覆盖模型名`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 40: `var AIModelOverride = ""`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 41: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 42: `// Moonshot 请求结构`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 43: `type MoonshotMessage struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 44: `Role    string \`json:"role"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `Content string \`json:"content"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `type MoonshotRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 49: `Model       string            \`json:"model"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `Messages    []MoonshotMessage \`json:"messages"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `Temperature float64           \`json:"temperature"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `MaxTokens   int               \`json:"max_tokens"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `Stream      bool              \`json:"stream"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 55: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 56: `type MoonshotResponse struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 57: `ID      string \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 58: `Choices []struct {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 59: `Message struct {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `Role    string \`json:"role"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `Content string \`json:"content"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `} \`json:"message"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `} \`json:"choices"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `Usage struct {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `PromptTokens     int \`json:"prompt_tokens"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `CompletionTokens int \`json:"completion_tokens"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `TotalTokens      int \`json:"total_tokens"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `} \`json:"usage"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `// ============== 服务结构 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 72: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 73: `type Service struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 74: `logger      *logrus.Logger`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 75: `redis       *redis.Client`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `aiProvider  string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `aiAPI       string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 78: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 79: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 80: `type Conversation struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 81: `ID        string    \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `UserID    string    \`json:"user_id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `SessionID string    \`json:"session_id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `Role      string    \`json:"role"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `Content   string    \`json:"content"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `Intent    string    \`json:"intent"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `CreatedAt time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 89: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 90: `type MessageRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 91: `Message   string \`json:"message" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 92: `SessionID string \`json:"session_id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `SystemPrompt string \`json:"system_prompt"\` // 自定义系统提示词`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 95: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 96: `type IntentResponse struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 97: `Intent     string  \`json:"intent"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `Confidence float64 \`json:"confidence"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 100: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 101: `type ItineraryRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 102: `Destination string                 \`json:"destination" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `Days        int                    \`json:"days" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 104: `Preferences map[string]interface{} \`json:"preferences"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 106: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 107: `type ItineraryResponse struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 108: `ID        string         \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `Title     string         \`json:"title"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 110: `Days      []ItineraryDay \`json:"days"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 111: `Budget    float64        \`json:"budget"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 112: `Tags      []string       \`json:"tags"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `CreatedAt time.Time      \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 114: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 115: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 116: `type ItineraryDay struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 117: `Day        int        \`json:"day"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `Date       string     \`json:"date"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `Activities []Activity \`json:"activities"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 120: `Meals      []Meal     \`json:"meals"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 122: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 123: `type Activity struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 124: `ID          string  \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 125: `Name        string  \`json:"name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `Type        string  \`json:"type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 127: `Location    string  \`json:"location"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 128: `StartTime   string  \`json:"start_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `EndTime     string  \`json:"end_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `Description string  \`json:"description"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `Cost        float64 \`json:"cost"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `type Meal struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 135: `Type     string  \`json:"type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `Name     string  \`json:"name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 137: `Location string  \`json:"location"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 138: `Cost     float64 \`json:"cost"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `Cuisine  string  \`json:"cuisine"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 140: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 141: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 142: `type RecommendationRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 143: `UserID  string                 \`json:"user_id" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `Context map[string]interface{} \`json:"context"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 146: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 147: `type Recommendation struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 148: `ID        string                 \`json:"id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `UserID    string                 \`json:"user_id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `Type      string                 \`json:"type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 151: `TargetID  string                 \`json:"target_id"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `Score     float64                \`json:"score"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `Reason    string                 \`json:"reason"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `CreatedAt time.Time              \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 155: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 156: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 157: `// ============== 构造函数 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 158: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 159: `func NewService(logger *logrus.Logger, redis *redis.Client, aiProvider, aiAPI string) *Service {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 160: `if _, ok := AIProviders[aiProvider]; !ok {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 161: `aiProvider = DefaultAIProvider`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 162: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 163: `return &Service{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 164: `logger:     logger,`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 165: `redis:      redis,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `aiProvider: aiProvider,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 167: `aiAPI:      aiAPI,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 169: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 170: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 171: `// GetModelName 获取当前模型名（支持环境变量覆盖）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 172: `func (s *Service) GetModelName() string {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 173: `if AIModelOverride != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 174: `return AIModelOverride`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 175: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 176: `return AIProviders[s.aiProvider].Model`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 177: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 178: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 179: `// GetBaseURL 获取当前 BaseURL`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 180: `func (s *Service) GetBaseURL() string {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 181: `return AIProviders[s.aiProvider].BaseURL`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 182: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 183: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 184: `// ============== 路由设置 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 185: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 186: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 187: `ai := router.Group("/ai")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 188: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `ai.POST("/chat", s.SendMessage)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 190: `ai.GET("/chat/:session_id/history", s.GetChatHistory)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 191: `ai.POST("/intent", s.DetectIntent)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 192: `ai.POST("/itinerary/generate", s.GenerateItinerary)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 193: `ai.POST("/recommendations", s.GetRecommendations)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 194: `ai.POST("/knowledge/search", s.SearchKnowledge)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 195: `ai.POST("/knowledge/update", s.UpdateKnowledge)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 196: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 197: `// 流式对话端点`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 198: `ai.POST("/chat/stream", s.SendMessageStream)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 199: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 200: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 201: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 202: `// ============== 核心对话功能 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 203: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 204: `func (s *Service) SendMessage(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 205: `var req MessageRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 206: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 207: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 208: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 209: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 210: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 211: `sessionID := req.SessionID`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `if sessionID == "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 213: `sessionID = fmt.Sprintf("session_%d", time.Now().Unix())`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 214: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 215: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 216: `// 获取对话历史`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 217: `history, _ := s.getConversationHistory(sessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 219: `// 构建消息列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 220: `messages := s.buildMessages(req.Message, req.SystemPrompt, history)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 222: `// 调用 Moonshot AI`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 223: `response, err := s.callMoonshot(messages)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 225: `s.logger.WithError(err).Error("Moonshot API 调用失败")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 226: `c.JSON(http.StatusInternalServerError, gin.H{"error": fmt.Sprintf("AI服务调用失败: %v", err)})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 227: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 229: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 230: `// 保存对话`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 231: `s.saveConversation(Conversation{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 232: `ID:        fmt.Sprintf("msg_%d", time.Now().Unix()),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 233: `SessionID: sessionID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `Role:      "user",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 235: `Content:   req.Message,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 236: `CreatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 237: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `s.saveConversation(Conversation{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `ID:        fmt.Sprintf("msg_%d", time.Now().Unix()+1),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 240: `SessionID: sessionID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 241: `Role:      "assistant",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 242: `Content:   response,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 243: `CreatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 244: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 245: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 246: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 247: `"message":    response,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 248: `"session_id": sessionID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 249: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 250: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 251: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 252: `// 流式对话`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 253: `func (s *Service) SendMessageStream(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 254: `var req MessageRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 255: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 256: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 257: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 258: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 259: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 260: `sessionID := req.SessionID`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 261: `if sessionID == "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 262: `sessionID = fmt.Sprintf("session_%d", time.Now().Unix())`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 263: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 264: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 265: `// 获取对话历史`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 266: `history, _ := s.getConversationHistory(sessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 267: `messages := s.buildMessages(req.Message, req.SystemPrompt, history)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 268: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 269: `// 设置 SSE`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 270: `c.Header("Content-Type", "text/event-stream")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 271: `c.Header("Cache-Control", "no-cache")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 272: `c.Header("Connection", "keep-alive")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 273: `c.Header("Transfer-Encoding", "chunked")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 274: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 275: `// 流式调用`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 276: `err := s.callMoonshotStream(c, messages, sessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 277: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 278: `s.logger.WithError(err).Error("流式调用失败")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 279: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 280: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 281: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 282: `// ============== Moonshot AI 调用 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 283: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 284: `func (s *Service) buildMessages(userMessage, systemPrompt string, history []Conversation) []MoonshotMessage {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 285: `var messages []MoonshotMessage`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 286: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 287: `// 系统提示词 - 这是让AI变"聪明"的关键！`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 288: `systemContent := systemPrompt`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 289: `if systemContent == "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 290: `systemContent = \`你是「智旅助手」，一个专业、友好的旅行规划AI助手。`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 291: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 292: `【核心能力】`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 293: `1. 行程规划：根据用户偏好定制最佳路线`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 294: `2. 景点推荐：结合季节、预算、人数给出最优建议`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 295: `3. 预算估算：提供透明、合理的费用预估`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 296: `4. 实时问答：解答各类旅行相关问题`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 297: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 298: `【回答风格】`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 299: `- 语言简洁有条理，善用emoji增加趣味`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 300: `- 不确定时主动说明，不要瞎编`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 301: `- 涉及价格/政策时提醒以官方为准`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 302: `- 根据上下文保持对话连贯性`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 303: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 304: `【知识库】`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 305: `如果有相关文档参考，优先基于文档回答；文档未覆盖的再用通用知识。\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 306: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 307: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 308: `messages = append(messages, MoonshotMessage{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 309: `Role:    "system",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 310: `Content: systemContent,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 311: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 312: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 313: `// 添加历史对话（限制最近10条，避免超出token限制）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 314: `startIdx := 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 315: `if len(history) > 20 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 316: `startIdx = len(history) - 20`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 317: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 318: `for _, h := range history[startIdx:] {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 319: `messages = append(messages, MoonshotMessage{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 320: `Role:    h.Role,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 321: `Content: h.Content,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 322: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 323: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 324: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 325: `// 添加当前用户消息`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 326: `messages = append(messages, MoonshotMessage{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 327: `Role:    "user",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 328: `Content: userMessage,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 329: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 330: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 331: `return messages`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 332: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 333: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 334: `func (s *Service) callMoonshot(messages []MoonshotMessage) (string, error) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 335: `reqBody := MoonshotRequest{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 336: `Model:       s.GetModelName(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 337: `Messages:    messages,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 338: `Temperature: 0.7, // 适度创意但不胡编`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 339: `MaxTokens:   2000,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 340: `Stream:      false,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 341: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 342: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 343: `jsonData, err := json.Marshal(reqBody)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 344: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 345: `return "", err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 346: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 347: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 348: `req, err := http.NewRequest("POST", fmt.Sprintf("%s/chat/completions", s.GetBaseURL()), bytes.NewBuffer(jsonData))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 349: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 350: `return "", err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 351: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 352: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 353: `req.Header.Set("Content-Type", "application/json")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 354: `req.Header.Set("Authorization", fmt.Sprintf("Bearer %s", s.aiAPI))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 355: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 356: `client := &http.Client{Timeout: 60 * time.Second}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 357: `resp, err := client.Do(req)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 358: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 359: `return "", err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 360: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 361: `defer resp.Body.Close()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 362: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 363: `if resp.StatusCode != http.StatusOK {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 364: `body, _ := io.ReadAll(resp.Body)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 365: `return "", fmt.Errorf("API返回错误: %s", string(body))`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 366: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 367: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 368: `var moonshotResp MoonshotResponse`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 369: `if err := json.NewDecoder(resp.Body).Decode(&moonshotResp); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 370: `return "", err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 371: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 372: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 373: `if len(moonshotResp.Choices) == 0 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 374: `return "", fmt.Errorf("API返回空响应")`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 375: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 376: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 377: `return moonshotResp.Choices[0].Message.Content, nil`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 378: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 379: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 380: `func (s *Service) callMoonshotStream(c *gin.Context, messages []MoonshotMessage, sessionID string) error {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 381: `reqBody := MoonshotRequest{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 382: `Model:       s.GetModelName(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 383: `Messages:    messages,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `Temperature: 0.7,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 385: `MaxTokens:   2000,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 386: `Stream:      true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 387: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 388: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 389: `jsonData, err := json.Marshal(reqBody)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 390: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 391: `return err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 392: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 393: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 394: `req, err := http.NewRequest("POST", fmt.Sprintf("%s/chat/completions", s.GetBaseURL()), bytes.NewBuffer(jsonData))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 395: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 396: `return err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 397: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 398: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 399: `req.Header.Set("Content-Type", "application/json")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 400: `req.Header.Set("Authorization", fmt.Sprintf("Bearer %s", s.aiAPI))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 401: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 402: `client := &http.Client{Timeout: 120 * time.Second}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 403: `resp, err := client.Do(req)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 404: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 405: `return err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 406: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 407: `defer resp.Body.Close()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 408: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 409: `if resp.StatusCode != http.StatusOK {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 410: `return fmt.Errorf("API返回错误: %d", resp.StatusCode)`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 411: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 412: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 413: `// 保存完整响应`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 414: `fullContent := ""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 415: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 416: `// 设置flush`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 417: `flusher, ok := c.Writer.(http.Flusher)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 418: `if !ok {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 419: `return fmt.Errorf("不支持流式响应")`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 420: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 421: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 422: `reader := resp.Body`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 423: `buffer := make([]byte, 1024)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 424: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 425: `for {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 426: `n, err := reader.Read(buffer)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 427: `if n > 0 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 428: `chunk := string(buffer[:n])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 429: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 430: `// SSE格式`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 431: `fmt.Fprintf(c.Writer, "data: %s\n\n", chunk)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 432: `flusher.Flush()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 433: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 434: `fullContent += chunk`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 435: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 436: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 437: `break`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 438: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 439: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 440: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 441: `// 保存对话`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 442: `s.saveConversation(Conversation{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 443: `ID:        fmt.Sprintf("msg_%d", time.Now().Unix()),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 444: `SessionID: sessionID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 445: `Role:      "user",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 446: `Content:   messages[len(messages)-1].Content,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 447: `CreatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 448: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 449: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 450: `return nil`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 451: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 452: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 453: `// ============== 其他功能（保留原有实现） ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 454: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 455: `func (s *Service) GetChatHistory(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 456: `sessionID := c.Param("session_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 457: `messages, err := s.getConversationHistory(sessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 458: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 459: `c.JSON(http.StatusInternalServerError, gin.H{"error": "获取历史记录失败"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 460: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 461: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 462: `c.JSON(http.StatusOK, gin.H{"session_id": sessionID, "messages": messages})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 463: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 464: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 465: `func (s *Service) DetectIntent(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 466: `var req MessageRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 467: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 468: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 469: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 470: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 471: `intent, _ := s.DetectIntentInternal(req.Message)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 472: `c.JSON(http.StatusOK, intent)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 473: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 474: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 475: `func (s *Service) DetectIntentInternal(message string) (*IntentResponse, error) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 476: `intent := "general"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 477: `confidence := 0.85`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 478: `lowerMessage := strings.ToLower(message)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 479: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 480: `if strings.Contains(lowerMessage, "行程") || strings.Contains(lowerMessage, "规划") || strings.Contains(lowerMessage, "几天") {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 481: `intent = "itinerary_planning"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 482: `confidence = 0.9`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 483: `} else if strings.Contains(lowerMessage, "推荐") || strings.Contains(lowerMessage, "建议") || strings.Contains(lowerMessage, "好玩") {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 484: `intent = "recommendation"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 485: `confidence = 0.88`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 486: `} else if strings.Contains(lowerMessage, "价格") || strings.Contains(lowerMessage, "费用") || strings.Contains(lowerMessage, "多少钱") {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 487: `intent = "pricing"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 488: `confidence = 0.92`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 489: `} else if strings.Contains(lowerMessage, "预订") || strings.Contains(lowerMessage, "订票") || strings.Contains(lowerMessage, "买票") {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 490: `intent = "booking"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 491: `confidence = 0.95`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 492: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 493: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 494: `return &IntentResponse{Intent: intent, Confidence: confidence}, nil`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 495: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 496: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 497: `func (s *Service) GenerateItinerary(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 498: `var req ItineraryRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 499: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 500: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 501: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 502: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 503: `itinerary, _ := s.generateItineraryInternal(req)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 504: `c.JSON(http.StatusOK, gin.H{"message": "生成成功", "itinerary": itinerary})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 505: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 506: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 507: `func (s *Service) generateItineraryInternal(req ItineraryRequest) (*ItineraryResponse, error) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 508: `title := fmt.Sprintf("%s %d日游", req.Destination, req.Days)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 509: `days := make([]ItineraryDay, req.Days)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 510: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 511: `for i := 0; i < req.Days; i++ {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 512: `days[i] = ItineraryDay{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 513: `Day:        i + 1,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 514: `Date:       time.Now().AddDate(0, 0, i).Format("2006-01-02"),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 515: `Activities: []Activity{},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 516: `Meals:      []Meal{},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 517: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 518: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 519: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 520: `return &ItineraryResponse{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 521: `ID:        fmt.Sprintf("itinerary_%d", time.Now().Unix()),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 522: `Title:     title,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 523: `Days:      days,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 524: `Budget:    float64(req.Days * 500),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 525: `Tags:      []string{req.Destination},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 526: `CreatedAt: time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 527: `}, nil`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 528: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 529: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 530: `func (s *Service) GetRecommendations(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 531: `var req RecommendationRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 532: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 533: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 534: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 535: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 536: `c.JSON(http.StatusOK, gin.H{"recommendations": []Recommendation{}, "user_id": req.UserID})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 537: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 538: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 539: `func (s *Service) SearchKnowledge(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 540: `c.JSON(http.StatusOK, gin.H{"results": []map[string]interface{}{}, "query": ""})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 541: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 542: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 543: `func (s *Service) UpdateKnowledge(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 544: `c.JSON(http.StatusOK, gin.H{"message": "更新成功"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 545: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 546: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 547: `func (s *Service) saveConversation(conv Conversation) error {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 548: `key := fmt.Sprintf("chat:%s", conv.SessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 549: `data, _ := json.Marshal(conv)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 550: `return s.redis.RPush(context.Background(), key, data).Err()`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 551: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 552: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 553: `func (s *Service) getConversationHistory(sessionID string) ([]Conversation, error) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 554: `key := fmt.Sprintf("chat:%s", sessionID)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 555: `results, err := s.redis.LRange(context.Background(), key, 0, -1).Result()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 556: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 557: `return nil, err`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 558: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 559: `var messages []Conversation`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 560: `for _, result := range results {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 561: `var conv Conversation`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 562: `if err := json.Unmarshal([]byte(result), &conv); err == nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 563: `messages = append(messages, conv)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 564: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 565: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 566: `return messages, nil`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 567: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
