# `deployment/docker/apisix/apisix.yml` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `apisix:` -> 执行当前语句，参与该文件整体逻辑。
- 2: `node_listen: 9080` -> 执行当前语句，参与该文件整体逻辑。
- 3: `enable_admin: true` -> 执行当前语句，参与该文件整体逻辑。
- 4: `admin_key:` -> 执行当前语句，参与该文件整体逻辑。
- 5: `- name: admin` -> 执行当前语句，参与该文件整体逻辑。
- 6: `key: edd1c9f034335f136f7ad780378a5e0d` -> 执行当前语句，参与该文件整体逻辑。
- 7: `role: admin` -> 执行当前语句，参与该文件整体逻辑。
- 8: `(空行)` -> 空行，用于提升代码结构可读性。
- 9: `etcd:` -> 执行当前语句，参与该文件整体逻辑。
- 10: `host:` -> 执行当前语句，参与该文件整体逻辑。
- 11: `- http://apisix-etcd:2379` -> 执行当前语句，参与该文件整体逻辑。
- 12: `prefix: /apisix` -> 执行当前语句，参与该文件整体逻辑。
- 13: `timeout: 30` -> 执行当前语句，参与该文件整体逻辑。
- 14: `(空行)` -> 空行，用于提升代码结构可读性。
- 15: `plugins:` -> 执行当前语句，参与该文件整体逻辑。
- 16: `- proxy-rewrite` -> 执行当前语句，参与该文件整体逻辑。
- 17: `- cors` -> 执行当前语句，参与该文件整体逻辑。
- 18: `- limit-req` -> 执行当前语句，参与该文件整体逻辑。
- 19: `- limit-count` -> 执行当前语句，参与该文件整体逻辑。
- 20: `- limit-conn` -> 执行当前语句，参与该文件整体逻辑。
- 21: `- prometheus` -> 执行当前语句，参与该文件整体逻辑。
- 22: `- grpc-transcode` -> 执行当前语句，参与该文件整体逻辑。
- 23: `- request-id` -> 执行当前语句，参与该文件整体逻辑。
- 24: `- error-log-logger` -> 执行当前语句，参与该文件整体逻辑。
- 25: `(空行)` -> 空行，用于提升代码结构可读性。
- 26: `plugin_attr:` -> 执行当前语句，参与该文件整体逻辑。
- 27: `prometheus:` -> 执行当前语句，参与该文件整体逻辑。
- 28: `export_uri: /apisix/prometheus` -> 执行当前语句，参与该文件整体逻辑。
- 29: `meter_interval: 15` -> 执行当前语句，参与该文件整体逻辑。
