# `backend/ai-service/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package main` -> 声明当前文件所属的 Go 包。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `import (` -> 开始导入依赖包列表。
- 4: `"context"` -> 执行当前语句，参与该函数的业务流程。
- 5: `"log"` -> 执行当前语句，参与该函数的业务流程。
- 6: `"net/http"` -> 执行当前语句，参与该函数的业务流程。
- 7: `"os"` -> 执行当前语句，参与该函数的业务流程。
- 8: `"time"` -> 执行当前语句，参与该函数的业务流程。
- 9: `(空行)` -> 空行，用于提升代码结构可读性。
- 10: `"github.com/gin-contrib/cors"` -> 执行当前语句，参与该函数的业务流程。
- 11: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该函数的业务流程。
- 12: `"github.com/redis/go-redis/v9"` -> 执行当前语句，参与该函数的业务流程。
- 13: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该函数的业务流程。
- 14: `)` -> 结束当前代码块或导入块。
- 15: `(空行)` -> 空行，用于提升代码结构可读性。
- 16: `func main() {` -> 定义函数或方法，实现具体业务逻辑。
- 17: `// 日志配置` -> 注释行，用于说明后续逻辑意图。
- 18: `logger := logrus.New()` -> 记录日志，便于运行观测与故障排查。
- 19: `logger.SetFormatter(&logrus.JSONFormatter{})` -> 记录日志，便于运行观测与故障排查。
- 20: `logger.SetOutput(os.Stdout)` -> 记录日志，便于运行观测与故障排查。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `// Redis 连接` -> 注释行，用于说明后续逻辑意图。
- 23: `redisAddr := getEnv("REDIS_ADDR", "localhost:6379")` -> 执行当前语句，参与该函数的业务流程。
- 24: `redisPassword := getEnv("REDIS_PASSWORD", "")` -> 执行当前语句，参与该函数的业务流程。
- 25: `(空行)` -> 空行，用于提升代码结构可读性。
- 26: `client := redis.NewClient(&redis.Options{` -> 执行当前语句，参与该函数的业务流程。
- 27: `Addr:     redisAddr,` -> 执行当前语句，参与该函数的业务流程。
- 28: `Password: redisPassword,` -> 执行当前语句，参与该函数的业务流程。
- 29: `DB:       0,` -> 执行当前语句，参与该函数的业务流程。
- 30: `})` -> 执行当前语句，参与该函数的业务流程。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `ctx := context.Background()` -> 执行当前语句，参与该函数的业务流程。
- 33: `if _, err := client.Ping(ctx).Result(); err != nil {` -> 条件判断，根据分支处理不同情况。
- 34: `logger.Warnf("Redis 连接失败: %v，将使用内存存储", err)` -> 记录日志，便于运行观测与故障排查。
- 35: `}` -> 代码块边界，标记作用域开始或结束。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `// AI 提供商配置（默认 MiMo）` -> 注释行，用于说明后续逻辑意图。
- 38: `aiProvider := getEnv("AI_PROVIDER", "mimo")` -> 执行当前语句，参与该函数的业务流程。
- 39: `aiAPI := getEnv("AI_API_KEY", "")` -> 执行当前语句，参与该函数的业务流程。
- 40: `if aiAPI == "" {` -> 条件判断，根据分支处理不同情况。
- 41: `// 兼容旧的 MOONSHOT_API_KEY` -> 注释行，用于说明后续逻辑意图。
- 42: `aiAPI = getEnv("MOONSHOT_API_KEY", "")` -> 执行当前语句，参与该函数的业务流程。
- 43: `}` -> 代码块边界，标记作用域开始或结束。
- 44: `if aiAPI == "" {` -> 条件判断，根据分支处理不同情况。
- 45: `logger.Warn("未设置 AI_API_KEY，对话功能将受限")` -> 记录日志，便于运行观测与故障排查。
- 46: `}` -> 代码块边界，标记作用域开始或结束。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `// 环境变量覆盖模型名` -> 注释行，用于说明后续逻辑意图。
- 49: `if model := getEnv("AI_MODEL", ""); model != "" {` -> 条件判断，根据分支处理不同情况。
- 50: `AIModelOverride = model` -> 执行当前语句，参与该函数的业务流程。
- 51: `}` -> 代码块边界，标记作用域开始或结束。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `// 创建服务` -> 注释行，用于说明后续逻辑意图。
- 54: `service := NewService(logger, client, aiProvider, aiAPI)` -> 记录日志，便于运行观测与故障排查。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `// Gin 路由` -> 注释行，用于说明后续逻辑意图。
- 57: `gin.SetMode(gin.ReleaseMode)` -> 执行当前语句，参与该函数的业务流程。
- 58: `r := gin.New()` -> 执行当前语句，参与该函数的业务流程。
- 59: `r.Use(gin.Recovery())` -> 执行当前语句，参与该函数的业务流程。
- 60: `r.Use(cors.New(cors.Config{` -> 执行当前语句，参与该函数的业务流程。
- 61: `AllowOrigins:     []string{"*"},` -> 执行当前语句，参与该函数的业务流程。
- 62: `AllowMethods:     []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"},` -> 执行当前语句，参与该函数的业务流程。
- 63: `AllowHeaders:     []string{"Origin", "Content-Type", "Authorization"},` -> 执行当前语句，参与该函数的业务流程。
- 64: `ExposeHeaders:    []string{"Content-Length"},` -> 执行当前语句，参与该函数的业务流程。
- 65: `AllowCredentials: true,` -> 执行当前语句，参与该函数的业务流程。
- 66: `MaxAge:           12 * time.Hour,` -> 执行当前语句，参与该函数的业务流程。
- 67: `}))` -> 执行当前语句，参与该函数的业务流程。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `// 健康检查` -> 注释行，用于说明后续逻辑意图。
- 70: `r.GET("/health", func(c *gin.Context) {` -> 注册路由或组织接口分组。
- 71: `c.JSON(http.StatusOK, gin.H{` -> 向客户端返回 JSON 响应。
- 72: `"status":       "ok",` -> 执行当前语句，参与该函数的业务流程。
- 73: `"timestamp":    time.Now().Unix(),` -> 获取当前时间，用于时间字段或时序控制。
- 74: `"service":      "ai-service",` -> 执行当前语句，参与该函数的业务流程。
- 75: `"ai_provider":  aiProvider,` -> 执行当前语句，参与该函数的业务流程。
- 76: `})` -> 执行当前语句，参与该函数的业务流程。
- 77: `})` -> 执行当前语句，参与该函数的业务流程。
- 78: `(空行)` -> 空行，用于提升代码结构可读性。
- 79: `// API 路由` -> 注释行，用于说明后续逻辑意图。
- 80: `api := r.Group("/api")` -> 执行当前语句，参与该函数的业务流程。
- 81: `service.SetupRoutes(api)` -> 执行当前语句，参与该函数的业务流程。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `// 启动服务` -> 注释行，用于说明后续逻辑意图。
- 84: `port := getEnv("PORT", "8080")` -> 执行当前语句，参与该函数的业务流程。
- 85: `logger.Infof("AI 服务启动中，使用 %s 模型，监听端口: %s", aiProvider, port)` -> 记录日志，便于运行观测与故障排查。
- 86: `if err := r.Run(":" + port); err != nil {` -> 条件判断，根据分支处理不同情况。
- 87: `log.Fatalf("服务启动失败: %v", err)` -> 执行当前语句，参与该函数的业务流程。
- 88: `}` -> 代码块边界，标记作用域开始或结束。
- 89: `}` -> 代码块边界，标记作用域开始或结束。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `func getEnv(key, defaultValue string) string {` -> 定义函数或方法，实现具体业务逻辑。
- 92: `if value := os.Getenv(key); value != "" {` -> 条件判断，根据分支处理不同情况。
- 93: `return value` -> 返回函数结果或提前结束当前流程。
- 94: `}` -> 代码块边界，标记作用域开始或结束。
- 95: `return defaultValue` -> 返回函数结果或提前结束当前流程。
- 96: `}` -> 代码块边界，标记作用域开始或结束。
