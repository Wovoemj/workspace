# `frontend/user-web/src/app/admin/database/page.tsx` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 3: `* 管理后台 - 数据库管理模块`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 5: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `* 【功能列表】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 7: `* - 数据库表列表展示`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `* - 表结构查看（列名、类型、约束）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 9: `* - 表数据查看（分页）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 10: `* - 数据增删改查`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 11: `* - 管理员权限校验`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 13: `* 【组件依赖】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 14: `* - Navbar, Footer: 布局组件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 16: `* 【API 接口】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `* - GET /api/admin/database/tables: 获取所有数据表列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 18: `* - GET /api/admin/database/tables/${name}: 获取表数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 19: `* - POST /api/admin/database/tables/${name}/row: 插入数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 20: `* - PUT /api/admin/database/tables/${name}/row: 更新数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 21: `* - DELETE /api/admin/database/tables/${name}/row: 删除数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 22: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `'use client'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 25: `import { useState, useEffect } from 'react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 26: `import { Navbar } from '@/components/Navbar'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 27: `import { Footer } from '@/components/Footer'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 28: `import { Pencil, Trash2, Plus, X, Check, AlertCircle } from 'lucide-react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 29: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 30: `interface TableColumn {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 31: `cid: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `name: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `type: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `notnull: boolean`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `default: any`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `pk: boolean`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 38: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 39: `interface TableInfo {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 40: `name: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `columns: TableColumn[]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `row_count: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 44: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 45: `interface TableData {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 46: `table_name: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 47: `columns: string[]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `data: Record<string, any>[]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `total: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `page: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `per_page: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 53: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 54: `export default function DatabasePage() {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 55: `const [tables, setTables] = useState<TableInfo[]>([])`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 56: `const [selectedTable, setSelectedTable] = useState<string | null>(null)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 57: `const [tableData, setTableData] = useState<TableData | null>(null)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 58: `const [loading, setLoading] = useState(true)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 59: `const [loadingData, setLoadingData] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 60: `const [activeTab, setActiveTab] = useState<'tables' | 'structure' | 'data'>('tables')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 61: `const [currentPage, setCurrentPage] = useState(1)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 62: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 63: `// 编辑/新增状态`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 64: `const [editMode, setEditMode] = useState<'none' | 'add' | 'edit'>('none')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 65: `const [editRow, setEditRow] = useState<Record<string, any>>({})`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 66: `const [originalRow, setOriginalRow] = useState<Record<string, any>>({})`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 67: `const [editError, setEditError] = useState('')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 68: `const [editLoading, setEditLoading] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 69: `const [deleteConfirm, setDeleteConfirm] = useState<string | null>(null)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `const adminToken = typeof window !== 'undefined' ? localStorage.getItem('admin_token') : ''`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 72: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 73: `// 获取表列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 74: `const fetchTables = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 75: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 76: `setLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `const res = await fetch('/api/admin/database/tables', {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 78: `headers: { 'Authorization': \`Bearer ${adminToken || ''}\` }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 80: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 81: `if (data.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 82: `setTables(data.tables)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 84: `} catch (e) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `console.error('获取表列表失败:', e)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `setLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 89: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 90: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 91: `// 获取表数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 92: `const fetchTableData = async (tableName: string, page: number = 1) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 93: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 94: `setLoadingData(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 95: `setCurrentPage(page)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `const res = await fetch(\`/api/admin/database/tables/${tableName}?page=${page}&per_page=50\`, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 97: `headers: { 'Authorization': \`Bearer ${adminToken || ''}\` }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 100: `if (data.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 101: `setTableData(data)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 103: `} catch (e) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 104: `console.error('获取表数据失败:', e)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 106: `setLoadingData(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 107: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 108: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 109: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 110: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 111: `fetchTables()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 112: `}, [])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 114: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 115: `if (selectedTable) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 116: `fetchTableData(selectedTable)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 118: `}, [selectedTable])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 120: `const handleTableClick = (tableName: string) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 121: `setSelectedTable(tableName)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 122: `setActiveTab('data')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 124: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 125: `const selectedTableInfo = tables.find(t => t.name === selectedTable)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 126: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 127: `// 获取主键列`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 128: `const getPkColumn = (tableInfo?: TableInfo): string | null => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 129: `if (!tableInfo) return null`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 130: `const pk = tableInfo.columns.find(c => c.pk)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 131: `return pk ? pk.name : null`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 132: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `// 开始新增`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 135: `const handleAdd = () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 136: `if (!selectedTableInfo) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 137: `const emptyRow: Record<string, any> = {}`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 138: `selectedTableInfo.columns.forEach(col => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `emptyRow[col.name] = col.default ?? ''`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 140: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 141: `setEditRow(emptyRow)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 142: `setOriginalRow({})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 143: `setEditMode('add')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `setEditError('')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 146: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 147: `// 开始编辑`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 148: `const handleEdit = (row: Record<string, any>) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 149: `const copy = { ...row }`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 150: `// 尝试解析 JSON 字符串以便编辑`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 151: `selectedTableInfo?.columns.forEach(col => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `if (col.type?.toLowerCase().includes('text') && typeof copy[col.name] === 'string') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 153: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 154: `const parsed = JSON.parse(copy[col.name])`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 155: `if (typeof parsed === 'object') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 156: `copy[col.name] = JSON.stringify(parsed, null, 2)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 157: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 158: `} catch {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `// 不是 JSON，保持原样`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 160: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 161: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 162: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 163: `// 密码哈希不在编辑框显示，留空表示不修改`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 164: `if (copy.password_hash !== undefined) copy.password_hash = ''`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 165: `if (copy.password !== undefined) copy.password = ''`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 166: `setEditRow(copy)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 167: `setOriginalRow({ ...row })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `setEditMode('edit')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `setEditError('')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 171: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 172: `// 保存（新增或更新）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 173: `const handleSave = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 174: `if (!selectedTable) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 175: `setEditLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 176: `setEditError('')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 178: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 179: `const pkCol = getPkColumn(selectedTableInfo)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 180: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 181: `if (editMode === 'add') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 182: `// 过滤空值`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 183: `const values: Record<string, any> = {}`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 184: `Object.entries(editRow).forEach(([k, v]) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `if (v !== '' && v !== null && v !== undefined) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 186: `// 尝试解析 JSON`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 187: `if (typeof v === 'string' && (v.startsWith('{') || v.startsWith('['))) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 188: `try { JSON.parse(v) } catch { /* 不是 JSON */ }`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 189: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 190: `values[k] = v`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 191: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 192: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 193: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 194: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 195: `method: 'POST',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 196: `headers: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 197: `'Content-Type': 'application/json',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `'Authorization': \`Bearer ${adminToken || ''}\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 199: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 200: `body: JSON.stringify(values)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 203: `if (data.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 204: `setEditMode('none')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 205: `fetchTableData(selectedTable, currentPage)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 206: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 207: `setEditError(data.error || '插入失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 208: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 209: `} else if (editMode === 'edit') {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 210: `const where: Record<string, any> = {}`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 211: `if (pkCol && originalRow[pkCol] !== undefined) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 212: `where[pkCol] = originalRow[pkCol]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 213: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `// 没有主键，用所有原始值做 WHERE`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 215: `Object.entries(originalRow).forEach(([k, v]) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `if (v !== null && v !== undefined) where[k] = v`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 217: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 219: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 220: `const values: Record<string, any> = {}`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 221: `Object.entries(editRow).forEach(([k, v]) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `if (v !== '' && v !== null && v !== undefined) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 223: `values[k] = v`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 225: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 227: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 228: `method: 'PUT',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 229: `headers: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 230: `'Content-Type': 'application/json',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 231: `'Authorization': \`Bearer ${adminToken || ''}\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 232: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 233: `body: JSON.stringify({ where, values })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 235: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 236: `if (data.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 237: `setEditMode('none')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `fetchTableData(selectedTable, currentPage)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 240: `setEditError(data.error || '更新失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 241: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 242: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 243: `} catch (e) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 244: `setEditError(String(e))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 245: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `setEditLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 247: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 248: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 249: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 250: `// 删除`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 251: `const handleDelete = async (row: Record<string, any>) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 252: `if (!selectedTable) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 253: `setEditLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 254: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 255: `const pkCol = getPkColumn(selectedTableInfo)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 256: `const where: Record<string, any> = {}`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 257: `if (pkCol && row[pkCol] !== undefined) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 258: `where[pkCol] = row[pkCol]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 259: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 260: `Object.entries(row).forEach(([k, v]) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 261: `if (v !== null && v !== undefined) where[k] = v`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 262: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 263: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 264: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 265: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 266: `method: 'DELETE',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 267: `headers: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 268: `'Content-Type': 'application/json',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 269: `'Authorization': \`Bearer ${adminToken || ''}\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 270: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 271: `body: JSON.stringify(where)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 272: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 273: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 274: `if (data.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 275: `setDeleteConfirm(null)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 276: `fetchTableData(selectedTable, currentPage)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 277: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 278: `alert(data.error || '删除失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 279: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 280: `} catch (e) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 281: `alert(String(e))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 282: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 283: `setEditLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 284: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 285: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 286: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 287: `// 编辑表单输入处理`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 288: `const handleInputChange = (colName: string, value: string) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 289: `setEditRow(prev => ({ ...prev, [colName]: value }))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 290: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 291: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 292: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 293: `<div className="min-h-screen page-bg">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 294: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 295: `<main className="pt-24">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 296: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 297: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 298: `{/* 页面标题 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 299: `<div className="text-center mb-8">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 300: `<h1 className="text-3xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 301: `数据库管理`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 302: `</h1>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 303: `<p className="text-gray-500 mt-2">查看和管理数据库表结构与数据</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 304: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 305: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 306: `<div className="card-glass rounded-2xl overflow-hidden">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 307: `{/* 标签页 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 308: `<div className="flex border-b border-gray-100">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 309: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 310: `onClick={() => setActiveTab('tables')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 311: `className={\`px-6 py-4 font-medium transition-colors ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 312: `activeTab === 'tables'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 313: `? 'text-indigo-600 border-b-2 border-indigo-600'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 314: `: 'text-gray-500 hover:text-gray-700'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 315: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 316: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 317: `所有表 ({tables.length})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 318: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 319: `{selectedTable && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 320: `<>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 321: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 322: `onClick={() => setActiveTab('structure')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 323: `className={\`px-6 py-4 font-medium transition-colors ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 324: `activeTab === 'structure'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 325: `? 'text-indigo-600 border-b-2 border-indigo-600'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 326: `: 'text-gray-500 hover:text-gray-700'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 327: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 328: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 329: `表结构`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 330: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 331: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 332: `onClick={() => setActiveTab('data')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 333: `className={\`px-6 py-4 font-medium transition-colors ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 334: `activeTab === 'data'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 335: `? 'text-indigo-600 border-b-2 border-indigo-600'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 336: `: 'text-gray-500 hover:text-gray-700'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 337: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 338: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 339: `数据`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 340: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 341: `</>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 342: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 343: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 344: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 345: `{/* 内容区 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 346: `<div className="p-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 347: `{loading ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 348: `<div className="flex justify-center py-12">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 349: `<div className="w-8 h-8 border-3 border-indigo-600 border-t-transparent rounded-full animate-spin"></div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 350: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 351: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 352: `<>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 353: `{/* 表列表 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 354: `{activeTab === 'tables' && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 355: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 356: `{tables.map((table) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 357: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 358: `key={table.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 359: `onClick={() => handleTableClick(table.name)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 360: `className={\`p-5 rounded-xl border-2 cursor-pointer transition-all hover:scale-[1.02] ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 361: `selectedTable === table.name`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 362: `? 'border-indigo-400 bg-indigo-50 shadow-md'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 363: `: 'border-gray-100 bg-white hover:border-indigo-200 hover:shadow-sm'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 364: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 365: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 366: `<div className="flex items-center gap-3 mb-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 367: `<div className="w-10 h-10 rounded-lg bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 368: `<svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 369: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 370: `</svg>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 371: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 372: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 373: `<h3 className="font-semibold text-gray-900">{table.name}</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 374: `<span className="text-xs text-gray-400">{table.row_count} 行</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 375: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 376: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 377: `<div className="text-sm text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 378: `{table.columns.length} 个字段`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 379: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 380: `<div className="mt-2 flex flex-wrap gap-1">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 381: `{table.columns.slice(0, 3).map((col) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 382: `<span`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 383: `key={col.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `className="inline-block px-2 py-0.5 text-xs bg-gray-100 text-gray-600 rounded"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 385: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 386: `{col.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 387: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 388: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 389: `{table.columns.length > 3 && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 390: `<span className="inline-block px-2 py-0.5 text-xs bg-gray-100 text-gray-400 rounded">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 391: `+{table.columns.length - 3}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 392: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 393: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 394: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 395: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 396: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 397: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 398: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 399: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 400: `{/* 表结构 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 401: `{activeTab === 'structure' && selectedTableInfo && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 402: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 403: `<div className="flex items-center justify-between mb-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 404: `<h2 className="text-lg font-semibold text-gray-900">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 405: `{selectedTableInfo.name} 表结构`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 406: `</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 407: `<span className="text-sm text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 408: `{selectedTableInfo.columns.length} 个字段 · {selectedTableInfo.row_count} 行数据`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 409: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 410: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 411: `<div className="overflow-x-auto">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 412: `<table className="w-full">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 413: `<thead>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 414: `<tr className="bg-gray-50">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 415: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">字段名</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 416: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">类型</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 417: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase">非空</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 418: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase">主键</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 419: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">默认值</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 420: `</tr>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 421: `</thead>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 422: `<tbody className="divide-y divide-gray-100">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 423: `{selectedTableInfo.columns.map((col) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 424: `<tr key={col.name} className="hover:bg-gray-50 transition-colors">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 425: `<td className="px-4 py-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 426: `<span className="font-mono text-sm text-indigo-600 font-medium">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 427: `{col.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 428: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 429: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 430: `<td className="px-4 py-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 431: `<span className="font-mono text-xs bg-gray-100 px-2 py-1 rounded text-gray-700">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 432: `{col.type || 'ANY'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 433: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 434: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 435: `<td className="px-4 py-3 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 436: `{col.notnull ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 437: `<span className="text-red-500">✓</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 438: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 439: `<span className="text-gray-300">-</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 440: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 441: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 442: `<td className="px-4 py-3 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 443: `{col.pk ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 444: `<span className="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 445: `PK`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 446: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 447: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 448: `<span className="text-gray-300">-</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 449: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 450: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 451: `<td className="px-4 py-3 text-sm text-gray-500 font-mono">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 452: `{col.default ?? '-'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 453: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 454: `</tr>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 455: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 456: `</tbody>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 457: `</table>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 458: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 459: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 460: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 461: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 462: `{/* 表数据 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 463: `{activeTab === 'data' && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 464: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 465: `{loadingData ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 466: `<div className="flex justify-center py-12">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 467: `<div className="w-8 h-8 border-3 border-indigo-600 border-t-transparent rounded-full animate-spin"></div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 468: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 469: `) : tableData ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 470: `<>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 471: `<div className="flex items-center justify-between mb-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 472: `<h2 className="text-lg font-semibold text-gray-900">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 473: `{tableData.table_name} 数据`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 474: `</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 475: `<div className="flex items-center gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 476: `<span className="text-sm text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 477: `共 {tableData.total} 条记录`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 478: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 479: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 480: `onClick={handleAdd}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 481: `className="inline-flex items-center gap-1.5 px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 482: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 483: `<Plus className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 484: `新增数据`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 485: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 486: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 487: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 488: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 489: `<div className="overflow-x-auto rounded-xl border border-gray-200">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 490: `<table className="w-full text-sm">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 491: `<thead>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 492: `<tr className="bg-gradient-to-r from-indigo-50 to-purple-50">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 493: `{tableData.columns.map((col) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 494: `<th`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 495: `key={col}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 496: `className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase whitespace-nowrap"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 497: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 498: `{col}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 499: `</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 500: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 501: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase whitespace-nowrap w-24">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 502: `操作`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 503: `</th>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 504: `</tr>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 505: `</thead>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 506: `<tbody className="divide-y divide-gray-100 bg-white">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 507: `{tableData.data.map((row, idx) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 508: `<tr key={idx} className="hover:bg-gray-50 transition-colors group">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 509: `{tableData.columns.map((col) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 510: `<td`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 511: `key={col}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 512: `className="px-4 py-2 text-gray-700 font-mono text-xs max-w-xs truncate"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 513: `title={String(row[col] ?? '')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 514: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 515: `{formatCellValue(row[col])}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 516: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 517: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 518: `<td className="px-4 py-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 519: `<div className="flex items-center justify-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 520: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 521: `onClick={() => handleEdit(row)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 522: `className="p-1.5 text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 523: `title="编辑"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 524: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 525: `<Pencil className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 526: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 527: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 528: `onClick={() => setDeleteConfirm(String(idx))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 529: `className="p-1.5 text-red-600 hover:bg-red-50 rounded-lg transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 530: `title="删除"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 531: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 532: `<Trash2 className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 533: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 534: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 535: `</td>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 536: `</tr>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 537: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 538: `</tbody>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 539: `</table>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 540: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 541: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 542: `{/* 分页 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 543: `{tableData.total > tableData.per_page && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 544: `<div className="flex items-center justify-center gap-2 mt-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 545: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 546: `onClick={() => fetchTableData(selectedTable!, currentPage - 1)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 547: `disabled={currentPage <= 1}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 548: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-200 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 549: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 550: `上一页`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 551: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 552: `<span className="px-4 py-2 text-sm text-gray-600">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 553: `第 {currentPage} / {Math.ceil(tableData.total / tableData.per_page)} 页`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 554: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 555: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 556: `onClick={() => fetchTableData(selectedTable!, currentPage + 1)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 557: `disabled={currentPage >= Math.ceil(tableData.total / tableData.per_page)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 558: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-200 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 559: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 560: `下一页`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 561: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 562: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 563: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 564: `</>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 565: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 566: `<div className="text-center py-12 text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 567: `选择一个表查看数据`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 568: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 569: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 570: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 571: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 572: `</>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 573: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 574: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 575: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 576: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 577: `</main>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 578: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 579: `{/* 编辑/新增弹窗 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 580: `{editMode !== 'none' && selectedTableInfo && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 581: `<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 582: `<div className="bg-white rounded-2xl shadow-xl w-full max-w-2xl max-h-[85vh] overflow-hidden flex flex-col">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 583: `{/* 弹窗头部 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 584: `<div className="flex items-center justify-between px-6 py-4 border-b border-gray-100">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 585: `<h3 className="text-lg font-semibold text-gray-900">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 586: `{editMode === 'add' ? '新增数据' : '编辑数据'} — {selectedTableInfo.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 587: `</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 588: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 589: `onClick={() => setEditMode('none')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 590: `className="p-2 text-gray-400 hover:text-gray-600 hover:bg-gray-100 rounded-lg transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 591: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 592: `<X className="h-5 w-5" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 593: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 594: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 595: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 596: `{/* 弹窗内容 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 597: `<div className="flex-1 overflow-y-auto px-6 py-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 598: `{editError && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 599: `<div className="mb-4 flex items-center gap-2 px-4 py-3 bg-red-50 text-red-700 rounded-lg text-sm">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 600: `<AlertCircle className="h-4 w-4 shrink-0" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 601: `{editError}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 602: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 603: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 604: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 605: `<div className="space-y-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 606: `{selectedTableInfo.columns.map((col) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 607: `<div key={col.name}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 608: `<label className="block text-sm font-medium text-gray-700 mb-1">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 609: `{col.name}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 610: `{col.pk && <span className="ml-1.5 px-1.5 py-0.5 text-[10px] bg-yellow-100 text-yellow-700 rounded-full">主键</span>}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 611: `{col.notnull && <span className="ml-1.5 text-red-500">*</span>}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 612: `</label>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 613: `{col.name === 'password_hash' || col.name === 'password' ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 614: `<>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 615: `<input`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 616: `type="password"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 617: `value={editRow[col.name] ?? ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 618: `onChange={(e) => handleInputChange(col.name, e.target.value)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 619: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 620: `placeholder={editMode === 'edit' ? '留空则不修改密码，输入新密码将自动加密' : '请输入密码'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 621: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 622: `<span className="text-xs text-gray-400 mt-0.5">{col.type} · 输入明文密码，保存时自动哈希</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 623: `</>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 624: `) : col.type?.toLowerCase().includes('text') && (editRow[col.name] === null || String(editRow[col.name] ?? '').length > 50) ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 625: `<textarea`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 626: `value={editRow[col.name] ?? ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 627: `onChange={(e) => handleInputChange(col.name, e.target.value)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 628: `rows={3}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 629: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 630: `placeholder={col.default ?? ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 631: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 632: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 633: `<input`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 634: `type={col.type?.toLowerCase().includes('int') ? 'number' : 'text'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 635: `value={editRow[col.name] ?? ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 636: `onChange={(e) => handleInputChange(col.name, e.target.value)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 637: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 638: `placeholder={col.default ?? ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 639: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 640: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 641: `{!(col.name === 'password_hash' || col.name === 'password') && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 642: `<span className="text-xs text-gray-400 mt-0.5">{col.type}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 643: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 644: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 645: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 646: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 647: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 648: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 649: `{/* 弹窗底部 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 650: `<div className="flex items-center justify-end gap-3 px-6 py-4 border-t border-gray-100">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 651: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 652: `onClick={() => setEditMode('none')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 653: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 654: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 655: `取消`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 656: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 657: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 658: `onClick={handleSave}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 659: `disabled={editLoading}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 660: `className="inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-white bg-indigo-600 rounded-lg hover:bg-indigo-700 disabled:opacity-50 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 661: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 662: `{editLoading ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 663: `<div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 664: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 665: `<Check className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 666: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 667: `保存`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 668: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 669: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 670: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 671: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 672: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 673: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 674: `{/* 删除确认弹窗 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 675: `{deleteConfirm !== null && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 676: `<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 677: `<div className="bg-white rounded-2xl shadow-xl w-full max-w-sm p-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 678: `<div className="flex items-center gap-3 mb-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 679: `<div className="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 680: `<AlertCircle className="h-5 w-5 text-red-600" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 681: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 682: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 683: `<h3 className="text-lg font-semibold text-gray-900">确认删除</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 684: `<p className="text-sm text-gray-500">此操作不可撤销</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 685: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 686: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 687: `<div className="flex items-center justify-end gap-3 mt-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 688: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 689: `onClick={() => setDeleteConfirm(null)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 690: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 691: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 692: `取消`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 693: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 694: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 695: `onClick={() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 696: `const idx = parseInt(deleteConfirm)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 697: `if (tableData && !isNaN(idx)) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 698: `handleDelete(tableData.data[idx])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 699: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 700: `}}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 701: `className="px-4 py-2 text-sm font-medium text-white bg-red-600 rounded-lg hover:bg-red-700 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 702: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 703: `删除`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 704: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 705: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 706: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 707: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 708: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 709: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 710: `<Footer />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 711: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 712: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 713: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 714: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 715: `// 格式化单元格值`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 716: `function formatCellValue(value: any): React.ReactNode {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 717: `if (value === null || value === undefined) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 718: `return <span className="text-gray-400 italic">NULL</span>`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 719: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 720: `if (typeof value === 'object') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 721: `return JSON.stringify(value)`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 722: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 723: `return String(value)`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 724: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
