# `backend/ai-service/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package main` -> 声明当前文件所属包或模块命名空间。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `import (` -> 导入依赖模块，供当前文件使用。
- 4: `"context"` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"log"` -> 执行当前语句，参与该文件整体逻辑。
- 6: `"net/http"` -> 执行当前语句，参与该文件整体逻辑。
- 7: `"os"` -> 执行当前语句，参与该文件整体逻辑。
- 8: `"time"` -> 执行当前语句，参与该文件整体逻辑。
- 9: `(空行)` -> 空行，用于提升代码结构可读性。
- 10: `"github.com/gin-contrib/cors"` -> 执行当前语句，参与该文件整体逻辑。
- 11: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该文件整体逻辑。
- 12: `"github.com/redis/go-redis/v9"` -> 执行当前语句，参与该文件整体逻辑。
- 13: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该文件整体逻辑。
- 14: `)` -> 结束当前语句或代码块。
- 15: `(空行)` -> 空行，用于提升代码结构可读性。
- 16: `func main() {` -> 定义函数或方法，实现具体业务逻辑。
- 17: `// 日志配置` -> 注释行，用于解释设计意图或使用说明。
- 18: `logger := logrus.New()` -> 执行当前语句，参与该文件整体逻辑。
- 19: `logger.SetFormatter(&logrus.JSONFormatter{})` -> 执行当前语句，参与该文件整体逻辑。
- 20: `logger.SetOutput(os.Stdout)` -> 执行当前语句，参与该文件整体逻辑。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `// Redis 连接` -> 注释行，用于解释设计意图或使用说明。
- 23: `redisAddr := getEnv("REDIS_ADDR", "localhost:6379")` -> 执行当前语句，参与该文件整体逻辑。
- 24: `redisPassword := getEnv("REDIS_PASSWORD", "")` -> 执行当前语句，参与该文件整体逻辑。
- 25: `(空行)` -> 空行，用于提升代码结构可读性。
- 26: `client := redis.NewClient(&redis.Options{` -> 执行当前语句，参与该文件整体逻辑。
- 27: `Addr:     redisAddr,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `Password: redisPassword,` -> 执行当前语句，参与该文件整体逻辑。
- 29: `DB:       0,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `})` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `ctx := context.Background()` -> 执行当前语句，参与该文件整体逻辑。
- 33: `if _, err := client.Ping(ctx).Result(); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 34: `logger.Warnf("Redis 连接失败: %v，将使用内存存储", err)` -> 执行当前语句，参与该文件整体逻辑。
- 35: `}` -> 结束当前语句或代码块。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `// AI 提供商配置（默认 MiMo）` -> 注释行，用于解释设计意图或使用说明。
- 38: `aiProvider := getEnv("AI_PROVIDER", "mimo")` -> 执行当前语句，参与该文件整体逻辑。
- 39: `aiAPI := getEnv("AI_API_KEY", "")` -> 执行当前语句，参与该文件整体逻辑。
- 40: `if aiAPI == "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 41: `// 兼容旧的 MOONSHOT_API_KEY` -> 注释行，用于解释设计意图或使用说明。
- 42: `aiAPI = getEnv("MOONSHOT_API_KEY", "")` -> 执行当前语句，参与该文件整体逻辑。
- 43: `}` -> 结束当前语句或代码块。
- 44: `if aiAPI == "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 45: `logger.Warn("未设置 AI_API_KEY，对话功能将受限")` -> 执行当前语句，参与该文件整体逻辑。
- 46: `}` -> 结束当前语句或代码块。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `// 环境变量覆盖模型名` -> 注释行，用于解释设计意图或使用说明。
- 49: `if model := getEnv("AI_MODEL", ""); model != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 50: `AIModelOverride = model` -> 执行当前语句，参与该文件整体逻辑。
- 51: `}` -> 结束当前语句或代码块。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `// 创建服务` -> 注释行，用于解释设计意图或使用说明。
- 54: `service := NewService(logger, client, aiProvider, aiAPI)` -> 执行当前语句，参与该文件整体逻辑。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `// Gin 路由` -> 注释行，用于解释设计意图或使用说明。
- 57: `gin.SetMode(gin.ReleaseMode)` -> 执行当前语句，参与该文件整体逻辑。
- 58: `r := gin.New()` -> 执行当前语句，参与该文件整体逻辑。
- 59: `r.Use(gin.Recovery())` -> 执行当前语句，参与该文件整体逻辑。
- 60: `r.Use(cors.New(cors.Config{` -> 执行当前语句，参与该文件整体逻辑。
- 61: `AllowOrigins:     []string{"*"},` -> 执行当前语句，参与该文件整体逻辑。
- 62: `AllowMethods:     []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"},` -> 执行当前语句，参与该文件整体逻辑。
- 63: `AllowHeaders:     []string{"Origin", "Content-Type", "Authorization"},` -> 执行当前语句，参与该文件整体逻辑。
- 64: `ExposeHeaders:    []string{"Content-Length"},` -> 执行当前语句，参与该文件整体逻辑。
- 65: `AllowCredentials: true,` -> 执行当前语句，参与该文件整体逻辑。
- 66: `MaxAge:           12 * time.Hour,` -> 执行当前语句，参与该文件整体逻辑。
- 67: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `// 健康检查` -> 注释行，用于解释设计意图或使用说明。
- 70: `r.GET("/health", func(c *gin.Context) {` -> 执行当前语句，参与该文件整体逻辑。
- 71: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 72: `"status":       "ok",` -> 执行当前语句，参与该文件整体逻辑。
- 73: `"timestamp":    time.Now().Unix(),` -> 执行当前语句，参与该文件整体逻辑。
- 74: `"service":      "ai-service",` -> 执行当前语句，参与该文件整体逻辑。
- 75: `"ai_provider":  aiProvider,` -> 执行当前语句，参与该文件整体逻辑。
- 76: `})` -> 执行当前语句，参与该文件整体逻辑。
- 77: `})` -> 执行当前语句，参与该文件整体逻辑。
- 78: `(空行)` -> 空行，用于提升代码结构可读性。
- 79: `// API 路由` -> 注释行，用于解释设计意图或使用说明。
- 80: `api := r.Group("/api")` -> 执行当前语句，参与该文件整体逻辑。
- 81: `service.SetupRoutes(api)` -> 执行当前语句，参与该文件整体逻辑。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `// 启动服务` -> 注释行，用于解释设计意图或使用说明。
- 84: `port := getEnv("PORT", "8080")` -> 执行当前语句，参与该文件整体逻辑。
- 85: `logger.Infof("AI 服务启动中，使用 %s 模型，监听端口: %s", aiProvider, port)` -> 执行当前语句，参与该文件整体逻辑。
- 86: `if err := r.Run(":" + port); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 87: `log.Fatalf("服务启动失败: %v", err)` -> 执行当前语句，参与该文件整体逻辑。
- 88: `}` -> 结束当前语句或代码块。
- 89: `}` -> 结束当前语句或代码块。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `func getEnv(key, defaultValue string) string {` -> 定义函数或方法，实现具体业务逻辑。
- 92: `if value := os.Getenv(key); value != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 93: `return value` -> 返回结果或提前结束当前流程。
- 94: `}` -> 结束当前语句或代码块。
- 95: `return defaultValue` -> 返回结果或提前结束当前流程。
- 96: `}` -> 结束当前语句或代码块。
