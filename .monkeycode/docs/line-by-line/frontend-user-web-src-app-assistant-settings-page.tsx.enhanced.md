# `frontend/user-web/src/app/assistant/settings/page.tsx` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 3: `* AI 助手设置模块 - AI Agent 配置管理`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 5: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `* 【功能列表】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 7: `* - AI 模型选择（多模型支持）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `* - 工具配置（天气查询/地图搜索/网络搜索/翻译等）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 9: `* - 知识库文件管理：`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 10: `*   - 上传文档（txt/md/pdf）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 11: `*   - 删除文档`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `*   - 文档列表展示`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 13: `* - 系统提示词配置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 14: `* - API 配置管理`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `* - 设置保存与重置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 16: `* - 统计信息展示（对话数/用户数/Token 使用）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 18: `* 【组件依赖】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 19: `* - Navbar, Footer: 布局组件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 20: `* - aiAgentSettings: AI 设置管理工具`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 21: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 22: `* 【本地存储】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `* - ai_agent_settings_v1: 存储 AI 配置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 24: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 25: `* 【配置项】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 26: `* - MODEL_OPTIONS: 可选模型列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 27: `* - DEFAULT_SETTINGS: 默认设置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 28: `* - KbFile: 知识库文件类型`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 29: `* - WebToolKey: 网络工具配置类型`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 30: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 31: `'use client'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 33: `import { useCallback, useEffect, useMemo, useRef, useState } from 'react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 34: `import Link from 'next/link'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 35: `import {`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 36: `BookOpen,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `Bot,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `ChevronRight,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `Copy,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `ExternalLink,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `Globe,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `Info,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `Loader2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `Maximize2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `Mic2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `Minimize2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 47: `Newspaper,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `Save,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `Search,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `Settings2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `Sparkles,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `Trash2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `Upload,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `UserCircle2,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `CloudSun,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `} from 'lucide-react'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `import { toast } from 'react-hot-toast'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 58: `import { Navbar } from '@/components/Navbar'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 59: `import { Footer } from '@/components/Footer'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 60: `import {`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 61: `DEFAULT_SETTINGS,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `incrementStats,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `loadAiAgentSettings,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `MODEL_OPTIONS,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `saveAiAgentSettings,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `type AiAgentSettingsState,`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 67: `type KbFile,`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 68: `type WebToolKey,`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 69: `} from '@/lib/aiAgentSettings'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `function uid() {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 72: `return \`${Date.now()}-${Math.random().toString(16).slice(2)}\``
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 73: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 74: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 75: `/** 内联删除确认按钮：首次点击进入「确认？」状态，再次点击执行删除，移开鼠标则重置 */`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 76: `function DeleteConfirmButton({ onConfirm, label = '删除' }: { onConfirm: () => void; label?: string }) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 77: `const [confirming, setConfirming] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 78: `return confirming ? (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 79: `<span className="inline-flex items-center gap-1">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 80: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `className="px-2 py-1 rounded-lg text-xs font-semibold bg-red-500 text-white hover:bg-red-600 active:scale-95 transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `onClick={() => { onConfirm(); setConfirming(false) }}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `确认删除`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 87: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `className="px-2 py-1 rounded-lg text-xs font-semibold bg-muted text-muted-foreground hover:bg-muted/80 transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 90: `onClick={() => setConfirming(false)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 92: `取消`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 94: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 95: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `className="p-2 rounded-lg text-muted-foreground hover:text-destructive hover:bg-destructive/10 transition active:scale-95"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `onClick={() => setConfirming(true)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `aria-label={label}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `title={label}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `<Trash2 className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 104: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 105: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 106: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 107: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 108: `function fmtBytes(n: number) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 109: `if (n < 1024) return \`${n} B\``
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 110: `if (n < 1024 * 1024) return \`${(n / 1024).toFixed(1)} KB\``
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 111: `return \`${(n / (1024 * 1024)).toFixed(1)} MB\``
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 112: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 113: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 114: `const WEB_TOOL_META: Record<`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 115: `WebToolKey,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 116: `{ label: string; desc: string; icon: typeof Search }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `> = {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `brave: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `label: 'Brave 搜索',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 120: `desc: '网页检索与摘要，适合事实核对与资料查找',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `icon: Search,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 122: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `news: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `label: '联网新闻',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 125: `desc: '抓取近期新闻标题与摘要（演示：由助手侧工具启用后生效）',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `icon: Newspaper,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 127: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 128: `weather: {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `label: '实时天气',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `desc: '按城市查询当前天气概况（演示：需后端工具接入）',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `icon: CloudSun,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 133: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 134: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 135: `export default function AssistantSettingsPage() {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 136: `const [settings, setSettings] = useState<AiAgentSettingsState>(DEFAULT_SETTINGS)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 137: `const [hydrated, setHydrated] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 138: `const [saving, setSaving] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 139: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 140: `const [dhLoading, setDhLoading] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 141: `const [vcLoading, setVcLoading] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 142: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 143: `const [previewTab, setPreviewTab] = useState<'model' | 'kb'>('kb')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 144: `const [previewExpanded, setPreviewExpanded] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 145: `const [previewInput, setPreviewInput] = useState('')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 146: `const [previewSending, setPreviewSending] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 147: `const [previewMsgs, setPreviewMsgs] = useState<Array<{ role: 'user' | 'assistant'; content: string }>>([])`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 148: `const previewEndRef = useRef<HTMLDivElement | null>(null)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 149: `const fileRef = useRef<HTMLInputElement | null>(null)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 150: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 151: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `setSettings(loadAiAgentSettings())`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `setHydrated(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `}, [])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 155: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 156: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 157: `if (!hydrated) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 158: `setPreviewMsgs([`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `{ role: 'assistant', content: settings.welcomeMessage || DEFAULT_SETTINGS.welcomeMessage },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 160: `])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 161: `}, [hydrated, settings.welcomeMessage])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 162: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 163: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `previewEndRef.current?.scrollIntoView({ behavior: 'smooth' })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 165: `}, [previewMsgs.length, previewExpanded])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 167: `const persist = useCallback((next: AiAgentSettingsState) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 168: `setSettings(next)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `saveAiAgentSettings(next)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `}, [])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 172: `const onSave = () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 173: `setSaving(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 174: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 175: `saveAiAgentSettings(settings)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 176: `toast.success('配置已保存到本机')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 178: `setTimeout(() => setSaving(false), 280)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 179: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 180: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 181: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 182: `const onUpload = (e: React.ChangeEvent<HTMLInputElement>) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 183: `const f = e.target.files?.[0]`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 184: `e.target.value = ''   // e.target.value?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `if (!f) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 186: `const id = uid()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 187: `const row: KbFile = {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 188: `id,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `name: f.name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 190: `size: f.size,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 191: `status: 'processing',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 192: `uploadedAt: Date.now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 193: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 194: `persist({ ...settings, kbFiles: [...settings.kbFiles, row] })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `toast.success('已加入处理队列')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 196: `window.setTimeout(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 197: `const next = loadAiAgentSettings()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 198: `const kbFiles = next.kbFiles.map((k) =>`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 199: `k.id === id`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 200: `? { ...k, status: (Math.random() > 0.08 ? 'ready' : 'failed') as KbFile['status'] }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `: k,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 203: `persist({ ...next, kbFiles })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 204: `const done = kbFiles.find((k) => k.id === id)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 205: `if (done?.status === 'ready') toast.success(\`文档「${done.name}」已就绪\`)`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 206: `if (done?.status === 'failed') toast.error(\`${done?.name}」处理失败，可删除后重试\`)`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 207: `}, 1400)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 208: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 209: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 210: `const removeKb = (id: string) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 211: `persist({ ...settings, kbFiles: settings.kbFiles.filter((k) => k.id !== id) })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `toast.success('已移除')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 213: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 214: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 215: `const openExternal = (kind: 'dh' | 'vc') => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 216: `if (kind === 'dh') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 217: `setDhLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `window.setTimeout(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 219: `setDhLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 220: `persist({ ...settings, digitalHumanConfigured: true })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `toast.success('已标记数字人工作台（演示）')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `}, 900)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 223: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 225: `setVcLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `window.setTimeout(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 227: `setVcLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `persist({ ...settings, voiceCloneName: settings.voiceCloneName || '默认音色 · 旅行女声' })`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 229: `toast.success('已打开音色克隆（演示）')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 230: `}, 900)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 231: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 232: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 233: `const exportJson = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 234: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 235: `await navigator.clipboard.writeText(JSON.stringify(settings, null, 2))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 236: `toast.success('配置 JSON 已复制')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 237: `} catch {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `toast.error('复制失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 240: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 241: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 242: `const sendPreview = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 243: `const text = previewInput.trim()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 244: `if (!text || previewSending) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 245: `setPreviewSending(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `setPreviewInput('')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 247: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 248: `const sys =`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 249: `previewTab === 'kb'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 250: `? \`${settings.systemPrompt}\n\n【知识库模式】优先根据已上传文档作答；文档未覆盖时请明确说明并给出通用建议。\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 251: `: settings.systemPrompt`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 252: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 253: `const messages = [`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 254: `{ role: 'system' as const, content: sys },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 255: `...previewMsgs.map((m) => ({ role: m.role, content: m.content })),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 256: `{ role: 'user' as const, content: text },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 257: `]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 258: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 259: `const nextUser = [...previewMsgs, { role: 'user' as const, content: text }]`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 260: `setPreviewMsgs(nextUser)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 261: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 262: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 263: `const res = await fetch('/api/chat', {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 264: `method: 'POST',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 265: `headers: { 'Content-Type': 'application/json' },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 266: `body: JSON.stringify({`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 267: `messages: messages.map(({ role, content }) => ({ role, content })),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 268: `temperature: 0.6,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 269: `max_tokens: 900,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 270: `}),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 271: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 272: `const data = await res.json().catch(() => ({}))`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 273: `if (!res.ok || !data?.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 274: `const errText = data?.error || \`HTTP ${res.status}\``
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 275: `throw new Error(errText)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 276: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 277: `const reply = String(data?.reply || '(无内容)')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 278: `setPreviewMsgs((m) => [...m, { role: 'assistant', content: reply }])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 279: `const approx = Math.ceil((text.length + reply.length) / 4)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 280: `setSettings((prev) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 281: `const n = incrementStats(prev, approx)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 282: `saveAiAgentSettings(n)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 283: `return n`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 284: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 285: `} catch (e: any) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 286: `const reply = \`预览调用失败?{e?.message || '未知错误'}。你可先保存配置，并检查后?/api/chat 与模型密钥。\``
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 287: `setPreviewMsgs((m) => [...m, { role: 'assistant', content: reply }])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 288: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 289: `setPreviewSending(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 290: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 291: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 292: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 293: `const clearPreview = () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 294: `setPreviewMsgs([{ role: 'assistant', content: settings.welcomeMessage || DEFAULT_SETTINGS.welcomeMessage }])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 295: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 296: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 297: `const kbBadge = useMemo(() => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 298: `const n = settings.kbFiles.length`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 299: `const ready = settings.kbFiles.filter((k) => k.status === 'ready').length`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 300: `if (!n) return { text: '未上传', tone: 'bg-slate-100 text-slate-600 border-slate-200' }`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 301: `if (ready === n) return { text: '已就绪', tone: 'bg-emerald-50 text-emerald-800 border-emerald-200' }`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 302: `return { text: '处理中', tone: 'bg-amber-50 text-amber-900 border-amber-200' }`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 303: `}, [settings.kbFiles])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 304: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 305: `if (!hydrated) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 306: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 307: `<div className="min-h-screen flex items-center justify-center bg-gradient-to-b from-slate-50 to-sky-50">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 308: `<Loader2 className="h-8 w-8 animate-spin text-primary" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 309: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 310: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 311: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 312: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 313: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 314: `<div className="min-h-screen page-bg">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 315: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 316: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 317: `<main className="pt-16 pb-20">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 318: `<div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 319: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 320: `<nav className="mb-6 flex items-center gap-1.5 text-sm text-muted-foreground">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 321: `<Link href="/assistant" className="hover:text-primary transition-colors font-medium">AI 助手</Link>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 322: `<ChevronRight className="h-3.5 w-3.5 opacity-50" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 323: `<span className="text-foreground font-semibold">智能体配置中</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 324: `</nav>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 325: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 326: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 327: `<div className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-6 mb-10">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 328: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 329: `<div className="inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-3 py-1 text-xs font-semibold text-primary">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 330: `<Settings2 className="h-3.5 w-3.5" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 331: `智能体配置中`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 332: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 333: `<h1 className="mt-3 text-3xl font-extrabold tracking-tight text-foreground">智能体配置</h1>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 334: `<p className="mt-2 text-sm text-muted-foreground max-w-2xl leading-relaxed">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 335: `集中管理知识库、桌面扩展能力与联网工具开关；下方预览区用于快速调试对话。配置默认保存在浏览器本地，可导?JSON 备份?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 336: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 337: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 338: `<div className="flex flex-wrap gap-2 shrink-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 339: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 340: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 341: `onClick={exportJson}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 342: `className="btn btn-outline inline-flex items-center gap-2 active:scale-[0.98] transition hover:border-primary/50"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 343: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 344: `<Copy className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 345: `导出配置`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 346: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 347: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 348: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 349: `onClick={onSave}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 350: `disabled={saving}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 351: `className="travel-btn-gradient inline-flex items-center gap-2 px-5 py-2.5 text-sm font-semibold disabled:opacity-60 active:scale-[0.98] transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 352: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 353: `{saving ? <Loader2 className="h-4 w-4 animate-spin" /> : <Save className="h-4 w-4" />}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 354: `{saving ? '保存中...' : '保存并应用'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 355: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 356: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 357: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 358: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 359: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 360: `<section className="rounded-3xl border border-border/70 bg-card/80 backdrop-blur shadow-sm p-6 mb-8">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 361: `<div className="flex items-center justify-between gap-3 flex-wrap">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 362: `<h2 className="text-lg font-bold text-foreground flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 363: `<Sparkles className="h-5 w-5 text-violet-500" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 364: `人设与模?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 365: `</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 366: `{settings.systemPrompt.trim() ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 367: `<span className="inline-flex items-center gap-1 text-[12px] font-bold px-2.5 py-1 rounded-full border bg-emerald-50 text-emerald-700 border-emerald-200">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 368: `?已配?System Prompt`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 369: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 370: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 371: `<span className="inline-flex items-center gap-1 text-[12px] font-bold px-2.5 py-1 rounded-full border bg-amber-50 text-amber-700 border-amber-200">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 372: `⚠️ 未配?System Prompt`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 373: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 374: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 375: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 376: `<p className="text-sm text-muted-foreground mt-1">系统提示词、欢迎语与底层模型（展示用选项可与后端实际可用模型对齐）</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 377: `<div className="mt-5 grid grid-cols-1 md:grid-cols-2 gap-5">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 378: `<div className="md:col-span-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 379: `<label className="text-sm font-semibold text-foreground">系统提示词（System Prompt）</label>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 380: `<textarea`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 381: `className="input mt-1.5 min-h-[100px] w-full resize-y"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 382: `value={settings.systemPrompt}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 383: `onChange={(e) => setSettings((s) => ({ ...s, systemPrompt: e.target.value }))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `placeholder="定义助手人设、语气与边界"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 385: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 386: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 387: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 388: `<label className="text-sm font-semibold text-foreground">欢迎</label>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 389: `<textarea`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 390: `className="input mt-1.5 min-h-[72px] w-full"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 391: `value={settings.welcomeMessage}   // value?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 392: `onChange={(e) => setSettings((s) => ({ ...s, welcomeMessage: e.target.value }))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 393: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 394: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 395: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 396: `<label className="text-sm font-semibold text-foreground">对话模型</label>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 397: `<select`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 398: `className="input mt-1.5 w-full"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 399: `value={settings.modelId}   // value?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 400: `onChange={(e) => setSettings((s) => ({ ...s, modelId: e.target.value }))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 401: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 402: `{MODEL_OPTIONS.map((m) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 403: `<option key={m.id} value={m.id}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 404: `{m.label}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 405: `</option>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 406: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 407: `</select>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 408: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 409: `<div className="md:col-span-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 410: `<label className="text-sm font-semibold text-foreground">敏感词/ 输出过滤（每行一条，可选）</label>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 411: `<textarea`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 412: `className="input mt-1.5 min-h-[80px] w-full font-mono text-xs"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 413: `value={settings.filterRules}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 414: `onChange={(e) => setSettings((s) => ({ ...s, filterRules: e.target.value }))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 415: `placeholder="例如：竞品名A&#10;内部代号"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 416: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 417: `<p className="text-xs text-muted-foreground mt-1">当前为前端记录；真正拦截需在服务端审核链路实现</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 418: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 419: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 420: `</section>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 421: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 422: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 423: `<div className="mb-3 flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 424: `<h2 className="text-sm font-bold uppercase tracking-wide text-muted-foreground">工具能力</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 425: `<span className="h-px flex-1 bg-border/80" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 426: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 427: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 428: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-5 mb-8">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 429: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 430: `<div className="rounded-3xl border border-border/70 bg-card p-5 shadow-sm hover:shadow-md transition-shadow">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 431: `<div className="flex items-start justify-between gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 432: `<div className="flex items-center gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 433: `<div className="h-12 w-12 rounded-2xl bg-amber-100 text-amber-700 flex items-center justify-center ring-1 ring-amber-200/80">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 434: `<BookOpen className="h-6 w-6" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 435: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 436: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 437: `<h3 className="font-bold text-foreground">知识</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 438: `<p className="text-xs text-muted-foreground mt-0.5 max-w-[240px]">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 439: `上传文档，让助手基于你的资料回答问题?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 440: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 441: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 442: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 443: `<span className={\`text-[11px] font-bold px-2 py-0.5 rounded-full border ${kbBadge.tone}\`}>{kbBadge.text}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 444: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 445: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 446: `<p className="text-xs text-muted-foreground mt-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 447: `支持格式：<span className="font-medium text-foreground">PDF、Word（.doc/.docx）、TXT、Markdown</span>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 448: `（演示：任意文件会进入模拟解析队列）`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 449: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 450: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 451: `<input ref={fileRef} type="file" className="hidden" accept=".pdf,.doc,.docx,.txt,.md" onChange={onUpload} />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 452: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 453: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 454: `onClick={() => fileRef.current?.click()}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 455: `className="mt-3 w-full btn btn-outline inline-flex items-center justify-center gap-2 py-2.5 hover:bg-primary/5 hover:border-primary/40 active:scale-[0.99] transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 456: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 457: `<Upload className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 458: `上传文件`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 459: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 460: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 461: `<div className="mt-4 space-y-2 max-h-48 overflow-y-auto">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 462: `{settings.kbFiles.length === 0 ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 463: `<div className="text-sm text-muted-foreground py-6 text-center border border-dashed rounded-2xl">暂无文件</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 464: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 465: `settings.kbFiles.map((f) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 466: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 467: `key={f.id}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 468: `className="flex items-center justify-between gap-2 rounded-xl border border-border/60 bg-background/50 px-3 py-2 text-sm"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 469: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 470: `<div className="min-w-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 471: `<div className="font-medium truncate">{f.name}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 472: `<div className="text-xs text-muted-foreground">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 473: `{fmtBytes(f.size)} ·{' '}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 474: `{f.status === 'processing' ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 475: `<span className="text-amber-600">处理</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 476: `) : f.status === 'ready' ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 477: `<span className="text-emerald-600">就绪</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 478: `) : (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 479: `<span className="text-red-600">失败</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 480: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 481: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 482: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 483: `<DeleteConfirmButton onConfirm={() => removeKb(f.id)} label="删除文件" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 484: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 485: `))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 486: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 487: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 488: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 489: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 490: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 491: `<div className="rounded-3xl border border-border/70 bg-card p-5 shadow-sm hover:shadow-md transition-shadow">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 492: `<div className="flex items-start justify-between gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 493: `<div className="flex items-center gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 494: `<div className="h-12 w-12 rounded-2xl bg-violet-100 text-violet-700 flex items-center justify-center ring-1 ring-violet-200/80">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 495: `<UserCircle2 className="h-6 w-6" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 496: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 497: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 498: `<h3 className="font-bold text-foreground">数字</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 499: `<p className="text-xs text-muted-foreground mt-0.5">桌面数字人工作台，用于形象与动作配置</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 500: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 501: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 502: `<span`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 503: `className={\`text-[11px] font-bold px-2 py-0.5 rounded-full border ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 504: `settings.digitalHumanConfigured`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 505: `? 'bg-emerald-50 text-emerald-800 border-emerald-200'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 506: `: 'bg-slate-100 text-slate-600 border-slate-200'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 507: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 508: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 509: `{settings.digitalHumanConfigured ? '已配置' : '未配置'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 510: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 511: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 512: `<p className="text-xs text-muted-foreground mt-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 513: `<span className="font-medium text-foreground">什么是数字人？</span> 将助手以虚拟形象呈现，适合接待与讲解场景`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 514: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 515: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 516: `title="在桌面客户端完成摄像头与形象绑定"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 517: `className="ml-1 text-primary font-semibold hover:underline"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 518: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 519: `了解更多`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 520: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 521: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 522: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 523: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 524: `disabled={dhLoading}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 525: `onClick={() => openExternal('dh')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 526: `className="mt-3 w-full travel-btn-gradient inline-flex items-center justify-center gap-2 py-2.5 active:scale-[0.99] transition disabled:opacity-70"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 527: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 528: `{dhLoading ? <Loader2 className="h-4 w-4 animate-spin" /> : <ExternalLink className="h-4 w-4" />}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 529: `{dhLoading ? '正在打开...' : '进入桌面数字人工作台'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 530: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 531: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 532: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 533: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 534: `<div className="rounded-3xl border border-border/70 bg-card p-5 shadow-sm hover:shadow-md transition-shadow">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 535: `<div className="flex items-start justify-between gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 536: `<div className="flex items-center gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 537: `<div className="h-12 w-12 rounded-2xl bg-rose-100 text-rose-700 flex items-center justify-center ring-1 ring-rose-200/80">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 538: `<Mic2 className="h-6 w-6" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 539: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 540: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 541: `<h3 className="font-bold text-foreground">音色克隆</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 542: `<p className="text-xs text-muted-foreground mt-0.5">录制样本生成专属朗读音色</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 543: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 544: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 545: `<span`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 546: `className={\`text-[11px] font-bold px-2 py-0.5 rounded-full border ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 547: `settings.voiceCloneName ? 'bg-emerald-50 text-emerald-800 border-emerald-200' : 'bg-slate-100 text-slate-600 border-slate-200'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 548: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 549: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 550: `{settings.voiceCloneName ? '已克隆' : '未配置'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 551: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 552: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 553: `<div className="mt-4 rounded-xl border border-border/50 bg-muted/30 px-3 py-2 text-sm">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 554: `<span className="text-muted-foreground">当前默认音色</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 555: `<span className="font-medium">{settings.voiceCloneName || '未设置（使用系统默认）'}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 556: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 557: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 558: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 559: `disabled={vcLoading}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 560: `onClick={() => openExternal('vc')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 561: `className="mt-3 w-full btn btn-outline inline-flex items-center justify-center gap-2 py-2.5 hover:bg-rose-50/50 hover:border-rose-300/50 active:scale-[0.99] transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 562: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 563: `{vcLoading ? <Loader2 className="h-4 w-4 animate-spin" /> : <ExternalLink className="h-4 w-4" />}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 564: `{vcLoading ? '正在打开...' : '进入桌面音色克隆工作台'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 565: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 566: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 567: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 568: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 569: `<div className="rounded-3xl border border-border/70 bg-card p-5 shadow-sm hover:shadow-md transition-shadow">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 570: `<div className="flex items-center gap-3 mb-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 571: `<div className="h-12 w-12 rounded-2xl bg-cyan-100 text-cyan-800 flex items-center justify-center ring-1 ring-cyan-200/80">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 572: `<Globe className="h-6 w-6" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 573: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 574: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 575: `<h3 className="font-bold text-foreground">联网搜索</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 576: `<p className="text-xs text-muted-foreground">控制助手可调用哪些联网工具</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 577: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 578: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 579: `<div className="mt-4 space-y-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 580: `{(Object.keys(WEB_TOOL_META) as WebToolKey[]).map((key) => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 581: `const meta = WEB_TOOL_META[key]`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 582: `const Icon = meta.icon`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 583: `const on = settings.webTools[key]`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 584: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 585: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 586: `key={key}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 587: `className={\`flex items-center justify-between gap-3 rounded-2xl border px-3 py-2.5 transition-colors ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 588: `on ? 'border-cyan-200/80 bg-cyan-50/50' : 'border-border/60 bg-background/40'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 589: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 590: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 591: `<div className="flex items-center gap-2 min-w-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 592: `<Icon className={\`h-4 w-4 shrink-0 ${on ? 'text-cyan-600' : 'text-muted-foreground'}\`} />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 593: `<div className="min-w-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 594: `<div className="text-sm font-semibold flex items-center gap-1.5">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 595: `{meta.label}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 596: `<span title={meta.desc} className="inline-flex text-muted-foreground hover:text-foreground cursor-help">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 597: `<Info className="h-3.5 w-3.5" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 598: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 599: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 600: `<div className={\`text-[11px] mt-0.5 font-medium ${on ? 'text-cyan-600' : 'text-muted-foreground/60'}\`}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 601: `{on ? '🟢 已启用' : '⚪ 已关闭'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 602: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 603: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 604: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 605: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 606: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 607: `role="switch"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 608: `aria-checked={on}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 609: `onClick={() =>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 610: `setSettings((s) => ({`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 611: `...s,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 612: `webTools: { ...s.webTools, [key]: !s.webTools[key] },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 613: `}))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 614: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 615: `className={\`relative h-7 w-12 rounded-full transition-colors focus-visible:outline focus-visible:ring-2 focus-visible:ring-primary/40 ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 616: `on ? 'bg-cyan-500' : 'bg-muted'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 617: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 618: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 619: `<span`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 620: `className={\`absolute top-0.5 left-0.5 h-6 w-6 rounded-full bg-white shadow transition-transform ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 621: `on ? 'translate-x-5' : 'translate-x-0'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 622: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 623: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 624: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 625: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 626: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 627: `})}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 628: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 629: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 630: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 631: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 632: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 633: `<section className="rounded-3xl border border-border/70 bg-card/80 p-6 mb-8">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 634: `<h2 className="text-lg font-bold flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 635: `<Bot className="h-5 w-5 text-sky-600" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 636: `API 调用统计（本地累计）`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 637: `</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 638: `<p className="text-sm text-muted-foreground mt-1">预览区每次成功调用 /api/chat 后累加，便于观察用量（费用为粗略估算）</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 639: `<div className="mt-4 grid grid-cols-3 gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 640: `{[`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 641: `{ k: '调用次数', v: String(settings.stats.calls) },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 642: `{ k: 'Token（估算）', v: String(settings.stats.tokens) },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 643: `{ k: '费用（估算）', v: \`¥${settings.stats.estCost.toFixed(4)}\` },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 644: `].map((row) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 645: `<div key={row.k} className="rounded-2xl border border-border/60 bg-background/60 px-4 py-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 646: `<div className="text-xs text-muted-foreground">{row.k}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 647: `<div className="text-lg font-extrabold text-foreground mt-1">{row.v}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 648: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 649: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 650: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 651: `</section>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 652: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 653: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 654: `<div className="mb-3 flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 655: `<h2 className="text-sm font-bold uppercase tracking-wide text-muted-foreground">预览调试</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 656: `<span className="h-px flex-1 bg-border/80" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 657: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 658: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 659: `<section`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 660: `className={\`rounded-3xl border border-border/70 bg-card shadow-lg overflow-hidden ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 661: `previewExpanded ? 'fixed inset-3 z-[60] flex flex-col' : 'relative'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 662: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 663: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 664: `<div className="flex items-center justify-between gap-3 px-4 py-3 border-b border-border/60 bg-muted/30">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 665: `<div className="flex items-center gap-2 min-w-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 666: `<Sparkles className="h-5 w-5 text-primary shrink-0" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 667: `<span className="font-bold truncate">智能体在线预</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 668: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 669: `<div className="flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 670: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 671: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 672: `onClick={clearPreview}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 673: `className="text-xs font-semibold px-2 py-1 rounded-lg hover:bg-background/80 transition active:scale-95"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 674: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 675: `清空对话`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 676: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 677: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 678: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 679: `onClick={() => setPreviewExpanded((v) => !v)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 680: `className="p-2 rounded-xl border border-border/60 hover:bg-background/80 transition active:scale-95"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 681: `title={previewExpanded ? '退出全屏' : '全屏预览'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 682: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 683: `{previewExpanded ? <Minimize2 className="h-4 w-4" /> : <Maximize2 className="h-4 w-4" />}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 684: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 685: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 686: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 687: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 688: `<div className="px-4 pt-3 flex gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 689: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 690: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 691: `onClick={() => setPreviewTab('model')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 692: `className={\`px-4 py-2 rounded-xl text-sm font-semibold transition ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 693: `previewTab === 'model' ? 'bg-primary text-primary-foreground shadow-sm' : 'bg-muted/50 text-muted-foreground hover:text-foreground'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 694: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 695: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 696: `模型助手`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 697: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 698: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 699: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 700: `onClick={() => setPreviewTab('kb')}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 701: `className={\`px-4 py-2 rounded-xl text-sm font-semibold transition ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 702: `previewTab === 'kb' ? 'bg-primary text-primary-foreground shadow-sm' : 'bg-muted/50 text-muted-foreground hover:text-foreground'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 703: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 704: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 705: `知识库问?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 706: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 707: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 708: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 709: `<div className={\`px-4 py-3 space-y-3 ${previewExpanded ? 'flex-1 min-h-0 overflow-y-auto' : 'max-h-[360px] overflow-y-auto'}\`}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 710: `{previewMsgs.map((m, i) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 711: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 712: `key={\`${i}-${m.role}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 713: `className={\`flex ${m.role === 'user' ? 'justify-end' : 'justify-start'}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 714: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 715: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 716: `className={\`max-w-[92%] rounded-2xl px-3 py-2 text-sm whitespace-pre-wrap leading-relaxed ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 717: `m.role === 'user' ? 'bg-primary text-primary-foreground' : 'bg-muted/80 text-foreground border border-border/50'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 718: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 719: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 720: `{m.content}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 721: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 722: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 723: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 724: `<div ref={previewEndRef} />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 725: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 726: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 727: `<div className="p-4 border-t border-border/60 bg-background/50">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 728: `<div className="flex gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 729: `<input`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 730: `className="input flex-1"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 731: `placeholder={previewTab === 'kb' ? '基于知识库提问...' : '向模型助手发送消息...'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 732: `value={previewInput}   // value?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 733: `onChange={(e) => setPreviewInput(e.target.value)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 734: `onKeyDown={(e) => e.key === 'Enter' && !e.shiftKey && (e.preventDefault(), sendPreview())}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 735: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 736: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 737: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 738: `disabled={previewSending}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 739: `onClick={sendPreview}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 740: `className="travel-btn-gradient px-5 shrink-0 inline-flex items-center gap-2 disabled:opacity-60 active:scale-[0.98] transition"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 741: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 742: `{previewSending ? <Loader2 className="h-4 w-4 animate-spin" /> : <ChevronRight className="h-4 w-4" />}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 743: `发?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 744: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 745: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 746: `<p className="text-[11px] text-muted-foreground mt-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 747: `联网工具开关：<span className="font-medium">{settings.webTools.brave ? '搜索 ' : ''}</span>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 748: `<span className="font-medium">{settings.webTools.news ? '新闻 ' : ''}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 749: `<span className="font-medium">{settings.webTools.weather ? '天气' : ''}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 750: `{!settings.webTools.brave && !settings.webTools.news && !settings.webTools.weather ? '（均已关闭）' : ''}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 751: `（实际生效依赖后端工具接入）`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 752: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 753: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 754: `</section>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 755: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 756: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 757: `<div className="mt-6 rounded-3xl border-2 border-dashed border-border/50 bg-background/30 p-6 flex flex-col sm:flex-row items-center justify-between gap-4 hover:border-primary/30 hover:bg-primary/2 transition-colors group">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 758: `<div className="flex items-center gap-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 759: `<div className="h-12 w-12 rounded-2xl border-2 border-dashed border-border/60 flex items-center justify-center text-2xl group-hover:border-primary/40 transition-colors">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 760: `?`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 761: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 762: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 763: `<div className="font-bold text-foreground">创建更多智能</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 764: `<div className="text-sm text-muted-foreground mt-0.5">针对不同场景（文案写作、文档分析、行程规划）创建专属配置</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 765: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 766: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 767: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 768: `type="button"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 769: `className="btn btn-outline shrink-0 inline-flex items-center gap-2 hover:border-primary/50 hover:text-primary"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 770: `onClick={() => toast('多智能体管理功能即将上线 🎉', { icon: '🚀' })}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 771: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 772: `<Sparkles className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 773: `即将推出`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 774: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 775: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 776: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 777: `<div className="mt-8 flex flex-wrap gap-3 justify-between items-center text-sm text-muted-foreground">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 778: `<Link href="/assistant" className="inline-flex items-center gap-1 font-semibold text-primary hover:underline">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 779: `?返回 AI 助手`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 780: `</Link>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 781: `<button type="button" onClick={onSave} className="text-primary font-semibold hover:underline">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 782: `再次保存配置`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 783: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 784: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 785: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 786: `</main>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 787: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 788: `<Footer />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 789: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 790: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 791: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
