# `frontend/user-web/src/types/index.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 类型定义模块 - 智能旅游助手数据类型接口` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 本模块定义了项目中使用的所有 TypeScript 接口类型，包括：` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 用户相关：User, UserPreferences` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 产品相关：Product, ProductMetadata` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 行程相关：Itinerary, ItineraryDay, ItineraryActivity` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 订单相关：Order, OrderItem` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - AI服务：AIConversation, Recommendation` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 目的地评论：DestinationComment` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 搜索过滤：SearchFilters` -> 注释行，用于解释设计意图或使用说明。
- 14: `*/` -> 注释行，用于解释设计意图或使用说明。
- 15: `(空行)` -> 空行，用于提升代码结构可读性。
- 16: `/**` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 用户基本信息接口` -> 注释行，用于解释设计意图或使用说明。
- 18: `* @description 存储用户账户信息、偏好设置和会员等级` -> 注释行，用于解释设计意图或使用说明。
- 19: `*/` -> 注释行，用于解释设计意图或使用说明。
- 20: `export interface User {` -> 导出当前声明，供其他模块复用。
- 21: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 22: `phone: string` -> 执行当前语句，参与该文件整体逻辑。
- 23: `email: string` -> 执行当前语句，参与该文件整体逻辑。
- 24: `nickname: string` -> 执行当前语句，参与该文件整体逻辑。
- 25: `avatar_url?: string` -> 执行当前语句，参与该文件整体逻辑。
- 26: `membership_level: number` -> 执行当前语句，参与该文件整体逻辑。
- 27: `is_admin?: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 28: `preferences: UserPreferences` -> 执行当前语句，参与该文件整体逻辑。
- 29: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 30: `updated_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 31: `}` -> 结束当前语句或代码块。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `/**` -> 注释行，用于解释设计意图或使用说明。
- 34: `* 用户偏好设置接口` -> 注释行，用于解释设计意图或使用说明。
- 35: `* @description 定义用户的旅行偏好，包括预算范围、旅行风格等` -> 注释行，用于解释设计意图或使用说明。
- 36: `*/` -> 注释行，用于解释设计意图或使用说明。
- 37: `export interface UserPreferences {` -> 导出当前声明，供其他模块复用。
- 38: `destinations: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 39: `budget_range: {` -> 执行当前语句，参与该文件整体逻辑。
- 40: `min: number` -> 执行当前语句，参与该文件整体逻辑。
- 41: `max: number` -> 执行当前语句，参与该文件整体逻辑。
- 42: `}` -> 结束当前语句或代码块。
- 43: `travel_style: 'adventure' | 'relaxation' | 'cultural' | 'business'` -> 执行当前语句，参与该文件整体逻辑。
- 44: `group_size: number` -> 执行当前语句，参与该文件整体逻辑。
- 45: `interests: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 46: `}` -> 结束当前语句或代码块。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `/**` -> 注释行，用于解释设计意图或使用说明。
- 49: `* 旅游产品接口` -> 注释行，用于解释设计意图或使用说明。
- 50: `* @description 表示机票、酒店、门票、体验等旅游产品` -> 注释行，用于解释设计意图或使用说明。
- 51: `* @example  { type: 'hotel', name: '北京饭店', price: 500, location: { city: '北京' } }` -> 注释行，用于解释设计意图或使用说明。
- 52: `*/` -> 注释行，用于解释设计意图或使用说明。
- 53: `export interface Product {` -> 导出当前声明，供其他模块复用。
- 54: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 55: `type: 'flight' | 'hotel' | 'ticket' | 'experience'` -> 执行当前语句，参与该文件整体逻辑。
- 56: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 57: `subtitle?: string` -> 执行当前语句，参与该文件整体逻辑。
- 58: `description: string` -> 执行当前语句，参与该文件整体逻辑。
- 59: `price: number` -> 执行当前语句，参与该文件整体逻辑。
- 60: `original_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 61: `base_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 62: `discount_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 63: `inventory: number` -> 执行当前语句，参与该文件整体逻辑。
- 64: `inventory_available?: number` -> 执行当前语句，参与该文件整体逻辑。
- 65: `tags: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 66: `metadata: ProductMetadata` -> 执行当前语句，参与该文件整体逻辑。
- 67: `status: 'active' | 'inactive' | 'sold_out'` -> 执行当前语句，参与该文件整体逻辑。
- 68: `images: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 69: `// 兼容 Flask API 的 cover_image 字段` -> 注释行，用于解释设计意图或使用说明。
- 70: `cover_image?: string` -> 执行当前语句，参与该文件整体逻辑。
- 71: `// 兼容 Flask API 的 category/type 字段` -> 注释行，用于解释设计意图或使用说明。
- 72: `category?: string` -> 执行当前语句，参与该文件整体逻辑。
- 73: `location: {` -> 执行当前语句，参与该文件整体逻辑。
- 74: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 75: `country: string` -> 执行当前语句，参与该文件整体逻辑。
- 76: `address?: string` -> 执行当前语句，参与该文件整体逻辑。
- 77: `coordinates?: {` -> 执行当前语句，参与该文件整体逻辑。
- 78: `lat: number` -> 执行当前语句，参与该文件整体逻辑。
- 79: `lng: number` -> 执行当前语句，参与该文件整体逻辑。
- 80: `} | null` -> 执行当前语句，参与该文件整体逻辑。
- 81: `}` -> 结束当前语句或代码块。
- 82: `rating: number` -> 执行当前语句，参与该文件整体逻辑。
- 83: `review_count: number` -> 执行当前语句，参与该文件整体逻辑。
- 84: `sold_count?: number` -> 执行当前语句，参与该文件整体逻辑。
- 85: `}` -> 结束当前语句或代码块。
- 86: `(空行)` -> 空行，用于提升代码结构可读性。
- 87: `/**` -> 注释行，用于解释设计意图或使用说明。
- 88: `* 产品元数据接口` -> 注释行，用于解释设计意图或使用说明。
- 89: `* @description 根据产品类型包含不同的扩展信息（机票/酒店/门票/体验）` -> 注释行，用于解释设计意图或使用说明。
- 90: `*/` -> 注释行，用于解释设计意图或使用说明。
- 91: `export interface ProductMetadata {` -> 导出当前声明，供其他模块复用。
- 92: `// Flight specific` -> 注释行，用于解释设计意图或使用说明。
- 93: `airline?: string` -> 执行当前语句，参与该文件整体逻辑。
- 94: `flight_number?: string` -> 执行当前语句，参与该文件整体逻辑。
- 95: `departure_airport?: string` -> 执行当前语句，参与该文件整体逻辑。
- 96: `arrival_airport?: string` -> 执行当前语句，参与该文件整体逻辑。
- 97: `departure_time?: string` -> 执行当前语句，参与该文件整体逻辑。
- 98: `arrival_time?: string` -> 执行当前语句，参与该文件整体逻辑。
- 99: `duration?: string` -> 执行当前语句，参与该文件整体逻辑。
- 100: `(空行)` -> 空行，用于提升代码结构可读性。
- 101: `// Hotel specific` -> 注释行，用于解释设计意图或使用说明。
- 102: `star_rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 103: `amenities?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 104: `room_type?: string` -> 执行当前语句，参与该文件整体逻辑。
- 105: `check_in_time?: string` -> 执行当前语句，参与该文件整体逻辑。
- 106: `check_out_time?: string` -> 执行当前语句，参与该文件整体逻辑。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `// Ticket specific` -> 注释行，用于解释设计意图或使用说明。
- 109: `attraction_name?: string` -> 执行当前语句，参与该文件整体逻辑。
- 110: `opening_hours?: string` -> 执行当前语句，参与该文件整体逻辑。
- 111: `valid_days?: number` -> 执行当前语句，参与该文件整体逻辑。
- 112: `time_slots?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 113: `(空行)` -> 空行，用于提升代码结构可读性。
- 114: `// Experience specific` -> 注释行，用于解释设计意图或使用说明。
- 115: `experience_duration?: string` -> 执行当前语句，参与该文件整体逻辑。
- 116: `difficulty?: 'easy' | 'medium' | 'hard'` -> 执行当前语句，参与该文件整体逻辑。
- 117: `group_size_min?: number` -> 执行当前语句，参与该文件整体逻辑。
- 118: `group_size_max?: number` -> 执行当前语句，参与该文件整体逻辑。
- 119: `includes?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 120: `}` -> 结束当前语句或代码块。
- 121: `(空行)` -> 空行，用于提升代码结构可读性。
- 122: `/**` -> 注释行，用于解释设计意图或使用说明。
- 123: `* 旅行行程接口` -> 注释行，用于解释设计意图或使用说明。
- 124: `* @description 表示用户规划的多日行程，包含每天的活动安排` -> 注释行，用于解释设计意图或使用说明。
- 125: `*/` -> 注释行，用于解释设计意图或使用说明。
- 126: `export interface Itinerary {` -> 导出当前声明，供其他模块复用。
- 127: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 128: `user_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 129: `title: string` -> 执行当前语句，参与该文件整体逻辑。
- 130: `start_date: string` -> 执行当前语句，参与该文件整体逻辑。
- 131: `end_date: string` -> 执行当前语句，参与该文件整体逻辑。
- 132: `days: ItineraryDay[]` -> 执行当前语句，参与该文件整体逻辑。
- 133: `budget: number` -> 执行当前语句，参与该文件整体逻辑。
- 134: `tags: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 135: `is_public: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 136: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 137: `updated_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 138: `}` -> 结束当前语句或代码块。
- 139: `(空行)` -> 空行，用于提升代码结构可读性。
- 140: `export interface ItineraryDay {` -> 导出当前声明，供其他模块复用。
- 141: `day: number` -> 执行当前语句，参与该文件整体逻辑。
- 142: `date: string` -> 执行当前语句，参与该文件整体逻辑。
- 143: `activities: ItineraryActivity[]` -> 执行当前语句，参与该文件整体逻辑。
- 144: `meals: ItineraryMeal[]` -> 执行当前语句，参与该文件整体逻辑。
- 145: `accommodation?: ItineraryAccommodation` -> 执行当前语句，参与该文件整体逻辑。
- 146: `transportation?: Transportation[]` -> 执行当前语句，参与该文件整体逻辑。
- 147: `}` -> 结束当前语句或代码块。
- 148: `(空行)` -> 空行，用于提升代码结构可读性。
- 149: `export interface ItineraryActivity {` -> 导出当前声明，供其他模块复用。
- 150: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 151: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 152: `type: 'sightseeing' | 'dining' | 'shopping' | 'entertainment' | 'relaxation'` -> 执行当前语句，参与该文件整体逻辑。
- 153: `location: string` -> 执行当前语句，参与该文件整体逻辑。
- 154: `start_time: string` -> 执行当前语句，参与该文件整体逻辑。
- 155: `end_time: string` -> 执行当前语句，参与该文件整体逻辑。
- 156: `description: string` -> 执行当前语句，参与该文件整体逻辑。
- 157: `cost: number` -> 执行当前语句，参与该文件整体逻辑。
- 158: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 159: `images?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 160: `}` -> 结束当前语句或代码块。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `export interface ItineraryMeal {` -> 导出当前声明，供其他模块复用。
- 163: `type: 'breakfast' | 'lunch' | 'dinner' | 'snack'` -> 执行当前语句，参与该文件整体逻辑。
- 164: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 165: `location: string` -> 执行当前语句，参与该文件整体逻辑。
- 166: `cost: number` -> 执行当前语句，参与该文件整体逻辑。
- 167: `cuisine: string` -> 执行当前语句，参与该文件整体逻辑。
- 168: `}` -> 结束当前语句或代码块。
- 169: `(空行)` -> 空行，用于提升代码结构可读性。
- 170: `export interface ItineraryAccommodation {` -> 导出当前声明，供其他模块复用。
- 171: `hotel_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 172: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 173: `check_in: string` -> 执行当前语句，参与该文件整体逻辑。
- 174: `check_out: string` -> 执行当前语句，参与该文件整体逻辑。
- 175: `room_type: string` -> 执行当前语句，参与该文件整体逻辑。
- 176: `cost_per_night: number` -> 执行当前语句，参与该文件整体逻辑。
- 177: `total_cost: number` -> 执行当前语句，参与该文件整体逻辑。
- 178: `}` -> 结束当前语句或代码块。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `export interface Transportation {` -> 导出当前声明，供其他模块复用。
- 181: `type: 'flight' | 'train' | 'bus' | 'car' | 'taxi'` -> 执行当前语句，参与该文件整体逻辑。
- 182: `from: string` -> 执行当前语句，参与该文件整体逻辑。
- 183: `to: string` -> 执行当前语句，参与该文件整体逻辑。
- 184: `departure_time: string` -> 执行当前语句，参与该文件整体逻辑。
- 185: `arrival_time: string` -> 执行当前语句，参与该文件整体逻辑。
- 186: `cost: number` -> 执行当前语句，参与该文件整体逻辑。
- 187: `provider?: string` -> 执行当前语句，参与该文件整体逻辑。
- 188: `booking_reference?: string` -> 执行当前语句，参与该文件整体逻辑。
- 189: `}` -> 结束当前语句或代码块。
- 190: `(空行)` -> 空行，用于提升代码结构可读性。
- 191: `/**` -> 注释行，用于解释设计意图或使用说明。
- 192: `* 订单接口` -> 注释行，用于解释设计意图或使用说明。
- 193: `* @description 表示用户的旅游产品订单，包含多个订单项` -> 注释行，用于解释设计意图或使用说明。
- 194: `*/` -> 注释行，用于解释设计意图或使用说明。
- 195: `export interface Order {` -> 导出当前声明，供其他模块复用。
- 196: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 197: `user_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 198: `order_no: string` -> 执行当前语句，参与该文件整体逻辑。
- 199: `items: OrderItem[]` -> 执行当前语句，参与该文件整体逻辑。
- 200: `total_amount: number` -> 执行当前语句，参与该文件整体逻辑。
- 201: `status: 'pending' | 'paid' | 'cancelled' | 'completed' | 'refunded'` -> 执行当前语句，参与该文件整体逻辑。
- 202: `payment_method: 'alipay' | 'wechat' | 'credit_card' | 'bank_transfer'` -> 执行当前语句，参与该文件整体逻辑。
- 203: `payment_time?: string` -> 执行当前语句，参与该文件整体逻辑。
- 204: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 205: `updated_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 206: `}` -> 结束当前语句或代码块。
- 207: `(空行)` -> 空行，用于提升代码结构可读性。
- 208: `export interface OrderItem {` -> 导出当前声明，供其他模块复用。
- 209: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 210: `product_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 211: `product_name: string` -> 执行当前语句，参与该文件整体逻辑。
- 212: `product_type: 'flight' | 'hotel' | 'ticket' | 'experience'` -> 执行当前语句，参与该文件整体逻辑。
- 213: `quantity: number` -> 执行当前语句，参与该文件整体逻辑。
- 214: `unit_price: number` -> 执行当前语句，参与该文件整体逻辑。
- 215: `total_price: number` -> 执行当前语句，参与该文件整体逻辑。
- 216: `booking_details: any` -> 执行当前语句，参与该文件整体逻辑。
- 217: `}` -> 结束当前语句或代码块。
- 218: `(空行)` -> 空行，用于提升代码结构可读性。
- 219: `/**` -> 注释行，用于解释设计意图或使用说明。
- 220: `* AI对话记录接口` -> 注释行，用于解释设计意图或使用说明。
- 221: `* @description 存储用户与AI助手的对话历史` -> 注释行，用于解释设计意图或使用说明。
- 222: `*/` -> 注释行，用于解释设计意图或使用说明。
- 223: `export interface AIConversation {` -> 导出当前声明，供其他模块复用。
- 224: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 225: `user_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 226: `session_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 227: `role: 'user' | 'assistant'` -> 执行当前语句，参与该文件整体逻辑。
- 228: `content: string` -> 执行当前语句，参与该文件整体逻辑。
- 229: `intent?: string` -> 执行当前语句，参与该文件整体逻辑。
- 230: `metadata?: Record<string, any>` -> 执行当前语句，参与该文件整体逻辑。
- 231: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 232: `}` -> 结束当前语句或代码块。
- 233: `(空行)` -> 空行，用于提升代码结构可读性。
- 234: `export interface Recommendation {` -> 导出当前声明，供其他模块复用。
- 235: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 236: `user_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 237: `type: 'product' | 'destination' | 'itinerary'` -> 执行当前语句，参与该文件整体逻辑。
- 238: `target_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 239: `score: number` -> 执行当前语句，参与该文件整体逻辑。
- 240: `reason: string` -> 执行当前语句，参与该文件整体逻辑。
- 241: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 242: `expires_at?: string` -> 执行当前语句，参与该文件整体逻辑。
- 243: `}` -> 结束当前语句或代码块。
- 244: `(空行)` -> 空行，用于提升代码结构可读性。
- 245: `/**` -> 注释行，用于解释设计意图或使用说明。
- 246: `* 应用通知接口` -> 注释行，用于解释设计意图或使用说明。
- 247: `* @description 表示用户收到的系统通知` -> 注释行，用于解释设计意图或使用说明。
- 248: `*/` -> 注释行，用于解释设计意图或使用说明。
- 249: `export interface AppNotification {` -> 导出当前声明，供其他模块复用。
- 250: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 251: `user_id: number` -> 执行当前语句，参与该文件整体逻辑。
- 252: `title: string` -> 执行当前语句，参与该文件整体逻辑。
- 253: `content: string` -> 执行当前语句，参与该文件整体逻辑。
- 254: `notification_type: string` -> 执行当前语句，参与该文件整体逻辑。
- 255: `is_read: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 256: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 257: `read_at?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 258: `}` -> 结束当前语句或代码块。
- 259: `(空行)` -> 空行，用于提升代码结构可读性。
- 260: `export interface DestinationComment {` -> 导出当前声明，供其他模块复用。
- 261: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 262: `destination_id: number` -> 执行当前语句，参与该文件整体逻辑。
- 263: `user_id: string` -> 执行当前语句，参与该文件整体逻辑。
- 264: `content: string` -> 执行当前语句，参与该文件整体逻辑。
- 265: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 266: `updated_at?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 267: `user?: {` -> 执行当前语句，参与该文件整体逻辑。
- 268: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 269: `nickname: string` -> 执行当前语句，参与该文件整体逻辑。
- 270: `avatar_url?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 271: `}` -> 结束当前语句或代码块。
- 272: `}` -> 结束当前语句或代码块。
- 273: `(空行)` -> 空行，用于提升代码结构可读性。
- 274: `export interface SearchFilters {` -> 导出当前声明，供其他模块复用。
- 275: `destination?: string` -> 执行当前语句，参与该文件整体逻辑。
- 276: `start_date?: string` -> 执行当前语句，参与该文件整体逻辑。
- 277: `end_date?: string` -> 执行当前语句，参与该文件整体逻辑。
- 278: `budget_min?: number` -> 执行当前语句，参与该文件整体逻辑。
- 279: `budget_max?: number` -> 执行当前语句，参与该文件整体逻辑。
- 280: `travel_style?: string` -> 执行当前语句，参与该文件整体逻辑。
- 281: `group_size?: number` -> 执行当前语句，参与该文件整体逻辑。
- 282: `tags?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 283: `rating_min?: number` -> 执行当前语句，参与该文件整体逻辑。
- 284: `}` -> 结束当前语句或代码块。
