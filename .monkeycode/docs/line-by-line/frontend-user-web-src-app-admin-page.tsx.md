# `frontend/user-web/src/app/admin/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 管理后台首页模块 - 数据统计与概览` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 平台数据统计展示（目的地/用户/行程/订单数等）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 各省份目的地分布图表` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 热门城市排行` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 行程状态分布` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 订单状态与金额统计` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 会员等级分布` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 最近活动列表（用户/行程/评论/订单）` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - 目的地管理入口` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 管理员权限校验（AdminGuard）` -> 注释行，用于解释设计意图或使用说明。
- 16: `*` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 19: `* - AdminGuard: 管理员权限守卫组件` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - 图表库（内联实现）` -> 注释行，用于解释设计意图或使用说明。
- 21: `*` -> 注释行，用于解释设计意图或使用说明。
- 22: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 23: `* - GET /api/admin/stats: 获取平台统计数据` -> 注释行，用于解释设计意图或使用说明。
- 24: `* - GET /api/destinations?limit=1000: 获取目的地列表` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - GET /api/users?limit=100: 获取用户列表` -> 注释行，用于解释设计意图或使用说明。
- 26: `* - GET /api/orders?limit=100: 获取订单列表` -> 注释行，用于解释设计意图或使用说明。
- 27: `* - GET /api/comments?limit=100: 获取评论列表` -> 注释行，用于解释设计意图或使用说明。
- 28: `*` -> 注释行，用于解释设计意图或使用说明。
- 29: `* 【认证方式】` -> 注释行，用于解释设计意图或使用说明。
- 30: `* - admin_token: 存储在 localStorage` -> 注释行，用于解释设计意图或使用说明。
- 31: `* - Authorization: Bearer token 请求头` -> 注释行，用于解释设计意图或使用说明。
- 32: `*/` -> 注释行，用于解释设计意图或使用说明。
- 33: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 34: `(空行)` -> 空行，用于提升代码结构可读性。
- 35: `import { useEffect, useState, useCallback } from 'react'` -> 导入依赖模块，供当前文件使用。
- 36: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 37: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 38: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 39: `import {` -> 导入依赖模块，供当前文件使用。
- 40: `BarChart3, Loader2, MapPin, Users, Calendar, FileText, MessageSquare,` -> 执行当前语句，参与该文件整体逻辑。
- 41: `ShoppingBag, Footprints, Bell, Settings, Search, ChevronLeft, ChevronRight,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `Edit2, Save, X, TrendingUp, Eye, Trash2, RefreshCw, Database, AlertCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 43: `Upload, ImagePlus` -> 执行当前语句，参与该文件整体逻辑。
- 44: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 45: `import { AdminGuard } from '@/components/AdminGuard'` -> 导入依赖模块，供当前文件使用。
- 46: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `/** 后端地址 */` -> 注释行，用于解释设计意图或使用说明。
- 49: `const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 50: `(空行)` -> 空行，用于提升代码结构可读性。
- 51: `/* ==================== 类型定义 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `type Stats = {` -> 定义类型或类结构，约束数据与行为。
- 54: `destinations: number; users: number; trips: number; pages?: number;` -> 执行当前语句，参与该文件整体逻辑。
- 55: `configs?: number; comments?: number; orders?: number; footprints?: number;` -> 执行当前语句，参与该文件整体逻辑。
- 56: `notifications?: number; menus?: number;` -> 执行当前语句，参与该文件整体逻辑。
- 57: `province_distribution?: { province: string; count: number }[]` -> 执行当前语句，参与该文件整体逻辑。
- 58: `city_top?: { city: string; count: number; province?: string }[]` -> 执行当前语句，参与该文件整体逻辑。
- 59: `trip_status?: { status: string; count: number }[]` -> 执行当前语句，参与该文件整体逻辑。
- 60: `order_status?: { status: string; count: number; amount: number }[]` -> 执行当前语句，参与该文件整体逻辑。
- 61: `recent_users?: any[]` -> 执行当前语句，参与该文件整体逻辑。
- 62: `recent_trips?: any[]` -> 执行当前语句，参与该文件整体逻辑。
- 63: `recent_comments?: any[]` -> 执行当前语句，参与该文件整体逻辑。
- 64: `recent_orders?: any[]` -> 执行当前语句，参与该文件整体逻辑。
- 65: `rating_summary?: { avg_rating: number; total_rated: number }` -> 执行当前语句，参与该文件整体逻辑。
- 66: `}` -> 结束当前语句或代码块。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `type Destination = {` -> 定义类型或类结构，约束数据与行为。
- 69: `id: number; name: string; city: string; province: string;` -> 执行当前语句，参与该文件整体逻辑。
- 70: `description: string; cover_image: string; rating: number;` -> 执行当前语句，参与该文件整体逻辑。
- 71: `ticket_price: number; open_time: string; created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 72: `}` -> 结束当前语句或代码块。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `type AdminUser = {` -> 定义类型或类结构，约束数据与行为。
- 75: `id: string; username: string; nickname: string; email: string;` -> 执行当前语句，参与该文件整体逻辑。
- 76: `phone: string; avatar_url: string; is_admin: boolean;` -> 执行当前语句，参与该文件整体逻辑。
- 77: `membership_level: number; created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 78: `}` -> 结束当前语句或代码块。
- 79: `(空行)` -> 空行，用于提升代码结构可读性。
- 80: `/* ==================== 工具函数 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 81: `(空行)` -> 空行，用于提升代码结构可读性。
- 82: `function adminHeaders() {` -> 定义函数或方法，实现具体业务逻辑。
- 83: `const token = typeof window !== 'undefined' ? localStorage.getItem('admin_token') : null` -> 声明变量或常量，保存运行时数据。
- 84: `const h: Record<string, string> = { 'Content-Type': 'application/json' }` -> 声明变量或常量，保存运行时数据。
- 85: `if (token) h['Authorization'] = \`Bearer ${token}\`` -> 条件判断分支，根据场景执行不同逻辑。
- 86: `return h` -> 返回结果或提前结束当前流程。
- 87: `}` -> 结束当前语句或代码块。
- 88: `(空行)` -> 空行，用于提升代码结构可读性。
- 89: `async function adminFetch<T>(path: string, options?: RequestInit): Promise<T> {` -> 定义函数或方法，实现具体业务逻辑。
- 90: `const url = \`${API_BASE}${path}\`` -> 声明变量或常量，保存运行时数据。
- 91: `const res = await fetch(url, {` -> 声明变量或常量，保存运行时数据。
- 92: `...options,` -> 执行当前语句，参与该文件整体逻辑。
- 93: `headers: { ...adminHeaders(), ...(options?.headers as Record<string, string>) },` -> 执行当前语句，参与该文件整体逻辑。
- 94: `})` -> 执行当前语句，参与该文件整体逻辑。
- 95: `if (!res.ok) {` -> 条件判断分支，根据场景执行不同逻辑。
- 96: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 97: `throw new Error((data as any)?.error || \`HTTP ${res.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 98: `}` -> 结束当前语句或代码块。
- 99: `return res.json()` -> 返回结果或提前结束当前流程。
- 100: `}` -> 结束当前语句或代码块。
- 101: `(空行)` -> 空行，用于提升代码结构可读性。
- 102: `/* ==================== 统计卡片 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 103: `(空行)` -> 空行，用于提升代码结构可读性。
- 104: `function StatCard({ icon: Icon, label, value, color, subText }: {` -> 定义函数或方法，实现具体业务逻辑。
- 105: `icon: any; label: string; value: number | string; color: string; subText?: string` -> 执行当前语句，参与该文件整体逻辑。
- 106: `}) {` -> 执行当前语句，参与该文件整体逻辑。
- 107: `const colorMap: Record<string, { bg: string; icon: string; text: string }> = {` -> 声明变量或常量，保存运行时数据。
- 108: `blue:   { bg: 'from-blue-500 to-blue-600', icon: 'text-blue-500', text: 'text-blue-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 109: `green:  { bg: 'from-emerald-500 to-emerald-600', icon: 'text-emerald-500', text: 'text-emerald-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 110: `purple: { bg: 'from-purple-500 to-purple-600', icon: 'text-purple-500', text: 'text-purple-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 111: `orange: { bg: 'from-orange-500 to-orange-600', icon: 'text-orange-500', text: 'text-orange-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 112: `red:    { bg: 'from-red-500 to-red-600', icon: 'text-red-500', text: 'text-red-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 113: `pink:   { bg: 'from-pink-500 to-pink-600', icon: 'text-pink-500', text: 'text-pink-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 114: `cyan:   { bg: 'from-cyan-500 to-cyan-600', icon: 'text-cyan-500', text: 'text-cyan-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 115: `indigo: { bg: 'from-indigo-500 to-indigo-600', icon: 'text-indigo-500', text: 'text-indigo-600' },` -> 执行当前语句，参与该文件整体逻辑。
- 116: `}` -> 结束当前语句或代码块。
- 117: `const c = colorMap[color] || colorMap.blue` -> 声明变量或常量，保存运行时数据。
- 118: `return (` -> 返回结果或提前结束当前流程。
- 119: `<div className="group relative overflow-hidden rounded-2xl border border-gray-200/80 bg-white p-5 shadow-sm hover:shadow-md transition-all duration-300">` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `{/* Logo移到右上角 */}` -> 执行当前语句，参与该文件整体逻辑。
- 121: `<div className={\`absolute top-3 right-3 flex h-10 w-10 items-center justify-center rounded-xl bg-gradient-to-br ${c.bg} shadow-sm\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `<Icon className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 124: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 125: `<p className="text-xs font-medium uppercase tracking-wider text-gray-400 pr-12">{label}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 126: `<p className="mt-2 text-3xl font-extrabold tracking-tight text-gray-900">{value.toLocaleString()}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 127: `{subText && <p className="mt-1 text-xs text-gray-400">{subText}</p>}` -> 执行当前语句，参与该文件整体逻辑。
- 128: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 130: `)` -> 结束当前语句或代码块。
- 131: `}` -> 结束当前语句或代码块。
- 132: `(空行)` -> 空行，用于提升代码结构可读性。
- 133: `/* ==================== 省份分布条形图组件（可点击展开）==================== */` -> 注释行，用于解释设计意图或使用说明。
- 134: `(空行)` -> 空行，用于提升代码结构可读性。
- 135: `type CityInProvince = { city: string; count: number }` -> 定义类型或类结构，约束数据与行为。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `function ProvinceChart({ data }: { data: { province: string; count: number }[] }) {` -> 定义函数或方法，实现具体业务逻辑。
- 138: `const [expanded, setExpanded] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 139: `const [cities, setCities] = useState<CityInProvince[]>([])` -> 声明变量或常量，保存运行时数据。
- 140: `const [loadingCities, setLoadingCities] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 141: `if (!data.length) return <p className="text-sm text-gray-400">暂无数据</p>` -> 条件判断分支，根据场景执行不同逻辑。
- 142: `const maxCount = Math.max(...data.map(d => d.count), 1)` -> 声明变量或常量，保存运行时数据。
- 143: `(空行)` -> 空行，用于提升代码结构可读性。
- 144: `const toggleProvince = async (province: string) => {` -> 声明变量或常量，保存运行时数据。
- 145: `if (expanded === province) { setExpanded(null); return }` -> 条件判断分支，根据场景执行不同逻辑。
- 146: `setExpanded(province); setLoadingCities(true)` -> 执行当前语句，参与该文件整体逻辑。
- 147: `try {` -> 异常处理逻辑，提升程序健壮性。
- 148: `// 从省份详情API获取该省城市列表` -> 注释行，用于解释设计意图或使用说明。
- 149: `const res = await adminFetch<{ success: boolean; data: Array<{ province: string; cities: CityInProvince[] }> }>('/api/admin/geo/provinces')` -> 声明变量或常量，保存运行时数据。
- 150: `const provData = res.data?.find(p => p.province === province)` -> 声明变量或常量，保存运行时数据。
- 151: `setCities(provData?.cities || [])` -> 执行当前语句，参与该文件整体逻辑。
- 152: `} catch { setCities([]) } finally { setLoadingCities(false) }` -> 执行当前语句，参与该文件整体逻辑。
- 153: `}` -> 结束当前语句或代码块。
- 154: `(空行)` -> 空行，用于提升代码结构可读性。
- 155: `return (` -> 返回结果或提前结束当前流程。
- 156: `<div className="space-y-2.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 157: `{data.map((item) => {` -> 执行当前语句，参与该文件整体逻辑。
- 158: `const isOpen = expanded === item.province` -> 声明变量或常量，保存运行时数据。
- 159: `return (` -> 返回结果或提前结束当前流程。
- 160: `<div key={item.province}>` -> JSX/HTML 结构行，用于描述页面元素。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 163: `onClick={() => toggleProvince(item.province)}` -> 执行当前语句，参与该文件整体逻辑。
- 164: `className={\`group flex items-center gap-3 cursor-pointer rounded-lg px-2 py-1 transition-colors ${isOpen ? 'bg-blue-50' : 'hover:bg-gray-50'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 165: `title={isOpen ? '收起' : \`查看 ${item.province} 的所有城市\`}` -> 执行当前语句，参与该文件整体逻辑。
- 166: `>` -> 执行当前语句，参与该文件整体逻辑。
- 167: `(空行)` -> 空行，用于提升代码结构可读性。
- 168: `<svg className={\`w-3 h-3 text-gray-400 transition-transform shrink-0 ${isOpen ? 'rotate-90' : ''}\`} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" /></svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 169: `<span className="w-14 truncate text-xs font-medium text-gray-600 group-hover:text-blue-600 shrink-0">{item.province}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 170: `<div className="flex-1 h-6 bg-gray-100 rounded-full overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 171: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 172: `className="h-full rounded-full bg-gradient-to-r from-blue-500 to-indigo-500 transition-all duration-500 flex items-center justify-end pr-2"` -> 执行当前语句，参与该文件整体逻辑。
- 173: `style={{ width: \`${(item.count / maxCount) * 100}%\` }}` -> 执行当前语句，参与该文件整体逻辑。
- 174: `>` -> 执行当前语句，参与该文件整体逻辑。
- 175: `<span className="text-[10px] font-semibold text-white drop-shadow">{item.count} 处</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `(空行)` -> 空行，用于提升代码结构可读性。
- 181: `{isOpen && (` -> 执行当前语句，参与该文件整体逻辑。
- 182: `<div className="ml-8 mt-1 mb-2 pl-3 border-l-2 border-blue-200 space-y-1 animate-in slide-in-from-left-2 duration-200">` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `{loadingCities ? (` -> 执行当前语句，参与该文件整体逻辑。
- 184: `<div className="py-3 text-center"><Loader2 className="h-4 w-4 animate-spin mx-auto text-blue-400" /></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 185: `) : cities.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 186: `<p className="py-2 text-xs text-gray-400">无城市数据</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 187: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 188: `cities.map(c => (` -> 执行当前语句，参与该文件整体逻辑。
- 189: `<CityLinkRow key={c.city} city={c.city} count={c.count} />` -> JSX/HTML 结构行，用于描述页面元素。
- 190: `))` -> 执行当前语句，参与该文件整体逻辑。
- 191: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 192: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 193: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 194: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 195: `)` -> 结束当前语句或代码块。
- 196: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 197: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 198: `)` -> 结束当前语句或代码块。
- 199: `}` -> 结束当前语句或代码块。
- 200: `(空行)` -> 空行，用于提升代码结构可读性。
- 201: `/* 城市链接行：点击跳转景点列表 */` -> 注释行，用于解释设计意图或使用说明。
- 202: `function CityLinkRow({ city, count }: { city: string; count: number }) {` -> 定义函数或方法，实现具体业务逻辑。
- 203: `return (` -> 返回结果或提前结束当前流程。
- 204: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 205: `href={\`/admin/destinations?city=${encodeURIComponent(city)}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 206: `className="flex items-center justify-between py-1.5 px-2 rounded-md hover:bg-green-50 hover:text-green-700 text-xs text-gray-600 transition-colors group/city"` -> 执行当前语句，参与该文件整体逻辑。
- 207: `>` -> 执行当前语句，参与该文件整体逻辑。
- 208: `<span className="flex items-center gap-1.5 truncate max-w-[140px]">` -> JSX/HTML 结构行，用于描述页面元素。
- 209: `<MapPin className="h-3 w-3 text-green-400 group-hover/city:text-green-500 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 210: `<span className="truncate group-hover/city:font-medium">{city}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 211: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 212: `<span className="shrink-0 tabular-nums text-[11px] text-gray-400 group-hover/city:text-green-600">{count} 处</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 213: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 214: `)` -> 结束当前语句或代码块。
- 215: `}` -> 结束当前语句或代码块。
- 216: `(空行)` -> 空行，用于提升代码结构可读性。
- 217: `/* 城市排行组件（可点击跳转）*/` -> 注释行，用于解释设计意图或使用说明。
- 218: `function CityRanking({ data, onCityClick }: { data: { city: string; count: number; province?: string }[]; onCityClick?: (city: string) => void }) {` -> 定义函数或方法，实现具体业务逻辑。
- 219: `if (!data.length) return <p className="text-sm text-gray-400">暂无数据</p>` -> 条件判断分支，根据场景执行不同逻辑。
- 220: `return (` -> 返回结果或提前结束当前流程。
- 221: `<div className="space-y-1.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 222: `{(data || []).slice(0, 8).map((city, idx) => (` -> 执行当前语句，参与该文件整体逻辑。
- 223: `<Link key={city.city}` -> 执行当前语句，参与该文件整体逻辑。
- 224: `href={\`/admin/destinations?city=${encodeURIComponent(city.city)}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 225: `className="group/city flex items-center justify-between py-1.5 px-2 rounded-lg hover:bg-green-50 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 226: `>` -> 执行当前语句，参与该文件整体逻辑。
- 227: `<div className="flex items-center gap-2 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 228: `<span className={\`w-5 h-5 flex items-center justify-center rounded-full text-[10px] font-bold shrink-0 ${` -> 执行当前语句，参与该文件整体逻辑。
- 229: `idx < 3 ? 'bg-gradient-to-br from-green-400 to-emerald-500 text-white' : 'bg-gray-100 text-gray-500'` -> 执行当前语句，参与该文件整体逻辑。
- 230: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 231: `{idx + 1}` -> 执行当前语句，参与该文件整体逻辑。
- 232: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 233: `<MapPin className="h-3 w-3 text-gray-300 group-hover/city:text-green-400 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `<span className="text-sm text-gray-700 group-hover/city:text-green-700 group-hover/city:font-medium truncate">{city.city}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `<span className="text-xs font-semibold text-gray-400 group-hover/city:text-green-600 shrink-0 tabular-nums ml-2">{city.count} 处</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 238: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 239: `{(data || []).length === 0 && <p className="text-sm text-gray-400 text-center py-2">暂无数据</p>}` -> 执行当前语句，参与该文件整体逻辑。
- 240: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 241: `)` -> 结束当前语句或代码块。
- 242: `}` -> 结束当前语句或代码块。
- 243: `(空行)` -> 空行，用于提升代码结构可读性。
- 244: `/* ==================== 目的地管理表格 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 245: `(空行)` -> 空行，用于提升代码结构可读性。
- 246: `function DestinationTable() {` -> 定义函数或方法，实现具体业务逻辑。
- 247: `const [destinations, setDestinations] = useState<Destination[]>([])` -> 声明变量或常量，保存运行时数据。
- 248: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 249: `const [page, setPage] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 250: `const [total, setTotal] = useState(0)` -> 声明变量或常量，保存运行时数据。
- 251: `const [keyword, setKeyword] = useState('')` -> 声明变量或常量，保存运行时数据。
- 252: `const [editingId, setEditingId] = useState<number | null>(null)` -> 声明变量或常量，保存运行时数据。
- 253: `const [editData, setEditData] = useState<Partial<Destination>>({})` -> 声明变量或常量，保存运行时数据。
- 254: `const [searchTimer, setSearchTimer] = useState<any>(null)` -> 声明变量或常量，保存运行时数据。
- 255: `/* 图片上传状态 */` -> 注释行，用于解释设计意图或使用说明。
- 256: `const [uploadingId, setUploadingId] = useState<number | null>(null)` -> 声明变量或常量，保存运行时数据。
- 257: `const perPage = 8` -> 声明变量或常量，保存运行时数据。
- 258: `(空行)` -> 空行，用于提升代码结构可读性。
- 259: `const fetchDestinations = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 260: `try {` -> 异常处理逻辑，提升程序健壮性。
- 261: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 262: `const params = new URLSearchParams({ page: String(page), per_page: String(perPage) })` -> 声明变量或常量，保存运行时数据。
- 263: `if (keyword.trim()) params.set('keyword', keyword.trim())` -> 条件判断分支，根据场景执行不同逻辑。
- 264: `(空行)` -> 空行，用于提升代码结构可读性。
- 265: `// 使用原生fetch添加错误处理` -> 注释行，用于解释设计意图或使用说明。
- 266: `const token = typeof window !== 'undefined' ? localStorage.getItem('admin_token') : ''` -> 声明变量或常量，保存运行时数据。
- 267: `const url = \`${API_BASE}/api/admin/destinations?${params.toString()}\`` -> 声明变量或常量，保存运行时数据。
- 268: `(空行)` -> 空行，用于提升代码结构可读性。
- 269: `const res = await fetch(url, {` -> 声明变量或常量，保存运行时数据。
- 270: `headers: token ? { 'Authorization': \`Bearer ${token}\` } : {}` -> 执行当前语句，参与该文件整体逻辑。
- 271: `})` -> 执行当前语句，参与该文件整体逻辑。
- 272: `(空行)` -> 空行，用于提升代码结构可读性。
- 273: `if (!res.ok) {` -> 条件判断分支，根据场景执行不同逻辑。
- 274: `throw new Error(\`HTTP ${res.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 275: `}` -> 结束当前语句或代码块。
- 276: `(空行)` -> 空行，用于提升代码结构可读性。
- 277: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 278: `console.log('API Response:', data) // 调试日志` -> 执行当前语句，参与该文件整体逻辑。
- 279: `(空行)` -> 空行，用于提升代码结构可读性。
- 280: `setDestinations(data.destinations || [])` -> 执行当前语句，参与该文件整体逻辑。
- 281: `setTotal(data.total || 0)` -> 执行当前语句，参与该文件整体逻辑。
- 282: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 283: `console.error('加载目的地失败', e.message)` -> 执行当前语句，参与该文件整体逻辑。
- 284: `toast.error('加载数据失败: ' + e.message)` -> 执行当前语句，参与该文件整体逻辑。
- 285: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 286: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 287: `}` -> 结束当前语句或代码块。
- 288: `}, [page, keyword])` -> 执行当前语句，参与该文件整体逻辑。
- 289: `(空行)` -> 空行，用于提升代码结构可读性。
- 290: `useEffect(() => { fetchDestinations() }, [fetchDestinations])` -> 执行当前语句，参与该文件整体逻辑。
- 291: `(空行)` -> 空行，用于提升代码结构可读性。
- 292: `const handleSearch = (val: string) => {` -> 声明变量或常量，保存运行时数据。
- 293: `setKeyword(val); setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 294: `if (searchTimer) clearTimeout(searchTimer)` -> 条件判断分支，根据场景执行不同逻辑。
- 295: `setSearchTimer(setTimeout(() => {}, 300))` -> 执行当前语句，参与该文件整体逻辑。
- 296: `}` -> 结束当前语句或代码块。
- 297: `(空行)` -> 空行，用于提升代码结构可读性。
- 298: `const startEdit = (d: Destination) => { setEditingId(d.id); setEditData({ ...d }) }` -> 声明变量或常量，保存运行时数据。
- 299: `const cancelEdit = () => { setEditingId(null); setEditData({}) }` -> 声明变量或常量，保存运行时数据。
- 300: `(空行)` -> 空行，用于提升代码结构可读性。
- 301: `const saveEdit = async () => {` -> 声明变量或常量，保存运行时数据。
- 302: `if (!editingId) return` -> 条件判断分支，根据场景执行不同逻辑。
- 303: `try {` -> 异常处理逻辑，提升程序健壮性。
- 304: `await adminFetch(\`/api/admin/destinations/${editingId}\`, { method: 'PUT', body: JSON.stringify(editData) })` -> 执行当前语句，参与该文件整体逻辑。
- 305: `setEditingId(null); setEditData({}); fetchDestinations()` -> 执行当前语句，参与该文件整体逻辑。
- 306: `} catch (e: any) { alert('保存失败: ' + e.message) }` -> 执行当前语句，参与该文件整体逻辑。
- 307: `}` -> 结束当前语句或代码块。
- 308: `(空行)` -> 空行，用于提升代码结构可读性。
- 309: `const deleteDest = async (id: number) => {` -> 声明变量或常量，保存运行时数据。
- 310: `if (!confirm('确定要删除这个目的地吗？')) return` -> 条件判断分支，根据场景执行不同逻辑。
- 311: `try { await adminFetch(\`/api/admin/destinations/${id}\`, { method: 'DELETE' }); fetchDestinations() }` -> 异常处理逻辑，提升程序健壮性。
- 312: `catch (e: any) { alert('删除失败: ' + e.message) }` -> 异常处理逻辑，提升程序健壮性。
- 313: `}` -> 结束当前语句或代码块。
- 314: `(空行)` -> 空行，用于提升代码结构可读性。
- 315: `/** 图片上传处理 */` -> 注释行，用于解释设计意图或使用说明。
- 316: `const handleImageUpload = async (destId: number, e: React.ChangeEvent<HTMLInputElement>) => {` -> 声明变量或常量，保存运行时数据。
- 317: `const file = e.target.files?.[0]` -> 声明变量或常量，保存运行时数据。
- 318: `if (!file) return` -> 条件判断分支，根据场景执行不同逻辑。
- 319: `// 校验类型` -> 注释行，用于解释设计意图或使用说明。
- 320: `const allowed = ['image/jpeg', 'image/png', 'image/webp', 'image/gif', 'image/bmp']` -> 声明变量或常量，保存运行时数据。
- 321: `if (!allowed.includes(file.type)) { alert('仅支持 JPG/PNG/WebP/GIF/BMP 格式'); return }` -> 条件判断分支，根据场景执行不同逻辑。
- 322: `if (file.size > 10 * 1024 * 1024) { alert('文件大小不能超过 10MB'); return }` -> 条件判断分支，根据场景执行不同逻辑。
- 323: `(空行)` -> 空行，用于提升代码结构可读性。
- 324: `setUploadingId(destId)` -> 执行当前语句，参与该文件整体逻辑。
- 325: `try {` -> 异常处理逻辑，提升程序健壮性。
- 326: `const formData = new FormData()` -> 声明变量或常量，保存运行时数据。
- 327: `formData.append('image', file)` -> 执行当前语句，参与该文件整体逻辑。
- 328: `(空行)` -> 空行，用于提升代码结构可读性。
- 329: `// 用原生 fetch 上传（multipart 不能用 json headers）` -> 注释行，用于解释设计意图或使用说明。
- 330: `const token = typeof window !== 'undefined' ? localStorage.getItem('admin_token') : ''` -> 声明变量或常量，保存运行时数据。
- 331: `const res = await fetch(\`${API_BASE}/api/admin/destinations/${destId}/upload-image\`, {` -> 声明变量或常量，保存运行时数据。
- 332: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 333: `headers: token ? { 'Authorization': \`Bearer ${token}\` } : {},` -> 执行当前语句，参与该文件整体逻辑。
- 334: `body: formData,` -> 执行当前语句，参与该文件整体逻辑。
- 335: `})` -> 执行当前语句，参与该文件整体逻辑。
- 336: `const data = await res.json().catch(() => ({ success: false, error: '上传响应解析失败' }))` -> 声明变量或常量，保存运行时数据。
- 337: `(空行)` -> 空行，用于提升代码结构可读性。
- 338: `if (!res.ok || !data.success) throw new Error(data.error || \`HTTP ${res.status}\`)` -> 条件判断分支，根据场景执行不同逻辑。
- 339: `(空行)` -> 空行，用于提升代码结构可读性。
- 340: `// 更新本地数据中的 cover_image` -> 注释行，用于解释设计意图或使用说明。
- 341: `setDestinations(prev => prev.map(d =>` -> 执行当前语句，参与该文件整体逻辑。
- 342: `d.id === destId ? { ...d, cover_image: data.destination?.cover_image || d.cover_image } : d` -> 执行当前语句，参与该文件整体逻辑。
- 343: `))` -> 执行当前语句，参与该文件整体逻辑。
- 344: `(空行)` -> 空行，用于提升代码结构可读性。
- 345: `// 显示成功提示` -> 注释行，用于解释设计意图或使用说明。
- 346: `alert(\`${data.message || '图片上传成功'}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 347: `} catch (err: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 348: `alert('上传失败: ' + err.message)` -> 执行当前语句，参与该文件整体逻辑。
- 349: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 350: `setUploadingId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 351: `// 清空 input 以便重复上传同一文件` -> 注释行，用于解释设计意图或使用说明。
- 352: `e.target.value = ''   // e.target.value?` -> 执行当前语句，参与该文件整体逻辑。
- 353: `}` -> 结束当前语句或代码块。
- 354: `}` -> 结束当前语句或代码块。
- 355: `(空行)` -> 空行，用于提升代码结构可读性。
- 356: `/** 构建图片完整URL */` -> 注释行，用于解释设计意图或使用说明。
- 357: `const imageUrl = (coverImage?: string | null) => {` -> 声明变量或常量，保存运行时数据。
- 358: `if (!coverImage) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 359: `if (coverImage.startsWith('http')) return coverImage` -> 条件判断分支，根据场景执行不同逻辑。
- 360: `return \`${API_BASE}/api/media?path=${encodeURIComponent(coverImage)}\`` -> 返回结果或提前结束当前流程。
- 361: `}` -> 结束当前语句或代码块。
- 362: `(空行)` -> 空行，用于提升代码结构可读性。
- 363: `const totalPages = Math.ceil(total / perPage)` -> 声明变量或常量，保存运行时数据。
- 364: `(空行)` -> 空行，用于提升代码结构可读性。
- 365: `return (` -> 返回结果或提前结束当前流程。
- 366: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 367: `<div className="flex items-center justify-between px-5 py-4 border-b border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `<h3 className="font-bold text-gray-800 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `<MapPin className="h-4 w-4 text-blue-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 370: `目的地管理` -> 执行当前语句，参与该文件整体逻辑。
- 371: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 373: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 374: `<Search className="absolute left-2.5 top-1/2 -translate-y-1/2 h-4 w-4 text-gray-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 375: `<input type="text" placeholder="搜索目的地..." value={keyword}` -> 执行当前语句，参与该文件整体逻辑。
- 376: `onChange={(e) => handleSearch(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 377: `className="pl-8 pr-3 py-1.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-300 focus:border-transparent w-48" />` -> 执行当前语句，参与该文件整体逻辑。
- 378: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 379: `<button onClick={fetchDestinations} className="p-1.5 hover:bg-gray-100 rounded-lg transition-colors" title="刷新">` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `<RefreshCw className="h-4 w-4 text-gray-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 382: `<span className="text-xs text-gray-400">共 {total} 处</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 383: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 384: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 385: `(空行)` -> 空行，用于提升代码结构可读性。
- 386: `<div className="overflow-x-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 387: `<table className="w-full text-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 388: `<thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 389: `<tr className="bg-gray-50/80 text-left">` -> JSX/HTML 结构行，用于描述页面元素。
- 390: `<th className="px-3 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">ID</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 391: `<th className="px-3 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500 w-20">封面</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 392: `<th className="px-4 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">名称</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 393: `<th className="px-4 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">城市</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 394: `<th className="px-4 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">省份</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 395: `<th className="px-3 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">评分</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 396: `<th className="px-3 py-3 font-semibold text-xs uppercase tracking-wider text-gray-500">票价</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 397: `<th className="px-4 py-3 font-semibold text-xs uppercase tracking-wider text-gray-50">操作</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 398: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 399: `</thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 400: `<tbody className="divide-y divide-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 401: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 402: `<tr><td colSpan={8} className="px-4 py-12 text-center"><Loader2 className="h-6 w-6 animate-spin mx-auto text-blue-500" /></td></tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 403: `) : destinations.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 404: `<tr><td colSpan={8} className="px-4 py-12 text-center text-gray-400">暂无数据</td></tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 405: `) : destinations.map((d) => (` -> 执行当前语句，参与该文件整体逻辑。
- 406: `<tr key={d.id} className="hover:bg-blue-50/30 transition-colors group">` -> JSX/HTML 结构行，用于描述页面元素。
- 407: `(空行)` -> 空行，用于提升代码结构可读性。
- 408: `<td className="px-3 py-3 text-gray-400 font-mono text-xs whitespace-nowrap">#{d.id}</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 409: `(空行)` -> 空行，用于提升代码结构可读性。
- 410: `(空行)` -> 空行，用于提升代码结构可读性。
- 411: `<td className="px-3 py-2 whitespace-nowrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 412: `<div className="relative group/img w-[52px] h-[38px] rounded-lg overflow-hidden bg-gray-100 border border-gray-200 shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 413: `{imageUrl(d.cover_image) ? (` -> 执行当前语句，参与该文件整体逻辑。
- 414: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 415: `<img src={imageUrl(d.cover_image)!} alt={d.name}` -> 执行当前语句，参与该文件整体逻辑。
- 416: `className="w-full h-full object-cover"` -> 执行当前语句，参与该文件整体逻辑。
- 417: `loading="lazy"` -> 执行当前语句，参与该文件整体逻辑。
- 418: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 419: `(空行)` -> 空行，用于提升代码结构可读性。
- 420: `<label` -> 执行当前语句，参与该文件整体逻辑。
- 421: `title="更换封面图"` -> 执行当前语句，参与该文件整体逻辑。
- 422: `className="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 group-hover/img:opacity-100 cursor-pointer transition-opacity"` -> 执行当前语句，参与该文件整体逻辑。
- 423: `>` -> 执行当前语句，参与该文件整体逻辑。
- 424: `{uploadingId === d.id` -> 执行当前语句，参与该文件整体逻辑。
- 425: `? <Loader2 className="h-5 w-5 animate-spin text-white" />` -> 执行当前语句，参与该文件整体逻辑。
- 426: `: <Upload className="h-4 w-4 text-white" />` -> 执行当前语句，参与该文件整体逻辑。
- 427: `}` -> 结束当前语句或代码块。
- 428: `<input type="file" accept="image/jpeg,image/png,image/webp,image/gif,image/bmp"` -> 执行当前语句，参与该文件整体逻辑。
- 429: `className="hidden" capture="user"` -> 执行当前语句，参与该文件整体逻辑。
- 430: `onChange={(e) => handleImageUpload(d.id, e)}` -> 执行当前语句，参与该文件整体逻辑。
- 431: `disabled={uploadingId === d.id}` -> 执行当前语句，参与该文件整体逻辑。
- 432: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 433: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 434: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 435: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 436: `<label className="absolute inset-0 flex flex-col items-center justify-center cursor-pointer hover:bg-blue-50 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 437: `{uploadingId === d.id` -> 执行当前语句，参与该文件整体逻辑。
- 438: `? <Loader2 className="h-4 w-4 animate-spin text-blue-500" />` -> 执行当前语句，参与该文件整体逻辑。
- 439: `: <><ImagePlus className="h-4 w-4 text-gray-300 mb-0.5" /><span className="text-[9px] text-gray-300">上传</span></>` -> 执行当前语句，参与该文件整体逻辑。
- 440: `}` -> 结束当前语句或代码块。
- 441: `<input type="file" accept="image/jpeg,image/png,image/webp,image/gif,image/bmp"` -> 执行当前语句，参与该文件整体逻辑。
- 442: `className="hidden" onChange={(e) => handleImageUpload(d.id, e)}` -> 执行当前语句，参与该文件整体逻辑。
- 443: `disabled={uploadingId === d.id}` -> 执行当前语句，参与该文件整体逻辑。
- 444: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 445: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 446: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 447: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 448: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 449: `(空行)` -> 空行，用于提升代码结构可读性。
- 450: `{editingId === d.id ? (` -> 执行当前语句，参与该文件整体逻辑。
- 451: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 452: `<td className="px-4 py-2"><input value={editData.name || ''} onChange={(e) => setEditData({ ...editData, name: e.target.value })} className="w-full px-2 py-1 border border-blue-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-300" /></td>` -> JSX/HTML 结构行，用于描述页面元素。
- 453: `<td className="px-4 py-2"><input value={editData.city || ''} onChange={(e) => setEditData({ ...editData, city: e.target.value })} className="w-full px-2 py-1 border border-blue-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-300" /></td>` -> JSX/HTML 结构行，用于描述页面元素。
- 454: `<td className="px-4 py-2"><input value={editData.province || ''} onChange={(e) => setEditData({ ...editData, province: e.target.value })} className="w-full px-2 py-1 border border-blue-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-300" /></td>` -> JSX/HTML 结构行，用于描述页面元素。
- 455: `<td className="px-3 py-2"><input type="number" step="0.1" min="0" max="5" value={editData.rating ?? ''} onChange={(e) => setEditData({ ...editData, rating: parseFloat(e.target.value) || 0 })} className="w-16 px-2 py-1 border border-blue-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-300" /></td>` -> JSX/HTML 结构行，用于描述页面元素。
- 456: `<td className="px-3 py-2"><input type="number" min="0" value={editData.ticket_price ?? ''} onChange={(e) => setEditData({ ...editData, ticket_price: parseFloat(e.target.value) || 0 })} className="w-20 px-2 py-1 border border-blue-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-300" /></td>` -> JSX/HTML 结构行，用于描述页面元素。
- 457: `<td className="px-4 py-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 458: `<div className="flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 459: `<button onClick={saveEdit} className="p-1.5 bg-green-500 text-white rounded-lg hover:bg-green-600 transition-colors"><Save className="h-3.5 w-3.5" /></button>` -> JSX/HTML 结构行，用于描述页面元素。
- 460: `<button onClick={cancelEdit} className="p-1.5 bg-gray-400 text-white rounded-lg hover:bg-gray-500 transition-colors"><X className="h-3.5 w-3.5" /></button>` -> JSX/HTML 结构行，用于描述页面元素。
- 461: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 462: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 463: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 464: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 465: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 466: `<td className="px-4 py-3 font-medium text-gray-800">{d.name}</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 467: `<td className="px-4 py-3 text-gray-600">{d.city}</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 468: `<td className="px-4 py-3 text-gray-600">{d.province}</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 469: `<td className="px-3 py-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 470: `<span className={\`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium ${d.rating >= 4 ? 'bg-green-100 text-green-700' : d.rating >= 3 ? 'bg-yellow-100 text-yellow-700' : 'bg-gray-100 text-gray-600'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 471: `⭐ {d.rating?.toFixed(1) || '-'}` -> 执行当前语句，参与该文件整体逻辑。
- 472: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 473: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 474: `<td className="px-3 py-3 text-gray-600">¥{d.ticket_price || 0}</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 475: `<td className="px-4 py-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 476: `<div className="flex items-center gap-1 opacity-60 group-hover:opacity-100 transition-opacity">` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `<button onClick={() => startEdit(d)} className="p-1.5 text-blue-500 hover:bg-blue-50 rounded-lg transition-colors" title="编辑"><Edit2 className="h-3.5 w-3.5" /></button>` -> JSX/HTML 结构行，用于描述页面元素。
- 478: `<button onClick={() => deleteDest(d.id)} className="p-1.5 text-red-500 hover:bg-red-50 rounded-lg transition-colors" title="删除"><Trash2 className="h-3.5 w-3.5" /></button>` -> JSX/HTML 结构行，用于描述页面元素。
- 479: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 480: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 481: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 482: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 483: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 484: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 485: `</tbody>` -> JSX/HTML 结构行，用于描述页面元素。
- 486: `</table>` -> JSX/HTML 结构行，用于描述页面元素。
- 487: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 488: `(空行)` -> 空行，用于提升代码结构可读性。
- 489: `(空行)` -> 空行，用于提升代码结构可读性。
- 490: `{totalPages > 1 && (` -> 执行当前语句，参与该文件整体逻辑。
- 491: `<div className="flex items-center justify-between px-5 py-3 border-t border-gray-100 bg-gray-50/50">` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `<span className="text-xs text-gray-400">第 {page} / {totalPages} 页</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 493: `<div className="flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 494: `<button disabled={page <= 1} onClick={() => setPage(p => p - 1)} className="px-2.5 py-1.5 text-sm border border-gray-200 rounded-lg hover:bg-gray-100 disabled:opacity-40 disabled:cursor-not-allowed transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 495: `<ChevronLeft className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 496: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 497: `{Array.from({ length: Math.min(totalPages, 5) }, (_, i) => {` -> 执行当前语句，参与该文件整体逻辑。
- 498: `let p = i + 1` -> 声明变量或常量，保存运行时数据。
- 499: `if (totalPages > 5 && page > 3) p = page - 2 + i` -> 条件判断分支，根据场景执行不同逻辑。
- 500: `if (p < 1 || p > totalPages) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 501: `return (` -> 返回结果或提前结束当前流程。
- 502: `<button key={p} onClick={() => setPage(p)} className={\`w-8 h-8 text-sm rounded-lg transition-colors ${p === page ? 'bg-blue-500 text-white' : 'border border-gray-200 hover:bg-gray-100'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 503: `{p}` -> 执行当前语句，参与该文件整体逻辑。
- 504: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 505: `)` -> 结束当前语句或代码块。
- 506: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 507: `<button disabled={page >= totalPages} onClick={() => setPage(p => p + 1)} className="px-2.5 py-1.5 text-sm border border-gray-200 rounded-lg hover:bg-gray-100 disabled:opacity-40 disabled:cursor-not-allowed transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 508: `<ChevronRight className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 509: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 510: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 511: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 512: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 513: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 514: `)` -> 结束当前语句或代码块。
- 515: `}` -> 结束当前语句或代码块。
- 516: `(空行)` -> 空行，用于提升代码结构可读性。
- 517: `/* ==================== 用户快速列表 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 518: `(空行)` -> 空行，用于提升代码结构可读性。
- 519: `function UserQuickList() {` -> 定义函数或方法，实现具体业务逻辑。
- 520: `const [users, setUsers] = useState<AdminUser[]>([])` -> 声明变量或常量，保存运行时数据。
- 521: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 522: `(空行)` -> 空行，用于提升代码结构可读性。
- 523: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 524: `adminFetch<{ success: boolean; users: AdminUser[] }>('/api/admin/users?limit=10')` -> 执行当前语句，参与该文件整体逻辑。
- 525: `.then(data => { if (data.success) setUsers(data.users || []) })` -> 执行当前语句，参与该文件整体逻辑。
- 526: `.catch(() => {})` -> 执行当前语句，参与该文件整体逻辑。
- 527: `.finally(() => setLoading(false))` -> 执行当前语句，参与该文件整体逻辑。
- 528: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 529: `(空行)` -> 空行，用于提升代码结构可读性。
- 530: `const deleteUser = async (id: string) => {` -> 声明变量或常量，保存运行时数据。
- 531: `if (!confirm('确定删除该用户？此操作不可逆！')) return` -> 条件判断分支，根据场景执行不同逻辑。
- 532: `try {` -> 异常处理逻辑，提升程序健壮性。
- 533: `await adminFetch(\`/api/admin/users/${id}\`, { method: 'DELETE' })` -> 执行当前语句，参与该文件整体逻辑。
- 534: `setUsers(users.filter(u => u.id !== id))` -> 执行当前语句，参与该文件整体逻辑。
- 535: `} catch (e: any) { alert('删除失败: ' + e.message) }` -> 执行当前语句，参与该文件整体逻辑。
- 536: `}` -> 结束当前语句或代码块。
- 537: `(空行)` -> 空行，用于提升代码结构可读性。
- 538: `return (` -> 返回结果或提前结束当前流程。
- 539: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 540: `<div className="flex items-center justify-between px-5 py-4 border-b border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 541: `<h3 className="font-bold text-gray-800 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 542: `<Users className="h-4 w-4 text-green-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 543: `最近注册用户` -> 执行当前语句，参与该文件整体逻辑。
- 544: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 545: `<Link href="/admin/users" className="text-xs text-blue-500 hover:text-blue-700 font-medium">查看全部 →</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 546: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 547: `<div className="divide-y divide-gray-100 max-h-[360px] overflow-y-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 548: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 549: `<div className="px-5 py-8 text-center"><Loader2 className="h-6 w-6 animate-spin mx-auto text-green-500" /></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 550: `) : users.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 551: `<div className="px-5 py-8 text-center text-gray-400 text-sm">暂无用户</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 552: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 553: `users.map(u => (` -> 执行当前语句，参与该文件整体逻辑。
- 554: `<div key={u.id} className="flex items-center justify-between px-5 py-3 hover:bg-gray-50/60 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 555: `<div className="flex items-center gap-3 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 556: `<div className="h-9 w-9 rounded-full bg-gradient-to-br from-blue-400 to-purple-500 flex items-center justify-center text-white text-sm font-bold shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 557: `{(u.nickname || u.username || '?').charAt(0).toUpperCase()}` -> 执行当前语句，参与该文件整体逻辑。
- 558: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 559: `<div className="min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 560: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 561: `<span className="font-medium text-gray-800 text-sm truncate">{u.nickname || u.username}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 562: `{u.is_admin && <span className="shrink-0 inline-flex items-center px-1.5 py-0.5 rounded-[4px] bg-red-100 text-red-600 text-[10px] font-bold">管理</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 563: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 564: `<div className="text-xs text-gray-400 truncate">{u.email || u.phone || '未填写联系方式'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 565: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 566: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 567: `<div className="flex items-center gap-2 shrink-0 ml-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 568: `<span className="text-[11px] text-gray-400">{u.created_at ? new Date(u.created_at).toLocaleDateString('zh-CN') : '-'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 569: `<button onClick={() => deleteUser(u.id)} className="p-1 text-red-400 hover:text-red-600 hover:bg-red-50 rounded transition-colors" title="删除用户">` -> JSX/HTML 结构行，用于描述页面元素。
- 570: `<Trash2 className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 571: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 572: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 573: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 574: `))` -> 执行当前语句，参与该文件整体逻辑。
- 575: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 576: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 577: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 578: `)` -> 结束当前语句或代码块。
- 579: `}` -> 结束当前语句或代码块。
- 580: `(空行)` -> 空行，用于提升代码结构可读性。
- 581: `/* ==================== 最近动态（评论+行程+订单）==================== */` -> 注释行，用于解释设计意图或使用说明。
- 582: `(空行)` -> 空行，用于提升代码结构可读性。
- 583: `function RecentActivity({ stats }: { stats: Stats | null }) {` -> 定义函数或方法，实现具体业务逻辑。
- 584: `const [activeTab, setActiveTab] = useState<'comments' | 'trips' | 'orders'>('comments')` -> 声明变量或常量，保存运行时数据。
- 585: `(空行)` -> 空行，用于提升代码结构可读性。
- 586: `return (` -> 返回结果或提前结束当前流程。
- 587: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 588: `<div className="flex items-center gap-1 px-5 pt-4 pb-0 border-b border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 589: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 590: `{ key: 'comments' as const, label: '最新评论', icon: MessageSquare, color: 'text-orange-500', dataKey: 'recent_comments' as keyof Stats },` -> 执行当前语句，参与该文件整体逻辑。
- 591: `{ key: 'trips' as const, label: '最近行程', icon: Calendar, color: 'text-purple-500', dataKey: 'recent_trips' as keyof Stats },` -> 执行当前语句，参与该文件整体逻辑。
- 592: `{ key: 'orders' as const, label: '最近订单', icon: ShoppingBag, color: 'text-cyan-500', dataKey: 'recent_orders' as keyof Stats },` -> 执行当前语句，参与该文件整体逻辑。
- 593: `].map(tab => (` -> 执行当前语句，参与该文件整体逻辑。
- 594: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 595: `key={tab.key}` -> 执行当前语句，参与该文件整体逻辑。
- 596: `onClick={() => setActiveTab(tab.key)}` -> 执行当前语句，参与该文件整体逻辑。
- 597: `className={\`flex items-center gap-1.5 px-4 py-3 text-sm font-medium border-b-2 transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 598: `activeTab === tab.key` -> 执行当前语句，参与该文件整体逻辑。
- 599: `? \`border-blue-500 ${tab.color}\`` -> 执行当前语句，参与该文件整体逻辑。
- 600: `: 'border-transparent text-gray-400 hover:text-gray-600'` -> 执行当前语句，参与该文件整体逻辑。
- 601: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 602: `>` -> 执行当前语句，参与该文件整体逻辑。
- 603: `<tab.icon className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 604: `{tab.label}` -> 执行当前语句，参与该文件整体逻辑。
- 605: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 606: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 607: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 608: `<div className="p-5 min-h-[240px]">` -> JSX/HTML 结构行，用于描述页面元素。
- 609: `{activeTab === 'comments' && (` -> 执行当前语句，参与该文件整体逻辑。
- 610: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 611: `{(stats?.recent_comments || []).length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 612: `<p className="text-sm text-gray-400 text-center py-6">暂无评论</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 613: `) : (stats?.recent_comments || []).map((c: any, i: number) => (` -> 执行当前语句，参与该文件整体逻辑。
- 614: `<div key={i} className="flex gap-3 p-3 rounded-xl bg-orange-50/40 hover:bg-orange-50/70 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 615: `<div className="h-8 w-8 rounded-full bg-orange-100 flex items-center justify-center shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 616: `<MessageSquare className="h-4 w-4 text-orange-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 617: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 618: `<div className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 619: `<div className="flex items-center gap-2 mb-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 620: `<span className="font-medium text-sm text-gray-800">{c.user?.nickname || \`用户#${c.user_id}\`}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 621: `<span className="text-xs text-gray-400">目标 #{c.destination_id}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 622: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 623: `<p className="text-sm text-gray-600 line-clamp-2">{c.content}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 624: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 625: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 626: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 627: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 628: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 629: `{activeTab === 'trips' && (` -> 执行当前语句，参与该文件整体逻辑。
- 630: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 631: `{(stats?.recent_trips || []).length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 632: `<p className="text-sm text-gray-400 text-center py-6">暂无行程</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 633: `) : (stats?.recent_trips || []).map((t: any, i: number) => (` -> 执行当前语句，参与该文件整体逻辑。
- 634: `<div key={i} className="flex gap-3 p-3 rounded-xl bg-purple-50/40 hover:bg-purple-50/70 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 635: `<div className="h-8 w-8 rounded-full bg-purple-100 flex items-center justify-center shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 636: `<Calendar className="h-4 w-4 text-purple-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 637: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 638: `<div className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 639: `<div className="font-medium text-sm text-gray-800">{t.title}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 640: `<div className="flex items-center gap-3 mt-1 text-xs text-gray-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 641: `<span>{t.start_date || '-'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 642: `<span>→</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 643: `<span>{t.end_date || '-'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 644: `<span className={\`px-1.5 py-0.5 rounded text-[10px] font-medium ${` -> 执行当前语句，参与该文件整体逻辑。
- 645: `t.status === 'completed' ? 'bg-green-100 text-green-700' :` -> 执行当前语句，参与该文件整体逻辑。
- 646: `t.status === 'ongoing' ? 'bg-blue-100 text-blue-700' :` -> 执行当前语句，参与该文件整体逻辑。
- 647: `'bg-gray-100 text-gray-600'` -> 执行当前语句，参与该文件整体逻辑。
- 648: `}\`}>{t.status || 'planning'}</span>` -> 执行当前语句，参与该文件整体逻辑。
- 649: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 650: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 651: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 652: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 653: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 654: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 655: `{activeTab === 'orders' && (` -> 执行当前语句，参与该文件整体逻辑。
- 656: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 657: `{(stats?.recent_orders || []).length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 658: `<p className="text-sm text-gray-400 text-center py-6">暂无订单</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 659: `) : (stats?.recent_orders || []).map((o: any, i: number) => (` -> 执行当前语句，参与该文件整体逻辑。
- 660: `<div key={i} className="flex gap-3 p-3 rounded-xl bg-cyan-50/40 hover:bg-cyan-50/70 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 661: `<div className="h-8 w-8 rounded-full bg-cyan-100 flex items-center justify-center shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 662: `<ShoppingBag className="h-4 w-4 text-cyan-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 663: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 664: `<div className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 665: `<div className="flex items-center justify-between mb-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 666: `<span className="font-mono text-xs text-gray-500">{o.order_no}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 667: `<span className="font-semibold text-sm text-red-500">¥{o.total_amount?.toFixed(2) || '0.00'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 668: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 669: `<div className="flex items-center gap-2 mt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 670: `<span className={\`px-1.5 py-0.5 rounded text-[10px] font-medium ${` -> 执行当前语句，参与该文件整体逻辑。
- 671: `o.status === 'paid' || o.status === 'completed' ? 'bg-green-100 text-green-700' :` -> 执行当前语句，参与该文件整体逻辑。
- 672: `o.status === 'pending' ? 'bg-yellow-100 text-yellow-700' :` -> 执行当前语句，参与该文件整体逻辑。
- 673: `'bg-gray-100 text-gray-600'` -> 执行当前语句，参与该文件整体逻辑。
- 674: `}\`}>{o.status || 'unknown'}</span>` -> 执行当前语句，参与该文件整体逻辑。
- 675: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 676: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 677: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 678: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 679: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 680: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 681: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 682: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 683: `)` -> 结束当前语句或代码块。
- 684: `}` -> 结束当前语句或代码块。
- 685: `(空行)` -> 空行，用于提升代码结构可读性。
- 686: `/* ==================== 状态分布面板 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 687: `(空行)` -> 空行，用于提升代码结构可读性。
- 688: `function StatusPanels({ stats }: { stats: Stats | null }) {` -> 定义函数或方法，实现具体业务逻辑。
- 689: `const statusLabels: Record<string, string> = {` -> 声明变量或常量，保存运行时数据。
- 690: `planning: '规划中', ongoing: '进行中', completed: '已完成', cancelled: '已取消',` -> 执行当前语句，参与该文件整体逻辑。
- 691: `pending: '待支付', paid: '已支付', shipped: '已发货', refunded: '已退款',` -> 执行当前语句，参与该文件整体逻辑。
- 692: `}` -> 结束当前语句或代码块。
- 693: `const statusColors: Record<string, string> = {` -> 声明变量或常量，保存运行时数据。
- 694: `planning: 'bg-blue-100 text-blue-700', ongoing: 'bg-yellow-100 text-yellow-700', completed: 'bg-green-100 text-green-700', cancelled: 'bg-red-100 text-red-700',` -> 执行当前语句，参与该文件整体逻辑。
- 695: `pending: 'bg-yellow-100 text-yellow-700', paid: 'bg-green-100 text-green-700', shipped: 'bg-blue-100 text-blue-700', refunded: 'bg-gray-100 text-gray-700',` -> 执行当前语句，参与该文件整体逻辑。
- 696: `}` -> 结束当前语句或代码块。
- 697: `(空行)` -> 空行，用于提升代码结构可读性。
- 698: `return (` -> 返回结果或提前结束当前流程。
- 699: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 700: `(空行)` -> 空行，用于提升代码结构可读性。
- 701: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 702: `<h3 className="font-bold text-gray-800 flex items-center gap-2 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 703: `<TrendingUp className="h-4 w-4 text-purple-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 704: `行程状态分布` -> 执行当前语句，参与该文件整体逻辑。
- 705: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 706: `{(stats?.trip_status || []).length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 707: `<p className="text-sm text-gray-400 text-center py-4">暂无行程数据</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 708: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 709: `<div className="space-y-2.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 710: `{(stats?.trip_status || []).map((s: any) => (` -> 执行当前语句，参与该文件整体逻辑。
- 711: `<div key={s.status} className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 712: `<span className={\`px-2 py-1 rounded-md text-xs font-medium ${statusColors[s.status] || 'bg-gray-100 text-gray-600'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 713: `{statusLabels[s.status] || s.status}` -> 执行当前语句，参与该文件整体逻辑。
- 714: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 715: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 716: `<div className="w-32 h-2 bg-gray-100 rounded-full overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 717: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 718: `className="h-full bg-purple-500 rounded-full transition-all duration-500"` -> 执行当前语句，参与该文件整体逻辑。
- 719: `style={{ width: \`${(s.count / Math.max(...(stats?.trip_status || []).map((t: any) => t.count))) * 100}%\` }}` -> 执行当前语句，参与该文件整体逻辑。
- 720: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 721: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 722: `<span className="text-sm font-semibold text-gray-700 w-8 text-right">{s.count}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 723: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 724: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 725: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 726: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 727: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 728: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 729: `(空行)` -> 空行，用于提升代码结构可读性。
- 730: `(空行)` -> 空行，用于提升代码结构可读性。
- 731: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 732: `<h3 className="font-bold text-gray-800 flex items-center gap-2 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 733: `<ShoppingBag className="h-4 w-4 text-cyan-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 734: `订单状态分布` -> 执行当前语句，参与该文件整体逻辑。
- 735: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 736: `{(stats?.order_status || []).length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 737: `<p className="text-sm text-gray-400 text-center py-4">暂无订单数据</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 738: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 739: `<div className="space-y-2.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 740: `{(stats?.order_status || []).map((o: any) => (` -> 执行当前语句，参与该文件整体逻辑。
- 741: `<div key={o.status} className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 742: `<span className={\`px-2 py-1 rounded-md text-xs font-medium ${statusColors[o.status] || 'bg-gray-100 text-gray-600'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 743: `{statusLabels[o.status] || o.status}` -> 执行当前语句，参与该文件整体逻辑。
- 744: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 745: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 746: `<div className="w-24 h-2 bg-gray-100 rounded-full overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 747: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 748: `className="h-full bg-cyan-500 rounded-full transition-all duration-500"` -> 执行当前语句，参与该文件整体逻辑。
- 749: `style={{ width: \`${(o.count / Math.max(...(stats?.order_status || []).map((x: any) => x.count), 1)) * 100}%\` }}` -> 执行当前语句，参与该文件整体逻辑。
- 750: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 751: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 752: `<span className="text-sm font-semibold text-gray-700 w-8 text-right">{o.count}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 753: `<span className="text-xs text-red-500 w-14 text-right">¥{o.amount?.toFixed(0) || '0'}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 754: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 755: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 756: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 757: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 758: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 759: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 760: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 761: `)` -> 结束当前语句或代码块。
- 762: `}` -> 结束当前语句或代码块。
- 763: `(空行)` -> 空行，用于提升代码结构可读性。
- 764: `/* ==================== 主页面 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 765: `(空行)` -> 空行，用于提升代码结构可读性。
- 766: `export default function AdminPage() {` -> 导出当前声明，供其他模块复用。
- 767: `const [stats, setStats] = useState<Stats | null>(null)` -> 声明变量或常量，保存运行时数据。
- 768: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 769: `const [error, setError] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 770: `(空行)` -> 空行，用于提升代码结构可读性。
- 771: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 772: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 773: `async function run() {` -> 定义函数或方法，实现具体业务逻辑。
- 774: `try {` -> 异常处理逻辑，提升程序健壮性。
- 775: `setLoading(true); setError(null)` -> 执行当前语句，参与该文件整体逻辑。
- 776: `const data = await adminFetch<{ stats: Stats }>('/api/stats')` -> 声明变量或常量，保存运行时数据。
- 777: `if (!cancelled) setStats(data?.stats ?? null)` -> 条件判断分支，根据场景执行不同逻辑。
- 778: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 779: `if (!cancelled) setError(e?.message || '加载失败')` -> 条件判断分支，根据场景执行不同逻辑。
- 780: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 781: `if (!cancelled) setLoading(false)` -> 条件判断分支，根据场景执行不同逻辑。
- 782: `}` -> 结束当前语句或代码块。
- 783: `}` -> 结束当前语句或代码块。
- 784: `run()` -> 执行当前语句，参与该文件整体逻辑。
- 785: `return () => { cancelled = true }` -> 返回结果或提前结束当前流程。
- 786: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 787: `(空行)` -> 空行，用于提升代码结构可读性。
- 788: `/* 统计卡片配置 */` -> 注释行，用于解释设计意图或使用说明。
- 789: `const statCards = [` -> 声明变量或常量，保存运行时数据。
- 790: `{ icon: MapPin, label: '目的地总数', value: stats?.destinations ?? 0, color: 'blue' as const, subText: '旅游景点数据' },` -> 执行当前语句，参与该文件整体逻辑。
- 791: `{ icon: Users, label: '注册用户', value: stats?.users ?? 0, color: 'green' as const, subText: '平台用户总数' },` -> 执行当前语句，参与该文件整体逻辑。
- 792: `{ icon: Calendar, label: '旅行行程', value: stats?.trips ?? 0, color: 'purple' as const, subText: '用户创建的行程' },` -> 执行当前语句，参与该文件整体逻辑。
- 793: `{ icon: MessageSquare, label: '用户评论', value: stats?.comments ?? 0, color: 'orange' as const, subText: '景点评论总数' },` -> 执行当前语句，参与该文件整体逻辑。
- 794: `{ icon: ShoppingBag, label: '订单数量', value: stats?.orders ?? 0, color: 'red' as const, subText: '交易订单统计' },` -> 执行当前语句，参与该文件整体逻辑。
- 795: `{ icon: Footprints, label: '用户足迹', value: stats?.footprints ?? 0, color: 'pink' as const, subText: '浏览访问记录' },` -> 执行当前语句，参与该文件整体逻辑。
- 796: `{ icon: FileText, label: '内容页面', value: stats?.pages ?? 0, color: 'cyan' as const, subText: 'CMS 内容管理' },` -> 执行当前语句，参与该文件整体逻辑。
- 797: `{ icon: Bell, label: '通知消息', value: stats?.notifications ?? 0, color: 'indigo' as const, subText: '站内通知' },` -> 执行当前语句，参与该文件整体逻辑。
- 798: `]` -> 执行当前语句，参与该文件整体逻辑。
- 799: `(空行)` -> 空行，用于提升代码结构可读性。
- 800: `return (` -> 返回结果或提前结束当前流程。
- 801: `<AdminGuard>` -> JSX/HTML 结构行，用于描述页面元素。
- 802: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 803: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 804: `<main className="pt-16 pb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 805: `<div className="max-w-[1440px] mx-auto px-4 sm:px-6 lg:px-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 806: `(空行)` -> 空行，用于提升代码结构可读性。
- 807: `(空行)` -> 空行，用于提升代码结构可读性。
- 808: `<div className="flex items-end justify-between gap-4 flex-wrap mt-8 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 809: `<div className="flex items-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 810: `<div className="inline-flex h-13 w-13 items-center justify-center rounded-2xl bg-gradient-to-br from-blue-600 via-blue-500 to-indigo-600 text-white shadow-lg shadow-blue-500/25">` -> JSX/HTML 结构行，用于描述页面元素。
- 811: `<BarChart3 className="h-6.5 w-6.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 812: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 813: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 814: `<h1 className="text-3xl font-extrabold tracking-tight text-gray-900">管理仪表</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 815: `<p className="text-sm text-gray-500 mt-0.5">实时数据监控 · 内容管理 · 用户运营</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 816: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 817: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 818: `<div className="flex items-center gap-2 text-xs text-gray-400 bg-white/80 backdrop-blur px-3 py-2 rounded-lg border border-gray-200/60">` -> JSX/HTML 结构行，用于描述页面元素。
- 819: `<Database className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 820: `<span className="font-mono">{API_BASE}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 821: `<div className="relative group">` -> JSX/HTML 结构行，用于描述页面元素。
- 822: `<Eye className="h-3.5 w-3.5 cursor-help text-green-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 823: `<div className="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-2 py-1 text-xs text-white bg-gray-800 rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none z-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 824: `后端运行正常` -> 执行当前语句，参与该文件整体逻辑。
- 825: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 826: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 827: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 828: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 829: `(空行)` -> 空行，用于提升代码结构可读性。
- 830: `(空行)` -> 空行，用于提升代码结构可读性。
- 831: `{loading && (` -> 执行当前语句，参与该文件整体逻辑。
- 832: `<div className="flex flex-col items-center justify-center py-24 gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 833: `<Loader2 className="h-10 w-10 animate-spin text-blue-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 834: `<p className="text-gray-500 font-medium">正在加载数据...</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 835: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 836: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 837: `{error && (` -> 执行当前语句，参与该文件整体逻辑。
- 838: `<div className="rounded-2xl border border-red-200 bg-red-50/80 px-6 py-5 flex items-start gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 839: `<AlertCircle className="h-5 w-5 text-red-500 shrink-0 mt-0.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 840: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 841: `<p className="font-medium text-red-700">数据加载失败</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 842: `<p className="text-sm text-red-500 mt-1">{error}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 843: `<p className="text-xs text-red-400 mt-2">请确认后端服务已在 {API_BASE} 运行</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 844: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 845: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 846: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 847: `(空行)` -> 空行，用于提升代码结构可读性。
- 848: `{!loading && !error && stats && (` -> 执行当前语句，参与该文件整体逻辑。
- 849: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 850: `(空行)` -> 空行，用于提升代码结构可读性。
- 851: `<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-8 gap-4 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 852: `{statCards.map(card => (` -> 执行当前语句，参与该文件整体逻辑。
- 853: `<StatCard key={card.label} {...card} />` -> JSX/HTML 结构行，用于描述页面元素。
- 854: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 855: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 856: `(空行)` -> 空行，用于提升代码结构可读性。
- 857: `(空行)` -> 空行，用于提升代码结构可读性。
- 858: `<div className="grid grid-cols-1 xl:grid-cols-3 gap-6 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 859: `(空行)` -> 空行，用于提升代码结构可读性。
- 860: `<div className="xl:col-span-1 space-y-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 861: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 862: `<h3 className="font-bold text-gray-800 flex items-center gap-2 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 863: `<MapPin className="h-4 w-4 text-blue-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 864: `省份分布 TOP 10` -> 执行当前语句，参与该文件整体逻辑。
- 865: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 866: `<ProvinceChart data={stats.province_distribution || []} />` -> JSX/HTML 结构行，用于描述页面元素。
- 867: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 868: `<div className="rounded-2xl border border-gray-200/80 bg-white shadow-sm p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 869: `<h3 className="font-bold text-gray-800 flex items-center gap-2 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 870: `<TrendingUp className="h-4 w-4 text-indigo-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 871: `城市排行 TOP 10` -> 执行当前语句，参与该文件整体逻辑。
- 872: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 873: `<CityRanking data={stats.city_top || []} />` -> JSX/HTML 结构行，用于描述页面元素。
- 874: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 875: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 876: `(空行)` -> 空行，用于提升代码结构可读性。
- 877: `(空行)` -> 空行，用于提升代码结构可读性。
- 878: `<div className="xl:col-span-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 879: `<DestinationTable />` -> JSX/HTML 结构行，用于描述页面元素。
- 880: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 881: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 882: `(空行)` -> 空行，用于提升代码结构可读性。
- 883: `(空行)` -> 空行，用于提升代码结构可读性。
- 884: `<StatusPanels stats={stats} />` -> JSX/HTML 结构行，用于描述页面元素。
- 885: `(空行)` -> 空行，用于提升代码结构可读性。
- 886: `(空行)` -> 空行，用于提升代码结构可读性。
- 887: `<div className="grid grid-cols-1 xl:grid-cols-3 gap-6 mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 888: `<div className="xl:col-span-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 889: `<RecentActivity stats={stats} />` -> JSX/HTML 结构行，用于描述页面元素。
- 890: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 891: `<div className="xl:col-span-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 892: `<UserQuickList />` -> JSX/HTML 结构行，用于描述页面元素。
- 893: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 894: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 895: `(空行)` -> 空行，用于提升代码结构可读性。
- 896: `(空行)` -> 空行，用于提升代码结构可读性。
- 897: `<div className="mt-8 rounded-2xl border border-gray-200/80 bg-white shadow-sm p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 898: `<h3 className="font-bold text-gray-800 flex items-center gap-2 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 899: `<Settings className="h-4 w-4 text-gray-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 900: `快捷操作入口` -> 执行当前语句，参与该文件整体逻辑。
- 901: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 902: `<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 903: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 904: `{ href: '/admin/comments', label: '评论管理', desc: '审核/删除评论' },` -> 执行当前语句，参与该文件整体逻辑。
- 905: `{ href: '/admin/users', label: '用户管理', desc: '查看/编辑用户' },` -> 执行当前语句，参与该文件整体逻辑。
- 906: `{ href: '/admin/footprints', label: '足迹管理', desc: '浏览记录管理' },` -> 执行当前语句，参与该文件整体逻辑。
- 907: `{ href: '/api/docs', label: 'API 文档', desc: '接口说明', external: true },` -> 执行当前语句，参与该文件整体逻辑。
- 908: `{ href: '/api/health', label: '健康检查', desc: '系统状态', external: true },` -> 执行当前语句，参与该文件整体逻辑。
- 909: `{ href: '/destinations', label: '前台目的地', desc: '前端展示' },` -> 执行当前语句，参与该文件整体逻辑。
- 910: `].map(link => (` -> 执行当前语句，参与该文件整体逻辑。
- 911: `<a key={link.href}` -> 执行当前语句，参与该文件整体逻辑。
- 912: `href={link.href}` -> 执行当前语句，参与该文件整体逻辑。
- 913: `target={link.external ? '_blank' : undefined}` -> 执行当前语句，参与该文件整体逻辑。
- 914: `rel={link.external ? 'noreferrer' : undefined}` -> 执行当前语句，参与该文件整体逻辑。
- 915: `className="group flex flex-col items-center gap-2 p-4 rounded-xl border border-gray-200/80 hover:border-blue-300 hover:bg-blue-50/40 transition-all duration-200"` -> 执行当前语句，参与该文件整体逻辑。
- 916: `>` -> 执行当前语句，参与该文件整体逻辑。
- 917: `<div className="h-10 w-10 rounded-xl bg-gray-100 group-hover:bg-blue-100 flex items-center justify-center transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 918: `<Settings className="h-5 w-5 text-gray-400 group-hover:text-blue-500 transition-colors" />` -> JSX/HTML 结构行，用于描述页面元素。
- 919: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 920: `<span className="text-sm font-medium text-gray-700 group-hover:text-blue-600 transition-colors">{link.label}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 921: `<span className="text-[11px] text-gray-400">{link.desc}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 922: `</a>` -> JSX/HTML 结构行，用于描述页面元素。
- 923: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 924: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 925: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 926: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 927: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 928: `(空行)` -> 空行，用于提升代码结构可读性。
- 929: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 930: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 931: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 932: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 933: `</AdminGuard>` -> JSX/HTML 结构行，用于描述页面元素。
- 934: `)` -> 结束当前语句或代码块。
- 935: `}` -> 结束当前语句或代码块。
