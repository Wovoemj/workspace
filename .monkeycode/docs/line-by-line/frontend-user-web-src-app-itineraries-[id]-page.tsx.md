# `frontend/user-web/src/app/itineraries/[id]/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 行程详情模块 - 单个行程详细展示与编辑` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 行程基础信息展示（标题、日期、状态、旅行风格、人数、预算）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 行程日历 TripCalendar 组件` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 每日行程安排展示（时间、地点、活动描述）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 目的地关联卡片（点击跳转到目的地详情）` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 添加行程项功能（选择目的地、时间安排）` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 编辑行程项（修改时间、地点、内容）` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 删除行程项` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - 行程状态管理（待完善→规划中→进行中→已完成）` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 分享行程功能` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 删除整个行程` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 费用估算展示` -> 注释行，用于解释设计意图或使用说明。
- 18: `*` -> 注释行，用于解释设计意图或使用说明。
- 19: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - TripCalendar: 行程日历组件` -> 注释行，用于解释设计意图或使用说明。
- 22: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 23: `*` -> 注释行，用于解释设计意图或使用说明。
- 24: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - GET /api/itineraries/${id}: 获取行程详情` -> 注释行，用于解释设计意图或使用说明。
- 26: `* - PUT /api/itineraries/${id}: 更新行程信息` -> 注释行，用于解释设计意图或使用说明。
- 27: `* - DELETE /api/itineraries/${id}: 删除行程` -> 注释行，用于解释设计意图或使用说明。
- 28: `* - GET /api/itineraries/${id}/items: 获取行程项目列表` -> 注释行，用于解释设计意图或使用说明。
- 29: `* - POST /api/itineraries/${id}/items: 添加行程项目` -> 注释行，用于解释设计意图或使用说明。
- 30: `* - PUT /api/itineraries/${id}/items/${itemId}: 更新行程项目` -> 注释行，用于解释设计意图或使用说明。
- 31: `* - DELETE /api/itineraries/${id}/items/${itemId}: 删除行程项目` -> 注释行，用于解释设计意图或使用说明。
- 32: `*` -> 注释行，用于解释设计意图或使用说明。
- 33: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 34: `* - useState: trip(行程数据), items(行程项), loading, editingItem` -> 注释行，用于解释设计意图或使用说明。
- 35: `* - useCallback: loadTrip, updateTrip, deleteTrip, addItem, updateItem, deleteItem` -> 注释行，用于解释设计意图或使用说明。
- 36: `*/` -> 注释行，用于解释设计意图或使用说明。
- 37: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `import { useCallback, useEffect, useMemo, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 40: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 41: `import { useParams, useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 42: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 43: `import {` -> 导入依赖模块，供当前文件使用。
- 44: `ArrowLeft,` -> 执行当前语句，参与该文件整体逻辑。
- 45: `Calendar,` -> 执行当前语句，参与该文件整体逻辑。
- 46: `Loader2,` -> 执行当前语句，参与该文件整体逻辑。
- 47: `Share2,` -> 执行当前语句，参与该文件整体逻辑。
- 48: `Trash2,` -> 执行当前语句，参与该文件整体逻辑。
- 49: `MapPin,` -> 执行当前语句，参与该文件整体逻辑。
- 50: `Clock,` -> 执行当前语句，参与该文件整体逻辑。
- 51: `Sparkles,` -> 执行当前语句，参与该文件整体逻辑。
- 52: `Users,` -> 执行当前语句，参与该文件整体逻辑。
- 53: `Plane,` -> 执行当前语句，参与该文件整体逻辑。
- 54: `DollarSign,` -> 执行当前语句，参与该文件整体逻辑。
- 55: `Plus,` -> 执行当前语句，参与该文件整体逻辑。
- 56: `CheckCircle2,` -> 执行当前语句，参与该文件整体逻辑。
- 57: `AlertCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 58: `PlayCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 59: `ExternalLink,` -> 执行当前语句，参与该文件整体逻辑。
- 60: `Download,` -> 执行当前语句，参与该文件整体逻辑。
- 61: `ChevronRight,` -> 执行当前语句，参与该文件整体逻辑。
- 62: `Star,` -> 执行当前语句，参与该文件整体逻辑。
- 63: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 64: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 65: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 66: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 67: `import { TripCalendar } from '@/components/TripCalendar'` -> 导入依赖模块，供当前文件使用。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `type TripItem = {` -> 定义类型或类结构，约束数据与行为。
- 70: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 71: `trip_id: number` -> 执行当前语句，参与该文件整体逻辑。
- 72: `destination_id?: number | null` -> 执行当前语句，参与该文件整体逻辑。
- 73: `day_number: number` -> 执行当前语句，参与该文件整体逻辑。
- 74: `title?: string` -> 执行当前语句，参与该文件整体逻辑。
- 75: `description?: string` -> 执行当前语句，参与该文件整体逻辑。
- 76: `location?: string` -> 执行当前语句，参与该文件整体逻辑。
- 77: `start_time?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 78: `end_time?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 79: `sort_order?: number` -> 执行当前语句，参与该文件整体逻辑。
- 80: `destination?: {` -> 执行当前语句，参与该文件整体逻辑。
- 81: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 82: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 83: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 84: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 85: `cover_image?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 86: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 87: `ticket_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 88: `}` -> 结束当前语句或代码块。
- 89: `}` -> 结束当前语句或代码块。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `type Trip = {` -> 定义类型或类结构，约束数据与行为。
- 92: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 93: `user_id: number` -> 执行当前语句，参与该文件整体逻辑。
- 94: `title: string` -> 执行当前语句，参与该文件整体逻辑。
- 95: `description?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 96: `status?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 97: `start_date?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 98: `end_date?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 99: `created_at?: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 100: `travel_style?: string` -> 执行当前语句，参与该文件整体逻辑。
- 101: `budget_max?: number` -> 执行当前语句，参与该文件整体逻辑。
- 102: `group_size?: number` -> 执行当前语句，参与该文件整体逻辑。
- 103: `}` -> 结束当前语句或代码块。
- 104: `(空行)` -> 空行，用于提升代码结构可读性。
- 105: `function formatDate(dateStr?: string | null) {` -> 定义函数或方法，实现具体业务逻辑。
- 106: `if (!dateStr) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 107: `return dateStr.split('T')[0]` -> 返回结果或提前结束当前流程。
- 108: `}` -> 结束当前语句或代码块。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `function formatTime(v?: string | null) {` -> 定义函数或方法，实现具体业务逻辑。
- 111: `if (!v) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 112: `const s = String(v)` -> 声明变量或常量，保存运行时数据。
- 113: `return s.length >= 5 ? s.slice(0, 5) : s` -> 返回结果或提前结束当前流程。
- 114: `}` -> 结束当前语句或代码块。
- 115: `(空行)` -> 空行，用于提升代码结构可读性。
- 116: `const statusConfig: Record<string, { label: string; color: string; bgColor: string; icon: React.ComponentType<{ className?: string }> }> = {` -> 声明变量或常量，保存运行时数据。
- 117: `pending: { label: '待完善', color: 'text-amber-600', bgColor: 'bg-amber-50', icon: AlertCircle },` -> 执行当前语句，参与该文件整体逻辑。
- 118: `planning: { label: '规划中', color: 'text-blue-600', bgColor: 'bg-blue-50', icon: Clock },` -> 执行当前语句，参与该文件整体逻辑。
- 119: `in_progress: { label: '进行中', color: 'text-emerald-600', bgColor: 'bg-emerald-50', icon: PlayCircle },` -> 执行当前语句，参与该文件整体逻辑。
- 120: `completed: { label: '已完成', color: 'text-violet-600', bgColor: 'bg-violet-50', icon: CheckCircle2 },` -> 执行当前语句，参与该文件整体逻辑。
- 121: `}` -> 结束当前语句或代码块。
- 122: `(空行)` -> 空行，用于提升代码结构可读性。
- 123: `const styleLabels: Record<string, { label: string; emoji: string }> = {` -> 声明变量或常量，保存运行时数据。
- 124: `relaxation: { label: '休闲放松', emoji: '🏖️' },` -> 执行当前语句，参与该文件整体逻辑。
- 125: `cultural: { label: '人文探索', emoji: '🏛️' },` -> 执行当前语句，参与该文件整体逻辑。
- 126: `adventure: { label: '冒险体验', emoji: '🧗' },` -> 执行当前语句，参与该文件整体逻辑。
- 127: `business: { label: '商务出行', emoji: '💼' },` -> 执行当前语句，参与该文件整体逻辑。
- 128: `}` -> 结束当前语句或代码块。
- 129: `(空行)` -> 空行，用于提升代码结构可读性。
- 130: `export default function TripDetailPage() {` -> 导出当前声明，供其他模块复用。
- 131: `const params = useParams()` -> 声明变量或常量，保存运行时数据。
- 132: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 133: `const idParam = params?.id` -> 声明变量或常量，保存运行时数据。
- 134: `const tripId = typeof idParam === 'string' ? parseInt(idParam, 10) : NaN` -> 声明变量或常量，保存运行时数据。
- 135: `(空行)` -> 空行，用于提升代码结构可读性。
- 136: `const { isAuthenticated, user } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 137: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 138: `const [trip, setTrip] = useState<Trip | null>(null)` -> 声明变量或常量，保存运行时数据。
- 139: `const [items, setItems] = useState<TripItem[]>([])` -> 声明变量或常量，保存运行时数据。
- 140: `const [error, setError] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 141: `const [deleting, setDeleting] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 142: `const [viewMode, setViewMode] = useState<'list' | 'calendar'>('list')` -> 声明变量或常量，保存运行时数据。
- 143: `const [selectedDay, setSelectedDay] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 144: `(空行)` -> 空行，用于提升代码结构可读性。
- 145: `const groupedByDay = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 146: `const map = new Map<number, TripItem[]>()` -> 声明变量或常量，保存运行时数据。
- 147: `for (const it of items) {` -> 循环处理集合或重复执行逻辑。
- 148: `const day = Number(it.day_number || 1)` -> 声明变量或常量，保存运行时数据。
- 149: `if (!map.has(day)) map.set(day, [])` -> 条件判断分支，根据场景执行不同逻辑。
- 150: `map.get(day)!.push(it)` -> 执行当前语句，参与该文件整体逻辑。
- 151: `}` -> 结束当前语句或代码块。
- 152: `return Array.from(map.entries())` -> 返回结果或提前结束当前流程。
- 153: `.sort(([a], [b]) => a - b)` -> 执行当前语句，参与该文件整体逻辑。
- 154: `.map(([day, dayItems]) => ({` -> 执行当前语句，参与该文件整体逻辑。
- 155: `day,` -> 执行当前语句，参与该文件整体逻辑。
- 156: `items: dayItems.sort((a, b) => (a.sort_order ?? 0) - (b.sort_order ?? 0)),` -> 执行当前语句，参与该文件整体逻辑。
- 157: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 158: `}, [items])` -> 执行当前语句，参与该文件整体逻辑。
- 159: `(空行)` -> 空行，用于提升代码结构可读性。
- 160: `const load = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 161: `if (!Number.isFinite(tripId)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 162: `setError('无效的行程 ID')` -> 执行当前语句，参与该文件整体逻辑。
- 163: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 164: `return` -> 返回结果或提前结束当前流程。
- 165: `}` -> 结束当前语句或代码块。
- 166: `const token = typeof window !== 'undefined' ? localStorage.getItem('auth_token') : null` -> 声明变量或常量，保存运行时数据。
- 167: `if (!token) {` -> 条件判断分支，根据场景执行不同逻辑。
- 168: `setError('请先登录')` -> 执行当前语句，参与该文件整体逻辑。
- 169: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 170: `return` -> 返回结果或提前结束当前流程。
- 171: `}` -> 结束当前语句或代码块。
- 172: `(空行)` -> 空行，用于提升代码结构可读性。
- 173: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 174: `setError(null)` -> 执行当前语句，参与该文件整体逻辑。
- 175: `try {` -> 异常处理逻辑，提升程序健壮性。
- 176: `const res = await fetch(\`/api/trips/${tripId}/items\`, {` -> 声明变量或常量，保存运行时数据。
- 177: `headers: { Authorization: \`Bearer ${token}\` },` -> 执行当前语句，参与该文件整体逻辑。
- 178: `cache: 'no-store',` -> 执行当前语句，参与该文件整体逻辑。
- 179: `})` -> 执行当前语句，参与该文件整体逻辑。
- 180: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 181: `if (res.status === 401) {` -> 条件判断分支，根据场景执行不同逻辑。
- 182: `localStorage.removeItem('auth_token')` -> 执行当前语句，参与该文件整体逻辑。
- 183: `toast.error('登录已过期')` -> 执行当前语句，参与该文件整体逻辑。
- 184: `router.replace(\`/login?returnUrl=${encodeURIComponent(\`/itineraries/${tripId}\`)}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 185: `return` -> 返回结果或提前结束当前流程。
- 186: `}` -> 结束当前语句或代码块。
- 187: `if (res.status === 403) {` -> 条件判断分支，根据场景执行不同逻辑。
- 188: `setError('无权查看该行程')` -> 执行当前语句，参与该文件整体逻辑。
- 189: `return` -> 返回结果或提前结束当前流程。
- 190: `}` -> 结束当前语句或代码块。
- 191: `if (res.status === 404) {` -> 条件判断分支，根据场景执行不同逻辑。
- 192: `setError('行程不存在或已删除')` -> 执行当前语句，参与该文件整体逻辑。
- 193: `return` -> 返回结果或提前结束当前流程。
- 194: `}` -> 结束当前语句或代码块。
- 195: `if (!data?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 196: `setError(data?.error || '加载失败')` -> 执行当前语句，参与该文件整体逻辑。
- 197: `return` -> 返回结果或提前结束当前流程。
- 198: `}` -> 结束当前语句或代码块。
- 199: `setTrip(data.trip || null)` -> 执行当前语句，参与该文件整体逻辑。
- 200: `setItems(Array.isArray(data.items) ? data.items : [])` -> 执行当前语句，参与该文件整体逻辑。
- 201: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 202: `setError('加载失败，请稍后重试')` -> 执行当前语句，参与该文件整体逻辑。
- 203: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 204: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 205: `}` -> 结束当前语句或代码块。
- 206: `}, [tripId, router])` -> 执行当前语句，参与该文件整体逻辑。
- 207: `(空行)` -> 空行，用于提升代码结构可读性。
- 208: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 209: `if (!Number.isFinite(tripId)) return` -> 条件判断分支，根据场景执行不同逻辑。
- 210: `if (!isAuthenticated || !user) {` -> 条件判断分支，根据场景执行不同逻辑。
- 211: `router.replace(\`/login?returnUrl=${encodeURIComponent(\`/itineraries/${tripId}\`)}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 212: `return` -> 返回结果或提前结束当前流程。
- 213: `}` -> 结束当前语句或代码块。
- 214: `load()` -> 执行当前语句，参与该文件整体逻辑。
- 215: `}, [isAuthenticated, user, load, router, tripId])` -> 执行当前语句，参与该文件整体逻辑。
- 216: `(空行)` -> 空行，用于提升代码结构可读性。
- 217: `async function onShare() {` -> 定义函数或方法，实现具体业务逻辑。
- 218: `const url = typeof window !== 'undefined' ? window.location.href : ''` -> 声明变量或常量，保存运行时数据。
- 219: `const title = trip?.title || '我的行程'` -> 声明变量或常量，保存运行时数据。
- 220: `try {` -> 异常处理逻辑，提升程序健壮性。
- 221: `if (typeof navigator !== 'undefined' && navigator.share) {` -> 条件判断分支，根据场景执行不同逻辑。
- 222: `await navigator.share({ title, text: \`${title} — 智能旅游助手\`, url })` -> 执行当前语句，参与该文件整体逻辑。
- 223: `return` -> 返回结果或提前结束当前流程。
- 224: `}` -> 结束当前语句或代码块。
- 225: `await navigator.clipboard.writeText(url)` -> 执行当前语句，参与该文件整体逻辑。
- 226: `toast.success('链接已复制，可粘贴分享')` -> 执行当前语句，参与该文件整体逻辑。
- 227: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 228: `if (e?.name === 'AbortError') return` -> 条件判断分支，根据场景执行不同逻辑。
- 229: `toast.error('分享失败，请手动复制地址栏链接')` -> 执行当前语句，参与该文件整体逻辑。
- 230: `}` -> 结束当前语句或代码块。
- 231: `}` -> 结束当前语句或代码块。
- 232: `(空行)` -> 空行，用于提升代码结构可读性。
- 233: `async function onDelete() {` -> 定义函数或方法，实现具体业务逻辑。
- 234: `if (!trip || deleting) return` -> 条件判断分支，根据场景执行不同逻辑。
- 235: `if (!confirm(\`确定删除「${trip.title}」？此操作不可恢复。\`)) return` -> 条件判断分支，根据场景执行不同逻辑。
- 236: `const token = localStorage.getItem('auth_token')` -> 声明变量或常量，保存运行时数据。
- 237: `if (!token) return` -> 条件判断分支，根据场景执行不同逻辑。
- 238: `setDeleting(true)` -> 执行当前语句，参与该文件整体逻辑。
- 239: `try {` -> 异常处理逻辑，提升程序健壮性。
- 240: `const res = await fetch(\`/api/trips/${trip.id}\`, {` -> 声明变量或常量，保存运行时数据。
- 241: `method: 'DELETE',` -> 执行当前语句，参与该文件整体逻辑。
- 242: `headers: { Authorization: \`Bearer ${token}\` },` -> 执行当前语句，参与该文件整体逻辑。
- 243: `})` -> 执行当前语句，参与该文件整体逻辑。
- 244: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 245: `if (!res.ok || !data?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 246: `throw new Error(data?.error || \`HTTP ${res.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 247: `}` -> 结束当前语句或代码块。
- 248: `toast.success('已删除')` -> 执行当前语句，参与该文件整体逻辑。
- 249: `router.replace('/itineraries')` -> 执行当前语句，参与该文件整体逻辑。
- 250: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 251: `toast.error(e?.message || '删除失败')` -> 执行当前语句，参与该文件整体逻辑。
- 252: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 253: `setDeleting(false)` -> 执行当前语句，参与该文件整体逻辑。
- 254: `}` -> 结束当前语句或代码块。
- 255: `}` -> 结束当前语句或代码块。
- 256: `(空行)` -> 空行，用于提升代码结构可读性。
- 257: `if (!Number.isFinite(tripId)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 258: `return (` -> 返回结果或提前结束当前流程。
- 259: `<div className="min-h-screen bg-slate-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 260: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 261: `<main className="pt-20 px-4 text-center text-slate-600">无效的行程地址</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 262: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 263: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 264: `)` -> 结束当前语句或代码块。
- 265: `}` -> 结束当前语句或代码块。
- 266: `(空行)` -> 空行，用于提升代码结构可读性。
- 267: `const status = trip?.status || 'pending'` -> 声明变量或常量，保存运行时数据。
- 268: `const statusInfo = statusConfig[status] || statusConfig.pending` -> 声明变量或常量，保存运行时数据。
- 269: `const StatusIcon = statusInfo.icon` -> 声明变量或常量，保存运行时数据。
- 270: `const styleInfo = trip?.travel_style ? styleLabels[trip.travel_style] : null` -> 声明变量或常量，保存运行时数据。
- 271: `const coverGradients = [` -> 声明变量或常量，保存运行时数据。
- 272: `'from-sky-600 to-blue-700',` -> 执行当前语句，参与该文件整体逻辑。
- 273: `'from-emerald-600 to-teal-700',` -> 执行当前语句，参与该文件整体逻辑。
- 274: `'from-coral-500 to-coral-600',` -> 执行当前语句，参与该文件整体逻辑。
- 275: `'from-violet-600 to-indigo-700',` -> 执行当前语句，参与该文件整体逻辑。
- 276: `]` -> 执行当前语句，参与该文件整体逻辑。
- 277: `const gradient = coverGradients[tripId % coverGradients.length] || coverGradients[0]` -> 声明变量或常量，保存运行时数据。
- 278: `(空行)` -> 空行，用于提升代码结构可读性。
- 279: `return (` -> 返回结果或提前结束当前流程。
- 280: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 281: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 282: `<main className="pt-16 pb-28 lg:pb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 283: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 284: `{/* 顶部导航 */}` -> 执行当前语句，参与该文件整体逻辑。
- 285: `<div className="flex items-center gap-2 mb-6 text-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 286: `<Link href="/itineraries" className="text-slate-500 hover:text-blue-600 transition-colors flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 287: `<ArrowLeft className="h-4 w-4" />返回行程列表` -> 执行当前语句，参与该文件整体逻辑。
- 288: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 289: `<span className="text-slate-300">/</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 290: `<Link href="/itineraries/routes" className="text-slate-500 hover:text-blue-600 transition-colors flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 291: `行程路线` -> 执行当前语句，参与该文件整体逻辑。
- 292: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 293: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 294: `(空行)` -> 空行，用于提升代码结构可读性。
- 295: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 296: `<div className="flex flex-col items-center justify-center py-20 gap-3 text-slate-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 297: `<Loader2 className="h-10 w-10 animate-spin text-blue-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 298: `<span className="text-lg">加载行程中…</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 299: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `) : error ? (` -> 执行当前语句，参与该文件整体逻辑。
- 301: `<div className="rounded-2xl border border-red-200 bg-red-50 p-8 text-red-800">` -> JSX/HTML 结构行，用于描述页面元素。
- 302: `<div className="flex items-center gap-2 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 303: `<AlertCircle className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 304: `<p className="font-semibold text-lg">{error}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 305: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 306: `<div className="mt-4 flex flex-wrap gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 307: `<Link href="/itineraries" className="px-4 py-2 border border-red-300 rounded-xl hover:bg-red-100 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 308: `返回行程列表` -> 执行当前语句，参与该文件整体逻辑。
- 309: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 310: `{error === '请先登录' && (` -> 执行当前语句，参与该文件整体逻辑。
- 311: `<Link href={\`/login?returnUrl=${encodeURIComponent(\`/itineraries/${tripId}\`)}\`} className="px-4 py-2 bg-red-600 text-white rounded-xl hover:bg-red-700 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 312: `去登录` -> 执行当前语句，参与该文件整体逻辑。
- 313: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 314: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 315: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 316: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 317: `) : trip ? (` -> 执行当前语句，参与该文件整体逻辑。
- 318: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 319: `{/* 主封面卡片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 320: `<div className={\`rounded-3xl bg-gradient-to-br ${gradient} p-6 text-white mb-6 shadow-xl\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 321: `<div className="flex flex-col sm:flex-row sm:items-start justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 322: `<div className="flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 323: `<div className="flex items-center gap-3 mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 324: `<span className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-sm font-medium bg-white/20 backdrop-blur-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 325: `<StatusIcon className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 326: `{statusInfo.label}` -> 执行当前语句，参与该文件整体逻辑。
- 327: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 328: `{styleInfo && <span className="text-lg">{styleInfo.emoji}</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 329: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 330: `<h1 className="text-3xl font-bold mb-2">{trip.title}</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 331: `{trip.description && <p className="text-white/90 text-lg mb-4">{trip.description}</p>}` -> 执行当前语句，参与该文件整体逻辑。
- 332: `<div className="flex flex-wrap items-center gap-3 text-white/80 text-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 333: `{trip.start_date && trip.end_date && (` -> 执行当前语句，参与该文件整体逻辑。
- 334: `<div className="flex items-center gap-2 bg-white/10 px-3 py-1.5 rounded-lg backdrop-blur-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 335: `<Calendar className="h-4 w-4" />{formatDate(trip.start_date)} ~ {formatDate(trip.end_date)}` -> 执行当前语句，参与该文件整体逻辑。
- 336: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 337: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 338: `{trip.group_size && (` -> 执行当前语句，参与该文件整体逻辑。
- 339: `<div className="flex items-center gap-2 bg-white/10 px-3 py-1.5 rounded-lg backdrop-blur-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 340: `<Users className="h-4 w-4" />{trip.group_size}人` -> 执行当前语句，参与该文件整体逻辑。
- 341: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 342: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 343: `{trip.budget_max && (` -> 执行当前语句，参与该文件整体逻辑。
- 344: `<div className="flex items-center gap-2 bg-white/10 px-3 py-1.5 rounded-lg backdrop-blur-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 345: `<DollarSign className="h-4 w-4" />预算 ¥{trip.budget_max}` -> 执行当前语句，参与该文件整体逻辑。
- 346: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 347: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 348: `{styleInfo && (` -> 执行当前语句，参与该文件整体逻辑。
- 349: `<div className="flex items-center gap-2 bg-white/10 px-3 py-1.5 rounded-lg backdrop-blur-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 350: `<Plane className="h-4 w-4" />{styleInfo.label}` -> 执行当前语句，参与该文件整体逻辑。
- 351: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 352: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 353: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 354: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 355: `<div className="flex flex-wrap gap-2 shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 356: `<button onClick={onShare} className="inline-flex items-center gap-2 px-4 py-2 bg-white/20 backdrop-blur-sm rounded-xl hover:bg-white/30 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 357: `<Share2 className="h-4 w-4" />分享` -> 执行当前语句，参与该文件整体逻辑。
- 358: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 359: `<button onClick={onDelete} disabled={deleting} className="inline-flex items-center gap-2 px-4 py-2 bg-red-500/80 hover:bg-red-500 rounded-xl transition disabled:opacity-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 360: `{deleting ? <Loader2 className="h-4 w-4 animate-spin" /> : <Trash2 className="h-4 w-4" />}删除` -> 执行当前语句，参与该文件整体逻辑。
- 361: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 362: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 363: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 364: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 365: `(空行)` -> 空行，用于提升代码结构可读性。
- 366: `{/* 统计卡片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 367: `<div className="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `<div className="bg-white rounded-xl border border-slate-200 p-4 text-center shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `<div className="text-3xl font-bold text-blue-600">{items.length}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 370: `<div className="text-sm text-slate-500 mt-1">行程节点</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 371: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `<div className="bg-white rounded-xl border border-slate-200 p-4 text-center shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 373: `<div className="text-3xl font-bold text-emerald-600">{groupedByDay.length}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 374: `<div className="text-sm text-slate-500 mt-1">旅行天数</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 375: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 376: `<div className="bg-white rounded-xl border border-slate-200 p-4 text-center shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 377: `<div className="text-3xl font-bold text-violet-600">{new Set(items.map(i => i.destination_id).filter(Boolean)).size}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 378: `<div className="text-sm text-slate-500 mt-1">涉及景点</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 379: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `<div className="bg-white rounded-xl border border-slate-200 p-4 text-center shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `<div className="text-3xl font-bold text-coral-500">{trip.budget_max ? \`¥${trip.budget_max}\` : '-'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 382: `<div className="text-sm text-slate-500 mt-1">预算上限</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 383: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 384: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 385: `(空行)` -> 空行，用于提升代码结构可读性。
- 386: `{/* AI 优化建议 */}` -> 执行当前语句，参与该文件整体逻辑。
- 387: `<div className="bg-gradient-to-r from-purple-50 to-blue-50 rounded-xl border border-purple-100 p-4 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 388: `<div className="flex items-start gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 389: `<div className="w-10 h-10 bg-gradient-to-br from-purple-500 to-blue-500 rounded-xl flex items-center justify-center shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 390: `<Sparkles className="h-5 w-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 391: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 392: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 393: `<h3 className="font-semibold text-slate-900">AI 行程优化</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 394: `<p className="text-sm text-slate-600 mt-1">根据您的行程安排，AI 可以帮您优化路线、推荐最佳用餐地点、提醒注意事项等。</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 395: `<Link href={\`/assistant?trip_id=${tripId}\`} className="inline-flex items-center gap-2 mt-3 text-sm text-blue-600 hover:text-blue-700 font-medium">` -> JSX/HTML 结构行，用于描述页面元素。
- 396: `让 AI 优化行程<ExternalLink className="h-3.5 w-3.5" />` -> 执行当前语句，参与该文件整体逻辑。
- 397: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 398: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 399: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 400: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 401: `(空行)` -> 空行，用于提升代码结构可读性。
- 402: `{/* 每日行程 */}` -> 执行当前语句，参与该文件整体逻辑。
- 403: `<div className="mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 404: `<div className="flex items-center justify-between mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 405: `<h2 className="text-xl font-bold text-slate-900 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 406: `<Calendar className="h-5 w-5 text-blue-600" />每日行程安排` -> 执行当前语句，参与该文件整体逻辑。
- 407: `</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 408: `{items.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 409: `<div className="flex items-center gap-2 bg-white rounded-lg border border-slate-200 p-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 410: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 411: `onClick={() => setViewMode('list')}` -> 执行当前语句，参与该文件整体逻辑。
- 412: `className={\`px-3 py-1.5 rounded-md text-sm font-medium transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 413: `viewMode === 'list'` -> 执行当前语句，参与该文件整体逻辑。
- 414: `? 'bg-sky-100 text-sky-700'` -> 执行当前语句，参与该文件整体逻辑。
- 415: `: 'text-slate-600 hover:bg-slate-50'` -> 执行当前语句，参与该文件整体逻辑。
- 416: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 417: `>` -> 执行当前语句，参与该文件整体逻辑。
- 418: `列表` -> 执行当前语句，参与该文件整体逻辑。
- 419: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 420: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 421: `onClick={() => setViewMode('calendar')}` -> 执行当前语句，参与该文件整体逻辑。
- 422: `className={\`px-3 py-1.5 rounded-md text-sm font-medium transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 423: `viewMode === 'calendar'` -> 执行当前语句，参与该文件整体逻辑。
- 424: `? 'bg-sky-100 text-sky-700'` -> 执行当前语句，参与该文件整体逻辑。
- 425: `: 'text-slate-600 hover:bg-slate-50'` -> 执行当前语句，参与该文件整体逻辑。
- 426: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 427: `>` -> 执行当前语句，参与该文件整体逻辑。
- 428: `日历` -> 执行当前语句，参与该文件整体逻辑。
- 429: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 430: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 431: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 432: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 433: `(空行)` -> 空行，用于提升代码结构可读性。
- 434: `{items.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 435: `<div className="rounded-2xl border border-dashed border-slate-300 bg-white/80 p-10 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 436: `<Sparkles className="h-14 w-14 text-coral-500 mx-auto mb-3 opacity-90" />` -> JSX/HTML 结构行，用于描述页面元素。
- 437: `<p className="text-slate-700 text-lg font-medium">该行程暂无安排</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 438: `<p className="text-slate-500 mt-1 mb-6">让 AI 帮您规划完美的行程吧！</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 439: `<Link href="/assistant" className="inline-flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-blue-600 to-sky-500 text-white rounded-xl font-medium hover:shadow-lg transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 440: `<Sparkles className="h-5 w-5" />AI 生成行程` -> 执行当前语句，参与该文件整体逻辑。
- 441: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 442: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 443: `) : viewMode === 'calendar' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 444: `<div className="grid grid-cols-1 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 445: `<div className="lg:col-span-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 446: `<TripCalendar` -> 执行当前语句，参与该文件整体逻辑。
- 447: `trip={trip}` -> 执行当前语句，参与该文件整体逻辑。
- 448: `items={items}` -> 执行当前语句，参与该文件整体逻辑。
- 449: `selectedDay={selectedDay}` -> 执行当前语句，参与该文件整体逻辑。
- 450: `onDayClick={setSelectedDay}` -> 执行当前语句，参与该文件整体逻辑。
- 451: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 452: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 453: `<div className="lg:col-span-2 space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 454: `{groupedByDay` -> 执行当前语句，参与该文件整体逻辑。
- 455: `.filter(({ day }) => selectedDay === 0 || day === selectedDay)` -> 执行当前语句，参与该文件整体逻辑。
- 456: `.map(({ day, items: dayItems }) => {` -> 执行当前语句，参与该文件整体逻辑。
- 457: `const totalDuration = dayItems.reduce((acc, it) => {` -> 声明变量或常量，保存运行时数据。
- 458: `if (it.start_time && it.end_time) {` -> 条件判断分支，根据场景执行不同逻辑。
- 459: `const start = it.start_time.split(':').map(Number)` -> 声明变量或常量，保存运行时数据。
- 460: `const end = it.end_time.split(':').map(Number)` -> 声明变量或常量，保存运行时数据。
- 461: `return acc + (end[0] * 60 + end[1]) - (start[0] * 60 + start[1])` -> 返回结果或提前结束当前流程。
- 462: `}` -> 结束当前语句或代码块。
- 463: `return acc` -> 返回结果或提前结束当前流程。
- 464: `}, 0)` -> 执行当前语句，参与该文件整体逻辑。
- 465: `const hours = Math.floor(totalDuration / 60)` -> 声明变量或常量，保存运行时数据。
- 466: `const mins = totalDuration % 60` -> 声明变量或常量，保存运行时数据。
- 467: `(空行)` -> 空行，用于提升代码结构可读性。
- 468: `return (` -> 返回结果或提前结束当前流程。
- 469: `<div key={day} className="rounded-2xl border border-slate-200 bg-white shadow-sm overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 470: `<div className="bg-gradient-to-r from-slate-800 to-slate-700 px-5 py-4 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 471: `<div className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 472: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 473: `<div className="w-10 h-10 bg-white/20 rounded-xl flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 474: `<span className="text-lg font-bold">D{day}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 475: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 476: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `<div className="font-bold text-lg">第 {day} 天</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 478: `{dayItems[0]?.location && (` -> 执行当前语句，参与该文件整体逻辑。
- 479: `<div className="text-white/70 text-sm flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 480: `<MapPin className="h-3.5 w-3.5" />{dayItems[0].location.split(',')[0]}` -> 执行当前语句，参与该文件整体逻辑。
- 481: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 482: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 483: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 484: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 485: `{totalDuration > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 486: `<div className="text-sm text-white/80 bg-white/10 px-3 py-1.5 rounded-lg">` -> JSX/HTML 结构行，用于描述页面元素。
- 487: `总计 {hours > 0 ? \`${hours}小时\` : ''}{mins > 0 ? \`${mins}分钟\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 488: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 489: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 490: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 491: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `<div className="p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 493: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 494: `<div className="absolute left-6 top-0 bottom-0 w-0.5 bg-slate-200" />` -> JSX/HTML 结构行，用于描述页面元素。
- 495: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 496: `{dayItems.map((it, index) => (` -> 执行当前语句，参与该文件整体逻辑。
- 497: `<div key={it.id} className="relative flex gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 498: `<div className={\`relative z-10 w-12 h-12 rounded-xl flex items-center justify-center shrink-0 ${` -> 执行当前语句，参与该文件整体逻辑。
- 499: `it.destination?.cover_image ? 'bg-blue-100 text-blue-600' : 'bg-slate-100 text-slate-600'` -> 执行当前语句，参与该文件整体逻辑。
- 500: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 501: `<span className="text-sm font-bold">{index + 1}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 502: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 503: `<div className="flex-1 min-w-0 pt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 504: `{it.start_time && (` -> 执行当前语句，参与该文件整体逻辑。
- 505: `<span className="inline-block px-2 py-0.5 bg-blue-50 text-blue-700 text-xs rounded-md mb-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 506: `{it.start_time}{it.end_time ? \` - ${it.end_time}\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 507: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 508: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 509: `<h4 className="font-semibold text-slate-900">{it.title || '未命名行程'}</h4>` -> JSX/HTML 结构行，用于描述页面元素。
- 510: `{it.description && (` -> 执行当前语句，参与该文件整体逻辑。
- 511: `<p className="text-sm text-slate-600 mt-1 line-clamp-2">{it.description}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 512: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 513: `{it.location && (` -> 执行当前语句，参与该文件整体逻辑。
- 514: `<p className="text-xs text-slate-500 mt-1 flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 515: `<MapPin className="h-3 w-3" />{it.location}` -> 执行当前语句，参与该文件整体逻辑。
- 516: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 517: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 518: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 519: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 520: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 521: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 522: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 523: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 524: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 525: `)` -> 结束当前语句或代码块。
- 526: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 527: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 528: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 529: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 530: `<div className="space-y-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 531: `{groupedByDay.map(({ day, items: dayItems }) => {` -> 执行当前语句，参与该文件整体逻辑。
- 532: `const totalDuration = dayItems.reduce((acc, it) => {` -> 声明变量或常量，保存运行时数据。
- 533: `if (it.start_time && it.end_time) {` -> 条件判断分支，根据场景执行不同逻辑。
- 534: `const start = it.start_time.split(':').map(Number)` -> 声明变量或常量，保存运行时数据。
- 535: `const end = it.end_time.split(':').map(Number)` -> 声明变量或常量，保存运行时数据。
- 536: `return acc + (end[0] * 60 + end[1]) - (start[0] * 60 + start[1])` -> 返回结果或提前结束当前流程。
- 537: `}` -> 结束当前语句或代码块。
- 538: `return acc` -> 返回结果或提前结束当前流程。
- 539: `}, 0)` -> 执行当前语句，参与该文件整体逻辑。
- 540: `const hours = Math.floor(totalDuration / 60)` -> 声明变量或常量，保存运行时数据。
- 541: `const mins = totalDuration % 60` -> 声明变量或常量，保存运行时数据。
- 542: `(空行)` -> 空行，用于提升代码结构可读性。
- 543: `return (` -> 返回结果或提前结束当前流程。
- 544: `<div key={day} className="rounded-2xl border border-slate-200 bg-white shadow-sm overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 545: `<div className="bg-gradient-to-r from-slate-800 to-slate-700 px-5 py-4 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 546: `<div className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 547: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 548: `<div className="w-10 h-10 bg-white/20 rounded-xl flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 549: `<span className="text-lg font-bold">D{day}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 550: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 551: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 552: `<div className="font-bold text-lg">第 {day} 天</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 553: `{dayItems[0]?.location && (` -> 执行当前语句，参与该文件整体逻辑。
- 554: `<div className="text-white/70 text-sm flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 555: `<MapPin className="h-3.5 w-3.5" />{dayItems[0].location.split(',')[0]}` -> 执行当前语句，参与该文件整体逻辑。
- 556: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 557: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 558: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 559: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 560: `{totalDuration > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 561: `<div className="text-sm text-white/80 bg-white/10 px-3 py-1.5 rounded-lg">` -> JSX/HTML 结构行，用于描述页面元素。
- 562: `总计 {hours > 0 ? \`${hours}小时\` : ''}{mins > 0 ? \`${mins}分钟\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 563: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 564: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 565: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 566: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 567: `<div className="p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 568: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 569: `<div className="absolute left-6 top-0 bottom-0 w-0.5 bg-slate-200" />` -> JSX/HTML 结构行，用于描述页面元素。
- 570: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 571: `{dayItems.map((it, index) => (` -> 执行当前语句，参与该文件整体逻辑。
- 572: `<div key={it.id} className="relative flex gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 573: `<div className={\`relative z-10 w-12 h-12 rounded-xl flex items-center justify-center shrink-0 ${` -> 执行当前语句，参与该文件整体逻辑。
- 574: `it.destination?.cover_image ? 'bg-blue-100 text-blue-600' : 'bg-slate-100 text-slate-600'` -> 执行当前语句，参与该文件整体逻辑。
- 575: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 576: `{it.destination?.cover_image ? <Star className="h-5 w-5" /> : <MapPin className="h-5 w-5" />}` -> 执行当前语句，参与该文件整体逻辑。
- 577: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 578: `<div className="flex-1 bg-slate-50 rounded-xl p-4 border border-slate-100 hover:border-blue-200 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 579: `<div className="flex items-start justify-between gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 580: `<div className="flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 581: `<div className="flex items-center gap-2 mb-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 582: `<h4 className="font-semibold text-slate-900">{it.title || '行程节点'}</h4>` -> JSX/HTML 结构行，用于描述页面元素。
- 583: `{index === 0 && <span className="px-2 py-0.5 bg-blue-100 text-blue-600 text-xs rounded-full">起点</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 584: `{index === dayItems.length - 1 && <span className="px-2 py-0.5 bg-emerald-100 text-emerald-600 text-xs rounded-full">终点</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 585: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 586: `{(it.start_time || it.end_time) && (` -> 执行当前语句，参与该文件整体逻辑。
- 587: `<div className="flex items-center gap-1.5 text-sm text-slate-500 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 588: `<Clock className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 589: `{it.start_time && formatTime(it.start_time)}` -> 执行当前语句，参与该文件整体逻辑。
- 590: `{it.start_time && it.end_time && ' - '}` -> 执行当前语句，参与该文件整体逻辑。
- 591: `{it.end_time && formatTime(it.end_time)}` -> 执行当前语句，参与该文件整体逻辑。
- 592: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 593: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 594: `{it.location && (` -> 执行当前语句，参与该文件整体逻辑。
- 595: `<div className="flex items-center gap-1.5 text-sm text-slate-600 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 596: `<MapPin className="h-3.5 w-3.5" />{it.location}` -> 执行当前语句，参与该文件整体逻辑。
- 597: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 598: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 599: `{it.description && <p className="text-sm text-slate-700 whitespace-pre-wrap">{it.description}</p>}` -> 执行当前语句，参与该文件整体逻辑。
- 600: `{it.destination && (` -> 执行当前语句，参与该文件整体逻辑。
- 601: `<Link href={\`/destinations/${it.destination.id}\`} className="inline-flex items-center gap-2 mt-3 px-3 py-1.5 bg-white border border-blue-200 rounded-lg text-sm text-blue-600 hover:bg-blue-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 602: `<Star className="h-3.5 w-3.5" />查看 {it.destination.name}` -> 执行当前语句，参与该文件整体逻辑。
- 603: `{it.destination.rating && <span className="text-amber-500">★{it.destination.rating}</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 604: `{it.destination.ticket_price !== undefined && it.destination.ticket_price > 0 && <span className="text-slate-500">¥{it.destination.ticket_price}</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 605: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 606: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 607: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 608: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 609: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 610: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 611: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 612: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 613: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 614: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 615: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 616: `)` -> 结束当前语句或代码块。
- 617: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 618: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 619: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 620: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 621: `(空行)` -> 空行，用于提升代码结构可读性。
- 622: `{/* 操作按钮 */}` -> 执行当前语句，参与该文件整体逻辑。
- 623: `<div className="bg-white rounded-2xl border border-slate-200 p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 624: `<h3 className="font-semibold text-slate-900 mb-4">更多操作</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 625: `<div className="flex flex-wrap gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 626: `<Link href={\`/assistant?trip_id=${tripId}\`} className="inline-flex items-center gap-2 px-4 py-2.5 bg-gradient-to-r from-blue-600 to-sky-500 text-white rounded-xl hover:shadow-lg transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 627: `<Sparkles className="h-4 w-4" />AI 优化行程` -> 执行当前语句，参与该文件整体逻辑。
- 628: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 629: `<Link href="/destinations" className="inline-flex items-center gap-2 px-4 py-2.5 border border-slate-300 rounded-xl text-slate-700 hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 630: `<Plus className="h-4 w-4" />添加景点` -> 执行当前语句，参与该文件整体逻辑。
- 631: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 632: `<button onClick={onShare} className="inline-flex items-center gap-2 px-4 py-2.5 border border-slate-300 rounded-xl text-slate-700 hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 633: `<Share2 className="h-4 w-4" />分享行程` -> 执行当前语句，参与该文件整体逻辑。
- 634: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 635: `<button onClick={onDelete} disabled={deleting} className="inline-flex items-center gap-2 px-4 py-2.5 border border-red-200 text-red-600 rounded-xl hover:bg-red-50 transition disabled:opacity-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 636: `{deleting ? <Loader2 className="h-4 w-4 animate-spin" /> : <Trash2 className="h-4 w-4" />}删除行程` -> 执行当前语句，参与该文件整体逻辑。
- 637: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 638: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 639: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 640: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 641: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 642: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 643: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 644: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 645: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 646: `)` -> 结束当前语句或代码块。
- 647: `}` -> 结束当前语句或代码块。
