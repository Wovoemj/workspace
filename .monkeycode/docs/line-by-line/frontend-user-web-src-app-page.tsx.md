# `frontend/user-web/src/app/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 首页模块 - 小游智能旅行助手` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 6: `*   - 首页主入口，展示热门目的地和精选产品` -> 注释行，用于解释设计意图或使用说明。
- 7: `*   - 提供搜索功能、分类快速入口` -> 注释行，用于解释设计意图或使用说明。
- 8: `*   - 可拖动悬浮的 AI 助手入口` -> 注释行，用于解释设计意图或使用说明。
- 9: `*` -> 注释行，用于解释设计意图或使用说明。
- 10: `* 依赖项：` -> 注释行，用于解释设计意图或使用说明。
- 11: `*   - components/Navbar：顶部导航栏` -> 注释行，用于解释设计意图或使用说明。
- 12: `*   - components/Footer：底部页脚` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - components/ProductCard：产品卡片组件` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - types/index：TypeScript 类型定义` -> 注释行，用于解释设计意图或使用说明。
- 15: `*   - lib/display：显示格式化工具函数` -> 注释行，用于解释设计意图或使用说明。
- 16: `*` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 数据来源：` -> 注释行，用于解释设计意图或使用说明。
- 18: `*   - GET /api/destinations：热门目的地列表` -> 注释行，用于解释设计意图或使用说明。
- 19: `*   - GET /api/products：精选产品列表` -> 注释行，用于解释设计意图或使用说明。
- 20: `*` -> 注释行，用于解释设计意图或使用说明。
- 21: `* 状态管理：` -> 注释行，用于解释设计意图或使用说明。
- 22: `*   - 使用 React useState 管理组件状态` -> 注释行，用于解释设计意图或使用说明。
- 23: `*   - 5分钟数据缓存机制` -> 注释行，用于解释设计意图或使用说明。
- 24: `*/` -> 注释行，用于解释设计意图或使用说明。
- 25: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `import { FormEvent, useEffect, useMemo, useRef, useState, useCallback, Suspense } from 'react'` -> 导入依赖模块，供当前文件使用。
- 28: `import { useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 29: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 30: `import dynamic from 'next/dynamic'` -> 导入依赖模块，供当前文件使用。
- 31: `import { CalendarDays, ChevronLeft, ChevronRight, Drama, Hotel, MapPin, Plane, Search, Star, Ticket } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 32: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 33: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 34: `import { ProductCard } from '@/components/ProductCard'` -> 导入依赖模块，供当前文件使用。
- 35: `import type { Product } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 36: `import { formatRating, shouldShowRating } from '@/lib/display'` -> 导入依赖模块，供当前文件使用。
- 37: `import { apiMediaUrl, FALLBACK_MEDIA_PATH, onImgErrorUseFallback, resolveCoverSrc } from '@/lib/media'` -> 导入依赖模块，供当前文件使用。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `type Destination = {` -> 定义类型或类结构，约束数据与行为。
- 40: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 41: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 42: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 43: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 44: `description?: string` -> 执行当前语句，参与该文件整体逻辑。
- 45: `cover_image?: string` -> 执行当前语句，参与该文件整体逻辑。
- 46: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 47: `ticket_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 48: `}` -> 结束当前语句或代码块。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `function formatDestinationTicket(d: Destination) {` -> 定义函数或方法，实现具体业务逻辑。
- 51: `const v = typeof d.ticket_price === 'number' ? d.ticket_price : NaN` -> 声明变量或常量，保存运行时数据。
- 52: `if (Number.isFinite(v) && v > 0) return \`¥${Number(v).toLocaleString()}起\`` -> 条件判断分支，根据场景执行不同逻辑。
- 53: `if (Number.isFinite(v) && v === 0) return '免费'` -> 条件判断分支，根据场景执行不同逻辑。
- 54: `return '价格待补充'` -> 返回结果或提前结束当前流程。
- 55: `}` -> 结束当前语句或代码块。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `// 生成图片 srcset` -> 注释行，用于解释设计意图或使用说明。
- 58: `function generateSrcSet(baseSrc: string): string {` -> 定义函数或方法，实现具体业务逻辑。
- 59: `if (!baseSrc || baseSrc.startsWith('data:') || baseSrc.includes('placeholder')) {` -> 条件判断分支，根据场景执行不同逻辑。
- 60: `return ''` -> 返回结果或提前结束当前流程。
- 61: `}` -> 结束当前语句或代码块。
- 62: `const widths = [320, 480, 640, 800, 1200]` -> 声明变量或常量，保存运行时数据。
- 63: `return widths` -> 返回结果或提前结束当前流程。
- 64: `.map(w => {` -> 执行当前语句，参与该文件整体逻辑。
- 65: `if (baseSrc.includes('?')) {` -> 条件判断分支，根据场景执行不同逻辑。
- 66: `return \`${baseSrc}&w=${w} ${w}w\`` -> 返回结果或提前结束当前流程。
- 67: `}` -> 结束当前语句或代码块。
- 68: `return \`${baseSrc}?w=${w} ${w}w\`` -> 返回结果或提前结束当前流程。
- 69: `})` -> 执行当前语句，参与该文件整体逻辑。
- 70: `.join(', ')` -> 执行当前语句，参与该文件整体逻辑。
- 71: `}` -> 结束当前语句或代码块。
- 72: `(空行)` -> 空行，用于提升代码结构可读性。
- 73: `// 骨架屏组件` -> 注释行，用于解释设计意图或使用说明。
- 74: `function SkeletonCard({ className = '' }: { className?: string }) {` -> 定义函数或方法，实现具体业务逻辑。
- 75: `return (` -> 返回结果或提前结束当前流程。
- 76: `<div className={\`rounded-2xl bg-card border border-border/70 p-3 animate-pulse ${className}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `<div className="h-36 sm:aspect-[16/10] sm:h-auto rounded-xl bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `<div className="mt-3 h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-1/2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 80: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 81: `)` -> 结束当前语句或代码块。
- 82: `}` -> 结束当前语句或代码块。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `/* ==================== 可拖动 AI 助手 ==================== */` -> 注释行，用于解释设计意图或使用说明。
- 85: `function DraggableAIAssistant() {` -> 定义函数或方法，实现具体业务逻辑。
- 86: `const [position, setPosition] = useState({ x: -1, y: 80 })` -> 声明变量或常量，保存运行时数据。
- 87: `const [isDragging, setIsDragging] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 88: `const [hasMoved, setHasMoved] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 89: `const dragRef = useRef<{ startX: number; startY: number; startPosX: number; startPosY: number } | null>(null)` -> 声明变量或常量，保存运行时数据。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `const defaultRight = typeof window !== 'undefined' ? window.innerWidth - 56 - 24 : 600` -> 声明变量或常量，保存运行时数据。
- 92: `(空行)` -> 空行，用于提升代码结构可读性。
- 93: `const handleMouseDown = (e: React.MouseEvent) => {` -> 声明变量或常量，保存运行时数据。
- 94: `e.preventDefault()` -> 执行当前语句，参与该文件整体逻辑。
- 95: `e.stopPropagation()` -> 执行当前语句，参与该文件整体逻辑。
- 96: `setIsDragging(true)` -> 执行当前语句，参与该文件整体逻辑。
- 97: `setHasMoved(false)` -> 执行当前语句，参与该文件整体逻辑。
- 98: `dragRef.current = {` -> 执行当前语句，参与该文件整体逻辑。
- 99: `startX: e.clientX,` -> 执行当前语句，参与该文件整体逻辑。
- 100: `startY: e.clientY,` -> 执行当前语句，参与该文件整体逻辑。
- 101: `startPosX: position.x < 0 ? defaultRight : position.x,` -> 执行当前语句，参与该文件整体逻辑。
- 102: `startPosY: position.y,` -> 执行当前语句，参与该文件整体逻辑。
- 103: `}` -> 结束当前语句或代码块。
- 104: `}` -> 结束当前语句或代码块。
- 105: `(空行)` -> 空行，用于提升代码结构可读性。
- 106: `const handleTouchStart = (e: React.TouchEvent) => {` -> 声明变量或常量，保存运行时数据。
- 107: `const touch = e.touches[0]` -> 声明变量或常量，保存运行时数据。
- 108: `setIsDragging(true)` -> 执行当前语句，参与该文件整体逻辑。
- 109: `setHasMoved(false)` -> 执行当前语句，参与该文件整体逻辑。
- 110: `dragRef.current = {` -> 执行当前语句，参与该文件整体逻辑。
- 111: `startX: touch.clientX,` -> 执行当前语句，参与该文件整体逻辑。
- 112: `startY: touch.clientY,` -> 执行当前语句，参与该文件整体逻辑。
- 113: `startPosX: position.x < 0 ? defaultRight : position.x,` -> 执行当前语句，参与该文件整体逻辑。
- 114: `startPosY: position.y,` -> 执行当前语句，参与该文件整体逻辑。
- 115: `}` -> 结束当前语句或代码块。
- 116: `}` -> 结束当前语句或代码块。
- 117: `(空行)` -> 空行，用于提升代码结构可读性。
- 118: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 119: `if (!isDragging) return` -> 条件判断分支，根据场景执行不同逻辑。
- 120: `(空行)` -> 空行，用于提升代码结构可读性。
- 121: `const handleMove = (clientX: number, clientY: number) => {` -> 声明变量或常量，保存运行时数据。
- 122: `if (!dragRef.current) return` -> 条件判断分支，根据场景执行不同逻辑。
- 123: `const dx = Math.abs(clientX - dragRef.current.startX)` -> 声明变量或常量，保存运行时数据。
- 124: `const dy = Math.abs(clientY - dragRef.current.startY)` -> 声明变量或常量，保存运行时数据。
- 125: `if (dx > 5 || dy > 5) setHasMoved(true)` -> 条件判断分支，根据场景执行不同逻辑。
- 126: `(空行)` -> 空行，用于提升代码结构可读性。
- 127: `const newX = clientX - dragRef.current.startX + dragRef.current.startPosX` -> 声明变量或常量，保存运行时数据。
- 128: `const newY = clientY - dragRef.current.startY + dragRef.current.startPosY` -> 声明变量或常量，保存运行时数据。
- 129: `setPosition({ x: Math.max(0, newX), y: Math.max(0, newY) })` -> 执行当前语句，参与该文件整体逻辑。
- 130: `}` -> 结束当前语句或代码块。
- 131: `(空行)` -> 空行，用于提升代码结构可读性。
- 132: `const handleMouseMove = (e: MouseEvent) => handleMove(e.clientX, e.clientY)` -> 声明变量或常量，保存运行时数据。
- 133: `const handleTouchMove = (e: TouchEvent) => {` -> 声明变量或常量，保存运行时数据。
- 134: `if (e.touches.length === 1) handleMove(e.touches[0].clientX, e.touches[0].clientY)` -> 条件判断分支，根据场景执行不同逻辑。
- 135: `}` -> 结束当前语句或代码块。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `const handleEnd = () => {` -> 声明变量或常量，保存运行时数据。
- 138: `setIsDragging(false)` -> 执行当前语句，参与该文件整体逻辑。
- 139: `dragRef.current = null` -> 执行当前语句，参与该文件整体逻辑。
- 140: `}` -> 结束当前语句或代码块。
- 141: `(空行)` -> 空行，用于提升代码结构可读性。
- 142: `window.addEventListener('mousemove', handleMouseMove)` -> 执行当前语句，参与该文件整体逻辑。
- 143: `window.addEventListener('mouseup', handleEnd)` -> 执行当前语句，参与该文件整体逻辑。
- 144: `window.addEventListener('touchmove', handleTouchMove, { passive: true })` -> 执行当前语句，参与该文件整体逻辑。
- 145: `window.addEventListener('touchend', handleEnd)` -> 执行当前语句，参与该文件整体逻辑。
- 146: `return () => {` -> 返回结果或提前结束当前流程。
- 147: `window.removeEventListener('mousemove', handleMouseMove)` -> 执行当前语句，参与该文件整体逻辑。
- 148: `window.removeEventListener('mouseup', handleEnd)` -> 执行当前语句，参与该文件整体逻辑。
- 149: `window.removeEventListener('touchmove', handleTouchMove)` -> 执行当前语句，参与该文件整体逻辑。
- 150: `window.removeEventListener('touchend', handleEnd)` -> 执行当前语句，参与该文件整体逻辑。
- 151: `}` -> 结束当前语句或代码块。
- 152: `}, [isDragging])` -> 执行当前语句，参与该文件整体逻辑。
- 153: `(空行)` -> 空行，用于提升代码结构可读性。
- 154: `const handleClick = (e: React.MouseEvent) => {` -> 声明变量或常量，保存运行时数据。
- 155: `if (hasMoved) {` -> 条件判断分支，根据场景执行不同逻辑。
- 156: `e.preventDefault()` -> 执行当前语句，参与该文件整体逻辑。
- 157: `e.stopPropagation()` -> 执行当前语句，参与该文件整体逻辑。
- 158: `setHasMoved(false)` -> 执行当前语句，参与该文件整体逻辑。
- 159: `}` -> 结束当前语句或代码块。
- 160: `}` -> 结束当前语句或代码块。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `return (` -> 返回结果或提前结束当前流程。
- 163: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 164: `className="fixed z-40 flex flex-col items-end gap-2 select-none"` -> 执行当前语句，参与该文件整体逻辑。
- 165: `style={position.x < 0 ? { right: '1.5rem', top: position.y } : { left: position.x, top: position.y }}` -> 执行当前语句，参与该文件整体逻辑。
- 166: `>` -> 执行当前语句，参与该文件整体逻辑。
- 167: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 168: `className="hidden sm:block px-3 py-1.5 rounded-xl rounded-br-sm bg-white/95 shadow shadow-black/10 border border-gray-100 text-[11px] text-gray-400 font-medium whitespace-nowrap ai-bubble-fade"` -> 执行当前语句，参与该文件整体逻辑。
- 169: `>` -> 执行当前语句，参与该文件整体逻辑。
- 170: `有什么旅行计划？` -> 执行当前语句，参与该文件整体逻辑。
- 171: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 173: `href="/assistant"` -> 执行当前语句，参与该文件整体逻辑。
- 174: `aria-label="AI 助手"` -> 执行当前语句，参与该文件整体逻辑。
- 175: `title="AI 助手"` -> 执行当前语句，参与该文件整体逻辑。
- 176: `onClick={handleClick}` -> 执行当前语句，参与该文件整体逻辑。
- 177: `onMouseDown={handleMouseDown}` -> 执行当前语句，参与该文件整体逻辑。
- 178: `onTouchStart={handleTouchStart}` -> 执行当前语句，参与该文件整体逻辑。
- 179: `className="ai-assistant-bubble group flex h-10 w-10 items-center justify-center rounded-2xl shadow-[0_0_20px_rgba(99,102,241,0.6)] bg-gradient-to-br from-blue-500 via-indigo-500 to-violet-500 border-2 border-indigo-300/60 hover:shadow-[0_0_28px_rgba(99,102,241,0.8)] hover:-translate-y-0.5 transition-all"` -> 执行当前语句，参与该文件整体逻辑。
- 180: `>` -> 执行当前语句，参与该文件整体逻辑。
- 181: `<span className="text-white text-base drop-shadow-sm">🤖</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 184: `)` -> 结束当前语句或代码块。
- 185: `}` -> 结束当前语句或代码块。
- 186: `(空行)` -> 空行，用于提升代码结构可读性。
- 187: `function HomeDestinationCarouselCard({ d, coverSrc, ticketLabel, priority = false }: {` -> 定义函数或方法，实现具体业务逻辑。
- 188: `d: Destination` -> 执行当前语句，参与该文件整体逻辑。
- 189: `coverSrc: string` -> 执行当前语句，参与该文件整体逻辑。
- 190: `ticketLabel: string` -> 执行当前语句，参与该文件整体逻辑。
- 191: `priority?: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 192: `}) {` -> 执行当前语句，参与该文件整体逻辑。
- 193: `const [imgLoaded, setImgLoaded] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 194: `const [hasError, setHasError] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 195: `(空行)` -> 空行，用于提升代码结构可读性。
- 196: `const optimizedSrc = hasError ? FALLBACK_MEDIA_PATH : coverSrc` -> 声明变量或常量，保存运行时数据。
- 197: `const srcSet = useMemo(() => generateSrcSet(coverSrc), [coverSrc])` -> 声明变量或常量，保存运行时数据。
- 198: `(空行)` -> 空行，用于提升代码结构可读性。
- 199: `return (` -> 返回结果或提前结束当前流程。
- 200: `<Link href={\`/destinations/${d.id}\`} className="block w-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 201: `<div className="group rounded-2xl border border-white/15 bg-white/60 backdrop-blur-md hover:bg-white/80 transition-all overflow-hidden hover:-translate-y-1 hover:shadow-xl hover:shadow-blue-500/10 will-change-transform mx-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 202: `<div className="relative h-52 sm:h-60">` -> JSX/HTML 结构行，用于描述页面元素。
- 203: `{/* 骨架屏 */}` -> 执行当前语句，参与该文件整体逻辑。
- 204: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 205: `className={\`absolute inset-0 bg-gradient-to-br from-slate-200 to-slate-300 animate-pulse transition-opacity duration-300 ${` -> 执行当前语句，参与该文件整体逻辑。
- 206: `imgLoaded ? 'opacity-0 pointer-events-none' : 'opacity-100'` -> 执行当前语句，参与该文件整体逻辑。
- 207: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 208: `aria-hidden` -> 执行当前语句，参与该文件整体逻辑。
- 209: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 210: `(空行)` -> 空行，用于提升代码结构可读性。
- 211: `{/* 图片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 212: `<img` -> 执行当前语句，参与该文件整体逻辑。
- 213: `src={optimizedSrc}` -> 执行当前语句，参与该文件整体逻辑。
- 214: `srcSet={srcSet || undefined}` -> 执行当前语句，参与该文件整体逻辑。
- 215: `sizes="(max-width: 768px) 50vw, 240px"` -> 执行当前语句，参与该文件整体逻辑。
- 216: `alt={\`${d.name}风景\`}` -> 执行当前语句，参与该文件整体逻辑。
- 217: `className={\`absolute inset-0 h-full w-full object-cover transition-all duration-500 group-hover:scale-105 ${imgLoaded ? 'opacity-100' : 'opacity-0'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 218: `loading={priority ? 'eager' : 'lazy'}` -> 执行当前语句，参与该文件整体逻辑。
- 219: `decoding={priority ? 'sync' : 'async'}` -> 执行当前语句，参与该文件整体逻辑。
- 220: `onLoad={() => setImgLoaded(true)}` -> 执行当前语句，参与该文件整体逻辑。
- 221: `onError={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 222: `if (!hasError) setHasError(true)` -> 条件判断分支，根据场景执行不同逻辑。
- 223: `setImgLoaded(true)` -> 执行当前语句，参与该文件整体逻辑。
- 224: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 225: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 226: `(空行)` -> 空行，用于提升代码结构可读性。
- 227: `{/* 标签 */}` -> 执行当前语句，参与该文件整体逻辑。
- 228: `<div className="absolute top-2 left-2 flex gap-1.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 229: `<span className="px-2 py-0.5 rounded-full text-[10px] font-semibold bg-emerald-500/90 text-white backdrop-blur-sm">必玩推荐</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 230: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `(空行)` -> 空行，用于提升代码结构可读性。
- 232: `{shouldShowRating(d.rating) ? (` -> 执行当前语句，参与该文件整体逻辑。
- 233: `<div className="absolute top-2 right-2 inline-flex items-center gap-1 bg-black/60 backdrop-blur-md border border-white/20 px-2 py-1 rounded-full text-white shadow-lg">` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `<Star className="h-3 w-3 fill-amber-400 text-amber-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `<span className="text-[11px] font-bold text-white drop-shadow-sm">{formatRating(d.rating)}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 238: `(空行)` -> 空行，用于提升代码结构可读性。
- 239: `{/* 底部渐变和信息 */}` -> 执行当前语句，参与该文件整体逻辑。
- 240: `<div className="absolute inset-x-0 bottom-0 h-20 bg-gradient-to-t from-black/50 to-transparent" />` -> JSX/HTML 结构行，用于描述页面元素。
- 241: `<div className="absolute bottom-3 left-3 right-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 242: `<div className="flex items-center gap-1.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 243: `<MapPin className="h-3 w-3 text-white/70 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 244: `<span className="text-white/80 text-[11px] truncate">{d.city} · {d.province}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 245: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 246: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 247: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 248: `<div className="p-3.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 249: `<div className="flex items-center justify-between gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 250: `<h3 className="text-sm font-bold text-slate-800 truncate">{d.name}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 251: `<span className="text-xs font-bold text-orange-500 shrink-0">{ticketLabel}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 252: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 253: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 254: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 255: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 256: `)` -> 结束当前语句或代码块。
- 257: `}` -> 结束当前语句或代码块。
- 258: `(空行)` -> 空行，用于提升代码结构可读性。
- 259: `// 数据缓存` -> 注释行，用于解释设计意图或使用说明。
- 260: `const dataCache = {` -> 声明变量或常量，保存运行时数据。
- 261: `destinations: null as Destination[] | null,` -> 执行当前语句，参与该文件整体逻辑。
- 262: `products: null as Product[] | null,` -> 执行当前语句，参与该文件整体逻辑。
- 263: `timestamp: 0` -> 执行当前语句，参与该文件整体逻辑。
- 264: `}` -> 结束当前语句或代码块。
- 265: `(空行)` -> 空行，用于提升代码结构可读性。
- 266: `const CACHE_DURATION = 5 * 60 * 1000 // 5分钟` -> 声明变量或常量，保存运行时数据。
- 267: `(空行)` -> 空行，用于提升代码结构可读性。
- 268: `/** 清除数据缓存 */` -> 注释行，用于解释设计意图或使用说明。
- 269: `export function clearHomeCache() {` -> 导出当前声明，供其他模块复用。
- 270: `dataCache.destinations = null` -> 执行当前语句，参与该文件整体逻辑。
- 271: `dataCache.products = null` -> 执行当前语句，参与该文件整体逻辑。
- 272: `dataCache.timestamp = 0` -> 执行当前语句，参与该文件整体逻辑。
- 273: `}` -> 结束当前语句或代码块。
- 274: `(空行)` -> 空行，用于提升代码结构可读性。
- 275: `export default function HomePage() {` -> 导出当前声明，供其他模块复用。
- 276: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 277: `(空行)` -> 空行，用于提升代码结构可读性。
- 278: `const [keyword, setKeyword] = useState('')` -> 声明变量或常量，保存运行时数据。
- 279: `const [popularDestinations, setPopularDestinations] = useState<Destination[]>([])` -> 声明变量或常量，保存运行时数据。
- 280: `const [popularLoading, setPopularLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 281: `const [products, setProducts] = useState<Product[]>([])` -> 声明变量或常量，保存运行时数据。
- 282: `const [productsLoading, setProductsLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `const scrollRef = useRef<HTMLDivElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 285: `(空行)` -> 空行，用于提升代码结构可读性。
- 286: `const getDestinationCoverSrc = (d: Destination) => {` -> 声明变量或常量，保存运行时数据。
- 287: `return resolveCoverSrc(d.cover_image)` -> 返回结果或提前结束当前流程。
- 288: `}` -> 结束当前语句或代码块。
- 289: `(空行)` -> 空行，用于提升代码结构可读性。
- 290: `// 预加载详情页数据` -> 注释行，用于解释设计意图或使用说明。
- 291: `const preloadDestination = useCallback((id: number) => {` -> 声明变量或常量，保存运行时数据。
- 292: `// 使用 requestIdleCallback 在浏览器空闲时预加载` -> 注释行，用于解释设计意图或使用说明。
- 293: `if (typeof window !== 'undefined' && 'requestIdleCallback' in window) {` -> 条件判断分支，根据场景执行不同逻辑。
- 294: `(window as any).requestIdleCallback(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 295: `// 预加载详情页` -> 注释行，用于解释设计意图或使用说明。
- 296: `const link = document.createElement('link')` -> 声明变量或常量，保存运行时数据。
- 297: `link.rel = 'prefetch'` -> 执行当前语句，参与该文件整体逻辑。
- 298: `link.href = \`/destinations/${id}\`` -> 执行当前语句，参与该文件整体逻辑。
- 299: `document.head.appendChild(link)` -> 执行当前语句，参与该文件整体逻辑。
- 300: `})` -> 执行当前语句，参与该文件整体逻辑。
- 301: `}` -> 结束当前语句或代码块。
- 302: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 303: `(空行)` -> 空行，用于提升代码结构可读性。
- 304: `// 加载热门目的地 - 带缓存` -> 注释行，用于解释设计意图或使用说明。
- 305: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 306: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 307: `async function run() {` -> 定义函数或方法，实现具体业务逻辑。
- 308: `// 检查缓存（开发模式下每次刷新都重新加载）` -> 注释行，用于解释设计意图或使用说明。
- 309: `const isDev = process.env.NODE_ENV === 'development'` -> 声明变量或常量，保存运行时数据。
- 310: `if (!isDev && dataCache.destinations && Date.now() - dataCache.timestamp < CACHE_DURATION) {` -> 条件判断分支，根据场景执行不同逻辑。
- 311: `if (!cancelled) {` -> 条件判断分支，根据场景执行不同逻辑。
- 312: `setPopularDestinations(dataCache.destinations!)` -> 执行当前语句，参与该文件整体逻辑。
- 313: `setPopularLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 314: `}` -> 结束当前语句或代码块。
- 315: `return` -> 返回结果或提前结束当前流程。
- 316: `}` -> 结束当前语句或代码块。
- 317: `(空行)` -> 空行，用于提升代码结构可读性。
- 318: `setPopularLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 319: `try {` -> 异常处理逻辑，提升程序健壮性。
- 320: `const res = await fetch(\`/api/destinations?per_page=4&page=1&light=true&sort_by=popular&order=desc\`, {` -> 声明变量或常量，保存运行时数据。
- 321: `cache: 'default' // 使用浏览器缓存` -> 执行当前语句，参与该文件整体逻辑。
- 322: `})` -> 执行当前语句，参与该文件整体逻辑。
- 323: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 324: `const list = (data?.destinations ?? data?.items ?? data?.data ?? []) as Destination[]` -> 声明变量或常量，保存运行时数据。
- 325: `if (!cancelled) {` -> 条件判断分支，根据场景执行不同逻辑。
- 326: `setPopularDestinations(list)` -> 执行当前语句，参与该文件整体逻辑。
- 327: `// 更新缓存` -> 注释行，用于解释设计意图或使用说明。
- 328: `dataCache.destinations = list` -> 执行当前语句，参与该文件整体逻辑。
- 329: `dataCache.timestamp = Date.now()` -> 执行当前语句，参与该文件整体逻辑。
- 330: `}` -> 结束当前语句或代码块。
- 331: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 332: `if (!cancelled) setPopularDestinations([])` -> 条件判断分支，根据场景执行不同逻辑。
- 333: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 334: `if (!cancelled) setPopularLoading(false)` -> 条件判断分支，根据场景执行不同逻辑。
- 335: `}` -> 结束当前语句或代码块。
- 336: `}` -> 结束当前语句或代码块。
- 337: `run()` -> 执行当前语句，参与该文件整体逻辑。
- 338: `return () => {` -> 返回结果或提前结束当前流程。
- 339: `cancelled = true` -> 执行当前语句，参与该文件整体逻辑。
- 340: `}` -> 结束当前语句或代码块。
- 341: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 342: `(空行)` -> 空行，用于提升代码结构可读性。
- 343: `// 加载精选产品 - 带缓存` -> 注释行，用于解释设计意图或使用说明。
- 344: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 345: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 346: `async function run() {` -> 定义函数或方法，实现具体业务逻辑。
- 347: `// 检查缓存（开发模式下每次刷新都重新加载）` -> 注释行，用于解释设计意图或使用说明。
- 348: `const isDev = process.env.NODE_ENV === 'development'` -> 声明变量或常量，保存运行时数据。
- 349: `if (!isDev && dataCache.products && Date.now() - dataCache.timestamp < CACHE_DURATION) {` -> 条件判断分支，根据场景执行不同逻辑。
- 350: `if (!cancelled) {` -> 条件判断分支，根据场景执行不同逻辑。
- 351: `setProducts(dataCache.products!)` -> 执行当前语句，参与该文件整体逻辑。
- 352: `setProductsLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 353: `}` -> 结束当前语句或代码块。
- 354: `return` -> 返回结果或提前结束当前流程。
- 355: `}` -> 结束当前语句或代码块。
- 356: `(空行)` -> 空行，用于提升代码结构可读性。
- 357: `setProductsLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 358: `try {` -> 异常处理逻辑，提升程序健壮性。
- 359: `const res = await fetch(\`/api/products?status=active&sort=rating&limit=8&_t=${Date.now()}\`, {` -> 声明变量或常量，保存运行时数据。
- 360: `cache: 'no-store'` -> 执行当前语句，参与该文件整体逻辑。
- 361: `})` -> 执行当前语句，参与该文件整体逻辑。
- 362: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 363: `const list = (data?.products ?? data?.items ?? data?.data ?? []) as Product[]` -> 声明变量或常量，保存运行时数据。
- 364: `if (!cancelled) {` -> 条件判断分支，根据场景执行不同逻辑。
- 365: `setProducts(list)` -> 执行当前语句，参与该文件整体逻辑。
- 366: `// 更新缓存` -> 注释行，用于解释设计意图或使用说明。
- 367: `dataCache.products = list` -> 执行当前语句，参与该文件整体逻辑。
- 368: `dataCache.timestamp = Date.now()` -> 执行当前语句，参与该文件整体逻辑。
- 369: `}` -> 结束当前语句或代码块。
- 370: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 371: `if (!cancelled) setProducts([])` -> 条件判断分支，根据场景执行不同逻辑。
- 372: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 373: `if (!cancelled) setProductsLoading(false)` -> 条件判断分支，根据场景执行不同逻辑。
- 374: `}` -> 结束当前语句或代码块。
- 375: `}` -> 结束当前语句或代码块。
- 376: `run()` -> 执行当前语句，参与该文件整体逻辑。
- 377: `return () => {` -> 返回结果或提前结束当前流程。
- 378: `cancelled = true` -> 执行当前语句，参与该文件整体逻辑。
- 379: `}` -> 结束当前语句或代码块。
- 380: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 381: `(空行)` -> 空行，用于提升代码结构可读性。
- 382: `const categories = useMemo(` -> 声明变量或常量，保存运行时数据。
- 383: `() => [` -> 执行当前语句，参与该文件整体逻辑。
- 384: `{ type: 'transport' as const, label: '机票', Icon: Plane, hint: '直达省心' },` -> 执行当前语句，参与该文件整体逻辑。
- 385: `{ type: 'hotel' as const, label: '酒店', Icon: Hotel, hint: '舒适住宿' },` -> 执行当前语句，参与该文件整体逻辑。
- 386: `{ type: 'ticket' as const, label: '门票', Icon: Ticket, hint: '热门景点' },` -> 执行当前语句，参与该文件整体逻辑。
- 387: `{ type: 'experience' as const, label: '当地体验', Icon: Drama, hint: '沉浸玩法' },` -> 执行当前语句，参与该文件整体逻辑。
- 388: `],` -> 执行当前语句，参与该文件整体逻辑。
- 389: `[],` -> 执行当前语句，参与该文件整体逻辑。
- 390: `)` -> 结束当前语句或代码块。
- 391: `(空行)` -> 空行，用于提升代码结构可读性。
- 392: `const features = useMemo(` -> 声明变量或常量，保存运行时数据。
- 393: `() => [` -> 执行当前语句，参与该文件整体逻辑。
- 394: `{` -> 执行当前语句，参与该文件整体逻辑。
- 395: `icon: '🌐',` -> 执行当前语句，参与该文件整体逻辑。
- 396: `title: '永久免费 · 开源共享',` -> 执行当前语句，参与该文件整体逻辑。
- 397: `desc: '完全开源，社区驱动，无隐藏费用，无功能限制，真正的旅行助手为每个人服务 ✨',` -> 执行当前语句，参与该文件整体逻辑。
- 398: `color: '#10b981',` -> 执行当前语句，参与该文件整体逻辑。
- 399: `cta: '了解开源',` -> 执行当前语句，参与该文件整体逻辑。
- 400: `href: '/about',` -> 执行当前语句，参与该文件整体逻辑。
- 401: `},` -> 执行当前语句，参与该文件整体逻辑。
- 402: `{` -> 执行当前语句，参与该文件整体逻辑。
- 403: `icon: '🤖',` -> 执行当前语句，参与该文件整体逻辑。
- 404: `title: 'AI 驱动 · 智能规划',` -> 执行当前语句，参与该文件整体逻辑。
- 405: `desc: '基于大模型的行程规划与个性化推荐，让旅行更轻松愉快，发现更多惊喜 ✨',` -> 执行当前语句，参与该文件整体逻辑。
- 406: `color: '#8b5cf6',` -> 执行当前语句，参与该文件整体逻辑。
- 407: `cta: '开始规划',` -> 执行当前语句，参与该文件整体逻辑。
- 408: `href: '/assistant',` -> 执行当前语句，参与该文件整体逻辑。
- 409: `},` -> 执行当前语句，参与该文件整体逻辑。
- 410: `{` -> 执行当前语句，参与该文件整体逻辑。
- 411: `icon: '🔒',` -> 执行当前语句，参与该文件整体逻辑。
- 412: `title: '隐私保护 · 数据本地化',` -> 执行当前语句，参与该文件整体逻辑。
- 413: `desc: '优先本地化存储与最小化采集，你的旅行数据只属于你，安心使用，放心探索 🛡️',` -> 执行当前语句，参与该文件整体逻辑。
- 414: `color: '#3b82f6',` -> 执行当前语句，参与该文件整体逻辑。
- 415: `cta: '查看隐私',` -> 执行当前语句，参与该文件整体逻辑。
- 416: `href: '/privacy',` -> 执行当前语句，参与该文件整体逻辑。
- 417: `},` -> 执行当前语句，参与该文件整体逻辑。
- 418: `],` -> 执行当前语句，参与该文件整体逻辑。
- 419: `[],` -> 执行当前语句，参与该文件整体逻辑。
- 420: `)` -> 结束当前语句或代码块。
- 421: `(空行)` -> 空行，用于提升代码结构可读性。
- 422: `const handleHeroSubmit = (e: FormEvent) => {` -> 声明变量或常量，保存运行时数据。
- 423: `e.preventDefault()` -> 执行当前语句，参与该文件整体逻辑。
- 424: `const q = keyword.trim()` -> 声明变量或常量，保存运行时数据。
- 425: `router.push(\`/destinations?keyword=${encodeURIComponent(q)}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 426: `}` -> 结束当前语句或代码块。
- 427: `(空行)` -> 空行，用于提升代码结构可读性。
- 428: `const scrollByAmount = (dir: -1 | 1) => {` -> 声明变量或常量，保存运行时数据。
- 429: `if (!scrollRef.current) return` -> 条件判断分支，根据场景执行不同逻辑。
- 430: `scrollRef.current.scrollBy({ left: dir * 520, behavior: 'smooth' })` -> 执行当前语句，参与该文件整体逻辑。
- 431: `}` -> 结束当前语句或代码块。
- 432: `(空行)` -> 空行，用于提升代码结构可读性。
- 433: `return (` -> 返回结果或提前结束当前流程。
- 434: `<div className="min-h-screen">` -> JSX/HTML 结构行，用于描述页面元素。
- 435: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 436: `(空行)` -> 空行，用于提升代码结构可读性。
- 437: `<main className="pt-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 438: `<section className="relative overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 439: `<div className="absolute inset-0 bg-gradient-to-b from-[#0c4a6e] via-[#0369a1] via-[#0284c7]/70 via-[#38bdf8]/40 to-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 440: `(空行)` -> 空行，用于提升代码结构可读性。
- 441: `<div className="hero-stars" />` -> JSX/HTML 结构行，用于描述页面元素。
- 442: `(空行)` -> 空行，用于提升代码结构可读性。
- 443: `<div className="hero-glow hero-glow-1" />` -> JSX/HTML 结构行，用于描述页面元素。
- 444: `<div className="hero-glow hero-glow-2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 445: `<div className="hero-glow hero-glow-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 446: `<div className="hero-glow hero-glow-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 447: `<div className="hero-glow hero-glow-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 448: `(空行)` -> 空行，用于提升代码结构可读性。
- 449: `<div className="hero-light-band" />` -> JSX/HTML 结构行，用于描述页面元素。
- 450: `(空行)` -> 空行，用于提升代码结构可读性。
- 451: `<div className="hero-waves absolute bottom-0 left-0 right-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 452: `<svg className="hero-wave hero-wave-1" viewBox="0 0 1440 120" preserveAspectRatio="none">` -> JSX/HTML 结构行，用于描述页面元素。
- 453: `<path d="M0,80 C360,120 720,40 1080,80 C1260,100 1380,60 1440,80 L1440,120 L0,120 Z" fill="rgba(3,105,161,0.12)" />` -> JSX/HTML 结构行，用于描述页面元素。
- 454: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 455: `<svg className="hero-wave hero-wave-2" viewBox="0 0 1440 120" preserveAspectRatio="none">` -> JSX/HTML 结构行，用于描述页面元素。
- 456: `<path d="M0,60 C240,100 480,40 720,70 C960,100 1200,30 1440,60 L1440,120 L0,120 Z" fill="rgba(2,132,199,0.08)" />` -> JSX/HTML 结构行，用于描述页面元素。
- 457: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 458: `<svg className="hero-wave hero-wave-3" viewBox="0 0 1440 120" preserveAspectRatio="none">` -> JSX/HTML 结构行，用于描述页面元素。
- 459: `<path d="M0,90 C480,60 960,100 1440,70 L1440,120 L0,120 Z" fill="rgba(14,165,233,0.06)" />` -> JSX/HTML 结构行，用于描述页面元素。
- 460: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 461: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 462: `(空行)` -> 空行，用于提升代码结构可读性。
- 463: `<div className="absolute inset-0 hero-content-safe" />` -> JSX/HTML 结构行，用于描述页面元素。
- 464: `(空行)` -> 空行，用于提升代码结构可读性。
- 465: `<div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 466: `<div className="w-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 467: `<div className="text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 468: `<p className="text-xs md:text-sm tracking-[0.3em] uppercase text-white/50 font-light mb-2 hero-subtitle-reveal">` -> JSX/HTML 结构行，用于描述页面元素。
- 469: `Discover Your Next Adventure` -> 执行当前语句，参与该文件整体逻辑。
- 470: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 471: `<h1 className="text-4xl md:text-6xl font-extrabold tracking-tight text-white hero-title-glow">` -> JSX/HTML 结构行，用于描述页面元素。
- 472: `发现你的` -> 执行当前语句，参与该文件整体逻辑。
- 473: `<span className="text-transparent bg-clip-text bg-gradient-to-r from-[#fbbf24] via-[#f59e0b] to-[#fcd34d]">` -> JSX/HTML 结构行，用于描述页面元素。
- 474: `{' '}` -> 执行当前语句，参与该文件整体逻辑。
- 475: `完美旅程` -> 执行当前语句，参与该文件整体逻辑。
- 476: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 478: `<p className="mt-2 text-sm md:text-base text-white/65 max-w-xl mx-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 479: `热门目的地 + 精选产品，一站式为你把行程安排得明明白白` -> 执行当前语句，参与该文件整体逻辑。
- 480: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 481: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 482: `(空行)` -> 空行，用于提升代码结构可读性。
- 483: `<div className="mt-4 max-w-3xl mx-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 484: `<form` -> 执行当前语句，参与该文件整体逻辑。
- 485: `onSubmit={handleHeroSubmit}` -> 执行当前语句，参与该文件整体逻辑。
- 486: `className="flex flex-col lg:flex-row items-stretch lg:items-center gap-2 rounded-2xl bg-white/85 backdrop-blur-md border border-white/40 shadow-lg shadow-black/10 px-3 py-3"` -> 执行当前语句，参与该文件整体逻辑。
- 487: `>` -> 执行当前语句，参与该文件整体逻辑。
- 488: `<div className="flex items-center gap-2 flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 489: `<span className="inline-flex items-center justify-center rounded-xl bg-amber-500/15 text-amber-600 p-2 shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 490: `<Search className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 491: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 493: `value={keyword}` -> 执行当前语句，参与该文件整体逻辑。
- 494: `onChange={(e) => setKeyword(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 495: `placeholder="目的地关键词搜索（如：成都、海鲜、亲子…）"` -> 执行当前语句，参与该文件整体逻辑。
- 496: `className="w-full bg-transparent outline-none text-sm md:text-base text-slate-800 placeholder:text-slate-400"` -> 执行当前语句，参与该文件整体逻辑。
- 497: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 498: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 499: `<div className="flex gap-2 shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 500: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 501: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 502: `onClick={() => router.push('/search')}` -> 执行当前语句，参与该文件整体逻辑。
- 503: `className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200/80 bg-white/70 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-white transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 504: `>` -> 执行当前语句，参与该文件整体逻辑。
- 505: `<CalendarDays className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 506: `选择出行日期` -> 执行当前语句，参与该文件整体逻辑。
- 507: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 508: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 509: `type="submit"` -> 执行当前语句，参与该文件整体逻辑。
- 510: `className="px-5 py-2.5 text-sm md:text-base shrink-0 rounded-xl bg-gradient-to-r from-amber-500 to-orange-500 text-white font-semibold shadow-md shadow-amber-500/25 hover:shadow-lg hover:brightness-110 transition-all active:scale-[0.98]"` -> 执行当前语句，参与该文件整体逻辑。
- 511: `>` -> 执行当前语句，参与该文件整体逻辑。
- 512: `搜索` -> 执行当前语句，参与该文件整体逻辑。
- 513: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 514: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 515: `</form>` -> JSX/HTML 结构行，用于描述页面元素。
- 516: `<div className="mt-3 flex flex-wrap items-center justify-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 517: `{['川西环线', '亲子酒店', '避暑胜地', '海岛度假', '古镇美食', '雪山徒步'].map((tag) => (` -> 执行当前语句，参与该文件整体逻辑。
- 518: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 519: `key={tag}` -> 执行当前语句，参与该文件整体逻辑。
- 520: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 521: `onClick={() => { setKeyword(tag); router.push(\`/destinations?keyword=${encodeURIComponent(tag)}\`) }}` -> 执行当前语句，参与该文件整体逻辑。
- 522: `className="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium text-amber-200/80 bg-white/8 border border-white/10 hover:bg-white/15 hover:text-amber-100 transition-all backdrop-blur-sm"` -> 执行当前语句，参与该文件整体逻辑。
- 523: `>` -> 执行当前语句，参与该文件整体逻辑。
- 524: `{tag}` -> 执行当前语句，参与该文件整体逻辑。
- 525: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 526: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 527: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 528: `<div className="mt-2 flex flex-wrap items-center justify-center gap-x-4 gap-y-1 text-sm text-white/50">` -> JSX/HTML 结构行，用于描述页面元素。
- 529: `<Link href="/destinations" className="hover:text-white/80 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 530: `浏览全部目的地` -> 执行当前语句，参与该文件整体逻辑。
- 531: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 532: `<span className="hidden sm:inline">·</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 533: `<Link href="/search" className="hover:text-white/80 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 534: `高级搜索与地图` -> 执行当前语句，参与该文件整体逻辑。
- 535: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 536: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 537: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 538: `(空行)` -> 空行，用于提升代码结构可读性。
- 539: `<div className="mt-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 540: `<div className="flex items-center justify-between gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 541: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 542: `<h2 className="text-lg md:text-xl font-bold text-white/90">热门目的地</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 543: `<p className="text-sm text-white/60 mt-1">滑动选择，马上发现相似产品</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 544: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 545: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 546: `href="/destinations"` -> 执行当前语句，参与该文件整体逻辑。
- 547: `className="hidden sm:inline-flex items-center gap-1 text-sm font-semibold text-amber-300 hover:text-amber-200 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 548: `aria-label="探索更多目的地"` -> 执行当前语句，参与该文件整体逻辑。
- 549: `>` -> 执行当前语句，参与该文件整体逻辑。
- 550: `探索更多 <span aria-hidden>→</span>` -> 执行当前语句，参与该文件整体逻辑。
- 551: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 552: `<div className="hidden items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 553: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 554: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 555: `onClick={() => scrollByAmount(-1)}` -> 执行当前语句，参与该文件整体逻辑。
- 556: `className="h-10 w-10 rounded-full bg-card border border-border/70 hover:bg-card/90 flex items-center justify-center"` -> 执行当前语句，参与该文件整体逻辑。
- 557: `aria-label="向左"` -> 执行当前语句，参与该文件整体逻辑。
- 558: `>` -> 执行当前语句，参与该文件整体逻辑。
- 559: `<ChevronLeft className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 560: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 561: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 562: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 563: `onClick={() => scrollByAmount(1)}` -> 执行当前语句，参与该文件整体逻辑。
- 564: `className="h-10 w-10 rounded-full bg-card border border-border/70 hover:bg-card/90 flex items-center justify-center"` -> 执行当前语句，参与该文件整体逻辑。
- 565: `aria-label="向右"` -> 执行当前语句，参与该文件整体逻辑。
- 566: `>` -> 执行当前语句，参与该文件整体逻辑。
- 567: `<ChevronRight className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 568: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 569: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 570: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 571: `(空行)` -> 空行，用于提升代码结构可读性。
- 572: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 573: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 574: `ref={scrollRef}` -> 执行当前语句，参与该文件整体逻辑。
- 575: `className="grid grid-cols-2 md:grid-cols-4 gap-4 pb-2"` -> 执行当前语句，参与该文件整体逻辑。
- 576: `>` -> 执行当前语句，参与该文件整体逻辑。
- 577: `{popularLoading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 578: `Array.from({ length: 4 }).map((_, i) => (` -> 执行当前语句，参与该文件整体逻辑。
- 579: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 580: `key={i}` -> 执行当前语句，参与该文件整体逻辑。
- 581: `className="rounded-2xl bg-card border border-border/70 p-3 animate-pulse"` -> 执行当前语句，参与该文件整体逻辑。
- 582: `>` -> 执行当前语句，参与该文件整体逻辑。
- 583: `<div className="h-36 sm:aspect-[16/10] sm:h-auto rounded-xl bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 584: `<div className="mt-3 h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 585: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-1/2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 586: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 587: `))` -> 执行当前语句，参与该文件整体逻辑。
- 588: `) : popularDestinations.length ? (` -> 执行当前语句，参与该文件整体逻辑。
- 589: `popularDestinations.map((d, index) => (` -> 执行当前语句，参与该文件整体逻辑。
- 590: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 591: `key={d.id}` -> 执行当前语句，参与该文件整体逻辑。
- 592: `className="w-full"` -> 执行当前语句，参与该文件整体逻辑。
- 593: `onMouseEnter={() => preloadDestination(d.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 594: `>` -> 执行当前语句，参与该文件整体逻辑。
- 595: `<HomeDestinationCarouselCard` -> 执行当前语句，参与该文件整体逻辑。
- 596: `d={d}` -> 执行当前语句，参与该文件整体逻辑。
- 597: `coverSrc={getDestinationCoverSrc(d)}` -> 执行当前语句，参与该文件整体逻辑。
- 598: `ticketLabel={formatDestinationTicket(d)}` -> 执行当前语句，参与该文件整体逻辑。
- 599: `priority={index < 4}` -> 执行当前语句，参与该文件整体逻辑。
- 600: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 601: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 602: `))` -> 执行当前语句，参与该文件整体逻辑。
- 603: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 604: `<div className="text-sm text-muted-foreground p-2">暂无数据</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 605: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 606: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 607: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 608: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 609: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 610: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 611: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 612: `(空行)` -> 空行，用于提升代码结构可读性。
- 613: `<section className="py-20 bg-slate-50/70 dark:bg-slate-950/40 border-y border-border/40">` -> JSX/HTML 结构行，用于描述页面元素。
- 614: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 615: `<div className="text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 616: `<h2 className="text-2xl md:text-3xl font-extrabold text-foreground">核心特色</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 617: `<p className="text-sm md:text-base text-muted-foreground mt-2">为什么选择智旅助手</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 618: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 619: `(空行)` -> 空行，用于提升代码结构可读性。
- 620: `<div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 621: `{features.map((f) => (` -> 执行当前语句，参与该文件整体逻辑。
- 622: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 623: `key={f.title}` -> 执行当前语句，参与该文件整体逻辑。
- 624: `className="group rounded-2xl border border-border/60 bg-card dark:bg-slate-900/60 dark:border-border/80 shadow-sm hover:shadow-md dark:shadow-black/30 transition-shadow overflow-hidden"` -> 执行当前语句，参与该文件整体逻辑。
- 625: `>` -> 执行当前语句，参与该文件整体逻辑。
- 626: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 627: `className="h-1.5 dark:brightness-125 dark:shadow-[0_0_24px_rgba(0,0,0,0.35)]"` -> 执行当前语句，参与该文件整体逻辑。
- 628: `style={{ backgroundColor: f.color }}` -> 执行当前语句，参与该文件整体逻辑。
- 629: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 630: `<div className="p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 631: `<div className="text-3xl mb-3" aria-hidden>` -> JSX/HTML 结构行，用于描述页面元素。
- 632: `{f.icon}` -> 执行当前语句，参与该文件整体逻辑。
- 633: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 634: `<h3 className="text-lg font-bold text-foreground">{f.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 635: `<p className="text-sm text-muted-foreground mt-2 leading-relaxed">{f.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 636: `(空行)` -> 空行，用于提升代码结构可读性。
- 637: `<div className="mt-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 638: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 639: `href={f.href}` -> 执行当前语句，参与该文件整体逻辑。
- 640: `className="inline-flex items-center justify-center text-sm font-semibold text-white px-4 py-2 rounded-xl transition-opacity hover:opacity-90 dark:brightness-110 dark:ring-1 dark:ring-white/15"` -> 执行当前语句，参与该文件整体逻辑。
- 641: `style={{ backgroundColor: f.color }}` -> 执行当前语句，参与该文件整体逻辑。
- 642: `>` -> 执行当前语句，参与该文件整体逻辑。
- 643: `{f.cta}` -> 执行当前语句，参与该文件整体逻辑。
- 644: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 645: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 646: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 647: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 648: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 649: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 650: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 651: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 652: `(空行)` -> 空行，用于提升代码结构可读性。
- 653: `{/* 视觉分隔区 */}` -> 执行当前语句，参与该文件整体逻辑。
- 654: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 655: `<div className="h-px bg-gradient-to-r from-transparent via-slate-300/50 to-transparent" />` -> JSX/HTML 结构行，用于描述页面元素。
- 656: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 657: `(空行)` -> 空行，用于提升代码结构可读性。
- 658: `<section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 659: `<div className="rounded-3xl border border-border/60 bg-card/70 backdrop-blur p-6 md:p-8 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 660: `<div className="flex items-center justify-between gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 661: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 662: `<h2 className="text-lg md:text-xl font-bold">快速入口</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 663: `<p className="text-sm text-muted-foreground mt-1">一键直达常用功能</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 664: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 665: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 666: `(空行)` -> 空行，用于提升代码结构可读性。
- 667: `<div className="grid grid-cols-2 sm:grid-cols-4 gap-3 md:gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 668: `{categories.map(({ type, label, Icon, hint }) => (` -> 执行当前语句，参与该文件整体逻辑。
- 669: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 670: `key={type}` -> 执行当前语句，参与该文件整体逻辑。
- 671: `href={\`/products?category=${type}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 672: `className="group rounded-2xl border border-border/60 bg-gradient-to-b from-primary/5 to-transparent hover:shadow-md transition-all p-4"` -> 执行当前语句，参与该文件整体逻辑。
- 673: `>` -> 执行当前语句，参与该文件整体逻辑。
- 674: `<div className="flex items-start gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 675: `<div className="h-11 w-11 rounded-2xl bg-primary/10 text-primary flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 676: `<Icon className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 677: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 678: `<div className="min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 679: `<div className="text-sm font-extrabold text-foreground truncate">{label}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 680: `<div className="text-xs text-muted-foreground mt-1">{hint}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 681: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 682: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 683: `<div className="mt-3 h-1 w-10 rounded-full bg-primary/20 group-hover:bg-primary/35 transition-colors" />` -> JSX/HTML 结构行，用于描述页面元素。
- 684: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 685: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 686: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 687: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 688: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 689: `(空行)` -> 空行，用于提升代码结构可读性。
- 690: `{/* 精选产品区 */}` -> 执行当前语句，参与该文件整体逻辑。
- 691: `<section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 692: `<div className="flex items-end justify-between gap-4 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 693: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 694: `<h2 className="text-xl md:text-2xl font-extrabold text-foreground">精选推荐产品</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 695: `<p className="text-sm text-muted-foreground mt-2">按用户评分与口碑精选，未登录也可浏览</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 696: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 697: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 698: `(空行)` -> 空行，用于提升代码结构可读性。
- 699: `{productsLoading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 700: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 701: `{Array.from({ length: 8 }).map((_, i) => (` -> 执行当前语句，参与该文件整体逻辑。
- 702: `<div key={i} className="rounded-2xl bg-card border border-border/60 overflow-hidden animate-pulse">` -> JSX/HTML 结构行，用于描述页面元素。
- 703: `<div className="aspect-[4/3] bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 704: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 705: `<div className="h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 706: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-1/2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 707: `<div className="mt-4 h-6 bg-muted-foreground/10 rounded w-3/5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 708: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 709: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 710: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 711: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 712: `) : products.length ? (` -> 执行当前语句，参与该文件整体逻辑。
- 713: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 714: `{products.map((p, idx) => (` -> 执行当前语句，参与该文件整体逻辑。
- 715: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 716: `key={p.id}` -> 执行当前语句，参与该文件整体逻辑。
- 717: `href={\`/products/${p.id}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 718: `className={idx === 0 ? 'sm:col-span-2 lg:col-span-2' : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 719: `>` -> 执行当前语句，参与该文件整体逻辑。
- 720: `<ProductCard product={p} variant={idx === 0 ? 'featured' : 'default'} className="h-full" />` -> JSX/HTML 结构行，用于描述页面元素。
- 721: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 722: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 723: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 724: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 725: `<div className="rounded-2xl border border-border/60 bg-card p-8 text-center text-muted-foreground">暂无推荐产品</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 726: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 727: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 728: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 729: `(空行)` -> 空行，用于提升代码结构可读性。
- 730: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 731: `(空行)` -> 空行，用于提升代码结构可读性。
- 732: `{/* 右上角：AI 助手（可拖动悬浮） */}` -> 执行当前语句，参与该文件整体逻辑。
- 733: `<DraggableAIAssistant />` -> JSX/HTML 结构行，用于描述页面元素。
- 734: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 735: `)` -> 结束当前语句或代码块。
- 736: `}` -> 结束当前语句或代码块。
