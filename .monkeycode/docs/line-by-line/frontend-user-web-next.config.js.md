# `frontend/user-web/next.config.js` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/** @type {import('next').NextConfig} */` -> 注释行，用于解释设计意图或使用说明。
- 2: `const nextConfig = {   // nextConfig配置  // nextConfig配置` -> 声明变量或常量，保存运行时数据。
- 3: `reactStrictMode: true,` -> 执行当前语句，参与该文件整体逻辑。
- 4: `swcMinify: true,` -> 执行当前语句，参与该文件整体逻辑。
- 5: `env: {` -> 执行当前语句，参与该文件整体逻辑。
- 6: `NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001',` -> 执行当前语句，参与该文件整体逻辑。
- 7: `NEXT_PUBLIC_API_BASE_URL: process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001',` -> 执行当前语句，参与该文件整体逻辑。
- 8: `NEXT_PUBLIC_AI_SERVICE_URL: process.env.NEXT_PUBLIC_AI_SERVICE_URL || 'http://localhost:8081',` -> 执行当前语句，参与该文件整体逻辑。
- 9: `},` -> 执行当前语句，参与该文件整体逻辑。
- 10: `images: {` -> 执行当前语句，参与该文件整体逻辑。
- 11: `domains: ['localhost', 'example.com', 'picsum.photos'],` -> 执行当前语句，参与该文件整体逻辑。
- 12: `},` -> 执行当前语句，参与该文件整体逻辑。
- 13: `async rewrites() {` -> 执行当前语句，参与该文件整体逻辑。
- 14: `const apiBase = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 15: `if (!apiBase) return []   // 条件判断` -> 条件判断分支，根据场景执行不同逻辑。
- 16: `return [   // 返回结果` -> 返回结果或提前结束当前流程。
- 17: `// Align FE contract: /api/auth/* -> backend /api/users/*` -> 注释行，用于解释设计意图或使用说明。
- 18: `{` -> 执行当前语句，参与该文件整体逻辑。
- 19: `source: '/api/auth/login',` -> 执行当前语句，参与该文件整体逻辑。
- 20: `destination: \`${apiBase}/api/users/login\`,` -> 执行当前语句，参与该文件整体逻辑。
- 21: `},` -> 执行当前语句，参与该文件整体逻辑。
- 22: `{` -> 执行当前语句，参与该文件整体逻辑。
- 23: `source: '/api/auth/register',` -> 执行当前语句，参与该文件整体逻辑。
- 24: `destination: \`${apiBase}/api/users/register\`,` -> 执行当前语句，参与该文件整体逻辑。
- 25: `},` -> 执行当前语句，参与该文件整体逻辑。
- 26: `// Align FE contract: /api/ai/chat -> backend /api/chat` -> 注释行，用于解释设计意图或使用说明。
- 27: `{` -> 执行当前语句，参与该文件整体逻辑。
- 28: `source: '/api/ai/chat',` -> 执行当前语句，参与该文件整体逻辑。
- 29: `destination: \`${apiBase}/api/chat\`,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `},` -> 执行当前语句，参与该文件整体逻辑。
- 31: `{` -> 执行当前语句，参与该文件整体逻辑。
- 32: `source: '/api/:path*',` -> 执行当前语句，参与该文件整体逻辑。
- 33: `destination: \`${apiBase}/api/:path*\`,` -> 执行当前语句，参与该文件整体逻辑。
- 34: `},` -> 执行当前语句，参与该文件整体逻辑。
- 35: `];` -> 结束当前语句或代码块。
- 36: `},` -> 执行当前语句，参与该文件整体逻辑。
- 37: `}` -> 结束当前语句或代码块。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `module.exports = nextConfig` -> 执行当前语句，参与该文件整体逻辑。
