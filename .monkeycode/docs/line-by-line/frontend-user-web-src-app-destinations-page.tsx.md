# `frontend/user-web/src/app/destinations/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 目的地列表页模块 - 景点与旅行产品浏览` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 6: `*   - 展示所有目的地景点，支持搜索和筛选` -> 注释行，用于解释设计意图或使用说明。
- 7: `*   - 提供景点/产品双视图切换` -> 注释行，用于解释设计意图或使用说明。
- 8: `*   - 无限滚动/分页加载更多内容` -> 注释行，用于解释设计意图或使用说明。
- 9: `*   - 省份筛选、评分筛选功能` -> 注释行，用于解释设计意图或使用说明。
- 10: `*` -> 注释行，用于解释设计意图或使用说明。
- 11: `* 依赖项：` -> 注释行，用于解释设计意图或使用说明。
- 12: `*   - components/Navbar：顶部导航栏` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - components/Footer：底部页脚` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - components/ProductCard：产品卡片` -> 注释行，用于解释设计意图或使用说明。
- 15: `*   - components/DestinationCard：目的地卡片` -> 注释行，用于解释设计意图或使用说明。
- 16: `*   - hooks/index：自定义 Hooks（useDebounce 防抖）` -> 注释行，用于解释设计意图或使用说明。
- 17: `*   - lib/destinationRegions：大洲/地区定义` -> 注释行，用于解释设计意图或使用说明。
- 18: `*` -> 注释行，用于解释设计意图或使用说明。
- 19: `* 数据来源：` -> 注释行，用于解释设计意图或使用说明。
- 20: `*   - GET /api/destinations：目的地列表` -> 注释行，用于解释设计意图或使用说明。
- 21: `*   - GET /api/destinations/metadata：省份元数据` -> 注释行，用于解释设计意图或使用说明。
- 22: `*   - GET /api/products：产品列表` -> 注释行，用于解释设计意图或使用说明。
- 23: `*` -> 注释行，用于解释设计意图或使用说明。
- 24: `* 状态管理：` -> 注释行，用于解释设计意图或使用说明。
- 25: `*   - useSearchParams：URL 参数同步` -> 注释行，用于解释设计意图或使用说明。
- 26: `*   - useInfiniteScroll：无限滚动 Hook` -> 注释行，用于解释设计意图或使用说明。
- 27: `*/` -> 注释行，用于解释设计意图或使用说明。
- 28: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `import { useEffect, useMemo, useRef, useState, useCallback } from 'react'` -> 导入依赖模块，供当前文件使用。
- 31: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 32: `import { usePathname, useRouter, useSearchParams } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 33: `import {` -> 导入依赖模块，供当前文件使用。
- 34: `ChevronRight,` -> 执行当前语句，参与该文件整体逻辑。
- 35: `Filter,` -> 执行当前语句，参与该文件整体逻辑。
- 36: `MapPin,` -> 执行当前语句，参与该文件整体逻辑。
- 37: `RotateCcw,` -> 执行当前语句，参与该文件整体逻辑。
- 38: `Search,` -> 执行当前语句，参与该文件整体逻辑。
- 39: `SlidersHorizontal,` -> 执行当前语句，参与该文件整体逻辑。
- 40: `Star,` -> 执行当前语句，参与该文件整体逻辑。
- 41: `Loader2,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 43: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 44: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 45: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 46: `import { ProductCard } from '@/components/ProductCard'` -> 导入依赖模块，供当前文件使用。
- 47: `import { DestinationCard } from '@/components/DestinationCard'` -> 导入依赖模块，供当前文件使用。
- 48: `import type { Product } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 49: `import { useDebounce } from '@/hooks'` -> 导入依赖模块，供当前文件使用。
- 50: `import { type ContinentKey } from '@/lib/destinationRegions'` -> 导入依赖模块，供当前文件使用。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `const perPage = 12` -> 声明变量或常量，保存运行时数据。
- 53: `const destPerPage = 9` -> 声明变量或常量，保存运行时数据。
- 54: `(空行)` -> 空行，用于提升代码结构可读性。
- 55: `type SortKey = 'recommended' | 'price_asc' | 'price_desc' | 'rating_desc' | 'sales_desc'` -> 定义类型或类结构，约束数据与行为。
- 56: `type DestSortKey = 'hot_desc' | 'rating_desc' | 'created_desc'` -> 定义类型或类结构，约束数据与行为。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `type DestinationRow = {` -> 定义类型或类结构，约束数据与行为。
- 59: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 60: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 61: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 62: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 63: `description?: string` -> 执行当前语句，参与该文件整体逻辑。
- 64: `cover_image?: string` -> 执行当前语句，参与该文件整体逻辑。
- 65: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 66: `created_at?: string` -> 执行当前语句，参与该文件整体逻辑。
- 67: `}` -> 结束当前语句或代码块。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `// 使用 IntersectionObserver 实现无限滚动` -> 注释行，用于解释设计意图或使用说明。
- 70: `function useInfiniteScroll(` -> 定义函数或方法，实现具体业务逻辑。
- 71: `callback: () => void,` -> 执行当前语句，参与该文件整体逻辑。
- 72: `hasMore: boolean,` -> 执行当前语句，参与该文件整体逻辑。
- 73: `loading: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 74: `) {` -> 执行当前语句，参与该文件整体逻辑。
- 75: `const observerRef = useRef<IntersectionObserver | null>(null)` -> 声明变量或常量，保存运行时数据。
- 76: `const targetRef = useRef<HTMLDivElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 77: `(空行)` -> 空行，用于提升代码结构可读性。
- 78: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 79: `if (loading || !hasMore) return` -> 条件判断分支，根据场景执行不同逻辑。
- 80: `(空行)` -> 空行，用于提升代码结构可读性。
- 81: `if (observerRef.current) {` -> 条件判断分支，根据场景执行不同逻辑。
- 82: `observerRef.current.disconnect()` -> 执行当前语句，参与该文件整体逻辑。
- 83: `}` -> 结束当前语句或代码块。
- 84: `(空行)` -> 空行，用于提升代码结构可读性。
- 85: `observerRef.current = new IntersectionObserver(` -> 执行当前语句，参与该文件整体逻辑。
- 86: `(entries) => {` -> 执行当前语句，参与该文件整体逻辑。
- 87: `if (entries[0].isIntersecting && hasMore && !loading) {` -> 条件判断分支，根据场景执行不同逻辑。
- 88: `callback()` -> 执行当前语句，参与该文件整体逻辑。
- 89: `}` -> 结束当前语句或代码块。
- 90: `},` -> 执行当前语句，参与该文件整体逻辑。
- 91: `{ rootMargin: '100px' }` -> 执行当前语句，参与该文件整体逻辑。
- 92: `)` -> 结束当前语句或代码块。
- 93: `(空行)` -> 空行，用于提升代码结构可读性。
- 94: `if (targetRef.current) {` -> 条件判断分支，根据场景执行不同逻辑。
- 95: `observerRef.current.observe(targetRef.current)` -> 执行当前语句，参与该文件整体逻辑。
- 96: `}` -> 结束当前语句或代码块。
- 97: `(空行)` -> 空行，用于提升代码结构可读性。
- 98: `return () => {` -> 返回结果或提前结束当前流程。
- 99: `if (observerRef.current) {` -> 条件判断分支，根据场景执行不同逻辑。
- 100: `observerRef.current.disconnect()` -> 执行当前语句，参与该文件整体逻辑。
- 101: `}` -> 结束当前语句或代码块。
- 102: `}` -> 结束当前语句或代码块。
- 103: `}, [callback, hasMore, loading])` -> 执行当前语句，参与该文件整体逻辑。
- 104: `(空行)` -> 空行，用于提升代码结构可读性。
- 105: `return targetRef` -> 返回结果或提前结束当前流程。
- 106: `}` -> 结束当前语句或代码块。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `export default function DestinationsPage() {` -> 导出当前声明，供其他模块复用。
- 109: `const searchParams = useSearchParams()` -> 声明变量或常量，保存运行时数据。
- 110: `const pathname = usePathname()` -> 声明变量或常量，保存运行时数据。
- 111: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 112: `(空行)` -> 空行，用于提升代码结构可读性。
- 113: `const initialKeyword = searchParams.get('keyword') ?? ''` -> 声明变量或常量，保存运行时数据。
- 114: `const initialCity = searchParams.get('city') ?? ''` -> 声明变量或常量，保存运行时数据。
- 115: `const initialType = (searchParams.get('type') ?? 'all') as 'all' | Product['type']` -> 声明变量或常量，保存运行时数据。
- 116: `const initialSort = (searchParams.get('sort') ?? 'recommended') as SortKey` -> 声明变量或常量，保存运行时数据。
- 117: `const initialPage = Number(searchParams.get('page') ?? '1') || 1` -> 声明变量或常量，保存运行时数据。
- 118: `const initialView = searchParams.get('view') === 'products' ? 'products' : 'spots'` -> 声明变量或常量，保存运行时数据。
- 119: `(空行)` -> 空行，用于提升代码结构可读性。
- 120: `const [tab, setTab] = useState<'spots' | 'products'>(initialView)` -> 声明变量或常量，保存运行时数据。
- 121: `(空行)` -> 空行，用于提升代码结构可读性。
- 122: `const [keyword, setKeyword] = useState(initialKeyword)` -> 声明变量或常量，保存运行时数据。
- 123: `const debouncedKeyword = useDebounce(keyword, 300)` -> 声明变量或常量，保存运行时数据。
- 124: `(空行)` -> 空行，用于提升代码结构可读性。
- 125: `const [city, setCity] = useState(initialCity)` -> 声明变量或常量，保存运行时数据。
- 126: `const [province, setProvince] = useState('')` -> 声明变量或常量，保存运行时数据。
- 127: `const [type, setType] = useState<'all' | Product['type']>(initialType)` -> 声明变量或常量，保存运行时数据。
- 128: `const [ratingMin, setRatingMin] = useState<number | ''>('')` -> 声明变量或常量，保存运行时数据。
- 129: `const [priceMin, setPriceMin] = useState<number | ''>('')` -> 声明变量或常量，保存运行时数据。
- 130: `const [priceMax, setPriceMax] = useState<number | ''>('')` -> 声明变量或常量，保存运行时数据。
- 131: `const [sortKey, setSortKey] = useState<SortKey>(initialSort)` -> 声明变量或常量，保存运行时数据。
- 132: `const [page, setPage] = useState(initialPage)` -> 声明变量或常量，保存运行时数据。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `// 景点相关状态 - 服务端分页` -> 注释行，用于解释设计意图或使用说明。
- 135: `const [spotsDest, setSpotsDest] = useState<DestinationRow[]>([])` -> 声明变量或常量，保存运行时数据。
- 136: `const [loadingSpots, setLoadingSpots] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 137: `const [spotsError, setSpotsError] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 138: `const [continent] = useState<ContinentKey>('all')` -> 声明变量或常量，保存运行时数据。
- 139: `const [destSort, setDestSort] = useState<DestSortKey>('hot_desc')` -> 声明变量或常量，保存运行时数据。
- 140: `const [destPage, setDestPage] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 141: `const [destTotal, setDestTotal] = useState(0)` -> 声明变量或常量，保存运行时数据。
- 142: `const [hasMoreSpots, setHasMoreSpots] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 143: `(空行)` -> 空行，用于提升代码结构可读性。
- 144: `// 元数据状态` -> 注释行，用于解释设计意图或使用说明。
- 145: `const [provinces, setProvinces] = useState<{ name: string; count: number }[]>([])` -> 声明变量或常量，保存运行时数据。
- 146: `const [loadingProvinces, setLoadingProvinces] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 147: `const [cities, setCities] = useState<{ name: string; count: number }[]>([])` -> 声明变量或常量，保存运行时数据。
- 148: `const [loadingCities, setLoadingCities] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 149: `(空行)` -> 空行，用于提升代码结构可读性。
- 150: `// 产品相关状态` -> 注释行，用于解释设计意图或使用说明。
- 151: `const [products, setProducts] = useState<Product[]>([])` -> 声明变量或常量，保存运行时数据。
- 152: `const [total, setTotal] = useState(0)` -> 声明变量或常量，保存运行时数据。
- 153: `const [loadingProducts, setLoadingProducts] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 154: `const [error, setError] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 155: `(空行)` -> 空行，用于提升代码结构可读性。
- 156: `const syncTabToUrl = (next: 'spots' | 'products') => {` -> 声明变量或常量，保存运行时数据。
- 157: `const q = new URLSearchParams(searchParams.toString())` -> 声明变量或常量，保存运行时数据。
- 158: `if (next === 'products') q.set('view', 'products')` -> 条件判断分支，根据场景执行不同逻辑。
- 159: `else q.delete('view')` -> 条件判断分支，根据场景执行不同逻辑。
- 160: `const qs = q.toString()` -> 声明变量或常量，保存运行时数据。
- 161: `router.replace(qs ? \`${pathname}?${qs}\` : pathname, { scroll: false })` -> 执行当前语句，参与该文件整体逻辑。
- 162: `}` -> 结束当前语句或代码块。
- 163: `(空行)` -> 空行，用于提升代码结构可读性。
- 164: `const sortMeta = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 165: `const map: Record<SortKey, { apiSort: string; order: 'asc' | 'desc' }> = {` -> 声明变量或常量，保存运行时数据。
- 166: `recommended: { apiSort: 'rating', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 167: `rating_desc: { apiSort: 'rating', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 168: `price_asc: { apiSort: 'price', order: 'asc' },` -> 执行当前语句，参与该文件整体逻辑。
- 169: `price_desc: { apiSort: 'price', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 170: `sales_desc: { apiSort: 'sales', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 171: `}` -> 结束当前语句或代码块。
- 172: `return map[sortKey]` -> 返回结果或提前结束当前流程。
- 173: `}, [sortKey])` -> 执行当前语句，参与该文件整体逻辑。
- 174: `(空行)` -> 空行，用于提升代码结构可读性。
- 175: `const sortLabel = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 176: `switch (sortKey) {` -> 执行当前语句，参与该文件整体逻辑。
- 177: `case 'recommended':` -> 执行当前语句，参与该文件整体逻辑。
- 178: `return '推荐（评分优先）'` -> 返回结果或提前结束当前流程。
- 179: `case 'sales_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 180: `return '销量优先'` -> 返回结果或提前结束当前流程。
- 181: `case 'rating_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 182: `return '评分优先'` -> 返回结果或提前结束当前流程。
- 183: `case 'price_asc':` -> 执行当前语句，参与该文件整体逻辑。
- 184: `return '价格从低到高'` -> 返回结果或提前结束当前流程。
- 185: `case 'price_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 186: `return '价格从高到低'` -> 返回结果或提前结束当前流程。
- 187: `default:` -> 执行当前语句，参与该文件整体逻辑。
- 188: `return '推荐'` -> 返回结果或提前结束当前流程。
- 189: `}` -> 结束当前语句或代码块。
- 190: `}, [sortKey])` -> 执行当前语句，参与该文件整体逻辑。
- 191: `(空行)` -> 空行，用于提升代码结构可读性。
- 192: `const totalPages = Math.max(1, Math.ceil(total / perPage))` -> 声明变量或常量，保存运行时数据。
- 193: `(空行)` -> 空行，用于提升代码结构可读性。
- 194: `// 获取元数据（省份列表等）` -> 注释行，用于解释设计意图或使用说明。
- 195: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 196: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 197: `async function run() {` -> 定义函数或方法，实现具体业务逻辑。
- 198: `setLoadingProvinces(true)` -> 执行当前语句，参与该文件整体逻辑。
- 199: `try {` -> 异常处理逻辑，提升程序健壮性。
- 200: `const res = await fetch(\`/api/destinations/metadata\`, {` -> 声明变量或常量，保存运行时数据。
- 201: `cache: 'default' // 使用浏览器缓存` -> 执行当前语句，参与该文件整体逻辑。
- 202: `})` -> 执行当前语句，参与该文件整体逻辑。
- 203: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 204: `if (!cancelled && data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 205: `setProvinces(data.metadata?.provinces || [])` -> 执行当前语句，参与该文件整体逻辑。
- 206: `}` -> 结束当前语句或代码块。
- 207: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 208: `if (!cancelled) setProvinces([])` -> 条件判断分支，根据场景执行不同逻辑。
- 209: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 210: `if (!cancelled) setLoadingProvinces(false)` -> 条件判断分支，根据场景执行不同逻辑。
- 211: `}` -> 结束当前语句或代码块。
- 212: `}` -> 结束当前语句或代码块。
- 213: `run()` -> 执行当前语句，参与该文件整体逻辑。
- 214: `return () => {` -> 返回结果或提前结束当前流程。
- 215: `cancelled = true` -> 执行当前语句，参与该文件整体逻辑。
- 216: `}` -> 结束当前语句或代码块。
- 217: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 218: `(空行)` -> 空行，用于提升代码结构可读性。
- 219: `// 获取景点列表 - 服务端分页` -> 注释行，用于解释设计意图或使用说明。
- 220: `const fetchDestinations = useCallback(async (pageNum: number, isLoadMore: boolean = false) => {` -> 声明变量或常量，保存运行时数据。
- 221: `setLoadingSpots(true)` -> 执行当前语句，参与该文件整体逻辑。
- 222: `if (!isLoadMore) {` -> 条件判断分支，根据场景执行不同逻辑。
- 223: `setSpotsError(null)` -> 执行当前语句，参与该文件整体逻辑。
- 224: `}` -> 结束当前语句或代码块。
- 225: `(空行)` -> 空行，用于提升代码结构可读性。
- 226: `try {` -> 异常处理逻辑，提升程序健壮性。
- 227: `const params = new URLSearchParams()` -> 声明变量或常量，保存运行时数据。
- 228: `params.set('page', String(pageNum))` -> 执行当前语句，参与该文件整体逻辑。
- 229: `params.set('per_page', String(destPerPage))` -> 执行当前语句，参与该文件整体逻辑。
- 230: `params.set('light', 'true') // 轻量模式` -> 执行当前语句，参与该文件整体逻辑。
- 231: `(空行)` -> 空行，用于提升代码结构可读性。
- 232: `// 排序映射` -> 注释行，用于解释设计意图或使用说明。
- 233: `const sortMap: Record<DestSortKey, { sort: string; order: string }> = {` -> 声明变量或常量，保存运行时数据。
- 234: `'hot_desc': { sort: 'popular', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 235: `'rating_desc': { sort: 'rating', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 236: `'created_desc': { sort: 'created_at', order: 'desc' },` -> 执行当前语句，参与该文件整体逻辑。
- 237: `}` -> 结束当前语句或代码块。
- 238: `const sortConfig = sortMap[destSort]` -> 声明变量或常量，保存运行时数据。
- 239: `params.set('sort_by', sortConfig.sort)` -> 执行当前语句，参与该文件整体逻辑。
- 240: `params.set('order', sortConfig.order)` -> 执行当前语句，参与该文件整体逻辑。
- 241: `(空行)` -> 空行，用于提升代码结构可读性。
- 242: `if (debouncedKeyword.trim()) {` -> 条件判断分支，根据场景执行不同逻辑。
- 243: `params.set('keyword', debouncedKeyword.trim())` -> 执行当前语句，参与该文件整体逻辑。
- 244: `}` -> 结束当前语句或代码块。
- 245: `(空行)` -> 空行，用于提升代码结构可读性。
- 246: `if (province.trim()) {` -> 条件判断分支，根据场景执行不同逻辑。
- 247: `params.set('province', province.trim())` -> 执行当前语句，参与该文件整体逻辑。
- 248: `}` -> 结束当前语句或代码块。
- 249: `(空行)` -> 空行，用于提升代码结构可读性。
- 250: `const res = await fetch(\`/api/destinations?${params.toString()}\`, {` -> 声明变量或常量，保存运行时数据。
- 251: `cache: 'default'` -> 执行当前语句，参与该文件整体逻辑。
- 252: `})` -> 执行当前语句，参与该文件整体逻辑。
- 253: `(空行)` -> 空行，用于提升代码结构可读性。
- 254: `if (!res.ok) {` -> 条件判断分支，根据场景执行不同逻辑。
- 255: `throw new Error(\`请求失败: ${res.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 256: `}` -> 结束当前语句或代码块。
- 257: `(空行)` -> 空行，用于提升代码结构可读性。
- 258: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 259: `(空行)` -> 空行，用于提升代码结构可读性。
- 260: `if (!data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 261: `throw new Error(data.error || '获取数据失败')` -> 执行当前语句，参与该文件整体逻辑。
- 262: `}` -> 结束当前语句或代码块。
- 263: `(空行)` -> 空行，用于提升代码结构可读性。
- 264: `const list = (data?.destinations || []) as DestinationRow[]` -> 声明变量或常量，保存运行时数据。
- 265: `const total = data?.total || 0` -> 声明变量或常量，保存运行时数据。
- 266: `(空行)` -> 空行，用于提升代码结构可读性。
- 267: `setSpotsDest(prev => isLoadMore ? [...prev, ...list] : list)` -> 执行当前语句，参与该文件整体逻辑。
- 268: `setDestTotal(total)` -> 执行当前语句，参与该文件整体逻辑。
- 269: `setHasMoreSpots(list.length === destPerPage && pageNum * destPerPage < total)` -> 执行当前语句，参与该文件整体逻辑。
- 270: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 271: `setSpotsError(e?.message || '加载失败')` -> 执行当前语句，参与该文件整体逻辑。
- 272: `toast.error(e?.message || '加载失败')` -> 执行当前语句，参与该文件整体逻辑。
- 273: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 274: `setLoadingSpots(false)` -> 执行当前语句，参与该文件整体逻辑。
- 275: `}` -> 结束当前语句或代码块。
- 276: `}, [province, debouncedKeyword, destSort])` -> 执行当前语句，参与该文件整体逻辑。
- 277: `(空行)` -> 空行，用于提升代码结构可读性。
- 278: `// 初始加载景点` -> 注释行，用于解释设计意图或使用说明。
- 279: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 280: `setDestPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 281: `fetchDestinations(1, false)` -> 执行当前语句，参与该文件整体逻辑。
- 282: `}, [fetchDestinations])` -> 执行当前语句，参与该文件整体逻辑。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `// 计算景点总页数` -> 注释行，用于解释设计意图或使用说明。
- 285: `const destTotalPages = Math.max(1, Math.ceil(destTotal / destPerPage))` -> 声明变量或常量，保存运行时数据。
- 286: `(空行)` -> 空行，用于提升代码结构可读性。
- 287: `// 获取产品列表` -> 注释行，用于解释设计意图或使用说明。
- 288: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 289: `if (tab !== 'products') return` -> 条件判断分支，根据场景执行不同逻辑。
- 290: `const controller = new AbortController()` -> 声明变量或常量，保存运行时数据。
- 291: `async function run() {` -> 定义函数或方法，实现具体业务逻辑。
- 292: `try {` -> 异常处理逻辑，提升程序健壮性。
- 293: `setLoadingProducts(true)` -> 执行当前语句，参与该文件整体逻辑。
- 294: `setError(null)` -> 执行当前语句，参与该文件整体逻辑。
- 295: `(空行)` -> 空行，用于提升代码结构可读性。
- 296: `const params = new URLSearchParams()` -> 声明变量或常量，保存运行时数据。
- 297: `params.set('status', 'active')` -> 执行当前语句，参与该文件整体逻辑。
- 298: `params.set('sort', sortMeta.apiSort)` -> 执行当前语句，参与该文件整体逻辑。
- 299: `params.set('order', sortMeta.order)` -> 执行当前语句，参与该文件整体逻辑。
- 300: `params.set('limit', String(perPage))` -> 执行当前语句，参与该文件整体逻辑。
- 301: `params.set('offset', String((page - 1) * perPage))` -> 执行当前语句，参与该文件整体逻辑。
- 302: `(空行)` -> 空行，用于提升代码结构可读性。
- 303: `if (city.trim()) params.set('destination', city.trim())` -> 条件判断分支，根据场景执行不同逻辑。
- 304: `if (type !== 'all') params.set('type', type)` -> 条件判断分支，根据场景执行不同逻辑。
- 305: `if (typeof ratingMin === 'number') params.set('rating_min', String(ratingMin))` -> 条件判断分支，根据场景执行不同逻辑。
- 306: `if (typeof priceMin === 'number') params.set('budget_min', String(priceMin))` -> 条件判断分支，根据场景执行不同逻辑。
- 307: `if (typeof priceMax === 'number') params.set('budget_max', String(priceMax))` -> 条件判断分支，根据场景执行不同逻辑。
- 308: `if (debouncedKeyword.trim()) params.set('q', debouncedKeyword.trim())` -> 条件判断分支，根据场景执行不同逻辑。
- 309: `(空行)` -> 空行，用于提升代码结构可读性。
- 310: `params.append('_t', String(Date.now()))` -> 执行当前语句，参与该文件整体逻辑。
- 311: `const res = await fetch(\`/api/products?${params.toString()}\`, {` -> 声明变量或常量，保存运行时数据。
- 312: `cache: 'no-store',` -> 执行当前语句，参与该文件整体逻辑。
- 313: `signal: controller.signal` -> 执行当前语句，参与该文件整体逻辑。
- 314: `})` -> 执行当前语句，参与该文件整体逻辑。
- 315: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 316: `(空行)` -> 空行，用于提升代码结构可读性。
- 317: `const list = (data?.products ?? data?.items ?? data?.data ?? []) as Product[]` -> 声明变量或常量，保存运行时数据。
- 318: `const nextTotal = Number(data?.total ?? data?.count ?? data?.total_count ?? list.length)` -> 声明变量或常量，保存运行时数据。
- 319: `(空行)` -> 空行，用于提升代码结构可读性。
- 320: `const sorted = [...list].sort((a, b) => {` -> 声明变量或常量，保存运行时数据。
- 321: `switch (sortKey) {` -> 执行当前语句，参与该文件整体逻辑。
- 322: `case 'price_asc':` -> 执行当前语句，参与该文件整体逻辑。
- 323: `return a.price - b.price` -> 返回结果或提前结束当前流程。
- 324: `case 'price_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 325: `return b.price - a.price` -> 返回结果或提前结束当前流程。
- 326: `case 'rating_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 327: `case 'recommended':` -> 执行当前语句，参与该文件整体逻辑。
- 328: `return b.rating - a.rating || b.review_count - a.review_count` -> 返回结果或提前结束当前流程。
- 329: `case 'sales_desc':` -> 执行当前语句，参与该文件整体逻辑。
- 330: `return b.review_count - a.review_count || b.rating - a.rating` -> 返回结果或提前结束当前流程。
- 331: `default:` -> 执行当前语句，参与该文件整体逻辑。
- 332: `return b.rating - a.rating` -> 返回结果或提前结束当前流程。
- 333: `}` -> 结束当前语句或代码块。
- 334: `})` -> 执行当前语句，参与该文件整体逻辑。
- 335: `(空行)` -> 空行，用于提升代码结构可读性。
- 336: `const keywordLower = debouncedKeyword.trim().toLowerCase()` -> 声明变量或常量，保存运行时数据。
- 337: `const pageFiltered =` -> 声明变量或常量，保存运行时数据。
- 338: `keywordLower.length > 0` -> 执行当前语句，参与该文件整体逻辑。
- 339: `? sorted.filter((p) => {` -> 执行当前语句，参与该文件整体逻辑。
- 340: `const hay = \`${p.name} ${p.location.city} ${(p.tags ?? []).join(' ')}\`.toLowerCase()` -> 声明变量或常量，保存运行时数据。
- 341: `return hay.includes(keywordLower)` -> 返回结果或提前结束当前流程。
- 342: `})` -> 执行当前语句，参与该文件整体逻辑。
- 343: `: sorted` -> 执行当前语句，参与该文件整体逻辑。
- 344: `(空行)` -> 空行，用于提升代码结构可读性。
- 345: `setProducts(pageFiltered)` -> 执行当前语句，参与该文件整体逻辑。
- 346: `setTotal(nextTotal)` -> 执行当前语句，参与该文件整体逻辑。
- 347: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 348: `if (e?.name === 'AbortError') return` -> 条件判断分支，根据场景执行不同逻辑。
- 349: `const msg = e?.message || '加载失败'` -> 声明变量或常量，保存运行时数据。
- 350: `setError(msg)` -> 执行当前语句，参与该文件整体逻辑。
- 351: `toast.error(msg)` -> 执行当前语句，参与该文件整体逻辑。
- 352: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 353: `setLoadingProducts(false)` -> 执行当前语句，参与该文件整体逻辑。
- 354: `}` -> 结束当前语句或代码块。
- 355: `}` -> 结束当前语句或代码块。
- 356: `(空行)` -> 空行，用于提升代码结构可读性。
- 357: `run()` -> 执行当前语句，参与该文件整体逻辑。
- 358: `return () => controller.abort()` -> 返回结果或提前结束当前流程。
- 359: `}, [tab, city, type, ratingMin, priceMin, priceMax, sortKey, page, debouncedKeyword, sortMeta])` -> 执行当前语句，参与该文件整体逻辑。
- 360: `(空行)` -> 空行，用于提升代码结构可读性。
- 361: `const loadingSkeleton = (` -> 声明变量或常量，保存运行时数据。
- 362: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 363: `{Array.from({ length: 12 }).map((_, i) => (` -> 执行当前语句，参与该文件整体逻辑。
- 364: `<div key={i} className="rounded-2xl bg-card border border-border/60 overflow-hidden animate-pulse">` -> JSX/HTML 结构行，用于描述页面元素。
- 365: `<div className="aspect-[4/3] bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 366: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 367: `<div className="h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-1/2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `<div className="mt-4 h-5 bg-muted-foreground/10 rounded w-1/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 370: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 371: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 373: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 374: `)` -> 结束当前语句或代码块。
- 375: `(空行)` -> 空行，用于提升代码结构可读性。
- 376: `const spotsSkeleton = (` -> 声明变量或常量，保存运行时数据。
- 377: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 378: `{Array.from({ length: 9 }).map((_, i) => (` -> 执行当前语句，参与该文件整体逻辑。
- 379: `<div key={i} className="rounded-2xl bg-card border border-border/60 overflow-hidden animate-pulse">` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `<div className="h-40 sm:aspect-[4/3] sm:h-auto bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 382: `<div className="h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 383: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-full" />` -> JSX/HTML 结构行，用于描述页面元素。
- 384: `<div className="mt-4 h-3 bg-muted-foreground/10 rounded w-1/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 385: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 386: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 387: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 388: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 389: `)` -> 结束当前语句或代码块。
- 390: `(空行)` -> 空行，用于提升代码结构可读性。
- 391: `return (` -> 返回结果或提前结束当前流程。
- 392: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 393: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 394: `(空行)` -> 空行，用于提升代码结构可读性。
- 395: `<main className="pt-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 396: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 397: `<div className="mb-8 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 398: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 399: `<h1 className="text-2xl md:text-3xl font-extrabold text-foreground">探索目的地</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 400: `<p className="text-sm text-muted-foreground mt-1">浏览目的地景点，或筛选旅行产品</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 401: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 402: `<div className="inline-flex rounded-2xl border border-border/70 bg-card/80 p-1 backdrop-blur">` -> JSX/HTML 结构行，用于描述页面元素。
- 403: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 404: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 405: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 406: `setTab('spots')` -> 执行当前语句，参与该文件整体逻辑。
- 407: `syncTabToUrl('spots')` -> 执行当前语句，参与该文件整体逻辑。
- 408: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 409: `className={\`px-4 py-2 rounded-xl text-sm font-semibold transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 410: `tab === 'spots' ? 'bg-primary text-primary-foreground shadow-sm' : 'text-muted-foreground hover:text-foreground'` -> 执行当前语句，参与该文件整体逻辑。
- 411: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 412: `>` -> 执行当前语句，参与该文件整体逻辑。
- 413: `目的地` -> 执行当前语句，参与该文件整体逻辑。
- 414: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 415: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 416: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 417: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 418: `setTab('products')` -> 执行当前语句，参与该文件整体逻辑。
- 419: `syncTabToUrl('products')` -> 执行当前语句，参与该文件整体逻辑。
- 420: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 421: `className={\`px-4 py-2 rounded-xl text-sm font-semibold transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 422: `tab === 'products' ? 'bg-primary text-primary-foreground shadow-sm' : 'text-muted-foreground hover:text-foreground'` -> 执行当前语句，参与该文件整体逻辑。
- 423: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 424: `>` -> 执行当前语句，参与该文件整体逻辑。
- 425: `旅行产品` -> 执行当前语句，参与该文件整体逻辑。
- 426: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 427: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 428: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 429: `(空行)` -> 空行，用于提升代码结构可读性。
- 430: `{tab === 'spots' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 431: `<section className="rounded-3xl border border-border/60 bg-card/70 backdrop-blur p-5 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 432: `<div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 433: `{/* 大洲筛选已移除 */}` -> 执行当前语句，参与该文件整体逻辑。
- 434: `<div className="flex flex-col sm:flex-row gap-3 w-full md:w-auto md:min-w-[260px]">` -> JSX/HTML 结构行，用于描述页面元素。
- 435: `<div className="relative flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 436: `<Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />` -> JSX/HTML 结构行，用于描述页面元素。
- 437: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 438: `value={keyword}` -> 执行当前语句，参与该文件整体逻辑。
- 439: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 440: `setKeyword(e.target.value)` -> 执行当前语句，参与该文件整体逻辑。
- 441: `setDestPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 442: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 443: `placeholder="搜索景点 / 省份"` -> 执行当前语句，参与该文件整体逻辑。
- 444: `className="input pl-10 w-full"` -> 执行当前语句，参与该文件整体逻辑。
- 445: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 446: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 447: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 448: `value={province}` -> 执行当前语句，参与该文件整体逻辑。
- 449: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 450: `setProvince(e.target.value)` -> 执行当前语句，参与该文件整体逻辑。
- 451: `setDestPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 452: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 453: `className="input w-full sm:w-auto"` -> 执行当前语句，参与该文件整体逻辑。
- 454: `>` -> 执行当前语句，参与该文件整体逻辑。
- 455: `<option value="">所有省份</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 456: `{loadingProvinces ? (` -> 执行当前语句，参与该文件整体逻辑。
- 457: `<option disabled>加载中...</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 458: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 459: `provinces.map((p) => (` -> 执行当前语句，参与该文件整体逻辑。
- 460: `<option key={p.name} value={p.name}>` -> JSX/HTML 结构行，用于描述页面元素。
- 461: `{p.name} ({p.count})` -> 执行当前语句，参与该文件整体逻辑。
- 462: `</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 463: `))` -> 执行当前语句，参与该文件整体逻辑。
- 464: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 465: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 466: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 467: `value={destSort}` -> 执行当前语句，参与该文件整体逻辑。
- 468: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 469: `setDestSort(e.target.value as DestSortKey)` -> 执行当前语句，参与该文件整体逻辑。
- 470: `setDestPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 471: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 472: `className="input w-full sm:w-auto"` -> 执行当前语句，参与该文件整体逻辑。
- 473: `>` -> 执行当前语句，参与该文件整体逻辑。
- 474: `<option value="hot_desc">按热度排序</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 475: `<option value="rating_desc">按评分排序</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 476: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 478: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 479: `(空行)` -> 空行，用于提升代码结构可读性。
- 480: `{spotsError ? (` -> 执行当前语句，参与该文件整体逻辑。
- 481: `<div className="card p-6 border-red-200 bg-red-50 dark:bg-red-950/30 dark:border-red-800 text-red-700 dark:text-red-200">` -> JSX/HTML 结构行，用于描述页面元素。
- 482: `<div className="font-semibold">加载失败</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 483: `<div className="text-sm opacity-90 mt-2">{spotsError}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 484: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 485: `className="btn btn-outline mt-4"` -> 执行当前语句，参与该文件整体逻辑。
- 486: `onClick={() => fetchDestinations(1, false)}` -> 执行当前语句，参与该文件整体逻辑。
- 487: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 488: `>` -> 执行当前语句，参与该文件整体逻辑。
- 489: `<RotateCcw className="h-4 w-4 mr-2" />` -> JSX/HTML 结构行，用于描述页面元素。
- 490: `重试` -> 执行当前语句，参与该文件整体逻辑。
- 491: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 493: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 494: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 495: `<p className="text-sm text-muted-foreground mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 496: `共 {destTotal} 个景点` -> 执行当前语句，参与该文件整体逻辑。
- 497: `{province ? \` · ${province}\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 498: `{debouncedKeyword ? \` · 搜索 "${debouncedKeyword}"\` : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 499: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 500: `(空行)` -> 空行，用于提升代码结构可读性。
- 501: `{spotsDest.length === 0 && !loadingSpots ? (` -> 执行当前语句，参与该文件整体逻辑。
- 502: `<div className="rounded-2xl border border-border/60 bg-card p-10 text-center text-muted-foreground">` -> JSX/HTML 结构行，用于描述页面元素。
- 503: `暂无符合条件的景点。试试其它筛选条件` -> 执行当前语句，参与该文件整体逻辑。
- 504: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 505: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 506: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 507: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 508: `{spotsDest.map((d) => (` -> 执行当前语句，参与该文件整体逻辑。
- 509: `<DestinationCard key={d.id} destination={d} imageHeightClass="h-40" />` -> JSX/HTML 结构行，用于描述页面元素。
- 510: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 511: `{loadingSpots && (` -> 执行当前语句，参与该文件整体逻辑。
- 512: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 513: `{Array.from({ length: 3 }).map((_, i) => (` -> 执行当前语句，参与该文件整体逻辑。
- 514: `<div key={\`loading-${i}\`} className="rounded-2xl bg-card border border-border/60 overflow-hidden animate-pulse">` -> JSX/HTML 结构行，用于描述页面元素。
- 515: `<div className="h-40 sm:aspect-[4/3] sm:h-auto bg-muted-foreground/10" />` -> JSX/HTML 结构行，用于描述页面元素。
- 516: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 517: `<div className="h-4 bg-muted-foreground/10 rounded w-2/3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 518: `<div className="mt-2 h-3 bg-muted-foreground/10 rounded w-full" />` -> JSX/HTML 结构行，用于描述页面元素。
- 519: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 520: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 521: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 522: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 523: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 524: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 525: `(空行)` -> 空行，用于提升代码结构可读性。
- 526: `{/* 底部分页器 */}` -> 执行当前语句，参与该文件整体逻辑。
- 527: `{destTotalPages > 1 && spotsDest.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 528: `<div className="mt-7 flex items-center justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 529: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 530: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 531: `disabled={destPage <= 1}` -> 执行当前语句，参与该文件整体逻辑。
- 532: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 533: `setDestPage((p) => Math.max(1, p - 1))` -> 执行当前语句，参与该文件整体逻辑。
- 534: `fetchDestinations(Math.max(1, destPage - 1), false)` -> 执行当前语句，参与该文件整体逻辑。
- 535: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 536: `className="btn btn-outline disabled:opacity-50 disabled:cursor-not-allowed"` -> 执行当前语句，参与该文件整体逻辑。
- 537: `>` -> 执行当前语句，参与该文件整体逻辑。
- 538: `上一页` -> 执行当前语句，参与该文件整体逻辑。
- 539: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 540: `(空行)` -> 空行，用于提升代码结构可读性。
- 541: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 542: `{Array.from({ length: Math.min(destTotalPages, 7) }).map((_, i) => {` -> 执行当前语句，参与该文件整体逻辑。
- 543: `const start = Math.max(1, destPage - 3)` -> 声明变量或常量，保存运行时数据。
- 544: `const idx = start + i` -> 声明变量或常量，保存运行时数据。
- 545: `if (idx > destTotalPages) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 546: `const active = idx === destPage` -> 声明变量或常量，保存运行时数据。
- 547: `return (` -> 返回结果或提前结束当前流程。
- 548: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 549: `key={idx}` -> 执行当前语句，参与该文件整体逻辑。
- 550: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 551: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 552: `setDestPage(idx)` -> 执行当前语句，参与该文件整体逻辑。
- 553: `fetchDestinations(idx, false)` -> 执行当前语句，参与该文件整体逻辑。
- 554: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 555: `className={\`h-9 min-w-9 rounded-xl border px-3 text-sm font-semibold transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 556: `active` -> 执行当前语句，参与该文件整体逻辑。
- 557: `? 'bg-primary text-primary-foreground border-primary'` -> 执行当前语句，参与该文件整体逻辑。
- 558: `: 'bg-transparent border-border hover:bg-card/70'` -> 执行当前语句，参与该文件整体逻辑。
- 559: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 560: `>` -> 执行当前语句，参与该文件整体逻辑。
- 561: `{idx}` -> 执行当前语句，参与该文件整体逻辑。
- 562: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 563: `)` -> 结束当前语句或代码块。
- 564: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 565: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 566: `(空行)` -> 空行，用于提升代码结构可读性。
- 567: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 568: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 569: `disabled={destPage >= destTotalPages}` -> 执行当前语句，参与该文件整体逻辑。
- 570: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 571: `const nextPage = destPage + 1` -> 声明变量或常量，保存运行时数据。
- 572: `setDestPage(nextPage)` -> 执行当前语句，参与该文件整体逻辑。
- 573: `fetchDestinations(nextPage, false)` -> 执行当前语句，参与该文件整体逻辑。
- 574: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 575: `className="btn btn-outline disabled:opacity-50 disabled:cursor-not-allowed inline-flex items-center gap-2"` -> 执行当前语句，参与该文件整体逻辑。
- 576: `>` -> 执行当前语句，参与该文件整体逻辑。
- 577: `下一页` -> 执行当前语句，参与该文件整体逻辑。
- 578: `<ChevronRight className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 579: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 580: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 581: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 582: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 583: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 584: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 585: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 586: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 587: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 588: `<div className="flex flex-col lg:flex-row gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 589: `<aside className="lg:w-80 shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 590: `<div className="rounded-3xl border border-border/60 bg-card/70 backdrop-blur p-5 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 591: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 592: `<div className="h-11 w-11 rounded-2xl bg-primary/10 text-primary flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 593: `<SlidersHorizontal className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 594: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 595: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 596: `<h2 className="text-xl font-extrabold text-foreground">产品筛选</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 597: `<p className="text-sm text-muted-foreground mt-1">价格区间 · 评分 · 类型 · 城市</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 598: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 599: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 600: `(空行)` -> 空行，用于提升代码结构可读性。
- 601: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 602: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 603: `<div className="text-sm font-semibold text-foreground mb-2">关键词</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 604: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 605: `<Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />` -> JSX/HTML 结构行，用于描述页面元素。
- 606: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 607: `value={keyword}` -> 执行当前语句，参与该文件整体逻辑。
- 608: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 609: `setKeyword(e.target.value)` -> 执行当前语句，参与该文件整体逻辑。
- 610: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 611: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 612: `placeholder="目的地/商品/标签"` -> 执行当前语句，参与该文件整体逻辑。
- 613: `className="input pl-10"` -> 执行当前语句，参与该文件整体逻辑。
- 614: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 615: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 616: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 617: `(空行)` -> 空行，用于提升代码结构可读性。
- 618: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 619: `<div className="text-sm font-semibold text-foreground mb-2">城市</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 620: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 621: `<MapPin className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />` -> JSX/HTML 结构行，用于描述页面元素。
- 622: `{loadingCities ? (` -> 执行当前语句，参与该文件整体逻辑。
- 623: `<input disabled className="input pl-10" value="加载城市中..." />` -> JSX/HTML 结构行，用于描述页面元素。
- 624: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 625: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 626: `value={city}` -> 执行当前语句，参与该文件整体逻辑。
- 627: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 628: `setCity(e.target.value)` -> 执行当前语句，参与该文件整体逻辑。
- 629: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 630: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 631: `className="input pl-10"` -> 执行当前语句，参与该文件整体逻辑。
- 632: `>` -> 执行当前语句，参与该文件整体逻辑。
- 633: `<option value="">不限</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 634: `{cities.map((c) => (` -> 执行当前语句，参与该文件整体逻辑。
- 635: `<option key={c.name} value={c.name}>` -> JSX/HTML 结构行，用于描述页面元素。
- 636: `{c.name} ({c.count})` -> 执行当前语句，参与该文件整体逻辑。
- 637: `</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 638: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 639: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 640: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 641: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 642: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 643: `(空行)` -> 空行，用于提升代码结构可读性。
- 644: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 645: `<div className="text-sm font-semibold text-foreground mb-2">类型</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 646: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 647: `value={type}` -> 执行当前语句，参与该文件整体逻辑。
- 648: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 649: `setType(e.target.value as any)` -> 执行当前语句，参与该文件整体逻辑。
- 650: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 651: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 652: `className="input"` -> 执行当前语句，参与该文件整体逻辑。
- 653: `>` -> 执行当前语句，参与该文件整体逻辑。
- 654: `<option value="all">全部</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 655: `<option value="flight">机票</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 656: `<option value="hotel">酒店</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 657: `<option value="ticket">门票</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 658: `<option value="experience">当地体验</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 659: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 660: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 661: `(空行)` -> 空行，用于提升代码结构可读性。
- 662: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 663: `<div className="text-sm font-semibold text-foreground mb-2">价格区间</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 664: `<div className="grid grid-cols-2 gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 665: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 666: `<div className="text-xs text-muted-foreground mb-1">最低</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 667: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 668: `inputMode="numeric"` -> 执行当前语句，参与该文件整体逻辑。
- 669: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 670: `value={priceMin}` -> 执行当前语句，参与该文件整体逻辑。
- 671: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 672: `const v = e.target.value` -> 声明变量或常量，保存运行时数据。
- 673: `setPriceMin(v === '' ? '' : Number(v))` -> 执行当前语句，参与该文件整体逻辑。
- 674: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 675: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 676: `placeholder="例如 200"` -> 执行当前语句，参与该文件整体逻辑。
- 677: `className="input"` -> 执行当前语句，参与该文件整体逻辑。
- 678: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 679: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 680: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 681: `<div className="text-xs text-muted-foreground mb-1">最高</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 682: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 683: `inputMode="numeric"` -> 执行当前语句，参与该文件整体逻辑。
- 684: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 685: `value={priceMax}` -> 执行当前语句，参与该文件整体逻辑。
- 686: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 687: `const v = e.target.value` -> 声明变量或常量，保存运行时数据。
- 688: `setPriceMax(v === '' ? '' : Number(v))` -> 执行当前语句，参与该文件整体逻辑。
- 689: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 690: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 691: `placeholder="例如 1200"` -> 执行当前语句，参与该文件整体逻辑。
- 692: `className="input"` -> 执行当前语句，参与该文件整体逻辑。
- 693: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 694: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 695: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 696: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 697: `(空行)` -> 空行，用于提升代码结构可读性。
- 698: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 699: `<div className="text-sm font-semibold text-foreground mb-2">评分</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 700: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 701: `value={ratingMin === '' ? '' : String(ratingMin)}` -> 执行当前语句，参与该文件整体逻辑。
- 702: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 703: `const v = e.target.value` -> 声明变量或常量，保存运行时数据。
- 704: `setRatingMin(v === '' ? '' : Number(v))` -> 执行当前语句，参与该文件整体逻辑。
- 705: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 706: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 707: `className="input"` -> 执行当前语句，参与该文件整体逻辑。
- 708: `>` -> 执行当前语句，参与该文件整体逻辑。
- 709: `<option value="">不限</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 710: `<option value="4.8">4.8+ 优质</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 711: `<option value="4.5">4.5+ 推荐</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 712: `<option value="4.2">4.2+ 好评</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 713: `<option value="4.0">4.0+ 及格</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 714: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 715: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 716: `(空行)` -> 空行，用于提升代码结构可读性。
- 717: `<div className="flex gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 718: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 719: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 720: `className="flex-1 btn btn-outline"` -> 执行当前语句，参与该文件整体逻辑。
- 721: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 722: `setKeyword('')` -> 执行当前语句，参与该文件整体逻辑。
- 723: `setCity('')` -> 执行当前语句，参与该文件整体逻辑。
- 724: `setType('all')` -> 执行当前语句，参与该文件整体逻辑。
- 725: `setRatingMin('')` -> 执行当前语句，参与该文件整体逻辑。
- 726: `setPriceMin('')` -> 执行当前语句，参与该文件整体逻辑。
- 727: `setPriceMax('')` -> 执行当前语句，参与该文件整体逻辑。
- 728: `setSortKey('recommended')` -> 执行当前语句，参与该文件整体逻辑。
- 729: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 730: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 731: `>` -> 执行当前语句，参与该文件整体逻辑。
- 732: `重置` -> 执行当前语句，参与该文件整体逻辑。
- 733: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 734: `<Link href="/assistant" className="flex-1 btn btn-primary inline-flex items-center justify-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 735: `<Filter className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 736: `AI筛选` -> 执行当前语句，参与该文件整体逻辑。
- 737: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 738: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 739: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 740: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 741: `</aside>` -> JSX/HTML 结构行，用于描述页面元素。
- 742: `(空行)` -> 空行，用于提升代码结构可读性。
- 743: `<section className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 744: `<div className="rounded-3xl border border-border/60 bg-card/70 backdrop-blur p-5 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 745: `<div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 746: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 747: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 748: `<Star className="h-4 w-4 text-amber-500 fill-amber-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 749: `<h2 className="text-lg md:text-xl font-extrabold text-foreground">产品列表</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 750: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 751: `<p className="text-sm text-muted-foreground mt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 752: `{total ? \`共${total}个产品 · ${sortLabel}\` : \`根据筛选条件实时更新 · ${sortLabel}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 753: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 754: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 755: `(空行)` -> 空行，用于提升代码结构可读性。
- 756: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 757: `<div className="hidden sm:block text-sm text-muted-foreground">排序</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 758: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 759: `value={sortKey}` -> 执行当前语句，参与该文件整体逻辑。
- 760: `onChange={(e) => {` -> 执行当前语句，参与该文件整体逻辑。
- 761: `setSortKey(e.target.value as SortKey)` -> 执行当前语句，参与该文件整体逻辑。
- 762: `setPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 763: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 764: `className="input"` -> 执行当前语句，参与该文件整体逻辑。
- 765: `>` -> 执行当前语句，参与该文件整体逻辑。
- 766: `<option value="recommended">推荐（评分优先）</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 767: `<option value="sales_desc">销量优先</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 768: `<option value="rating_desc">评分优先</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 769: `<option value="price_asc">价格从低到高</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 770: `<option value="price_desc">价格从高到低</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 771: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 772: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 773: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 774: `(空行)` -> 空行，用于提升代码结构可读性。
- 775: `{loadingProducts && products.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 776: `loadingSkeleton` -> 执行当前语句，参与该文件整体逻辑。
- 777: `) : error ? (` -> 执行当前语句，参与该文件整体逻辑。
- 778: `<div className="card p-6 border-red-200 bg-red-50 dark:bg-red-950/30 dark:border-red-800 text-red-700 dark:text-red-200">` -> JSX/HTML 结构行，用于描述页面元素。
- 779: `<div className="font-semibold">加载失败</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 780: `<div className="text-sm opacity-90 mt-2">{error}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 781: `<div className="mt-4 flex gap-3 flex-wrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 782: `<button className="btn btn-outline" onClick={() => window.location.reload()} type="button">` -> JSX/HTML 结构行，用于描述页面元素。
- 783: `<RotateCcw className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 784: `重试` -> 执行当前语句，参与该文件整体逻辑。
- 785: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 786: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 787: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 788: `) : products.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 789: `<div className="rounded-2xl border border-border/60 bg-card p-10 text-center text-muted-foreground">` -> JSX/HTML 结构行，用于描述页面元素。
- 790: `暂无符合条件的产品。你可以放宽筛选条件再试试` -> 执行当前语句，参与该文件整体逻辑。
- 791: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 792: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 793: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 794: `{products.map((p, idx) => (` -> 执行当前语句，参与该文件整体逻辑。
- 795: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 796: `key={p.id}` -> 执行当前语句，参与该文件整体逻辑。
- 797: `href={\`/products/${p.id}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 798: `className={idx === 0 ? 'sm:col-span-2 lg:col-span-2' : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 799: `>` -> 执行当前语句，参与该文件整体逻辑。
- 800: `<ProductCard product={p} variant={idx === 0 ? 'featured' : 'default'} className="h-full" />` -> JSX/HTML 结构行，用于描述页面元素。
- 801: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 802: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 803: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 804: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 805: `(空行)` -> 空行，用于提升代码结构可读性。
- 806: `{!loadingProducts && !error && totalPages > 1 && products.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 807: `<div className="mt-7 flex items-center justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 808: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 809: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 810: `disabled={page <= 1}` -> 执行当前语句，参与该文件整体逻辑。
- 811: `onClick={() => setPage((p) => Math.max(1, p - 1))}` -> 执行当前语句，参与该文件整体逻辑。
- 812: `className="btn btn-outline disabled:opacity-50 disabled:cursor-not-allowed"` -> 执行当前语句，参与该文件整体逻辑。
- 813: `>` -> 执行当前语句，参与该文件整体逻辑。
- 814: `上一页` -> 执行当前语句，参与该文件整体逻辑。
- 815: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 816: `(空行)` -> 空行，用于提升代码结构可读性。
- 817: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 818: `{Array.from({ length: Math.min(totalPages, 7) }).map((_, i) => {` -> 执行当前语句，参与该文件整体逻辑。
- 819: `const start = Math.max(1, page - 3)` -> 声明变量或常量，保存运行时数据。
- 820: `const idx = start + i` -> 声明变量或常量，保存运行时数据。
- 821: `if (idx > totalPages) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 822: `const active = idx === page` -> 声明变量或常量，保存运行时数据。
- 823: `return (` -> 返回结果或提前结束当前流程。
- 824: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 825: `key={idx}` -> 执行当前语句，参与该文件整体逻辑。
- 826: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 827: `onClick={() => setPage(idx)}` -> 执行当前语句，参与该文件整体逻辑。
- 828: `className={\`h-9 min-w-9 rounded-xl border px-3 text-sm font-semibold transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 829: `active` -> 执行当前语句，参与该文件整体逻辑。
- 830: `? 'bg-primary text-primary-foreground border-primary'` -> 执行当前语句，参与该文件整体逻辑。
- 831: `: 'bg-transparent border-border hover:bg-card/70'` -> 执行当前语句，参与该文件整体逻辑。
- 832: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 833: `>` -> 执行当前语句，参与该文件整体逻辑。
- 834: `{idx}` -> 执行当前语句，参与该文件整体逻辑。
- 835: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 836: `)` -> 结束当前语句或代码块。
- 837: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 838: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 839: `(空行)` -> 空行，用于提升代码结构可读性。
- 840: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 841: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 842: `disabled={page >= totalPages}` -> 执行当前语句，参与该文件整体逻辑。
- 843: `onClick={() => setPage((p) => Math.min(totalPages, p + 1))}` -> 执行当前语句，参与该文件整体逻辑。
- 844: `className="btn btn-outline disabled:opacity-50 disabled:cursor-not-allowed inline-flex items-center gap-2"` -> 执行当前语句，参与该文件整体逻辑。
- 845: `>` -> 执行当前语句，参与该文件整体逻辑。
- 846: `下一页` -> 执行当前语句，参与该文件整体逻辑。
- 847: `<ChevronRight className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 848: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 849: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 850: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 851: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 852: `</section>` -> JSX/HTML 结构行，用于描述页面元素。
- 853: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 854: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 855: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 856: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 857: `(空行)` -> 空行，用于提升代码结构可读性。
- 858: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 859: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 860: `)` -> 结束当前语句或代码块。
- 861: `}` -> 结束当前语句或代码块。
