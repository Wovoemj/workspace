# `frontend/user-web/src/app/feedback/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 意见反馈模块 - 用户反馈提交` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 反馈表单：姓名、联系方式（选填）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 反馈类型选择：功能建议/问题反馈/体验优化/其他` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 星级满意度评分（5星）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 详细反馈内容文本框` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 附件上传（选填）` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 提交反馈（静默处理，模拟提交）` -> 注释行，用于解释设计意图或使用说明。
- 13: `*` -> 注释行，用于解释设计意图或使用说明。
- 14: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 16: `*` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 【数据处理】` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - 表单状态本地管理` -> 注释行，用于解释设计意图或使用说明。
- 19: `* - 提交后显示成功提示（react-hot-toast）` -> 注释行，用于解释设计意图或使用说明。
- 20: `*/` -> 注释行，用于解释设计意图或使用说明。
- 21: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 24: `import {` -> 导入依赖模块，供当前文件使用。
- 25: `MessageSquare,` -> 执行当前语句，参与该文件整体逻辑。
- 26: `Send,` -> 执行当前语句，参与该文件整体逻辑。
- 27: `Star,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `ThumbsUp,` -> 执行当前语句，参与该文件整体逻辑。
- 29: `AlertCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 31: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 32: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `export default function FeedbackPage() {` -> 导出当前声明，供其他模块复用。
- 35: `return (` -> 返回结果或提前结束当前流程。
- 36: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 37: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 38: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 39: `<div className="bg-gradient-to-r from-blue-600 to-indigo-600 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 40: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 41: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 42: `<MessageSquare className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 43: `<span className="text-xl font-bold">意见反馈</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 44: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 45: `<h1 className="text-3xl md:text-4xl font-bold mb-2">您的反馈是我们进步的动力</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 46: `<p className="text-white/80">我们珍视每一位用户的意见，期待您的宝贵建</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 47: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 48: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 51: `<div className="bg-white rounded-2xl border border-slate-200 p-6 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 52: `<h2 className="text-xl font-bold text-slate-900 mb-6">提交反馈</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `<form className="space-y-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 54: `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 56: `<label className="block text-sm font-medium text-slate-700 mb-1">您的姓名</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 57: `<input type="text" className="w-full px-4 py-2.5 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-300" placeholder="请输入姓名（选填?" />` -> JSX/HTML 结构行，用于描述页面元素。
- 58: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 60: `<label className="block text-sm font-medium text-slate-700 mb-1">联系方式</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 61: `<input type="text" className="w-full px-4 py-2.5 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-300" placeholder="手机号或邮箱（选填?" />` -> JSX/HTML 结构行，用于描述页面元素。
- 62: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 63: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `<label className="block text-sm font-medium text-slate-700 mb-1">反馈类型</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 67: `<div className="grid grid-cols-2 sm:grid-cols-4 gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 68: `{['功能建议', '问题反馈', '体验优化', '其他'].map((type) => (` -> 执行当前语句，参与该文件整体逻辑。
- 69: `<label key={type} className="flex items-center gap-2 p-3 border border-slate-200 rounded-xl cursor-pointer hover:border-blue-300 has-[:checked]:border-blue-500 has-[:checked]:bg-blue-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 70: `<input type="radio" name="type" value={type} className="text-blue-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `<span className="text-sm text-slate-700">{type}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 72: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 73: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 74: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 76: `(空行)` -> 空行，用于提升代码结构可读性。
- 77: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `<label className="block text-sm font-medium text-slate-700 mb-1">满意度评</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `<div className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 80: `{[1, 2, 3, 4, 5].map((star) => (` -> 执行当前语句，参与该文件整体逻辑。
- 81: `<button key={star} type="button" className="p-1 hover:scale-110 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 82: `<Star className="h-8 w-8 text-slate-300 hover:text-amber-400 fill-transparent hover:fill-amber-400 transition" />` -> JSX/HTML 结构行，用于描述页面元素。
- 83: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 84: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 85: `<span className="ml-3 text-sm text-slate-500">（点击星星评分）</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 87: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 88: `(空行)` -> 空行，用于提升代码结构可读性。
- 89: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 90: `<label className="block text-sm font-medium text-slate-700 mb-1">详细描述</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 91: `<textarea rows={5}` -> 执行当前语句，参与该文件整体逻辑。
- 92: `className="w-full px-4 py-3 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-300 resize-none"` -> 执行当前语句，参与该文件整体逻辑。
- 93: `placeholder="请详细描述您的问题或建议..."` -> 执行当前语句，参与该文件整体逻辑。
- 94: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 95: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 98: `<label className="block text-sm font-medium text-slate-700 mb-1">上传截图（选填</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 99: `<div className="border-2 border-dashed border-slate-300 rounded-xl p-8 text-center hover:border-blue-400 transition cursor-pointer">` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `<div className="text-4xl mb-2">📷</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 101: `<p className="text-slate-500 text-sm">点击或拖拽上传图</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 104: `(空行)` -> 空行，用于提升代码结构可读性。
- 105: `<button type="submit" className="w-full py-3 bg-blue-600 text-white rounded-xl font-medium hover:bg-blue-700 transition flex items-center justify-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 106: `<Send className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 107: `提交反馈` -> 执行当前语句，参与该文件整体逻辑。
- 108: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 109: `</form>` -> JSX/HTML 结构行，用于描述页面元素。
- 110: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `(空行)` -> 空行，用于提升代码结构可读性。
- 112: `(空行)` -> 空行，用于提升代码结构可读性。
- 113: `<div className="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 114: `<div className="flex items-start gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 115: `<AlertCircle className="h-5 w-5 text-blue-600 mt-0.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 116: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `<h3 className="font-bold text-slate-900 mb-2">温馨提示</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `<ul className="text-sm text-slate-600 space-y-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `<li>?我们重视每一位用户的反馈，通常?-3个工作日内回</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `<li>?如果您的问题紧急，请拨打客服热线：400-888-9999</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 121: `<li>?如需实时帮助，建议使用在线客服功</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `</ul>` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 124: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 125: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 126: `(空行)` -> 空行，用于提升代码结构可读性。
- 127: `(空行)` -> 空行，用于提升代码结构可读性。
- 128: `<div className="mt-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `<p className="text-slate-500 mb-4">您也可以通过以下方式反馈</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 130: `<div className="flex justify-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `<Link href="/contact" className="px-4 py-2 border border-slate-300 rounded-lg text-slate-600 hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 132: `联系客服` -> 执行当前语句，参与该文件整体逻辑。
- 133: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 134: `<a href="mailto:feedback@travelai.com" className="px-4 py-2 border border-slate-300 rounded-lg text-slate-600 hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 135: `发送邮?` -> 执行当前语句，参与该文件整体逻辑。
- 136: `</a>` -> JSX/HTML 结构行，用于描述页面元素。
- 137: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 138: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 139: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 140: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 141: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 142: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 143: `)` -> 结束当前语句或代码块。
- 144: `}` -> 结束当前语句或代码块。
