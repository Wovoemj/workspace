# `backend/gateway/main.go` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `package main`
  - 作用：声明包名。
  - 原因：通过包边界组织可见性与编译单元。
- 2: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 3: `import (`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 4: `"context"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"fmt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `"net/http"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `"net/http/httputil"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `"net/url"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `"strings"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `"time"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 12: `"github.com/gin-gonic/gin"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `"github.com/sirupsen/logrus"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 15: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 16: `type Config struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 17: `Port        int`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `ServiceURLs map[string]string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 20: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 21: `type Gateway struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 22: `config     *Config`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `logger     *logrus.Logger`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 24: `router     *gin.Engine`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `httpServer *http.Server`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 27: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 28: `func NewGateway(config *Config) *Gateway {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 29: `logger := logrus.New()`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 30: `logger.SetFormatter(&logrus.TextFormatter{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 31: `FullTimestamp: true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `logger.SetLevel(logrus.InfoLevel)`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 34: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 35: `router := gin.New()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `router.Use(gin.Recovery())`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `router.Use(corsMiddleware())`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `router.Use(loggingMiddleware(logger))`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 39: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 40: `return &Gateway{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 41: `config: config,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `logger: logger,`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 43: `router: router,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 45: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 46: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 47: `func (g *Gateway) SetupRoutes() {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 48: `// Health check`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 49: `g.router.GET("/health", func(c *gin.Context) {`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 50: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 51: `"status":  "ok",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `"message": "Gateway is running",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `"time":    time.Now().Format(time.RFC3339),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 54: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 57: `// API routes proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 58: `api := g.router.Group("/api")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 59: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `// Product service proxy (景点数据)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 61: `productGroup := api.Group("")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `productGroup.Any("/destinations", g.proxyToService("product-service", ""))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 63: `productGroup.Any("/destinations/:id", g.proxyToService("product-service", ""))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 64: `productGroup.Any("/products", g.proxyToService("product-service", ""))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 65: `productGroup.Any("/products/:id", g.proxyToService("product-service", ""))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 66: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 67: `// User service proxy - strip /api/users prefix`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 68: `userGroup := api.Group("/users")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `userGroup.Any("/*path", g.proxyToService("user-service", "/api/users"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `// Favorites proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 72: `api.Any("/favorites", g.proxyToService("user-service", "/api/favorites"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 73: `api.Any("/favorites/*path", g.proxyToService("user-service", "/api/favorites"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 74: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 75: `// Order service proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 76: `orderGroup := api.Group("/orders")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `orderGroup.Any("/*path", g.proxyToService("order-service", "/api/orders"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 78: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 79: `// AI service proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 80: `aiGroup := api.Group("/ai")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `// 具体路由必须先注册，通配符路由后注册`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 83: `aiGroup.Any("/chat", g.proxyToService("ai-service", "/api/ai"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 84: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 85: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 86: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 87: `// Recommendation service proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 88: `recommendGroup := api.Group("/recommendations")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `recommendGroup.Any("/*path", g.proxyToService("recommend-service", "/api/recommendations"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 90: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 91: `// Notification service proxy`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 92: `notificationGroup := api.Group("/notifications")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `notificationGroup.Any("/*path", g.proxyToService("notification-service", "/api/notifications"))`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 94: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 95: `// Stats endpoint (临时实现)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 96: `api.GET("/stats", func(c *gin.Context) {`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 97: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 98: `"success": true,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `"stats": gin.H{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `"destinations": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `"users": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `"trips": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `"comments": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 104: `"orders": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `"footprints": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 106: `"pages": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 107: `"notifications": 0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 108: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 110: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 111: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 112: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 113: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 114: `func (g *Gateway) proxyToService(serviceName string, stripPrefix string) gin.HandlerFunc {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 115: `return func(c *gin.Context) {`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 116: `serviceURL, exists := g.config.ServiceURLs[serviceName]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `if !exists {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 118: `g.logger.Errorf("Service %s not found", serviceName)`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 119: `c.JSON(http.StatusNotFound, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 120: `"error": fmt.Sprintf("Service %s not found", serviceName),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 122: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 124: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 125: `target, err := url.Parse(serviceURL)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `if err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 127: `g.logger.Errorf("Invalid service URL: %v", err)`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 128: `c.JSON(http.StatusInternalServerError, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 129: `"error": "Invalid service configuration",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `// 创建反向代理`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 135: `proxy := httputil.NewSingleHostReverseProxy(target)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 137: `originalPath := c.Request.URL.Path`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 138: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 139: `// 替换路径前缀`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 140: `if stripPrefix != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 141: `c.Request.URL.Path = strings.Replace(c.Request.URL.Path, stripPrefix, "", 1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 142: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 143: `c.Request.URL.Host = target.Host`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `c.Request.URL.Scheme = target.Scheme`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 146: `g.logger.Infof("Proxying %s %s -> %s%s",`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 147: `c.Request.Method, originalPath, serviceURL, c.Request.URL.Path)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 149: `// 代理请求`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 150: `proxy.ServeHTTP(c.Writer, c.Request)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 151: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 152: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 153: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 154: `func (g *Gateway) Start() error {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 155: `g.SetupRoutes()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 156: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 157: `addr := fmt.Sprintf(":%d", g.config.Port)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 158: `g.httpServer = &http.Server{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `Addr:    addr,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 160: `Handler: g.router,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 161: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 162: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 163: `g.logger.Infof("Starting gateway on port %d", g.config.Port)`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 164: `return g.httpServer.ListenAndServe()`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 165: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 166: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 167: `func (g *Gateway) Stop() error {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 168: `ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `defer cancel()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 171: `return g.httpServer.Shutdown(ctx)`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 172: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 173: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 174: `func corsMiddleware() gin.HandlerFunc {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 175: `return func(c *gin.Context) {`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 176: `c.Header("Access-Control-Allow-Origin", "*")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `c.Header("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 178: `c.Header("Access-Control-Allow-Headers", "Content-Type, Authorization")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 179: `c.Header("Access-Control-Max-Age", "86400")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 180: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 181: `if c.Request.Method == "OPTIONS" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 182: `c.AbortWithStatus(204)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 183: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 184: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 185: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 186: `c.Next()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 187: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 188: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 189: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 190: `func loggingMiddleware(logger *logrus.Logger) gin.HandlerFunc {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 191: `return func(c *gin.Context) {`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 192: `start := time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 193: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 194: `c.Next()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 196: `duration := time.Since(start)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 197: `status := c.Writer.Status()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 199: `logger.WithFields(logrus.Fields{`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 200: `"method":     c.Request.Method,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `"path":       c.Request.URL.Path,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `"status":     status,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 203: `"duration":   duration,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 204: `"ip":         c.ClientIP(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 205: `"user_agent": c.Request.UserAgent(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 206: `}).Info("HTTP request")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 207: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 208: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 209: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 210: `func main() {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 211: `config := &Config{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `Port: 8080,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 213: `ServiceURLs: map[string]string{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `"user-service":        "http://localhost:8081",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 215: `"product-service":     "http://localhost:8082",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `"order-service":       "http://localhost:8083",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 217: `"ai-service":          "http://localhost:8084",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `"recommend-service":   "http://localhost:8085",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 219: `"notification-service": "http://localhost:8086",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 220: `},`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 222: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 223: `gateway := NewGateway(config)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `if err := gateway.Start(); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 225: `logrus.Fatal(err)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 227: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
