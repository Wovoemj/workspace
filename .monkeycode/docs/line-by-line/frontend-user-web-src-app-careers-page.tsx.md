# `frontend/user-web/src/app/careers/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 招聘页面模块 - 职位展示与福利介绍` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 公司招聘职位列表展示（静态数据）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 职位卡片：职位名称、部门、地点、类型、薪资` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 公司福利展示：六险一金、弹性工作、成长空间、免费旅游` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 招聘职位详情（静态展示）` -> 注释行，用于解释设计意图或使用说明。
- 11: `*` -> 注释行，用于解释设计意图或使用说明。
- 12: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 14: `*` -> 注释行，用于解释设计意图或使用说明。
- 15: `* 【数据来源】` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 静态数据：jobs 数组（6个招聘职位）` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 静态数据：benefits 数组（4项员工福利）` -> 注释行，用于解释设计意图或使用说明。
- 18: `*/` -> 注释行，用于解释设计意图或使用说明。
- 19: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 20: `(空行)` -> 空行，用于提升代码结构可读性。
- 21: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 22: `import {` -> 导入依赖模块，供当前文件使用。
- 23: `Briefcase,` -> 执行当前语句，参与该文件整体逻辑。
- 24: `MapPin,` -> 执行当前语句，参与该文件整体逻辑。
- 25: `Clock,` -> 执行当前语句，参与该文件整体逻辑。
- 26: `Heart,` -> 执行当前语句，参与该文件整体逻辑。
- 27: `Sparkles,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `Users,` -> 执行当前语句，参与该文件整体逻辑。
- 29: `Rocket,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 31: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 32: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `const jobs = [` -> 声明变量或常量，保存运行时数据。
- 35: `{ title: '高级前端工程？', dept: '技术部', location: '北京', type: '全职', salary: '25K-45K' },` -> 执行当前语句，参与该文件整体逻辑。
- 36: `{ title: 'AI算法工程？', dept: '技术部', location: '北京/上海', type: '全职', salary: '30K-60K' },` -> 执行当前语句，参与该文件整体逻辑。
- 37: `{ title: '产品经理', dept: '产品？', location: '北京', type: '全职', salary: '20K-40K' },` -> 执行当前语句，参与该文件整体逻辑。
- 38: `{ title: 'UI/UX设计？', dept: '设计？', location: '北京', type: '全职', salary: '18K-35K' },` -> 执行当前语句，参与该文件整体逻辑。
- 39: `{ title: '旅游产品运营', dept: '运营？', location: '北京', type: '全职', salary: '15K-25K' },` -> 执行当前语句，参与该文件整体逻辑。
- 40: `{ title: '内容编辑', dept: '内容？', location: '远程', type: '兼职', salary: '8K-15K' },` -> 执行当前语句，参与该文件整体逻辑。
- 41: `]` -> 执行当前语句，参与该文件整体逻辑。
- 42: `(空行)` -> 空行，用于提升代码结构可读性。
- 43: `const benefits = [` -> 声明变量或常量，保存运行时数据。
- 44: `{ icon: Heart, title: '六险一金', desc: '全额缴纳，安心保障' },` -> 执行当前语句，参与该文件整体逻辑。
- 45: `{ icon: Sparkles, title: '弹性工作', desc: '灵活上下班，拒绝996' },` -> 执行当前语句，参与该文件整体逻辑。
- 46: `{ icon: Rocket, title: '成长空间', desc: '扁平管理，快速晋升' },` -> 执行当前语句，参与该文件整体逻辑。
- 47: `{ icon: Users, title: '免费旅游', desc: '每年旅游基金，说走就走' },` -> 执行当前语句，参与该文件整体逻辑。
- 48: `]` -> 执行当前语句，参与该文件整体逻辑。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `export default function CareersPage() {` -> 导出当前声明，供其他模块复用。
- 51: `return (` -> 返回结果或提前结束当前流程。
- 52: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 54: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `<div className="bg-gradient-to-r from-violet-600 to-purple-500 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 56: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 57: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 58: `<Briefcase className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `<span className="text-xl font-bold">加入我们</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 60: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 61: `<h1 className="text-4xl md:text-5xl font-bold mb-4">热招职位</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 62: `<p className="text-white/90 text-lg max-w-2xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 63: `加入我们，一起用AI改变旅行方式，创造更有趣的旅行体?` -> 执行当前语句，参与该文件整体逻辑。
- 64: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 65: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `<div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `{benefits.map((benefit) => {` -> 执行当前语句，参与该文件整体逻辑。
- 72: `const Icon = benefit.icon` -> 声明变量或常量，保存运行时数据。
- 73: `return (` -> 返回结果或提前结束当前流程。
- 74: `<div key={benefit.title} className="bg-white rounded-xl border border-slate-200 p-5 text-center hover:shadow-md transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `<Icon className="h-8 w-8 text-violet-600 mx-auto mb-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 76: `<h3 className="font-bold text-slate-900 mb-1">{benefit.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `<p className="text-sm text-slate-500">{benefit.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `)` -> 结束当前语句或代码块。
- 80: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 81: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `<h2 className="text-xl font-bold text-slate-900 mb-6">所有职</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 85: `<div className="space-y-4 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `{jobs.map((job, index) => (` -> 执行当前语句，参与该文件整体逻辑。
- 87: `<div key={index} className="bg-white rounded-2xl border border-slate-200 p-6 hover:shadow-lg hover:border-violet-300 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 88: `<div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 89: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 90: `<h3 className="text-lg font-bold text-slate-900 mb-2">{job.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 91: `<div className="flex flex-wrap items-center gap-3 text-sm text-slate-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 92: `<span className="px-2 py-0.5 bg-violet-100 text-violet-700 rounded">{job.dept}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 93: `<span className="flex items-center gap-1"><MapPin className="h-4 w-4" />{job.location}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 94: `<span className="flex items-center gap-1"><Clock className="h-4 w-4" />{job.type}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 95: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 96: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 97: `<div className="flex items-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 98: `<span className="text-lg font-bold text-violet-600">{job.salary}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 99: `<button className="px-5 py-2 bg-violet-600 text-white rounded-lg font-medium hover:bg-violet-700 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `申请` -> 执行当前语句，参与该文件整体逻辑。
- 101: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 104: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 105: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 106: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `(空行)` -> 空行，用于提升代码结构可读性。
- 109: `<div className="bg-gradient-to-r from-violet-50 to-purple-50 rounded-3xl p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 110: `<h2 className="text-2xl font-bold text-slate-900 mb-4">没有找到合适的职位</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `<p className="text-slate-500 mb-6">也可以发送简历至 hr@travelai.com，我们会尽快与您联系</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 112: `<Link href="/contact" className="inline-flex items-center gap-2 px-6 py-3 bg-violet-600 text-white rounded-xl font-medium hover:bg-violet-700 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 113: `联系我们` -> 执行当前语句，参与该文件整体逻辑。
- 114: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 115: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 116: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `)` -> 结束当前语句或代码块。
- 121: `}` -> 结束当前语句或代码块。
