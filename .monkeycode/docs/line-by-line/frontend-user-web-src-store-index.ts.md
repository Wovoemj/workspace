# `frontend/user-web/src/store/index.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* ============================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* Zustand 状态管理 - 全局状态存储` -> 注释行，用于解释设计意图或使用说明。
- 4: `* ============================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【状态管理说明】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 使用 Zustand + persist 中间件实现全局状态管理` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 用户状态持久化到 localStorage` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 购物车状态仅在内存中` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - UI状态包括主题、侧边栏、通知等` -> 注释行，用于解释设计意图或使用说明。
- 11: `*/` -> 注释行，用于解释设计意图或使用说明。
- 12: `(空行)` -> 空行，用于提升代码结构可读性。
- 13: `import { create } from 'zustand'` -> 导入依赖模块，供当前文件使用。
- 14: `import { persist } from 'zustand/middleware'` -> 导入依赖模块，供当前文件使用。
- 15: `import { User, UserPreferences } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 16: `(空行)` -> 空行，用于提升代码结构可读性。
- 17: `/** ============================================` -> 注释行，用于解释设计意图或使用说明。
- 18: `* 用户状态管理` -> 注释行，用于解释设计意图或使用说明。
- 19: `* ============================================ */` -> 注释行，用于解释设计意图或使用说明。
- 20: `interface UserState {` -> 定义类型或类结构，约束数据与行为。
- 21: `// 用户信息` -> 注释行，用于解释设计意图或使用说明。
- 22: `user: User | null` -> 执行当前语句，参与该文件整体逻辑。
- 23: `// 用户偏好设置` -> 注释行，用于解释设计意图或使用说明。
- 24: `preferences: UserPreferences | null` -> 执行当前语句，参与该文件整体逻辑。
- 25: `// 是否已认证` -> 注释行，用于解释设计意图或使用说明。
- 26: `isAuthenticated: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 27: `// 是否正在加载` -> 注释行，用于解释设计意图或使用说明。
- 28: `isLoading: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 29: `// 登录方法` -> 注释行，用于解释设计意图或使用说明。
- 30: `login: (user: User) => void` -> 执行当前语句，参与该文件整体逻辑。
- 31: `// 登出方法` -> 注释行，用于解释设计意图或使用说明。
- 32: `logout: () => void` -> 执行当前语句，参与该文件整体逻辑。
- 33: `// 更新用户资料` -> 注释行，用于解释设计意图或使用说明。
- 34: `updateProfile: (profile: Partial<User>) => void` -> 执行当前语句，参与该文件整体逻辑。
- 35: `// 更新偏好设置` -> 注释行，用于解释设计意图或使用说明。
- 36: `updatePreferences: (preferences: Partial<UserPreferences>) => void` -> 执行当前语句，参与该文件整体逻辑。
- 37: `// 设置加载状态` -> 注释行，用于解释设计意图或使用说明。
- 38: `setLoading: (loading: boolean) => void` -> 执行当前语句，参与该文件整体逻辑。
- 39: `}` -> 结束当前语句或代码块。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `/**` -> 注释行，用于解释设计意图或使用说明。
- 42: `* 用户状态Store` -> 注释行，用于解释设计意图或使用说明。
- 43: `*` -> 注释行，用于解释设计意图或使用说明。
- 44: `* 【持久化配置】` -> 注释行，用于解释设计意图或使用说明。
- 45: `* - 存储到 localStorage，key 为 'user-storage'` -> 注释行，用于解释设计意图或使用说明。
- 46: `* - 只持久化 user、preferences、isAuthenticated` -> 注释行，用于解释设计意图或使用说明。
- 47: `* - 页面重载时检查 Token 是否存在` -> 注释行，用于解释设计意图或使用说明。
- 48: `* - 无 Token 则自动登出` -> 注释行，用于解释设计意图或使用说明。
- 49: `*/` -> 注释行，用于解释设计意图或使用说明。
- 50: `export const useUserStore = create<UserState>()(` -> 导出当前声明，供其他模块复用。
- 51: `persist(` -> 执行当前语句，参与该文件整体逻辑。
- 52: `(set, get) => ({` -> 执行当前语句，参与该文件整体逻辑。
- 53: `user: null,` -> 执行当前语句，参与该文件整体逻辑。
- 54: `preferences: null,` -> 执行当前语句，参与该文件整体逻辑。
- 55: `isAuthenticated: false,` -> 执行当前语句，参与该文件整体逻辑。
- 56: `isLoading: false,` -> 执行当前语句，参与该文件整体逻辑。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `/**` -> 注释行，用于解释设计意图或使用说明。
- 59: `* 用户登录` -> 注释行，用于解释设计意图或使用说明。
- 60: `* @param user - 用户信息对象` -> 注释行，用于解释设计意图或使用说明。
- 61: `* @description 登录后设置用户信息，清除加载状态` -> 注释行，用于解释设计意图或使用说明。
- 62: `*/` -> 注释行，用于解释设计意图或使用说明。
- 63: `login: (user: User) => {` -> 执行当前语句，参与该文件整体逻辑。
- 64: `set({ user, isAuthenticated: true, isLoading: false })` -> 执行当前语句，参与该文件整体逻辑。
- 65: `},` -> 执行当前语句，参与该文件整体逻辑。
- 66: `(空行)` -> 空行，用于提升代码结构可读性。
- 67: `/**` -> 注释行，用于解释设计意图或使用说明。
- 68: `* 用户登出` -> 注释行，用于解释设计意图或使用说明。
- 69: `* @description 清除用户信息，同时清除 localStorage 中的 Token` -> 注释行，用于解释设计意图或使用说明。
- 70: `*/` -> 注释行，用于解释设计意图或使用说明。
- 71: `logout: () => {` -> 执行当前语句，参与该文件整体逻辑。
- 72: `set({ user: null, preferences: null, isAuthenticated: false, isLoading: false })` -> 执行当前语句，参与该文件整体逻辑。
- 73: `localStorage.removeItem('auth_token')` -> 执行当前语句，参与该文件整体逻辑。
- 74: `},` -> 执行当前语句，参与该文件整体逻辑。
- 75: `(空行)` -> 空行，用于提升代码结构可读性。
- 76: `/**` -> 注释行，用于解释设计意图或使用说明。
- 77: `* 更新用户资料` -> 注释行，用于解释设计意图或使用说明。
- 78: `* @param profile - 需要更新的字段` -> 注释行，用于解释设计意图或使用说明。
- 79: `* @description 合并更新用户信息` -> 注释行，用于解释设计意图或使用说明。
- 80: `*/` -> 注释行，用于解释设计意图或使用说明。
- 81: `updateProfile: (profile: Partial<User>) => {` -> 执行当前语句，参与该文件整体逻辑。
- 82: `const currentUser = get().user` -> 声明变量或常量，保存运行时数据。
- 83: `if (currentUser) {` -> 条件判断分支，根据场景执行不同逻辑。
- 84: `set({ user: { ...currentUser, ...profile } })` -> 执行当前语句，参与该文件整体逻辑。
- 85: `}` -> 结束当前语句或代码块。
- 86: `},` -> 执行当前语句，参与该文件整体逻辑。
- 87: `(空行)` -> 空行，用于提升代码结构可读性。
- 88: `/**` -> 注释行，用于解释设计意图或使用说明。
- 89: `* 更新用户偏好` -> 注释行，用于解释设计意图或使用说明。
- 90: `* @param preferences - 需要更新的偏好设置` -> 注释行，用于解释设计意图或使用说明。
- 91: `*/` -> 注释行，用于解释设计意图或使用说明。
- 92: `updatePreferences: (preferences: Partial<UserPreferences>) => {` -> 执行当前语句，参与该文件整体逻辑。
- 93: `const currentPreferences = get().preferences` -> 声明变量或常量，保存运行时数据。
- 94: `if (currentPreferences) {` -> 条件判断分支，根据场景执行不同逻辑。
- 95: `set({ preferences: { ...currentPreferences, ...preferences } })` -> 执行当前语句，参与该文件整体逻辑。
- 96: `}` -> 结束当前语句或代码块。
- 97: `},` -> 执行当前语句，参与该文件整体逻辑。
- 98: `(空行)` -> 空行，用于提升代码结构可读性。
- 99: `/**` -> 注释行，用于解释设计意图或使用说明。
- 100: `* 设置加载状态` -> 注释行，用于解释设计意图或使用说明。
- 101: `* @param loading - 是否加载中` -> 注释行，用于解释设计意图或使用说明。
- 102: `*/` -> 注释行，用于解释设计意图或使用说明。
- 103: `setLoading: (loading: boolean) => {` -> 执行当前语句，参与该文件整体逻辑。
- 104: `set({ isLoading: loading })` -> 执行当前语句，参与该文件整体逻辑。
- 105: `},` -> 执行当前语句，参与该文件整体逻辑。
- 106: `}),` -> 执行当前语句，参与该文件整体逻辑。
- 107: `{` -> 执行当前语句，参与该文件整体逻辑。
- 108: `name: 'user-storage',  // localStorage 的键名` -> 执行当前语句，参与该文件整体逻辑。
- 109: `// 只持久化这些字段` -> 注释行，用于解释设计意图或使用说明。
- 110: `partialize: (state) => ({` -> 执行当前语句，参与该文件整体逻辑。
- 111: `user: state.user,` -> 执行当前语句，参与该文件整体逻辑。
- 112: `preferences: state.preferences,` -> 执行当前语句，参与该文件整体逻辑。
- 113: `isAuthenticated: state.isAuthenticated,` -> 执行当前语句，参与该文件整体逻辑。
- 114: `}),` -> 执行当前语句，参与该文件整体逻辑。
- 115: `// 重hydration时的回调：检查Token是否有效` -> 注释行，用于解释设计意图或使用说明。
- 116: `onRehydrateStorage: () => (state) => {` -> 执行当前语句，参与该文件整体逻辑。
- 117: `try {` -> 异常处理逻辑，提升程序健壮性。
- 118: `if (typeof window === 'undefined') return` -> 条件判断分支，根据场景执行不同逻辑。
- 119: `const authToken = window.localStorage.getItem('auth_token')` -> 声明变量或常量，保存运行时数据。
- 120: `const adminToken = window.localStorage.getItem('admin_token')` -> 声明变量或常量，保存运行时数据。
- 121: `// 如果两个Token都没有，自动登出` -> 注释行，用于解释设计意图或使用说明。
- 122: `if (!authToken && !adminToken) state?.logout?.()` -> 条件判断分支，根据场景执行不同逻辑。
- 123: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 124: `// 忽略错误` -> 注释行，用于解释设计意图或使用说明。
- 125: `}` -> 结束当前语句或代码块。
- 126: `},` -> 执行当前语句，参与该文件整体逻辑。
- 127: `}` -> 结束当前语句或代码块。
- 128: `)` -> 结束当前语句或代码块。
- 129: `)` -> 结束当前语句或代码块。
- 130: `(空行)` -> 空行，用于提升代码结构可读性。
- 131: `/** ============================================` -> 注释行，用于解释设计意图或使用说明。
- 132: `* 购物车状态管理` -> 注释行，用于解释设计意图或使用说明。
- 133: `* ============================================ */` -> 注释行，用于解释设计意图或使用说明。
- 134: `interface CartItem {` -> 定义类型或类结构，约束数据与行为。
- 135: `product_id: string       // 商品ID` -> 执行当前语句，参与该文件整体逻辑。
- 136: `product_name: string     // 商品名称` -> 执行当前语句，参与该文件整体逻辑。
- 137: `product_type: 'flight' | 'hotel' | 'ticket' | 'experience'  // 商品类型` -> 执行当前语句，参与该文件整体逻辑。
- 138: `quantity: number         // 数量` -> 执行当前语句，参与该文件整体逻辑。
- 139: `unit_price: number       // 单价` -> 执行当前语句，参与该文件整体逻辑。
- 140: `total_price: number      // 小计` -> 执行当前语句，参与该文件整体逻辑。
- 141: `}` -> 结束当前语句或代码块。
- 142: `(空行)` -> 空行，用于提升代码结构可读性。
- 143: `interface CartState {` -> 定义类型或类结构，约束数据与行为。
- 144: `items: CartItem[]        // 购物车商品列表` -> 执行当前语句，参与该文件整体逻辑。
- 145: `addItem: (item: CartItem) => void        // 添加商品` -> 执行当前语句，参与该文件整体逻辑。
- 146: `removeItem: (productId: string) => void  // 移除商品` -> 执行当前语句，参与该文件整体逻辑。
- 147: `updateQuantity: (productId: string, quantity: number) => void  // 更新数量` -> 执行当前语句，参与该文件整体逻辑。
- 148: `clearCart: () => void                   // 清空购物车` -> 执行当前语句，参与该文件整体逻辑。
- 149: `getTotal: () => number                  // 计算总价` -> 执行当前语句，参与该文件整体逻辑。
- 150: `}` -> 结束当前语句或代码块。
- 151: `(空行)` -> 空行，用于提升代码结构可读性。
- 152: `/**` -> 注释行，用于解释设计意图或使用说明。
- 153: `* 购物车Store` -> 注释行，用于解释设计意图或使用说明。
- 154: `*` -> 注释行，用于解释设计意图或使用说明。
- 155: `* 【功能说明】` -> 注释行，用于解释设计意图或使用说明。
- 156: `* - 支持添加/删除/更新商品数量` -> 注释行，用于解释设计意图或使用说明。
- 157: `* - 自动计算小计和总价` -> 注释行，用于解释设计意图或使用说明。
- 158: `* - 仅在内存中，不持久化` -> 注释行，用于解释设计意图或使用说明。
- 159: `*/` -> 注释行，用于解释设计意图或使用说明。
- 160: `export const useCartStore = create<CartState>((set, get) => ({` -> 导出当前声明，供其他模块复用。
- 161: `items: [],` -> 执行当前语句，参与该文件整体逻辑。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `/**` -> 注释行，用于解释设计意图或使用说明。
- 164: `* 添加商品到购物车` -> 注释行，用于解释设计意图或使用说明。
- 165: `* @param item - 商品信息` -> 注释行，用于解释设计意图或使用说明。
- 166: `* @description 如果商品已存在则增加数量，否则添加新商品` -> 注释行，用于解释设计意图或使用说明。
- 167: `*/` -> 注释行，用于解释设计意图或使用说明。
- 168: `addItem: (item) => {` -> 执行当前语句，参与该文件整体逻辑。
- 169: `const cart = get()` -> 声明变量或常量，保存运行时数据。
- 170: `const existingItem = cart.items.find(i => i.product_id === item.product_id)` -> 声明变量或常量，保存运行时数据。
- 171: `(空行)` -> 空行，用于提升代码结构可读性。
- 172: `if (existingItem) {` -> 条件判断分支，根据场景执行不同逻辑。
- 173: `// 更新已有商品数量` -> 注释行，用于解释设计意图或使用说明。
- 174: `const updatedItems = cart.items.map(i =>` -> 声明变量或常量，保存运行时数据。
- 175: `i.product_id === item.product_id` -> 执行当前语句，参与该文件整体逻辑。
- 176: `? { ...i, quantity: i.quantity + item.quantity, total_price: i.unit_price * (i.quantity + item.quantity) }` -> 执行当前语句，参与该文件整体逻辑。
- 177: `: i` -> 执行当前语句，参与该文件整体逻辑。
- 178: `)` -> 结束当前语句或代码块。
- 179: `set({ items: updatedItems })` -> 执行当前语句，参与该文件整体逻辑。
- 180: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 181: `// 添加新商品` -> 注释行，用于解释设计意图或使用说明。
- 182: `const newItem = {` -> 声明变量或常量，保存运行时数据。
- 183: `...item,` -> 执行当前语句，参与该文件整体逻辑。
- 184: `total_price: item.unit_price * item.quantity,` -> 执行当前语句，参与该文件整体逻辑。
- 185: `}` -> 结束当前语句或代码块。
- 186: `set({ items: [...cart.items, newItem] })` -> 执行当前语句，参与该文件整体逻辑。
- 187: `}` -> 结束当前语句或代码块。
- 188: `},` -> 执行当前语句，参与该文件整体逻辑。
- 189: `(空行)` -> 空行，用于提升代码结构可读性。
- 190: `/**` -> 注释行，用于解释设计意图或使用说明。
- 191: `* 移除商品` -> 注释行，用于解释设计意图或使用说明。
- 192: `* @param productId - 商品ID` -> 注释行，用于解释设计意图或使用说明。
- 193: `*/` -> 注释行，用于解释设计意图或使用说明。
- 194: `removeItem: (productId) => {` -> 执行当前语句，参与该文件整体逻辑。
- 195: `const cart = get()` -> 声明变量或常量，保存运行时数据。
- 196: `const updatedItems = cart.items.filter(i => i.product_id !== productId)` -> 声明变量或常量，保存运行时数据。
- 197: `set({ items: updatedItems })` -> 执行当前语句，参与该文件整体逻辑。
- 198: `},` -> 执行当前语句，参与该文件整体逻辑。
- 199: `(空行)` -> 空行，用于提升代码结构可读性。
- 200: `/**` -> 注释行，用于解释设计意图或使用说明。
- 201: `* 更新商品数量` -> 注释行，用于解释设计意图或使用说明。
- 202: `* @param productId - 商品ID` -> 注释行，用于解释设计意图或使用说明。
- 203: `* @param quantity - 新数量` -> 注释行，用于解释设计意图或使用说明。
- 204: `*/` -> 注释行，用于解释设计意图或使用说明。
- 205: `updateQuantity: (productId, quantity) => {` -> 执行当前语句，参与该文件整体逻辑。
- 206: `const cart = get()` -> 声明变量或常量，保存运行时数据。
- 207: `const updatedItems = cart.items.map(i =>` -> 声明变量或常量，保存运行时数据。
- 208: `i.product_id === productId` -> 执行当前语句，参与该文件整体逻辑。
- 209: `? { ...i, quantity, total_price: i.unit_price * quantity }` -> 执行当前语句，参与该文件整体逻辑。
- 210: `: i` -> 执行当前语句，参与该文件整体逻辑。
- 211: `)` -> 结束当前语句或代码块。
- 212: `set({ items: updatedItems })` -> 执行当前语句，参与该文件整体逻辑。
- 213: `},` -> 执行当前语句，参与该文件整体逻辑。
- 214: `(空行)` -> 空行，用于提升代码结构可读性。
- 215: `/** 清空购物车 */` -> 注释行，用于解释设计意图或使用说明。
- 216: `clearCart: () => {` -> 执行当前语句，参与该文件整体逻辑。
- 217: `set({ items: [] })` -> 执行当前语句，参与该文件整体逻辑。
- 218: `},` -> 执行当前语句，参与该文件整体逻辑。
- 219: `(空行)` -> 空行，用于提升代码结构可读性。
- 220: `/**` -> 注释行，用于解释设计意图或使用说明。
- 221: `* 计算购物车总价` -> 注释行，用于解释设计意图或使用说明。
- 222: `* @returns 总价` -> 注释行，用于解释设计意图或使用说明。
- 223: `*/` -> 注释行，用于解释设计意图或使用说明。
- 224: `getTotal: () => {` -> 执行当前语句，参与该文件整体逻辑。
- 225: `const cart = get()` -> 声明变量或常量，保存运行时数据。
- 226: `return cart.items.reduce((total, item) => total + item.total_price, 0)` -> 返回结果或提前结束当前流程。
- 227: `},` -> 执行当前语句，参与该文件整体逻辑。
- 228: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 229: `(空行)` -> 空行，用于提升代码结构可读性。
- 230: `/** ============================================` -> 注释行，用于解释设计意图或使用说明。
- 231: `* UI状态管理` -> 注释行，用于解释设计意图或使用说明。
- 232: `* ============================================ */` -> 注释行，用于解释设计意图或使用说明。
- 233: `interface Notification {` -> 定义类型或类结构，约束数据与行为。
- 234: `id: string                                    // 通知ID` -> 执行当前语句，参与该文件整体逻辑。
- 235: `type: 'success' | 'error' | 'warning' | 'info' // 通知类型` -> 执行当前语句，参与该文件整体逻辑。
- 236: `message: string                               // 通知内容` -> 执行当前语句，参与该文件整体逻辑。
- 237: `timestamp: string                             // 时间戳` -> 执行当前语句，参与该文件整体逻辑。
- 238: `}` -> 结束当前语句或代码块。
- 239: `(空行)` -> 空行，用于提升代码结构可读性。
- 240: `interface UIState {` -> 定义类型或类结构，约束数据与行为。
- 241: `sidebarOpen: boolean                           // 侧边栏是否打开` -> 执行当前语句，参与该文件整体逻辑。
- 242: `theme: 'light' | 'dark'                       // 主题模式` -> 执行当前语句，参与该文件整体逻辑。
- 243: `notifications: Notification[]                   // 通知列表` -> 执行当前语句，参与该文件整体逻辑。
- 244: `toggleSidebar: () => void                     // 切换侧边栏` -> 执行当前语句，参与该文件整体逻辑。
- 245: `setSidebar: (open: boolean) => void           // 设置侧边栏` -> 执行当前语句，参与该文件整体逻辑。
- 246: `addNotification: (notification: Omit<Notification, 'id' | 'timestamp'>) => void  // 添加通知` -> 执行当前语句，参与该文件整体逻辑。
- 247: `removeNotification: (id: string) => void      // 移除通知` -> 执行当前语句，参与该文件整体逻辑。
- 248: `clearNotifications: () => void                // 清空通知` -> 执行当前语句，参与该文件整体逻辑。
- 249: `setTheme: (theme: 'light' | 'dark') => void  // 设置主题` -> 执行当前语句，参与该文件整体逻辑。
- 250: `}` -> 结束当前语句或代码块。
- 251: `(空行)` -> 空行，用于提升代码结构可读性。
- 252: `/**` -> 注释行，用于解释设计意图或使用说明。
- 253: `* UI状态Store` -> 注释行，用于解释设计意图或使用说明。
- 254: `*` -> 注释行，用于解释设计意图或使用说明。
- 255: `* 【功能说明】` -> 注释行，用于解释设计意图或使用说明。
- 256: `* - 管理侧边栏开关状态` -> 注释行，用于解释设计意图或使用说明。
- 257: `* - 管理明暗主题切换` -> 注释行，用于解释设计意图或使用说明。
- 258: `* - 管理全局通知列表（最多10条）` -> 注释行，用于解释设计意图或使用说明。
- 259: `*/` -> 注释行，用于解释设计意图或使用说明。
- 260: `export const useUIStore = create<UIState>((set, get) => ({` -> 导出当前声明，供其他模块复用。
- 261: `sidebarOpen: false,` -> 执行当前语句，参与该文件整体逻辑。
- 262: `theme: 'light',` -> 执行当前语句，参与该文件整体逻辑。
- 263: `notifications: [],` -> 执行当前语句，参与该文件整体逻辑。
- 264: `(空行)` -> 空行，用于提升代码结构可读性。
- 265: `/** 切换侧边栏状态 */` -> 注释行，用于解释设计意图或使用说明。
- 266: `toggleSidebar: () => {` -> 执行当前语句，参与该文件整体逻辑。
- 267: `set({ sidebarOpen: !get().sidebarOpen })` -> 执行当前语句，参与该文件整体逻辑。
- 268: `},` -> 执行当前语句，参与该文件整体逻辑。
- 269: `(空行)` -> 空行，用于提升代码结构可读性。
- 270: `/** 设置侧边栏 */` -> 注释行，用于解释设计意图或使用说明。
- 271: `setSidebar: (open: boolean) => {` -> 执行当前语句，参与该文件整体逻辑。
- 272: `set({ sidebarOpen: open })` -> 执行当前语句，参与该文件整体逻辑。
- 273: `},` -> 执行当前语句，参与该文件整体逻辑。
- 274: `(空行)` -> 空行，用于提升代码结构可读性。
- 275: `/**` -> 注释行，用于解释设计意图或使用说明。
- 276: `* 添加通知` -> 注释行，用于解释设计意图或使用说明。
- 277: `* @param notification - 通知内容（不含ID和时间戳）` -> 注释行，用于解释设计意图或使用说明。
- 278: `* @description 自动添加ID和时间戳，最多保留10条` -> 注释行，用于解释设计意图或使用说明。
- 279: `*/` -> 注释行，用于解释设计意图或使用说明。
- 280: `addNotification: (notification) => {` -> 执行当前语句，参与该文件整体逻辑。
- 281: `const newNotification = {` -> 声明变量或常量，保存运行时数据。
- 282: `...notification,` -> 执行当前语句，参与该文件整体逻辑。
- 283: `id: Date.now().toString(),` -> 执行当前语句，参与该文件整体逻辑。
- 284: `timestamp: new Date().toISOString(),` -> 执行当前语句，参与该文件整体逻辑。
- 285: `}` -> 结束当前语句或代码块。
- 286: `set((state) => ({` -> 执行当前语句，参与该文件整体逻辑。
- 287: `notifications: [newNotification, ...state.notifications].slice(0, 10),` -> 执行当前语句，参与该文件整体逻辑。
- 288: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 289: `},` -> 执行当前语句，参与该文件整体逻辑。
- 290: `(空行)` -> 空行，用于提升代码结构可读性。
- 291: `/** 移除指定通知 */` -> 注释行，用于解释设计意图或使用说明。
- 292: `removeNotification: (id) => {` -> 执行当前语句，参与该文件整体逻辑。
- 293: `set((state) => ({` -> 执行当前语句，参与该文件整体逻辑。
- 294: `notifications: state.notifications.filter(n => n.id !== id),` -> 执行当前语句，参与该文件整体逻辑。
- 295: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 296: `},` -> 执行当前语句，参与该文件整体逻辑。
- 297: `(空行)` -> 空行，用于提升代码结构可读性。
- 298: `/** 清空所有通知 */` -> 注释行，用于解释设计意图或使用说明。
- 299: `clearNotifications: () => {` -> 执行当前语句，参与该文件整体逻辑。
- 300: `set({ notifications: [] })` -> 执行当前语句，参与该文件整体逻辑。
- 301: `},` -> 执行当前语句，参与该文件整体逻辑。
- 302: `(空行)` -> 空行，用于提升代码结构可读性。
- 303: `/**` -> 注释行，用于解释设计意图或使用说明。
- 304: `* 设置主题` -> 注释行，用于解释设计意图或使用说明。
- 305: `* @param theme - 主题模式` -> 注释行，用于解释设计意图或使用说明。
- 306: `* @description 同时更新DOM和localStorage` -> 注释行，用于解释设计意图或使用说明。
- 307: `*/` -> 注释行，用于解释设计意图或使用说明。
- 308: `setTheme: (theme: 'light' | 'dark') => {` -> 执行当前语句，参与该文件整体逻辑。
- 309: `set({ theme })` -> 执行当前语句，参与该文件整体逻辑。
- 310: `if (typeof window !== 'undefined') {` -> 条件判断分支，根据场景执行不同逻辑。
- 311: `// 切换HTML元素的dark类` -> 注释行，用于解释设计意图或使用说明。
- 312: `document.documentElement.classList.toggle('dark', theme === 'dark')` -> 执行当前语句，参与该文件整体逻辑。
- 313: `// 持久化到localStorage` -> 注释行，用于解释设计意图或使用说明。
- 314: `localStorage.setItem('theme', theme)` -> 执行当前语句，参与该文件整体逻辑。
- 315: `}` -> 结束当前语句或代码块。
- 316: `},` -> 执行当前语句，参与该文件整体逻辑。
- 317: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 318: `(空行)` -> 空行，用于提升代码结构可读性。
- 319: `/** ============================================` -> 注释行，用于解释设计意图或使用说明。
- 320: `* 初始化主题` -> 注释行，用于解释设计意图或使用说明。
- 321: `* ============================================ */` -> 注释行，用于解释设计意图或使用说明。
- 322: `// 页面加载时恢复保存的主题设置` -> 注释行，用于解释设计意图或使用说明。
- 323: `if (typeof window !== 'undefined') {` -> 条件判断分支，根据场景执行不同逻辑。
- 324: `const savedTheme = localStorage.getItem('theme') as 'light' | 'dark' | null` -> 声明变量或常量，保存运行时数据。
- 325: `if (savedTheme) {` -> 条件判断分支，根据场景执行不同逻辑。
- 326: `useUIStore.getState().setTheme(savedTheme)` -> 执行当前语句，参与该文件整体逻辑。
- 327: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 328: `useUIStore.getState().setTheme('light')` -> 执行当前语句，参与该文件整体逻辑。
- 329: `}` -> 结束当前语句或代码块。
- 330: `}` -> 结束当前语句或代码块。
