# `frontend/user-web/src/app/services/insurance/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 旅游保险模块 - 保险产品展示` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 保险方案展示（静态数据）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 三种方案对比：基础保障/尊享保障/至尊保障` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 保障内容列表（意外伤害/医疗/航班延误/行李丢失/紧急救援）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 价格展示` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 热门推荐标签` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 咨询客服入口` -> 注释行，用于解释设计意图或使用说明。
- 13: `*` -> 注释行，用于解释设计意图或使用说明。
- 14: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 16: `*` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 【数据来源】` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - 静态数据：plans 数组（3种保险方案）` -> 注释行，用于解释设计意图或使用说明。
- 19: `*/` -> 注释行，用于解释设计意图或使用说明。
- 20: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 23: `import {` -> 导入依赖模块，供当前文件使用。
- 24: `Shield,` -> 执行当前语句，参与该文件整体逻辑。
- 25: `Heart,` -> 执行当前语句，参与该文件整体逻辑。
- 26: `Clock,` -> 执行当前语句，参与该文件整体逻辑。
- 27: `DollarSign,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `CheckCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 29: `Phone,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 31: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 32: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `const plans = [` -> 声明变量或常量，保存运行时数据。
- 35: `{ name: '基础保障', price: '¥20', coverage: ['意外伤害 10万', '意外医疗 1万', '航班延误 200元'], popular: false },` -> 执行当前语句，参与该文件整体逻辑。
- 36: `{ name: '尊享保障', price: '¥50', coverage: ['意外伤害 30万', '意外医疗 3万', '航班延误 500元', '行李丢失 500元'], popular: true },` -> 执行当前语句，参与该文件整体逻辑。
- 37: `{ name: '至尊保障', price: '¥100', coverage: ['意外伤害 50万', '意外医疗 5万', '航班延误 1000元', '行李丢失 2000元', '紧急救援'], popular: false },` -> 执行当前语句，参与该文件整体逻辑。
- 38: `]` -> 执行当前语句，参与该文件整体逻辑。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `export default function InsurancePage() {` -> 导出当前声明，供其他模块复用。
- 41: `return (` -> 返回结果或提前结束当前流程。
- 42: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 43: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 44: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 45: `<div className="bg-gradient-to-r from-blue-600 to-indigo-600 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 46: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 47: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 48: `<Shield className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 49: `<span className="text-xl font-bold">增值服</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 50: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 51: `<h1 className="text-4xl md:text-5xl font-bold mb-4">旅游保险</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 52: `<p className="text-white/90 text-lg max-w-2xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `安心出行，让每一次旅行都有保障。我们与多家知名保险公司合作，提供多种保障方?` -> 执行当前语句，参与该文件整体逻辑。
- 54: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 56: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `(空行)` -> 空行，用于提升代码结构可读性。
- 60: `<div className="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 61: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 62: `{ icon: Heart, title: '全程保障', desc: '从出发到返回，全程守护' },` -> 执行当前语句，参与该文件整体逻辑。
- 63: `{ icon: Clock, title: '快速理赔', desc: '线上报案，快速审核' },` -> 执行当前语句，参与该文件整体逻辑。
- 64: `{ icon: DollarSign, title: '高性价比', desc: '最低20元起，保障一整程' },` -> 执行当前语句，参与该文件整体逻辑。
- 65: `].map((item) => {` -> 执行当前语句，参与该文件整体逻辑。
- 66: `const Icon = item.icon` -> 声明变量或常量，保存运行时数据。
- 67: `return (` -> 返回结果或提前结束当前流程。
- 68: `<div key={item.title} className="bg-white rounded-xl border border-slate-200 p-5 text-center hover:shadow-md transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 69: `<Icon className="h-8 w-8 text-blue-600 mx-auto mb-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 70: `<h3 className="font-bold text-slate-900 mb-1">{item.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `<p className="text-sm text-slate-500">{item.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 72: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 73: `)` -> 结束当前语句或代码块。
- 74: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 75: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 76: `(空行)` -> 空行，用于提升代码结构可读性。
- 77: `(空行)` -> 空行，用于提升代码结构可读性。
- 78: `<h2 className="text-2xl font-bold text-slate-900 mb-8 text-center">选择您的保障方案</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 80: `{plans.map((plan) => (` -> 执行当前语句，参与该文件整体逻辑。
- 81: `<div key={plan.name} className={\`bg-white rounded-2xl border-2 p-6 ${plan.popular ? 'border-blue-500 shadow-lg' : 'border-slate-200'} relative\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 82: `{plan.popular && (` -> 执行当前语句，参与该文件整体逻辑。
- 83: `<div className="absolute -top-3 left-1/2 -translate-x-1/2 px-4 py-1 bg-blue-600 text-white text-sm rounded-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 84: `最受欢?` -> 执行当前语句，参与该文件整体逻辑。
- 85: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 87: `<div className="text-center mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 88: `<h3 className="text-xl font-bold text-slate-900 mb-2">{plan.name}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 89: `<div className="text-3xl font-bold text-blue-600">{plan.price}<span className="text-base text-slate-500">/起</span></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 90: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 91: `<ul className="space-y-3 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 92: `{plan.coverage.map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 93: `<li key={item} className="flex items-center gap-2 text-slate-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 94: `<CheckCircle className="h-5 w-5 text-green-500 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 95: `{item}` -> 执行当前语句，参与该文件整体逻辑。
- 96: `</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 97: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 98: `</ul>` -> JSX/HTML 结构行，用于描述页面元素。
- 99: `<button className={\`w-full py-3 rounded-xl font-medium transition ${plan.popular ? 'bg-blue-600 text-white hover:bg-blue-700' : 'border border-slate-300 text-slate-700 hover:bg-slate-50'}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `立即购买` -> 执行当前语句，参与该文件整体逻辑。
- 101: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 104: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 105: `(空行)` -> 空行，用于提升代码结构可读性。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `<div className="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-3xl p-8 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 108: `<h2 className="text-2xl font-bold text-slate-900 mb-6 text-center">理赔流程</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 109: `<div className="grid grid-cols-1 md:grid-cols-4 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 110: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 111: `{ step: '01', title: '报案', desc: '发生事故后，联系客服或线上报案' },` -> 执行当前语句，参与该文件整体逻辑。
- 112: `{ step: '02', title: '提交材料', desc: '上传相关证明材料' },` -> 执行当前语句，参与该文件整体逻辑。
- 113: `{ step: '03', title: '审核', desc: '保险公司审核材料' },` -> 执行当前语句，参与该文件整体逻辑。
- 114: `{ step: '04', title: '理赔', desc: '审核通过后，快速打款' },` -> 执行当前语句，参与该文件整体逻辑。
- 115: `].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 116: `<div key={item.step} className="text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `<div className="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `{item.step}` -> 执行当前语句，参与该文件整体逻辑。
- 119: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `<h3 className="font-bold text-slate-900 mb-2">{item.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 121: `<p className="text-sm text-slate-500">{item.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 124: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 125: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 126: `(空行)` -> 空行，用于提升代码结构可读性。
- 127: `(空行)` -> 空行，用于提升代码结构可读性。
- 128: `<div className="bg-white rounded-2xl border border-slate-200 p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `<h2 className="text-2xl font-bold text-slate-900 mb-4">需要更多帮助？</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 130: `<p className="text-slate-500 mb-6">我们的保险顾问随时为您解</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `<div className="flex flex-wrap justify-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 132: `<a href="tel:400-888-9999" className="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-xl font-medium hover:bg-blue-700 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 133: `<Phone className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 134: `400-888-9999` -> 执行当前语句，参与该文件整体逻辑。
- 135: `</a>` -> JSX/HTML 结构行，用于描述页面元素。
- 136: `<Link href="/contact" className="inline-flex items-center gap-2 px-6 py-3 border border-slate-300 text-slate-700 rounded-xl font-medium hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 137: `在线咨询` -> 执行当前语句，参与该文件整体逻辑。
- 138: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 139: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 140: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 141: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 142: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 143: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 144: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 145: `)` -> 结束当前语句或代码块。
- 146: `}` -> 结束当前语句或代码块。
