# `frontend/admin-web/package.json` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `{ //` -> 执行当前语句，参与该文件整体逻辑。
- 2: `"name": "travel-assistant-admin-web",` -> 执行当前语句，参与该文件整体逻辑。
- 3: `"version": "1.0.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 4: `"private": true, //` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"scripts": {` -> 执行当前语句，参与该文件整体逻辑。
- 6: `"dev": "vite", //` -> 执行当前语句，参与该文件整体逻辑。
- 7: `"build": "tsc && vite build", //` -> 执行当前语句，参与该文件整体逻辑。
- 8: `"preview": "vite preview",` -> 执行当前语句，参与该文件整体逻辑。
- 9: `"lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0", //` -> 执行当前语句，参与该文件整体逻辑。
- 10: `"type-check": "tsc --noEmit"` -> 执行当前语句，参与该文件整体逻辑。
- 11: `}, //` -> 执行当前语句，参与该文件整体逻辑。
- 12: `"dependencies": { //` -> 执行当前语句，参与该文件整体逻辑。
- 13: `"react": "^18.2.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 14: `"react-dom": "^18.2.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 15: `"antd": "^5.11.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 16: `"@ant-design/icons": "^5.2.6",` -> 执行当前语句，参与该文件整体逻辑。
- 17: `"axios": "^1.5.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 18: `"zustand": "^4.4.1",` -> 执行当前语句，参与该文件整体逻辑。
- 19: `"react-router-dom": "^6.15.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 20: `"echarts": "^5.4.3", //` -> 执行当前语句，参与该文件整体逻辑。
- 21: `"echarts-for-react": "^3.0.2", //` -> 执行当前语句，参与该文件整体逻辑。
- 22: `"dayjs": "^1.11.9",` -> 执行当前语句，参与该文件整体逻辑。
- 23: `"lodash": "^4.17.21" //` -> 执行当前语句，参与该文件整体逻辑。
- 24: `},` -> 执行当前语句，参与该文件整体逻辑。
- 25: `"devDependencies": { //` -> 执行当前语句，参与该文件整体逻辑。
- 26: `"@types/react": "^18.2.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 27: `"@types/react-dom": "^18.2.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 28: `"@types/lodash": "^4.14.0",` -> 执行当前语句，参与该文件整体逻辑。
- 29: `"@typescript-eslint/eslint-plugin": "^6.0.0",` -> 执行当前语句，参与该文件整体逻辑。
- 30: `"@typescript-eslint/parser": "^6.0.0",` -> 执行当前语句，参与该文件整体逻辑。
- 31: `"@vitejs/plugin-react": "^4.0.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 32: `"typescript": "^5.2.2",` -> 执行当前语句，参与该文件整体逻辑。
- 33: `"vite": "^4.4.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 34: `"eslint": "^8.45.0", //` -> 执行当前语句，参与该文件整体逻辑。
- 35: `"eslint-plugin-react-hooks": "^4.6.0",` -> 执行当前语句，参与该文件整体逻辑。
- 36: `"eslint-plugin-react-refresh": "^0.4.0"` -> 执行当前语句，参与该文件整体逻辑。
- 37: `}` -> 结束当前语句或代码块。
- 38: `}` -> 结束当前语句或代码块。
