# `amap_service.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `#!/usr/bin/env python3` -> 注释行，用于解释设计意图或使用说明。
- 2: `# -*- coding: utf-8 -*-` -> 注释行，用于解释设计意图或使用说明。
- 3: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 4: `腾讯地图API数据获取与缓存服务` -> 执行当前语句，参与该文件整体逻辑。
- 5: `功能：` -> 执行当前语句，参与该文件整体逻辑。
- 6: `1. 通过腾讯地图POI搜索获取景点/酒店/餐厅等数据` -> 执行当前语句，参与该文件整体逻辑。
- 7: `2. 数据自动存入SQLite数据库（destinations + product表）` -> 执行当前语句，参与该文件整体逻辑。
- 8: `3. 智能缓存：已获取的城市数据不再重复调用API` -> 执行当前语句，参与该文件整体逻辑。
- 9: `4. 每日限额6000次，优先保障增量更新` -> 执行当前语句，参与该文件整体逻辑。
- 10: `(空行)` -> 空行，用于提升代码结构可读性。
- 11: `使用方法：` -> 执行当前语句，参与该文件整体逻辑。
- 12: `python amap_service.py --city 北京 --keyword 景点` -> 执行当前语句，参与该文件整体逻辑。
- 13: `python amap_service.py --city 上海 --keyword 酒店` -> 执行当前语句，参与该文件整体逻辑。
- 14: `python amap_service.py --batch-cities 北京,上海,杭州,成都` -> 执行当前语句，参与该文件整体逻辑。
- 15: `python amap_service.py --stats          # 查看缓存统计` -> 执行当前语句，参与该文件整体逻辑。
- 16: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 17: `(空行)` -> 空行，用于提升代码结构可读性。
- 18: `import os` -> 导入依赖模块，供当前文件使用。
- 19: `import sys` -> 导入依赖模块，供当前文件使用。
- 20: `import json` -> 导入依赖模块，供当前文件使用。
- 21: `import time` -> 导入依赖模块，供当前文件使用。
- 22: `import sqlite3` -> 导入依赖模块，供当前文件使用。
- 23: `import argparse` -> 导入依赖模块，供当前文件使用。
- 24: `import requests` -> 导入依赖模块，供当前文件使用。
- 25: `from datetime import datetime` -> 执行当前语句，参与该文件整体逻辑。
- 26: `from typing import List, Dict, Optional` -> 执行当前语句，参与该文件整体逻辑。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `# ==================== 配置 ====================` -> 注释行，用于解释设计意图或使用说明。
- 29: `QQ_MAP_KEY = "JLSBZ-SRPKI-2LXGS-UUYM7-NSDAQ-5PBY3"` -> 执行当前语句，参与该文件整体逻辑。
- 30: `DB_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)), "instance", "travel.db")` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `# 腾讯地图API接口` -> 注释行，用于解释设计意图或使用说明。
- 33: `QQ_MAP_BASE = "https://apis.map.qq.com"` -> 执行当前语句，参与该文件整体逻辑。
- 34: `GEOCODE_URL = f"{QQ_MAP_BASE}/ws/geocoder/v1"` -> 执行当前语句，参与该文件整体逻辑。
- 35: `POI_SEARCH_URL = f"{QQ_MAP_BASE}/ws/place/v1/search"` -> 执行当前语句，参与该文件整体逻辑。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `# 常用城市坐标映射表（避免每次调用地理编码API，节省配额）` -> 注释行，用于解释设计意图或使用说明。
- 38: `CITY_COORDINATES = {` -> 执行当前语句，参与该文件整体逻辑。
- 39: `"北京": (116.407526, 39.90403),` -> 执行当前语句，参与该文件整体逻辑。
- 40: `"上海": (121.473701, 31.230416),` -> 执行当前语句，参与该文件整体逻辑。
- 41: `"杭州": (120.15507, 30.274084),` -> 执行当前语句，参与该文件整体逻辑。
- 42: `"成都": (104.066541, 30.572269),` -> 执行当前语句，参与该文件整体逻辑。
- 43: `"西安": (108.93977, 34.341574),` -> 执行当前语句，参与该文件整体逻辑。
- 44: `"广州": (113.264434, 23.129162),` -> 执行当前语句，参与该文件整体逻辑。
- 45: `"深圳": (114.057868, 22.543099),` -> 执行当前语句，参与该文件整体逻辑。
- 46: `"南京": (118.796877, 32.060255),` -> 执行当前语句，参与该文件整体逻辑。
- 47: `"苏州": (120.585316, 31.298886),` -> 执行当前语句，参与该文件整体逻辑。
- 48: `"重庆": (106.551556, 29.563009),` -> 执行当前语句，参与该文件整体逻辑。
- 49: `"武汉": (114.305393, 30.593099),` -> 执行当前语句，参与该文件整体逻辑。
- 50: `"长沙": (112.938814, 28.228209),` -> 执行当前语句，参与该文件整体逻辑。
- 51: `"厦门": (118.089425, 24.479833),` -> 执行当前语句，参与该文件整体逻辑。
- 52: `"青岛": (120.38264, 36.067082),` -> 执行当前语句，参与该文件整体逻辑。
- 53: `"大连": (121.614682, 38.914003),` -> 执行当前语句，参与该文件整体逻辑。
- 54: `"昆明": (102.832891, 24.880095),` -> 执行当前语句，参与该文件整体逻辑。
- 55: `"桂林": (110.179953, 25.234479),` -> 执行当前语句，参与该文件整体逻辑。
- 56: `"拉萨": (91.140856, 29.645554),` -> 执行当前语句，参与该文件整体逻辑。
- 57: `"乌鲁木齐": (87.616848, 43.825592),` -> 执行当前语句，参与该文件整体逻辑。
- 58: `"哈尔滨": (126.534967, 45.803775),` -> 执行当前语句，参与该文件整体逻辑。
- 59: `"郑州": (113.625368, 34.746599),` -> 执行当前语句，参与该文件整体逻辑。
- 60: `"洛阳": (112.434468, 34.663041),` -> 执行当前语句，参与该文件整体逻辑。
- 61: `"黄山": (118.317325, 29.714655),` -> 执行当前语句，参与该文件整体逻辑。
- 62: `"天津": (117.200983, 39.084158),` -> 执行当前语句，参与该文件整体逻辑。
- 63: `"宁波": (121.550357, 29.874556),` -> 执行当前语句，参与该文件整体逻辑。
- 64: `"无锡": (120.31191, 31.491169),` -> 执行当前语句，参与该文件整体逻辑。
- 65: `"福州": (119.296494, 26.074508),` -> 执行当前语句，参与该文件整体逻辑。
- 66: `"济南": (117.1205, 36.651216),` -> 执行当前语句，参与该文件整体逻辑。
- 67: `"沈阳": (123.42944, 41.835441),` -> 执行当前语句，参与该文件整体逻辑。
- 68: `"长春": (125.323544, 43.817071),` -> 执行当前语句，参与该文件整体逻辑。
- 69: `"石家庄": (114.514859, 38.042306),` -> 执行当前语句，参与该文件整体逻辑。
- 70: `"太原": (112.548879, 37.87059),` -> 执行当前语句，参与该文件整体逻辑。
- 71: `"兰州": (103.834303, 36.061089),` -> 执行当前语句，参与该文件整体逻辑。
- 72: `"银川": (106.230909, 38.487193),` -> 执行当前语句，参与该文件整体逻辑。
- 73: `"西宁": (101.778916, 36.623178),` -> 执行当前语句，参与该文件整体逻辑。
- 74: `"呼和浩特": (111.74918, 40.841389),` -> 执行当前语句，参与该文件整体逻辑。
- 75: `"海口": (110.349228, 20.017377),` -> 执行当前语句，参与该文件整体逻辑。
- 76: `"南宁": (108.366543, 22.817002),` -> 执行当前语句，参与该文件整体逻辑。
- 77: `"贵阳": (106.630153, 26.647661),` -> 执行当前语句，参与该文件整体逻辑。
- 78: `"南昌": (115.857963, 28.68202),` -> 执行当前语句，参与该文件整体逻辑。
- 79: `"合肥": (117.227239, 31.820586),` -> 执行当前语句，参与该文件整体逻辑。
- 80: `"温州": (120.699366, 27.994267),` -> 执行当前语句，参与该文件整体逻辑。
- 81: `"泉州": (118.675675, 24.874332),` -> 执行当前语句，参与该文件整体逻辑。
- 82: `"烟台": (121.447935, 37.463822),` -> 执行当前语句，参与该文件整体逻辑。
- 83: `"威海": (122.12042, 37.513092),` -> 执行当前语句，参与该文件整体逻辑。
- 84: `"丽江": (100.233026, 26.872108),` -> 执行当前语句，参与该文件整体逻辑。
- 85: `"大理": (100.225668, 25.589449),` -> 执行当前语句，参与该文件整体逻辑。
- 86: `"三亚": (109.508268, 18.247872),` -> 执行当前语句，参与该文件整体逻辑。
- 87: `"珠海": (113.576726, 22.270715),` -> 执行当前语句，参与该文件整体逻辑。
- 88: `"珠海": (113.576726, 22.270715),` -> 执行当前语句，参与该文件整体逻辑。
- 89: `"佛山": (113.121416, 23.021548),` -> 执行当前语句，参与该文件整体逻辑。
- 90: `"东莞": (113.746262, 23.046237),` -> 执行当前语句，参与该文件整体逻辑。
- 91: `"中山": (113.392782, 22.517645),` -> 执行当前语句，参与该文件整体逻辑。
- 92: `"惠州": (114.415801, 23.111847),` -> 执行当前语句，参与该文件整体逻辑。
- 93: `"绍兴": (120.580232, 30.029752),` -> 执行当前语句，参与该文件整体逻辑。
- 94: `"嘉兴": (120.755486, 30.74501),` -> 执行当前语句，参与该文件整体逻辑。
- 95: `"扬州": (119.421003, 32.399108),` -> 执行当前语句，参与该文件整体逻辑。
- 96: `"镇江": (119.425836, 32.187849),` -> 执行当前语句，参与该文件整体逻辑。
- 97: `"泰州": (119.923116, 32.455778),` -> 执行当前语句，参与该文件整体逻辑。
- 98: `"南通": (120.894291, 31.980171),` -> 执行当前语句，参与该文件整体逻辑。
- 99: `"盐城": (120.163561, 33.377631),` -> 执行当前语句，参与该文件整体逻辑。
- 100: `"徐州": (117.284124, 34.205768),` -> 执行当前语句，参与该文件整体逻辑。
- 101: `"连云港": (119.221611, 34.596653),` -> 执行当前语句，参与该文件整体逻辑。
- 102: `"淮安": (119.015285, 33.610353),` -> 执行当前语句，参与该文件整体逻辑。
- 103: `"宿迁": (118.275162, 33.963232),` -> 执行当前语句，参与该文件整体逻辑。
- 104: `"常州": (119.974061, 31.811226),` -> 执行当前语句，参与该文件整体逻辑。
- 105: `"湖州": (120.086809, 30.894348),` -> 执行当前语句，参与该文件整体逻辑。
- 106: `"金华": (119.649506, 29.089524),` -> 执行当前语句，参与该文件整体逻辑。
- 107: `"衢州": (118.859457, 28.970619),` -> 执行当前语句，参与该文件整体逻辑。
- 108: `"舟山": (122.207216, 29.985295),` -> 执行当前语句，参与该文件整体逻辑。
- 109: `"台州": (121.420757, 28.656386),` -> 执行当前语句，参与该文件整体逻辑。
- 110: `"丽水": (119.922796, 28.46763),` -> 执行当前语句，参与该文件整体逻辑。
- 111: `}` -> 结束当前语句或代码块。
- 112: `(空行)` -> 空行，用于提升代码结构可读性。
- 113: `# 关键词到腾讯地图category的映射` -> 注释行，用于解释设计意图或使用说明。
- 114: `KEYWORD_CATEGORY_MAP = {` -> 执行当前语句，参与该文件整体逻辑。
- 115: `"景点": "风景名胜",` -> 执行当前语句，参与该文件整体逻辑。
- 116: `"酒店": "住宿服务",` -> 执行当前语句，参与该文件整体逻辑。
- 117: `"餐饮": "餐饮服务",` -> 执行当前语句，参与该文件整体逻辑。
- 118: `"购物": "购物服务",` -> 执行当前语句，参与该文件整体逻辑。
- 119: `"交通": "交通设施",` -> 执行当前语句，参与该文件整体逻辑。
- 120: `"娱乐": "生活服务",` -> 执行当前语句，参与该文件整体逻辑。
- 121: `}` -> 结束当前语句或代码块。
- 122: `(空行)` -> 空行，用于提升代码结构可读性。
- 123: `# 城市列表（可扩展）` -> 注释行，用于解释设计意图或使用说明。
- 124: `DEFAULT_CITIES = [` -> 执行当前语句，参与该文件整体逻辑。
- 125: `"北京", "上海", "杭州", "成都", "西安", "广州", "深圳",` -> 执行当前语句，参与该文件整体逻辑。
- 126: `"南京", "苏州", "重庆", "武汉", "长沙", "厦门", "青岛",` -> 执行当前语句，参与该文件整体逻辑。
- 127: `"大连", "昆明", "桂林", "拉萨", "乌鲁木齐", "哈尔滨"` -> 执行当前语句，参与该文件整体逻辑。
- 128: `]` -> 执行当前语句，参与该文件整体逻辑。
- 129: `(空行)` -> 空行，用于提升代码结构可读性。
- 130: `# ==================== API请求 ====================` -> 注释行，用于解释设计意图或使用说明。
- 131: `(空行)` -> 空行，用于提升代码结构可读性。
- 132: `def qqmap_get(url: str, params: dict, retries: int = 2) -> Optional[dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 133: `"""带重试的腾讯地图API GET请求"""` -> 执行当前语句，参与该文件整体逻辑。
- 134: `params["key"] = QQ_MAP_KEY` -> 执行当前语句，参与该文件整体逻辑。
- 135: `for attempt in range(retries + 1):` -> 循环处理集合或重复执行逻辑。
- 136: `try:` -> 异常处理逻辑，提升程序健壮性。
- 137: `resp = requests.get(url, params=params, timeout=15)` -> 执行当前语句，参与该文件整体逻辑。
- 138: `data = resp.json()` -> 执行当前语句，参与该文件整体逻辑。
- 139: `# 腾讯地图返回 status=0 表示成功` -> 注释行，用于解释设计意图或使用说明。
- 140: `if data.get("status") == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 141: `return data` -> 返回结果或提前结束当前流程。
- 142: `# 配额超限` -> 注释行，用于解释设计意图或使用说明。
- 143: `if data.get("status") == 301 or "quota" in str(data).lower():` -> 条件判断分支，根据场景执行不同逻辑。
- 144: `print(f"[警告] API配额超限或无效Key: {data.get('message', '')}")` -> 执行当前语句，参与该文件整体逻辑。
- 145: `return None` -> 返回结果或提前结束当前流程。
- 146: `if data.get("status") != 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 147: `print(f"[警告] API返回错误: {data.get('message', '')} (status={data.get('status')})")` -> 执行当前语句，参与该文件整体逻辑。
- 148: `except requests.exceptions.RequestException as e:` -> 异常处理逻辑，提升程序健壮性。
- 149: `print(f"[错误] 请求失败(尝试{attempt+1}/{retries+1}): {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 150: `if attempt < retries:` -> 条件判断分支，根据场景执行不同逻辑。
- 151: `time.sleep(1)` -> 执行当前语句，参与该文件整体逻辑。
- 152: `return None` -> 返回结果或提前结束当前流程。
- 153: `(空行)` -> 空行，用于提升代码结构可读性。
- 154: `(空行)` -> 空行，用于提升代码结构可读性。
- 155: `def get_city_coordinates(city: str) -> Optional[tuple]:` -> 定义 Python 函数，封装可复用逻辑。
- 156: `"""获取城市中心坐标，优先使用本地缓存"""` -> 执行当前语句，参与该文件整体逻辑。
- 157: `# 优先查本地映射表（节省API配额）` -> 注释行，用于解释设计意图或使用说明。
- 158: `city_clean = city.replace("市", "").replace("省", "")` -> 执行当前语句，参与该文件整体逻辑。
- 159: `for k, v in CITY_COORDINATES.items():` -> 循环处理集合或重复执行逻辑。
- 160: `if city_clean in k or k in city_clean:` -> 条件判断分支，根据场景执行不同逻辑。
- 161: `return v` -> 返回结果或提前结束当前流程。
- 162: `# 本地没有则调用API` -> 注释行，用于解释设计意图或使用说明。
- 163: `data = qqmap_get(GEOCODE_URL, {"address": city})` -> 执行当前语句，参与该文件整体逻辑。
- 164: `if data and data.get("status") == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 165: `result = data.get("result", {})` -> 执行当前语句，参与该文件整体逻辑。
- 166: `location = result.get("location", {})` -> 执行当前语句，参与该文件整体逻辑。
- 167: `return location.get("lng"), location.get("lat")` -> 返回结果或提前结束当前流程。
- 168: `return None` -> 返回结果或提前结束当前流程。
- 169: `(空行)` -> 空行，用于提升代码结构可读性。
- 170: `(空行)` -> 空行，用于提升代码结构可读性。
- 171: `def check_key() -> bool:` -> 定义 Python 函数，封装可复用逻辑。
- 172: `"""验证腾讯地图Key是否有效"""` -> 执行当前语句，参与该文件整体逻辑。
- 173: `data = qqmap_get(GEOCODE_URL, {"address": "北京"})` -> 执行当前语句，参与该文件整体逻辑。
- 174: `if data and data.get("status") == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 175: `return True` -> 返回结果或提前结束当前流程。
- 176: `print("[错误] 腾讯地图API Key无效或未启用WebService API，请检查控制台配置")` -> 执行当前语句，参与该文件整体逻辑。
- 177: `print("        控制台地址: https://lbs.qq.com/dev/console")` -> 执行当前语句，参与该文件整体逻辑。
- 178: `return False` -> 返回结果或提前结束当前流程。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `(空行)` -> 空行，用于提升代码结构可读性。
- 181: `def search_poi(city: str, keyword: str = "", category: str = "", page: int = 1, page_size: int = 20) -> List[dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 182: `"""搜索POI（腾讯地图）"""` -> 执行当前语句，参与该文件整体逻辑。
- 183: `params = {` -> 执行当前语句，参与该文件整体逻辑。
- 184: `"keyword": keyword or category or "景点",` -> 执行当前语句，参与该文件整体逻辑。
- 185: `"boundary": f"region({city},0)",  # 0=模糊匹配，1=精确城市名` -> 执行当前语句，参与该文件整体逻辑。
- 186: `"page_size": page_size,` -> 执行当前语句，参与该文件整体逻辑。
- 187: `"page_index": page,` -> 执行当前语句，参与该文件整体逻辑。
- 188: `"output": "json",` -> 执行当前语句，参与该文件整体逻辑。
- 189: `}` -> 结束当前语句或代码块。
- 190: `(空行)` -> 空行，用于提升代码结构可读性。
- 191: `data = qqmap_get(POI_SEARCH_URL, params)` -> 执行当前语句，参与该文件整体逻辑。
- 192: `if not data:` -> 条件判断分支，根据场景执行不同逻辑。
- 193: `return []` -> 返回结果或提前结束当前流程。
- 194: `(空行)` -> 空行，用于提升代码结构可读性。
- 195: `pois = data.get("data", []) or []` -> 执行当前语句，参与该文件整体逻辑。
- 196: `return pois` -> 返回结果或提前结束当前流程。
- 197: `(空行)` -> 空行，用于提升代码结构可读性。
- 198: `(空行)` -> 空行，用于提升代码结构可读性。
- 199: `def fetch_all_poi(city: str, keyword: str = "", category: str = "", max_count: int = 100) -> List[dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 200: `"""分页获取POI，最多max_count条"""` -> 执行当前语句，参与该文件整体逻辑。
- 201: `all_pois = []` -> 执行当前语句，参与该文件整体逻辑。
- 202: `page = 1` -> 执行当前语句，参与该文件整体逻辑。
- 203: `page_size = 20` -> 执行当前语句，参与该文件整体逻辑。
- 204: `while len(all_pois) < max_count:` -> 循环处理集合或重复执行逻辑。
- 205: `pois = search_poi(city, keyword, category, page, page_size)` -> 执行当前语句，参与该文件整体逻辑。
- 206: `if not pois:` -> 条件判断分支，根据场景执行不同逻辑。
- 207: `break` -> 执行当前语句，参与该文件整体逻辑。
- 208: `all_pois.extend(pois)` -> 执行当前语句，参与该文件整体逻辑。
- 209: `if len(pois) < page_size:` -> 条件判断分支，根据场景执行不同逻辑。
- 210: `break` -> 执行当前语句，参与该文件整体逻辑。
- 211: `page += 1` -> 执行当前语句，参与该文件整体逻辑。
- 212: `time.sleep(0.2)  # 控制请求频率` -> 执行当前语句，参与该文件整体逻辑。
- 213: `return all_pois[:max_count]` -> 返回结果或提前结束当前流程。
- 214: `(空行)` -> 空行，用于提升代码结构可读性。
- 215: `(空行)` -> 空行，用于提升代码结构可读性。
- 216: `# ==================== 数据库操作 ====================` -> 注释行，用于解释设计意图或使用说明。
- 217: `(空行)` -> 空行，用于提升代码结构可读性。
- 218: `def get_db_connection():` -> 定义 Python 函数，封装可复用逻辑。
- 219: `"""获取数据库连接"""` -> 执行当前语句，参与该文件整体逻辑。
- 220: `conn = sqlite3.connect(DB_PATH)` -> 执行当前语句，参与该文件整体逻辑。
- 221: `conn.row_factory = sqlite3.Row` -> 执行当前语句，参与该文件整体逻辑。
- 222: `return conn` -> 返回结果或提前结束当前流程。
- 223: `(空行)` -> 空行，用于提升代码结构可读性。
- 224: `(空行)` -> 空行，用于提升代码结构可读性。
- 225: `def get_cached_city_names() -> set:` -> 定义 Python 函数，封装可复用逻辑。
- 226: `"""获取数据库中已缓存的城市名称集合"""` -> 执行当前语句，参与该文件整体逻辑。
- 227: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 228: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 229: `cursor.execute("SELECT DISTINCT city FROM destinations WHERE city IS NOT NULL")` -> 执行当前语句，参与该文件整体逻辑。
- 230: `cities = {row["city"].replace("市", "").replace("省", "") for row in cursor.fetchall() if row["city"]}` -> 执行当前语句，参与该文件整体逻辑。
- 231: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 232: `return cities` -> 返回结果或提前结束当前流程。
- 233: `(空行)` -> 空行，用于提升代码结构可读性。
- 234: `(空行)` -> 空行，用于提升代码结构可读性。
- 235: `def get_destination_count_by_city(city: str) -> int:` -> 定义 Python 函数，封装可复用逻辑。
- 236: `"""获取某城市的景点数量"""` -> 执行当前语句，参与该文件整体逻辑。
- 237: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 238: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 239: `cursor.execute("SELECT COUNT(*) FROM destinations WHERE city LIKE ?", (f"%{city}%",))` -> 执行当前语句，参与该文件整体逻辑。
- 240: `count = cursor.fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 241: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 242: `return count` -> 返回结果或提前结束当前流程。
- 243: `(空行)` -> 空行，用于提升代码结构可读性。
- 244: `(空行)` -> 空行，用于提升代码结构可读性。
- 245: `def poi_to_destination(poi: dict, city: str = "") -> dict:` -> 定义 Python 函数，封装可复用逻辑。
- 246: `"""将腾讯地图POI数据转换为本项目destination格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 247: `# 解析经纬度` -> 注释行，用于解释设计意图或使用说明。
- 248: `lng, lat = None, None` -> 执行当前语句，参与该文件整体逻辑。
- 249: `location = poi.get("location", {})` -> 执行当前语句，参与该文件整体逻辑。
- 250: `if isinstance(location, dict):` -> 条件判断分支，根据场景执行不同逻辑。
- 251: `lng = location.get("lng")` -> 执行当前语句，参与该文件整体逻辑。
- 252: `lat = location.get("lat")` -> 执行当前语句，参与该文件整体逻辑。
- 253: `elif isinstance(location, str) and "," in location:` -> 条件判断分支，根据场景执行不同逻辑。
- 254: `parts = location.split(",")` -> 执行当前语句，参与该文件整体逻辑。
- 255: `lng, lat = float(parts[0]), float(parts[1])` -> 执行当前语句，参与该文件整体逻辑。
- 256: `(空行)` -> 空行，用于提升代码结构可读性。
- 257: `# 解析地址` -> 注释行，用于解释设计意图或使用说明。
- 258: `address = poi.get("address", "")` -> 执行当前语句，参与该文件整体逻辑。
- 259: `if not address:` -> 条件判断分支，根据场景执行不同逻辑。
- 260: `address = poi.get("addr", "")` -> 执行当前语句，参与该文件整体逻辑。
- 261: `(空行)` -> 空行，用于提升代码结构可读性。
- 262: `# 解析分类` -> 注释行，用于解释设计意图或使用说明。
- 263: `category = poi.get("category", "")` -> 执行当前语句，参与该文件整体逻辑。
- 264: `(空行)` -> 空行，用于提升代码结构可读性。
- 265: `# 解析电话` -> 注释行，用于解释设计意图或使用说明。
- 266: `tel = poi.get("tel", "")` -> 执行当前语句，参与该文件整体逻辑。
- 267: `(空行)` -> 空行，用于提升代码结构可读性。
- 268: `# 省份/城市` -> 注释行，用于解释设计意图或使用说明。
- 269: `province = poi.get("province", "")` -> 执行当前语句，参与该文件整体逻辑。
- 270: `city_name = poi.get("city", "") or city` -> 执行当前语句，参与该文件整体逻辑。
- 271: `(空行)` -> 空行，用于提升代码结构可读性。
- 272: `return {` -> 返回结果或提前结束当前流程。
- 273: `"name": poi.get("title", "").strip(),` -> 执行当前语句，参与该文件整体逻辑。
- 274: `"city": city_name,` -> 执行当前语句，参与该文件整体逻辑。
- 275: `"province": province,` -> 执行当前语句，参与该文件整体逻辑。
- 276: `"description": f"位于{address or city_name}。{category}",` -> 执行当前语句，参与该文件整体逻辑。
- 277: `"cover_image": None,  # 腾讯地图POI不含图片URL，需后续补充` -> 执行当前语句，参与该文件整体逻辑。
- 278: `"rating": 4.5,  # 腾讯地图无评分，用默认值` -> 执行当前语句，参与该文件整体逻辑。
- 279: `"ticket_price": 0.0,  # 腾讯地图无价格，用默认值` -> 执行当前语句，参与该文件整体逻辑。
- 280: `"open_time": "",` -> 执行当前语句，参与该文件整体逻辑。
- 281: `"lng": lng,` -> 执行当前语句，参与该文件整体逻辑。
- 282: `"lat": lat,` -> 执行当前语句，参与该文件整体逻辑。
- 283: `"address": address,` -> 执行当前语句，参与该文件整体逻辑。
- 284: `"tel": tel,` -> 执行当前语句，参与该文件整体逻辑。
- 285: `}` -> 结束当前语句或代码块。
- 286: `(空行)` -> 空行，用于提升代码结构可读性。
- 287: `(空行)` -> 空行，用于提升代码结构可读性。
- 288: `def save_destinations(destinations: List[dict]) -> int:` -> 定义 Python 函数，封装可复用逻辑。
- 289: `"""保存景点到destinations表，跳过已存在的（按name+city去重）"""` -> 执行当前语句，参与该文件整体逻辑。
- 290: `if not destinations:` -> 条件判断分支，根据场景执行不同逻辑。
- 291: `return 0` -> 返回结果或提前结束当前流程。
- 292: `(空行)` -> 空行，用于提升代码结构可读性。
- 293: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 294: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 295: `inserted = 0` -> 执行当前语句，参与该文件整体逻辑。
- 296: `skipped = 0` -> 执行当前语句，参与该文件整体逻辑。
- 297: `(空行)` -> 空行，用于提升代码结构可读性。
- 298: `now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")` -> 执行当前语句，参与该文件整体逻辑。
- 299: `(空行)` -> 空行，用于提升代码结构可读性。
- 300: `for dest in destinations:` -> 循环处理集合或重复执行逻辑。
- 301: `if not dest.get("name"):` -> 条件判断分支，根据场景执行不同逻辑。
- 302: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 303: `# 检查是否已存在` -> 注释行，用于解释设计意图或使用说明。
- 304: `cursor.execute(` -> 执行当前语句，参与该文件整体逻辑。
- 305: `"SELECT id FROM destinations WHERE name = ? AND city = ?",` -> 执行当前语句，参与该文件整体逻辑。
- 306: `(dest["name"], dest.get("city", ""))` -> 执行当前语句，参与该文件整体逻辑。
- 307: `)` -> 结束当前语句或代码块。
- 308: `if cursor.fetchone():` -> 条件判断分支，根据场景执行不同逻辑。
- 309: `skipped += 1` -> 执行当前语句，参与该文件整体逻辑。
- 310: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 311: `(空行)` -> 空行，用于提升代码结构可读性。
- 312: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 313: `INSERT INTO destinations (name, city, province, description, cover_image, rating, ticket_price, open_time, lng, lat, created_at, updated_at)` -> SQL 语句核心部分，用于数据查询或变更。
- 314: `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)` -> 执行当前语句，参与该文件整体逻辑。
- 315: `""", (` -> 执行当前语句，参与该文件整体逻辑。
- 316: `dest["name"],` -> 执行当前语句，参与该文件整体逻辑。
- 317: `dest.get("city", ""),` -> 执行当前语句，参与该文件整体逻辑。
- 318: `dest.get("province", ""),` -> 执行当前语句，参与该文件整体逻辑。
- 319: `dest.get("description", ""),` -> 执行当前语句，参与该文件整体逻辑。
- 320: `dest.get("cover_image"),` -> 执行当前语句，参与该文件整体逻辑。
- 321: `dest.get("rating", 4.5),` -> 执行当前语句，参与该文件整体逻辑。
- 322: `dest.get("ticket_price", 0),` -> 执行当前语句，参与该文件整体逻辑。
- 323: `dest.get("open_time", ""),` -> 执行当前语句，参与该文件整体逻辑。
- 324: `dest.get("lng"),` -> 执行当前语句，参与该文件整体逻辑。
- 325: `dest.get("lat"),` -> 执行当前语句，参与该文件整体逻辑。
- 326: `now, now` -> 执行当前语句，参与该文件整体逻辑。
- 327: `))` -> 执行当前语句，参与该文件整体逻辑。
- 328: `inserted += 1` -> 执行当前语句，参与该文件整体逻辑。
- 329: `(空行)` -> 空行，用于提升代码结构可读性。
- 330: `conn.commit()` -> 执行当前语句，参与该文件整体逻辑。
- 331: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 332: `print(f"  -> destinations: 新增 {inserted} 条, 跳过 {skipped} 条")` -> 执行当前语句，参与该文件整体逻辑。
- 333: `return inserted` -> 返回结果或提前结束当前流程。
- 334: `(空行)` -> 空行，用于提升代码结构可读性。
- 335: `(空行)` -> 空行，用于提升代码结构可读性。
- 336: `def save_products_from_destinations(destinations: List[dict], category: str = "ticket") -> int:` -> 定义 Python 函数，封装可复用逻辑。
- 337: `"""基于景点生成product产品数据"""` -> 执行当前语句，参与该文件整体逻辑。
- 338: `if not destinations:` -> 条件判断分支，根据场景执行不同逻辑。
- 339: `return 0` -> 返回结果或提前结束当前流程。
- 340: `(空行)` -> 空行，用于提升代码结构可读性。
- 341: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 342: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 343: `inserted = 0` -> 执行当前语句，参与该文件整体逻辑。
- 344: `skipped = 0` -> 执行当前语句，参与该文件整体逻辑。
- 345: `now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")` -> 执行当前语句，参与该文件整体逻辑。
- 346: `import random` -> 导入依赖模块，供当前文件使用。
- 347: `(空行)` -> 空行，用于提升代码结构可读性。
- 348: `for dest in destinations:` -> 循环处理集合或重复执行逻辑。
- 349: `name = dest.get("name", "")` -> 执行当前语句，参与该文件整体逻辑。
- 350: `if not name:` -> 条件判断分支，根据场景执行不同逻辑。
- 351: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 352: `(空行)` -> 空行，用于提升代码结构可读性。
- 353: `# 检查是否已有对应产品` -> 注释行，用于解释设计意图或使用说明。
- 354: `cursor.execute("SELECT id FROM product WHERE name = ?", (name,))` -> 执行当前语句，参与该文件整体逻辑。
- 355: `if cursor.fetchone():` -> 条件判断分支，根据场景执行不同逻辑。
- 356: `skipped += 1` -> 执行当前语句，参与该文件整体逻辑。
- 357: `continue` -> 执行当前语句，参与该文件整体逻辑。
- 358: `(空行)` -> 空行，用于提升代码结构可读性。
- 359: `# 查找关联的destination_id` -> 注释行，用于解释设计意图或使用说明。
- 360: `cursor.execute("SELECT id FROM destinations WHERE name = ? AND city = ?", (name, dest.get("city", "")))` -> 执行当前语句，参与该文件整体逻辑。
- 361: `row = cursor.fetchone()` -> 执行当前语句，参与该文件整体逻辑。
- 362: `destination_id = row["id"] if row else None` -> 执行当前语句，参与该文件整体逻辑。
- 363: `(空行)` -> 空行，用于提升代码结构可读性。
- 364: `price = dest.get("ticket_price", 0) or 0` -> 执行当前语句，参与该文件整体逻辑。
- 365: `if price == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 366: `price = random.randint(30, 200)` -> 执行当前语句，参与该文件整体逻辑。
- 367: `(空行)` -> 空行，用于提升代码结构可读性。
- 368: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 369: `INSERT INTO product (type, name, description, price, original_price, status, tags, images, rating, review_count, view_count, sales_count, created_at, updated_at, subtitle, destination_id, category, cover_image)` -> SQL 语句核心部分，用于数据查询或变更。
- 370: `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)` -> 执行当前语句，参与该文件整体逻辑。
- 371: `""", (` -> 执行当前语句，参与该文件整体逻辑。
- 372: `category,` -> 执行当前语句，参与该文件整体逻辑。
- 373: `name,` -> 执行当前语句，参与该文件整体逻辑。
- 374: `dest.get("description", ""),` -> 执行当前语句，参与该文件整体逻辑。
- 375: `price,` -> 执行当前语句，参与该文件整体逻辑。
- 376: `round(price * 1.2, 2),` -> 执行当前语句，参与该文件整体逻辑。
- 377: `"active",` -> 执行当前语句，参与该文件整体逻辑。
- 378: `json.dumps([category]),` -> 执行当前语句，参与该文件整体逻辑。
- 379: `json.dumps([]),` -> 执行当前语句，参与该文件整体逻辑。
- 380: `dest.get("rating", 4.5),` -> 执行当前语句，参与该文件整体逻辑。
- 381: `random.randint(10, 999),` -> 执行当前语句，参与该文件整体逻辑。
- 382: `random.randint(50, 5000),` -> 执行当前语句，参与该文件整体逻辑。
- 383: `random.randint(0, 200),` -> 执行当前语句，参与该文件整体逻辑。
- 384: `now, now,` -> 执行当前语句，参与该文件整体逻辑。
- 385: `f"{dest.get('city','')}热门推荐",` -> 执行当前语句，参与该文件整体逻辑。
- 386: `destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 387: `category,` -> 执行当前语句，参与该文件整体逻辑。
- 388: `dest.get("cover_image")` -> 执行当前语句，参与该文件整体逻辑。
- 389: `))` -> 执行当前语句，参与该文件整体逻辑。
- 390: `inserted += 1` -> 执行当前语句，参与该文件整体逻辑。
- 391: `(空行)` -> 空行，用于提升代码结构可读性。
- 392: `conn.commit()` -> 执行当前语句，参与该文件整体逻辑。
- 393: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 394: `print(f"  -> products: 新增 {inserted} 条, 跳过 {skipped} 条")` -> 执行当前语句，参与该文件整体逻辑。
- 395: `return inserted` -> 返回结果或提前结束当前流程。
- 396: `(空行)` -> 空行，用于提升代码结构可读性。
- 397: `(空行)` -> 空行，用于提升代码结构可读性。
- 398: `# ==================== 主流程 ====================` -> 注释行，用于解释设计意图或使用说明。
- 399: `(空行)` -> 空行，用于提升代码结构可读性。
- 400: `def fetch_and_save_city_pois(city: str, keyword: str = "景点", category: str = "", max_count: int = 100, skip_cached: bool = True) -> dict:` -> 定义 Python 函数，封装可复用逻辑。
- 401: `"""获取并保存某城市的POI数据"""` -> 执行当前语句，参与该文件整体逻辑。
- 402: `result = {"city": city, "fetched": 0, "saved_dest": 0, "saved_prod": 0, "skipped": False}` -> 执行当前语句，参与该文件整体逻辑。
- 403: `(空行)` -> 空行，用于提升代码结构可读性。
- 404: `# 检查是否已缓存` -> 注释行，用于解释设计意图或使用说明。
- 405: `existing_count = get_destination_count_by_city(city)` -> 执行当前语句，参与该文件整体逻辑。
- 406: `if skip_cached and existing_count >= 10:` -> 条件判断分支，根据场景执行不同逻辑。
- 407: `print(f"[{city}] 已有 {existing_count} 条数据，跳过（加 --force 强制更新）")` -> 执行当前语句，参与该文件整体逻辑。
- 408: `result["skipped"] = True` -> 执行当前语句，参与该文件整体逻辑。
- 409: `return result` -> 返回结果或提前结束当前流程。
- 410: `(空行)` -> 空行，用于提升代码结构可读性。
- 411: `print(f"\n[{city}] 开始获取 '{keyword or category}' 数据...")` -> 执行当前语句，参与该文件整体逻辑。
- 412: `(空行)` -> 空行，用于提升代码结构可读性。
- 413: `pois = fetch_all_poi(city, keyword, category, max_count)` -> 执行当前语句，参与该文件整体逻辑。
- 414: `if not pois:` -> 条件判断分支，根据场景执行不同逻辑。
- 415: `print(f"[{city}] 未获取到数据")` -> 执行当前语句，参与该文件整体逻辑。
- 416: `return result` -> 返回结果或提前结束当前流程。
- 417: `(空行)` -> 空行，用于提升代码结构可读性。
- 418: `result["fetched"] = len(pois)` -> 执行当前语句，参与该文件整体逻辑。
- 419: `print(f"[{city}] 获取到 {len(pois)} 条POI")` -> 执行当前语句，参与该文件整体逻辑。
- 420: `(空行)` -> 空行，用于提升代码结构可读性。
- 421: `# 转换为destination格式` -> 注释行，用于解释设计意图或使用说明。
- 422: `destinations = []` -> 执行当前语句，参与该文件整体逻辑。
- 423: `for poi in pois:` -> 循环处理集合或重复执行逻辑。
- 424: `dest = poi_to_destination(poi, city)` -> 执行当前语句，参与该文件整体逻辑。
- 425: `if dest["name"]:` -> 条件判断分支，根据场景执行不同逻辑。
- 426: `destinations.append(dest)` -> 执行当前语句，参与该文件整体逻辑。
- 427: `(空行)` -> 空行，用于提升代码结构可读性。
- 428: `# 保存到数据库` -> 注释行，用于解释设计意图或使用说明。
- 429: `result["saved_dest"] = save_destinations(destinations)` -> 执行当前语句，参与该文件整体逻辑。
- 430: `(空行)` -> 空行，用于提升代码结构可读性。
- 431: `# 同时生成product（根据关键词分类）` -> 注释行，用于解释设计意图或使用说明。
- 432: `if keyword in ("景点", "") or category == "风景名胜":` -> 条件判断分支，根据场景执行不同逻辑。
- 433: `result["saved_prod"] = save_products_from_destinations(destinations, category="ticket")` -> 执行当前语句，参与该文件整体逻辑。
- 434: `elif keyword == "酒店" or category == "住宿服务":` -> 条件判断分支，根据场景执行不同逻辑。
- 435: `result["saved_prod"] = save_products_from_destinations(destinations, category="hotel")` -> 执行当前语句，参与该文件整体逻辑。
- 436: `elif keyword in ("餐饮", "餐厅") or category == "餐饮服务":` -> 条件判断分支，根据场景执行不同逻辑。
- 437: `result["saved_prod"] = save_products_from_destinations(destinations, category="experience")` -> 执行当前语句，参与该文件整体逻辑。
- 438: `(空行)` -> 空行，用于提升代码结构可读性。
- 439: `return result` -> 返回结果或提前结束当前流程。
- 440: `(空行)` -> 空行，用于提升代码结构可读性。
- 441: `(空行)` -> 空行，用于提升代码结构可读性。
- 442: `def batch_fetch(cities: List[str], keyword: str = "景点", max_per_city: int = 100, skip_cached: bool = True):` -> 定义 Python 函数，封装可复用逻辑。
- 443: `"""批量获取多个城市的数据"""` -> 执行当前语句，参与该文件整体逻辑。
- 444: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 445: `print("腾讯地图POI数据批量获取")` -> 执行当前语句，参与该文件整体逻辑。
- 446: `print(f"Key配额: 6000次/天 | 城市数: {len(cities)} | 关键词: {keyword}")` -> 执行当前语句，参与该文件整体逻辑。
- 447: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 448: `(空行)` -> 空行，用于提升代码结构可读性。
- 449: `total = {"fetched": 0, "saved_dest": 0, "saved_prod": 0, "skipped": 0}` -> 执行当前语句，参与该文件整体逻辑。
- 450: `api_calls = 0` -> 执行当前语句，参与该文件整体逻辑。
- 451: `(空行)` -> 空行，用于提升代码结构可读性。
- 452: `for i, city in enumerate(cities, 1):` -> 循环处理集合或重复执行逻辑。
- 453: `print(f"\n进度: [{i}/{len(cities)}]")` -> 执行当前语句，参与该文件整体逻辑。
- 454: `result = fetch_and_save_city_pois(city, keyword, "", max_per_city, skip_cached)` -> 执行当前语句，参与该文件整体逻辑。
- 455: `(空行)` -> 空行，用于提升代码结构可读性。
- 456: `if result["skipped"]:` -> 条件判断分支，根据场景执行不同逻辑。
- 457: `total["skipped"] += 1` -> 执行当前语句，参与该文件整体逻辑。
- 458: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 459: `total["fetched"] += result["fetched"]` -> 执行当前语句，参与该文件整体逻辑。
- 460: `total["saved_dest"] += result["saved_dest"]` -> 执行当前语句，参与该文件整体逻辑。
- 461: `total["saved_prod"] += result["saved_prod"]` -> 执行当前语句，参与该文件整体逻辑。
- 462: `# 估算API调用次数（每页20条）` -> 注释行，用于解释设计意图或使用说明。
- 463: `api_calls += max(1, (result["fetched"] + 19) // 20)` -> 执行当前语句，参与该文件整体逻辑。
- 464: `(空行)` -> 空行，用于提升代码结构可读性。
- 465: `time.sleep(0.3)` -> 执行当前语句，参与该文件整体逻辑。
- 466: `(空行)` -> 空行，用于提升代码结构可读性。
- 467: `print("\n" + "=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 468: `print("批量获取完成")` -> 执行当前语句，参与该文件整体逻辑。
- 469: `print(f"  跳过城市: {total['skipped']}")` -> 执行当前语句，参与该文件整体逻辑。
- 470: `print(f"  获取POI: {total['fetched']}")` -> 执行当前语句，参与该文件整体逻辑。
- 471: `print(f"  新增景点: {total['saved_dest']}")` -> 执行当前语句，参与该文件整体逻辑。
- 472: `print(f"  新增产品: {total['saved_prod']}")` -> 执行当前语句，参与该文件整体逻辑。
- 473: `print(f"  估算API调用: {api_calls} 次")` -> 执行当前语句，参与该文件整体逻辑。
- 474: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 475: `(空行)` -> 空行，用于提升代码结构可读性。
- 476: `(空行)` -> 空行，用于提升代码结构可读性。
- 477: `def show_stats():` -> 定义 Python 函数，封装可复用逻辑。
- 478: `"""显示数据库统计信息"""` -> 执行当前语句，参与该文件整体逻辑。
- 479: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 480: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 481: `(空行)` -> 空行，用于提升代码结构可读性。
- 482: `cursor.execute("SELECT COUNT(*) FROM destinations")` -> 执行当前语句，参与该文件整体逻辑。
- 483: `dest_count = cursor.fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 484: `(空行)` -> 空行，用于提升代码结构可读性。
- 485: `cursor.execute("SELECT COUNT(*) FROM product")` -> 执行当前语句，参与该文件整体逻辑。
- 486: `prod_count = cursor.fetchone()[0]` -> 执行当前语句，参与该文件整体逻辑。
- 487: `(空行)` -> 空行，用于提升代码结构可读性。
- 488: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 489: `SELECT city, COUNT(*) as cnt FROM destinations` -> SQL 语句核心部分，用于数据查询或变更。
- 490: `WHERE city IS NOT NULL AND city != ''` -> 执行当前语句，参与该文件整体逻辑。
- 491: `GROUP BY city` -> 执行当前语句，参与该文件整体逻辑。
- 492: `ORDER BY cnt DESC` -> 执行当前语句，参与该文件整体逻辑。
- 493: `LIMIT 20` -> 执行当前语句，参与该文件整体逻辑。
- 494: `""")` -> 执行当前语句，参与该文件整体逻辑。
- 495: `city_stats = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 496: `(空行)` -> 空行，用于提升代码结构可读性。
- 497: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 498: `(空行)` -> 空行，用于提升代码结构可读性。
- 499: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 500: `print("数据库缓存统计")` -> 执行当前语句，参与该文件整体逻辑。
- 501: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 502: `print(f"总景点数 (destinations): {dest_count}")` -> 执行当前语句，参与该文件整体逻辑。
- 503: `print(f"总产品数 (products): {prod_count}")` -> 执行当前语句，参与该文件整体逻辑。
- 504: `print("\n城市分布Top20:")` -> 执行当前语句，参与该文件整体逻辑。
- 505: `print(f"{'城市':<12} {'数量':>8}")` -> 执行当前语句，参与该文件整体逻辑。
- 506: `print("-" * 22)` -> 执行当前语句，参与该文件整体逻辑。
- 507: `for row in city_stats:` -> 循环处理集合或重复执行逻辑。
- 508: `print(f"{row['city']:<12} {row['cnt']:>8}")` -> 执行当前语句，参与该文件整体逻辑。
- 509: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 510: `(空行)` -> 空行，用于提升代码结构可读性。
- 511: `(空行)` -> 空行，用于提升代码结构可读性。
- 512: `def sync_products_from_existing(city: str = None, batch_size: int = 500) -> int:` -> 定义 Python 函数，封装可复用逻辑。
- 513: `"""从数据库已有destinations生成products，无需调用API"""` -> 执行当前语句，参与该文件整体逻辑。
- 514: `conn = get_db_connection()` -> 执行当前语句，参与该文件整体逻辑。
- 515: `cursor = conn.cursor()` -> 执行当前语句，参与该文件整体逻辑。
- 516: `(空行)` -> 空行，用于提升代码结构可读性。
- 517: `if city:` -> 条件判断分支，根据场景执行不同逻辑。
- 518: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 519: `SELECT d.id, d.name, d.city, d.province, d.description, d.rating, d.ticket_price, d.cover_image` -> SQL 语句核心部分，用于数据查询或变更。
- 520: `FROM destinations d` -> SQL 语句核心部分，用于数据查询或变更。
- 521: `LEFT JOIN product p ON d.name = p.name` -> 执行当前语句，参与该文件整体逻辑。
- 522: `WHERE p.id IS NULL AND d.city LIKE ?` -> 执行当前语句，参与该文件整体逻辑。
- 523: `LIMIT ?` -> 执行当前语句，参与该文件整体逻辑。
- 524: `""", (f"%{city}%", batch_size))` -> 执行当前语句，参与该文件整体逻辑。
- 525: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 526: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 527: `SELECT d.id, d.name, d.city, d.province, d.description, d.rating, d.ticket_price, d.cover_image` -> SQL 语句核心部分，用于数据查询或变更。
- 528: `FROM destinations d` -> SQL 语句核心部分，用于数据查询或变更。
- 529: `LEFT JOIN product p ON d.name = p.name` -> 执行当前语句，参与该文件整体逻辑。
- 530: `WHERE p.id IS NULL` -> 执行当前语句，参与该文件整体逻辑。
- 531: `LIMIT ?` -> 执行当前语句，参与该文件整体逻辑。
- 532: `""", (batch_size,))` -> 执行当前语句，参与该文件整体逻辑。
- 533: `(空行)` -> 空行，用于提升代码结构可读性。
- 534: `rows = cursor.fetchall()` -> 执行当前语句，参与该文件整体逻辑。
- 535: `if not rows:` -> 条件判断分支，根据场景执行不同逻辑。
- 536: `print("没有需要同步的数据（所有景点已有对应产品）")` -> 执行当前语句，参与该文件整体逻辑。
- 537: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 538: `return 0` -> 返回结果或提前结束当前流程。
- 539: `(空行)` -> 空行，用于提升代码结构可读性。
- 540: `inserted = 0` -> 执行当前语句，参与该文件整体逻辑。
- 541: `now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")` -> 执行当前语句，参与该文件整体逻辑。
- 542: `import random` -> 导入依赖模块，供当前文件使用。
- 543: `(空行)` -> 空行，用于提升代码结构可读性。
- 544: `for row in rows:` -> 循环处理集合或重复执行逻辑。
- 545: `price = row["ticket_price"] or 0` -> 执行当前语句，参与该文件整体逻辑。
- 546: `if price == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 547: `price = random.randint(20, 260)` -> 执行当前语句，参与该文件整体逻辑。
- 548: `(空行)` -> 空行，用于提升代码结构可读性。
- 549: `name = row["name"] or ""` -> 执行当前语句，参与该文件整体逻辑。
- 550: `category = "ticket"` -> 执行当前语句，参与该文件整体逻辑。
- 551: `if any(k in name for k in ["酒店", "宾馆", "旅馆", "民宿", "客栈", "度假村"]):` -> 条件判断分支，根据场景执行不同逻辑。
- 552: `category = "hotel"` -> 执行当前语句，参与该文件整体逻辑。
- 553: `elif any(k in name for k in ["机场", "车站", "码头", "高铁"]):` -> 条件判断分支，根据场景执行不同逻辑。
- 554: `category = "transport"` -> 执行当前语句，参与该文件整体逻辑。
- 555: `elif any(k in name for k in ["餐厅", "饭店", "美食", "小吃"]):` -> 条件判断分支，根据场景执行不同逻辑。
- 556: `category = "experience"` -> 执行当前语句，参与该文件整体逻辑。
- 557: `(空行)` -> 空行，用于提升代码结构可读性。
- 558: `subtitle = f"{row['city'] or '热门'}推荐" if row['city'] else "热门推荐"` -> 执行当前语句，参与该文件整体逻辑。
- 559: `(空行)` -> 空行，用于提升代码结构可读性。
- 560: `try:` -> 异常处理逻辑，提升程序健壮性。
- 561: `cursor.execute("""` -> 执行当前语句，参与该文件整体逻辑。
- 562: `INSERT INTO product (type, name, description, price, original_price, status, tags, images, rating, review_count, view_count, sales_count, created_at, updated_at, subtitle, destination_id, category, cover_image)` -> SQL 语句核心部分，用于数据查询或变更。
- 563: `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)` -> 执行当前语句，参与该文件整体逻辑。
- 564: `""", (` -> 执行当前语句，参与该文件整体逻辑。
- 565: `category,` -> 执行当前语句，参与该文件整体逻辑。
- 566: `name,` -> 执行当前语句，参与该文件整体逻辑。
- 567: `row["description"] or f"{name}，位于{row['city'] or '当地'}",` -> 执行当前语句，参与该文件整体逻辑。
- 568: `price,` -> 执行当前语句，参与该文件整体逻辑。
- 569: `round(price * 1.2, 2),` -> 执行当前语句，参与该文件整体逻辑。
- 570: `"active",` -> 执行当前语句，参与该文件整体逻辑。
- 571: `json.dumps([category]),` -> 执行当前语句，参与该文件整体逻辑。
- 572: `json.dumps([row["cover_image"]] if row["cover_image"] else []),` -> 执行当前语句，参与该文件整体逻辑。
- 573: `row["rating"] or 4.5,` -> 执行当前语句，参与该文件整体逻辑。
- 574: `random.randint(10, 999),` -> 执行当前语句，参与该文件整体逻辑。
- 575: `random.randint(50, 5000),` -> 执行当前语句，参与该文件整体逻辑。
- 576: `random.randint(0, 200),` -> 执行当前语句，参与该文件整体逻辑。
- 577: `now, now,` -> 执行当前语句，参与该文件整体逻辑。
- 578: `subtitle,` -> 执行当前语句，参与该文件整体逻辑。
- 579: `row["id"],` -> 执行当前语句，参与该文件整体逻辑。
- 580: `category,` -> 执行当前语句，参与该文件整体逻辑。
- 581: `row["cover_image"]` -> 执行当前语句，参与该文件整体逻辑。
- 582: `))` -> 执行当前语句，参与该文件整体逻辑。
- 583: `inserted += 1` -> 执行当前语句，参与该文件整体逻辑。
- 584: `except sqlite3.IntegrityError:` -> 异常处理逻辑，提升程序健壮性。
- 585: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 586: `(空行)` -> 空行，用于提升代码结构可读性。
- 587: `conn.commit()` -> 执行当前语句，参与该文件整体逻辑。
- 588: `conn.close()` -> 执行当前语句，参与该文件整体逻辑。
- 589: `print(f"同步完成：新增 {inserted} 条 product 数据")` -> 执行当前语句，参与该文件整体逻辑。
- 590: `return inserted` -> 返回结果或提前结束当前流程。
- 591: `(空行)` -> 空行，用于提升代码结构可读性。
- 592: `(空行)` -> 空行，用于提升代码结构可读性。
- 593: `def main():` -> 定义 Python 函数，封装可复用逻辑。
- 594: `parser = argparse.ArgumentParser(description="腾讯地图API数据获取与缓存")` -> 执行当前语句，参与该文件整体逻辑。
- 595: `parser.add_argument("--city", help="目标城市，如：北京")` -> 执行当前语句，参与该文件整体逻辑。
- 596: `parser.add_argument("--keyword", default="景点", help="搜索关键词，如：景点/酒店/餐厅")` -> 执行当前语句，参与该文件整体逻辑。
- 597: `parser.add_argument("--max", type=int, default=100, help="每城市最大获取条数（默认100）")` -> 执行当前语句，参与该文件整体逻辑。
- 598: `parser.add_argument("--batch-cities", help="批量城市，逗号分隔，如：北京,上海,杭州")` -> 执行当前语句，参与该文件整体逻辑。
- 599: `parser.add_argument("--force", action="store_true", help="强制更新，忽略缓存")` -> 执行当前语句，参与该文件整体逻辑。
- 600: `parser.add_argument("--stats", action="store_true", help="显示缓存统计")` -> 执行当前语句，参与该文件整体逻辑。
- 601: `parser.add_argument("--check-key", action="store_true", help="验证API Key有效性")` -> 执行当前语句，参与该文件整体逻辑。
- 602: `parser.add_argument("--sync-products", action="store_true", help="从已有destinations同步生成products（不调用API）")` -> 执行当前语句，参与该文件整体逻辑。
- 603: `parser.add_argument("--sync-batch", type=int, default=500, help="每次同步数量（默认500）")` -> 执行当前语句，参与该文件整体逻辑。
- 604: `(空行)` -> 空行，用于提升代码结构可读性。
- 605: `args = parser.parse_args()` -> 执行当前语句，参与该文件整体逻辑。
- 606: `(空行)` -> 空行，用于提升代码结构可读性。
- 607: `if args.stats:` -> 条件判断分支，根据场景执行不同逻辑。
- 608: `show_stats()` -> 执行当前语句，参与该文件整体逻辑。
- 609: `return` -> 返回结果或提前结束当前流程。
- 610: `(空行)` -> 空行，用于提升代码结构可读性。
- 611: `if args.check_key:` -> 条件判断分支，根据场景执行不同逻辑。
- 612: `if check_key():` -> 条件判断分支，根据场景执行不同逻辑。
- 613: `print("[OK] API Key有效")` -> 执行当前语句，参与该文件整体逻辑。
- 614: `return` -> 返回结果或提前结束当前流程。
- 615: `(空行)` -> 空行，用于提升代码结构可读性。
- 616: `if args.sync_products:` -> 条件判断分支，根据场景执行不同逻辑。
- 617: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 618: `print("从现有景点数据同步生成产品数据（无需API调用）")` -> 执行当前语句，参与该文件整体逻辑。
- 619: `print("=" * 60)` -> 执行当前语句，参与该文件整体逻辑。
- 620: `total_synced = 0` -> 执行当前语句，参与该文件整体逻辑。
- 621: `while True:` -> 循环处理集合或重复执行逻辑。
- 622: `synced = sync_products_from_existing(city=args.city, batch_size=args.sync_batch)` -> 执行当前语句，参与该文件整体逻辑。
- 623: `total_synced += synced` -> 执行当前语句，参与该文件整体逻辑。
- 624: `if synced == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 625: `break` -> 执行当前语句，参与该文件整体逻辑。
- 626: `print(f"  累计同步: {total_synced} 条")` -> 执行当前语句，参与该文件整体逻辑。
- 627: `print(f"\n同步完成，共新增 {total_synced} 条 product 数据")` -> 执行当前语句，参与该文件整体逻辑。
- 628: `show_stats()` -> 执行当前语句，参与该文件整体逻辑。
- 629: `return` -> 返回结果或提前结束当前流程。
- 630: `(空行)` -> 空行，用于提升代码结构可读性。
- 631: `# API调用前验证Key` -> 注释行，用于解释设计意图或使用说明。
- 632: `if not check_key():` -> 条件判断分支，根据场景执行不同逻辑。
- 633: `print("\n提示: 可以先执行 --sync-products 利用已有数据生成产品")` -> 执行当前语句，参与该文件整体逻辑。
- 634: `print("      python amap_service.py --sync-products")` -> 执行当前语句，参与该文件整体逻辑。
- 635: `return` -> 返回结果或提前结束当前流程。
- 636: `(空行)` -> 空行，用于提升代码结构可读性。
- 637: `skip_cached = not args.force` -> 执行当前语句，参与该文件整体逻辑。
- 638: `(空行)` -> 空行，用于提升代码结构可读性。
- 639: `if args.batch_cities:` -> 条件判断分支，根据场景执行不同逻辑。
- 640: `cities = [c.strip() for c in args.batch_cities.split(",") if c.strip()]` -> 执行当前语句，参与该文件整体逻辑。
- 641: `batch_fetch(cities, args.keyword, args.max, skip_cached)` -> 执行当前语句，参与该文件整体逻辑。
- 642: `elif args.city:` -> 条件判断分支，根据场景执行不同逻辑。
- 643: `fetch_and_save_city_pois(args.city, args.keyword, "", args.max, skip_cached)` -> 执行当前语句，参与该文件整体逻辑。
- 644: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 645: `print("未指定城市，使用默认城市列表...")` -> 执行当前语句，参与该文件整体逻辑。
- 646: `batch_fetch(DEFAULT_CITIES, args.keyword, args.max, skip_cached)` -> 执行当前语句，参与该文件整体逻辑。
- 647: `(空行)` -> 空行，用于提升代码结构可读性。
- 648: `(空行)` -> 空行，用于提升代码结构可读性。
- 649: `if __name__ == "__main__":` -> 条件判断分支，根据场景执行不同逻辑。
- 650: `main()` -> 执行当前语句，参与该文件整体逻辑。
