# `frontend/user-web/public/icons/generate_icons.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `#!/usr/bin/env python3` -> 注释行，用于解释设计意图或使用说明。
- 2: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 3: `生成PWA图标` -> 执行当前语句，参与该文件整体逻辑。
- 4: `需要安装: pip install cairosvg pillow` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `import os` -> 导入依赖模块，供当前文件使用。
- 8: `import subprocess` -> 导入依赖模块，供当前文件使用。
- 9: `from PIL import Image` -> 执行当前语句，参与该文件整体逻辑。
- 10: `import io` -> 导入依赖模块，供当前文件使用。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `# 图标尺寸列表` -> 注释行，用于解释设计意图或使用说明。
- 13: `icon_sizes = [(72, 72), (96, 96), (128, 128), (144, 144),` -> 执行当前语句，参与该文件整体逻辑。
- 14: `(152, 152), (192, 192), (384, 384), (512, 512)]` -> 执行当前语句，参与该文件整体逻辑。
- 15: `(空行)` -> 空行，用于提升代码结构可读性。
- 16: `def create_placeholder_icon(size):` -> 定义 Python 函数，封装可复用逻辑。
- 17: `"""创建占位图标"""` -> 执行当前语句，参与该文件整体逻辑。
- 18: `from PIL import Image, ImageDraw` -> 执行当前语句，参与该文件整体逻辑。
- 19: `(空行)` -> 空行，用于提升代码结构可读性。
- 20: `# 创建新图像` -> 注释行，用于解释设计意图或使用说明。
- 21: `img = Image.new('RGBA', (size, size), (14, 165, 233, 255))  # #0ea5e9` -> 执行当前语句，参与该文件整体逻辑。
- 22: `draw = ImageDraw.Draw(img)` -> 执行当前语句，参与该文件整体逻辑。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `# 添加白色圆圈` -> 注释行，用于解释设计意图或使用说明。
- 25: `margin = size * 0.1` -> 执行当前语句，参与该文件整体逻辑。
- 26: `draw.ellipse([margin, margin, size-margin, size-margin], fill=(255, 255, 255, 255))` -> 执行当前语句，参与该文件整体逻辑。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `# 添加内部蓝色方块` -> 注释行，用于解释设计意图或使用说明。
- 29: `inner_margin = size * 0.3` -> 执行当前语句，参与该文件整体逻辑。
- 30: `inner_size = size * 0.4` -> 执行当前语句，参与该文件整体逻辑。
- 31: `draw.rectangle([inner_margin, inner_margin,` -> 执行当前语句，参与该文件整体逻辑。
- 32: `inner_margin+inner_size, inner_margin+inner_size],` -> 执行当前语句，参与该文件整体逻辑。
- 33: `fill=(14, 165, 233, 255))` -> 执行当前语句，参与该文件整体逻辑。
- 34: `(空行)` -> 空行，用于提升代码结构可读性。
- 35: `return img` -> 返回结果或提前结束当前流程。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `def main():` -> 定义 Python 函数，封装可复用逻辑。
- 38: `print("开始生成PWA图标...")` -> 执行当前语句，参与该文件整体逻辑。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `# 检查是否安装了cairosvg` -> 注释行，用于解释设计意图或使用说明。
- 41: `try:` -> 异常处理逻辑，提升程序健壮性。
- 42: `import cairosvg` -> 导入依赖模块，供当前文件使用。
- 43: `use_svg = True` -> 执行当前语句，参与该文件整体逻辑。
- 44: `print("检测到cairosvg，将使用SVG转换")` -> 执行当前语句，参与该文件整体逻辑。
- 45: `except ImportError:` -> 异常处理逻辑，提升程序健壮性。
- 46: `use_svg = False` -> 执行当前语句，参与该文件整体逻辑。
- 47: `print("未检测到cairosvg，将使用PIL生成占位图标")` -> 执行当前语句，参与该文件整体逻辑。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `# 确保输出目录存在` -> 注释行，用于解释设计意图或使用说明。
- 50: `output_dir = os.path.dirname(os.path.abspath(__file__))` -> 执行当前语句，参与该文件整体逻辑。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `for width, height in icon_sizes:` -> 循环处理集合或重复执行逻辑。
- 53: `filename = f"icon-{width}x{height}.png"` -> 执行当前语句，参与该文件整体逻辑。
- 54: `filepath = os.path.join(output_dir, filename)` -> 执行当前语句，参与该文件整体逻辑。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `try:` -> 异常处理逻辑，提升程序健壮性。
- 57: `if use_svg:` -> 条件判断分支，根据场景执行不同逻辑。
- 58: `# 使用cairosvg从SVG转换` -> 注释行，用于解释设计意图或使用说明。
- 59: `svg_path = os.path.join(output_dir, "icon.svg")` -> 执行当前语句，参与该文件整体逻辑。
- 60: `if os.path.exists(svg_path):` -> 条件判断分支，根据场景执行不同逻辑。
- 61: `cairosvg.svg2png(url=svg_path, write_to=filepath,` -> 执行当前语句，参与该文件整体逻辑。
- 62: `output_width=width, output_height=height)` -> 执行当前语句，参与该文件整体逻辑。
- 63: `print(f"✓ 生成 {filename}")` -> 执行当前语句，参与该文件整体逻辑。
- 64: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 65: `# 如果没有SVG，创建占位图标` -> 注释行，用于解释设计意图或使用说明。
- 66: `img = create_placeholder_icon(width)` -> 执行当前语句，参与该文件整体逻辑。
- 67: `img.save(filepath, 'PNG')` -> 执行当前语句，参与该文件整体逻辑。
- 68: `print(f"✓ 生成占位图标 {filename}")` -> 执行当前语句，参与该文件整体逻辑。
- 69: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 70: `# 使用PIL创建占位图标` -> 注释行，用于解释设计意图或使用说明。
- 71: `img = create_placeholder_icon(width)` -> 执行当前语句，参与该文件整体逻辑。
- 72: `img.save(filepath, 'PNG')` -> 执行当前语句，参与该文件整体逻辑。
- 73: `print(f"✓ 生成占位图标 {filename}")` -> 执行当前语句，参与该文件整体逻辑。
- 74: `(空行)` -> 空行，用于提升代码结构可读性。
- 75: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 76: `print(f"✗ 生成 {filename} 失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 77: `(空行)` -> 空行，用于提升代码结构可读性。
- 78: `print("图标生成完成！")` -> 执行当前语句，参与该文件整体逻辑。
- 79: `(空行)` -> 空行，用于提升代码结构可读性。
- 80: `if __name__ == "__main__":` -> 条件判断分支，根据场景执行不同逻辑。
- 81: `main()` -> 执行当前语句，参与该文件整体逻辑。
