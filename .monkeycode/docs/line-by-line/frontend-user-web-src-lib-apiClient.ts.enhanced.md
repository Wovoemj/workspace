# `frontend/user-web/src/lib/apiClient.ts` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `/** 后端直连地址（SSR 用） */`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `const BACKEND_URL = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 3: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 4: `function apiBaseURL() {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 5: `// SSR: direct-call backend with absolute base URL.`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `if (typeof window === 'undefined') return BACKEND_URL`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 7: `// Browser: hit Next.js origin, rewrites proxy /api/* to backend.`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `return window.location.origin`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 9: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 10: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 11: `type ApiClientOptions = {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 12: `method?: 'GET' | 'POST' | 'PUT' | 'DELETE'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `query?: Record<string, string | number | boolean | undefined | null>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `body?: unknown`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 15: `auth?: boolean`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `/** 管理员认证：?localStorage.admin_token 取值，并直连后端（绕过 Next.js rewrite?*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `adminAuth?: boolean`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `cache?: RequestCache`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `signal?: AbortSignal`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 21: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 22: `function buildUrl(pathOrUrl: string, query?: ApiClientOptions['query']) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 23: `// pathOrUrl: '/api/xxx' or 'https://...'`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 24: `const base = pathOrUrl.startsWith('http') ? '' : apiBaseURL()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 25: `const url = pathOrUrl.startsWith('http') ? new URL(pathOrUrl) : new URL(pathOrUrl, base)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 26: `if (query) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 27: `for (const [k, v] of Object.entries(query)) {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 28: `if (v === undefined || v === null) continue`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 29: `url.searchParams.set(k, String(v))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 31: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 32: `return url.toString()`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 33: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 34: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 35: `export async function apiClient<T>(pathOrUrl: string, options: ApiClientOptions = {}): Promise<T> {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 36: `const {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 37: `method = 'GET',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `query,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `body,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `auth = false,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `adminAuth = false,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `cache = 'no-store',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `signal,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `} = options`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 46: `const headers: Record<string, string> = {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 47: `'Content-Type': 'application/json',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 49: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 50: `if (typeof window !== 'undefined') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 51: `if (adminAuth) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 52: `const token = window.localStorage.getItem('admin_token')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 53: `if (token) headers.Authorization = \`Bearer ${token}\``
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 54: `} else if (auth) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `const token = window.localStorage.getItem('auth_token')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 56: `if (token) headers.Authorization = \`Bearer ${token}\``
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 57: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 58: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 59: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 60: `// 管理员请求直连后端，绕过 Next.js rewrite（rewrite 会丢?Authorization 头）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 61: `let url: string`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 62: `if (adminAuth && typeof window !== 'undefined') {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 63: `const backendBase = BACKEND_URL`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 64: `const parsed = pathOrUrl.startsWith('http')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 65: `? new URL(pathOrUrl)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `: new URL(pathOrUrl, backendBase)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `if (query) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 68: `for (const [k, v] of Object.entries(query)) {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 69: `if (v === undefined || v === null) continue`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 70: `parsed.searchParams.set(k, String(v))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 72: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 73: `url = parsed.toString()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `url = buildUrl(pathOrUrl, query)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 77: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 78: `const res = await fetch(url, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 79: `method,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 80: `headers,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `cache,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `signal,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `body: body === undefined ? undefined : JSON.stringify(body),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 86: `const data = await res.json().catch(() => ({}))`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 87: `if (!res.ok) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 88: `const msg = (data && (data as any).error) || \`HTTP ${res.status}\``
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 89: `throw new Error(msg)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 90: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 91: `return data as T`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 92: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 93: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
