# `backend/gateway/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package main` -> 声明当前文件所属包或模块命名空间。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `import (` -> 导入依赖模块，供当前文件使用。
- 4: `"context"` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"fmt"` -> 执行当前语句，参与该文件整体逻辑。
- 6: `"net/http"` -> 执行当前语句，参与该文件整体逻辑。
- 7: `"net/http/httputil"` -> 执行当前语句，参与该文件整体逻辑。
- 8: `"net/url"` -> 执行当前语句，参与该文件整体逻辑。
- 9: `"strings"` -> 执行当前语句，参与该文件整体逻辑。
- 10: `"time"` -> 执行当前语句，参与该文件整体逻辑。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该文件整体逻辑。
- 13: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该文件整体逻辑。
- 14: `)` -> 结束当前语句或代码块。
- 15: `(空行)` -> 空行，用于提升代码结构可读性。
- 16: `type Config struct {` -> 定义类型或类结构，约束数据与行为。
- 17: `Port        int` -> 执行当前语句，参与该文件整体逻辑。
- 18: `ServiceURLs map[string]string` -> 执行当前语句，参与该文件整体逻辑。
- 19: `}` -> 结束当前语句或代码块。
- 20: `(空行)` -> 空行，用于提升代码结构可读性。
- 21: `type Gateway struct {` -> 定义类型或类结构，约束数据与行为。
- 22: `config     *Config` -> 执行当前语句，参与该文件整体逻辑。
- 23: `logger     *logrus.Logger` -> 执行当前语句，参与该文件整体逻辑。
- 24: `router     *gin.Engine` -> 执行当前语句，参与该文件整体逻辑。
- 25: `httpServer *http.Server` -> 执行当前语句，参与该文件整体逻辑。
- 26: `}` -> 结束当前语句或代码块。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `func NewGateway(config *Config) *Gateway {` -> 定义函数或方法，实现具体业务逻辑。
- 29: `logger := logrus.New()` -> 执行当前语句，参与该文件整体逻辑。
- 30: `logger.SetFormatter(&logrus.TextFormatter{` -> 执行当前语句，参与该文件整体逻辑。
- 31: `FullTimestamp: true,` -> 执行当前语句，参与该文件整体逻辑。
- 32: `})` -> 执行当前语句，参与该文件整体逻辑。
- 33: `logger.SetLevel(logrus.InfoLevel)` -> 执行当前语句，参与该文件整体逻辑。
- 34: `(空行)` -> 空行，用于提升代码结构可读性。
- 35: `router := gin.New()` -> 执行当前语句，参与该文件整体逻辑。
- 36: `router.Use(gin.Recovery())` -> 执行当前语句，参与该文件整体逻辑。
- 37: `router.Use(corsMiddleware())` -> 执行当前语句，参与该文件整体逻辑。
- 38: `router.Use(loggingMiddleware(logger))` -> 执行当前语句，参与该文件整体逻辑。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `return &Gateway{` -> 返回结果或提前结束当前流程。
- 41: `config: config,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `logger: logger,` -> 执行当前语句，参与该文件整体逻辑。
- 43: `router: router,` -> 执行当前语句，参与该文件整体逻辑。
- 44: `}` -> 结束当前语句或代码块。
- 45: `}` -> 结束当前语句或代码块。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `func (g *Gateway) SetupRoutes() {` -> 定义函数或方法，实现具体业务逻辑。
- 48: `// Health check` -> 注释行，用于解释设计意图或使用说明。
- 49: `g.router.GET("/health", func(c *gin.Context) {` -> 执行当前语句，参与该文件整体逻辑。
- 50: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 51: `"status":  "ok",` -> 执行当前语句，参与该文件整体逻辑。
- 52: `"message": "Gateway is running",` -> 执行当前语句，参与该文件整体逻辑。
- 53: `"time":    time.Now().Format(time.RFC3339),` -> 执行当前语句，参与该文件整体逻辑。
- 54: `})` -> 执行当前语句，参与该文件整体逻辑。
- 55: `})` -> 执行当前语句，参与该文件整体逻辑。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `// API routes proxy` -> 注释行，用于解释设计意图或使用说明。
- 58: `api := g.router.Group("/api")` -> 执行当前语句，参与该文件整体逻辑。
- 59: `{` -> 执行当前语句，参与该文件整体逻辑。
- 60: `// Product service proxy (景点数据)` -> 注释行，用于解释设计意图或使用说明。
- 61: `productGroup := api.Group("")` -> 执行当前语句，参与该文件整体逻辑。
- 62: `productGroup.Any("/destinations", g.proxyToService("product-service", ""))` -> 执行当前语句，参与该文件整体逻辑。
- 63: `productGroup.Any("/destinations/:id", g.proxyToService("product-service", ""))` -> 执行当前语句，参与该文件整体逻辑。
- 64: `productGroup.Any("/products", g.proxyToService("product-service", ""))` -> 执行当前语句，参与该文件整体逻辑。
- 65: `productGroup.Any("/products/:id", g.proxyToService("product-service", ""))` -> 执行当前语句，参与该文件整体逻辑。
- 66: `(空行)` -> 空行，用于提升代码结构可读性。
- 67: `// User service proxy - strip /api/users prefix` -> 注释行，用于解释设计意图或使用说明。
- 68: `userGroup := api.Group("/users")` -> 执行当前语句，参与该文件整体逻辑。
- 69: `userGroup.Any("/*path", g.proxyToService("user-service", "/api/users"))` -> 执行当前语句，参与该文件整体逻辑。
- 70: `(空行)` -> 空行，用于提升代码结构可读性。
- 71: `// Favorites proxy` -> 注释行，用于解释设计意图或使用说明。
- 72: `api.Any("/favorites", g.proxyToService("user-service", "/api/favorites"))` -> 执行当前语句，参与该文件整体逻辑。
- 73: `api.Any("/favorites/*path", g.proxyToService("user-service", "/api/favorites"))` -> 执行当前语句，参与该文件整体逻辑。
- 74: `(空行)` -> 空行，用于提升代码结构可读性。
- 75: `// Order service proxy` -> 注释行，用于解释设计意图或使用说明。
- 76: `orderGroup := api.Group("/orders")` -> 执行当前语句，参与该文件整体逻辑。
- 77: `orderGroup.Any("/*path", g.proxyToService("order-service", "/api/orders"))` -> 执行当前语句，参与该文件整体逻辑。
- 78: `(空行)` -> 空行，用于提升代码结构可读性。
- 79: `// AI service proxy` -> 注释行，用于解释设计意图或使用说明。
- 80: `aiGroup := api.Group("/ai")` -> 执行当前语句，参与该文件整体逻辑。
- 81: `{` -> 执行当前语句，参与该文件整体逻辑。
- 82: `// 具体路由必须先注册，通配符路由后注册` -> 注释行，用于解释设计意图或使用说明。
- 83: `aiGroup.Any("/chat", g.proxyToService("ai-service", "/api/ai"))` -> 执行当前语句，参与该文件整体逻辑。
- 84: `(空行)` -> 空行，用于提升代码结构可读性。
- 85: `}` -> 结束当前语句或代码块。
- 86: `(空行)` -> 空行，用于提升代码结构可读性。
- 87: `// Recommendation service proxy` -> 注释行，用于解释设计意图或使用说明。
- 88: `recommendGroup := api.Group("/recommendations")` -> 执行当前语句，参与该文件整体逻辑。
- 89: `recommendGroup.Any("/*path", g.proxyToService("recommend-service", "/api/recommendations"))` -> 执行当前语句，参与该文件整体逻辑。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `// Notification service proxy` -> 注释行，用于解释设计意图或使用说明。
- 92: `notificationGroup := api.Group("/notifications")` -> 执行当前语句，参与该文件整体逻辑。
- 93: `notificationGroup.Any("/*path", g.proxyToService("notification-service", "/api/notifications"))` -> 执行当前语句，参与该文件整体逻辑。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `// Stats endpoint (临时实现)` -> 注释行，用于解释设计意图或使用说明。
- 96: `api.GET("/stats", func(c *gin.Context) {` -> 执行当前语句，参与该文件整体逻辑。
- 97: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 98: `"success": true,` -> 执行当前语句，参与该文件整体逻辑。
- 99: `"stats": gin.H{` -> 执行当前语句，参与该文件整体逻辑。
- 100: `"destinations": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 101: `"users": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 102: `"trips": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 103: `"comments": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 104: `"orders": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 105: `"footprints": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 106: `"pages": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 107: `"notifications": 0,` -> 执行当前语句，参与该文件整体逻辑。
- 108: `},` -> 执行当前语句，参与该文件整体逻辑。
- 109: `})` -> 执行当前语句，参与该文件整体逻辑。
- 110: `})` -> 执行当前语句，参与该文件整体逻辑。
- 111: `}` -> 结束当前语句或代码块。
- 112: `}` -> 结束当前语句或代码块。
- 113: `(空行)` -> 空行，用于提升代码结构可读性。
- 114: `func (g *Gateway) proxyToService(serviceName string, stripPrefix string) gin.HandlerFunc {` -> 定义函数或方法，实现具体业务逻辑。
- 115: `return func(c *gin.Context) {` -> 返回结果或提前结束当前流程。
- 116: `serviceURL, exists := g.config.ServiceURLs[serviceName]` -> 执行当前语句，参与该文件整体逻辑。
- 117: `if !exists {` -> 条件判断分支，根据场景执行不同逻辑。
- 118: `g.logger.Errorf("Service %s not found", serviceName)` -> 执行当前语句，参与该文件整体逻辑。
- 119: `c.JSON(http.StatusNotFound, gin.H{` -> 构造并返回接口响应数据。
- 120: `"error": fmt.Sprintf("Service %s not found", serviceName),` -> 执行当前语句，参与该文件整体逻辑。
- 121: `})` -> 执行当前语句，参与该文件整体逻辑。
- 122: `return` -> 返回结果或提前结束当前流程。
- 123: `}` -> 结束当前语句或代码块。
- 124: `(空行)` -> 空行，用于提升代码结构可读性。
- 125: `target, err := url.Parse(serviceURL)` -> 执行当前语句，参与该文件整体逻辑。
- 126: `if err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 127: `g.logger.Errorf("Invalid service URL: %v", err)` -> 执行当前语句，参与该文件整体逻辑。
- 128: `c.JSON(http.StatusInternalServerError, gin.H{` -> 构造并返回接口响应数据。
- 129: `"error": "Invalid service configuration",` -> 执行当前语句，参与该文件整体逻辑。
- 130: `})` -> 执行当前语句，参与该文件整体逻辑。
- 131: `return` -> 返回结果或提前结束当前流程。
- 132: `}` -> 结束当前语句或代码块。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `// 创建反向代理` -> 注释行，用于解释设计意图或使用说明。
- 135: `proxy := httputil.NewSingleHostReverseProxy(target)` -> 执行当前语句，参与该文件整体逻辑。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `originalPath := c.Request.URL.Path` -> 执行当前语句，参与该文件整体逻辑。
- 138: `(空行)` -> 空行，用于提升代码结构可读性。
- 139: `// 替换路径前缀` -> 注释行，用于解释设计意图或使用说明。
- 140: `if stripPrefix != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 141: `c.Request.URL.Path = strings.Replace(c.Request.URL.Path, stripPrefix, "", 1)` -> 执行当前语句，参与该文件整体逻辑。
- 142: `}` -> 结束当前语句或代码块。
- 143: `c.Request.URL.Host = target.Host` -> 执行当前语句，参与该文件整体逻辑。
- 144: `c.Request.URL.Scheme = target.Scheme` -> 执行当前语句，参与该文件整体逻辑。
- 145: `(空行)` -> 空行，用于提升代码结构可读性。
- 146: `g.logger.Infof("Proxying %s %s -> %s%s",` -> 执行当前语句，参与该文件整体逻辑。
- 147: `c.Request.Method, originalPath, serviceURL, c.Request.URL.Path)` -> 执行当前语句，参与该文件整体逻辑。
- 148: `(空行)` -> 空行，用于提升代码结构可读性。
- 149: `// 代理请求` -> 注释行，用于解释设计意图或使用说明。
- 150: `proxy.ServeHTTP(c.Writer, c.Request)` -> 执行当前语句，参与该文件整体逻辑。
- 151: `}` -> 结束当前语句或代码块。
- 152: `}` -> 结束当前语句或代码块。
- 153: `(空行)` -> 空行，用于提升代码结构可读性。
- 154: `func (g *Gateway) Start() error {` -> 定义函数或方法，实现具体业务逻辑。
- 155: `g.SetupRoutes()` -> 执行当前语句，参与该文件整体逻辑。
- 156: `(空行)` -> 空行，用于提升代码结构可读性。
- 157: `addr := fmt.Sprintf(":%d", g.config.Port)` -> 执行当前语句，参与该文件整体逻辑。
- 158: `g.httpServer = &http.Server{` -> 执行当前语句，参与该文件整体逻辑。
- 159: `Addr:    addr,` -> 执行当前语句，参与该文件整体逻辑。
- 160: `Handler: g.router,` -> 执行当前语句，参与该文件整体逻辑。
- 161: `}` -> 结束当前语句或代码块。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `g.logger.Infof("Starting gateway on port %d", g.config.Port)` -> 执行当前语句，参与该文件整体逻辑。
- 164: `return g.httpServer.ListenAndServe()` -> 返回结果或提前结束当前流程。
- 165: `}` -> 结束当前语句或代码块。
- 166: `(空行)` -> 空行，用于提升代码结构可读性。
- 167: `func (g *Gateway) Stop() error {` -> 定义函数或方法，实现具体业务逻辑。
- 168: `ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)` -> 执行当前语句，参与该文件整体逻辑。
- 169: `defer cancel()` -> 执行当前语句，参与该文件整体逻辑。
- 170: `(空行)` -> 空行，用于提升代码结构可读性。
- 171: `return g.httpServer.Shutdown(ctx)` -> 返回结果或提前结束当前流程。
- 172: `}` -> 结束当前语句或代码块。
- 173: `(空行)` -> 空行，用于提升代码结构可读性。
- 174: `func corsMiddleware() gin.HandlerFunc {` -> 定义函数或方法，实现具体业务逻辑。
- 175: `return func(c *gin.Context) {` -> 返回结果或提前结束当前流程。
- 176: `c.Header("Access-Control-Allow-Origin", "*")` -> 执行当前语句，参与该文件整体逻辑。
- 177: `c.Header("Access-Control-Allow-Methods", "GET, POST, PUT, DELETE, OPTIONS")` -> 执行当前语句，参与该文件整体逻辑。
- 178: `c.Header("Access-Control-Allow-Headers", "Content-Type, Authorization")` -> 执行当前语句，参与该文件整体逻辑。
- 179: `c.Header("Access-Control-Max-Age", "86400")` -> 执行当前语句，参与该文件整体逻辑。
- 180: `(空行)` -> 空行，用于提升代码结构可读性。
- 181: `if c.Request.Method == "OPTIONS" {` -> 条件判断分支，根据场景执行不同逻辑。
- 182: `c.AbortWithStatus(204)` -> 执行当前语句，参与该文件整体逻辑。
- 183: `return` -> 返回结果或提前结束当前流程。
- 184: `}` -> 结束当前语句或代码块。
- 185: `(空行)` -> 空行，用于提升代码结构可读性。
- 186: `c.Next()` -> 执行当前语句，参与该文件整体逻辑。
- 187: `}` -> 结束当前语句或代码块。
- 188: `}` -> 结束当前语句或代码块。
- 189: `(空行)` -> 空行，用于提升代码结构可读性。
- 190: `func loggingMiddleware(logger *logrus.Logger) gin.HandlerFunc {` -> 定义函数或方法，实现具体业务逻辑。
- 191: `return func(c *gin.Context) {` -> 返回结果或提前结束当前流程。
- 192: `start := time.Now()` -> 执行当前语句，参与该文件整体逻辑。
- 193: `(空行)` -> 空行，用于提升代码结构可读性。
- 194: `c.Next()` -> 执行当前语句，参与该文件整体逻辑。
- 195: `(空行)` -> 空行，用于提升代码结构可读性。
- 196: `duration := time.Since(start)` -> 执行当前语句，参与该文件整体逻辑。
- 197: `status := c.Writer.Status()` -> 执行当前语句，参与该文件整体逻辑。
- 198: `(空行)` -> 空行，用于提升代码结构可读性。
- 199: `logger.WithFields(logrus.Fields{` -> 执行当前语句，参与该文件整体逻辑。
- 200: `"method":     c.Request.Method,` -> 执行当前语句，参与该文件整体逻辑。
- 201: `"path":       c.Request.URL.Path,` -> 执行当前语句，参与该文件整体逻辑。
- 202: `"status":     status,` -> 执行当前语句，参与该文件整体逻辑。
- 203: `"duration":   duration,` -> 执行当前语句，参与该文件整体逻辑。
- 204: `"ip":         c.ClientIP(),` -> 执行当前语句，参与该文件整体逻辑。
- 205: `"user_agent": c.Request.UserAgent(),` -> 执行当前语句，参与该文件整体逻辑。
- 206: `}).Info("HTTP request")` -> 执行当前语句，参与该文件整体逻辑。
- 207: `}` -> 结束当前语句或代码块。
- 208: `}` -> 结束当前语句或代码块。
- 209: `(空行)` -> 空行，用于提升代码结构可读性。
- 210: `func main() {` -> 定义函数或方法，实现具体业务逻辑。
- 211: `config := &Config{` -> 执行当前语句，参与该文件整体逻辑。
- 212: `Port: 8080,` -> 执行当前语句，参与该文件整体逻辑。
- 213: `ServiceURLs: map[string]string{` -> 执行当前语句，参与该文件整体逻辑。
- 214: `"user-service":        "http://localhost:8081",` -> 执行当前语句，参与该文件整体逻辑。
- 215: `"product-service":     "http://localhost:8082",` -> 执行当前语句，参与该文件整体逻辑。
- 216: `"order-service":       "http://localhost:8083",` -> 执行当前语句，参与该文件整体逻辑。
- 217: `"ai-service":          "http://localhost:8084",` -> 执行当前语句，参与该文件整体逻辑。
- 218: `"recommend-service":   "http://localhost:8085",` -> 执行当前语句，参与该文件整体逻辑。
- 219: `"notification-service": "http://localhost:8086",` -> 执行当前语句，参与该文件整体逻辑。
- 220: `},` -> 执行当前语句，参与该文件整体逻辑。
- 221: `}` -> 结束当前语句或代码块。
- 222: `(空行)` -> 空行，用于提升代码结构可读性。
- 223: `gateway := NewGateway(config)` -> 执行当前语句，参与该文件整体逻辑。
- 224: `if err := gateway.Start(); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 225: `logrus.Fatal(err)` -> 执行当前语句，参与该文件整体逻辑。
- 226: `}` -> 结束当前语句或代码块。
- 227: `}` -> 结束当前语句或代码块。
