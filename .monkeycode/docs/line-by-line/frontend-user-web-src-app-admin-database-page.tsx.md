# `frontend/user-web/src/app/admin/database/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 管理后台 - 数据库管理模块` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 数据库表列表展示` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 表结构查看（列名、类型、约束）` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 表数据查看（分页）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 数据增删改查` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 管理员权限校验` -> 注释行，用于解释设计意图或使用说明。
- 12: `*` -> 注释行，用于解释设计意图或使用说明。
- 13: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 15: `*` -> 注释行，用于解释设计意图或使用说明。
- 16: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - GET /api/admin/database/tables: 获取所有数据表列表` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - GET /api/admin/database/tables/${name}: 获取表数据` -> 注释行，用于解释设计意图或使用说明。
- 19: `* - POST /api/admin/database/tables/${name}/row: 插入数据` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - PUT /api/admin/database/tables/${name}/row: 更新数据` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - DELETE /api/admin/database/tables/${name}/row: 删除数据` -> 注释行，用于解释设计意图或使用说明。
- 22: `*/` -> 注释行，用于解释设计意图或使用说明。
- 23: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 24: `(空行)` -> 空行，用于提升代码结构可读性。
- 25: `import { useState, useEffect } from 'react'` -> 导入依赖模块，供当前文件使用。
- 26: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 27: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 28: `import { Pencil, Trash2, Plus, X, Check, AlertCircle } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `interface TableColumn {` -> 定义类型或类结构，约束数据与行为。
- 31: `cid: number` -> 执行当前语句，参与该文件整体逻辑。
- 32: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 33: `type: string` -> 执行当前语句，参与该文件整体逻辑。
- 34: `notnull: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 35: `default: any` -> 执行当前语句，参与该文件整体逻辑。
- 36: `pk: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 37: `}` -> 结束当前语句或代码块。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `interface TableInfo {` -> 定义类型或类结构，约束数据与行为。
- 40: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 41: `columns: TableColumn[]` -> 执行当前语句，参与该文件整体逻辑。
- 42: `row_count: number` -> 执行当前语句，参与该文件整体逻辑。
- 43: `}` -> 结束当前语句或代码块。
- 44: `(空行)` -> 空行，用于提升代码结构可读性。
- 45: `interface TableData {` -> 定义类型或类结构，约束数据与行为。
- 46: `table_name: string` -> 执行当前语句，参与该文件整体逻辑。
- 47: `columns: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 48: `data: Record<string, any>[]` -> 执行当前语句，参与该文件整体逻辑。
- 49: `total: number` -> 执行当前语句，参与该文件整体逻辑。
- 50: `page: number` -> 执行当前语句，参与该文件整体逻辑。
- 51: `per_page: number` -> 执行当前语句，参与该文件整体逻辑。
- 52: `}` -> 结束当前语句或代码块。
- 53: `(空行)` -> 空行，用于提升代码结构可读性。
- 54: `export default function DatabasePage() {` -> 导出当前声明，供其他模块复用。
- 55: `const [tables, setTables] = useState<TableInfo[]>([])` -> 声明变量或常量，保存运行时数据。
- 56: `const [selectedTable, setSelectedTable] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 57: `const [tableData, setTableData] = useState<TableData | null>(null)` -> 声明变量或常量，保存运行时数据。
- 58: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 59: `const [loadingData, setLoadingData] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 60: `const [activeTab, setActiveTab] = useState<'tables' | 'structure' | 'data'>('tables')` -> 声明变量或常量，保存运行时数据。
- 61: `const [currentPage, setCurrentPage] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 62: `(空行)` -> 空行，用于提升代码结构可读性。
- 63: `// 编辑/新增状态` -> 注释行，用于解释设计意图或使用说明。
- 64: `const [editMode, setEditMode] = useState<'none' | 'add' | 'edit'>('none')` -> 声明变量或常量，保存运行时数据。
- 65: `const [editRow, setEditRow] = useState<Record<string, any>>({})` -> 声明变量或常量，保存运行时数据。
- 66: `const [originalRow, setOriginalRow] = useState<Record<string, any>>({})` -> 声明变量或常量，保存运行时数据。
- 67: `const [editError, setEditError] = useState('')` -> 声明变量或常量，保存运行时数据。
- 68: `const [editLoading, setEditLoading] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 69: `const [deleteConfirm, setDeleteConfirm] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 70: `(空行)` -> 空行，用于提升代码结构可读性。
- 71: `const adminToken = typeof window !== 'undefined' ? localStorage.getItem('admin_token') : ''` -> 声明变量或常量，保存运行时数据。
- 72: `(空行)` -> 空行，用于提升代码结构可读性。
- 73: `// 获取表列表` -> 注释行，用于解释设计意图或使用说明。
- 74: `const fetchTables = async () => {` -> 声明变量或常量，保存运行时数据。
- 75: `try {` -> 异常处理逻辑，提升程序健壮性。
- 76: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 77: `const res = await fetch('/api/admin/database/tables', {` -> 声明变量或常量，保存运行时数据。
- 78: `headers: { 'Authorization': \`Bearer ${adminToken || ''}\` }` -> 执行当前语句，参与该文件整体逻辑。
- 79: `})` -> 执行当前语句，参与该文件整体逻辑。
- 80: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 81: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 82: `setTables(data.tables)` -> 执行当前语句，参与该文件整体逻辑。
- 83: `}` -> 结束当前语句或代码块。
- 84: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 85: `console.error('获取表列表失败:', e)` -> 执行当前语句，参与该文件整体逻辑。
- 86: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 87: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 88: `}` -> 结束当前语句或代码块。
- 89: `}` -> 结束当前语句或代码块。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `// 获取表数据` -> 注释行，用于解释设计意图或使用说明。
- 92: `const fetchTableData = async (tableName: string, page: number = 1) => {` -> 声明变量或常量，保存运行时数据。
- 93: `try {` -> 异常处理逻辑，提升程序健壮性。
- 94: `setLoadingData(true)` -> 执行当前语句，参与该文件整体逻辑。
- 95: `setCurrentPage(page)` -> 执行当前语句，参与该文件整体逻辑。
- 96: `const res = await fetch(\`/api/admin/database/tables/${tableName}?page=${page}&per_page=50\`, {` -> 声明变量或常量，保存运行时数据。
- 97: `headers: { 'Authorization': \`Bearer ${adminToken || ''}\` }` -> 执行当前语句，参与该文件整体逻辑。
- 98: `})` -> 执行当前语句，参与该文件整体逻辑。
- 99: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 100: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 101: `setTableData(data)` -> 执行当前语句，参与该文件整体逻辑。
- 102: `}` -> 结束当前语句或代码块。
- 103: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 104: `console.error('获取表数据失败:', e)` -> 执行当前语句，参与该文件整体逻辑。
- 105: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 106: `setLoadingData(false)` -> 执行当前语句，参与该文件整体逻辑。
- 107: `}` -> 结束当前语句或代码块。
- 108: `}` -> 结束当前语句或代码块。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 111: `fetchTables()` -> 执行当前语句，参与该文件整体逻辑。
- 112: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 113: `(空行)` -> 空行，用于提升代码结构可读性。
- 114: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 115: `if (selectedTable) {` -> 条件判断分支，根据场景执行不同逻辑。
- 116: `fetchTableData(selectedTable)` -> 执行当前语句，参与该文件整体逻辑。
- 117: `}` -> 结束当前语句或代码块。
- 118: `}, [selectedTable])` -> 执行当前语句，参与该文件整体逻辑。
- 119: `(空行)` -> 空行，用于提升代码结构可读性。
- 120: `const handleTableClick = (tableName: string) => {` -> 声明变量或常量，保存运行时数据。
- 121: `setSelectedTable(tableName)` -> 执行当前语句，参与该文件整体逻辑。
- 122: `setActiveTab('data')` -> 执行当前语句，参与该文件整体逻辑。
- 123: `}` -> 结束当前语句或代码块。
- 124: `(空行)` -> 空行，用于提升代码结构可读性。
- 125: `const selectedTableInfo = tables.find(t => t.name === selectedTable)` -> 声明变量或常量，保存运行时数据。
- 126: `(空行)` -> 空行，用于提升代码结构可读性。
- 127: `// 获取主键列` -> 注释行，用于解释设计意图或使用说明。
- 128: `const getPkColumn = (tableInfo?: TableInfo): string | null => {` -> 声明变量或常量，保存运行时数据。
- 129: `if (!tableInfo) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 130: `const pk = tableInfo.columns.find(c => c.pk)` -> 声明变量或常量，保存运行时数据。
- 131: `return pk ? pk.name : null` -> 返回结果或提前结束当前流程。
- 132: `}` -> 结束当前语句或代码块。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `// 开始新增` -> 注释行，用于解释设计意图或使用说明。
- 135: `const handleAdd = () => {` -> 声明变量或常量，保存运行时数据。
- 136: `if (!selectedTableInfo) return` -> 条件判断分支，根据场景执行不同逻辑。
- 137: `const emptyRow: Record<string, any> = {}` -> 声明变量或常量，保存运行时数据。
- 138: `selectedTableInfo.columns.forEach(col => {` -> 执行当前语句，参与该文件整体逻辑。
- 139: `emptyRow[col.name] = col.default ?? ''` -> 执行当前语句，参与该文件整体逻辑。
- 140: `})` -> 执行当前语句，参与该文件整体逻辑。
- 141: `setEditRow(emptyRow)` -> 执行当前语句，参与该文件整体逻辑。
- 142: `setOriginalRow({})` -> 执行当前语句，参与该文件整体逻辑。
- 143: `setEditMode('add')` -> 执行当前语句，参与该文件整体逻辑。
- 144: `setEditError('')` -> 执行当前语句，参与该文件整体逻辑。
- 145: `}` -> 结束当前语句或代码块。
- 146: `(空行)` -> 空行，用于提升代码结构可读性。
- 147: `// 开始编辑` -> 注释行，用于解释设计意图或使用说明。
- 148: `const handleEdit = (row: Record<string, any>) => {` -> 声明变量或常量，保存运行时数据。
- 149: `const copy = { ...row }` -> 声明变量或常量，保存运行时数据。
- 150: `// 尝试解析 JSON 字符串以便编辑` -> 注释行，用于解释设计意图或使用说明。
- 151: `selectedTableInfo?.columns.forEach(col => {` -> 执行当前语句，参与该文件整体逻辑。
- 152: `if (col.type?.toLowerCase().includes('text') && typeof copy[col.name] === 'string') {` -> 条件判断分支，根据场景执行不同逻辑。
- 153: `try {` -> 异常处理逻辑，提升程序健壮性。
- 154: `const parsed = JSON.parse(copy[col.name])` -> 声明变量或常量，保存运行时数据。
- 155: `if (typeof parsed === 'object') {` -> 条件判断分支，根据场景执行不同逻辑。
- 156: `copy[col.name] = JSON.stringify(parsed, null, 2)` -> 执行当前语句，参与该文件整体逻辑。
- 157: `}` -> 结束当前语句或代码块。
- 158: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 159: `// 不是 JSON，保持原样` -> 注释行，用于解释设计意图或使用说明。
- 160: `}` -> 结束当前语句或代码块。
- 161: `}` -> 结束当前语句或代码块。
- 162: `})` -> 执行当前语句，参与该文件整体逻辑。
- 163: `// 密码哈希不在编辑框显示，留空表示不修改` -> 注释行，用于解释设计意图或使用说明。
- 164: `if (copy.password_hash !== undefined) copy.password_hash = ''` -> 条件判断分支，根据场景执行不同逻辑。
- 165: `if (copy.password !== undefined) copy.password = ''` -> 条件判断分支，根据场景执行不同逻辑。
- 166: `setEditRow(copy)` -> 执行当前语句，参与该文件整体逻辑。
- 167: `setOriginalRow({ ...row })` -> 执行当前语句，参与该文件整体逻辑。
- 168: `setEditMode('edit')` -> 执行当前语句，参与该文件整体逻辑。
- 169: `setEditError('')` -> 执行当前语句，参与该文件整体逻辑。
- 170: `}` -> 结束当前语句或代码块。
- 171: `(空行)` -> 空行，用于提升代码结构可读性。
- 172: `// 保存（新增或更新）` -> 注释行，用于解释设计意图或使用说明。
- 173: `const handleSave = async () => {` -> 声明变量或常量，保存运行时数据。
- 174: `if (!selectedTable) return` -> 条件判断分支，根据场景执行不同逻辑。
- 175: `setEditLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 176: `setEditError('')` -> 执行当前语句，参与该文件整体逻辑。
- 177: `(空行)` -> 空行，用于提升代码结构可读性。
- 178: `try {` -> 异常处理逻辑，提升程序健壮性。
- 179: `const pkCol = getPkColumn(selectedTableInfo)` -> 声明变量或常量，保存运行时数据。
- 180: `(空行)` -> 空行，用于提升代码结构可读性。
- 181: `if (editMode === 'add') {` -> 条件判断分支，根据场景执行不同逻辑。
- 182: `// 过滤空值` -> 注释行，用于解释设计意图或使用说明。
- 183: `const values: Record<string, any> = {}` -> 声明变量或常量，保存运行时数据。
- 184: `Object.entries(editRow).forEach(([k, v]) => {` -> 执行当前语句，参与该文件整体逻辑。
- 185: `if (v !== '' && v !== null && v !== undefined) {` -> 条件判断分支，根据场景执行不同逻辑。
- 186: `// 尝试解析 JSON` -> 注释行，用于解释设计意图或使用说明。
- 187: `if (typeof v === 'string' && (v.startsWith('{') || v.startsWith('['))) {` -> 条件判断分支，根据场景执行不同逻辑。
- 188: `try { JSON.parse(v) } catch { /* 不是 JSON */ }` -> 异常处理逻辑，提升程序健壮性。
- 189: `}` -> 结束当前语句或代码块。
- 190: `values[k] = v` -> 执行当前语句，参与该文件整体逻辑。
- 191: `}` -> 结束当前语句或代码块。
- 192: `})` -> 执行当前语句，参与该文件整体逻辑。
- 193: `(空行)` -> 空行，用于提升代码结构可读性。
- 194: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {` -> 声明变量或常量，保存运行时数据。
- 195: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 196: `headers: {` -> 执行当前语句，参与该文件整体逻辑。
- 197: `'Content-Type': 'application/json',` -> 执行当前语句，参与该文件整体逻辑。
- 198: `'Authorization': \`Bearer ${adminToken || ''}\`` -> 执行当前语句，参与该文件整体逻辑。
- 199: `},` -> 执行当前语句，参与该文件整体逻辑。
- 200: `body: JSON.stringify(values)` -> 执行当前语句，参与该文件整体逻辑。
- 201: `})` -> 执行当前语句，参与该文件整体逻辑。
- 202: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 203: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 204: `setEditMode('none')` -> 执行当前语句，参与该文件整体逻辑。
- 205: `fetchTableData(selectedTable, currentPage)` -> 执行当前语句，参与该文件整体逻辑。
- 206: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 207: `setEditError(data.error || '插入失败')` -> 执行当前语句，参与该文件整体逻辑。
- 208: `}` -> 结束当前语句或代码块。
- 209: `} else if (editMode === 'edit') {` -> 执行当前语句，参与该文件整体逻辑。
- 210: `const where: Record<string, any> = {}` -> 声明变量或常量，保存运行时数据。
- 211: `if (pkCol && originalRow[pkCol] !== undefined) {` -> 条件判断分支，根据场景执行不同逻辑。
- 212: `where[pkCol] = originalRow[pkCol]` -> 执行当前语句，参与该文件整体逻辑。
- 213: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 214: `// 没有主键，用所有原始值做 WHERE` -> 注释行，用于解释设计意图或使用说明。
- 215: `Object.entries(originalRow).forEach(([k, v]) => {` -> 执行当前语句，参与该文件整体逻辑。
- 216: `if (v !== null && v !== undefined) where[k] = v` -> 条件判断分支，根据场景执行不同逻辑。
- 217: `})` -> 执行当前语句，参与该文件整体逻辑。
- 218: `}` -> 结束当前语句或代码块。
- 219: `(空行)` -> 空行，用于提升代码结构可读性。
- 220: `const values: Record<string, any> = {}` -> 声明变量或常量，保存运行时数据。
- 221: `Object.entries(editRow).forEach(([k, v]) => {` -> 执行当前语句，参与该文件整体逻辑。
- 222: `if (v !== '' && v !== null && v !== undefined) {` -> 条件判断分支，根据场景执行不同逻辑。
- 223: `values[k] = v` -> 执行当前语句，参与该文件整体逻辑。
- 224: `}` -> 结束当前语句或代码块。
- 225: `})` -> 执行当前语句，参与该文件整体逻辑。
- 226: `(空行)` -> 空行，用于提升代码结构可读性。
- 227: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {` -> 声明变量或常量，保存运行时数据。
- 228: `method: 'PUT',` -> 执行当前语句，参与该文件整体逻辑。
- 229: `headers: {` -> 执行当前语句，参与该文件整体逻辑。
- 230: `'Content-Type': 'application/json',` -> 执行当前语句，参与该文件整体逻辑。
- 231: `'Authorization': \`Bearer ${adminToken || ''}\`` -> 执行当前语句，参与该文件整体逻辑。
- 232: `},` -> 执行当前语句，参与该文件整体逻辑。
- 233: `body: JSON.stringify({ where, values })` -> 执行当前语句，参与该文件整体逻辑。
- 234: `})` -> 执行当前语句，参与该文件整体逻辑。
- 235: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 236: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 237: `setEditMode('none')` -> 执行当前语句，参与该文件整体逻辑。
- 238: `fetchTableData(selectedTable, currentPage)` -> 执行当前语句，参与该文件整体逻辑。
- 239: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 240: `setEditError(data.error || '更新失败')` -> 执行当前语句，参与该文件整体逻辑。
- 241: `}` -> 结束当前语句或代码块。
- 242: `}` -> 结束当前语句或代码块。
- 243: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 244: `setEditError(String(e))` -> 执行当前语句，参与该文件整体逻辑。
- 245: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 246: `setEditLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 247: `}` -> 结束当前语句或代码块。
- 248: `}` -> 结束当前语句或代码块。
- 249: `(空行)` -> 空行，用于提升代码结构可读性。
- 250: `// 删除` -> 注释行，用于解释设计意图或使用说明。
- 251: `const handleDelete = async (row: Record<string, any>) => {` -> 声明变量或常量，保存运行时数据。
- 252: `if (!selectedTable) return` -> 条件判断分支，根据场景执行不同逻辑。
- 253: `setEditLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 254: `try {` -> 异常处理逻辑，提升程序健壮性。
- 255: `const pkCol = getPkColumn(selectedTableInfo)` -> 声明变量或常量，保存运行时数据。
- 256: `const where: Record<string, any> = {}` -> 声明变量或常量，保存运行时数据。
- 257: `if (pkCol && row[pkCol] !== undefined) {` -> 条件判断分支，根据场景执行不同逻辑。
- 258: `where[pkCol] = row[pkCol]` -> 执行当前语句，参与该文件整体逻辑。
- 259: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 260: `Object.entries(row).forEach(([k, v]) => {` -> 执行当前语句，参与该文件整体逻辑。
- 261: `if (v !== null && v !== undefined) where[k] = v` -> 条件判断分支，根据场景执行不同逻辑。
- 262: `})` -> 执行当前语句，参与该文件整体逻辑。
- 263: `}` -> 结束当前语句或代码块。
- 264: `(空行)` -> 空行，用于提升代码结构可读性。
- 265: `const res = await fetch(\`/api/admin/database/tables/${selectedTable}/row\`, {` -> 声明变量或常量，保存运行时数据。
- 266: `method: 'DELETE',` -> 执行当前语句，参与该文件整体逻辑。
- 267: `headers: {` -> 执行当前语句，参与该文件整体逻辑。
- 268: `'Content-Type': 'application/json',` -> 执行当前语句，参与该文件整体逻辑。
- 269: `'Authorization': \`Bearer ${adminToken || ''}\`` -> 执行当前语句，参与该文件整体逻辑。
- 270: `},` -> 执行当前语句，参与该文件整体逻辑。
- 271: `body: JSON.stringify(where)` -> 执行当前语句，参与该文件整体逻辑。
- 272: `})` -> 执行当前语句，参与该文件整体逻辑。
- 273: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 274: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 275: `setDeleteConfirm(null)` -> 执行当前语句，参与该文件整体逻辑。
- 276: `fetchTableData(selectedTable, currentPage)` -> 执行当前语句，参与该文件整体逻辑。
- 277: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 278: `alert(data.error || '删除失败')` -> 执行当前语句，参与该文件整体逻辑。
- 279: `}` -> 结束当前语句或代码块。
- 280: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 281: `alert(String(e))` -> 执行当前语句，参与该文件整体逻辑。
- 282: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 283: `setEditLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 284: `}` -> 结束当前语句或代码块。
- 285: `}` -> 结束当前语句或代码块。
- 286: `(空行)` -> 空行，用于提升代码结构可读性。
- 287: `// 编辑表单输入处理` -> 注释行，用于解释设计意图或使用说明。
- 288: `const handleInputChange = (colName: string, value: string) => {` -> 声明变量或常量，保存运行时数据。
- 289: `setEditRow(prev => ({ ...prev, [colName]: value }))` -> 执行当前语句，参与该文件整体逻辑。
- 290: `}` -> 结束当前语句或代码块。
- 291: `(空行)` -> 空行，用于提升代码结构可读性。
- 292: `return (` -> 返回结果或提前结束当前流程。
- 293: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 294: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 295: `<main className="pt-24">` -> JSX/HTML 结构行，用于描述页面元素。
- 296: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 297: `(空行)` -> 空行，用于提升代码结构可读性。
- 298: `{/* 页面标题 */}` -> 执行当前语句，参与该文件整体逻辑。
- 299: `<div className="text-center mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `<h1 className="text-3xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">` -> JSX/HTML 结构行，用于描述页面元素。
- 301: `数据库管理` -> 执行当前语句，参与该文件整体逻辑。
- 302: `</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 303: `<p className="text-gray-500 mt-2">查看和管理数据库表结构与数据</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 304: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 305: `(空行)` -> 空行，用于提升代码结构可读性。
- 306: `<div className="card-glass rounded-2xl overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 307: `{/* 标签页 */}` -> 执行当前语句，参与该文件整体逻辑。
- 308: `<div className="flex border-b border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 309: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 310: `onClick={() => setActiveTab('tables')}` -> 执行当前语句，参与该文件整体逻辑。
- 311: `className={\`px-6 py-4 font-medium transition-colors ${` -> 执行当前语句，参与该文件整体逻辑。
- 312: `activeTab === 'tables'` -> 执行当前语句，参与该文件整体逻辑。
- 313: `? 'text-indigo-600 border-b-2 border-indigo-600'` -> 执行当前语句，参与该文件整体逻辑。
- 314: `: 'text-gray-500 hover:text-gray-700'` -> 执行当前语句，参与该文件整体逻辑。
- 315: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 316: `>` -> 执行当前语句，参与该文件整体逻辑。
- 317: `所有表 ({tables.length})` -> 执行当前语句，参与该文件整体逻辑。
- 318: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 319: `{selectedTable && (` -> 执行当前语句，参与该文件整体逻辑。
- 320: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 321: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 322: `onClick={() => setActiveTab('structure')}` -> 执行当前语句，参与该文件整体逻辑。
- 323: `className={\`px-6 py-4 font-medium transition-colors ${` -> 执行当前语句，参与该文件整体逻辑。
- 324: `activeTab === 'structure'` -> 执行当前语句，参与该文件整体逻辑。
- 325: `? 'text-indigo-600 border-b-2 border-indigo-600'` -> 执行当前语句，参与该文件整体逻辑。
- 326: `: 'text-gray-500 hover:text-gray-700'` -> 执行当前语句，参与该文件整体逻辑。
- 327: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 328: `>` -> 执行当前语句，参与该文件整体逻辑。
- 329: `表结构` -> 执行当前语句，参与该文件整体逻辑。
- 330: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 331: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 332: `onClick={() => setActiveTab('data')}` -> 执行当前语句，参与该文件整体逻辑。
- 333: `className={\`px-6 py-4 font-medium transition-colors ${` -> 执行当前语句，参与该文件整体逻辑。
- 334: `activeTab === 'data'` -> 执行当前语句，参与该文件整体逻辑。
- 335: `? 'text-indigo-600 border-b-2 border-indigo-600'` -> 执行当前语句，参与该文件整体逻辑。
- 336: `: 'text-gray-500 hover:text-gray-700'` -> 执行当前语句，参与该文件整体逻辑。
- 337: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 338: `>` -> 执行当前语句，参与该文件整体逻辑。
- 339: `数据` -> 执行当前语句，参与该文件整体逻辑。
- 340: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 341: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 342: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 343: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 344: `(空行)` -> 空行，用于提升代码结构可读性。
- 345: `{/* 内容区 */}` -> 执行当前语句，参与该文件整体逻辑。
- 346: `<div className="p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 347: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 348: `<div className="flex justify-center py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 349: `<div className="w-8 h-8 border-3 border-indigo-600 border-t-transparent rounded-full animate-spin"></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 350: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 351: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 352: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 353: `{/* 表列表 */}` -> 执行当前语句，参与该文件整体逻辑。
- 354: `{activeTab === 'tables' && (` -> 执行当前语句，参与该文件整体逻辑。
- 355: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 356: `{tables.map((table) => (` -> 执行当前语句，参与该文件整体逻辑。
- 357: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 358: `key={table.name}` -> 执行当前语句，参与该文件整体逻辑。
- 359: `onClick={() => handleTableClick(table.name)}` -> 执行当前语句，参与该文件整体逻辑。
- 360: `className={\`p-5 rounded-xl border-2 cursor-pointer transition-all hover:scale-[1.02] ${` -> 执行当前语句，参与该文件整体逻辑。
- 361: `selectedTable === table.name` -> 执行当前语句，参与该文件整体逻辑。
- 362: `? 'border-indigo-400 bg-indigo-50 shadow-md'` -> 执行当前语句，参与该文件整体逻辑。
- 363: `: 'border-gray-100 bg-white hover:border-indigo-200 hover:shadow-sm'` -> 执行当前语句，参与该文件整体逻辑。
- 364: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 365: `>` -> 执行当前语句，参与该文件整体逻辑。
- 366: `<div className="flex items-center gap-3 mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 367: `<div className="w-10 h-10 rounded-lg bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `<svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 370: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 371: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 373: `<h3 className="font-semibold text-gray-900">{table.name}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 374: `<span className="text-xs text-gray-400">{table.row_count} 行</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 375: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 376: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 377: `<div className="text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 378: `{table.columns.length} 个字段` -> 执行当前语句，参与该文件整体逻辑。
- 379: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `<div className="mt-2 flex flex-wrap gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `{table.columns.slice(0, 3).map((col) => (` -> 执行当前语句，参与该文件整体逻辑。
- 382: `<span` -> 执行当前语句，参与该文件整体逻辑。
- 383: `key={col.name}` -> 执行当前语句，参与该文件整体逻辑。
- 384: `className="inline-block px-2 py-0.5 text-xs bg-gray-100 text-gray-600 rounded"` -> 执行当前语句，参与该文件整体逻辑。
- 385: `>` -> 执行当前语句，参与该文件整体逻辑。
- 386: `{col.name}` -> 执行当前语句，参与该文件整体逻辑。
- 387: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 388: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 389: `{table.columns.length > 3 && (` -> 执行当前语句，参与该文件整体逻辑。
- 390: `<span className="inline-block px-2 py-0.5 text-xs bg-gray-100 text-gray-400 rounded">` -> JSX/HTML 结构行，用于描述页面元素。
- 391: `+{table.columns.length - 3}` -> 执行当前语句，参与该文件整体逻辑。
- 392: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 393: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 394: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 395: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 396: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 397: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 398: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 399: `(空行)` -> 空行，用于提升代码结构可读性。
- 400: `{/* 表结构 */}` -> 执行当前语句，参与该文件整体逻辑。
- 401: `{activeTab === 'structure' && selectedTableInfo && (` -> 执行当前语句，参与该文件整体逻辑。
- 402: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 403: `<div className="flex items-center justify-between mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 404: `<h2 className="text-lg font-semibold text-gray-900">` -> JSX/HTML 结构行，用于描述页面元素。
- 405: `{selectedTableInfo.name} 表结构` -> 执行当前语句，参与该文件整体逻辑。
- 406: `</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 407: `<span className="text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 408: `{selectedTableInfo.columns.length} 个字段 · {selectedTableInfo.row_count} 行数据` -> 执行当前语句，参与该文件整体逻辑。
- 409: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 410: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 411: `<div className="overflow-x-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 412: `<table className="w-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 413: `<thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 414: `<tr className="bg-gray-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 415: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">字段名</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 416: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">类型</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 417: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase">非空</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 418: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase">主键</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 419: `<th className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase">默认值</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 420: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 421: `</thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 422: `<tbody className="divide-y divide-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 423: `{selectedTableInfo.columns.map((col) => (` -> 执行当前语句，参与该文件整体逻辑。
- 424: `<tr key={col.name} className="hover:bg-gray-50 transition-colors">` -> JSX/HTML 结构行，用于描述页面元素。
- 425: `<td className="px-4 py-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 426: `<span className="font-mono text-sm text-indigo-600 font-medium">` -> JSX/HTML 结构行，用于描述页面元素。
- 427: `{col.name}` -> 执行当前语句，参与该文件整体逻辑。
- 428: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 429: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 430: `<td className="px-4 py-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 431: `<span className="font-mono text-xs bg-gray-100 px-2 py-1 rounded text-gray-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 432: `{col.type || 'ANY'}` -> 执行当前语句，参与该文件整体逻辑。
- 433: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 434: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 435: `<td className="px-4 py-3 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 436: `{col.notnull ? (` -> 执行当前语句，参与该文件整体逻辑。
- 437: `<span className="text-red-500">✓</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 438: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 439: `<span className="text-gray-300">-</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 440: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 441: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 442: `<td className="px-4 py-3 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 443: `{col.pk ? (` -> 执行当前语句，参与该文件整体逻辑。
- 444: `<span className="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 445: `PK` -> 执行当前语句，参与该文件整体逻辑。
- 446: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 447: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 448: `<span className="text-gray-300">-</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 449: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 450: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 451: `<td className="px-4 py-3 text-sm text-gray-500 font-mono">` -> JSX/HTML 结构行，用于描述页面元素。
- 452: `{col.default ?? '-'}` -> 执行当前语句，参与该文件整体逻辑。
- 453: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 454: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 455: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 456: `</tbody>` -> JSX/HTML 结构行，用于描述页面元素。
- 457: `</table>` -> JSX/HTML 结构行，用于描述页面元素。
- 458: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 459: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 460: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 461: `(空行)` -> 空行，用于提升代码结构可读性。
- 462: `{/* 表数据 */}` -> 执行当前语句，参与该文件整体逻辑。
- 463: `{activeTab === 'data' && (` -> 执行当前语句，参与该文件整体逻辑。
- 464: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 465: `{loadingData ? (` -> 执行当前语句，参与该文件整体逻辑。
- 466: `<div className="flex justify-center py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 467: `<div className="w-8 h-8 border-3 border-indigo-600 border-t-transparent rounded-full animate-spin"></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 468: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 469: `) : tableData ? (` -> 执行当前语句，参与该文件整体逻辑。
- 470: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 471: `<div className="flex items-center justify-between mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 472: `<h2 className="text-lg font-semibold text-gray-900">` -> JSX/HTML 结构行，用于描述页面元素。
- 473: `{tableData.table_name} 数据` -> 执行当前语句，参与该文件整体逻辑。
- 474: `</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 475: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 476: `<span className="text-sm text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `共 {tableData.total} 条记录` -> 执行当前语句，参与该文件整体逻辑。
- 478: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 479: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 480: `onClick={handleAdd}` -> 执行当前语句，参与该文件整体逻辑。
- 481: `className="inline-flex items-center gap-1.5 px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 482: `>` -> 执行当前语句，参与该文件整体逻辑。
- 483: `<Plus className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 484: `新增数据` -> 执行当前语句，参与该文件整体逻辑。
- 485: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 486: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 487: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 488: `(空行)` -> 空行，用于提升代码结构可读性。
- 489: `<div className="overflow-x-auto rounded-xl border border-gray-200">` -> JSX/HTML 结构行，用于描述页面元素。
- 490: `<table className="w-full text-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 491: `<thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `<tr className="bg-gradient-to-r from-indigo-50 to-purple-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 493: `{tableData.columns.map((col) => (` -> 执行当前语句，参与该文件整体逻辑。
- 494: `<th` -> 执行当前语句，参与该文件整体逻辑。
- 495: `key={col}` -> 执行当前语句，参与该文件整体逻辑。
- 496: `className="px-4 py-3 text-left text-xs font-semibold text-gray-600 uppercase whitespace-nowrap"` -> 执行当前语句，参与该文件整体逻辑。
- 497: `>` -> 执行当前语句，参与该文件整体逻辑。
- 498: `{col}` -> 执行当前语句，参与该文件整体逻辑。
- 499: `</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 500: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 501: `<th className="px-4 py-3 text-center text-xs font-semibold text-gray-600 uppercase whitespace-nowrap w-24">` -> JSX/HTML 结构行，用于描述页面元素。
- 502: `操作` -> 执行当前语句，参与该文件整体逻辑。
- 503: `</th>` -> JSX/HTML 结构行，用于描述页面元素。
- 504: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 505: `</thead>` -> JSX/HTML 结构行，用于描述页面元素。
- 506: `<tbody className="divide-y divide-gray-100 bg-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 507: `{tableData.data.map((row, idx) => (` -> 执行当前语句，参与该文件整体逻辑。
- 508: `<tr key={idx} className="hover:bg-gray-50 transition-colors group">` -> JSX/HTML 结构行，用于描述页面元素。
- 509: `{tableData.columns.map((col) => (` -> 执行当前语句，参与该文件整体逻辑。
- 510: `<td` -> 执行当前语句，参与该文件整体逻辑。
- 511: `key={col}` -> 执行当前语句，参与该文件整体逻辑。
- 512: `className="px-4 py-2 text-gray-700 font-mono text-xs max-w-xs truncate"` -> 执行当前语句，参与该文件整体逻辑。
- 513: `title={String(row[col] ?? '')}` -> 执行当前语句，参与该文件整体逻辑。
- 514: `>` -> 执行当前语句，参与该文件整体逻辑。
- 515: `{formatCellValue(row[col])}` -> 执行当前语句，参与该文件整体逻辑。
- 516: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 517: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 518: `<td className="px-4 py-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 519: `<div className="flex items-center justify-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity">` -> JSX/HTML 结构行，用于描述页面元素。
- 520: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 521: `onClick={() => handleEdit(row)}` -> 执行当前语句，参与该文件整体逻辑。
- 522: `className="p-1.5 text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 523: `title="编辑"` -> 执行当前语句，参与该文件整体逻辑。
- 524: `>` -> 执行当前语句，参与该文件整体逻辑。
- 525: `<Pencil className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 526: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 527: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 528: `onClick={() => setDeleteConfirm(String(idx))}` -> 执行当前语句，参与该文件整体逻辑。
- 529: `className="p-1.5 text-red-600 hover:bg-red-50 rounded-lg transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 530: `title="删除"` -> 执行当前语句，参与该文件整体逻辑。
- 531: `>` -> 执行当前语句，参与该文件整体逻辑。
- 532: `<Trash2 className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 533: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 534: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 535: `</td>` -> JSX/HTML 结构行，用于描述页面元素。
- 536: `</tr>` -> JSX/HTML 结构行，用于描述页面元素。
- 537: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 538: `</tbody>` -> JSX/HTML 结构行，用于描述页面元素。
- 539: `</table>` -> JSX/HTML 结构行，用于描述页面元素。
- 540: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 541: `(空行)` -> 空行，用于提升代码结构可读性。
- 542: `{/* 分页 */}` -> 执行当前语句，参与该文件整体逻辑。
- 543: `{tableData.total > tableData.per_page && (` -> 执行当前语句，参与该文件整体逻辑。
- 544: `<div className="flex items-center justify-center gap-2 mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 545: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 546: `onClick={() => fetchTableData(selectedTable!, currentPage - 1)}` -> 执行当前语句，参与该文件整体逻辑。
- 547: `disabled={currentPage <= 1}` -> 执行当前语句，参与该文件整体逻辑。
- 548: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-200 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 549: `>` -> 执行当前语句，参与该文件整体逻辑。
- 550: `上一页` -> 执行当前语句，参与该文件整体逻辑。
- 551: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 552: `<span className="px-4 py-2 text-sm text-gray-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 553: `第 {currentPage} / {Math.ceil(tableData.total / tableData.per_page)} 页` -> 执行当前语句，参与该文件整体逻辑。
- 554: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 555: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 556: `onClick={() => fetchTableData(selectedTable!, currentPage + 1)}` -> 执行当前语句，参与该文件整体逻辑。
- 557: `disabled={currentPage >= Math.ceil(tableData.total / tableData.per_page)}` -> 执行当前语句，参与该文件整体逻辑。
- 558: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-200 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 559: `>` -> 执行当前语句，参与该文件整体逻辑。
- 560: `下一页` -> 执行当前语句，参与该文件整体逻辑。
- 561: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 562: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 563: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 564: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 565: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 566: `<div className="text-center py-12 text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 567: `选择一个表查看数据` -> 执行当前语句，参与该文件整体逻辑。
- 568: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 569: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 570: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 571: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 572: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 573: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 574: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 575: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 576: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 577: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 578: `(空行)` -> 空行，用于提升代码结构可读性。
- 579: `{/* 编辑/新增弹窗 */}` -> 执行当前语句，参与该文件整体逻辑。
- 580: `{editMode !== 'none' && selectedTableInfo && (` -> 执行当前语句，参与该文件整体逻辑。
- 581: `<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 582: `<div className="bg-white rounded-2xl shadow-xl w-full max-w-2xl max-h-[85vh] overflow-hidden flex flex-col">` -> JSX/HTML 结构行，用于描述页面元素。
- 583: `{/* 弹窗头部 */}` -> 执行当前语句，参与该文件整体逻辑。
- 584: `<div className="flex items-center justify-between px-6 py-4 border-b border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 585: `<h3 className="text-lg font-semibold text-gray-900">` -> JSX/HTML 结构行，用于描述页面元素。
- 586: `{editMode === 'add' ? '新增数据' : '编辑数据'} — {selectedTableInfo.name}` -> 执行当前语句，参与该文件整体逻辑。
- 587: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 588: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 589: `onClick={() => setEditMode('none')}` -> 执行当前语句，参与该文件整体逻辑。
- 590: `className="p-2 text-gray-400 hover:text-gray-600 hover:bg-gray-100 rounded-lg transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 591: `>` -> 执行当前语句，参与该文件整体逻辑。
- 592: `<X className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 593: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 594: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 595: `(空行)` -> 空行，用于提升代码结构可读性。
- 596: `{/* 弹窗内容 */}` -> 执行当前语句，参与该文件整体逻辑。
- 597: `<div className="flex-1 overflow-y-auto px-6 py-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 598: `{editError && (` -> 执行当前语句，参与该文件整体逻辑。
- 599: `<div className="mb-4 flex items-center gap-2 px-4 py-3 bg-red-50 text-red-700 rounded-lg text-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 600: `<AlertCircle className="h-4 w-4 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 601: `{editError}` -> 执行当前语句，参与该文件整体逻辑。
- 602: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 603: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 604: `(空行)` -> 空行，用于提升代码结构可读性。
- 605: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 606: `{selectedTableInfo.columns.map((col) => (` -> 执行当前语句，参与该文件整体逻辑。
- 607: `<div key={col.name}>` -> JSX/HTML 结构行，用于描述页面元素。
- 608: `<label className="block text-sm font-medium text-gray-700 mb-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 609: `{col.name}` -> 执行当前语句，参与该文件整体逻辑。
- 610: `{col.pk && <span className="ml-1.5 px-1.5 py-0.5 text-[10px] bg-yellow-100 text-yellow-700 rounded-full">主键</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 611: `{col.notnull && <span className="ml-1.5 text-red-500">*</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 612: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 613: `{col.name === 'password_hash' || col.name === 'password' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 614: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 615: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 616: `type="password"` -> 执行当前语句，参与该文件整体逻辑。
- 617: `value={editRow[col.name] ?? ''}` -> 执行当前语句，参与该文件整体逻辑。
- 618: `onChange={(e) => handleInputChange(col.name, e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 619: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"` -> 执行当前语句，参与该文件整体逻辑。
- 620: `placeholder={editMode === 'edit' ? '留空则不修改密码，输入新密码将自动加密' : '请输入密码'}` -> 执行当前语句，参与该文件整体逻辑。
- 621: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 622: `<span className="text-xs text-gray-400 mt-0.5">{col.type} · 输入明文密码，保存时自动哈希</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 623: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 624: `) : col.type?.toLowerCase().includes('text') && (editRow[col.name] === null || String(editRow[col.name] ?? '').length > 50) ? (` -> 执行当前语句，参与该文件整体逻辑。
- 625: `<textarea` -> 执行当前语句，参与该文件整体逻辑。
- 626: `value={editRow[col.name] ?? ''}` -> 执行当前语句，参与该文件整体逻辑。
- 627: `onChange={(e) => handleInputChange(col.name, e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 628: `rows={3}` -> 执行当前语句，参与该文件整体逻辑。
- 629: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"` -> 执行当前语句，参与该文件整体逻辑。
- 630: `placeholder={col.default ?? ''}` -> 执行当前语句，参与该文件整体逻辑。
- 631: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 632: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 633: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 634: `type={col.type?.toLowerCase().includes('int') ? 'number' : 'text'}` -> 执行当前语句，参与该文件整体逻辑。
- 635: `value={editRow[col.name] ?? ''}` -> 执行当前语句，参与该文件整体逻辑。
- 636: `onChange={(e) => handleInputChange(col.name, e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 637: `className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none text-sm font-mono"` -> 执行当前语句，参与该文件整体逻辑。
- 638: `placeholder={col.default ?? ''}` -> 执行当前语句，参与该文件整体逻辑。
- 639: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 640: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 641: `{!(col.name === 'password_hash' || col.name === 'password') && (` -> 执行当前语句，参与该文件整体逻辑。
- 642: `<span className="text-xs text-gray-400 mt-0.5">{col.type}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 643: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 644: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 645: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 646: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 647: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 648: `(空行)` -> 空行，用于提升代码结构可读性。
- 649: `{/* 弹窗底部 */}` -> 执行当前语句，参与该文件整体逻辑。
- 650: `<div className="flex items-center justify-end gap-3 px-6 py-4 border-t border-gray-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 651: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 652: `onClick={() => setEditMode('none')}` -> 执行当前语句，参与该文件整体逻辑。
- 653: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 654: `>` -> 执行当前语句，参与该文件整体逻辑。
- 655: `取消` -> 执行当前语句，参与该文件整体逻辑。
- 656: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 657: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 658: `onClick={handleSave}` -> 执行当前语句，参与该文件整体逻辑。
- 659: `disabled={editLoading}` -> 执行当前语句，参与该文件整体逻辑。
- 660: `className="inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-white bg-indigo-600 rounded-lg hover:bg-indigo-700 disabled:opacity-50 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 661: `>` -> 执行当前语句，参与该文件整体逻辑。
- 662: `{editLoading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 663: `<div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" />` -> JSX/HTML 结构行，用于描述页面元素。
- 664: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 665: `<Check className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 666: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 667: `保存` -> 执行当前语句，参与该文件整体逻辑。
- 668: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 669: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 670: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 671: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 672: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 673: `(空行)` -> 空行，用于提升代码结构可读性。
- 674: `{/* 删除确认弹窗 */}` -> 执行当前语句，参与该文件整体逻辑。
- 675: `{deleteConfirm !== null && (` -> 执行当前语句，参与该文件整体逻辑。
- 676: `<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 677: `<div className="bg-white rounded-2xl shadow-xl w-full max-w-sm p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 678: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 679: `<div className="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 680: `<AlertCircle className="h-5 w-5 text-red-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 681: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 682: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 683: `<h3 className="text-lg font-semibold text-gray-900">确认删除</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 684: `<p className="text-sm text-gray-500">此操作不可撤销</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 685: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 686: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 687: `<div className="flex items-center justify-end gap-3 mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 688: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 689: `onClick={() => setDeleteConfirm(null)}` -> 执行当前语句，参与该文件整体逻辑。
- 690: `className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 691: `>` -> 执行当前语句，参与该文件整体逻辑。
- 692: `取消` -> 执行当前语句，参与该文件整体逻辑。
- 693: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 694: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 695: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 696: `const idx = parseInt(deleteConfirm)` -> 声明变量或常量，保存运行时数据。
- 697: `if (tableData && !isNaN(idx)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 698: `handleDelete(tableData.data[idx])` -> 执行当前语句，参与该文件整体逻辑。
- 699: `}` -> 结束当前语句或代码块。
- 700: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 701: `className="px-4 py-2 text-sm font-medium text-white bg-red-600 rounded-lg hover:bg-red-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 702: `>` -> 执行当前语句，参与该文件整体逻辑。
- 703: `删除` -> 执行当前语句，参与该文件整体逻辑。
- 704: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 705: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 706: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 707: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 708: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 709: `(空行)` -> 空行，用于提升代码结构可读性。
- 710: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 711: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 712: `)` -> 结束当前语句或代码块。
- 713: `}` -> 结束当前语句或代码块。
- 714: `(空行)` -> 空行，用于提升代码结构可读性。
- 715: `// 格式化单元格值` -> 注释行，用于解释设计意图或使用说明。
- 716: `function formatCellValue(value: any): React.ReactNode {` -> 定义函数或方法，实现具体业务逻辑。
- 717: `if (value === null || value === undefined) {` -> 条件判断分支，根据场景执行不同逻辑。
- 718: `return <span className="text-gray-400 italic">NULL</span>` -> 返回结果或提前结束当前流程。
- 719: `}` -> 结束当前语句或代码块。
- 720: `if (typeof value === 'object') {` -> 条件判断分支，根据场景执行不同逻辑。
- 721: `return JSON.stringify(value)` -> 返回结果或提前结束当前流程。
- 722: `}` -> 结束当前语句或代码块。
- 723: `return String(value)` -> 返回结果或提前结束当前流程。
- 724: `}` -> 结束当前语句或代码块。
