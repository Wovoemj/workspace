# `frontend/user-web/src/app/profile/invite/page.tsx` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 3: `* 邀请好友模块 - 邀请码与邀请统计`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 5: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `* 【功能列表】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 7: `* - 邀请码展示与复制`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `* - 邀请链接展示与复制`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 9: `* - 邀请奖励积分展示`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 10: `* - 已邀请人数统计`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 11: `* - 总奖励积分统计`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `* - 已邀请用户列表（用户名、注册时间）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 13: `* - 登录状态校验`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 14: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `* 【组件依赖】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 16: `* - Navbar, Footer: 布局组件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `* - useUserStore: 用户状态（Zustand）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 18: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 19: `* 【API 接口】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 20: `* - GET /api/users/invite: 获取邀请码和链接`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 21: `* - GET /api/users/invite/stats: 获取邀请统计数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 22: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `* 【状态管理】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 24: `* - useState: inviteData, stats, loading`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 25: `* - useCallback: loadData`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 26: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 27: `'use client'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 29: `import { useCallback, useEffect, useState } from 'react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 30: `import { toast } from 'react-hot-toast'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 31: `import { Copy, Users, Gift, Link as LinkIcon, Loader2 } from 'lucide-react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 32: `import { Navbar } from '@/components/Navbar'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 33: `import { Footer } from '@/components/Footer'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 34: `import { useUserStore } from '@/store'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 35: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 36: `export default function InvitePage() {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 37: `const { isAuthenticated, user } = useUserStore()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 38: `const [loading, setLoading] = useState(true)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 39: `const [inviteData, setInviteData] = useState<{`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 40: `invite_code: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `invite_link: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `invited_count: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `reward_points: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `} | null>(null)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `const [stats, setStats] = useState<{`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 46: `total_invited: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 47: `total_reward_points: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `invited_users: Array<{ id: number; username: string; nickname: string; created_at: string }>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `} | null>(null)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 51: `const loadData = useCallback(async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 52: `if (!isAuthenticated) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 53: `setLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 56: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 57: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 58: `const token = localStorage.getItem('auth_token')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 59: `const [infoRes, statsRes] = await Promise.all([`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 60: `fetch('/api/users/invite', {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `headers: { Authorization: \`Bearer ${token}\` }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `}),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `fetch('/api/users/invite/stats', {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `headers: { Authorization: \`Bearer ${token}\` }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 68: `const infoData = await infoRes.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 69: `const statsData = await statsRes.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `if (infoData.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 72: `setInviteData(infoData)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 74: `if (statsData.success) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 75: `setStats(statsData)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 77: `} catch (error) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 78: `console.error('加载邀请数据失败:', error)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 80: `setLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 82: `}, [isAuthenticated])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 84: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `loadData()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `}, [loadData])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 88: `const copyToClipboard = async (text: string) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 89: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 90: `await navigator.clipboard.writeText(text)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `toast.success('复制成功')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 92: `} catch {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `toast.error('复制失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 95: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 96: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 97: `if (loading) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 98: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 99: `<div className="min-h-screen">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 100: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 101: `<div className="flex items-center justify-center pt-40">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 102: `<Loader2 className="h-8 w-8 animate-spin text-sky-500" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 103: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 104: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 105: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 106: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 107: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 108: `if (!isAuthenticated) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 109: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 110: `<div className="min-h-screen">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 111: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 112: `<div className="pt-24 pb-16">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 113: `<div className="mx-auto max-w-md px-4 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 114: `<div className="rounded-3xl border border-white/60 bg-white/85 p-12 shadow-2xl backdrop-blur-md">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 115: `<Users className="mx-auto h-16 w-16 text-sky-400 mb-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 116: `<h2 className="text-2xl font-bold text-gray-800 mb-4">请先登录</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 117: `<p className="text-gray-600 mb-6">登录后即可邀请好友</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 118: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 119: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 120: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 121: `<Footer />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 122: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 123: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 124: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 125: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 126: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 127: `<div className="min-h-screen">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 128: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 129: `<main className="relative z-10 pt-24 pb-16">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 130: `<div className="mx-auto max-w-2xl px-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 131: `<div className="mb-8 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 132: `<h1 className="bg-gradient-to-r from-gray-900 via-sky-800 to-indigo-900 bg-clip-text text-4xl font-black tracking-tight text-transparent">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 133: `邀请好友`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 134: `</h1>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 135: `<p className="mt-2 text-gray-600">邀请好友注册，双方各得 50 积分</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 136: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 137: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 138: `{/* 邀请码卡片 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `<div className="rounded-3xl border border-white/60 bg-white/85 p-6 shadow-xl backdrop-blur-md mb-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 140: `<div className="flex items-center gap-3 mb-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 141: `<div className="rounded-full bg-sky-100 p-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 142: `<LinkIcon className="h-5 w-5 text-sky-600" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 143: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 144: `<h2 className="text-lg font-semibold text-gray-800">我的邀请码</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 145: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 146: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 147: `{inviteData && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `<div className="space-y-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 149: `<div className="flex items-center gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 150: `<div className="flex-1 rounded-xl bg-gray-100 px-4 py-3 font-mono text-xl font-bold text-center tracking-wider">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 151: `{inviteData.invite_code}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 153: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `onClick={() => copyToClipboard(inviteData.invite_code)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 155: `className="rounded-xl bg-sky-500 px-4 py-3 text-white hover:bg-sky-600"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 156: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 157: `<Copy className="h-5 w-5" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 158: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 159: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 160: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 161: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 162: `<div className="text-sm text-gray-500 mb-1">邀请链接</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 163: `<div className="flex items-center gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 164: `<div className="flex-1 rounded-xl bg-gray-100 px-3 py-2 text-sm text-gray-600 truncate">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 165: `{inviteData.invite_link}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 167: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `onClick={() => copyToClipboard(inviteData.invite_link)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `className="rounded-xl bg-sky-500 px-4 py-2 text-white hover:bg-sky-600 whitespace-nowrap"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `复制链接`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 172: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 173: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 174: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 175: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 176: `<div className="flex items-center gap-2 text-sm text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 177: `<Gift className="h-4 w-4" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 178: `<span>已邀请 <strong className="text-sky-600">{inviteData.invited_count}</strong> 人</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 179: `<span className="mx-2">|</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 180: `<span>每次邀请得 <strong className="text-amber-600">{inviteData.reward_points}</strong> 积分</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 181: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 182: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 183: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 184: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 185: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 186: `{/* 邀请统计 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 187: `<div className="rounded-3xl border border-white/60 bg-white/85 p-6 shadow-xl backdrop-blur-md">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 188: `<div className="flex items-center gap-3 mb-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 189: `<div className="rounded-full bg-indigo-100 p-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 190: `<Users className="h-5 w-5 text-indigo-600" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 191: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 192: `<h2 className="text-lg font-semibold text-gray-800">邀请统计</h2>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 193: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 194: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 195: `{stats && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 196: `<div className="grid grid-cols-2 gap-4 mb-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 197: `<div className="rounded-xl bg-sky-50 p-4 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 198: `<div className="text-3xl font-bold text-sky-600">{stats.total_invited}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 199: `<div className="text-sm text-gray-600">已邀请人数</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 200: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 201: `<div className="rounded-xl bg-amber-50 p-4 text-center">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 202: `<div className="text-3xl font-bold text-amber-600">{stats.total_reward_points}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 203: `<div className="text-sm text-gray-600">累计获得积分</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 204: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 205: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 206: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 207: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 208: `{/* 最近邀请的用户 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 209: `{stats && stats.invited_users && stats.invited_users.length > 0 && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 210: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 211: `<h3 className="text-sm font-semibold text-gray-700 mb-3">最近邀请</h3>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 212: `<div className="space-y-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 213: `{stats.invited_users.map((u) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `<div key={u.id} className="flex items-center justify-between rounded-lg bg-gray-50 px-4 py-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 215: `<span className="font-medium text-gray-800">{u.nickname || u.username}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 216: `<span className="text-sm text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 217: `{u.created_at?.split('T')[0]}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 218: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 219: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 220: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 222: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 223: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 225: `{(!stats || stats.total_invited === 0) && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `<div className="text-center py-8 text-gray-500">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 227: `还没有邀请过好友，快去邀请吧！`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 229: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 230: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 231: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 232: `</main>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 233: `<Footer />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 234: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 235: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 236: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
