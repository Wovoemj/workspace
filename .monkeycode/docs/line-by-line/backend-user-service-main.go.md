# `backend/user-service/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package user` -> 声明当前文件所属包或模块命名空间。
- 2: `import (` -> 导入依赖模块，供当前文件使用。
- 3: `"context"` -> 执行当前语句，参与该文件整体逻辑。
- 4: `"database/sql"` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"encoding/json"` -> 执行当前语句，参与该文件整体逻辑。
- 6: `"fmt"` -> 执行当前语句，参与该文件整体逻辑。
- 7: `"net/http"` -> 执行当前语句，参与该文件整体逻辑。
- 8: `"time"` -> 执行当前语句，参与该文件整体逻辑。
- 9: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该文件整体逻辑。
- 10: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该文件整体逻辑。
- 11: `"golang.org/x/crypto/bcrypt"` -> 执行当前语句，参与该文件整体逻辑。
- 12: `"gorm.io/gorm"` -> 执行当前语句，参与该文件整体逻辑。
- 13: `)` -> 结束当前语句或代码块。
- 14: `type Service struct {` -> 定义类型或类结构，约束数据与行为。
- 15: `db     *gorm.DB` -> 执行当前语句，参与该文件整体逻辑。
- 16: `logger *logrus.Logger` -> 执行当前语句，参与该文件整体逻辑。
- 17: `}` -> 结束当前语句或代码块。
- 18: `type User struct {` -> 定义类型或类结构，约束数据与行为。
- 19: `ID            uint      \`json:"id" gorm:"primaryKey"\`` -> 执行当前语句，参与该文件整体逻辑。
- 20: `Username      string    \`json:"username" gorm:"unique"\`` -> 执行当前语句，参与该文件整体逻辑。
- 21: `Phone         string    \`json:"phone" gorm:"unique"\`` -> 执行当前语句，参与该文件整体逻辑。
- 22: `Email         string    \`json:"email"\`` -> 执行当前语句，参与该文件整体逻辑。
- 23: `Nickname      string    \`json:"nickname"\`` -> 执行当前语句，参与该文件整体逻辑。
- 24: `Password      string    \`json:"-"\`` -> 执行当前语句，参与该文件整体逻辑。
- 25: `AvatarURL     string    \`json:"avatar_url"\`` -> 执行当前语句，参与该文件整体逻辑。
- 26: `MembershipLevel int      \`json:"membership_level"\`` -> 执行当前语句，参与该文件整体逻辑。
- 27: `IsAdmin       bool      \`json:"is_admin" gorm:"column:is_admin"\`` -> 执行当前语句，参与该文件整体逻辑。
- 28: `Preferences   UserPreferences \`json:"preferences" gorm:"type:jsonb"\`` -> 执行当前语句，参与该文件整体逻辑。
- 29: `CreatedAt     time.Time \`json:"created_at"\`` -> 执行当前语句，参与该文件整体逻辑。
- 30: `UpdatedAt     time.Time \`json:"updated_at"\`` -> 执行当前语句，参与该文件整体逻辑。
- 31: `}` -> 结束当前语句或代码块。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `type UserPreferences struct {` -> 定义类型或类结构，约束数据与行为。
- 34: `Destinations []string \`json:"destinations"\`` -> 执行当前语句，参与该文件整体逻辑。
- 35: `BudgetRange  BudgetRange \`json:"budget_range"\`` -> 执行当前语句，参与该文件整体逻辑。
- 36: `TravelStyle  string   \`json:"travel_style"\`` -> 执行当前语句，参与该文件整体逻辑。
- 37: `GroupSize    int      \`json:"group_size"\`` -> 执行当前语句，参与该文件整体逻辑。
- 38: `Interests    []string \`json:"interests"\`` -> 执行当前语句，参与该文件整体逻辑。
- 39: `}` -> 结束当前语句或代码块。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `type BudgetRange struct {` -> 定义类型或类结构，约束数据与行为。
- 42: `Min float64 \`json:"min"\`` -> 执行当前语句，参与该文件整体逻辑。
- 43: `Max float64 \`json:"max"\`` -> 执行当前语句，参与该文件整体逻辑。
- 44: `}` -> 结束当前语句或代码块。
- 45: `(空行)` -> 空行，用于提升代码结构可读性。
- 46: `type RegisterRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 47: `Phone    string \`json:"phone" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 48: `Password string \`json:"password" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 49: `Nickname string \`json:"nickname"\`` -> 执行当前语句，参与该文件整体逻辑。
- 50: `}` -> 结束当前语句或代码块。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `type LoginRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 53: `Phone    string \`json:"phone"\`` -> 执行当前语句，参与该文件整体逻辑。
- 54: `Username string \`json:"username"\`` -> 执行当前语句，参与该文件整体逻辑。
- 55: `Password string \`json:"password" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 56: `}` -> 结束当前语句或代码块。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `type UpdateProfileRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 59: `Nickname  string  \`json:"nickname"\`` -> 执行当前语句，参与该文件整体逻辑。
- 60: `AvatarURL string  \`json:"avatar_url"\`` -> 执行当前语句，参与该文件整体逻辑。
- 61: `Level     *int    \`json:"membership_level"\`` -> 执行当前语句，参与该文件整体逻辑。
- 62: `}` -> 结束当前语句或代码块。
- 63: `(空行)` -> 空行，用于提升代码结构可读性。
- 64: `type UpdatePreferencesRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 65: `Destinations []string      \`json:"destinations"\`` -> 执行当前语句，参与该文件整体逻辑。
- 66: `BudgetRange  *BudgetRange \`json:"budget_range"\`` -> 执行当前语句，参与该文件整体逻辑。
- 67: `TravelStyle  *string      \`json:"travel_style"\`` -> 执行当前语句，参与该文件整体逻辑。
- 68: `GroupSize    *int         \`json:"group_size"\`` -> 执行当前语句，参与该文件整体逻辑。
- 69: `Interests    []string     \`json:"interests"\`` -> 执行当前语句，参与该文件整体逻辑。
- 70: `}` -> 结束当前语句或代码块。
- 71: `(空行)` -> 空行，用于提升代码结构可读性。
- 72: `type Favorite struct {` -> 定义类型或类结构，约束数据与行为。
- 73: `ID             uint      \`json:"id" gorm:"primaryKey"\`` -> 执行当前语句，参与该文件整体逻辑。
- 74: `UserID         uint      \`json:"user_id" gorm:"not null;index"\`` -> 执行当前语句，参与该文件整体逻辑。
- 75: `DestinationID  uint      \`json:"destination_id" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 76: `DestinationName string   \`json:"destination_name"\`` -> 执行当前语句，参与该文件整体逻辑。
- 77: `City           string    \`json:"city"\`` -> 执行当前语句，参与该文件整体逻辑。
- 78: `Province       string    \`json:"province"\`` -> 执行当前语句，参与该文件整体逻辑。
- 79: `CoverImage     string    \`json:"cover_image"\`` -> 执行当前语句，参与该文件整体逻辑。
- 80: `CreatedAt      time.Time \`json:"created_at"\`` -> 执行当前语句，参与该文件整体逻辑。
- 81: `}` -> 结束当前语句或代码块。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `type AddFavoriteRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 84: `DestinationID uint   \`json:"destination_id" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 85: `Name          string \`json:"name"\`` -> 执行当前语句，参与该文件整体逻辑。
- 86: `City          string \`json:"city"\`` -> 执行当前语句，参与该文件整体逻辑。
- 87: `Province      string \`json:"province"\`` -> 执行当前语句，参与该文件整体逻辑。
- 88: `CoverImage    string \`json:"cover_image"\`` -> 执行当前语句，参与该文件整体逻辑。
- 89: `}` -> 结束当前语句或代码块。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {` -> 定义函数或方法，实现具体业务逻辑。
- 92: `return &Service{` -> 返回结果或提前结束当前流程。
- 93: `db:     db,` -> 执行当前语句，参与该文件整体逻辑。
- 94: `logger: logger,` -> 执行当前语句，参与该文件整体逻辑。
- 95: `}` -> 结束当前语句或代码块。
- 96: `}` -> 结束当前语句或代码块。
- 97: `(空行)` -> 空行，用于提升代码结构可读性。
- 98: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {` -> 定义函数或方法，实现具体业务逻辑。
- 99: `users := router.Group("/users")` -> 执行当前语句，参与该文件整体逻辑。
- 100: `{` -> 执行当前语句，参与该文件整体逻辑。
- 101: `users.POST("/register", s.Register)` -> 执行当前语句，参与该文件整体逻辑。
- 102: `users.POST("/login", s.Login)` -> 执行当前语句，参与该文件整体逻辑。
- 103: `users.GET("/profile", s.GetProfile)` -> 执行当前语句，参与该文件整体逻辑。
- 104: `users.PUT("/profile", s.UpdateProfile)` -> 执行当前语句，参与该文件整体逻辑。
- 105: `users.GET("/preferences", s.GetPreferences)` -> 执行当前语句，参与该文件整体逻辑。
- 106: `users.PUT("/preferences", s.UpdatePreferences)` -> 执行当前语句，参与该文件整体逻辑。
- 107: `users.GET("/:id", s.GetUserByID)` -> 执行当前语句，参与该文件整体逻辑。
- 108: `}` -> 结束当前语句或代码块。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `// Favorites routes` -> 注释行，用于解释设计意图或使用说明。
- 111: `favorites := router.Group("/favorites")` -> 执行当前语句，参与该文件整体逻辑。
- 112: `{` -> 执行当前语句，参与该文件整体逻辑。
- 113: `favorites.GET("", s.GetFavorites)` -> 执行当前语句，参与该文件整体逻辑。
- 114: `favorites.POST("", s.AddFavorite)` -> 执行当前语句，参与该文件整体逻辑。
- 115: `favorites.DELETE("/:id", s.RemoveFavorite)` -> 执行当前语句，参与该文件整体逻辑。
- 116: `}` -> 结束当前语句或代码块。
- 117: `}` -> 结束当前语句或代码块。
- 118: `(空行)` -> 空行，用于提升代码结构可读性。
- 119: `func (s *Service) Register(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 120: `var req RegisterRequest` -> 声明变量或常量，保存运行时数据。
- 121: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 122: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 123: `return` -> 返回结果或提前结束当前流程。
- 124: `}` -> 结束当前语句或代码块。
- 125: `(空行)` -> 空行，用于提升代码结构可读性。
- 126: `// Check if user already exists` -> 注释行，用于解释设计意图或使用说明。
- 127: `var existingUser User` -> 声明变量或常量，保存运行时数据。
- 128: `if err := s.db.Where("phone = ?", req.Phone).First(&existingUser).Error; err == nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 129: `c.JSON(http.StatusConflict, gin.H{"error": "Phone already registered"})` -> 构造并返回接口响应数据。
- 130: `return` -> 返回结果或提前结束当前流程。
- 131: `}` -> 结束当前语句或代码块。
- 132: `(空行)` -> 空行，用于提升代码结构可读性。
- 133: `// Hash password` -> 注释行，用于解释设计意图或使用说明。
- 134: `hashedPassword, err := bcrypt.GenerateFromPassword([]byte(req.Password), bcrypt.DefaultCost)` -> 执行当前语句，参与该文件整体逻辑。
- 135: `if err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 136: `s.logger.WithError(err).Error("Failed to hash password")` -> 执行当前语句，参与该文件整体逻辑。
- 137: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to register user"})` -> 构造并返回接口响应数据。
- 138: `return` -> 返回结果或提前结束当前流程。
- 139: `}` -> 结束当前语句或代码块。
- 140: `(空行)` -> 空行，用于提升代码结构可读性。
- 141: `// Create user` -> 注释行，用于解释设计意图或使用说明。
- 142: `user := User{` -> 执行当前语句，参与该文件整体逻辑。
- 143: `Phone:         req.Phone,` -> 执行当前语句，参与该文件整体逻辑。
- 144: `Password:      string(hashedPassword),` -> 执行当前语句，参与该文件整体逻辑。
- 145: `Nickname:      req.Nickname,` -> 执行当前语句，参与该文件整体逻辑。
- 146: `MembershipLevel: 1, // Default membership level` -> 执行当前语句，参与该文件整体逻辑。
- 147: `Preferences: UserPreferences{` -> 执行当前语句，参与该文件整体逻辑。
- 148: `TravelStyle: "relaxation",` -> 执行当前语句，参与该文件整体逻辑。
- 149: `GroupSize:   2,` -> 执行当前语句，参与该文件整体逻辑。
- 150: `},` -> 执行当前语句，参与该文件整体逻辑。
- 151: `CreatedAt: time.Now(),` -> 执行当前语句，参与该文件整体逻辑。
- 152: `UpdatedAt: time.Now(),` -> 执行当前语句，参与该文件整体逻辑。
- 153: `}` -> 结束当前语句或代码块。
- 154: `(空行)` -> 空行，用于提升代码结构可读性。
- 155: `if err := s.db.Create(&user).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 156: `s.logger.WithError(err).Error("Failed to create user")` -> 执行当前语句，参与该文件整体逻辑。
- 157: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to register user"})` -> 构造并返回接口响应数据。
- 158: `return` -> 返回结果或提前结束当前流程。
- 159: `}` -> 结束当前语句或代码块。
- 160: `(空行)` -> 空行，用于提升代码结构可读性。
- 161: `s.logger.WithField("user_id", user.ID).Info("User registered successfully")` -> 执行当前语句，参与该文件整体逻辑。
- 162: `c.JSON(http.StatusCreated, gin.H{` -> 构造并返回接口响应数据。
- 163: `"message": "User registered successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 164: `"user":    user,` -> 执行当前语句，参与该文件整体逻辑。
- 165: `})` -> 执行当前语句，参与该文件整体逻辑。
- 166: `}` -> 结束当前语句或代码块。
- 167: `(空行)` -> 空行，用于提升代码结构可读性。
- 168: `func (s *Service) Login(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 169: `var req LoginRequest` -> 声明变量或常量，保存运行时数据。
- 170: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 171: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 172: `return` -> 返回结果或提前结束当前流程。
- 173: `}` -> 结束当前语句或代码块。
- 174: `(空行)` -> 空行，用于提升代码结构可读性。
- 175: `// Find user by phone or username` -> 注释行，用于解释设计意图或使用说明。
- 176: `var user User` -> 声明变量或常量，保存运行时数据。
- 177: `query := s.db.Where("1=1")` -> 执行当前语句，参与该文件整体逻辑。
- 178: `if req.Phone != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 179: `query = query.Where("phone = ?", req.Phone)` -> 执行当前语句，参与该文件整体逻辑。
- 180: `} else if req.Username != "" {` -> 执行当前语句，参与该文件整体逻辑。
- 181: `query = query.Where("username = ?", req.Username)` -> 执行当前语句，参与该文件整体逻辑。
- 182: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 183: `c.JSON(http.StatusBadRequest, gin.H{"error": "Phone or username is required"})` -> 构造并返回接口响应数据。
- 184: `return` -> 返回结果或提前结束当前流程。
- 185: `}` -> 结束当前语句或代码块。
- 186: `if err := query.First(&user).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 187: `c.JSON(http.StatusUnauthorized, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 188: `return` -> 返回结果或提前结束当前流程。
- 189: `}` -> 结束当前语句或代码块。
- 190: `(空行)` -> 空行，用于提升代码结构可读性。
- 191: `// Check password` -> 注释行，用于解释设计意图或使用说明。
- 192: `if err := bcrypt.CompareHashAndPassword([]byte(user.Password), []byte(req.Password)); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 193: `c.JSON(http.StatusUnauthorized, gin.H{"error": "Invalid password"})` -> 构造并返回接口响应数据。
- 194: `return` -> 返回结果或提前结束当前流程。
- 195: `}` -> 结束当前语句或代码块。
- 196: `(空行)` -> 空行，用于提升代码结构可读性。
- 197: `// Generate token (simplified - in production use JWT)` -> 注释行，用于解释设计意图或使用说明。
- 198: `token := fmt.Sprintf("token_%d_%d", user.ID, time.Now().Unix())` -> 执行当前语句，参与该文件整体逻辑。
- 199: `(空行)` -> 空行，用于提升代码结构可读性。
- 200: `s.logger.WithField("user_id", user.ID).Info("User logged in successfully")` -> 执行当前语句，参与该文件整体逻辑。
- 201: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 202: `"success": true,` -> 执行当前语句，参与该文件整体逻辑。
- 203: `"message": "Login successful",` -> 执行当前语句，参与该文件整体逻辑。
- 204: `"token":   token,` -> 执行当前语句，参与该文件整体逻辑。
- 205: `"user":    user,` -> 执行当前语句，参与该文件整体逻辑。
- 206: `})` -> 执行当前语句，参与该文件整体逻辑。
- 207: `}` -> 结束当前语句或代码块。
- 208: `(空行)` -> 空行，用于提升代码结构可读性。
- 209: `func (s *Service) GetProfile(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 210: `// Get user ID from token (simplified)` -> 注释行，用于解释设计意图或使用说明。
- 211: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 212: `(空行)` -> 空行，用于提升代码结构可读性。
- 213: `var user User` -> 声明变量或常量，保存运行时数据。
- 214: `if err := s.db.First(&user, userID).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 215: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 216: `return` -> 返回结果或提前结束当前流程。
- 217: `}` -> 结束当前语句或代码块。
- 218: `(空行)` -> 空行，用于提升代码结构可读性。
- 219: `c.JSON(http.StatusOK, user)` -> 构造并返回接口响应数据。
- 220: `}` -> 结束当前语句或代码块。
- 221: `(空行)` -> 空行，用于提升代码结构可读性。
- 222: `func (s *Service) UpdateProfile(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 223: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 224: `(空行)` -> 空行，用于提升代码结构可读性。
- 225: `var req UpdateProfileRequest` -> 声明变量或常量，保存运行时数据。
- 226: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 227: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 228: `return` -> 返回结果或提前结束当前流程。
- 229: `}` -> 结束当前语句或代码块。
- 230: `(空行)` -> 空行，用于提升代码结构可读性。
- 231: `var user User` -> 声明变量或常量，保存运行时数据。
- 232: `if err := s.db.First(&user, userID).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 233: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 234: `return` -> 返回结果或提前结束当前流程。
- 235: `}` -> 结束当前语句或代码块。
- 236: `(空行)` -> 空行，用于提升代码结构可读性。
- 237: `// Update fields` -> 注释行，用于解释设计意图或使用说明。
- 238: `if req.Nickname != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 239: `user.Nickname = req.Nickname` -> 执行当前语句，参与该文件整体逻辑。
- 240: `}` -> 结束当前语句或代码块。
- 241: `if req.AvatarURL != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 242: `user.AvatarURL = req.AvatarURL` -> 执行当前语句，参与该文件整体逻辑。
- 243: `}` -> 结束当前语句或代码块。
- 244: `if req.Level != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 245: `user.MembershipLevel = *req.Level` -> 执行当前语句，参与该文件整体逻辑。
- 246: `}` -> 结束当前语句或代码块。
- 247: `(空行)` -> 空行，用于提升代码结构可读性。
- 248: `user.UpdatedAt = time.Now()` -> 执行当前语句，参与该文件整体逻辑。
- 249: `(空行)` -> 空行，用于提升代码结构可读性。
- 250: `if err := s.db.Save(&user).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 251: `s.logger.WithError(err).Error("Failed to update user profile")` -> 执行当前语句，参与该文件整体逻辑。
- 252: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update profile"})` -> 构造并返回接口响应数据。
- 253: `return` -> 返回结果或提前结束当前流程。
- 254: `}` -> 结束当前语句或代码块。
- 255: `(空行)` -> 空行，用于提升代码结构可读性。
- 256: `s.logger.WithField("user_id", user.ID).Info("User profile updated")` -> 执行当前语句，参与该文件整体逻辑。
- 257: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 258: `"message": "Profile updated successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 259: `"user":    user,` -> 执行当前语句，参与该文件整体逻辑。
- 260: `})` -> 执行当前语句，参与该文件整体逻辑。
- 261: `}` -> 结束当前语句或代码块。
- 262: `(空行)` -> 空行，用于提升代码结构可读性。
- 263: `func (s *Service) GetPreferences(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 264: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 265: `(空行)` -> 空行，用于提升代码结构可读性。
- 266: `var user User` -> 声明变量或常量，保存运行时数据。
- 267: `if err := s.db.First(&user, userID).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 268: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 269: `return` -> 返回结果或提前结束当前流程。
- 270: `}` -> 结束当前语句或代码块。
- 271: `(空行)` -> 空行，用于提升代码结构可读性。
- 272: `c.JSON(http.StatusOK, user.Preferences)` -> 构造并返回接口响应数据。
- 273: `}` -> 结束当前语句或代码块。
- 274: `(空行)` -> 空行，用于提升代码结构可读性。
- 275: `func (s *Service) UpdatePreferences(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 276: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 277: `(空行)` -> 空行，用于提升代码结构可读性。
- 278: `var req UpdatePreferencesRequest` -> 声明变量或常量，保存运行时数据。
- 279: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 280: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 281: `return` -> 返回结果或提前结束当前流程。
- 282: `}` -> 结束当前语句或代码块。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `var user User` -> 声明变量或常量，保存运行时数据。
- 285: `if err := s.db.First(&user, userID).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 286: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 287: `return` -> 返回结果或提前结束当前流程。
- 288: `}` -> 结束当前语句或代码块。
- 289: `(空行)` -> 空行，用于提升代码结构可读性。
- 290: `// Update preferences` -> 注释行，用于解释设计意图或使用说明。
- 291: `if req.Destinations != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 292: `user.Preferences.Destinations = req.Destinations` -> 执行当前语句，参与该文件整体逻辑。
- 293: `}` -> 结束当前语句或代码块。
- 294: `if req.BudgetRange != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 295: `user.Preferences.BudgetRange = *req.BudgetRange` -> 执行当前语句，参与该文件整体逻辑。
- 296: `}` -> 结束当前语句或代码块。
- 297: `if req.TravelStyle != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 298: `user.Preferences.TravelStyle = *req.TravelStyle` -> 执行当前语句，参与该文件整体逻辑。
- 299: `}` -> 结束当前语句或代码块。
- 300: `if req.GroupSize != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 301: `user.Preferences.GroupSize = *req.GroupSize` -> 执行当前语句，参与该文件整体逻辑。
- 302: `}` -> 结束当前语句或代码块。
- 303: `if req.Interests != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 304: `user.Preferences.Interests = req.Interests` -> 执行当前语句，参与该文件整体逻辑。
- 305: `}` -> 结束当前语句或代码块。
- 306: `(空行)` -> 空行，用于提升代码结构可读性。
- 307: `user.UpdatedAt = time.Now()` -> 执行当前语句，参与该文件整体逻辑。
- 308: `(空行)` -> 空行，用于提升代码结构可读性。
- 309: `if err := s.db.Save(&user).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 310: `s.logger.WithError(err).Error("Failed to update user preferences")` -> 执行当前语句，参与该文件整体逻辑。
- 311: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update preferences"})` -> 构造并返回接口响应数据。
- 312: `return` -> 返回结果或提前结束当前流程。
- 313: `}` -> 结束当前语句或代码块。
- 314: `(空行)` -> 空行，用于提升代码结构可读性。
- 315: `s.logger.WithField("user_id", user.ID).Info("User preferences updated")` -> 执行当前语句，参与该文件整体逻辑。
- 316: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 317: `"message": "Preferences updated successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 318: `"preferences": user.Preferences,` -> 执行当前语句，参与该文件整体逻辑。
- 319: `})` -> 执行当前语句，参与该文件整体逻辑。
- 320: `}` -> 结束当前语句或代码块。
- 321: `(空行)` -> 空行，用于提升代码结构可读性。
- 322: `func (s *Service) GetUserByID(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 323: `id := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 324: `(空行)` -> 空行，用于提升代码结构可读性。
- 325: `var user User` -> 声明变量或常量，保存运行时数据。
- 326: `if err := s.db.First(&user, id).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 327: `c.JSON(http.StatusNotFound, gin.H{"error": "User not found"})` -> 构造并返回接口响应数据。
- 328: `return` -> 返回结果或提前结束当前流程。
- 329: `}` -> 结束当前语句或代码块。
- 330: `(空行)` -> 空行，用于提升代码结构可读性。
- 331: `c.JSON(http.StatusOK, user)` -> 构造并返回接口响应数据。
- 332: `}` -> 结束当前语句或代码块。
- 333: `(空行)` -> 空行，用于提升代码结构可读性。
- 334: `func (s *Service) GetFavorites(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 335: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 336: `(空行)` -> 空行，用于提升代码结构可读性。
- 337: `var favorites []Favorite` -> 声明变量或常量，保存运行时数据。
- 338: `if err := s.db.Where("user_id = ?", userID).Order("created_at DESC").Find(&favorites).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 339: `s.logger.WithError(err).Error("Failed to get favorites")` -> 执行当前语句，参与该文件整体逻辑。
- 340: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get favorites"})` -> 构造并返回接口响应数据。
- 341: `return` -> 返回结果或提前结束当前流程。
- 342: `}` -> 结束当前语句或代码块。
- 343: `(空行)` -> 空行，用于提升代码结构可读性。
- 344: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 345: `"success":      true,` -> 执行当前语句，参与该文件整体逻辑。
- 346: `"destinations": favorites,` -> 执行当前语句，参与该文件整体逻辑。
- 347: `})` -> 执行当前语句，参与该文件整体逻辑。
- 348: `}` -> 结束当前语句或代码块。
- 349: `(空行)` -> 空行，用于提升代码结构可读性。
- 350: `func (s *Service) AddFavorite(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 351: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 352: `(空行)` -> 空行，用于提升代码结构可读性。
- 353: `var req AddFavoriteRequest` -> 声明变量或常量，保存运行时数据。
- 354: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 355: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 356: `return` -> 返回结果或提前结束当前流程。
- 357: `}` -> 结束当前语句或代码块。
- 358: `(空行)` -> 空行，用于提升代码结构可读性。
- 359: `// Check if already favorited` -> 注释行，用于解释设计意图或使用说明。
- 360: `var existing Favorite` -> 声明变量或常量，保存运行时数据。
- 361: `if err := s.db.Where("user_id = ? AND destination_id = ?", userID, req.DestinationID).First(&existing).Error; err == nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 362: `c.JSON(http.StatusOK, gin.H{"success": true, "message": "Already favorited"})` -> 构造并返回接口响应数据。
- 363: `return` -> 返回结果或提前结束当前流程。
- 364: `}` -> 结束当前语句或代码块。
- 365: `(空行)` -> 空行，用于提升代码结构可读性。
- 366: `favorite := Favorite{` -> 执行当前语句，参与该文件整体逻辑。
- 367: `UserID:          userID,` -> 执行当前语句，参与该文件整体逻辑。
- 368: `DestinationID:   req.DestinationID,` -> 执行当前语句，参与该文件整体逻辑。
- 369: `DestinationName: req.Name,` -> 执行当前语句，参与该文件整体逻辑。
- 370: `City:            req.City,` -> 执行当前语句，参与该文件整体逻辑。
- 371: `Province:        req.Province,` -> 执行当前语句，参与该文件整体逻辑。
- 372: `CoverImage:      req.CoverImage,` -> 执行当前语句，参与该文件整体逻辑。
- 373: `}` -> 结束当前语句或代码块。
- 374: `(空行)` -> 空行，用于提升代码结构可读性。
- 375: `if err := s.db.Create(&favorite).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 376: `s.logger.WithError(err).Error("Failed to add favorite")` -> 执行当前语句，参与该文件整体逻辑。
- 377: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to add favorite"})` -> 构造并返回接口响应数据。
- 378: `return` -> 返回结果或提前结束当前流程。
- 379: `}` -> 结束当前语句或代码块。
- 380: `(空行)` -> 空行，用于提升代码结构可读性。
- 381: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 382: `"success": true,` -> 执行当前语句，参与该文件整体逻辑。
- 383: `"message": "Added to favorites",` -> 执行当前语句，参与该文件整体逻辑。
- 384: `})` -> 执行当前语句，参与该文件整体逻辑。
- 385: `}` -> 结束当前语句或代码块。
- 386: `(空行)` -> 空行，用于提升代码结构可读性。
- 387: `func (s *Service) RemoveFavorite(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 388: `userID := c.GetUint("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 389: `favoriteID := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 390: `(空行)` -> 空行，用于提升代码结构可读性。
- 391: `var favorite Favorite` -> 声明变量或常量，保存运行时数据。
- 392: `if err := s.db.Where("id = ? AND user_id = ?", favoriteID, userID).First(&favorite).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 393: `c.JSON(http.StatusNotFound, gin.H{"error": "Favorite not found"})` -> 构造并返回接口响应数据。
- 394: `return` -> 返回结果或提前结束当前流程。
- 395: `}` -> 结束当前语句或代码块。
- 396: `(空行)` -> 空行，用于提升代码结构可读性。
- 397: `if err := s.db.Delete(&favorite).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 398: `s.logger.WithError(err).Error("Failed to remove favorite")` -> 执行当前语句，参与该文件整体逻辑。
- 399: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to remove favorite"})` -> 构造并返回接口响应数据。
- 400: `return` -> 返回结果或提前结束当前流程。
- 401: `}` -> 结束当前语句或代码块。
- 402: `(空行)` -> 空行，用于提升代码结构可读性。
- 403: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 404: `"success": true,` -> 执行当前语句，参与该文件整体逻辑。
- 405: `"message": "Removed from favorites",` -> 执行当前语句，参与该文件整体逻辑。
- 406: `})` -> 执行当前语句，参与该文件整体逻辑。
- 407: `}` -> 结束当前语句或代码块。
