# `frontend/user-web/src/app/planner/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 行程规划重定向模块 - 功能整合说明` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能说明】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 旧版行程规划页面已整合到智能问答页面` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 自动重定向到 /assistant` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 显示功能迁移提示信息` -> 注释行，用于解释设计意图或使用说明。
- 10: `*` -> 注释行，用于解释设计意图或使用说明。
- 11: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 无独立组件（重定向页面）` -> 注释行，用于解释设计意图或使用说明。
- 13: `*` -> 注释行，用于解释设计意图或使用说明。
- 14: `* 【路由行为】` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 挂载时立即重定向到 /assistant` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 使用 router.replace()（替换当前历史记录）` -> 注释行，用于解释设计意图或使用说明。
- 17: `*/` -> 注释行，用于解释设计意图或使用说明。
- 18: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 19: `(空行)` -> 空行，用于提升代码结构可读性。
- 20: `import { useEffect } from 'react'` -> 导入依赖模块，供当前文件使用。
- 21: `import { useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 22: `import { Sparkles } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `export default function PlannerRedirectPage() {` -> 导出当前声明，供其他模块复用。
- 25: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 28: `// 立即重定向到智能问答页面` -> 注释行，用于解释设计意图或使用说明。
- 29: `router.replace('/assistant')` -> 执行当前语句，参与该文件整体逻辑。
- 30: `}, [router])` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `return (` -> 返回结果或提前结束当前流程。
- 33: `<div className="min-h-screen page-bg flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 34: `<div className="text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 35: `<div className="flex justify-center mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 36: `<div className="flex h-20 w-20 items-center justify-center rounded-full bg-gradient-to-br from-blue-500 to-cyan-400 shadow-2xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 37: `<Sparkles className="h-10 w-10 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 38: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 39: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 40: `<h1 className="text-3xl font-bold text-gray-900 mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 41: `行程规划功能已整合到智能问答` -> 执行当前语句，参与该文件整体逻辑。
- 42: `</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 43: `<p className="text-gray-600 text-lg mb-6 max-w-md mx-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 44: `现在你可以在智能问答页面直接描述旅行需求，AI会自动识别并规划行程?` -> 执行当前语句，参与该文件整体逻辑。
- 45: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 46: `<div className="text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 47: `正在重定向到智能问答页面...` -> 执行当前语句，参与该文件整体逻辑。
- 48: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 49: `<div className="mt-8 flex justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 50: `<div className="h-2 w-64 bg-blue-100 rounded-full overflow-hidden">` -> JSX/HTML 结构行，用于描述页面元素。
- 51: `<div className="h-full bg-gradient-to-r from-blue-500 to-cyan-400 animate-pulse"></div>` -> JSX/HTML 结构行，用于描述页面元素。
- 52: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 54: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 56: `)` -> 结束当前语句或代码块。
- 57: `}` -> 结束当前语句或代码块。
