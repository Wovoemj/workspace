# `frontend/user-web/src/app/coupons/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 优惠券中心模块 - 优惠券领取与管理` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 可领取优惠券列表展示` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 已领取优惠券列表展示` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 优惠券状态管理（可用/已使用/已过期）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 优惠券领取功能（需登录）` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 优惠券详情展示（满减条件、有效期、剩余数量）` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 折扣计算展示（固定金额/百分比）` -> 注释行，用于解释设计意图或使用说明。
- 13: `*` -> 注释行，用于解释设计意图或使用说明。
- 14: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - couponApi: 优惠券 API 封装` -> 注释行，用于解释设计意图或使用说明。
- 18: `*` -> 注释行，用于解释设计意图或使用说明。
- 19: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - couponApi.list(): 获取优惠券列表` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - couponApi.myList(): 获取用户已领取优惠券` -> 注释行，用于解释设计意图或使用说明。
- 22: `* - couponApi.claim(id): 领取优惠券` -> 注释行，用于解释设计意图或使用说明。
- 23: `*` -> 注释行，用于解释设计意图或使用说明。
- 24: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - useState: coupons(可用), userCoupons(已领取), loading` -> 注释行，用于解释设计意图或使用说明。
- 26: `* - useCallback: loadCoupons, loadUserCoupons, claimCoupon` -> 注释行，用于解释设计意图或使用说明。
- 27: `*/` -> 注释行，用于解释设计意图或使用说明。
- 28: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `import { useCallback, useEffect, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 31: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 32: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 33: `import {` -> 导入依赖模块，供当前文件使用。
- 34: `Ticket,` -> 执行当前语句，参与该文件整体逻辑。
- 35: `Gift,` -> 执行当前语句，参与该文件整体逻辑。
- 36: `ChevronRight,` -> 执行当前语句，参与该文件整体逻辑。
- 37: `Loader2,` -> 执行当前语句，参与该文件整体逻辑。
- 38: `CheckCircle2,` -> 执行当前语句，参与该文件整体逻辑。
- 39: `XCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 40: `Calendar,` -> 执行当前语句，参与该文件整体逻辑。
- 41: `DollarSign,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `Percent` -> 执行当前语句，参与该文件整体逻辑。
- 43: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 44: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 45: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 46: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 47: `import { couponApi } from '@/lib/api'` -> 导入依赖模块，供当前文件使用。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `type Coupon = {` -> 定义类型或类结构，约束数据与行为。
- 50: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 51: `code: string` -> 执行当前语句，参与该文件整体逻辑。
- 52: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 53: `type: 'fixed' | 'percent'` -> 执行当前语句，参与该文件整体逻辑。
- 54: `value: number` -> 执行当前语句，参与该文件整体逻辑。
- 55: `min_order: number` -> 执行当前语句，参与该文件整体逻辑。
- 56: `max_uses: number` -> 执行当前语句，参与该文件整体逻辑。
- 57: `used_count: number` -> 执行当前语句，参与该文件整体逻辑。
- 58: `start_date: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 59: `end_date: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 60: `status: string` -> 执行当前语句，参与该文件整体逻辑。
- 61: `}` -> 结束当前语句或代码块。
- 62: `(空行)` -> 空行，用于提升代码结构可读性。
- 63: `type UserCoupon = {` -> 定义类型或类结构，约束数据与行为。
- 64: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 65: `coupon_id: number` -> 执行当前语句，参与该文件整体逻辑。
- 66: `is_used: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 67: `used_at: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 68: `created_at: string` -> 执行当前语句，参与该文件整体逻辑。
- 69: `coupon: Coupon` -> 执行当前语句，参与该文件整体逻辑。
- 70: `}` -> 结束当前语句或代码块。
- 71: `(空行)` -> 空行，用于提升代码结构可读性。
- 72: `function formatDate(dateStr: string | null) {` -> 定义函数或方法，实现具体业务逻辑。
- 73: `if (!dateStr) return '不限'` -> 条件判断分支，根据场景执行不同逻辑。
- 74: `return new Date(dateStr).toLocaleDateString('zh-CN', { month: 'short', day: 'numeric' })` -> 返回结果或提前结束当前流程。
- 75: `}` -> 结束当前语句或代码块。
- 76: `(空行)` -> 空行，用于提升代码结构可读性。
- 77: `function formatDiscount(coupon: Coupon) {` -> 定义函数或方法，实现具体业务逻辑。
- 78: `if (coupon.type === 'fixed') {` -> 条件判断分支，根据场景执行不同逻辑。
- 79: `return \`¥${coupon.value}\`` -> 返回结果或提前结束当前流程。
- 80: `}` -> 结束当前语句或代码块。
- 81: `return \`${coupon.value}%\`` -> 返回结果或提前结束当前流程。
- 82: `}` -> 结束当前语句或代码块。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `export default function CouponsPage() {` -> 导出当前声明，供其他模块复用。
- 85: `const { isAuthenticated, user } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 86: `const [activeTab, setActiveTab] = useState<'available' | 'my'>('available')` -> 声明变量或常量，保存运行时数据。
- 87: `const [availableCoupons, setAvailableCoupons] = useState<Coupon[]>([])` -> 声明变量或常量，保存运行时数据。
- 88: `const [myCoupons, setMyCoupons] = useState<UserCoupon[]>([])` -> 声明变量或常量，保存运行时数据。
- 89: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 90: `const [claiming, setClaiming] = useState<number | null>(null)` -> 声明变量或常量，保存运行时数据。
- 91: `(空行)` -> 空行，用于提升代码结构可读性。
- 92: `const loadAvailable = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 93: `try {` -> 异常处理逻辑，提升程序健壮性。
- 94: `const res = await couponApi.available()` -> 声明变量或常量，保存运行时数据。
- 95: `if (res.data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 96: `setAvailableCoupons(res.data.coupons || [])` -> 执行当前语句，参与该文件整体逻辑。
- 97: `}` -> 结束当前语句或代码块。
- 98: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 99: `console.error('加载优惠券失败', e)` -> 执行当前语句，参与该文件整体逻辑。
- 100: `}` -> 结束当前语句或代码块。
- 101: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `const loadMyCoupons = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 104: `if (!isAuthenticated) return` -> 条件判断分支，根据场景执行不同逻辑。
- 105: `try {` -> 异常处理逻辑，提升程序健壮性。
- 106: `const res = await couponApi.my()` -> 声明变量或常量，保存运行时数据。
- 107: `if (res.data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 108: `setMyCoupons(res.data.coupons || [])` -> 执行当前语句，参与该文件整体逻辑。
- 109: `}` -> 结束当前语句或代码块。
- 110: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 111: `console.error('加载我的优惠券失败', e)` -> 执行当前语句，参与该文件整体逻辑。
- 112: `}` -> 结束当前语句或代码块。
- 113: `}, [isAuthenticated])` -> 执行当前语句，参与该文件整体逻辑。
- 114: `(空行)` -> 空行，用于提升代码结构可读性。
- 115: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 116: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 117: `if (activeTab === 'available') {` -> 条件判断分支，根据场景执行不同逻辑。
- 118: `loadAvailable().finally(() => setLoading(false))` -> 执行当前语句，参与该文件整体逻辑。
- 119: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 120: `loadMyCoupons().finally(() => setLoading(false))` -> 执行当前语句，参与该文件整体逻辑。
- 121: `}` -> 结束当前语句或代码块。
- 122: `}, [activeTab, loadAvailable, loadMyCoupons])` -> 执行当前语句，参与该文件整体逻辑。
- 123: `(空行)` -> 空行，用于提升代码结构可读性。
- 124: `const handleClaim = async (couponId: number) => {` -> 声明变量或常量，保存运行时数据。
- 125: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 126: `toast.error('请先登录')` -> 执行当前语句，参与该文件整体逻辑。
- 127: `return` -> 返回结果或提前结束当前流程。
- 128: `}` -> 结束当前语句或代码块。
- 129: `setClaiming(couponId)` -> 执行当前语句，参与该文件整体逻辑。
- 130: `try {` -> 异常处理逻辑，提升程序健壮性。
- 131: `const res = await couponApi.claim(couponId)` -> 声明变量或常量，保存运行时数据。
- 132: `if (res.data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 133: `toast.success('领取成功')` -> 执行当前语句，参与该文件整体逻辑。
- 134: `loadAvailable()` -> 执行当前语句，参与该文件整体逻辑。
- 135: `loadMyCoupons()` -> 执行当前语句，参与该文件整体逻辑。
- 136: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 137: `toast.error(res.data.error || '领取失败')` -> 执行当前语句，参与该文件整体逻辑。
- 138: `}` -> 结束当前语句或代码块。
- 139: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 140: `toast.error(e.response?.data?.error || '领取失败')` -> 执行当前语句，参与该文件整体逻辑。
- 141: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 142: `setClaiming(null)` -> 执行当前语句，参与该文件整体逻辑。
- 143: `}` -> 结束当前语句或代码块。
- 144: `}` -> 结束当前语句或代码块。
- 145: `(空行)` -> 空行，用于提升代码结构可读性。
- 146: `const renderCouponCard = (coupon: Coupon, isMyCoupon = false) => {` -> 声明变量或常量，保存运行时数据。
- 147: `const isExpired = coupon.end_date && new Date(coupon.end_date) < new Date()` -> 声明变量或常量，保存运行时数据。
- 148: `const isFull = coupon.max_uses > 0 && coupon.used_count >= coupon.max_uses` -> 声明变量或常量，保存运行时数据。
- 149: `(空行)` -> 空行，用于提升代码结构可读性。
- 150: `return (` -> 返回结果或提前结束当前流程。
- 151: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 152: `key={coupon.id}` -> 执行当前语句，参与该文件整体逻辑。
- 153: `className={\`relative overflow-hidden rounded-2xl border ${` -> 执行当前语句，参与该文件整体逻辑。
- 154: `isExpired || isFull` -> 执行当前语句，参与该文件整体逻辑。
- 155: `? 'border-gray-200 bg-gray-50 opacity-75'` -> 执行当前语句，参与该文件整体逻辑。
- 156: `: 'border-orange-200 bg-gradient-to-r from-orange-50 to-amber-50'` -> 执行当前语句，参与该文件整体逻辑。
- 157: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 158: `>` -> 执行当前语句，参与该文件整体逻辑。
- 159: `<div className="flex items-stretch">` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `{/* 左侧金额 */}` -> 执行当前语句，参与该文件整体逻辑。
- 161: `<div className={\`flex flex-col items-center justify-center px-6 py-4 ${` -> 执行当前语句，参与该文件整体逻辑。
- 162: `isExpired || isFull ? 'bg-gray-100' : 'bg-orange-500'` -> 执行当前语句，参与该文件整体逻辑。
- 163: `} text-white\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 164: `<div className="flex items-baseline">` -> JSX/HTML 结构行，用于描述页面元素。
- 165: `{coupon.type === 'fixed' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 166: `<DollarSign className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 168: `<Percent className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 169: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 170: `<span className="text-3xl font-black">{coupon.value}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 171: `{coupon.type === 'percent' && <span className="text-lg">折</span>}` -> 执行当前语句，参与该文件整体逻辑。
- 172: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 173: `<span className="text-xs opacity-80">` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `{coupon.min_order > 0 ? \`满${coupon.min_order}可用\` : '无门槛'}` -> 执行当前语句，参与该文件整体逻辑。
- 175: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `(空行)` -> 空行，用于提升代码结构可读性。
- 178: `{/* 右侧信息 */}` -> 执行当前语句，参与该文件整体逻辑。
- 179: `<div className="flex-1 p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `<h3 className="font-bold text-gray-800">{coupon.name}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `<p className="mt-1 text-xs text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `有效期：{formatDate(coupon.start_date)} - {formatDate(coupon.end_date)}` -> 执行当前语句，参与该文件整体逻辑。
- 183: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 184: `<div className="mt-2 flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 185: `<span className="text-xs text-gray-400">券码：{coupon.code}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 186: `{isMyCoupon ? (` -> 执行当前语句，参与该文件整体逻辑。
- 187: `<span className={\`text-xs font-medium ${` -> 执行当前语句，参与该文件整体逻辑。
- 188: `isExpired ? 'text-gray-400' : 'text-orange-600'` -> 执行当前语句，参与该文件整体逻辑。
- 189: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 190: `{isExpired ? '已过期' : '未使用'}` -> 执行当前语句，参与该文件整体逻辑。
- 191: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 192: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 193: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 194: `onClick={() => handleClaim(coupon.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 195: `disabled={claiming === coupon.id || isExpired || isFull}` -> 执行当前语句，参与该文件整体逻辑。
- 196: `className={\`rounded-full px-4 py-1.5 text-xs font-semibold transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 197: `isExpired || isFull` -> 执行当前语句，参与该文件整体逻辑。
- 198: `? 'bg-gray-200 text-gray-400 cursor-not-allowed'` -> 执行当前语句，参与该文件整体逻辑。
- 199: `: 'bg-orange-500 text-white hover:bg-orange-600'` -> 执行当前语句，参与该文件整体逻辑。
- 200: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 201: `>` -> 执行当前语句，参与该文件整体逻辑。
- 202: `{claiming === coupon.id ? (` -> 执行当前语句，参与该文件整体逻辑。
- 203: `<Loader2 className="h-4 w-4 animate-spin" />` -> JSX/HTML 结构行，用于描述页面元素。
- 204: `) : isExpired ? (` -> 执行当前语句，参与该文件整体逻辑。
- 205: `'已过期'` -> 执行当前语句，参与该文件整体逻辑。
- 206: `) : isFull ? (` -> 执行当前语句，参与该文件整体逻辑。
- 207: `'已领完'` -> 执行当前语句，参与该文件整体逻辑。
- 208: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 209: `'立即领取'` -> 执行当前语句，参与该文件整体逻辑。
- 210: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 211: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 212: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 213: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 214: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 215: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 216: `(空行)` -> 空行，用于提升代码结构可读性。
- 217: `{/* 装饰 */}` -> 执行当前语句，参与该文件整体逻辑。
- 218: `<div className="absolute -right-2 top-1/2 h-4 w-4 -translate-y-1/2 rounded-full bg-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 219: `<div className="absolute -right-2 bottom-1/2 h-4 w-4 -translate-y-1/2 rounded-full bg-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 220: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 221: `)` -> 结束当前语句或代码块。
- 222: `}` -> 结束当前语句或代码块。
- 223: `(空行)` -> 空行，用于提升代码结构可读性。
- 224: `return (` -> 返回结果或提前结束当前流程。
- 225: `<div className="relative min-h-screen bg-gradient-to-b from-sky-50 to-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 226: `<div className="pointer-events-none absolute -right-20 top-24 h-72 w-72 rounded-full bg-orange-300/20 blur-3xl" />` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `<div className="pointer-events-none absolute -bottom-8 left-0 h-80 w-80 rounded-full bg-amber-300/15 blur-3xl" />` -> JSX/HTML 结构行，用于描述页面元素。
- 228: `(空行)` -> 空行，用于提升代码结构可读性。
- 229: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 230: `<main className="relative z-10 pt-24 pb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `<div className="mx-auto max-w-4xl px-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 232: `{/* 标题 */}` -> 执行当前语句，参与该文件整体逻辑。
- 233: `<div className="mb-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `<div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-gradient-to-r from-orange-500 to-amber-500 shadow-lg">` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `<Gift className="h-8 w-8 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `<h1 className="bg-gradient-to-r from-gray-900 via-orange-800 to-amber-900 bg-clip-text text-4xl font-black tracking-tight text-transparent">` -> JSX/HTML 结构行，用于描述页面元素。
- 238: `领券中心` -> 执行当前语句，参与该文件整体逻辑。
- 239: `</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 240: `<p className="mt-2 text-gray-600">领取优惠券，畅享优惠出行</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 241: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 242: `(空行)` -> 空行，用于提升代码结构可读性。
- 243: `{/* Tab 切换 */}` -> 执行当前语句，参与该文件整体逻辑。
- 244: `<div className="mb-6 flex rounded-2xl bg-white p-1 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 245: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 246: `onClick={() => setActiveTab('available')}` -> 执行当前语句，参与该文件整体逻辑。
- 247: `className={\`flex-1 rounded-xl py-3 text-center font-semibold transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 248: `activeTab === 'available'` -> 执行当前语句，参与该文件整体逻辑。
- 249: `? 'bg-gradient-to-r from-orange-500 to-amber-500 text-white shadow-md'` -> 执行当前语句，参与该文件整体逻辑。
- 250: `: 'text-gray-500 hover:text-gray-700'` -> 执行当前语句，参与该文件整体逻辑。
- 251: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 252: `>` -> 执行当前语句，参与该文件整体逻辑。
- 253: `<Ticket className="mr-2 inline h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 254: `可领取` -> 执行当前语句，参与该文件整体逻辑。
- 255: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 256: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 257: `onClick={() => setActiveTab('my')}` -> 执行当前语句，参与该文件整体逻辑。
- 258: `className={\`flex-1 rounded-xl py-3 text-center font-semibold transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 259: `activeTab === 'my'` -> 执行当前语句，参与该文件整体逻辑。
- 260: `? 'bg-gradient-to-r from-orange-500 to-amber-500 text-white shadow-md'` -> 执行当前语句，参与该文件整体逻辑。
- 261: `: 'text-gray-500 hover:text-gray-700'` -> 执行当前语句，参与该文件整体逻辑。
- 262: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 263: `>` -> 执行当前语句，参与该文件整体逻辑。
- 264: `<CheckCircle2 className="mr-2 inline h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 265: `我的优惠券` -> 执行当前语句，参与该文件整体逻辑。
- 266: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 267: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 268: `(空行)` -> 空行，用于提升代码结构可读性。
- 269: `{/* 内容 */}` -> 执行当前语句，参与该文件整体逻辑。
- 270: `{!isAuthenticated && activeTab === 'my' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 271: `<div className="rounded-3xl border border-white/60 bg-white/85 p-12 text-center shadow-2xl backdrop-blur-md">` -> JSX/HTML 结构行，用于描述页面元素。
- 272: `<Gift className="mx-auto h-16 w-16 text-orange-400 mb-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 273: `<h2 className="text-2xl font-bold text-gray-800 mb-4">登录后查看优惠券</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 274: `<p className="text-gray-600 mb-6">登录账号以查看和管理你的优惠券</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 275: `<Link href="/login" className="inline-flex items-center gap-2 rounded-xl bg-gradient-to-r from-orange-500 to-amber-500 px-6 py-3 font-semibold text-white shadow-lg hover:from-orange-600 hover:to-amber-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 276: `去登录` -> 执行当前语句，参与该文件整体逻辑。
- 277: `<ChevronRight className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 278: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 279: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 280: `) : loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 281: `<div className="flex items-center justify-center py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 282: `<Loader2 className="h-8 w-8 animate-spin text-orange-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 283: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 284: `) : activeTab === 'available' ? (` -> 执行当前语句，参与该文件整体逻辑。
- 285: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 286: `{availableCoupons.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 287: `<div className="rounded-2xl border border-gray-100 bg-white p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 288: `<Ticket className="mx-auto h-12 w-12 text-gray-300 mb-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 289: `<p className="text-gray-500">暂无优惠券</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 290: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 291: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 292: `availableCoupons.map(c => renderCouponCard(c))` -> 执行当前语句，参与该文件整体逻辑。
- 293: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 294: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 295: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 296: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 297: `{myCoupons.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 298: `<div className="rounded-2xl border border-gray-100 bg-white p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 299: `<CheckCircle2 className="mx-auto h-12 w-12 text-gray-300 mb-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `<p className="text-gray-500 mb-4">还没有优惠券</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 301: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 302: `onClick={() => setActiveTab('available')}` -> 执行当前语句，参与该文件整体逻辑。
- 303: `className="text-orange-600 hover:underline"` -> 执行当前语句，参与该文件整体逻辑。
- 304: `>` -> 执行当前语句，参与该文件整体逻辑。
- 305: `去领取 →` -> 执行当前语句，参与该文件整体逻辑。
- 306: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 307: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 308: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 309: `myCoupons.map(uc => renderCouponCard(uc.coupon, true))` -> 执行当前语句，参与该文件整体逻辑。
- 310: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 311: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 312: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 313: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 314: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 315: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 316: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 317: `)` -> 结束当前语句或代码块。
- 318: `}` -> 结束当前语句或代码块。
