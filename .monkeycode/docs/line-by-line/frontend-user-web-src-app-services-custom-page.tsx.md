# `frontend/user-web/src/app/services/custom/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 私人定制服务模块 - 定制旅游服务介绍` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 私人定制服务介绍` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 服务特点展示：专属顾问/量身定制/品质保障/全程跟进` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 服务咨询入口` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 联系方式展示（电话/在线客服）` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 立即定制按钮` -> 注释行，用于解释设计意图或使用说明。
- 12: `*` -> 注释行，用于解释设计意图或使用说明。
- 13: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 15: `*` -> 注释行，用于解释设计意图或使用说明。
- 16: `* 【服务特点】` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 专属旅行顾问一对一服务` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - 根据需求定制专属行程` -> 注释行，用于解释设计意图或使用说明。
- 19: `* - 精选酒店、航班、用车服务` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - 行前、行中、行后全程服务` -> 注释行，用于解释设计意图或使用说明。
- 21: `*/` -> 注释行，用于解释设计意图或使用说明。
- 22: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 25: `import {` -> 导入依赖模块，供当前文件使用。
- 26: `Users,` -> 执行当前语句，参与该文件整体逻辑。
- 27: `Sparkles,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `Plane,` -> 执行当前语句，参与该文件整体逻辑。
- 29: `Clock,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `Phone,` -> 执行当前语句，参与该文件整体逻辑。
- 31: `MessageCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 32: `CheckCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 33: `ArrowRight,` -> 执行当前语句，参与该文件整体逻辑。
- 34: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 35: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 36: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `const services = [` -> 声明变量或常量，保存运行时数据。
- 39: `{ icon: Users, title: '专属顾问', desc: '一对一专属旅行顾问，全程服务' },` -> 执行当前语句，参与该文件整体逻辑。
- 40: `{ icon: Sparkles, title: '量身定制', desc: '根据您的需求，定制专属行程' },` -> 执行当前语句，参与该文件整体逻辑。
- 41: `{ icon: Plane, title: '品质保障', desc: '精选酒店、航班、用车服务' },` -> 执行当前语句，参与该文件整体逻辑。
- 42: `{ icon: Clock, title: '全程跟进', desc: '行前、行中、行后全程服务' },` -> 执行当前语句，参与该文件整体逻辑。
- 43: `]` -> 执行当前语句，参与该文件整体逻辑。
- 44: `(空行)` -> 空行，用于提升代码结构可读性。
- 45: `export default function CustomServicePage() {` -> 导出当前声明，供其他模块复用。
- 46: `return (` -> 返回结果或提前结束当前流程。
- 47: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 48: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 49: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 50: `(空行)` -> 空行，用于提升代码结构可读性。
- 51: `<div className="bg-gradient-to-r from-purple-600 to-pink-500 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 52: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 54: `<Users className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `<span className="text-xl font-bold">尊享服务</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 56: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 57: `<h1 className="text-4xl md:text-5xl font-bold mb-4">私人定制</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 58: `<p className="text-white/90 text-lg max-w-2xl mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `专属旅行顾问为您打造独一无二的旅行体验，从行程到细节，全程定制服务` -> 执行当前语句，参与该文件整体逻辑。
- 60: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 61: `<div className="flex flex-wrap gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 62: `<button className="inline-flex items-center gap-2 px-6 py-3 bg-white text-purple-600 rounded-xl font-bold hover:bg-purple-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 63: `<MessageCircle className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 64: `在线咨询` -> 执行当前语句，参与该文件整体逻辑。
- 65: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `<button className="inline-flex items-center gap-2 px-6 py-3 bg-white/20 text-white rounded-xl font-bold hover:bg-white/30 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 67: `<Phone className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 68: `400-888-9999` -> 执行当前语句，参与该文件整体逻辑。
- 69: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 70: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 72: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `(空行)` -> 空行，用于提升代码结构可读性。
- 76: `<div className="mb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `<h2 className="text-2xl font-bold text-slate-900 mb-8 text-center">我们的服务</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `{services.map((service) => {` -> 执行当前语句，参与该文件整体逻辑。
- 80: `const Icon = service.icon` -> 声明变量或常量，保存运行时数据。
- 81: `return (` -> 返回结果或提前结束当前流程。
- 82: `<div key={service.title} className="bg-white rounded-2xl border border-slate-200 p-6 text-center hover:shadow-lg transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 83: `<div className="w-14 h-14 bg-gradient-to-br from-purple-500 to-pink-500 rounded-xl flex items-center justify-center mx-auto mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 84: `<Icon className="h-7 w-7 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 85: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `<h3 className="font-bold text-slate-900 mb-2">{service.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 87: `<p className="text-sm text-slate-500">{service.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 88: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 89: `)` -> 结束当前语句或代码块。
- 90: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 91: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 92: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 93: `(空行)` -> 空行，用于提升代码结构可读性。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `<div className="bg-gradient-to-r from-purple-50 to-pink-50 rounded-3xl p-8 mb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 96: `<h2 className="text-2xl font-bold text-slate-900 mb-6 text-center">定制流程</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 97: `<div className="grid grid-cols-1 md:grid-cols-4 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 98: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 99: `{ step: '01', title: '需求沟通', desc: '告知旅行目的地、时间和特殊需求' },` -> 执行当前语句，参与该文件整体逻辑。
- 100: `{ step: '02', title: '方案定制', desc: '专属顾问为您设计行程方案' },` -> 执行当前语句，参与该文件整体逻辑。
- 101: `{ step: '03', title: '确认行程', desc: '根据反馈调整至满意为止' },` -> 执行当前语句，参与该文件整体逻辑。
- 102: `{ step: '04', title: '享受旅程', desc: '全程贴心服务，无忧出行' },` -> 执行当前语句，参与该文件整体逻辑。
- 103: `].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 104: `<div key={item.step} className="text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 105: `<div className="w-12 h-12 bg-gradient-to-br from-purple-600 to-pink-500 text-white rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 106: `{item.step}` -> 执行当前语句，参与该文件整体逻辑。
- 107: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 108: `<h3 className="font-bold text-slate-900 mb-2">{item.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 109: `<p className="text-sm text-slate-500">{item.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 110: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 112: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 113: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 114: `(空行)` -> 空行，用于提升代码结构可读性。
- 115: `(空行)` -> 空行，用于提升代码结构可读性。
- 116: `<div className="bg-white rounded-3xl border border-slate-200 p-8 text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `<h2 className="text-2xl font-bold text-slate-900 mb-4">立即定制您的专属行程</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `<p className="text-slate-500 mb-8">我们的旅行顾问将在24小时内与您联系</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `<div className="flex flex-wrap justify-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `<Link href="/contact" className="inline-flex items-center gap-2 px-6 py-3 bg-purple-600 text-white rounded-xl font-bold hover:bg-purple-700 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 121: `<MessageCircle className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `在线咨询` -> 执行当前语句，参与该文件整体逻辑。
- 123: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 124: `<Link href="/assistant" className="inline-flex items-center gap-2 px-6 py-3 border border-purple-300 text-purple-600 rounded-xl font-bold hover:bg-purple-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 125: `AI 快速规划` -> 执行当前语句，参与该文件整体逻辑。
- 126: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 127: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 128: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 130: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 132: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 133: `)` -> 结束当前语句或代码块。
- 134: `}` -> 结束当前语句或代码块。
