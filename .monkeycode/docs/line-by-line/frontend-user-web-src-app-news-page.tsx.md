# `frontend/user-web/src/app/news/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 新闻资讯模块 - 平台动态与行业资讯` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 新闻列表展示（静态数据）` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 新闻卡片：标题、日期、分类、摘要` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 分类标签：产品更新/行业资讯/合作动态/旅行攻略/公司新闻` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 新闻详情链接（跳转到 /news/${id}）` -> 注释行，用于解释设计意图或使用说明。
- 11: `*` -> 注释行，用于解释设计意图或使用说明。
- 12: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 14: `*` -> 注释行，用于解释设计意图或使用说明。
- 15: `* 【数据来源】` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 静态数据：news 数组（6条新闻资讯）` -> 注释行，用于解释设计意图或使用说明。
- 17: `*/` -> 注释行，用于解释设计意图或使用说明。
- 18: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 19: `(空行)` -> 空行，用于提升代码结构可读性。
- 20: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 21: `import {` -> 导入依赖模块，供当前文件使用。
- 22: `Newspaper,` -> 执行当前语句，参与该文件整体逻辑。
- 23: `Calendar,` -> 执行当前语句，参与该文件整体逻辑。
- 24: `ArrowRight,` -> 执行当前语句，参与该文件整体逻辑。
- 25: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 26: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 27: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 28: `(空行)` -> 空行，用于提升代码结构可读性。
- 29: `const news = [` -> 声明变量或常量，保存运行时数据。
- 30: `{ id: 1, title: '智能旅游助手V2.0发布：AI规划能力全面升级', date: '2026-04-01', category: '产品更新', excerpt: '全新AI算法，更智能的行程规划，更精准的景点推荐...' },` -> 执行当前语句，参与该文件整体逻辑。
- 31: `{ id: 2, title: '清明小长假热门目的地榜单发布', date: '2026-03-28', category: '行业资讯', excerpt: '清明假期将至，哪些目的地最受欢迎？一起来?..' },` -> 执行当前语句，参与该文件整体逻辑。
- 32: `{ id: 3, title: '智能旅游助手与多家航空公司达成战略合？', date: '2026-03-20', category: '合作动？', excerpt: '强强联合，为用户提供更优惠的机票和更便捷的服?..' },` -> 执行当前语句，参与该文件整体逻辑。
- 33: `{ id: 4, title: '春季出游指南：全国赏花胜地推？', date: '2026-03-15', category: '旅行攻略', excerpt: '春天来了，这些赏花胜地不可错?..' },` -> 执行当前语句，参与该文件整体逻辑。
- 34: `{ id: 5, title: '智能旅游助手用户突破1000？', date: '2026-03-10', category: '公司新闻', excerpt: '感谢每一位用户的信任与支持，我们将继续努?..' },` -> 执行当前语句，参与该文件整体逻辑。
- 35: `{ id: 6, title: '五一假期旅行趋势预测报告', date: '2026-03-05', category: '行业资讯', excerpt: '五一假期即将到来，出行趋势如何？一起来看预?..' },` -> 执行当前语句，参与该文件整体逻辑。
- 36: `]` -> 执行当前语句，参与该文件整体逻辑。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `export default function NewsPage() {` -> 导出当前声明，供其他模块复用。
- 39: `return (` -> 返回结果或提前结束当前流程。
- 40: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 41: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 42: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 43: `<div className="bg-gradient-to-r from-orange-500 to-red-500 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 44: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 45: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 46: `<Newspaper className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 47: `<span className="text-xl font-bold">新闻资讯</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 48: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 49: `<h1 className="text-4xl md:text-5xl font-bold mb-4">最新资</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 50: `<p className="text-white/90 text-lg max-w-2xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 51: `了解行业动态、获取旅行灵感、发现最新优?` -> 执行当前语句，参与该文件整体逻辑。
- 52: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 53: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 54: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `<div className="flex flex-wrap gap-3 mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `{['全部', '产品更新', '行业资讯', '合作动？', '旅行攻略', '公司新闻'].map((cat, index) => (` -> 执行当前语句，参与该文件整体逻辑。
- 60: `<button key={cat}` -> 执行当前语句，参与该文件整体逻辑。
- 61: `className={\`px-4 py-2 rounded-full text-sm font-medium transition ${` -> 执行当前语句，参与该文件整体逻辑。
- 62: `index === 0 ? 'bg-orange-600 text-white' : 'bg-white border border-slate-200 text-slate-600 hover:border-orange-300'` -> 执行当前语句，参与该文件整体逻辑。
- 63: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 64: `{cat}` -> 执行当前语句，参与该文件整体逻辑。
- 65: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 67: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 68: `(空行)` -> 空行，用于提升代码结构可读性。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `{news.map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 72: `<Link key={item.id} href="#"` -> 执行当前语句，参与该文件整体逻辑。
- 73: `className="bg-white rounded-2xl border border-slate-200 overflow-hidden hover:shadow-lg hover:border-orange-300 transition group">` -> 执行当前语句，参与该文件整体逻辑。
- 74: `<div className="h-40 bg-gradient-to-br from-orange-400 to-red-500 flex items-center justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `<Newspaper className="h-16 w-16 text-white/50" />` -> JSX/HTML 结构行，用于描述页面元素。
- 76: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `<div className="p-5">` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `<div className="flex items-center gap-3 mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `<span className="px-2 py-0.5 bg-orange-100 text-orange-700 text-xs rounded">{item.category}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 80: `<span className="flex items-center gap-1 text-xs text-slate-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 81: `<Calendar className="h-3 w-3" />{item.date}` -> 执行当前语句，参与该文件整体逻辑。
- 82: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 83: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 84: `<h3 className="font-bold text-slate-900 mb-2 group-hover:text-orange-600 transition">{item.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 85: `<p className="text-sm text-slate-500 mb-4 line-clamp-2">{item.excerpt}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `<span className="text-sm text-orange-600 flex items-center gap-1 group-hover:gap-2 transition-all">` -> JSX/HTML 结构行，用于描述页面元素。
- 87: `阅读全文 <ArrowRight className="h-4 w-4" />` -> 执行当前语句，参与该文件整体逻辑。
- 88: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 89: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 90: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 91: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 92: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 93: `(空行)` -> 空行，用于提升代码结构可读性。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `<div className="text-center mt-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 96: `<button className="px-8 py-3 border border-slate-300 text-slate-600 rounded-xl font-medium hover:bg-slate-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 97: `加载更多` -> 执行当前语句，参与该文件整体逻辑。
- 98: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 99: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 101: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 104: `)` -> 结束当前语句或代码块。
- 105: `}` -> 结束当前语句或代码块。
