# `frontend/user-web/src/app/orders/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 订单列表页模块 - 订单管理` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 6: `*   - 展示用户所有订单` -> 注释行，用于解释设计意图或使用说明。
- 7: `*   - 订单状态展示（待支付/已支付/已取消/已退款）` -> 注释行，用于解释设计意图或使用说明。
- 8: `*   - 订单详情查看（商品明细、预订信息）` -> 注释行，用于解释设计意图或使用说明。
- 9: `*   - 支付功能（模拟支付宝流程）` -> 注释行，用于解释设计意图或使用说明。
- 10: `*   - 取消订单功能` -> 注释行，用于解释设计意图或使用说明。
- 11: `*` -> 注释行，用于解释设计意图或使用说明。
- 12: `* 依赖项：` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - components/Navbar：顶部导航栏` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - components/Footer：底部页脚` -> 注释行，用于解释设计意图或使用说明。
- 15: `*   - store/index：Zustand 用户状态管理` -> 注释行，用于解释设计意图或使用说明。
- 16: `*   - types/index：TypeScript 类型定义` -> 注释行，用于解释设计意图或使用说明。
- 17: `*   - lucide-react：图标库` -> 注释行，用于解释设计意图或使用说明。
- 18: `*   - react-hot-toast：消息提示` -> 注释行，用于解释设计意图或使用说明。
- 19: `*` -> 注释行，用于解释设计意图或使用说明。
- 20: `* 数据来源：` -> 注释行，用于解释设计意图或使用说明。
- 21: `*   - GET /api/orders：订单列表（需认证）` -> 注释行，用于解释设计意图或使用说明。
- 22: `*   - POST /api/orders/{id}/pay：支付订单` -> 注释行，用于解释设计意图或使用说明。
- 23: `*   - POST /api/orders/{id}/cancel：取消订单` -> 注释行，用于解释设计意图或使用说明。
- 24: `*` -> 注释行，用于解释设计意图或使用说明。
- 25: `* 订单状态：` -> 注释行，用于解释设计意图或使用说明。
- 26: `*   - pending：待支付` -> 注释行，用于解释设计意图或使用说明。
- 27: `*   - paid：已支付` -> 注释行，用于解释设计意图或使用说明。
- 28: `*   - cancelled：已取消` -> 注释行，用于解释设计意图或使用说明。
- 29: `*   - refunded：已退款` -> 注释行，用于解释设计意图或使用说明。
- 30: `*` -> 注释行，用于解释设计意图或使用说明。
- 31: `* 状态管理：` -> 注释行，用于解释设计意图或使用说明。
- 32: `*   - localStorage：存储 JWT Token` -> 注释行，用于解释设计意图或使用说明。
- 33: `*   - useUserStore：用户登录状态` -> 注释行，用于解释设计意图或使用说明。
- 34: `*/` -> 注释行，用于解释设计意图或使用说明。
- 35: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `import { useEffect, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 38: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 39: `import { useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 40: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 41: `import { Loader2, CreditCard, Calendar, Clock, MapPin, Package, CheckCircle, XCircle, RefreshCw, ArrowLeft } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 42: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 43: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 44: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 45: `import type { Order } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `const statusMap: Record<string, { label: string; color: string; icon: React.ReactNode }> = {` -> 声明变量或常量，保存运行时数据。
- 51: `pending: { label: '待支付', color: 'text-orange-500 bg-orange-50', icon: <CreditCard className="h-4 w-4" /> },` -> 执行当前语句，参与该文件整体逻辑。
- 52: `paid: { label: '已支付', color: 'text-green-500 bg-green-50', icon: <CheckCircle className="h-4 w-4" /> },` -> 执行当前语句，参与该文件整体逻辑。
- 53: `cancelled: { label: '已取消', color: 'text-gray-500 bg-gray-50', icon: <XCircle className="h-4 w-4" /> },` -> 执行当前语句，参与该文件整体逻辑。
- 54: `refunded: { label: '已退款', color: 'text-blue-500 bg-blue-50', icon: <RefreshCw className="h-4 w-4" /> },` -> 执行当前语句，参与该文件整体逻辑。
- 55: `}` -> 结束当前语句或代码块。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `export default function OrdersPage() {` -> 导出当前声明，供其他模块复用。
- 58: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 59: `const { isAuthenticated, logout } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 60: `const [orders, setOrders] = useState<Order[]>([])` -> 声明变量或常量，保存运行时数据。
- 61: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 62: `const [payingOrderId, setPayingOrderId] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 63: `const [cancellingOrderId, setCancellingOrderId] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 66: `const loadOrders = async () => {` -> 声明变量或常量，保存运行时数据。
- 67: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 68: `setOrders([])` -> 执行当前语句，参与该文件整体逻辑。
- 69: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 70: `return` -> 返回结果或提前结束当前流程。
- 71: `}` -> 结束当前语句或代码块。
- 72: `const token = typeof window !== 'undefined' ? localStorage.getItem('auth_token') : null` -> 声明变量或常量，保存运行时数据。
- 73: `if (!token) {` -> 条件判断分支，根据场景执行不同逻辑。
- 74: `router.push('/login')` -> 执行当前语句，参与该文件整体逻辑。
- 75: `return` -> 返回结果或提前结束当前流程。
- 76: `}` -> 结束当前语句或代码块。
- 77: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 78: `try {` -> 异常处理逻辑，提升程序健壮性。
- 79: `const res = await fetch(\`${API_BASE_URL}/api/orders\`, { headers: { Authorization: \`Bearer ${token}\` } })` -> 声明变量或常量，保存运行时数据。
- 80: `if (res.status === 401) {` -> 条件判断分支，根据场景执行不同逻辑。
- 81: `router.push('/login')` -> 执行当前语句，参与该文件整体逻辑。
- 82: `return` -> 返回结果或提前结束当前流程。
- 83: `}` -> 结束当前语句或代码块。
- 84: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 85: `if (!res.ok || !data?.success) throw new Error(data?.error || \`HTTP ${res.status}\`)` -> 条件判断分支，根据场景执行不同逻辑。
- 86: `setOrders((data?.orders ?? []) as Order[])` -> 执行当前语句，参与该文件整体逻辑。
- 87: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 88: `toast.error(e?.message || '加载订单失败')` -> 执行当前语句，参与该文件整体逻辑。
- 89: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 90: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 91: `}` -> 结束当前语句或代码块。
- 92: `}` -> 结束当前语句或代码块。
- 93: `loadOrders()` -> 执行当前语句，参与该文件整体逻辑。
- 94: `// eslint-disable-next-line react-hooks/exhaustive-deps` -> 注释行，用于解释设计意图或使用说明。
- 95: `}, [isAuthenticated])` -> 执行当前语句，参与该文件整体逻辑。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `const onLogout = () => {` -> 声明变量或常量，保存运行时数据。
- 98: `logout()` -> 执行当前语句，参与该文件整体逻辑。
- 99: `toast.success('已退出登录')` -> 执行当前语句，参与该文件整体逻辑。
- 100: `router.push('/')` -> 执行当前语句，参与该文件整体逻辑。
- 101: `}` -> 结束当前语句或代码块。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `// 支付订单` -> 注释行，用于解释设计意图或使用说明。
- 104: `const handlePay = async (orderId: string) => {` -> 声明变量或常量，保存运行时数据。
- 105: `const token = localStorage.getItem('auth_token')` -> 声明变量或常量，保存运行时数据。
- 106: `if (!token) {` -> 条件判断分支，根据场景执行不同逻辑。
- 107: `toast.error('请先登录')` -> 执行当前语句，参与该文件整体逻辑。
- 108: `router.push('/login')` -> 执行当前语句，参与该文件整体逻辑。
- 109: `return` -> 返回结果或提前结束当前流程。
- 110: `}` -> 结束当前语句或代码块。
- 111: `(空行)` -> 空行，用于提升代码结构可读性。
- 112: `setPayingOrderId(orderId)` -> 执行当前语句，参与该文件整体逻辑。
- 113: `try {` -> 异常处理逻辑，提升程序健壮性。
- 114: `const res = await fetch(\`${API_BASE_URL}/api/orders/${orderId}/pay\`, {` -> 声明变量或常量，保存运行时数据。
- 115: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 116: `headers: { Authorization: \`Bearer ${token}\`, 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 117: `body: JSON.stringify({ payment_method: 'alipay' }),` -> 执行当前语句，参与该文件整体逻辑。
- 118: `})` -> 执行当前语句，参与该文件整体逻辑。
- 119: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 120: `(空行)` -> 空行，用于提升代码结构可读性。
- 121: `if (!res.ok || !data?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 122: `throw new Error(data?.error || '支付请求失败')` -> 执行当前语句，参与该文件整体逻辑。
- 123: `}` -> 结束当前语句或代码块。
- 124: `(空行)` -> 空行，用于提升代码结构可读性。
- 125: `// 如果后端返回了模拟支付链接，走回调流程` -> 注释行，用于解释设计意图或使用说明。
- 126: `if (data.payment?.pay_url) {` -> 条件判断分支，根据场景执行不同逻辑。
- 127: `const payRes = await fetch(\`${API_BASE_URL}${data.payment.pay_url}/callback\`, {` -> 声明变量或常量，保存运行时数据。
- 128: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 129: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 130: `body: JSON.stringify({ order_no: data.payment.order_no }),` -> 执行当前语句，参与该文件整体逻辑。
- 131: `})` -> 执行当前语句，参与该文件整体逻辑。
- 132: `const payData = await payRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 133: `if (!payData?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 134: `throw new Error(payData?.error || '支付失败')` -> 执行当前语句，参与该文件整体逻辑。
- 135: `}` -> 结束当前语句或代码块。
- 136: `}` -> 结束当前语句或代码块。
- 137: `(空行)` -> 空行，用于提升代码结构可读性。
- 138: `toast.success('支付成功')` -> 执行当前语句，参与该文件整体逻辑。
- 139: `// 刷新订单列表` -> 注释行，用于解释设计意图或使用说明。
- 140: `const refreshRes = await fetch(\`${API_BASE_URL}/api/orders\`, { headers: { Authorization: \`Bearer ${token}\` } })` -> 声明变量或常量，保存运行时数据。
- 141: `const refreshData = await refreshRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 142: `if (refreshData?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 143: `setOrders(refreshData.orders)` -> 执行当前语句，参与该文件整体逻辑。
- 144: `}` -> 结束当前语句或代码块。
- 145: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 146: `toast.error(e?.message || '支付失败')` -> 执行当前语句，参与该文件整体逻辑。
- 147: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 148: `setPayingOrderId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 149: `}` -> 结束当前语句或代码块。
- 150: `}` -> 结束当前语句或代码块。
- 151: `(空行)` -> 空行，用于提升代码结构可读性。
- 152: `// 取消订单` -> 注释行，用于解释设计意图或使用说明。
- 153: `const handleCancel = async (orderId: string) => {` -> 声明变量或常量，保存运行时数据。
- 154: `if (!confirm('确定要取消这个订单吗？')) return` -> 条件判断分支，根据场景执行不同逻辑。
- 155: `(空行)` -> 空行，用于提升代码结构可读性。
- 156: `const token = localStorage.getItem('auth_token')` -> 声明变量或常量，保存运行时数据。
- 157: `if (!token) return` -> 条件判断分支，根据场景执行不同逻辑。
- 158: `(空行)` -> 空行，用于提升代码结构可读性。
- 159: `setCancellingOrderId(orderId)` -> 执行当前语句，参与该文件整体逻辑。
- 160: `try {` -> 异常处理逻辑，提升程序健壮性。
- 161: `const res = await fetch(\`${API_BASE_URL}/api/orders/${orderId}/cancel\`, {` -> 声明变量或常量，保存运行时数据。
- 162: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 163: `headers: { Authorization: \`Bearer ${token}\`, 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 164: `})` -> 执行当前语句，参与该文件整体逻辑。
- 165: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 166: `(空行)` -> 空行，用于提升代码结构可读性。
- 167: `if (!res.ok || !data?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 168: `throw new Error(data?.error || '取消失败')` -> 执行当前语句，参与该文件整体逻辑。
- 169: `}` -> 结束当前语句或代码块。
- 170: `(空行)` -> 空行，用于提升代码结构可读性。
- 171: `toast.success('订单已取？')` -> 执行当前语句，参与该文件整体逻辑。
- 172: `// 刷新订单列表` -> 注释行，用于解释设计意图或使用说明。
- 173: `const refreshRes = await fetch(\`${API_BASE_URL}/api/orders\`, { headers: { Authorization: \`Bearer ${token}\` } })` -> 声明变量或常量，保存运行时数据。
- 174: `const refreshData = await refreshRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 175: `if (refreshData?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 176: `setOrders(refreshData.orders)` -> 执行当前语句，参与该文件整体逻辑。
- 177: `}` -> 结束当前语句或代码块。
- 178: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 179: `toast.error(e?.message || '取消失败')` -> 执行当前语句，参与该文件整体逻辑。
- 180: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 181: `setCancellingOrderId(null)` -> 执行当前语句，参与该文件整体逻辑。
- 182: `}` -> 结束当前语句或代码块。
- 183: `}` -> 结束当前语句或代码块。
- 184: `(空行)` -> 空行，用于提升代码结构可读性。
- 185: `return (` -> 返回结果或提前结束当前流程。
- 186: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 187: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 188: `<main className="pt-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 189: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 190: `<div className="card p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 191: `<div className="flex items-start justify-between gap-4 flex-wrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 192: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 193: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 194: `onClick={() => router.back()}` -> 执行当前语句，参与该文件整体逻辑。
- 195: `className="p-2 rounded-xl border border-gray-200 bg-white text-gray-500 hover:text-gray-800 hover:border-gray-300 hover:shadow-sm transition-all duration-200 active:scale-95"` -> 执行当前语句，参与该文件整体逻辑。
- 196: `title="返回"` -> 执行当前语句，参与该文件整体逻辑。
- 197: `>` -> 执行当前语句，参与该文件整体逻辑。
- 198: `<ArrowLeft className="w-5 h-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 199: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 200: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 201: `<h1 className="text-2xl font-bold text-gray-900">我的订单</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 202: `<p className="text-gray-600 mt-2">订单来自后端 \`/api/orders\`</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 203: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 204: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 205: `{isAuthenticated ? (` -> 执行当前语句，参与该文件整体逻辑。
- 206: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 207: `onClick={onLogout}` -> 执行当前语句，参与该文件整体逻辑。
- 208: `className="inline-flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-gray-600 bg-white border border-gray-200 rounded-xl shadow-sm hover:text-red-600 hover:border-red-200 hover:bg-red-50 hover:shadow-md transition-all duration-200 active:scale-95"` -> 执行当前语句，参与该文件整体逻辑。
- 209: `>` -> 执行当前语句，参与该文件整体逻辑。
- 210: `<svg className="w-4 h-4" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">` -> JSX/HTML 结构行，用于描述页面元素。
- 211: `<path strokeLinecap="round" strokeLinejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0013.5 3h-6a2.25 2.25 0 00-2.25 2.25v13.5A2.25 2.25 0 007.5 21h6a2.25 2.25 0 002.25-2.25V15M18 12h4m0 0l-3-3m3 3l-3 3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 212: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 213: `退出登录` -> 执行当前语句，参与该文件整体逻辑。
- 214: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 215: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 216: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 217: `(空行)` -> 空行，用于提升代码结构可读性。
- 218: `{!isAuthenticated ? (` -> 执行当前语句，参与该文件整体逻辑。
- 219: `<div className="mt-6 card p-4 bg-white border border-gray-200 text-gray-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 220: `请先 <Link href="/login" className="underline text-blue-600">登录</Link> 后查看订单` -> 执行当前语句，参与该文件整体逻辑。
- 221: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 222: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 223: `<div className="mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 224: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 225: `<div className="flex items-center justify-center py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 226: `<Loader2 className="h-8 w-8 animate-spin text-blue-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `<span className="ml-2 text-gray-600">加载订单中...</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 228: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 229: `) : orders.length === 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 230: `<div className="card p-8 text-center text-gray-600 bg-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `<Package className="h-12 w-12 mx-auto text-gray-300 mb-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 232: `<p>暂无订单</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 233: `<Link href="/products" className="text-blue-600 hover:underline mt-2 inline-block">` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `去逛逛商城` -> 执行当前语句，参与该文件整体逻辑。
- 235: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 238: `<div className="space-y-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 239: `{orders.map((o) => {` -> 执行当前语句，参与该文件整体逻辑。
- 240: `const statusInfo = statusMap[o.status] || { label: o.status, color: 'text-gray-500 bg-gray-50', icon: null }` -> 声明变量或常量，保存运行时数据。
- 241: `return (` -> 返回结果或提前结束当前流程。
- 242: `<div key={o.id} className="card p-5 bg-white border border-gray-100 shadow-sm hover:shadow-md transition-shadow">` -> JSX/HTML 结构行，用于描述页面元素。
- 243: `(空行)` -> 空行，用于提升代码结构可读性。
- 244: `<div className="flex items-start justify-between gap-4 flex-wrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 245: `<div className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 246: `<div className="flex items-center gap-2 flex-wrap">` -> JSX/HTML 结构行，用于描述页面元素。
- 247: `<span className="text-sm text-gray-500">订单号：{o.order_no}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 248: `<span className={\`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium ${statusInfo.color}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 249: `{statusInfo.icon}` -> 执行当前语句，参与该文件整体逻辑。
- 250: `{statusInfo.label}` -> 执行当前语句，参与该文件整体逻辑。
- 251: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 252: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 253: `<div className="text-gray-900 font-bold text-lg mt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 254: `¥{Number(o.total_amount || 0).toLocaleString()}` -> 执行当前语句，参与该文件整体逻辑。
- 255: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 256: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 257: `<div className="text-xs text-gray-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 258: `{o.created_at ? new Date(o.created_at).toLocaleString('zh-CN') : ''}` -> 执行当前语句，参与该文件整体逻辑。
- 259: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 260: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 261: `(空行)` -> 空行，用于提升代码结构可读性。
- 262: `(空行)` -> 空行，用于提升代码结构可读性。
- 263: `<div className="mt-4 space-y-3 border-t border-gray-100 pt-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 264: `{(o.items || []).map((it: any) => (` -> 执行当前语句，参与该文件整体逻辑。
- 265: `<div key={it.id || it.product_id} className="flex items-start justify-between gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 266: `<div className="flex-1 min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 267: `<div className="font-medium text-gray-900">{it.product_name}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 268: `<div className="text-sm text-gray-500 mt-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 269: `{it.product_type === 'ticket' ? '门票' : it.product_type} · x{it.quantity}` -> 执行当前语句，参与该文件整体逻辑。
- 270: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 271: `(空行)` -> 空行，用于提升代码结构可读性。
- 272: `{it.booking_details && (` -> 执行当前语句，参与该文件整体逻辑。
- 273: `<div className="mt-2 flex flex-wrap gap-2 text-xs text-gray-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 274: `{it.booking_details.date && (` -> 执行当前语句，参与该文件整体逻辑。
- 275: `<span className="inline-flex items-center gap-1 px-2 py-1 bg-blue-50 text-blue-600 rounded">` -> JSX/HTML 结构行，用于描述页面元素。
- 276: `<Calendar className="h-3 w-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 277: `{it.booking_details.date}` -> 执行当前语句，参与该文件整体逻辑。
- 278: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 279: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 280: `{it.booking_details.time_slot && (` -> 执行当前语句，参与该文件整体逻辑。
- 281: `<span className="inline-flex items-center gap-1 px-2 py-1 bg-purple-50 text-purple-600 rounded">` -> JSX/HTML 结构行，用于描述页面元素。
- 282: `<Clock className="h-3 w-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 283: `{it.booking_details.time_slot}` -> 执行当前语句，参与该文件整体逻辑。
- 284: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 285: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 286: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 287: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 288: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 289: `<div className="font-semibold text-gray-900">` -> JSX/HTML 结构行，用于描述页面元素。
- 290: `¥{Number(it.total_price || 0).toLocaleString()}` -> 执行当前语句，参与该文件整体逻辑。
- 291: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 292: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 293: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 294: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 295: `(空行)` -> 空行，用于提升代码结构可读性。
- 296: `(空行)` -> 空行，用于提升代码结构可读性。
- 297: `<div className="mt-4 pt-4 border-t border-gray-100 flex items-center justify-end gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 298: `{o.status === 'pending' && (` -> 执行当前语句，参与该文件整体逻辑。
- 299: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 301: `onClick={() => handleCancel(o.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 302: `disabled={cancellingOrderId === o.id}` -> 执行当前语句，参与该文件整体逻辑。
- 303: `className="px-4 py-2 text-sm text-gray-600 hover:text-gray-800 hover:bg-gray-100 rounded-lg transition"` -> 执行当前语句，参与该文件整体逻辑。
- 304: `>` -> 执行当前语句，参与该文件整体逻辑。
- 305: `{cancellingOrderId === o.id ? (` -> 执行当前语句，参与该文件整体逻辑。
- 306: `<span className="flex items-center gap-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 307: `<Loader2 className="h-3 w-3 animate-spin" />` -> JSX/HTML 结构行，用于描述页面元素。
- 308: `取消中...` -> 执行当前语句，参与该文件整体逻辑。
- 309: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 310: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 311: `'取消订单'` -> 执行当前语句，参与该文件整体逻辑。
- 312: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 313: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 314: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 315: `onClick={() => handlePay(o.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 316: `disabled={payingOrderId === o.id}` -> 执行当前语句，参与该文件整体逻辑。
- 317: `className="px-5 py-2 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-lg transition flex items-center gap-2"` -> 执行当前语句，参与该文件整体逻辑。
- 318: `>` -> 执行当前语句，参与该文件整体逻辑。
- 319: `{payingOrderId === o.id ? (` -> 执行当前语句，参与该文件整体逻辑。
- 320: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 321: `<Loader2 className="h-4 w-4 animate-spin" />` -> JSX/HTML 结构行，用于描述页面元素。
- 322: `支付中...` -> 执行当前语句，参与该文件整体逻辑。
- 323: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 324: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 325: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 326: `<CreditCard className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 327: `立即支付` -> 执行当前语句，参与该文件整体逻辑。
- 328: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 329: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 330: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 331: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 332: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 333: `{o.status === 'paid' && (` -> 执行当前语句，参与该文件整体逻辑。
- 334: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 335: `href={\`/orders/${o.id}/ticket\`}` -> 执行当前语句，参与该文件整体逻辑。
- 336: `className="px-5 py-2 text-sm font-medium text-blue-600 bg-blue-50 hover:bg-blue-100 rounded-lg transition"` -> 执行当前语句，参与该文件整体逻辑。
- 337: `>` -> 执行当前语句，参与该文件整体逻辑。
- 338: `查看电子票` -> 执行当前语句，参与该文件整体逻辑。
- 339: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 340: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 341: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 342: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 343: `)` -> 结束当前语句或代码块。
- 344: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 345: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 346: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 347: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 348: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 349: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 350: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 351: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 352: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 353: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 354: `)` -> 结束当前语句或代码块。
- 355: `}` -> 结束当前语句或代码块。
- 356: `(空行)` -> 空行，用于提升代码结构可读性。
