# `scripts/import_destinations_from_json.py` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `import json`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 2: `import os`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 3: `import re`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 4: `import sys`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 5: `from pathlib import Path`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 7: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 8: `def _parse_ticket_price(price_range: str) -> float:`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 9: `if not price_range:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 10: `return 0.0`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 11: `# Examples: "60-100元", "免费-景点单独收费", "40-45元"`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `if "免费" in price_range:  # 检查是否包含  # 检查是否包含  # 检查是否包含`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 13: `return 0.0`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 14: `m = re.search(r"(\d+(\.\d+)?)", price_range)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 15: `if not m:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 16: `return 0.0`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 17: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 18: `return float(m.group(1))`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 19: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 20: `return 0.0`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 21: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 22: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 23: `def main():  # 主函数：程序入口点  # 主函数：程序入口点  # 主函数：程序入口点`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 24: `# Ensure project root is on sys.path so we can import app.py as module "app"`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 25: `project_root = Path(__file__).resolve().parents[1]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `if str(project_root) not in sys.path:  # 检查是否包含  # 检查是否包含  # 检查是否包含`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 27: `sys.path.insert(0, str(project_root))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 29: `json_path = Path(os.environ.get("DESTINATIONS_JSON_PATH", r"D:\travel-assisent-master\destinations.json"))  # 路径配置json_path  # 路径配置json_path  # 路径配置json_path`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `if not json_path.exists():`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 31: `raise SystemExit(f"destinations.json not found: {json_path}")  # 抛出异常  # 抛出异常  # 抛出异常`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 33: `raw = json.loads(json_path.read_text(encoding="utf-8"))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `if not isinstance(raw, list):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 35: `raise SystemExit("destinations.json must be a list")  # 抛出异常  # 抛出异常  # 抛出异常`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 37: `from app import app, db, Destination`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 39: `upserted = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `created = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `updated = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `skipped = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 44: `with app.app_context():`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `db.create_all()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 47: `for item in raw:  # 遍历item  # 遍历item  # 遍历item`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 48: `if not isinstance(item, dict):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 49: `skipped += 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `continue`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 52: `name = (item.get("name") or "").strip()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `city = (item.get("city") or "").strip()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `province = (item.get("province") or "").strip()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `if not name or not city or not province:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 56: `skipped += 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `continue`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 58: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 59: `description = item.get("description") or ""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `rating = float(item.get("rating") or 0) if item.get("rating") is not None else 0.0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `opening_hours = item.get("opening_hours") or ""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `cover_image = item.get("cover_image") or ""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `ticket_price = _parse_ticket_price(item.get("price_range") or "")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 65: `obj = Destination.query.filter_by(name=name, city=city, province=province).first()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 66: `if obj is None:  # 检查是否为空  # 检查是否为空  # 检查是否为空`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 67: `obj = Destination(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `name=name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `city=city,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `province=province,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `description=description,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `cover_image=cover_image,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `rating=rating or 5.0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `ticket_price=ticket_price,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `open_time=opening_hours,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 77: `db.session.add(obj)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 78: `created += 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `else:  # 否则执行  # 否则执行  # 否则执行`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 80: `# Only overwrite fields when new value is present`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 81: `obj.description = description or obj.description`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `obj.cover_image = cover_image or obj.cover_image`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `obj.rating = rating or obj.rating`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `obj.ticket_price = ticket_price if ticket_price is not None else obj.ticket_price`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `obj.open_time = opening_hours or obj.open_time`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `updated += 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 88: `upserted += 1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 90: `db.session.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 92: `print(f"import ok: total={len(raw)} upserted={upserted} created={created} updated={updated} skipped={skipped}")  # 总数变量print(f"import ok: total  # 总数变量print(f"import ok: total  # 总数变量print(f"import ok: total`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 93: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 94: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 95: `if __name__ == '__main__':`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 96: `main()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
