# `frontend/user-web/src/app/sitemap.ts` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `import { MetadataRoute } from 'next'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 2: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 3: `// 静态页面路?`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `const staticPaths = [`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 5: `'/',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `'/about',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `'/contact',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `'/privacy',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `'/terms',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `'/careers',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `'/partners',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `'/services',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `'/help',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `'/feedback',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 15: `'/news',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `'/deals',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 17: `'/destinations',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `'/planner',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `'/assistant'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 22: `// 获取动态路?`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `async function getDynamicPaths() {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 24: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 25: `// 获取所有目的地`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 26: `const destinationsRes = await fetch(`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 27: `\`${process.env.NEXT_PUBLIC_API_BASE_URL || 'http://127.0.0.1:5001'}/api/destinations?per_page=100\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `{ cache: 'force-cache' }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 30: `const destinationsData = await destinationsRes.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 31: `const destinations = destinationsData?.destinations || destinationsData?.items || []`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 32: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 33: `const destinationPaths = destinations.map((destination: any) => ({`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 34: `url: \`/destinations/${destination.id}\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `lastModified: new Date(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `changeFrequency: 'weekly' as const,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `priority: 0.8`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `}))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 40: `// 获取所有产?`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 41: `const productsRes = await fetch(`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 42: `\`${process.env.NEXT_PUBLIC_API_BASE_URL || 'http://127.0.0.1:5001'}/api/products?status=active&limit=100\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `{ cache: 'force-cache' }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 45: `const productsData = await productsRes.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 46: `const products = productsData?.products || productsData?.items || []`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `const productPaths = products.map((product: any) => ({`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 49: `url: \`/products/${product.id}\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `lastModified: new Date(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `changeFrequency: 'weekly' as const,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `priority: 0.7`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `}))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 55: `return [...destinationPaths, ...productPaths]`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 56: `} catch (error) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `console.warn('Failed to fetch dynamic paths for sitemap:', error)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 58: `// 返回空数组而不是抛出错误，这样sitemap仍能生成`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 59: `return []`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 60: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 61: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 62: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 63: `export default async function sitemap(): Promise<MetadataRoute.Sitemap> {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 64: `const dynamicPaths = await getDynamicPaths()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 65: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 66: `const staticSitemap = staticPaths.map(path => ({`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 67: `url: \`${process.env.NEXT_PUBLIC_SITE_URL}${path}\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `lastModified: new Date(),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `changeFrequency: 'monthly' as const,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `priority: path === '/' ? 1 : 0.5`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `}))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 73: `return [...staticSitemap, ...dynamicPaths]`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 74: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
