# `frontend/user-web/src/app/services/visa/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 签证服务模块 - 各国签证办理介绍` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 支持国家列表展示：日韩/美英/申根/澳洲等` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 办理时长信息` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 服务内容：材料指导/加急服务/全程代办/贴心跟进` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 签证咨询入口` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 签证办理入口` -> 注释行，用于解释设计意图或使用说明。
- 12: `*` -> 注释行，用于解释设计意图或使用说明。
- 13: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 15: `*` -> 注释行，用于解释设计意图或使用说明。
- 16: `* 【支持国家】` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 日本、韩国、美国、英国、澳大利亚、申根国家` -> 注释行，用于解释设计意图或使用说明。
- 18: `*` -> 注释行，用于解释设计意图或使用说明。
- 19: `* 【服务内容】` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - 专业顾问一对一指导` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - 加急办理服务` -> 注释行，用于解释设计意图或使用说明。
- 22: `* - 代填表格、代预约、代取护照` -> 注释行，用于解释设计意图或使用说明。
- 23: `* - 实时进度通知` -> 注释行，用于解释设计意图或使用说明。
- 24: `*/` -> 注释行，用于解释设计意图或使用说明。
- 25: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 28: `import {` -> 导入依赖模块，供当前文件使用。
- 29: `Stamp,` -> 执行当前语句，参与该文件整体逻辑。
- 30: `FileText,` -> 执行当前语句，参与该文件整体逻辑。
- 31: `Clock,` -> 执行当前语句，参与该文件整体逻辑。
- 32: `CheckCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 33: `Phone,` -> 执行当前语句，参与该文件整体逻辑。
- 34: `MessageCircle,` -> 执行当前语句，参与该文件整体逻辑。
- 35: `Globe,` -> 执行当前语句，参与该文件整体逻辑。
- 36: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 37: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 38: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `const countries = [` -> 声明变量或常量，保存运行时数据。
- 41: `{ name: '日本', flag: '🇯🇵', days: '5-10个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 42: `{ name: '韩国', flag: '🇰🇷', days: '3-5个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 43: `{ name: '美国', flag: '🇺🇸', days: '10-15个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 44: `{ name: '英国', flag: '🇬🇧', days: '15-20个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 45: `{ name: '澳大利亚', flag: '🇦🇺', days: '10-15个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 46: `{ name: '申根国家', flag: '🇪🇺', days: '7-10个工作日' },` -> 执行当前语句，参与该文件整体逻辑。
- 47: `]` -> 执行当前语句，参与该文件整体逻辑。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `const services = [` -> 声明变量或常量，保存运行时数据。
- 50: `{ icon: FileText, title: '材料指导', desc: '专业顾问一对一指导，准备材料不再迷茫' },` -> 执行当前语句，参与该文件整体逻辑。
- 51: `{ icon: Clock, title: '加急服务', desc: '提供加急办理，最快3个工作日出签' },` -> 执行当前语句，参与该文件整体逻辑。
- 52: `{ icon: CheckCircle, title: '全程代办', desc: '代填表格、代预约、代取护照' },` -> 执行当前语句，参与该文件整体逻辑。
- 53: `{ icon: MessageCircle, title: '贴心跟进', desc: '实时进度通知，有问题随时咨询' },` -> 执行当前语句，参与该文件整体逻辑。
- 54: `]` -> 执行当前语句，参与该文件整体逻辑。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `export default function VisaPage() {` -> 导出当前声明，供其他模块复用。
- 57: `return (` -> 返回结果或提前结束当前流程。
- 58: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 59: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 60: `<main className="pt-16 pb-28 lg:pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 61: `<div className="bg-gradient-to-r from-violet-600 to-purple-500 text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 62: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 63: `<div className="flex items-center gap-3 mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 64: `<Stamp className="h-8 w-8" />` -> JSX/HTML 结构行，用于描述页面元素。
- 65: `<span className="text-xl font-bold">签证服务</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 67: `<h1 className="text-4xl md:text-5xl font-bold mb-4">签证办理</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 68: `<p className="text-white/90 text-lg max-w-2xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 69: `专业签证服务，让您的出境旅行更简单。我们提供全球100+国家的签证办理服务` -> 执行当前语句，参与该文件整体逻辑。
- 70: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 72: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `(空行)` -> 空行，用于提升代码结构可读性。
- 76: `<div className="mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `<h2 className="text-2xl font-bold text-slate-900 mb-6">热门签证国家</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 78: `<div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 79: `{countries.map((country) => (` -> 执行当前语句，参与该文件整体逻辑。
- 80: `<Link key={country.name} href="/contact"` -> 执行当前语句，参与该文件整体逻辑。
- 81: `className="bg-white rounded-xl border border-slate-200 p-4 text-center hover:shadow-lg hover:border-violet-300 transition">` -> 执行当前语句，参与该文件整体逻辑。
- 82: `<div className="text-4xl mb-2">{country.flag}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 83: `<h3 className="font-bold text-slate-900 mb-1">{country.name}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 84: `<p className="text-xs text-slate-500">{country.days}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 85: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 86: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 87: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 88: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 89: `(空行)` -> 空行，用于提升代码结构可读性。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `<div className="mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 92: `<h2 className="text-2xl font-bold text-slate-900 mb-6 text-center">我们的服务</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 93: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 94: `{services.map((service) => {` -> 执行当前语句，参与该文件整体逻辑。
- 95: `const Icon = service.icon` -> 声明变量或常量，保存运行时数据。
- 96: `return (` -> 返回结果或提前结束当前流程。
- 97: `<div key={service.title} className="bg-white rounded-2xl border border-slate-200 p-6 text-center hover:shadow-lg transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 98: `<Icon className="h-10 w-10 text-violet-600 mx-auto mb-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 99: `<h3 className="font-bold text-slate-900 mb-2">{service.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `<p className="text-sm text-slate-500">{service.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 101: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `)` -> 结束当前语句或代码块。
- 103: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 104: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 105: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `<div className="bg-gradient-to-r from-violet-50 to-purple-50 rounded-3xl p-8 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 109: `<h2 className="text-2xl font-bold text-slate-900 mb-6 text-center">办理流程</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 110: `<div className="grid grid-cols-1 md:grid-cols-5 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `{[` -> 执行当前语句，参与该文件整体逻辑。
- 112: `{ step: '1', title: '咨询下单', desc: '选择国家，提交订单' },` -> 执行当前语句，参与该文件整体逻辑。
- 113: `{ step: '2', title: '准备材料', desc: '按清单准备所需材料' },` -> 执行当前语句，参与该文件整体逻辑。
- 114: `{ step: '3', title: '邮寄材料', desc: '将材料邮寄给我们' },` -> 执行当前语句，参与该文件整体逻辑。
- 115: `{ step: '4', title: '办理签证', desc: '我们代为递交申请' },` -> 执行当前语句，参与该文件整体逻辑。
- 116: `{ step: '5', title: '收取护照', desc: '签证办好后寄回给您' },` -> 执行当前语句，参与该文件整体逻辑。
- 117: `].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 118: `<div key={item.step} className="text-center relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `<div className="w-10 h-10 bg-violet-600 text-white rounded-full flex items-center justify-center text-lg font-bold mx-auto mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `{item.step}` -> 执行当前语句，参与该文件整体逻辑。
- 121: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 122: `<h3 className="font-bold text-slate-900 mb-1 text-sm">{item.title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `<p className="text-xs text-slate-500">{item.desc}</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 124: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 125: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 126: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 127: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 128: `(空行)` -> 空行，用于提升代码结构可读性。
- 129: `(空行)` -> 空行，用于提升代码结构可读性。
- 130: `<div className="bg-white rounded-2xl border border-slate-200 p-8 mb-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `<h2 className="text-2xl font-bold text-slate-900 mb-6">常见签证材料</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 132: `<div className="grid grid-cols-1 md:grid-cols-2 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 133: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 134: `<h3 className="font-bold text-slate-900 mb-3 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 135: `<Globe className="h-5 w-5 text-violet-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 136: `通用材料` -> 执行当前语句，参与该文件整体逻辑。
- 137: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 138: `<ul className="space-y-2 text-slate-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 139: `{['护照原件（有效期6个月以上）', '签证申请表', '白底彩照2张', '身份证复印件', '户口本复印件', '在职证明/在读证明'].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 140: `<li key={item} className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 141: `<CheckCircle className="h-4 w-4 text-green-500 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 142: `{item}` -> 执行当前语句，参与该文件整体逻辑。
- 143: `</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 144: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 145: `</ul>` -> JSX/HTML 结构行，用于描述页面元素。
- 146: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 147: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 148: `<h3 className="font-bold text-slate-900 mb-3 flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 149: `<FileText className="h-5 w-5 text-violet-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 150: `资产证明` -> 执行当前语句，参与该文件整体逻辑。
- 151: `</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 152: `<ul className="space-y-2 text-slate-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 153: `{['6个月银行流水', '收入证明', '房产证复印件（可选）', '车辆行驶证复印件（可选）', '存款证明（可选）'].map((item) => (` -> 执行当前语句，参与该文件整体逻辑。
- 154: `<li key={item} className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 155: `<CheckCircle className="h-4 w-4 text-green-500 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 156: `{item}` -> 执行当前语句，参与该文件整体逻辑。
- 157: `</li>` -> JSX/HTML 结构行，用于描述页面元素。
- 158: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 159: `</ul>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 161: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 162: `<p className="text-sm text-slate-500 mt-4">* 具体材料要求因国家和签证类型而异，请以顾问提供的清单为准</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 163: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 164: `(空行)` -> 空行，用于提升代码结构可读性。
- 165: `(空行)` -> 空行，用于提升代码结构可读性。
- 166: `<div className="bg-gradient-to-r from-violet-600 to-purple-600 rounded-3xl p-8 text-white text-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `<h2 className="text-2xl font-bold mb-4">开始办理您的签证</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 168: `<p className="text-white/80 mb-6">专业顾问在线为您解答，选择最适合您的签证方案</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 169: `<div className="flex flex-wrap justify-center gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 170: `<Link href="/contact" className="inline-flex items-center gap-2 px-6 py-3 bg-white text-violet-600 rounded-xl font-bold hover:bg-violet-50 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 171: `<MessageCircle className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `在线咨询` -> 执行当前语句，参与该文件整体逻辑。
- 173: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `<a href="tel:400-888-9999" className="inline-flex items-center gap-2 px-6 py-3 bg-white/20 text-white rounded-xl font-bold hover:bg-white/30 transition">` -> JSX/HTML 结构行，用于描述页面元素。
- 175: `<Phone className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `400-888-9999` -> 执行当前语句，参与该文件整体逻辑。
- 177: `</a>` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 179: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 184: `)` -> 结束当前语句或代码块。
- 185: `}` -> 结束当前语句或代码块。
