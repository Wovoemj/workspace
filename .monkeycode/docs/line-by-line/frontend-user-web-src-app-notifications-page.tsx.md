# `frontend/user-web/src/app/notifications/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 通知中心模块 - 用户消息通知管理` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 未读通知计数展示` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 通知列表展示（标题、内容、创建时间）` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 单条标记已读功能` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 全部标记已读功能` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 已读/未读状态区分样式（未读：靛蓝色背景；已读：灰色背景）` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 分页加载（每页 20 条）` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 登录状态校验` -> 注释行，用于解释设计意图或使用说明。
- 14: `*` -> 注释行，用于解释设计意图或使用说明。
- 15: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - AppNotification 类型定义` -> 注释行，用于解释设计意图或使用说明。
- 19: `*` -> 注释行，用于解释设计意图或使用说明。
- 20: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - GET /api/notifications?user_id=xxx&page=1&per_page=20: 获取通知列表` -> 注释行，用于解释设计意图或使用说明。
- 22: `* - PUT /api/notifications/${id}/read: 标记单条通知已读` -> 注释行，用于解释设计意图或使用说明。
- 23: `* - PUT /api/notifications/read-all: 标记全部已读` -> 注释行，用于解释设计意图或使用说明。
- 24: `*` -> 注释行，用于解释设计意图或使用说明。
- 25: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 26: `* - useState: notifications(通知列表), loading, error, unreadCount(计算属性)` -> 注释行，用于解释设计意图或使用说明。
- 27: `* - useMemo: unreadCount 计算（过滤未读通知数量）` -> 注释行，用于解释设计意图或使用说明。
- 28: `* - 依赖: isAuthenticated, user?.id（登录状态变化时重新加载）` -> 注释行，用于解释设计意图或使用说明。
- 29: `*/` -> 注释行，用于解释设计意图或使用说明。
- 30: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `import { useEffect, useMemo, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 33: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 34: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 35: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 36: `import { AppNotification } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `const PAGE_SIZE = 20` -> 声明变量或常量，保存运行时数据。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `export default function NotificationsPage() {` -> 导出当前声明，供其他模块复用。
- 41: `const { user, isAuthenticated } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 42: `const [notifications, setNotifications] = useState<AppNotification[]>([])` -> 声明变量或常量，保存运行时数据。
- 43: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 44: `const [error, setError] = useState('')` -> 声明变量或常量，保存运行时数据。
- 45: `(空行)` -> 空行，用于提升代码结构可读性。
- 46: `const backendUrl = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5001'` -> 声明变量或常量，保存运行时数据。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `const unreadCount = useMemo(` -> 声明变量或常量，保存运行时数据。
- 49: `() => notifications.filter((n) => !n.is_read).length,` -> 执行当前语句，参与该文件整体逻辑。
- 50: `[notifications]` -> 执行当前语句，参与该文件整体逻辑。
- 51: `)` -> 结束当前语句或代码块。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `const fetchNotifications = async () => {` -> 声明变量或常量，保存运行时数据。
- 54: `if (!isAuthenticated || !user?.id) {` -> 条件判断分支，根据场景执行不同逻辑。
- 55: `setNotifications([])` -> 执行当前语句，参与该文件整体逻辑。
- 56: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 57: `return` -> 返回结果或提前结束当前流程。
- 58: `}` -> 结束当前语句或代码块。
- 59: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 60: `setError('')` -> 执行当前语句，参与该文件整体逻辑。
- 61: `try {` -> 异常处理逻辑，提升程序健壮性。
- 62: `const res = await fetch(` -> 声明变量或常量，保存运行时数据。
- 63: `\`${backendUrl}/api/notifications?user_id=${encodeURIComponent(user.id)}&page=1&per_page=${PAGE_SIZE}\`,` -> 执行当前语句，参与该文件整体逻辑。
- 64: `{ cache: 'no-store' }` -> 执行当前语句，参与该文件整体逻辑。
- 65: `)` -> 结束当前语句或代码块。
- 66: `if (!res.ok) throw new Error('获取通知失败')` -> 条件判断分支，根据场景执行不同逻辑。
- 67: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 68: `if (!data?.success) throw new Error(data?.error || '获取通知失败')` -> 条件判断分支，根据场景执行不同逻辑。
- 69: `setNotifications(data.notifications || [])` -> 执行当前语句，参与该文件整体逻辑。
- 70: `} catch (e: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 71: `setError(e?.message || '获取通知失败')` -> 执行当前语句，参与该文件整体逻辑。
- 72: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 73: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 74: `}` -> 结束当前语句或代码块。
- 75: `}` -> 结束当前语句或代码块。
- 76: `(空行)` -> 空行，用于提升代码结构可读性。
- 77: `const markRead = async (id: number) => {` -> 声明变量或常量，保存运行时数据。
- 78: `try {` -> 异常处理逻辑，提升程序健壮性。
- 79: `const res = await fetch(\`${backendUrl}/api/notifications/${id}/read\`, { method: 'PUT' })` -> 声明变量或常量，保存运行时数据。
- 80: `if (!res.ok) throw new Error('操作失败')` -> 条件判断分支，根据场景执行不同逻辑。
- 81: `setNotifications((prev) =>` -> 执行当前语句，参与该文件整体逻辑。
- 82: `prev.map((n) => (n.id === id ? { ...n, is_read: true, read_at: new Date().toISOString() } : n))` -> 执行当前语句，参与该文件整体逻辑。
- 83: `)` -> 结束当前语句或代码块。
- 84: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 85: `// 静默失败，避免打断阅?` -> 注释行，用于解释设计意图或使用说明。
- 86: `}` -> 结束当前语句或代码块。
- 87: `}` -> 结束当前语句或代码块。
- 88: `(空行)` -> 空行，用于提升代码结构可读性。
- 89: `const markAllRead = async () => {` -> 声明变量或常量，保存运行时数据。
- 90: `if (!user?.id) return` -> 条件判断分支，根据场景执行不同逻辑。
- 91: `try {` -> 异常处理逻辑，提升程序健壮性。
- 92: `const res = await fetch(\`${backendUrl}/api/notifications/read-all\`, {` -> 声明变量或常量，保存运行时数据。
- 93: `method: 'PUT',` -> 执行当前语句，参与该文件整体逻辑。
- 94: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 95: `body: JSON.stringify({ user_id: user.id }),` -> 执行当前语句，参与该文件整体逻辑。
- 96: `})` -> 执行当前语句，参与该文件整体逻辑。
- 97: `if (!res.ok) throw new Error('操作失败')` -> 条件判断分支，根据场景执行不同逻辑。
- 98: `setNotifications((prev) => prev.map((n) => ({ ...n, is_read: true })))` -> 执行当前语句，参与该文件整体逻辑。
- 99: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 100: `// 静默失败` -> 注释行，用于解释设计意图或使用说明。
- 101: `}` -> 结束当前语句或代码块。
- 102: `}` -> 结束当前语句或代码块。
- 103: `(空行)` -> 空行，用于提升代码结构可读性。
- 104: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 105: `fetchNotifications()` -> 执行当前语句，参与该文件整体逻辑。
- 106: `// eslint-disable-next-line react-hooks/exhaustive-deps` -> 注释行，用于解释设计意图或使用说明。
- 107: `}, [isAuthenticated, user?.id])` -> 执行当前语句，参与该文件整体逻辑。
- 108: `(空行)` -> 空行，用于提升代码结构可读性。
- 109: `return (` -> 返回结果或提前结束当前流程。
- 110: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 111: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 112: `<main className="pt-20 pb-10">` -> JSX/HTML 结构行，用于描述页面元素。
- 113: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 114: `<div className="bg-white rounded-2xl shadow p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 115: `<div className="flex items-center justify-between mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 116: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 117: `<h1 className="text-2xl font-bold text-gray-900">通知中心</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 118: `<p className="text-sm text-gray-500 mt-1">未读 ({unreadCount})</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 120: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 121: `onClick={markAllRead}` -> 执行当前语句，参与该文件整体逻辑。
- 122: `className="px-4 py-2 rounded-lg bg-indigo-600 text-white hover:bg-indigo-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 123: `disabled={!notifications.length}` -> 执行当前语句，参与该文件整体逻辑。
- 124: `>` -> 执行当前语句，参与该文件整体逻辑。
- 125: `全部标记已读` -> 执行当前语句，参与该文件整体逻辑。
- 126: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 127: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 128: `(空行)` -> 空行，用于提升代码结构可读性。
- 129: `{!isAuthenticated && (` -> 执行当前语句，参与该文件整体逻辑。
- 130: `<div className="text-gray-600">请先登录后查看通知</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 131: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 132: `(空行)` -> 空行，用于提升代码结构可读性。
- 133: `{isAuthenticated && loading && (` -> 执行当前语句，参与该文件整体逻辑。
- 134: `<div className="text-gray-500">加载?..</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 135: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `{isAuthenticated && !loading && error && (` -> 执行当前语句，参与该文件整体逻辑。
- 138: `<div className="text-red-500">{error}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 139: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 140: `(空行)` -> 空行，用于提升代码结构可读性。
- 141: `{isAuthenticated && !loading && !error && notifications.length === 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 142: `<div className="text-gray-500">暂无通知</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 143: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 144: `(空行)` -> 空行，用于提升代码结构可读性。
- 145: `{isAuthenticated && !loading && !error && notifications.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 146: `<div className="space-y-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 147: `{notifications.map((n) => (` -> 执行当前语句，参与该文件整体逻辑。
- 148: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 149: `key={n.id}` -> 执行当前语句，参与该文件整体逻辑。
- 150: `className={\`border rounded-xl p-4 ${n.is_read ? 'bg-gray-50 border-gray-200' : 'bg-indigo-50 border-indigo-200'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 151: `>` -> 执行当前语句，参与该文件整体逻辑。
- 152: `<div className="flex items-start justify-between gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 153: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 154: `<div className="font-semibold text-gray-900">{n.title}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 155: `<div className="text-sm text-gray-600 mt-1">{n.content}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 156: `<div className="text-xs text-gray-400 mt-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 157: `{new Date(n.created_at).toLocaleString()}` -> 执行当前语句，参与该文件整体逻辑。
- 158: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 159: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `{!n.is_read && (` -> 执行当前语句，参与该文件整体逻辑。
- 161: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 162: `onClick={() => markRead(n.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 163: `className="text-sm px-3 py-1 rounded-md border border-indigo-300 text-indigo-700 hover:bg-indigo-100"` -> 执行当前语句，参与该文件整体逻辑。
- 164: `>` -> 执行当前语句，参与该文件整体逻辑。
- 165: `标记已读` -> 执行当前语句，参与该文件整体逻辑。
- 166: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 168: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 169: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 170: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 171: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 173: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 175: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `)` -> 结束当前语句或代码块。
- 179: `}` -> 结束当前语句或代码块。
