# `start.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `#!/usr/bin/env python3` -> 注释行，用于解释设计意图或使用说明。
- 2: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 3: `旅游助手 - 一键启动脚本` -> 执行当前语句，参与该文件整体逻辑。
- 4: `用法: python start.py` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `import os` -> 导入依赖模块，供当前文件使用。
- 8: `import sys` -> 导入依赖模块，供当前文件使用。
- 9: `import subprocess` -> 导入依赖模块，供当前文件使用。
- 10: `import time` -> 导入依赖模块，供当前文件使用。
- 11: `import signal` -> 导入依赖模块，供当前文件使用。
- 12: `import platform` -> 导入依赖模块，供当前文件使用。
- 13: `(空行)` -> 空行，用于提升代码结构可读性。
- 14: `# ============== 配置 ==============` -> 注释行，用于解释设计意图或使用说明。
- 15: `BACKEND_PORT = 5001` -> 执行当前语句，参与该文件整体逻辑。
- 16: `FRONTEND_PORT = 3000` -> 执行当前语句，参与该文件整体逻辑。
- 17: `AI_SERVICE_PORT = 8084` -> 执行当前语句，参与该文件整体逻辑。
- 18: `REDIS_PORT = 6379` -> 执行当前语句，参与该文件整体逻辑。
- 19: `(空行)` -> 空行，用于提升代码结构可读性。
- 20: `# AI 配置` -> 注释行，用于解释设计意图或使用说明。
- 21: `AI_PROVIDER = "mimo"  # "mimo" 或 "kimi"` -> 执行当前语句，参与该文件整体逻辑。
- 22: `MIMO_API_KEY = "tp-cs6449t23jpm0mwnimjsf4n1dcpny2zg4x6ihm1svgqn08zt"` -> 执行当前语句，参与该文件整体逻辑。
- 23: `KIMI_API_KEY = "sk-JcUSqOZ2FUdxICD3Plubl72fUJhY4j8cuWRpnvkjMlavNOI5"` -> 执行当前语句，参与该文件整体逻辑。
- 24: `MIMO_MODEL = "MiMo-V2.5"  # MiMo 模型: MiMo-V2.5, MiMo-V2, MiMo-V1` -> 执行当前语句，参与该文件整体逻辑。
- 25: `KIMI_MODEL = "kimi-k2.5"  # Kimi 模型: kimi-k2.5, kimi-k2.6, kimi-k2` -> 执行当前语句，参与该文件整体逻辑。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `PROJECT_ROOT = os.path.dirname(os.path.abspath(__file__))` -> 执行当前语句，参与该文件整体逻辑。
- 28: `(空行)` -> 空行，用于提升代码结构可读性。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `def print_banner():` -> 定义 Python 函数，封装可复用逻辑。
- 31: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 32: `print("   智旅助手 - 一键启动 ")` -> 执行当前语句，参与该文件整体逻辑。
- 33: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 34: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 35: `(空行)` -> 空行，用于提升代码结构可读性。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `def run_cmd(cmd, cwd=None, shell=True, env=None):` -> 定义 Python 函数，封装可复用逻辑。
- 38: `"""运行命令并返回进程"""` -> 执行当前语句，参与该文件整体逻辑。
- 39: `# 合并环境变量` -> 注释行，用于解释设计意图或使用说明。
- 40: `process_env = os.environ.copy()` -> 执行当前语句，参与该文件整体逻辑。
- 41: `if env:` -> 条件判断分支，根据场景执行不同逻辑。
- 42: `process_env.update(env)` -> 执行当前语句，参与该文件整体逻辑。
- 43: `(空行)` -> 空行，用于提升代码结构可读性。
- 44: `return subprocess.Popen(` -> 返回结果或提前结束当前流程。
- 45: `cmd,` -> 执行当前语句，参与该文件整体逻辑。
- 46: `cwd=cwd or PROJECT_ROOT,` -> 执行当前语句，参与该文件整体逻辑。
- 47: `shell=shell,` -> 执行当前语句，参与该文件整体逻辑。
- 48: `stdout=subprocess.PIPE,` -> 执行当前语句，参与该文件整体逻辑。
- 49: `stderr=subprocess.STDOUT,` -> 执行当前语句，参与该文件整体逻辑。
- 50: `text=True,` -> 执行当前语句，参与该文件整体逻辑。
- 51: `encoding='gbk',` -> 执行当前语句，参与该文件整体逻辑。
- 52: `errors='replace',` -> 执行当前语句，参与该文件整体逻辑。
- 53: `env=process_env` -> 执行当前语句，参与该文件整体逻辑。
- 54: `)` -> 结束当前语句或代码块。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `def is_docker_running():` -> 定义 Python 函数，封装可复用逻辑。
- 58: `"""检查 Docker 是否运行"""` -> 执行当前语句，参与该文件整体逻辑。
- 59: `try:` -> 异常处理逻辑，提升程序健壮性。
- 60: `result = subprocess.run(` -> 执行当前语句，参与该文件整体逻辑。
- 61: `'docker info',` -> 执行当前语句，参与该文件整体逻辑。
- 62: `shell=True,` -> 执行当前语句，参与该文件整体逻辑。
- 63: `capture_output=True,` -> 执行当前语句，参与该文件整体逻辑。
- 64: `text=True` -> 执行当前语句，参与该文件整体逻辑。
- 65: `)` -> 结束当前语句或代码块。
- 66: `return result.returncode == 0` -> 返回结果或提前结束当前流程。
- 67: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 68: `return False` -> 返回结果或提前结束当前流程。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `(空行)` -> 空行，用于提升代码结构可读性。
- 71: `def is_container_running(name):` -> 定义 Python 函数，封装可复用逻辑。
- 72: `"""检查容器是否运行"""` -> 执行当前语句，参与该文件整体逻辑。
- 73: `try:` -> 异常处理逻辑，提升程序健壮性。
- 74: `result = subprocess.run(` -> 执行当前语句，参与该文件整体逻辑。
- 75: `f'docker ps --filter "name={name}" --filter "status=running" --format "{{{{.Names}}}}"',` -> 执行当前语句，参与该文件整体逻辑。
- 76: `shell=True,` -> 执行当前语句，参与该文件整体逻辑。
- 77: `capture_output=True,` -> 执行当前语句，参与该文件整体逻辑。
- 78: `text=True` -> 执行当前语句，参与该文件整体逻辑。
- 79: `)` -> 结束当前语句或代码块。
- 80: `return name in result.stdout` -> 返回结果或提前结束当前流程。
- 81: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 82: `return False` -> 返回结果或提前结束当前流程。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `(空行)` -> 空行，用于提升代码结构可读性。
- 85: `def start_redis_container():` -> 定义 Python 函数，封装可复用逻辑。
- 86: `"""启动 Redis 容器"""` -> 执行当前语句，参与该文件整体逻辑。
- 87: `if is_container_running('travel-assistant-redis'):` -> 条件判断分支，根据场景执行不同逻辑。
- 88: `print("  [Redis] 已在运行")` -> 执行当前语句，参与该文件整体逻辑。
- 89: `return True` -> 返回结果或提前结束当前流程。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `print("  [Redis] 正在启动...")` -> 执行当前语句，参与该文件整体逻辑。
- 92: `try:` -> 异常处理逻辑，提升程序健壮性。
- 93: `result = subprocess.run(` -> 执行当前语句，参与该文件整体逻辑。
- 94: `'docker run -d --name travel-assistant-redis -p 6379:6379 redis:7-alpine',` -> 执行当前语句，参与该文件整体逻辑。
- 95: `shell=True,` -> 执行当前语句，参与该文件整体逻辑。
- 96: `capture_output=True,` -> 执行当前语句，参与该文件整体逻辑。
- 97: `text=True` -> 执行当前语句，参与该文件整体逻辑。
- 98: `)` -> 结束当前语句或代码块。
- 99: `if result.returncode == 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 100: `time.sleep(2)` -> 执行当前语句，参与该文件整体逻辑。
- 101: `print("  [Redis] 启动成功")` -> 执行当前语句，参与该文件整体逻辑。
- 102: `return True` -> 返回结果或提前结束当前流程。
- 103: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 104: `print(f"  [Redis] 启动失败: {result.stderr}")` -> 执行当前语句，参与该文件整体逻辑。
- 105: `return False` -> 返回结果或提前结束当前流程。
- 106: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 107: `print(f"  [Redis] 启动失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 108: `return False` -> 返回结果或提前结束当前流程。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `(空行)` -> 空行，用于提升代码结构可读性。
- 111: `def kill_port(port):` -> 定义 Python 函数，封装可复用逻辑。
- 112: `"""清理占用指定端口的进程"""` -> 执行当前语句，参与该文件整体逻辑。
- 113: `system = platform.system()` -> 执行当前语句，参与该文件整体逻辑。
- 114: `try:` -> 异常处理逻辑，提升程序健壮性。
- 115: `if system == "Windows":` -> 条件判断分支，根据场景执行不同逻辑。
- 116: `result = subprocess.run(` -> 执行当前语句，参与该文件整体逻辑。
- 117: `f'netstat -ano | findstr :{port} | findstr LISTENING',` -> 执行当前语句，参与该文件整体逻辑。
- 118: `shell=True, capture_output=True, text=True` -> 执行当前语句，参与该文件整体逻辑。
- 119: `)` -> 结束当前语句或代码块。
- 120: `for line in result.stdout.strip().split('\n'):` -> 循环处理集合或重复执行逻辑。
- 121: `if line.strip():` -> 条件判断分支，根据场景执行不同逻辑。
- 122: `parts = line.strip().split()` -> 执行当前语句，参与该文件整体逻辑。
- 123: `if len(parts) >= 5:` -> 条件判断分支，根据场景执行不同逻辑。
- 124: `pid = parts[-1]` -> 执行当前语句，参与该文件整体逻辑。
- 125: `print(f"  清理端口 {port} (PID: {pid})...")` -> 执行当前语句，参与该文件整体逻辑。
- 126: `subprocess.run(f'taskkill /F /PID {pid} >nul 2>&1', shell=True)` -> 执行当前语句，参与该文件整体逻辑。
- 127: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 128: `result = subprocess.run(` -> 执行当前语句，参与该文件整体逻辑。
- 129: `f"lsof -ti:{port}", shell=True, capture_output=True, text=True` -> 执行当前语句，参与该文件整体逻辑。
- 130: `)` -> 结束当前语句或代码块。
- 131: `if result.stdout.strip():` -> 条件判断分支，根据场景执行不同逻辑。
- 132: `pid = result.stdout.strip()` -> 执行当前语句，参与该文件整体逻辑。
- 133: `print(f"  清理端口 {port} (PID: {pid})...")` -> 执行当前语句，参与该文件整体逻辑。
- 134: `subprocess.run(f"kill {pid}", shell=True)` -> 执行当前语句，参与该文件整体逻辑。
- 135: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 136: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 137: `(空行)` -> 空行，用于提升代码结构可读性。
- 138: `(空行)` -> 空行，用于提升代码结构可读性。
- 139: `def check_health(url, max_retry=10):` -> 定义 Python 函数，封装可复用逻辑。
- 140: `"""检查服务健康状态"""` -> 执行当前语句，参与该文件整体逻辑。
- 141: `import urllib.request` -> 导入依赖模块，供当前文件使用。
- 142: `for i in range(max_retry):` -> 循环处理集合或重复执行逻辑。
- 143: `try:` -> 异常处理逻辑，提升程序健壮性。
- 144: `urllib.request.urlopen(url, timeout=2)` -> 执行当前语句，参与该文件整体逻辑。
- 145: `return True` -> 返回结果或提前结束当前流程。
- 146: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 147: `time.sleep(1)` -> 执行当前语句，参与该文件整体逻辑。
- 148: `return False` -> 返回结果或提前结束当前流程。
- 149: `(空行)` -> 空行，用于提升代码结构可读性。
- 150: `(空行)` -> 空行，用于提升代码结构可读性。
- 151: `def stream_output(proc, prefix):` -> 定义 Python 函数，封装可复用逻辑。
- 152: `"""实时输出进程日志"""` -> 执行当前语句，参与该文件整体逻辑。
- 153: `for line in proc.stdout:` -> 循环处理集合或重复执行逻辑。
- 154: `print(f"[{prefix}] {line.rstrip()}")` -> 执行当前语句，参与该文件整体逻辑。
- 155: `(空行)` -> 空行，用于提升代码结构可读性。
- 156: `(空行)` -> 空行，用于提升代码结构可读性。
- 157: `def main():` -> 定义 Python 函数，封装可复用逻辑。
- 158: `print_banner()` -> 执行当前语句，参与该文件整体逻辑。
- 159: `(空行)` -> 空行，用于提升代码结构可读性。
- 160: `# 检查 Docker（Redis 变成可选的）` -> 注释行，用于解释设计意图或使用说明。
- 161: `redis_enabled = is_docker_running()` -> 执行当前语句，参与该文件整体逻辑。
- 162: `if not redis_enabled:` -> 条件判断分支，根据场景执行不同逻辑。
- 163: `print("[提示] Docker 未运行，Redis 将被跳过（不影响核心功能）")` -> 执行当前语句，参与该文件整体逻辑。
- 164: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 165: `print("[提示] Docker 已检测到，Redis 将被启用")` -> 执行当前语句，参与该文件整体逻辑。
- 166: `(空行)` -> 空行，用于提升代码结构可读性。
- 167: `processes = []` -> 执行当前语句，参与该文件整体逻辑。
- 168: `(空行)` -> 空行，用于提升代码结构可读性。
- 169: `def cleanup(signum=None, frame=None):` -> 定义 Python 函数，封装可复用逻辑。
- 170: `"""清理所有进程"""` -> 执行当前语句，参与该文件整体逻辑。
- 171: `print("\n")` -> 执行当前语句，参与该文件整体逻辑。
- 172: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 173: `print("   正在停止服务...")` -> 执行当前语句，参与该文件整体逻辑。
- 174: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 175: `for proc in processes:` -> 循环处理集合或重复执行逻辑。
- 176: `try:` -> 异常处理逻辑，提升程序健壮性。
- 177: `proc.terminate()` -> 执行当前语句，参与该文件整体逻辑。
- 178: `proc.wait(timeout=3)` -> 执行当前语句，参与该文件整体逻辑。
- 179: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 180: `try:` -> 异常处理逻辑，提升程序健壮性。
- 181: `proc.kill()` -> 执行当前语句，参与该文件整体逻辑。
- 182: `except Exception:` -> 异常处理逻辑，提升程序健壮性。
- 183: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 184: `# Windows 下强制清理端口` -> 注释行，用于解释设计意图或使用说明。
- 185: `if platform.system() == "Windows":` -> 条件判断分支，根据场景执行不同逻辑。
- 186: `kill_port(BACKEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 187: `kill_port(FRONTEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 188: `kill_port(AI_SERVICE_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 189: `print("  服务已停止")` -> 执行当前语句，参与该文件整体逻辑。
- 190: `os._exit(0)` -> 执行当前语句，参与该文件整体逻辑。
- 191: `(空行)` -> 空行，用于提升代码结构可读性。
- 192: `signal.signal(signal.SIGINT, cleanup)` -> 执行当前语句，参与该文件整体逻辑。
- 193: `if hasattr(signal, 'SIGTERM'):` -> 条件判断分支，根据场景执行不同逻辑。
- 194: `signal.signal(signal.SIGTERM, cleanup)` -> 执行当前语句，参与该文件整体逻辑。
- 195: `(空行)` -> 空行，用于提升代码结构可读性。
- 196: `try:` -> 异常处理逻辑，提升程序健壮性。
- 197: `# 0. 清理旧进程` -> 注释行，用于解释设计意图或使用说明。
- 198: `print("[0/4] 清理旧进程...")` -> 执行当前语句，参与该文件整体逻辑。
- 199: `kill_port(BACKEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 200: `kill_port(FRONTEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 201: `kill_port(AI_SERVICE_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 202: `# 只杀 node 残留，不杀 python（会杀掉自己）` -> 注释行，用于解释设计意图或使用说明。
- 203: `if platform.system() == "Windows":` -> 条件判断分支，根据场景执行不同逻辑。
- 204: `try:` -> 异常处理逻辑，提升程序健壮性。
- 205: `subprocess.run('taskkill /F /IM node.exe >nul 2>&1', shell=True)` -> 执行当前语句，参与该文件整体逻辑。
- 206: `except:` -> 异常处理逻辑，提升程序健壮性。
- 207: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 208: `time.sleep(1)` -> 执行当前语句，参与该文件整体逻辑。
- 209: `(空行)` -> 空行，用于提升代码结构可读性。
- 210: `# 1. 启动 Redis（可选）` -> 注释行，用于解释设计意图或使用说明。
- 211: `print("[1/4] 检查 Redis...")` -> 执行当前语句，参与该文件整体逻辑。
- 212: `if redis_enabled:` -> 条件判断分支，根据场景执行不同逻辑。
- 213: `start_redis_container()` -> 执行当前语句，参与该文件整体逻辑。
- 214: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 215: `print("  [跳过] Redis 不可用，但不影响使用")` -> 执行当前语句，参与该文件整体逻辑。
- 216: `time.sleep(1)` -> 执行当前语句，参与该文件整体逻辑。
- 217: `(空行)` -> 空行，用于提升代码结构可读性。
- 218: `# 2. 清理端口` -> 注释行，用于解释设计意图或使用说明。
- 219: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 220: `print("[2/4] 检查端口占用...")` -> 执行当前语句，参与该文件整体逻辑。
- 221: `kill_port(BACKEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 222: `kill_port(FRONTEND_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 223: `kill_port(AI_SERVICE_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 224: `time.sleep(1)` -> 执行当前语句，参与该文件整体逻辑。
- 225: `(空行)` -> 空行，用于提升代码结构可读性。
- 226: `# 3. 启动 AI 服务` -> 注释行，用于解释设计意图或使用说明。
- 227: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 228: `provider_name = "MiMo" if AI_PROVIDER == "mimo" else "Kimi"` -> 执行当前语句，参与该文件整体逻辑。
- 229: `print(f"[3/4] 启动 AI 服务 ({provider_name}, 端口 {AI_SERVICE_PORT})...")` -> 执行当前语句，参与该文件整体逻辑。
- 230: `ai_dir = os.path.join(PROJECT_ROOT, "backend", "ai-service")` -> 执行当前语句，参与该文件整体逻辑。
- 231: `ai_api_key = MIMO_API_KEY if AI_PROVIDER == "mimo" else KIMI_API_KEY` -> 执行当前语句，参与该文件整体逻辑。
- 232: `ai_model = MIMO_MODEL if AI_PROVIDER == "mimo" else KIMI_MODEL` -> 执行当前语句，参与该文件整体逻辑。
- 233: `ai_env = {` -> 执行当前语句，参与该文件整体逻辑。
- 234: `"AI_PROVIDER": AI_PROVIDER,` -> 执行当前语句，参与该文件整体逻辑。
- 235: `"AI_API_KEY": ai_api_key,` -> 执行当前语句，参与该文件整体逻辑。
- 236: `"AI_MODEL": ai_model,` -> 执行当前语句，参与该文件整体逻辑。
- 237: `"REDIS_ADDR": f"localhost:{REDIS_PORT}" if redis_enabled else "",` -> 执行当前语句，参与该文件整体逻辑。
- 238: `"PORT": str(AI_SERVICE_PORT)` -> 执行当前语句，参与该文件整体逻辑。
- 239: `}` -> 结束当前语句或代码块。
- 240: `ai_proc = run_cmd(` -> 执行当前语句，参与该文件整体逻辑。
- 241: `"go run main.go moonshot_service.go",` -> 执行当前语句，参与该文件整体逻辑。
- 242: `cwd=ai_dir,` -> 执行当前语句，参与该文件整体逻辑。
- 243: `env=ai_env` -> 执行当前语句，参与该文件整体逻辑。
- 244: `)` -> 结束当前语句或代码块。
- 245: `processes.append(ai_proc)` -> 执行当前语句，参与该文件整体逻辑。
- 246: `(空行)` -> 空行，用于提升代码结构可读性。
- 247: `# 等待 AI 服务启动` -> 注释行，用于解释设计意图或使用说明。
- 248: `print("  等待 AI 服务启动...")` -> 执行当前语句，参与该文件整体逻辑。
- 249: `time.sleep(5)` -> 执行当前语句，参与该文件整体逻辑。
- 250: `if check_health(f"http://localhost:{AI_SERVICE_PORT}/health", max_retry=5):` -> 条件判断分支，根据场景执行不同逻辑。
- 251: `print(f"  AI 服务启动成功 ({provider_name})")` -> 执行当前语句，参与该文件整体逻辑。
- 252: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 253: `print("  AI 服务启动中...")` -> 执行当前语句，参与该文件整体逻辑。
- 254: `(空行)` -> 空行，用于提升代码结构可读性。
- 255: `# 4. 启动后端 + 前端` -> 注释行，用于解释设计意图或使用说明。
- 256: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 257: `print(f"[4/4] 启动后端服务 (端口 {BACKEND_PORT})...")` -> 执行当前语句，参与该文件整体逻辑。
- 258: `backend_proc = run_cmd("python app.py")` -> 执行当前语句，参与该文件整体逻辑。
- 259: `processes.append(backend_proc)` -> 执行当前语句，参与该文件整体逻辑。
- 260: `(空行)` -> 空行，用于提升代码结构可读性。
- 261: `# 等待后端启动` -> 注释行，用于解释设计意图或使用说明。
- 262: `print("  等待后端启动...")` -> 执行当前语句，参与该文件整体逻辑。
- 263: `if check_health(f"http://localhost:{BACKEND_PORT}/api/health", max_retry=15):` -> 条件判断分支，根据场景执行不同逻辑。
- 264: `print("  后端启动成功")` -> 执行当前语句，参与该文件整体逻辑。
- 265: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 266: `print("  后端启动中，请稍候...")` -> 执行当前语句，参与该文件整体逻辑。
- 267: `(空行)` -> 空行，用于提升代码结构可读性。
- 268: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 269: `print(f"启动前端服务 (端口 {FRONTEND_PORT})...")` -> 执行当前语句，参与该文件整体逻辑。
- 270: `frontend_dir = os.path.join(PROJECT_ROOT, "frontend", "user-web")` -> 执行当前语句，参与该文件整体逻辑。
- 271: `frontend_proc = run_cmd("npm run dev", cwd=frontend_dir)` -> 执行当前语句，参与该文件整体逻辑。
- 272: `processes.append(frontend_proc)` -> 执行当前语句，参与该文件整体逻辑。
- 273: `(空行)` -> 空行，用于提升代码结构可读性。
- 274: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 275: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 276: `print("   启动完成!")` -> 执行当前语句，参与该文件整体逻辑。
- 277: `print("=" * 50)` -> 执行当前语句，参与该文件整体逻辑。
- 278: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 279: `print("  服务地址:")` -> 执行当前语句，参与该文件整体逻辑。
- 280: `print(f"  - 前端:   http://localhost:{FRONTEND_PORT}")` -> 执行当前语句，参与该文件整体逻辑。
- 281: `print(f"  - 后端:   http://localhost:{BACKEND_PORT}")` -> 执行当前语句，参与该文件整体逻辑。
- 282: `print(f"  - AI API: http://localhost:{AI_SERVICE_PORT}")` -> 执行当前语句，参与该文件整体逻辑。
- 283: `if redis_enabled:` -> 条件判断分支，根据场景执行不同逻辑。
- 284: `print(f"  - Redis:  localhost:{REDIS_PORT}")` -> 执行当前语句，参与该文件整体逻辑。
- 285: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 286: `print(f"  AI 提供商: {provider_name}")` -> 执行当前语句，参与该文件整体逻辑。
- 287: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 288: `print("  按 Ctrl+C 停止服务")` -> 执行当前语句，参与该文件整体逻辑。
- 289: `print()` -> 执行当前语句，参与该文件整体逻辑。
- 290: `(空行)` -> 空行，用于提升代码结构可读性。
- 291: `# 自动打开浏览器` -> 注释行，用于解释设计意图或使用说明。
- 292: `import webbrowser` -> 导入依赖模块，供当前文件使用。
- 293: `webbrowser.open(f"http://localhost:{FRONTEND_PORT}")` -> 执行当前语句，参与该文件整体逻辑。
- 294: `(空行)` -> 空行，用于提升代码结构可读性。
- 295: `# 同时输出所有服务日志` -> 注释行，用于解释设计意图或使用说明。
- 296: `import threading` -> 导入依赖模块，供当前文件使用。
- 297: `threads = []` -> 执行当前语句，参与该文件整体逻辑。
- 298: `for proc, name in [(ai_proc, "AI"), (backend_proc, "Backend"), (frontend_proc, "Frontend")]:` -> 循环处理集合或重复执行逻辑。
- 299: `t = threading.Thread(target=stream_output, args=(proc, name), daemon=True)` -> 执行当前语句，参与该文件整体逻辑。
- 300: `t.start()` -> 执行当前语句，参与该文件整体逻辑。
- 301: `threads.append(t)` -> 执行当前语句，参与该文件整体逻辑。
- 302: `(空行)` -> 空行，用于提升代码结构可读性。
- 303: `# 等待进程结束` -> 注释行，用于解释设计意图或使用说明。
- 304: `while True:` -> 循环处理集合或重复执行逻辑。
- 305: `all_dead = all(p.poll() is not None for p in processes)` -> 执行当前语句，参与该文件整体逻辑。
- 306: `if all_dead:` -> 条件判断分支，根据场景执行不同逻辑。
- 307: `break` -> 执行当前语句，参与该文件整体逻辑。
- 308: `# 检查是否某个进程退出了` -> 注释行，用于解释设计意图或使用说明。
- 309: `for i, (proc, name) in enumerate(zip(processes, ["AI", "Backend", "Frontend"])):` -> 循环处理集合或重复执行逻辑。
- 310: `if proc.poll() is not None and proc.poll() != 0:` -> 条件判断分支，根据场景执行不同逻辑。
- 311: `print(f"[{name}] 进程已退出，代码: {proc.poll()}")` -> 执行当前语句，参与该文件整体逻辑。
- 312: `time.sleep(0.5)` -> 执行当前语句，参与该文件整体逻辑。
- 313: `(空行)` -> 空行，用于提升代码结构可读性。
- 314: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 315: `print(f"\n  启动失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 316: `cleanup()` -> 执行当前语句，参与该文件整体逻辑。
- 317: `(空行)` -> 空行，用于提升代码结构可读性。
- 318: `(空行)` -> 空行，用于提升代码结构可读性。
- 319: `if __name__ == "__main__":` -> 条件判断分支，根据场景执行不同逻辑。
- 320: `main()` -> 执行当前语句，参与该文件整体逻辑。
