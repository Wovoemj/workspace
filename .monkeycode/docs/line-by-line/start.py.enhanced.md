# `start.py` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `#!/usr/bin/env python3`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 3: `旅游助手 - 一键启动脚本`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `用法: python start.py`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 7: `import os`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 8: `import sys`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 9: `import subprocess`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 10: `import time`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 11: `import signal`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 12: `import platform`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 13: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 14: `# ============== 配置 ==============`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `BACKEND_PORT = 5001`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 16: `FRONTEND_PORT = 3000`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 17: `AI_SERVICE_PORT = 8084`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `REDIS_PORT = 6379`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 20: `# AI 配置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 21: `AI_PROVIDER = "mimo"  # "mimo" 或 "kimi"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 22: `MIMO_API_KEY = "tp-cs6449t23jpm0mwnimjsf4n1dcpny2zg4x6ihm1svgqn08zt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `KIMI_API_KEY = "sk-JcUSqOZ2FUdxICD3Plubl72fUJhY4j8cuWRpnvkjMlavNOI5"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `MIMO_MODEL = "MiMo-V2.5"  # MiMo 模型: MiMo-V2.5, MiMo-V2, MiMo-V1`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `KIMI_MODEL = "kimi-k2.5"  # Kimi 模型: kimi-k2.5, kimi-k2.6, kimi-k2`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 27: `PROJECT_ROOT = os.path.dirname(os.path.abspath(__file__))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 29: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 30: `def print_banner():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 31: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `print("   智旅助手 - 一键启动 ")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 36: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 37: `def run_cmd(cmd, cwd=None, shell=True, env=None):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 38: `"""运行命令并返回进程"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `# 合并环境变量`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 40: `process_env = os.environ.copy()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `if env:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 42: `process_env.update(env)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 44: `return subprocess.Popen(`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 45: `cmd,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `cwd=cwd or PROJECT_ROOT,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 47: `shell=shell,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `stdout=subprocess.PIPE,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 49: `stderr=subprocess.STDOUT,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `text=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `encoding='gbk',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `errors='replace',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `env=process_env`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 55: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 56: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 57: `def is_docker_running():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 58: `"""检查 Docker 是否运行"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 59: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 60: `result = subprocess.run(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `'docker info',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `shell=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `capture_output=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `text=True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 66: `return result.returncode == 0`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 67: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 68: `return False`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 69: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 70: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 71: `def is_container_running(name):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 72: `"""检查容器是否运行"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 74: `result = subprocess.run(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `f'docker ps --filter "name={name}" --filter "status=running" --format "{{{{.Names}}}}"',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `shell=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 77: `capture_output=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 78: `text=True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 80: `return name in result.stdout`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 81: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 82: `return False`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 83: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 84: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 85: `def start_redis_container():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 86: `"""启动 Redis 容器"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `if is_container_running('travel-assistant-redis'):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 88: `print("  [Redis] 已在运行")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `return True`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 90: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 91: `print("  [Redis] 正在启动...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 92: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 93: `result = subprocess.run(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `'docker run -d --name travel-assistant-redis -p 6379:6379 redis:7-alpine',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 95: `shell=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `capture_output=True,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `text=True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 99: `if result.returncode == 0:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 100: `time.sleep(2)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `print("  [Redis] 启动成功")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `return True`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 103: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 104: `print(f"  [Redis] 启动失败: {result.stderr}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `return False`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 106: `except Exception as e:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 107: `print(f"  [Redis] 启动失败: {e}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 108: `return False`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 109: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 110: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 111: `def kill_port(port):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 112: `"""清理占用指定端口的进程"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `system = platform.system()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 114: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 115: `if system == "Windows":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 116: `result = subprocess.run(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `f'netstat -ano | findstr :{port} | findstr LISTENING',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `shell=True, capture_output=True, text=True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 120: `for line in result.stdout.strip().split('\n'):`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 121: `if line.strip():`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 122: `parts = line.strip().split()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `if len(parts) >= 5:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 124: `pid = parts[-1]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 125: `print(f"  清理端口 {port} (PID: {pid})...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `subprocess.run(f'taskkill /F /PID {pid} >nul 2>&1', shell=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 127: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 128: `result = subprocess.run(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `f"lsof -ti:{port}", shell=True, capture_output=True, text=True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 131: `if result.stdout.strip():`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 132: `pid = result.stdout.strip()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 133: `print(f"  清理端口 {port} (PID: {pid})...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 134: `subprocess.run(f"kill {pid}", shell=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 135: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 136: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 137: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 138: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 139: `def check_health(url, max_retry=10):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 140: `"""检查服务健康状态"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 141: `import urllib.request`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 142: `for i in range(max_retry):`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 143: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 144: `urllib.request.urlopen(url, timeout=2)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `return True`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 146: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 147: `time.sleep(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `return False`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 149: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 150: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 151: `def stream_output(proc, prefix):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 152: `"""实时输出进程日志"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `for line in proc.stdout:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 154: `print(f"[{prefix}] {line.rstrip()}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 155: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 156: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 157: `def main():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 158: `print_banner()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 159: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 160: `# 检查 Docker（Redis 变成可选的）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 161: `redis_enabled = is_docker_running()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 162: `if not redis_enabled:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 163: `print("[提示] Docker 未运行，Redis 将被跳过（不影响核心功能）")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 165: `print("[提示] Docker 已检测到，Redis 将被启用")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 167: `processes = []`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 169: `def cleanup(signum=None, frame=None):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 170: `"""清理所有进程"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `print("\n")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 172: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `print("   正在停止服务...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 174: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 175: `for proc in processes:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 176: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 177: `proc.terminate()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 178: `proc.wait(timeout=3)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 179: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 180: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 181: `proc.kill()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 182: `except Exception:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 183: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 184: `# Windows 下强制清理端口`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 185: `if platform.system() == "Windows":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 186: `kill_port(BACKEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 187: `kill_port(FRONTEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 188: `kill_port(AI_SERVICE_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `print("  服务已停止")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 190: `os._exit(0)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 191: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 192: `signal.signal(signal.SIGINT, cleanup)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 193: `if hasattr(signal, 'SIGTERM'):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 194: `signal.signal(signal.SIGTERM, cleanup)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 196: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 197: `# 0. 清理旧进程`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 198: `print("[0/4] 清理旧进程...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 199: `kill_port(BACKEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 200: `kill_port(FRONTEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `kill_port(AI_SERVICE_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `# 只杀 node 残留，不杀 python（会杀掉自己）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 203: `if platform.system() == "Windows":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 204: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 205: `subprocess.run('taskkill /F /IM node.exe >nul 2>&1', shell=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 206: `except:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 207: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 208: `time.sleep(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 209: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 210: `# 1. 启动 Redis（可选）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 211: `print("[1/4] 检查 Redis...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `if redis_enabled:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 213: `start_redis_container()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 215: `print("  [跳过] Redis 不可用，但不影响使用")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `time.sleep(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 217: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 218: `# 2. 清理端口`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 219: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 220: `print("[2/4] 检查端口占用...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `kill_port(BACKEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `kill_port(FRONTEND_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 223: `kill_port(AI_SERVICE_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 224: `time.sleep(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 225: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 226: `# 3. 启动 AI 服务`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 227: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `provider_name = "MiMo" if AI_PROVIDER == "mimo" else "Kimi"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 229: `print(f"[3/4] 启动 AI 服务 ({provider_name}, 端口 {AI_SERVICE_PORT})...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 230: `ai_dir = os.path.join(PROJECT_ROOT, "backend", "ai-service")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 231: `ai_api_key = MIMO_API_KEY if AI_PROVIDER == "mimo" else KIMI_API_KEY`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 232: `ai_model = MIMO_MODEL if AI_PROVIDER == "mimo" else KIMI_MODEL`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 233: `ai_env = {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `"AI_PROVIDER": AI_PROVIDER,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 235: `"AI_API_KEY": ai_api_key,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 236: `"AI_MODEL": ai_model,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 237: `"REDIS_ADDR": f"localhost:{REDIS_PORT}" if redis_enabled else "",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `"PORT": str(AI_SERVICE_PORT)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 239: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 240: `ai_proc = run_cmd(`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 241: `"go run main.go moonshot_service.go",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 242: `cwd=ai_dir,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 243: `env=ai_env`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 244: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 245: `processes.append(ai_proc)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 247: `# 等待 AI 服务启动`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 248: `print("  等待 AI 服务启动...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 249: `time.sleep(5)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 250: `if check_health(f"http://localhost:{AI_SERVICE_PORT}/health", max_retry=5):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 251: `print(f"  AI 服务启动成功 ({provider_name})")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 252: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 253: `print("  AI 服务启动中...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 254: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 255: `# 4. 启动后端 + 前端`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 256: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 257: `print(f"[4/4] 启动后端服务 (端口 {BACKEND_PORT})...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 258: `backend_proc = run_cmd("python app.py")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 259: `processes.append(backend_proc)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 260: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 261: `# 等待后端启动`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 262: `print("  等待后端启动...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 263: `if check_health(f"http://localhost:{BACKEND_PORT}/api/health", max_retry=15):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 264: `print("  后端启动成功")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 265: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 266: `print("  后端启动中，请稍候...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 267: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 268: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 269: `print(f"启动前端服务 (端口 {FRONTEND_PORT})...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 270: `frontend_dir = os.path.join(PROJECT_ROOT, "frontend", "user-web")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 271: `frontend_proc = run_cmd("npm run dev", cwd=frontend_dir)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 272: `processes.append(frontend_proc)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 273: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 274: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 275: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 276: `print("   启动完成!")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 277: `print("=" * 50)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 278: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 279: `print("  服务地址:")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 280: `print(f"  - 前端:   http://localhost:{FRONTEND_PORT}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 281: `print(f"  - 后端:   http://localhost:{BACKEND_PORT}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 282: `print(f"  - AI API: http://localhost:{AI_SERVICE_PORT}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 283: `if redis_enabled:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 284: `print(f"  - Redis:  localhost:{REDIS_PORT}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 285: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 286: `print(f"  AI 提供商: {provider_name}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 287: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 288: `print("  按 Ctrl+C 停止服务")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 289: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 290: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 291: `# 自动打开浏览器`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 292: `import webbrowser`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 293: `webbrowser.open(f"http://localhost:{FRONTEND_PORT}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 294: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 295: `# 同时输出所有服务日志`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 296: `import threading`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 297: `threads = []`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 298: `for proc, name in [(ai_proc, "AI"), (backend_proc, "Backend"), (frontend_proc, "Frontend")]:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 299: `t = threading.Thread(target=stream_output, args=(proc, name), daemon=True)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 300: `t.start()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 301: `threads.append(t)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 302: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 303: `# 等待进程结束`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 304: `while True:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 305: `all_dead = all(p.poll() is not None for p in processes)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 306: `if all_dead:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 307: `break`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 308: `# 检查是否某个进程退出了`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 309: `for i, (proc, name) in enumerate(zip(processes, ["AI", "Backend", "Frontend"])):`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 310: `if proc.poll() is not None and proc.poll() != 0:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 311: `print(f"[{name}] 进程已退出，代码: {proc.poll()}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 312: `time.sleep(0.5)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 313: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 314: `except Exception as e:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 315: `print(f"\n  启动失败: {e}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 316: `cleanup()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 317: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 318: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 319: `if __name__ == "__main__":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 320: `main()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
