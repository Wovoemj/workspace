# `frontend/user-web/src/components/DestinationCard.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `/**` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 目的地卡片组件 (DestinationCard)` -> 注释行，用于解释设计意图或使用说明。
- 6: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 7: `*` -> 注释行，用于解释设计意图或使用说明。
- 8: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 显示目的地卡片，包含图片、名称、城市、评分等信息` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 支持响应式图片加载（srcset）和懒加载` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 自动生成目的地标签（如"5A景区"、"热门"等）` -> 注释行，用于解释设计意图或使用说明。
- 12: `*` -> 注释行，用于解释设计意图或使用说明。
- 13: `* 使用场景：` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - 首页目的地推荐列表` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 目的地搜索结果列表` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 收藏目的地展示` -> 注释行，用于解释设计意图或使用说明。
- 17: `*/` -> 注释行，用于解释设计意图或使用说明。
- 18: `(空行)` -> 空行，用于提升代码结构可读性。
- 19: `import { useState, useMemo } from 'react'` -> 导入依赖模块，供当前文件使用。
- 20: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 21: `import { MapPin, Star } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 22: `import { formatRating, shouldShowRating } from '@/lib/display'` -> 导入依赖模块，供当前文件使用。
- 23: `import { resolveCoverSrc } from '@/lib/media'` -> 导入依赖模块，供当前文件使用。
- 24: `import { deriveDestinationTags } from '@/lib/destinationTags'` -> 导入依赖模块，供当前文件使用。
- 25: `(空行)` -> 空行，用于提升代码结构可读性。
- 26: `/**` -> 注释行，用于解释设计意图或使用说明。
- 27: `* 目的地卡片数据项类型` -> 注释行，用于解释设计意图或使用说明。
- 28: `*/` -> 注释行，用于解释设计意图或使用说明。
- 29: `export type DestinationCardItem = {` -> 导出当前声明，供其他模块复用。
- 30: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 31: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 32: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 33: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 34: `description?: string` -> 执行当前语句，参与该文件整体逻辑。
- 35: `cover_image?: string` -> 执行当前语句，参与该文件整体逻辑。
- 36: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 37: `}` -> 结束当前语句或代码块。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `interface DestinationCardProps {` -> 定义类型或类结构，约束数据与行为。
- 40: `destination: DestinationCardItem` -> 执行当前语句，参与该文件整体逻辑。
- 41: `className?: string` -> 执行当前语句，参与该文件整体逻辑。
- 42: `/** 移动端压缩图片高度（tailwind class */` -> 注释行，用于解释设计意图或使用说明。
- 43: `imageHeightClass?: string` -> 执行当前语句，参与该文件整体逻辑。
- 44: `/** 是否首屏优先加载 */` -> 注释行，用于解释设计意图或使用说明。
- 45: `priority?: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 46: `}` -> 结束当前语句或代码块。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `/**` -> 注释行，用于解释设计意图或使用说明。
- 49: `* 生成响应式图片 srcset` -> 注释行，用于解释设计意图或使用说明。
- 50: `* @description 根据基础图片URL生成多尺寸的 srcset，支持响应式图片加载` -> 注释行，用于解释设计意图或使用说明。
- 51: `* @param baseSrc - 基础图片URL` -> 注释行，用于解释设计意图或使用说明。
- 52: `* @returns srcset 字符串，用于 <img> 标签` -> 注释行，用于解释设计意图或使用说明。
- 53: `*/` -> 注释行，用于解释设计意图或使用说明。
- 54: `function generateSrcSet(baseSrc: string): string {` -> 定义函数或方法，实现具体业务逻辑。
- 55: `if (!baseSrc || baseSrc.startsWith('data:') || baseSrc.includes('placeholder')) {` -> 条件判断分支，根据场景执行不同逻辑。
- 56: `return ''` -> 返回结果或提前结束当前流程。
- 57: `}` -> 结束当前语句或代码块。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `// 支持的尺寸` -> 注释行，用于解释设计意图或使用说明。
- 60: `const widths = [320, 480, 640, 800, 1200]` -> 声明变量或常量，保存运行时数据。
- 61: `(空行)` -> 空行，用于提升代码结构可读性。
- 62: `return widths` -> 返回结果或提前结束当前流程。
- 63: `.map(w => {` -> 执行当前语句，参与该文件整体逻辑。
- 64: `// 如果图片路径包含查询参数，尝试修改它` -> 注释行，用于解释设计意图或使用说明。
- 65: `if (baseSrc.includes('?')) {` -> 条件判断分支，根据场景执行不同逻辑。
- 66: `return \`${baseSrc}&w=${w} ${w}w\`` -> 返回结果或提前结束当前流程。
- 67: `}` -> 结束当前语句或代码块。
- 68: `// 否则添加参数` -> 注释行，用于解释设计意图或使用说明。
- 69: `return \`${baseSrc}?w=${w} ${w}w\`` -> 返回结果或提前结束当前流程。
- 70: `})` -> 执行当前语句，参与该文件整体逻辑。
- 71: `.join(', ')` -> 执行当前语句，参与该文件整体逻辑。
- 72: `}` -> 结束当前语句或代码块。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `/**` -> 注释行，用于解释设计意图或使用说明。
- 75: `* 目的地卡片主组件` -> 注释行，用于解释设计意图或使用说明。
- 76: `* @description 展示目的地信息的卡片组件，包含图片、名称、城市、评分和标签` -> 注释行，用于解释设计意图或使用说明。
- 77: `* @param props - 组件属性` -> 注释行，用于解释设计意图或使用说明。
- 78: `* @param props.destination - 目的地数据` -> 注释行，用于解释设计意图或使用说明。
- 79: `* @param props.className - 自定义样式类` -> 注释行，用于解释设计意图或使用说明。
- 80: `* @param props.imageHeightClass - 移动端图片高度` -> 注释行，用于解释设计意图或使用说明。
- 81: `* @param props.priority - 是否优先加载（用于首屏）` -> 注释行，用于解释设计意图或使用说明。
- 82: `*/` -> 注释行，用于解释设计意图或使用说明。
- 83: `export function DestinationCard({` -> 导出当前声明，供其他模块复用。
- 84: `destination: d,` -> 执行当前语句，参与该文件整体逻辑。
- 85: `className = '',` -> 执行当前语句，参与该文件整体逻辑。
- 86: `imageHeightClass,` -> 执行当前语句，参与该文件整体逻辑。
- 87: `priority = false` -> 执行当前语句，参与该文件整体逻辑。
- 88: `}: DestinationCardProps) {` -> 执行当前语句，参与该文件整体逻辑。
- 89: `const [imgLoaded, setImgLoaded] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 90: `const [hasError, setHasError] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 91: `const src = resolveCoverSrc(d.cover_image)` -> 声明变量或常量，保存运行时数据。
- 92: `const tags = deriveDestinationTags(d.name, d.description)` -> 声明变量或常量，保存运行时数据。
- 93: `(空行)` -> 空行，用于提升代码结构可读性。
- 94: `const imgWrap = imageHeightClass` -> 声明变量或常量，保存运行时数据。
- 95: `? \`relative w-full ${imageHeightClass} sm:h-auto sm:aspect-[4/3]\`` -> 执行当前语句，参与该文件整体逻辑。
- 96: `: 'relative w-full aspect-[4/3]'` -> 执行当前语句，参与该文件整体逻辑。
- 97: `(空行)` -> 空行，用于提升代码结构可读性。
- 98: `// 生成响应式图片配置` -> 注释行，用于解释设计意图或使用说明。
- 99: `const { optimizedSrc, srcSet } = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 100: `return {` -> 返回结果或提前结束当前流程。
- 101: `optimizedSrc: hasError ? '/images/placeholder.jpg' : src,` -> 执行当前语句，参与该文件整体逻辑。
- 102: `srcSet: generateSrcSet(src)` -> 执行当前语句，参与该文件整体逻辑。
- 103: `}` -> 结束当前语句或代码块。
- 104: `}, [src, hasError])` -> 执行当前语句，参与该文件整体逻辑。
- 105: `(空行)` -> 空行，用于提升代码结构可读性。
- 106: `const handleError = () => {` -> 声明变量或常量，保存运行时数据。
- 107: `if (!hasError) {` -> 条件判断分支，根据场景执行不同逻辑。
- 108: `setHasError(true)` -> 执行当前语句，参与该文件整体逻辑。
- 109: `}` -> 结束当前语句或代码块。
- 110: `}` -> 结束当前语句或代码块。
- 111: `(空行)` -> 空行，用于提升代码结构可读性。
- 112: `return (` -> 返回结果或提前结束当前流程。
- 113: `<Link href={\`/destinations/${d.id}\`} className={\`group block h-full ${className}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 114: `<div className="h-full rounded-2xl border border-border/60 bg-card shadow-sm overflow-hidden transition-all hover:shadow-md hover:-translate-y-0.5">` -> JSX/HTML 结构行，用于描述页面元素。
- 115: `<div className={imgWrap}>` -> JSX/HTML 结构行，用于描述页面元素。
- 116: `{/* 骨架屏占位 */}` -> 执行当前语句，参与该文件整体逻辑。
- 117: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 118: `className={\`absolute inset-0 bg-gradient-to-br from-muted to-muted/60 dark:from-slate-800 dark:to-slate-900 animate-pulse transition-opacity duration-300 ${` -> 执行当前语句，参与该文件整体逻辑。
- 119: `imgLoaded ? 'opacity-0 pointer-events-none' : 'opacity-100'` -> 执行当前语句，参与该文件整体逻辑。
- 120: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 121: `aria-hidden` -> 执行当前语句，参与该文件整体逻辑。
- 122: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 123: `(空行)` -> 空行，用于提升代码结构可读性。
- 124: `{/* 优化后的图片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 125: `<img` -> 执行当前语句，参与该文件整体逻辑。
- 126: `src={optimizedSrc}` -> 执行当前语句，参与该文件整体逻辑。
- 127: `srcSet={srcSet || undefined}` -> 执行当前语句，参与该文件整体逻辑。
- 128: `sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"` -> 执行当前语句，参与该文件整体逻辑。
- 129: `alt={\`${d.name}风景\`}` -> 执行当前语句，参与该文件整体逻辑。
- 130: `className={\`absolute inset-0 h-full w-full object-cover transition-all duration-300 group-hover:scale-105 ${imgLoaded ? 'opacity-100' : 'opacity-0'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 131: `loading={priority ? 'eager' : 'lazy'}` -> 执行当前语句，参与该文件整体逻辑。
- 132: `decoding={priority ? 'sync' : 'async'}` -> 执行当前语句，参与该文件整体逻辑。
- 133: `onLoad={() => setImgLoaded(true)}` -> 执行当前语句，参与该文件整体逻辑。
- 134: `onError={handleError}` -> 执行当前语句，参与该文件整体逻辑。
- 135: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `{/* 渐变遮罩 */}` -> 执行当前语句，参与该文件整体逻辑。
- 138: `<div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent" />` -> JSX/HTML 结构行，用于描述页面元素。
- 139: `(空行)` -> 空行，用于提升代码结构可读性。
- 140: `{/* 信息覆盖层 */}` -> 执行当前语句，参与该文件整体逻辑。
- 141: `<div className="absolute bottom-2.5 left-3 right-3 sm:bottom-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 142: `<div className="flex items-end justify-between gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 143: `<div className="min-w-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 144: `<div className="text-white font-bold text-base leading-snug line-clamp-2">{d.name}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 145: `<div className="mt-0.5 flex items-center gap-1 text-white/85 text-xs">` -> JSX/HTML 结构行，用于描述页面元素。
- 146: `<MapPin className="h-3.5 w-3.5 shrink-0" />` -> JSX/HTML 结构行，用于描述页面元素。
- 147: `<span className="truncate">` -> JSX/HTML 结构行，用于描述页面元素。
- 148: `{d.city} · {d.province}` -> 执行当前语句，参与该文件整体逻辑。
- 149: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 150: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 151: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 152: `{shouldShowRating(d.rating) ? (` -> 执行当前语句，参与该文件整体逻辑。
- 153: `<div className="inline-flex items-center gap-1 bg-white/12 border border-white/20 px-2 py-1 rounded-full text-white shrink-0">` -> JSX/HTML 结构行，用于描述页面元素。
- 154: `<Star className="h-3.5 w-3.5 fill-amber-400 text-amber-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 155: `<span className="text-[11px] font-semibold">{formatRating(d.rating)}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 156: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 157: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 158: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 159: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 160: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 163: `{tags.length > 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 164: `<div className="flex flex-wrap gap-1.5 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 165: `{tags.map((t) => (` -> 执行当前语句，参与该文件整体逻辑。
- 166: `<span` -> 执行当前语句，参与该文件整体逻辑。
- 167: `key={t}` -> 执行当前语句，参与该文件整体逻辑。
- 168: `className="px-2 py-0.5 rounded-full bg-primary/10 text-primary text-[11px] font-medium border border-primary/15"` -> 执行当前语句，参与该文件整体逻辑。
- 169: `>` -> 执行当前语句，参与该文件整体逻辑。
- 170: `{t}` -> 执行当前语句，参与该文件整体逻辑。
- 171: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 172: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 173: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 174: `) : null}` -> 执行当前语句，参与该文件整体逻辑。
- 175: `<p className="text-sm text-muted-foreground line-clamp-2 min-h-[2.5rem]">` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `{d.description?.replace(/\[citation:\d+\]/g, '').trim() || '发现这座城市的独特风景与旅行灵感'}` -> 执行当前语句，参与该文件整体逻辑。
- 177: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `<div className="mt-3 flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 179: `<span className="text-xs font-semibold text-primary group-hover:underline">探索这座城市</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 181: `href={\`/destinations/map?city=${encodeURIComponent(d.city)}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 182: `onClick={(e) => e.stopPropagation()}` -> 执行当前语句，参与该文件整体逻辑。
- 183: `className="text-xs font-semibold text-sky-600 hover:text-sky-700"` -> 执行当前语句，参与该文件整体逻辑。
- 184: `>` -> 执行当前语句，参与该文件整体逻辑。
- 185: `地图查看` -> 执行当前语句，参与该文件整体逻辑。
- 186: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 187: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 188: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 189: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 190: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 191: `)` -> 结束当前语句或代码块。
- 192: `}` -> 结束当前语句或代码块。
