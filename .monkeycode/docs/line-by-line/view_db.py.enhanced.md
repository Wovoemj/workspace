# `view_db.py` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `#!/usr/bin/env python3`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 3: `SQLite 数据库查看工具`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `用法: python view_db.py [表名]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 7: `import sqlite3`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 8: `import sys`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 9: `import os`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 10: `from tabulate import tabulate`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 11: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 12: `DB_PATH = os.path.join(os.path.dirname(__file__), "instance", "travel.db")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 14: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 15: `def list_tables(conn):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 16: `"""列出所有表"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 17: `cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table';")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 18: `tables = cursor.fetchall()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 19: `print("\n数据库中的表:")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 20: `print("-" * 40)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `for i, (table,) in enumerate(tables, 1):`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 22: `# 获取行数（表名加引号避免SQL保留字冲突）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `count = conn.execute(f'SELECT COUNT(*) FROM "{table}"').fetchone()[0]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `print(f"{i}. {table} ({count} 条记录)")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `return [t[0] for t in tables]`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 27: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 28: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 29: `def show_table_schema(conn, table_name):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 30: `"""显示表结构"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `cursor = conn.execute(f'PRAGMA table_info("{table_name}");')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `columns = cursor.fetchall()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 34: `print(f"\n表结构: {table_name}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `print("-" * 60)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 37: `schema_data = []`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `for col in columns:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 39: `cid, name, dtype, notnull, default, pk = col`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 40: `schema_data.append([`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `cid,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `dtype,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `"NOT NULL" if notnull else "",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `"PRIMARY KEY" if pk else "",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `default if default else ""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 47: `])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 48: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 49: `print(tabulate(schema_data,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `headers=["#", "列名", "类型", "约束", "主键", "默认值"],`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `tablefmt="grid"))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 53: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 54: `def show_table_data(conn, table_name, limit=20):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 55: `"""显示表数据"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 57: `# 获取列名`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 58: `cursor = conn.execute(f'PRAGMA table_info("{table_name}");')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 59: `columns = [col[1] for col in cursor.fetchall()]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 61: `# 获取数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 62: `cursor = conn.execute(f'SELECT * FROM "{table_name}" LIMIT {limit};')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `rows = cursor.fetchall()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 65: `if not rows:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 66: `print(f"\n表 {table_name} 为空")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 69: `print(f"\n表数据: {table_name} (前 {len(rows)} 条)")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `print("-" * 60)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 72: `# 格式化数据（截断长文本）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 73: `formatted_rows = []`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `for row in rows:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 75: `formatted_row = []`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `for cell in row:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 77: `if cell is None:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 78: `formatted_row.append("NULL")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 79: `elif isinstance(cell, str) and len(cell) > 50:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 80: `formatted_row.append(cell[:50] + "...")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 82: `formatted_row.append(cell)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `formatted_rows.append(formatted_row)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 85: `print(tabulate(formatted_rows, headers=columns, tablefmt="grid"))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 87: `# 显示总行数`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 88: `count = conn.execute(f'SELECT COUNT(*) FROM "{table_name}"').fetchone()[0]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `if count > limit:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 90: `print(f"\n... 还有 {count - limit} 条记录")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 92: `except Exception as e:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 93: `print(f"查询失败: {e}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 95: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 96: `def search_data(conn, keyword):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 97: `"""搜索数据"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `tables = list_tables(conn)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `print(f"\n搜索关键字: '{keyword}'")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `print("-" * 60)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 102: `found = False`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `for table in tables:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 104: `cursor = conn.execute(f'PRAGMA table_info("{table}");')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `columns = [col[1] for col in cursor.fetchall()]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 106: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 107: `# 构建搜索条件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 108: `conditions = [f"{col} LIKE ?" for col in columns]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `sql = f'SELECT * FROM "{table}" WHERE {" OR ".join(conditions)} LIMIT 5'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 110: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 111: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 112: `cursor = conn.execute(sql, [f"%{keyword}%"] * len(columns))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `rows = cursor.fetchall()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 114: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 115: `if rows:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 116: `found = True`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `print(f"\n在表 '{table}' 中找到 {len(rows)} 条记录:")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `print(tabulate(rows, headers=columns, tablefmt="simple"))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `except:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 120: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 122: `if not found:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 123: `print("未找到匹配的记录")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 125: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 126: `def export_to_sql(conn, output_file="export.sql"):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 127: `"""导出为 SQL 文件"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 128: `with open(output_file, 'w', encoding='utf-8') as f:`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `for line in conn.iterdump():`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 130: `f.write(f"{line}\n")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `print(f"\n已导出到: {output_file}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `def delete_record(conn, table_name, record_id):`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 135: `"""删除记录，自动处理外键约束"""`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 137: `# 获取所有外键引用`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 138: `cursor = conn.execute(f'PRAGMA foreign_key_list("{table_name}");')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 139: `fks = cursor.fetchall()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 140: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 141: `deleted_count = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 142: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 143: `# 查询所有其他表，查找外键引用`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 144: `cursor = conn.execute("SELECT name FROM sqlite_master WHERE type='table';")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 145: `all_tables = [t[0] for t in cursor.fetchall()]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 146: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 147: `for other_table in all_tables:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 148: `if other_table == table_name:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 149: `continue`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 151: `# 获取其他表的列`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 152: `cursor = conn.execute(f'PRAGMA table_info("{other_table}");')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `columns = [col[1] for col in cursor.fetchall()]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 155: `# 查找可能的外键列`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 156: `for col in columns:`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 157: `if col.endswith('_id') or col == 'user_id':`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 158: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 159: `cursor.execute(f'SELECT COUNT(*) FROM "{other_table}" WHERE "{col}" = ?', (record_id,))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 160: `count = cursor.fetchone()[0]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 161: `if count > 0:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 162: `cursor.execute(f'DELETE FROM "{other_table}" WHERE "{col}" = ?', (record_id,))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 163: `deleted_count += cursor.rowcount`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `print(f'  ✓ 已删除 {other_table}.{col} 中 {count} 条')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 165: `except:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 166: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 167: `# 也检查 id 字段`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 168: `try:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 169: `cursor.execute(f'SELECT COUNT(*) FROM "{other_table}" WHERE "id" = ?', (record_id,))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `count = cursor.fetchone()[0]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `if count > 0:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 172: `cursor.execute(f'DELETE FROM "{other_table}" WHERE "id" = ?', (record_id,))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `deleted_count += cursor.rowcount`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 174: `print(f'  ✓ 已删除 {other_table}.id 中 {count} 条')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 175: `except:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 176: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `except:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 178: `pass`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 179: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 180: `# 删除主记录`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 181: `cursor.execute(f'DELETE FROM "{table_name}" WHERE id = ?', (record_id,))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 182: `deleted_count += cursor.rowcount`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 183: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 184: `conn.commit()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `print(f'✓ 已删除 {table_name} id={record_id}，共 {deleted_count} 条')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 186: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 187: `except Exception as e:`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 188: `conn.rollback()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 189: `print(f'✗ 删除失败: {e}')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 190: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 191: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 192: `def main():`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 193: `if not os.path.exists(DB_PATH):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 194: `print(f"数据库文件不存在: {DB_PATH}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 195: `sys.exit(1)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 196: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 197: `conn = sqlite3.connect(DB_PATH)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 199: `print("=" * 60)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 200: `print("  SQLite 数据库查看工具")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 201: `print(f"  数据库: {DB_PATH}")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 202: `print("=" * 60)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 203: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 204: `# 如果没有参数，显示帮助`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 205: `if len(sys.argv) == 1:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 206: `print("\n用法:")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 207: `print("  python view_db.py              - 列出所有表")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 208: `print("  python view_db.py 表名         - 查看表结构和数据")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 209: `print("  python view_db.py -s 表名      - 只查看表结构")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 210: `print("  python view_db.py -a 表名      - 查看所有数据")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 211: `print("  python view_db.py -f 关键字    - 搜索数据")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 212: `print("  python view_db.py -e           - 导出为 SQL 文件")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 213: `print("  python view_db.py -d 表名 id   - 删除指定记录(自动处理外键)")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 214: `print()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 215: `list_tables(conn)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 216: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 217: `# 查看指定表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 218: `elif len(sys.argv) == 2 and not sys.argv[1].startswith('-'):`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 219: `table_name = sys.argv[1]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 220: `show_table_schema(conn, table_name)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 221: `show_table_data(conn, table_name)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 222: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 223: `# 只查看结构`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 224: `elif sys.argv[1] == '-s' and len(sys.argv) >= 3:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 225: `show_table_schema(conn, sys.argv[2])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 226: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 227: `# 查看所有数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 228: `elif sys.argv[1] == '-a' and len(sys.argv) >= 3:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 229: `show_table_data(conn, sys.argv[2], limit=10000)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 230: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 231: `# 搜索`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 232: `elif sys.argv[1] == '-f' and len(sys.argv) >= 3:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 233: `search_data(conn, sys.argv[2])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 234: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 235: `# 导出`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 236: `elif sys.argv[1] == '-e':`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 237: `export_to_sql(conn)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 238: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 239: `# 删除记录`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 240: `elif sys.argv[1] == '-d' and len(sys.argv) >= 4:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 241: `delete_record(conn, sys.argv[2], int(sys.argv[3]))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 242: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 243: `else:`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 244: `print("未知命令，使用: python view_db.py")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 245: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 246: `conn.close()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 247: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 248: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 249: `if __name__ == "__main__":`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 250: `main()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
