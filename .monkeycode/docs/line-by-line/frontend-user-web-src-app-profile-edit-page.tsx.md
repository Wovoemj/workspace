# `frontend/user-web/src/app/profile/edit/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 个人信息编辑模块 - 用户资料修改` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 用户头像上传预览` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 头像上传功能` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 基本信息编辑（昵称）` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 旅行偏好设置：` -> 注释行，用于解释设计意图或使用说明。
- 11: `*   - 旅行风格（冒险/休闲/人文/商务）` -> 注释行，用于解释设计意图或使用说明。
- 12: `*   - 同行人数` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - 感兴趣的目的地（逗号分隔）` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - 预算范围（最小/最大）` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 表单验证与提交` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 登录状态校验（未登录重定向到登录页）` -> 注释行，用于解释设计意图或使用说明。
- 17: `*` -> 注释行，用于解释设计意图或使用说明。
- 18: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 19: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 20: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 21: `*` -> 注释行，用于解释设计意图或使用说明。
- 22: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 23: `* - GET /api/users/profile: 获取当前用户资料` -> 注释行，用于解释设计意图或使用说明。
- 24: `* - PUT /api/users/profile: 更新用户资料（需登录）` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - POST /api/upload/avatar: 上传头像图片` -> 注释行，用于解释设计意图或使用说明。
- 26: `*` -> 注释行，用于解释设计意图或使用说明。
- 27: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 28: `* - useState: form(表单数据), avatarPreview, uploadingAvatar, fileInputRef` -> 注释行，用于解释设计意图或使用说明。
- 29: `* - useEffect: 初始化表单数据，同步用户信息` -> 注释行，用于解释设计意图或使用说明。
- 30: `*/` -> 注释行，用于解释设计意图或使用说明。
- 31: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `import type { FormEvent } from 'react'` -> 导入依赖模块，供当前文件使用。
- 34: `import { useEffect, useMemo, useRef, useState } from 'react'` -> 导入依赖模块，供当前文件使用。
- 35: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 36: `import { useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 37: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 38: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 39: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 40: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 41: `import { ArrowLeft, Camera, Loader2 } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 42: `(空行)` -> 空行，用于提升代码结构可读性。
- 43: `export default function ProfileEditPage() {` -> 导出当前声明，供其他模块复用。
- 44: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 45: `const { user, isAuthenticated, login } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `const [form, setForm] = useState({` -> 声明变量或常量，保存运行时数据。
- 48: `nickname: '',` -> 执行当前语句，参与该文件整体逻辑。
- 49: `travel_style: 'relaxation' as 'adventure' | 'relaxation' | 'cultural' | 'business',` -> 执行当前语句，参与该文件整体逻辑。
- 50: `group_size: 1,` -> 执行当前语句，参与该文件整体逻辑。
- 51: `interests: '',` -> 执行当前语句，参与该文件整体逻辑。
- 52: `destinations: '',` -> 执行当前语句，参与该文件整体逻辑。
- 53: `budget_min: 0,` -> 执行当前语句，参与该文件整体逻辑。
- 54: `budget_max: 0,` -> 执行当前语句，参与该文件整体逻辑。
- 55: `})` -> 执行当前语句，参与该文件整体逻辑。
- 56: `(空行)` -> 空行，用于提升代码结构可读性。
- 57: `const [avatarPreview, setAvatarPreview] = useState<string | null>(null)` -> 声明变量或常量，保存运行时数据。
- 58: `const [uploadingAvatar, setUploadingAvatar] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 59: `const fileInputRef = useRef<HTMLInputElement>(null)` -> 声明变量或常量，保存运行时数据。
- 60: `(空行)` -> 空行，用于提升代码结构可读性。
- 61: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 62: `if (!user) return` -> 条件判断分支，根据场景执行不同逻辑。
- 63: `setForm({` -> 执行当前语句，参与该文件整体逻辑。
- 64: `nickname: user.nickname || '',` -> 执行当前语句，参与该文件整体逻辑。
- 65: `travel_style: user.preferences?.travel_style || 'relaxation',` -> 执行当前语句，参与该文件整体逻辑。
- 66: `group_size: user.preferences?.group_size || 1,` -> 执行当前语句，参与该文件整体逻辑。
- 67: `interests: user.preferences?.interests?.join(', ') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 68: `destinations: user.preferences?.destinations?.join(', ') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 69: `budget_min: user.preferences?.budget_range?.min ?? 0,` -> 执行当前语句，参与该文件整体逻辑。
- 70: `budget_max: user.preferences?.budget_range?.max ?? 0,` -> 执行当前语句，参与该文件整体逻辑。
- 71: `})` -> 执行当前语句，参与该文件整体逻辑。
- 72: `setAvatarPreview(user.avatar_url || null)` -> 执行当前语句，参与该文件整体逻辑。
- 73: `}, [user])` -> 执行当前语句，参与该文件整体逻辑。
- 74: `(空行)` -> 空行，用于提升代码结构可读性。
- 75: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 76: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 77: `router.replace('/login?returnUrl=' + encodeURIComponent('/profile/edit'))` -> 执行当前语句，参与该文件整体逻辑。
- 78: `}` -> 结束当前语句或代码块。
- 79: `}, [isAuthenticated, router])` -> 执行当前语句，参与该文件整体逻辑。
- 80: `(空行)` -> 空行，用于提升代码结构可读性。
- 81: `const canSave = useMemo(() => isAuthenticated && !!user, [isAuthenticated, user])` -> 声明变量或常量，保存运行时数据。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `const handleAvatarClick = () => {` -> 声明变量或常量，保存运行时数据。
- 84: `fileInputRef.current?.click()` -> 执行当前语句，参与该文件整体逻辑。
- 85: `}` -> 结束当前语句或代码块。
- 86: `(空行)` -> 空行，用于提升代码结构可读性。
- 87: `const handleAvatarChange = async (e: React.ChangeEvent<HTMLInputElement>) => {` -> 声明变量或常量，保存运行时数据。
- 88: `const file = e.target.files?.[0]` -> 声明变量或常量，保存运行时数据。
- 89: `if (!file) return` -> 条件判断分支，根据场景执行不同逻辑。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `// 验证文件类型` -> 注释行，用于解释设计意图或使用说明。
- 92: `if (!file.type.startsWith('image/')) {` -> 条件判断分支，根据场景执行不同逻辑。
- 93: `toast.error('请选择图片文件')` -> 执行当前语句，参与该文件整体逻辑。
- 94: `return` -> 返回结果或提前结束当前流程。
- 95: `}` -> 结束当前语句或代码块。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `// 验证文件大小（最大 5MB）` -> 注释行，用于解释设计意图或使用说明。
- 98: `if (file.size > 2 * 1024 * 1024) {` -> 条件判断分支，根据场景执行不同逻辑。
- 99: `toast.error('图片大小不能超过2MB')` -> 执行当前语句，参与该文件整体逻辑。
- 100: `return` -> 返回结果或提前结束当前流程。
- 101: `}` -> 结束当前语句或代码块。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `// 预览` -> 注释行，用于解释设计意图或使用说明。
- 104: `const reader = new FileReader()` -> 声明变量或常量，保存运行时数据。
- 105: `reader.onload = (event) => {` -> 执行当前语句，参与该文件整体逻辑。
- 106: `setAvatarPreview(event.target?.result as string)` -> 执行当前语句，参与该文件整体逻辑。
- 107: `}` -> 结束当前语句或代码块。
- 108: `reader.readAsDataURL(file)` -> 执行当前语句，参与该文件整体逻辑。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `// 上传` -> 注释行，用于解释设计意图或使用说明。
- 111: `await uploadAvatar(file)` -> 执行当前语句，参与该文件整体逻辑。
- 112: `}` -> 结束当前语句或代码块。
- 113: `(空行)` -> 空行，用于提升代码结构可读性。
- 114: `const uploadAvatar = async (file: File) => {` -> 声明变量或常量，保存运行时数据。
- 115: `const token = typeof window !== 'undefined' ? localStorage.getItem('auth_token') : null` -> 声明变量或常量，保存运行时数据。
- 116: `if (!token) {` -> 条件判断分支，根据场景执行不同逻辑。
- 117: `toast.error('未登录')` -> 执行当前语句，参与该文件整体逻辑。
- 118: `return` -> 返回结果或提前结束当前流程。
- 119: `}` -> 结束当前语句或代码块。
- 120: `(空行)` -> 空行，用于提升代码结构可读性。
- 121: `setUploadingAvatar(true)` -> 执行当前语句，参与该文件整体逻辑。
- 122: `try {` -> 异常处理逻辑，提升程序健壮性。
- 123: `const formData = new FormData()` -> 声明变量或常量，保存运行时数据。
- 124: `formData.append('avatar', file)` -> 执行当前语句，参与该文件整体逻辑。
- 125: `(空行)` -> 空行，用于提升代码结构可读性。
- 126: `const res = await fetch('/api/users/avatar', {` -> 声明变量或常量，保存运行时数据。
- 127: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 128: `headers: { Authorization: \`Bearer ${token}\` },` -> 执行当前语句，参与该文件整体逻辑。
- 129: `body: formData,` -> 执行当前语句，参与该文件整体逻辑。
- 130: `})` -> 执行当前语句，参与该文件整体逻辑。
- 131: `(空行)` -> 空行，用于提升代码结构可读性。
- 132: `const data = await res.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 133: `if (!res.ok || !data?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 134: `throw new Error(data?.error || '上传失败')` -> 执行当前语句，参与该文件整体逻辑。
- 135: `}` -> 结束当前语句或代码块。
- 136: `(空行)` -> 空行，用于提升代码结构可读性。
- 137: `// 更新本地用户数据` -> 注释行，用于解释设计意图或使用说明。
- 138: `if (data.avatar_url) {` -> 条件判断分支，根据场景执行不同逻辑。
- 139: `login({ ...user!, avatar_url: data.avatar_url })` -> 执行当前语句，参与该文件整体逻辑。
- 140: `setAvatarPreview(data.avatar_url)` -> 执行当前语句，参与该文件整体逻辑。
- 141: `toast.success('头像上传成功')` -> 执行当前语句，参与该文件整体逻辑。
- 142: `}` -> 结束当前语句或代码块。
- 143: `} catch (err: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 144: `toast.error(err?.message || '头像上传失败')` -> 执行当前语句，参与该文件整体逻辑。
- 145: `// 恢复原头像` -> 注释行，用于解释设计意图或使用说明。
- 146: `setAvatarPreview(user?.avatar_url || null)` -> 执行当前语句，参与该文件整体逻辑。
- 147: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 148: `setUploadingAvatar(false)` -> 执行当前语句，参与该文件整体逻辑。
- 149: `}` -> 结束当前语句或代码块。
- 150: `}` -> 结束当前语句或代码块。
- 151: `(空行)` -> 空行，用于提升代码结构可读性。
- 152: `const onSave = async (e: FormEvent) => {` -> 声明变量或常量，保存运行时数据。
- 153: `e.preventDefault()` -> 执行当前语句，参与该文件整体逻辑。
- 154: `if (!user) return` -> 条件判断分支，根据场景执行不同逻辑。
- 155: `const token = typeof window !== 'undefined' ? localStorage.getItem('auth_token') : null` -> 声明变量或常量，保存运行时数据。
- 156: `if (!token) {` -> 条件判断分支，根据场景执行不同逻辑。
- 157: `toast.error('未登录或 token 失效')` -> 执行当前语句，参与该文件整体逻辑。
- 158: `router.push('/login?returnUrl=' + encodeURIComponent('/profile/edit'))` -> 执行当前语句，参与该文件整体逻辑。
- 159: `return` -> 返回结果或提前结束当前流程。
- 160: `}` -> 结束当前语句或代码块。
- 161: `const authHeaders = { Authorization: \`Bearer ${token}\` }` -> 声明变量或常量，保存运行时数据。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `const prefs = {` -> 声明变量或常量，保存运行时数据。
- 164: `destinations: form.destinations` -> 执行当前语句，参与该文件整体逻辑。
- 165: `.split(',')` -> 执行当前语句，参与该文件整体逻辑。
- 166: `.map((s) => s.trim())` -> 执行当前语句，参与该文件整体逻辑。
- 167: `.filter(Boolean),` -> 执行当前语句，参与该文件整体逻辑。
- 168: `budget_range: {` -> 执行当前语句，参与该文件整体逻辑。
- 169: `min: Number(form.budget_min) || 0,` -> 执行当前语句，参与该文件整体逻辑。
- 170: `max: Number(form.budget_max) || 0,` -> 执行当前语句，参与该文件整体逻辑。
- 171: `},` -> 执行当前语句，参与该文件整体逻辑。
- 172: `travel_style: form.travel_style,` -> 执行当前语句，参与该文件整体逻辑。
- 173: `group_size: Math.max(1, Number(form.group_size) || 1),` -> 执行当前语句，参与该文件整体逻辑。
- 174: `interests: form.interests` -> 执行当前语句，参与该文件整体逻辑。
- 175: `.split(',')` -> 执行当前语句，参与该文件整体逻辑。
- 176: `.map((s) => s.trim())` -> 执行当前语句，参与该文件整体逻辑。
- 177: `.filter(Boolean),` -> 执行当前语句，参与该文件整体逻辑。
- 178: `}` -> 结束当前语句或代码块。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `try {` -> 异常处理逻辑，提升程序健壮性。
- 181: `const profileRes = await fetch('/api/users/profile', {` -> 声明变量或常量，保存运行时数据。
- 182: `method: 'PUT',` -> 执行当前语句，参与该文件整体逻辑。
- 183: `headers: { ...authHeaders, 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 184: `body: JSON.stringify({` -> 执行当前语句，参与该文件整体逻辑。
- 185: `nickname: form.nickname.trim() || user.nickname,` -> 执行当前语句，参与该文件整体逻辑。
- 186: `}),` -> 执行当前语句，参与该文件整体逻辑。
- 187: `})` -> 执行当前语句，参与该文件整体逻辑。
- 188: `if (profileRes.status === 401) {` -> 条件判断分支，根据场景执行不同逻辑。
- 189: `router.push('/login?returnUrl=' + encodeURIComponent('/profile/edit'))` -> 执行当前语句，参与该文件整体逻辑。
- 190: `return` -> 返回结果或提前结束当前流程。
- 191: `}` -> 结束当前语句或代码块。
- 192: `const profileData = await profileRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 193: `if (!profileRes.ok || !profileData?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 194: `throw new Error(profileData?.error || \`HTTP ${profileRes.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 195: `}` -> 结束当前语句或代码块。
- 196: `(空行)` -> 空行，用于提升代码结构可读性。
- 197: `const prefRes = await fetch('/api/users/preferences', {` -> 声明变量或常量，保存运行时数据。
- 198: `method: 'PUT',` -> 执行当前语句，参与该文件整体逻辑。
- 199: `headers: { ...authHeaders, 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 200: `body: JSON.stringify(prefs),` -> 执行当前语句，参与该文件整体逻辑。
- 201: `})` -> 执行当前语句，参与该文件整体逻辑。
- 202: `if (prefRes.status === 401) {` -> 条件判断分支，根据场景执行不同逻辑。
- 203: `router.push('/login?returnUrl=' + encodeURIComponent('/profile/edit'))` -> 执行当前语句，参与该文件整体逻辑。
- 204: `return` -> 返回结果或提前结束当前流程。
- 205: `}` -> 结束当前语句或代码块。
- 206: `const prefData = await prefRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 207: `if (!prefRes.ok || !prefData?.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 208: `throw new Error(prefData?.error || \`HTTP ${prefRes.status}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 209: `}` -> 结束当前语句或代码块。
- 210: `(空行)` -> 空行，用于提升代码结构可读性。
- 211: `const meRes = await fetch('/api/users/me', { headers: authHeaders })` -> 声明变量或常量，保存运行时数据。
- 212: `const meData = await meRes.json().catch(() => ({}))` -> 声明变量或常量，保存运行时数据。
- 213: `if (!meRes.ok || !meData?.success) throw new Error(meData?.error || \`HTTP ${meRes.status}\`)` -> 条件判断分支，根据场景执行不同逻辑。
- 214: `(空行)` -> 空行，用于提升代码结构可读性。
- 215: `login(meData.user)` -> 执行当前语句，参与该文件整体逻辑。
- 216: `toast.success('保存成功')` -> 执行当前语句，参与该文件整体逻辑。
- 217: `router.push('/profile')` -> 执行当前语句，参与该文件整体逻辑。
- 218: `} catch (err: any) {` -> 执行当前语句，参与该文件整体逻辑。
- 219: `toast.error(err?.message || '保存失败')` -> 执行当前语句，参与该文件整体逻辑。
- 220: `}` -> 结束当前语句或代码块。
- 221: `}` -> 结束当前语句或代码块。
- 222: `(空行)` -> 空行，用于提升代码结构可读性。
- 223: `return (` -> 返回结果或提前结束当前流程。
- 224: `<div className="min-h-screen page-bg">` -> JSX/HTML 结构行，用于描述页面元素。
- 225: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 226: `<main className="pt-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `<div className="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 228: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 229: `<Link` -> 执行当前语句，参与该文件整体逻辑。
- 230: `href="/profile"` -> 执行当前语句，参与该文件整体逻辑。
- 231: `className="inline-flex items-center gap-1 text-sm font-medium text-slate-600 hover:text-blue-600"` -> 执行当前语句，参与该文件整体逻辑。
- 232: `>` -> 执行当前语句，参与该文件整体逻辑。
- 233: `<ArrowLeft className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `返回我的` -> 执行当前语句，参与该文件整体逻辑。
- 235: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 237: `(空行)` -> 空行，用于提升代码结构可读性。
- 238: `<div className="card p-6 rounded-xl">` -> JSX/HTML 结构行，用于描述页面元素。
- 239: `<h1 className="text-2xl font-bold text-gray-900">编辑资料与偏好</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 240: `<p className="text-sm text-gray-500 mt-1">保存后将影响行程生成与推荐</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 241: `(空行)` -> 空行，用于提升代码结构可读性。
- 242: `{!user ? (` -> 执行当前语句，参与该文件整体逻辑。
- 243: `<p className="mt-6 text-gray-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 244: `请先 <Link href="/login" className="text-blue-600 underline">登录</Link>` -> 执行当前语句，参与该文件整体逻辑。
- 245: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 246: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 247: `<form className="space-y-4 mt-6" onSubmit={onSave}>` -> JSX/HTML 结构行，用于描述页面元素。
- 248: `(空行)` -> 空行，用于提升代码结构可读性。
- 249: `<div className="flex items-center gap-4 p-4 rounded-2xl border border-slate-100 bg-slate-50/60">` -> JSX/HTML 结构行，用于描述页面元素。
- 250: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 251: `className="relative shrink-0 group cursor-pointer"` -> 执行当前语句，参与该文件整体逻辑。
- 252: `title="点击更换头像"` -> 执行当前语句，参与该文件整体逻辑。
- 253: `onClick={handleAvatarClick}` -> 执行当前语句，参与该文件整体逻辑。
- 254: `>` -> 执行当前语句，参与该文件整体逻辑。
- 255: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 256: `ref={fileInputRef}` -> 执行当前语句，参与该文件整体逻辑。
- 257: `type="file"` -> 执行当前语句，参与该文件整体逻辑。
- 258: `accept="image/*"` -> 执行当前语句，参与该文件整体逻辑。
- 259: `className="hidden"` -> 执行当前语句，参与该文件整体逻辑。
- 260: `onChange={handleAvatarChange}` -> 执行当前语句，参与该文件整体逻辑。
- 261: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 262: `<div className={\`w-16 h-16 rounded-2xl overflow-hidden shadow-md border-2 border-white ${` -> 执行当前语句，参与该文件整体逻辑。
- 263: `avatarPreview ? '' : 'bg-gradient-to-br from-sky-500 to-indigo-600 text-white flex items-center justify-center text-2xl font-bold'` -> 执行当前语句，参与该文件整体逻辑。
- 264: `}\`}>` -> 执行当前语句，参与该文件整体逻辑。
- 265: `{uploadingAvatar ? (` -> 执行当前语句，参与该文件整体逻辑。
- 266: `<div className="w-full h-full flex items-center justify-center bg-slate-100">` -> JSX/HTML 结构行，用于描述页面元素。
- 267: `<Loader2 className="h-6 w-6 animate-spin text-slate-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 268: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 269: `) : avatarPreview ? (` -> 执行当前语句，参与该文件整体逻辑。
- 270: `// eslint-disable-next-line @next/next/no-img-element` -> 注释行，用于解释设计意图或使用说明。
- 271: `<img` -> 执行当前语句，参与该文件整体逻辑。
- 272: `src={avatarPreview}` -> 执行当前语句，参与该文件整体逻辑。
- 273: `alt="头像"` -> 执行当前语句，参与该文件整体逻辑。
- 274: `className="w-full h-full object-cover"` -> 执行当前语句，参与该文件整体逻辑。
- 275: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 276: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 277: `<span>{(form.nickname || user?.nickname || '?')[0].toUpperCase()}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 278: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 279: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 280: `<div className="absolute inset-0 rounded-2xl bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center text-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 281: `<Camera className="h-6 w-6" />` -> JSX/HTML 结构行，用于描述页面元素。
- 282: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `<div className="absolute -bottom-1 -right-1 w-6 h-6 bg-blue-600 rounded-full flex items-center justify-center text-white shadow-md">` -> JSX/HTML 结构行，用于描述页面元素。
- 285: `<Camera className="h-3 w-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 286: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 287: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 288: `<div className="min-w-0 flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 289: `<div className="font-semibold text-slate-800 truncate">{user?.nickname || '旅行用户'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 290: `<div className="text-xs text-slate-500 mt-0.5 truncate">{user?.email || user?.phone || '未设置'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 291: `<div className="flex flex-wrap items-center gap-2 mt-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 292: `{user?.is_admin ? (` -> 执行当前语句，参与该文件整体逻辑。
- 293: `<span className="inline-flex items-center gap-1 text-[11px] font-bold px-2 py-0.5 rounded-full border border-amber-200 bg-amber-50 text-amber-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 294: `🔐 管理员` -> 执行当前语句，参与该文件整体逻辑。
- 295: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 296: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 297: `<span className="inline-flex items-center gap-1 text-[11px] font-bold px-2 py-0.5 rounded-full border border-blue-200 bg-blue-50 text-blue-700">` -> JSX/HTML 结构行，用于描述页面元素。
- 298: `普通用户` -> 执行当前语句，参与该文件整体逻辑。
- 299: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 301: `<span className="text-[11px] text-slate-400">` -> JSX/HTML 结构行，用于描述页面元素。
- 302: `注册时间：{user?.created_at ? new Date(user.created_at).toLocaleDateString('zh-CN') : '未知'}` -> 执行当前语句，参与该文件整体逻辑。
- 303: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 304: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 305: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 306: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 307: `(空行)` -> 空行，用于提升代码结构可读性。
- 308: `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 309: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 310: `<span className="text-sm text-gray-600">昵称</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 311: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 312: `className="input bg-white w-full mt-1"` -> 执行当前语句，参与该文件整体逻辑。
- 313: `value={form.nickname}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 314: `onChange={(e) => setForm((s) => ({ ...s, nickname: e.target.value }))}` -> 执行当前语句，参与该文件整体逻辑。
- 315: `placeholder={user?.nickname ? \`当前：${user.nickname}\` : '设置你的昵称'}` -> 执行当前语句，参与该文件整体逻辑。
- 316: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 317: `<span className="text-[11px] text-slate-400 mt-1 block">2-20个字符，支持中英文和数字</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 318: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 319: `(空行)` -> 空行，用于提升代码结构可读性。
- 320: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 321: `<span className="text-sm text-gray-600">偏好风格</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 322: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 323: `className="input bg-white w-full mt-1"` -> 执行当前语句，参与该文件整体逻辑。
- 324: `value={form.travel_style}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 325: `onChange={(e) => setForm((s) => ({ ...s, travel_style: e.target.value as typeof form.travel_style }))}` -> 执行当前语句，参与该文件整体逻辑。
- 326: `>` -> 执行当前语句，参与该文件整体逻辑。
- 327: `<option value="adventure">🧗 冒险 - 追求刺激与挑战</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 328: `<option value="relaxation">🏖 休闲 - 放松身心为主</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 329: `<option value="cultural">🏛 人文 - 探索历史文化</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 330: `<option value="business">💼 商务 - 兼顾工作与出行</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 331: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 332: `<span className="text-[11px] text-slate-400 mt-1 block">影响行程推荐的整体基调</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 333: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 334: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 335: `(空行)` -> 空行，用于提升代码结构可读性。
- 336: `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 337: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 338: `<span className="text-sm text-gray-600">出行人数</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 339: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 340: `className="input bg-white w-full mt-1"` -> 执行当前语句，参与该文件整体逻辑。
- 341: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 342: `min={1}` -> 执行当前语句，参与该文件整体逻辑。
- 343: `value={form.group_size}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 344: `onChange={(e) => setForm((s) => ({ ...s, group_size: Number(e.target.value) }))}` -> 执行当前语句，参与该文件整体逻辑。
- 345: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 346: `<span className="text-[11px] text-slate-400 mt-1 block">用于预算和行程规划参考</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 347: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 348: `(空行)` -> 空行，用于提升代码结构可读性。
- 349: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 350: `<span className="text-sm text-gray-600">预算区间（元）</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 351: `<div className="flex gap-2 mt-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 352: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 353: `className="input bg-white w-1/2"` -> 执行当前语句，参与该文件整体逻辑。
- 354: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 355: `value={form.budget_min}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 356: `onChange={(e) => setForm((s) => ({ ...s, budget_min: Number(e.target.value) }))}` -> 执行当前语句，参与该文件整体逻辑。
- 357: `placeholder="最低预算"` -> 执行当前语句，参与该文件整体逻辑。
- 358: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 359: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 360: `className="input bg-white w-1/2"` -> 执行当前语句，参与该文件整体逻辑。
- 361: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 362: `value={form.budget_max}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 363: `onChange={(e) => setForm((s) => ({ ...s, budget_max: Number(e.target.value) }))}` -> 执行当前语句，参与该文件整体逻辑。
- 364: `placeholder="最高预算"` -> 执行当前语句，参与该文件整体逻辑。
- 365: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 366: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 367: `<span className="text-[11px] text-slate-400 mt-1 block">0 表示不设上限</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 370: `(空行)` -> 空行，用于提升代码结构可读性。
- 371: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `<span className="text-sm text-gray-600">兴趣标签</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 373: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 374: `className="input bg-white w-full mt-1"` -> 执行当前语句，参与该文件整体逻辑。
- 375: `value={form.interests}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 376: `onChange={(e) => setForm((s) => ({ ...s, interests: e.target.value }))}` -> 执行当前语句，参与该文件整体逻辑。
- 377: `placeholder="美食, 自然风景, 博物馆, 摄影"` -> 执行当前语句，参与该文件整体逻辑。
- 378: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 379: `<span className="text-[11px] text-slate-400 mt-1 block">多个标签用逗号分隔，帮助 AI 推荐更精准</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `(空行)` -> 空行，用于提升代码结构可读性。
- 382: `<label className="block">` -> JSX/HTML 结构行，用于描述页面元素。
- 383: `<span className="text-sm text-gray-600">常去目的地</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 384: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 385: `className="input bg-white w-full mt-1"` -> 执行当前语句，参与该文件整体逻辑。
- 386: `value={form.destinations}   // value` -> 执行当前语句，参与该文件整体逻辑。
- 387: `onChange={(e) => setForm((s) => ({ ...s, destinations: e.target.value }))}` -> 执行当前语句，参与该文件整体逻辑。
- 388: `placeholder="上海, 杭州, 北京"` -> 执行当前语句，参与该文件整体逻辑。
- 389: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 390: `<span className="text-[11px] text-slate-400 mt-1 block">你曾经去过或常去的地方，方便个性化推荐</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 391: `</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 392: `(空行)` -> 空行，用于提升代码结构可读性。
- 393: `<div className="flex flex-wrap gap-3 pt-4 border-t border-slate-100 mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 394: `<button className="btn btn-primary justify-center gap-2 rounded-xl border-2" type="submit" disabled={!canSave}>` -> JSX/HTML 结构行，用于描述页面元素。
- 395: `<svg xmlns="http://www.w3.org/2000/svg" className="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">` -> JSX/HTML 结构行，用于描述页面元素。
- 396: `<path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/>` -> JSX/HTML 结构行，用于描述页面元素。
- 397: `<polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/>` -> JSX/HTML 结构行，用于描述页面元素。
- 398: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 399: `保存修改` -> 执行当前语句，参与该文件整体逻辑。
- 400: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 401: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 402: `type="button"` -> 执行当前语句，参与该文件整体逻辑。
- 403: `className="btn btn-outline justify-center gap-1 text-slate-500 rounded-xl border-2 border-slate-200 hover:border-slate-300"` -> 执行当前语句，参与该文件整体逻辑。
- 404: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 405: `if (user) {` -> 条件判断分支，根据场景执行不同逻辑。
- 406: `setForm({` -> 执行当前语句，参与该文件整体逻辑。
- 407: `nickname: user.nickname || '',` -> 执行当前语句，参与该文件整体逻辑。
- 408: `travel_style: user.preferences?.travel_style || 'relaxation',` -> 执行当前语句，参与该文件整体逻辑。
- 409: `group_size: user.preferences?.group_size || 1,` -> 执行当前语句，参与该文件整体逻辑。
- 410: `interests: user.preferences?.interests?.join(', ') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 411: `destinations: user.preferences?.destinations?.join(', ') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 412: `budget_min: user.preferences?.budget_range?.min ?? 0,` -> 执行当前语句，参与该文件整体逻辑。
- 413: `budget_max: user.preferences?.budget_range?.max ?? 0,` -> 执行当前语句，参与该文件整体逻辑。
- 414: `})` -> 执行当前语句，参与该文件整体逻辑。
- 415: `toast('已重置为原始值')` -> 执行当前语句，参与该文件整体逻辑。
- 416: `}` -> 结束当前语句或代码块。
- 417: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 418: `>` -> 执行当前语句，参与该文件整体逻辑。
- 419: `<svg xmlns="http://www.w3.org/2000/svg" className="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">` -> JSX/HTML 结构行，用于描述页面元素。
- 420: `<polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-3.52"/>` -> JSX/HTML 结构行，用于描述页面元素。
- 421: `</svg>` -> JSX/HTML 结构行，用于描述页面元素。
- 422: `重置` -> 执行当前语句，参与该文件整体逻辑。
- 423: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 424: `<Link href="/profile" className="btn btn-outline justify-center ml-auto rounded-xl border-2 border-slate-200 hover:border-slate-300">` -> JSX/HTML 结构行，用于描述页面元素。
- 425: `返回我的` -> 执行当前语句，参与该文件整体逻辑。
- 426: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 427: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 428: `</form>` -> JSX/HTML 结构行，用于描述页面元素。
- 429: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 430: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 431: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 432: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 433: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 434: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 435: `)` -> 结束当前语句或代码块。
- 436: `}` -> 结束当前语句或代码块。
