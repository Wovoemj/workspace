# `services/flight_service.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# -*- coding: utf-8 -*-` -> 注释行，用于解释设计意图或使用说明。
- 2: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 3: `============================================` -> 执行当前语句，参与该文件整体逻辑。
- 4: `航班服务模块` -> 执行当前语句，参与该文件整体逻辑。
- 5: `============================================` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `【模块说明】` -> 执行当前语句，参与该文件整体逻辑。
- 8: `- 提供航班搜索和详情查询功能` -> 执行当前语句，参与该文件整体逻辑。
- 9: `- 采用工厂模式，支持Mock和真实API切换` -> 执行当前语句，参与该文件整体逻辑。
- 10: `- 目前返回模拟数据，预留对接真实API的位置` -> 执行当前语句，参与该文件整体逻辑。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `【支持的API提供商（待对接）】` -> 执行当前语句，参与该文件整体逻辑。
- 13: `- 携程机票 API` -> 执行当前语句，参与该文件整体逻辑。
- 14: `- 飞猪机票 API` -> 执行当前语句，参与该文件整体逻辑。
- 15: `- Amadeus API` -> 执行当前语句，参与该文件整体逻辑。
- 16: `(空行)` -> 空行，用于提升代码结构可读性。
- 17: `【数据结构】` -> 执行当前语句，参与该文件整体逻辑。
- 18: `- Flight: 航班基本信息（航线、时间、价格）` -> 执行当前语句，参与该文件整体逻辑。
- 19: `- FlightDetail: 航班详情（登机口、状态、餐食等）` -> 执行当前语句，参与该文件整体逻辑。
- 20: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `from abc import ABC, abstractmethod` -> 执行当前语句，参与该文件整体逻辑。
- 23: `from datetime import datetime, timedelta` -> 执行当前语句，参与该文件整体逻辑。
- 24: `from typing import List, Dict, Optional` -> 执行当前语句，参与该文件整体逻辑。
- 25: `import random` -> 导入依赖模块，供当前文件使用。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `class FlightService(ABC):` -> 定义类型或类结构，约束数据与行为。
- 29: `"""航班服务抽象基类` -> 执行当前语句，参与该文件整体逻辑。
- 30: `(空行)` -> 空行，用于提升代码结构可读性。
- 31: `【设计模式】` -> 执行当前语句，参与该文件整体逻辑。
- 32: `- 抽象工厂模式：定义统一接口` -> 执行当前语句，参与该文件整体逻辑。
- 33: `- 便于后续接入真实API时保持接口一致` -> 执行当前语句，参与该文件整体逻辑。
- 34: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 35: `(空行)` -> 空行，用于提升代码结构可读性。
- 36: `@abstractmethod` -> 装饰器或样式指令，用于声明行为或规则。
- 37: `def search_flights(self, origin: str, destination: str, date: str,` -> 定义 Python 函数，封装可复用逻辑。
- 38: `passengers: int = 1) -> List[Dict]:` -> 执行当前语句，参与该文件整体逻辑。
- 39: `"""搜索航班` -> 执行当前语句，参与该文件整体逻辑。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 42: `origin: 出发城市` -> 执行当前语句，参与该文件整体逻辑。
- 43: `destination: 目的地城市` -> 执行当前语句，参与该文件整体逻辑。
- 44: `date: 出发日期 (YYYY-MM-DD)` -> 执行当前语句，参与该文件整体逻辑。
- 45: `passengers: 乘客数量` -> 执行当前语句，参与该文件整体逻辑。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `Returns:` -> 执行当前语句，参与该文件整体逻辑。
- 48: `航班列表` -> 执行当前语句，参与该文件整体逻辑。
- 49: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 50: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `@abstractmethod` -> 装饰器或样式指令，用于声明行为或规则。
- 53: `def get_flight_detail(self, flight_id: str) -> Optional[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 54: `"""获取航班详情` -> 执行当前语句，参与该文件整体逻辑。
- 55: `(空行)` -> 空行，用于提升代码结构可读性。
- 56: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 57: `flight_id: 航班号` -> 执行当前语句，参与该文件整体逻辑。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `Returns:` -> 执行当前语句，参与该文件整体逻辑。
- 60: `航班详情，未找到返回None` -> 执行当前语句，参与该文件整体逻辑。
- 61: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 62: `pass` -> 执行当前语句，参与该文件整体逻辑。
- 63: `(空行)` -> 空行，用于提升代码结构可读性。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `class MockFlightService(FlightService):` -> 定义类型或类结构，约束数据与行为。
- 66: `"""模拟航班服务（开发测试用）` -> 执行当前语句，参与该文件整体逻辑。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `【功能】` -> 执行当前语句，参与该文件整体逻辑。
- 69: `- 生成随机但合理的航班数据` -> 执行当前语句，参与该文件整体逻辑。
- 70: `- 便于前端开发和测试` -> 执行当前语句，参与该文件整体逻辑。
- 71: `- 数据结构与真实API保持一致` -> 执行当前语句，参与该文件整体逻辑。
- 72: `(空行)` -> 空行，用于提升代码结构可读性。
- 73: `【航空公司】` -> 执行当前语句，参与该文件整体逻辑。
- 74: `- CA: 中国国航` -> 执行当前语句，参与该文件整体逻辑。
- 75: `- MU: 东方航空` -> 执行当前语句，参与该文件整体逻辑。
- 76: `- CZ: 南方航空` -> 执行当前语句，参与该文件整体逻辑。
- 77: `- HU: 海南航空` -> 执行当前语句，参与该文件整体逻辑。
- 78: `- 3U: 四川航空` -> 执行当前语句，参与该文件整体逻辑。
- 79: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 80: `(空行)` -> 空行，用于提升代码结构可读性。
- 81: `def search_flights(self, origin: str, destination: str, date: str,` -> 定义 Python 函数，封装可复用逻辑。
- 82: `passengers: int = 1) -> List[Dict]:` -> 执行当前语句，参与该文件整体逻辑。
- 83: `"""返回模拟航班数据` -> 执行当前语句，参与该文件整体逻辑。
- 84: `(空行)` -> 空行，用于提升代码结构可读性。
- 85: `【生成规则】` -> 执行当前语句，参与该文件整体逻辑。
- 86: `- 每次返回5个航班` -> 执行当前语句，参与该文件整体逻辑。
- 87: `- 出发时间：6:00-22:59随机` -> 执行当前语句，参与该文件整体逻辑。
- 88: `- 飞行时长：90-300分钟随机` -> 执行当前语句，参与该文件整体逻辑。
- 89: `- 价格：300-1500元随机` -> 执行当前语句，参与该文件整体逻辑。
- 90: `- 座位余量：5-50随机` -> 执行当前语句，参与该文件整体逻辑。
- 91: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 92: `# 航空公司列表` -> 注释行，用于解释设计意图或使用说明。
- 93: `airlines = [` -> 执行当前语句，参与该文件整体逻辑。
- 94: `{'code': 'CA', 'name': '中国国航'},` -> 执行当前语句，参与该文件整体逻辑。
- 95: `{'code': 'MU', 'name': '东方航空'},` -> 执行当前语句，参与该文件整体逻辑。
- 96: `{'code': 'CZ', 'name': '南方航空'},` -> 执行当前语句，参与该文件整体逻辑。
- 97: `{'code': 'HU', 'name': '海南航空'},` -> 执行当前语句，参与该文件整体逻辑。
- 98: `{'code': '3U', 'name': '四川航空'},` -> 执行当前语句，参与该文件整体逻辑。
- 99: `]` -> 执行当前语句，参与该文件整体逻辑。
- 100: `(空行)` -> 空行，用于提升代码结构可读性。
- 101: `flights = []` -> 执行当前语句，参与该文件整体逻辑。
- 102: `for i in range(5):` -> 循环处理集合或重复执行逻辑。
- 103: `airline = random.choice(airlines)` -> 执行当前语句，参与该文件整体逻辑。
- 104: `(空行)` -> 空行，用于提升代码结构可读性。
- 105: `# 生成随机出发时间` -> 注释行，用于解释设计意图或使用说明。
- 106: `dep_time = f"{random.randint(6, 22):02d}:{random.randint(0, 59):02d}"` -> 执行当前语句，参与该文件整体逻辑。
- 107: `(空行)` -> 空行，用于提升代码结构可读性。
- 108: `# 计算飞行时长和到达时间` -> 注释行，用于解释设计意图或使用说明。
- 109: `duration = random.randint(90, 300)  # 分钟` -> 执行当前语句，参与该文件整体逻辑。
- 110: `arr_hour = (int(dep_time[:2]) + duration // 60) % 24` -> 执行当前语句，参与该文件整体逻辑。
- 111: `arr_min = (int(dep_time[3:]) + duration % 60)` -> 执行当前语句，参与该文件整体逻辑。
- 112: `if arr_min >= 60:` -> 条件判断分支，根据场景执行不同逻辑。
- 113: `arr_hour = (arr_hour + 1) % 24` -> 执行当前语句，参与该文件整体逻辑。
- 114: `arr_min -= 60` -> 执行当前语句，参与该文件整体逻辑。
- 115: `arr_time = f"{arr_hour:02d}:{arr_min:02d}"` -> 执行当前语句，参与该文件整体逻辑。
- 116: `(空行)` -> 空行，用于提升代码结构可读性。
- 117: `base_price = random.randint(300, 1500)` -> 执行当前语句，参与该文件整体逻辑。
- 118: `flights.append({` -> 执行当前语句，参与该文件整体逻辑。
- 119: `'flight_id': f"{airline['code']}{random.randint(1000, 9999)}",` -> 执行当前语句，参与该文件整体逻辑。
- 120: `'airline_code': airline['code'],` -> 执行当前语句，参与该文件整体逻辑。
- 121: `'airline_name': airline['name'],` -> 执行当前语句，参与该文件整体逻辑。
- 122: `'flight_number': f"{airline['code']}{random.randint(100, 999)}",` -> 执行当前语句，参与该文件整体逻辑。
- 123: `'origin': origin,` -> 执行当前语句，参与该文件整体逻辑。
- 124: `'destination': destination,` -> 执行当前语句，参与该文件整体逻辑。
- 125: `'departure_date': date,` -> 执行当前语句，参与该文件整体逻辑。
- 126: `'departure_time': dep_time,` -> 执行当前语句，参与该文件整体逻辑。
- 127: `'arrival_time': arr_time,` -> 执行当前语句，参与该文件整体逻辑。
- 128: `'duration': f"{duration // 60}h{duration % 60}m",` -> 执行当前语句，参与该文件整体逻辑。
- 129: `'price': base_price,` -> 执行当前语句，参与该文件整体逻辑。
- 130: `'price_child': int(base_price * 0.75),  # 儿童票75折` -> 执行当前语句，参与该文件整体逻辑。
- 131: `'seats_available': random.randint(5, 50),` -> 执行当前语句，参与该文件整体逻辑。
- 132: `'aircraft': random.choice(['波音737', '空客320', '波音787', '空客330']),` -> 执行当前语句，参与该文件整体逻辑。
- 133: `'cabin_class': '经济舱',` -> 执行当前语句，参与该文件整体逻辑。
- 134: `'refundable': random.choice([True, False]),  # 是否可退` -> 执行当前语句，参与该文件整体逻辑。
- 135: `'changeable': random.choice([True, False]),  # 是否可改签` -> 执行当前语句，参与该文件整体逻辑。
- 136: `})` -> 执行当前语句，参与该文件整体逻辑。
- 137: `(空行)` -> 空行，用于提升代码结构可读性。
- 138: `# 按价格排序` -> 注释行，用于解释设计意图或使用说明。
- 139: `flights.sort(key=lambda x: x['price'])` -> 执行当前语句，参与该文件整体逻辑。
- 140: `return flights` -> 返回结果或提前结束当前流程。
- 141: `(空行)` -> 空行，用于提升代码结构可读性。
- 142: `def get_flight_detail(self, flight_id: str) -> Optional[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 143: `"""返回模拟航班详情` -> 执行当前语句，参与该文件整体逻辑。
- 144: `(空行)` -> 空行，用于提升代码结构可读性。
- 145: `【详情内容】` -> 执行当前语句，参与该文件整体逻辑。
- 146: `- 航班状态：准点/延误/取消` -> 执行当前语句，参与该文件整体逻辑。
- 147: `- 航站楼、登机口、行李转盘` -> 执行当前语句，参与该文件整体逻辑。
- 148: `- 餐食、WiFi等信息` -> 执行当前语句，参与该文件整体逻辑。
- 149: `- 座位布局` -> 执行当前语句，参与该文件整体逻辑。
- 150: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 151: `return {` -> 返回结果或提前结束当前流程。
- 152: `'flight_id': flight_id,` -> 执行当前语句，参与该文件整体逻辑。
- 153: `'status': 'on_time',  # on_time/delay/cancelled` -> 执行当前语句，参与该文件整体逻辑。
- 154: `'terminal': f"T{random.randint(1, 3)}",` -> 执行当前语句，参与该文件整体逻辑。
- 155: `'gate': f"{random.choice(['A', 'B', 'C'])}{random.randint(1, 50)}",` -> 执行当前语句，参与该文件整体逻辑。
- 156: `'baggage_claim': f"{random.randint(1, 10)}",` -> 执行当前语句，参与该文件整体逻辑。
- 157: `'meals': random.choice([True, False]),` -> 执行当前语句，参与该文件整体逻辑。
- 158: `'wifi': random.choice([True, False]),` -> 执行当前语句，参与该文件整体逻辑。
- 159: `'seat_layout': '3-3',` -> 执行当前语句，参与该文件整体逻辑。
- 160: `'leg_room': random.choice(['标准', '宽敞', '紧急出口']),` -> 执行当前语句，参与该文件整体逻辑。
- 161: `}` -> 结束当前语句或代码块。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `(空行)` -> 空行，用于提升代码结构可读性。
- 164: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 165: `# 真实API服务类（预留，待实现）` -> 注释行，用于解释设计意图或使用说明。
- 166: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 167: `# class CtripFlightService(FlightService):` -> 注释行，用于解释设计意图或使用说明。
- 168: `#     """携程航班服务"""` -> 注释行，用于解释设计意图或使用说明。
- 169: `#     def __init__(self, api_key: str):` -> 注释行，用于解释设计意图或使用说明。
- 170: `#         self.api_key = api_key` -> 注释行，用于解释设计意图或使用说明。
- 171: `#` -> 注释行，用于解释设计意图或使用说明。
- 172: `#     def search_flights(self, origin, destination, date, passengers=1):` -> 注释行，用于解释设计意图或使用说明。
- 173: `#         # TODO: 对接携程 API` -> 注释行，用于解释设计意图或使用说明。
- 174: `#         pass` -> 注释行，用于解释设计意图或使用说明。
- 175: `(空行)` -> 空行，用于提升代码结构可读性。
- 176: `(空行)` -> 空行，用于提升代码结构可读性。
- 177: `def get_flight_service() -> FlightService:` -> 定义 Python 函数，封装可复用逻辑。
- 178: `"""获取航班服务实例` -> 执行当前语句，参与该文件整体逻辑。
- 179: `(空行)` -> 空行，用于提升代码结构可读性。
- 180: `【工厂模式】` -> 执行当前语句，参与该文件整体逻辑。
- 181: `- 目前返回Mock服务` -> 执行当前语句，参与该文件整体逻辑。
- 182: `- 后续可改为读取配置决定返回哪种服务` -> 执行当前语句，参与该文件整体逻辑。
- 183: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 184: `return MockFlightService()` -> 返回结果或提前结束当前流程。
- 185: `(空行)` -> 空行，用于提升代码结构可读性。
- 186: `(空行)` -> 空行，用于提升代码结构可读性。
- 187: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 188: `# 便捷函数` -> 注释行，用于解释设计意图或使用说明。
- 189: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 190: `# 提供直接调用的简化接口` -> 注释行，用于解释设计意图或使用说明。
- 191: `(空行)` -> 空行，用于提升代码结构可读性。
- 192: `def search_flights(origin: str, destination: str, date: str,` -> 定义 Python 函数，封装可复用逻辑。
- 193: `passengers: int = 1) -> List[Dict]:` -> 执行当前语句，参与该文件整体逻辑。
- 194: `"""搜索航班（便捷函数）"""` -> 执行当前语句，参与该文件整体逻辑。
- 195: `service = get_flight_service()` -> 执行当前语句，参与该文件整体逻辑。
- 196: `return service.search_flights(origin, destination, date, passengers)` -> 返回结果或提前结束当前流程。
- 197: `(空行)` -> 空行，用于提升代码结构可读性。
- 198: `(空行)` -> 空行，用于提升代码结构可读性。
- 199: `def get_flight_detail(flight_id: str) -> Optional[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 200: `"""获取航班详情（便捷函数）"""` -> 执行当前语句，参与该文件整体逻辑。
- 201: `service = get_flight_service()` -> 执行当前语句，参与该文件整体逻辑。
- 202: `return service.get_flight_detail(flight_id)` -> 返回结果或提前结束当前流程。
