# `init_db.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# ============================================` -> 注释行，用于解释设计意图或使用说明。
- 2: `# 数据库初始化脚本` -> 注释行，用于解释设计意图或使用说明。
- 3: `# ============================================` -> 注释行，用于解释设计意图或使用说明。
- 4: `#` -> 注释行，用于解释设计意图或使用说明。
- 5: `# 【功能说明】` -> 注释行，用于解释设计意图或使用说明。
- 6: `# - 创建数据库表结构` -> 注释行，用于解释设计意图或使用说明。
- 7: `# - 初始化默认数据（可选）` -> 注释行，用于解释设计意图或使用说明。
- 8: `# - 支持命令行参数控制行为` -> 注释行，用于解释设计意图或使用说明。
- 9: `#` -> 注释行，用于解释设计意图或使用说明。
- 10: `# 【使用方法】` -> 注释行，用于解释设计意图或使用说明。
- 11: `# python init_db.py              # 仅创建表` -> 注释行，用于解释设计意图或使用说明。
- 12: `# python init_db.py --with-data  # 创建表+初始化数据` -> 注释行，用于解释设计意图或使用说明。
- 13: `# python init_db.py --force      # 强制重建表（会清空数据）` -> 注释行，用于解释设计意图或使用说明。
- 14: `(空行)` -> 空行，用于提升代码结构可读性。
- 15: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 16: `数据库初始化脚本 - 用于创建和初始化数据库` -> 执行当前语句，参与该文件整体逻辑。
- 17: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 18: `(空行)` -> 空行，用于提升代码结构可读性。
- 19: `# 导入系统模块` -> 注释行，用于解释设计意图或使用说明。
- 20: `import sys` -> 导入依赖模块，供当前文件使用。
- 21: `# 导入路径处理模块` -> 注释行，用于解释设计意图或使用说明。
- 22: `from pathlib import Path` -> 执行当前语句，参与该文件整体逻辑。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `# 确保当前目录在 Python 路径中，以便能够导入其他模块` -> 注释行，用于解释设计意图或使用说明。
- 25: `sys.path.insert(0, str(Path(__file__).parent))` -> 执行当前语句，参与该文件整体逻辑。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `# 导入Flask应用、数据库实例和初始化函数` -> 注释行，用于解释设计意图或使用说明。
- 28: `from app import app, db, init_db` -> 执行当前语句，参与该文件整体逻辑。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `# 如果这是主程序入口` -> 注释行，用于解释设计意图或使用说明。
- 31: `if __name__ == '__main__':` -> 条件判断分支，根据场景执行不同逻辑。
- 32: `# 调用数据库初始化函数` -> 注释行，用于解释设计意图或使用说明。
- 33: `init_db()` -> 执行当前语句，参与该文件整体逻辑。
- 34: `(空行)` -> 空行，用于提升代码结构可读性。
- 35: `# 打印完成提示` -> 注释行，用于解释设计意图或使用说明。
- 36: `print("\n✅ 数据库初始化完成，可以启动服务器了")` -> 执行当前语句，参与该文件整体逻辑。
- 37: `print("   运行命令：python app.py")` -> 执行当前语句，参与该文件整体逻辑。
