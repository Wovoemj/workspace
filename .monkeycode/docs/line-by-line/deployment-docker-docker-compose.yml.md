# `deployment/docker/docker-compose.yml` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `version: '3.8'` -> 执行当前语句，参与该文件整体逻辑。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `services:` -> 执行当前语句，参与该文件整体逻辑。
- 4: `# MySQL数据库` -> 注释行，用于解释设计意图或使用说明。
- 5: `mysql:` -> 执行当前语句，参与该文件整体逻辑。
- 6: `image: mysql:8.0` -> 执行当前语句，参与该文件整体逻辑。
- 7: `container_name: travel-assistant-mysql` -> 执行当前语句，参与该文件整体逻辑。
- 8: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 9: `MYSQL_ROOT_PASSWORD: rootpassword` -> 执行当前语句，参与该文件整体逻辑。
- 10: `MYSQL_DATABASE: travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 11: `MYSQL_USER: travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 12: `MYSQL_PASSWORD: travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 13: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 14: `- "3306:3306"` -> 执行当前语句，参与该文件整体逻辑。
- 15: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 16: `- mysql_data:/var/lib/mysql` -> 执行当前语句，参与该文件整体逻辑。
- 17: `- ./database/schema:/docker-entrypoint-initdb.d` -> 执行当前语句，参与该文件整体逻辑。
- 18: `- ./database/seeds:/docker-entrypoint-seeds.d` -> 执行当前语句，参与该文件整体逻辑。
- 19: `command: --default-authentication-plugin=mysql_native_password` -> 执行当前语句，参与该文件整体逻辑。
- 20: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 21: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `# Redis缓存` -> 注释行，用于解释设计意图或使用说明。
- 24: `redis:` -> 执行当前语句，参与该文件整体逻辑。
- 25: `image: redis:7-alpine` -> 执行当前语句，参与该文件整体逻辑。
- 26: `container_name: travel-assistant-redis` -> 执行当前语句，参与该文件整体逻辑。
- 27: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 28: `- "6379:6379"` -> 执行当前语句，参与该文件整体逻辑。
- 29: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 30: `- redis_data:/data` -> 执行当前语句，参与该文件整体逻辑。
- 31: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 32: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `# Elasticsearch搜索引擎` -> 注释行，用于解释设计意图或使用说明。
- 35: `elasticsearch:` -> 执行当前语句，参与该文件整体逻辑。
- 36: `image: elasticsearch:8.8.0` -> 执行当前语句，参与该文件整体逻辑。
- 37: `container_name: travel-assistant-elasticsearch` -> 执行当前语句，参与该文件整体逻辑。
- 38: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 39: `- discovery.type=single-node` -> 执行当前语句，参与该文件整体逻辑。
- 40: `- "ES_JAVA_OPTS=-Xms512m -Xmx512m"` -> 执行当前语句，参与该文件整体逻辑。
- 41: `- xpack.security.enabled=false` -> 执行当前语句，参与该文件整体逻辑。
- 42: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 43: `- "9200:9200"` -> 执行当前语句，参与该文件整体逻辑。
- 44: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 45: `- elasticsearch_data:/usr/share/elasticsearch/data` -> 执行当前语句，参与该文件整体逻辑。
- 46: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 47: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `# Milvus向量数据库` -> 注释行，用于解释设计意图或使用说明。
- 50: `milvus:` -> 执行当前语句，参与该文件整体逻辑。
- 51: `image: milvusdb/milvus:v2.3.0` -> 执行当前语句，参与该文件整体逻辑。
- 52: `container_name: travel-assistant-milvus` -> 执行当前语句，参与该文件整体逻辑。
- 53: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 54: `- ETCD_ENDPOINTS=etcd:2379` -> 执行当前语句，参与该文件整体逻辑。
- 55: `- MINIO_ADDRESS=minio:9000` -> 执行当前语句，参与该文件整体逻辑。
- 56: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 57: `- "19530:19530"` -> 执行当前语句，参与该文件整体逻辑。
- 58: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 59: `- milvus_data:/var/lib/milvus` -> 执行当前语句，参与该文件整体逻辑。
- 60: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 61: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 62: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 63: `- etcd` -> 执行当前语句，参与该文件整体逻辑。
- 64: `- minio` -> 执行当前语句，参与该文件整体逻辑。
- 65: `(空行)` -> 空行，用于提升代码结构可读性。
- 66: `# Etcd (Milvus依赖)` -> 注释行，用于解释设计意图或使用说明。
- 67: `etcd:` -> 执行当前语句，参与该文件整体逻辑。
- 68: `image: quay.io/coreos/etcd:v3.5.0` -> 执行当前语句，参与该文件整体逻辑。
- 69: `container_name: travel-assistant-etcd` -> 执行当前语句，参与该文件整体逻辑。
- 70: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 71: `- ETCD_AUTO_COMPACTION_MODE=revision` -> 执行当前语句，参与该文件整体逻辑。
- 72: `- ETCD_AUTO_COMPACTION_RETENTION=1000` -> 执行当前语句，参与该文件整体逻辑。
- 73: `- ETCD_QUOTA_BACKEND_BYTES=4294967296` -> 执行当前语句，参与该文件整体逻辑。
- 74: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 75: `- etcd_data:/etcd` -> 执行当前语句，参与该文件整体逻辑。
- 76: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 77: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 78: `(空行)` -> 空行，用于提升代码结构可读性。
- 79: `# MinIO (Milvus依赖)` -> 注释行，用于解释设计意图或使用说明。
- 80: `minio:` -> 执行当前语句，参与该文件整体逻辑。
- 81: `image: minio/minio:RELEASE.2023-03-20T20-16-18Z` -> 执行当前语句，参与该文件整体逻辑。
- 82: `container_name: travel-assistant-minio` -> 执行当前语句，参与该文件整体逻辑。
- 83: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 84: `- MINIO_ROOT_USER=minioadmin` -> 执行当前语句，参与该文件整体逻辑。
- 85: `- MINIO_ROOT_PASSWORD=minioadmin` -> 执行当前语句，参与该文件整体逻辑。
- 86: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 87: `- "9000:9000"` -> 执行当前语句，参与该文件整体逻辑。
- 88: `- "9001:9001"` -> 执行当前语句，参与该文件整体逻辑。
- 89: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 90: `- minio_data:/data` -> 执行当前语句，参与该文件整体逻辑。
- 91: `command: server /data --console-address ":9001"` -> 执行当前语句，参与该文件整体逻辑。
- 92: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 93: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `# API网关 (Go)` -> 注释行，用于解释设计意图或使用说明。
- 96: `gateway:` -> 执行当前语句，参与该文件整体逻辑。
- 97: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 98: `context: ./backend/gateway` -> 执行当前语句，参与该文件整体逻辑。
- 99: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 100: `container_name: travel-assistant-gateway` -> 执行当前语句，参与该文件整体逻辑。
- 101: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 102: `- "8080:8080"` -> 执行当前语句，参与该文件整体逻辑。
- 103: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 104: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 105: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 106: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 107: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 108: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 109: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 110: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 111: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 112: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 113: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 114: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 115: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 116: `(空行)` -> 空行，用于提升代码结构可读性。
- 117: `# APISIX API网关` -> 注释行，用于解释设计意图或使用说明。
- 118: `apisix:` -> 执行当前语句，参与该文件整体逻辑。
- 119: `image: apache/apisix:3.9-debian` -> 执行当前语句，参与该文件整体逻辑。
- 120: `container_name: travel-assistant-apisix` -> 执行当前语句，参与该文件整体逻辑。
- 121: `restart: always` -> 执行当前语句，参与该文件整体逻辑。
- 122: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 123: `- "8088:9180"  # Admin API` -> 执行当前语句，参与该文件整体逻辑。
- 124: `- "80:9080"    # HTTP 入口` -> 执行当前语句，参与该文件整体逻辑。
- 125: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 126: `- ./apisix/apisix.yml:/usr/local/apisix/conf/apisix.yaml:ro` -> 执行当前语句，参与该文件整体逻辑。
- 127: `- ./apisix/routes.yml:/usr/local/apisix/conf/routes.yaml:ro` -> 执行当前语句，参与该文件整体逻辑。
- 128: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 129: `- APISIX_DEFAULTS_CONF_PATH=/usr/local/apisix/conf` -> 执行当前语句，参与该文件整体逻辑。
- 130: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 131: `- apisix-etcd` -> 执行当前语句，参与该文件整体逻辑。
- 132: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 133: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 134: `(空行)` -> 空行，用于提升代码结构可读性。
- 135: `# APISIX 配置存储 (etcd)` -> 注释行，用于解释设计意图或使用说明。
- 136: `apisix-etcd:` -> 执行当前语句，参与该文件整体逻辑。
- 137: `image: bitnami/etcd:3.5` -> 执行当前语句，参与该文件整体逻辑。
- 138: `container_name: travel-assistant-apisix-etcd` -> 执行当前语句，参与该文件整体逻辑。
- 139: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 140: `- ALLOW_NONE_AUTHENTICATION=yes` -> 执行当前语句，参与该文件整体逻辑。
- 141: `- ETCD_AUTO_COMPACTION_MODE=revision` -> 执行当前语句，参与该文件整体逻辑。
- 142: `- ETCD_AUTO_COMPACTION_RETENTION=1000` -> 执行当前语句，参与该文件整体逻辑。
- 143: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 144: `- apisix_etcd_data:/bitnami/etcd` -> 执行当前语句，参与该文件整体逻辑。
- 145: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 146: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 147: `(空行)` -> 空行，用于提升代码结构可读性。
- 148: `# APISIX Dashboard (可选)` -> 注释行，用于解释设计意图或使用说明。
- 149: `apisix-dashboard:` -> 执行当前语句，参与该文件整体逻辑。
- 150: `image: apache/apisix-dashboard:3.0` -> 执行当前语句，参与该文件整体逻辑。
- 151: `container_name: travel-assistant-apisix-dashboard` -> 执行当前语句，参与该文件整体逻辑。
- 152: `restart: always` -> 执行当前语句，参与该文件整体逻辑。
- 153: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 154: `- "8089:9000"` -> 执行当前语句，参与该文件整体逻辑。
- 155: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 156: `- ETCD_ENDPOINTS=http://apisix-etcd:2379` -> 执行当前语句，参与该文件整体逻辑。
- 157: `- CONSOLE_PROXY=http://apisix:9080` -> 执行当前语句，参与该文件整体逻辑。
- 158: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 159: `- apisix-etcd` -> 执行当前语句，参与该文件整体逻辑。
- 160: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 161: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 162: `(空行)` -> 空行，用于提升代码结构可读性。
- 163: `# 用户服务` -> 注释行，用于解释设计意图或使用说明。
- 164: `user-service:` -> 执行当前语句，参与该文件整体逻辑。
- 165: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 166: `context: ./backend/user-service` -> 执行当前语句，参与该文件整体逻辑。
- 167: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 168: `container_name: travel-assistant-user-service` -> 执行当前语句，参与该文件整体逻辑。
- 169: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 170: `- "8081:8081"` -> 执行当前语句，参与该文件整体逻辑。
- 171: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 172: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 173: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 174: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 175: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 176: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 177: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 178: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 179: `- JWT_SECRET=your-jwt-secret-key` -> 执行当前语句，参与该文件整体逻辑。
- 180: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 181: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 182: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 183: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 184: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 185: `(空行)` -> 空行，用于提升代码结构可读性。
- 186: `# 产品服务` -> 注释行，用于解释设计意图或使用说明。
- 187: `product-service:` -> 执行当前语句，参与该文件整体逻辑。
- 188: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 189: `context: ./backend/product-service` -> 执行当前语句，参与该文件整体逻辑。
- 190: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 191: `container_name: travel-assistant-product-service` -> 执行当前语句，参与该文件整体逻辑。
- 192: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 193: `- "8082:8082"` -> 执行当前语句，参与该文件整体逻辑。
- 194: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 195: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 196: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 197: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 198: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 199: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 200: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 201: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 202: `- ELASTICSEARCH_HOST=elasticsearch` -> 执行当前语句，参与该文件整体逻辑。
- 203: `- ELASTICSEARCH_PORT=9200` -> 执行当前语句，参与该文件整体逻辑。
- 204: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 205: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 206: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 207: `- elasticsearch` -> 执行当前语句，参与该文件整体逻辑。
- 208: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 209: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 210: `(空行)` -> 空行，用于提升代码结构可读性。
- 211: `# 订单服务` -> 注释行，用于解释设计意图或使用说明。
- 212: `order-service:` -> 执行当前语句，参与该文件整体逻辑。
- 213: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 214: `context: ./backend/order-service` -> 执行当前语句，参与该文件整体逻辑。
- 215: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 216: `container_name: travel-assistant-order-service` -> 执行当前语句，参与该文件整体逻辑。
- 217: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 218: `- "8083:8083"` -> 执行当前语句，参与该文件整体逻辑。
- 219: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 220: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 221: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 222: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 223: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 224: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 225: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 226: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 227: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 228: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 229: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 230: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 231: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 232: `(空行)` -> 空行，用于提升代码结构可读性。
- 233: `# AI服务` -> 注释行，用于解释设计意图或使用说明。
- 234: `ai-service:` -> 执行当前语句，参与该文件整体逻辑。
- 235: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 236: `context: ./backend/ai-service` -> 执行当前语句，参与该文件整体逻辑。
- 237: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 238: `container_name: travel-assistant-ai-service` -> 执行当前语句，参与该文件整体逻辑。
- 239: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 240: `- "8084:8084"` -> 执行当前语句，参与该文件整体逻辑。
- 241: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 242: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 243: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 244: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 245: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 246: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 247: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 248: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 249: `- MILVUS_HOST=milvus` -> 执行当前语句，参与该文件整体逻辑。
- 250: `- MILVUS_PORT=19530` -> 执行当前语句，参与该文件整体逻辑。
- 251: `- OPENAI_API_KEY=your-openai-api-key` -> 执行当前语句，参与该文件整体逻辑。
- 252: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 253: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 254: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 255: `- milvus` -> 执行当前语句，参与该文件整体逻辑。
- 256: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 257: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 258: `(空行)` -> 空行，用于提升代码结构可读性。
- 259: `# 推荐服务` -> 注释行，用于解释设计意图或使用说明。
- 260: `recommend-service:` -> 执行当前语句，参与该文件整体逻辑。
- 261: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 262: `context: ./backend/recommend-service` -> 执行当前语句，参与该文件整体逻辑。
- 263: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 264: `container_name: travel-assistant-recommend-service` -> 执行当前语句，参与该文件整体逻辑。
- 265: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 266: `- "8085:8085"` -> 执行当前语句，参与该文件整体逻辑。
- 267: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 268: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 269: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 270: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 271: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 272: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 273: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 274: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 275: `- MILVUS_HOST=milvus` -> 执行当前语句，参与该文件整体逻辑。
- 276: `- MILVUS_PORT=19530` -> 执行当前语句，参与该文件整体逻辑。
- 277: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 278: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 279: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 280: `- milvus` -> 执行当前语句，参与该文件整体逻辑。
- 281: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 282: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `# 通知服务` -> 注释行，用于解释设计意图或使用说明。
- 285: `notification-service:` -> 执行当前语句，参与该文件整体逻辑。
- 286: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 287: `context: ./backend/notification-service` -> 执行当前语句，参与该文件整体逻辑。
- 288: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 289: `container_name: travel-assistant-notification-service` -> 执行当前语句，参与该文件整体逻辑。
- 290: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 291: `- "8086:8086"` -> 执行当前语句，参与该文件整体逻辑。
- 292: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 293: `- DB_HOST=mysql` -> 执行当前语句，参与该文件整体逻辑。
- 294: `- DB_PORT=3306` -> 执行当前语句，参与该文件整体逻辑。
- 295: `- DB_NAME=travel_assistant` -> 执行当前语句，参与该文件整体逻辑。
- 296: `- DB_USER=travel_user` -> 执行当前语句，参与该文件整体逻辑。
- 297: `- DB_PASSWORD=travel_password` -> 执行当前语句，参与该文件整体逻辑。
- 298: `- REDIS_HOST=redis` -> 执行当前语句，参与该文件整体逻辑。
- 299: `- REDIS_PORT=6379` -> 执行当前语句，参与该文件整体逻辑。
- 300: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 301: `- mysql` -> 执行当前语句，参与该文件整体逻辑。
- 302: `- redis` -> 执行当前语句，参与该文件整体逻辑。
- 303: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 304: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 305: `(空行)` -> 空行，用于提升代码结构可读性。
- 306: `# 前端用户端` -> 注释行，用于解释设计意图或使用说明。
- 307: `user-web:` -> 执行当前语句，参与该文件整体逻辑。
- 308: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 309: `context: ./frontend/user-web` -> 执行当前语句，参与该文件整体逻辑。
- 310: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 311: `container_name: travel-assistant-user-web` -> 执行当前语句，参与该文件整体逻辑。
- 312: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 313: `- "3000:3000"` -> 执行当前语句，参与该文件整体逻辑。
- 314: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 315: `- NEXT_PUBLIC_API_URL=http://localhost:8080` -> 执行当前语句，参与该文件整体逻辑。
- 316: `- NEXT_PUBLIC_AI_SERVICE_URL=http://localhost:8084` -> 执行当前语句，参与该文件整体逻辑。
- 317: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 318: `- gateway` -> 执行当前语句，参与该文件整体逻辑。
- 319: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 320: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 321: `(空行)` -> 空行，用于提升代码结构可读性。
- 322: `# 前端管理端` -> 注释行，用于解释设计意图或使用说明。
- 323: `admin-web:` -> 执行当前语句，参与该文件整体逻辑。
- 324: `build:` -> 执行当前语句，参与该文件整体逻辑。
- 325: `context: ./frontend/admin-web` -> 执行当前语句，参与该文件整体逻辑。
- 326: `dockerfile: Dockerfile` -> 执行当前语句，参与该文件整体逻辑。
- 327: `container_name: travel-assistant-admin-web` -> 执行当前语句，参与该文件整体逻辑。
- 328: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 329: `- "3001:3000"` -> 执行当前语句，参与该文件整体逻辑。
- 330: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 331: `- REACT_APP_API_URL=http://localhost:8080` -> 执行当前语句，参与该文件整体逻辑。
- 332: `depends_on:` -> 执行当前语句，参与该文件整体逻辑。
- 333: `- gateway` -> 执行当前语句，参与该文件整体逻辑。
- 334: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 335: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 336: `(空行)` -> 空行，用于提升代码结构可读性。
- 337: `# Prometheus监控` -> 注释行，用于解释设计意图或使用说明。
- 338: `prometheus:` -> 执行当前语句，参与该文件整体逻辑。
- 339: `image: prom/prometheus:latest` -> 执行当前语句，参与该文件整体逻辑。
- 340: `container_name: travel-assistant-prometheus` -> 执行当前语句，参与该文件整体逻辑。
- 341: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 342: `- "9090:9090"` -> 执行当前语句，参与该文件整体逻辑。
- 343: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 344: `- ./deployment/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml` -> 执行当前语句，参与该文件整体逻辑。
- 345: `- prometheus_data:/prometheus` -> 执行当前语句，参与该文件整体逻辑。
- 346: `command:` -> 执行当前语句，参与该文件整体逻辑。
- 347: `- '--config.file=/etc/prometheus/prometheus.yml'` -> 执行当前语句，参与该文件整体逻辑。
- 348: `- '--storage.tsdb.path=/prometheus'` -> 执行当前语句，参与该文件整体逻辑。
- 349: `- '--web.console.libraries=/etc/prometheus/console_libraries'` -> 执行当前语句，参与该文件整体逻辑。
- 350: `- '--web.console.templates=/etc/prometheus/consoles'` -> 执行当前语句，参与该文件整体逻辑。
- 351: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 352: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 353: `(空行)` -> 空行，用于提升代码结构可读性。
- 354: `# Grafana可视化` -> 注释行，用于解释设计意图或使用说明。
- 355: `grafana:` -> 执行当前语句，参与该文件整体逻辑。
- 356: `image: grafana/grafana:latest` -> 执行当前语句，参与该文件整体逻辑。
- 357: `container_name: travel-assistant-grafana` -> 执行当前语句，参与该文件整体逻辑。
- 358: `ports:` -> 执行当前语句，参与该文件整体逻辑。
- 359: `- "3002:3000"` -> 执行当前语句，参与该文件整体逻辑。
- 360: `environment:` -> 执行当前语句，参与该文件整体逻辑。
- 361: `- GF_SECURITY_ADMIN_PASSWORD=admin` -> 执行当前语句，参与该文件整体逻辑。
- 362: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 363: `- grafana_data:/var/lib/grafana` -> 执行当前语句，参与该文件整体逻辑。
- 364: `- ./deployment/monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards` -> 执行当前语句，参与该文件整体逻辑。
- 365: `- ./deployment/monitoring/grafana/datasources:/etc/grafana/provisioning/datasources` -> 执行当前语句，参与该文件整体逻辑。
- 366: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 367: `- travel-network` -> 执行当前语句，参与该文件整体逻辑。
- 368: `(空行)` -> 空行，用于提升代码结构可读性。
- 369: `volumes:` -> 执行当前语句，参与该文件整体逻辑。
- 370: `mysql_data:` -> 执行当前语句，参与该文件整体逻辑。
- 371: `redis_data:` -> 执行当前语句，参与该文件整体逻辑。
- 372: `elasticsearch_data:` -> 执行当前语句，参与该文件整体逻辑。
- 373: `milvus_data:` -> 执行当前语句，参与该文件整体逻辑。
- 374: `etcd_data:` -> 执行当前语句，参与该文件整体逻辑。
- 375: `minio_data:` -> 执行当前语句，参与该文件整体逻辑。
- 376: `prometheus_data:` -> 执行当前语句，参与该文件整体逻辑。
- 377: `grafana_data:` -> 执行当前语句，参与该文件整体逻辑。
- 378: `apisix_etcd_data:` -> 执行当前语句，参与该文件整体逻辑。
- 379: `(空行)` -> 空行，用于提升代码结构可读性。
- 380: `networks:` -> 执行当前语句，参与该文件整体逻辑。
- 381: `travel-network:` -> 执行当前语句，参与该文件整体逻辑。
- 382: `driver: bridge` -> 执行当前语句，参与该文件整体逻辑。
