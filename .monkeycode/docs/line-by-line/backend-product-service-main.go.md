# `backend/product-service/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package product` -> 声明当前文件所属包或模块命名空间。
- 2: `import (` -> 导入依赖模块，供当前文件使用。
- 3: `"context"` -> 执行当前语句，参与该文件整体逻辑。
- 4: `"database/sql"` -> 执行当前语句，参与该文件整体逻辑。
- 5: `"encoding/json"` -> 执行当前语句，参与该文件整体逻辑。
- 6: `"fmt"` -> 执行当前语句，参与该文件整体逻辑。
- 7: `"net/http"` -> 执行当前语句，参与该文件整体逻辑。
- 8: `"strings"` -> 执行当前语句，参与该文件整体逻辑。
- 9: `"time"` -> 执行当前语句，参与该文件整体逻辑。
- 10: `(空行)` -> 空行，用于提升代码结构可读性。
- 11: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该文件整体逻辑。
- 12: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该文件整体逻辑。
- 13: `"gorm.io/gorm"` -> 执行当前语句，参与该文件整体逻辑。
- 14: `)` -> 结束当前语句或代码块。
- 15: `type Service struct {` -> 定义类型或类结构，约束数据与行为。
- 16: `db     *gorm.DB` -> 执行当前语句，参与该文件整体逻辑。
- 17: `logger *logrus.Logger` -> 执行当前语句，参与该文件整体逻辑。
- 18: `}` -> 结束当前语句或代码块。
- 19: `type Product struct {` -> 定义类型或类结构，约束数据与行为。
- 20: `ID           uint      \`json:"id" gorm:"primaryKey"\`` -> 执行当前语句，参与该文件整体逻辑。
- 21: `Type         string    \`json:"type" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 22: `Name         string    \`json:"name" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 23: `Description  string    \`json:"description"\`` -> 执行当前语句，参与该文件整体逻辑。
- 24: `Price        float64   \`json:"price" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 25: `OriginalPrice *float64  \`json:"original_price"\`` -> 执行当前语句，参与该文件整体逻辑。
- 26: `Inventory    int       \`json:"inventory" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 27: `Tags         []string  \`json:"tags" gorm:"type:jsonb"\`` -> 执行当前语句，参与该文件整体逻辑。
- 28: `Metadata     ProductMetadata \`json:"metadata" gorm:"type:jsonb"\`` -> 执行当前语句，参与该文件整体逻辑。
- 29: `Status       string    \`json:"status" gorm:"not null"\`` -> 执行当前语句，参与该文件整体逻辑。
- 30: `Images       []string  \`json:"images" gorm:"type:jsonb"\`` -> 执行当前语句，参与该文件整体逻辑。
- 31: `Location     Location  \`json:"location" gorm:"type:jsonb"\`` -> 执行当前语句，参与该文件整体逻辑。
- 32: `Rating       float64   \`json:"rating"\`` -> 执行当前语句，参与该文件整体逻辑。
- 33: `ReviewCount  int       \`json:"review_count"\`` -> 执行当前语句，参与该文件整体逻辑。
- 34: `CreatedAt    time.Time \`json:"created_at"\`` -> 执行当前语句，参与该文件整体逻辑。
- 35: `UpdatedAt    time.Time \`json:"updated_at"\`` -> 执行当前语句，参与该文件整体逻辑。
- 36: `CoverImage   string    \`json:"cover_image"\`` -> 执行当前语句，参与该文件整体逻辑。
- 37: `Subtitle     string    \`json:"subtitle"\`` -> 执行当前语句，参与该文件整体逻辑。
- 38: `City         string    \`json:"city" gorm:"column:subtitle"\`` -> 执行当前语句，参与该文件整体逻辑。
- 39: `}` -> 结束当前语句或代码块。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `type Location struct {` -> 定义类型或类结构，约束数据与行为。
- 42: `City       string  \`json:"city"\`` -> 执行当前语句，参与该文件整体逻辑。
- 43: `Country    string  \`json:"country"\`` -> 执行当前语句，参与该文件整体逻辑。
- 44: `Latitude   float64 \`json:"latitude"\`` -> 执行当前语句，参与该文件整体逻辑。
- 45: `Longitude  float64 \`json:"longitude"\`` -> 执行当前语句，参与该文件整体逻辑。
- 46: `}` -> 结束当前语句或代码块。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `type ProductMetadata struct {` -> 定义类型或类结构，约束数据与行为。
- 49: `// Flight specific` -> 注释行，用于解释设计意图或使用说明。
- 50: `Airline         string  \`json:"airline"\`` -> 执行当前语句，参与该文件整体逻辑。
- 51: `FlightNumber    string  \`json:"flight_number"\`` -> 执行当前语句，参与该文件整体逻辑。
- 52: `DepartureAirport string \`json:"departure_airport"\`` -> 执行当前语句，参与该文件整体逻辑。
- 53: `ArrivalAirport   string \`json:"arrival_airport"\`` -> 执行当前语句，参与该文件整体逻辑。
- 54: `DepartureTime   string \`json:"departure_time"\`` -> 执行当前语句，参与该文件整体逻辑。
- 55: `ArrivalTime     string \`json:"arrival_time"\`` -> 执行当前语句，参与该文件整体逻辑。
- 56: `Duration        string \`json:"duration"\`` -> 执行当前语句，参与该文件整体逻辑。
- 57: `(空行)` -> 空行，用于提升代码结构可读性。
- 58: `// Hotel specific` -> 注释行，用于解释设计意图或使用说明。
- 59: `StarRating      int      \`json:"star_rating"\`` -> 执行当前语句，参与该文件整体逻辑。
- 60: `Amenities       []string \`json:"amenities"\`` -> 执行当前语句，参与该文件整体逻辑。
- 61: `RoomType        string   \`json:"room_type"\`` -> 执行当前语句，参与该文件整体逻辑。
- 62: `CheckInTime     string   \`json:"check_in_time"\`` -> 执行当前语句，参与该文件整体逻辑。
- 63: `CheckOutTime    string   \`json:"check_out_time"\`` -> 执行当前语句，参与该文件整体逻辑。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `// Ticket specific` -> 注释行，用于解释设计意图或使用说明。
- 66: `AttractionName  string \`json:"attraction_name"\`` -> 执行当前语句，参与该文件整体逻辑。
- 67: `OpeningHours   string \`json:"opening_hours"\`` -> 执行当前语句，参与该文件整体逻辑。
- 68: `ValidDays       int    \`json:"valid_days"\`` -> 执行当前语句，参与该文件整体逻辑。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `// Experience specific` -> 注释行，用于解释设计意图或使用说明。
- 71: `Duration        string \`json:"duration"\`` -> 执行当前语句，参与该文件整体逻辑。
- 72: `Difficulty      string \`json:"difficulty"\`` -> 执行当前语句，参与该文件整体逻辑。
- 73: `GroupSizeMin    int    \`json:"group_size_min"\`` -> 执行当前语句，参与该文件整体逻辑。
- 74: `GroupSizeMax    int    \`json:"group_size_max"\`` -> 执行当前语句，参与该文件整体逻辑。
- 75: `Includes        []string \`json:"includes"\`` -> 执行当前语句，参与该文件整体逻辑。
- 76: `}` -> 结束当前语句或代码块。
- 77: `(空行)` -> 空行，用于提升代码结构可读性。
- 78: `type SearchFilters struct {` -> 定义类型或类结构，约束数据与行为。
- 79: `Destination    *string  \`json:"destination"\`` -> 执行当前语句，参与该文件整体逻辑。
- 80: `StartDate      *string  \`json:"start_date"\`` -> 执行当前语句，参与该文件整体逻辑。
- 81: `EndDate        *string  \`json:"end_date"\`` -> 执行当前语句，参与该文件整体逻辑。
- 82: `BudgetMin      *float64 \`json:"budget_min"\`` -> 执行当前语句，参与该文件整体逻辑。
- 83: `BudgetMax      *float64 \`json:"budget_max"\`` -> 执行当前语句，参与该文件整体逻辑。
- 84: `TravelStyle    *string  \`json:"travel_style"\`` -> 执行当前语句，参与该文件整体逻辑。
- 85: `GroupSize      *int     \`json:"group_size"\`` -> 执行当前语句，参与该文件整体逻辑。
- 86: `Tags           []string \`json:"tags"\`` -> 执行当前语句，参与该文件整体逻辑。
- 87: `RatingMin      *float64 \`json:"rating_min"\`` -> 执行当前语句，参与该文件整体逻辑。
- 88: `Type           *string  \`json:"type"\`` -> 执行当前语句，参与该文件整体逻辑。
- 89: `Limit          int      \`json:"limit"\`` -> 执行当前语句，参与该文件整体逻辑。
- 90: `Offset         int      \`json:"offset"\`` -> 执行当前语句，参与该文件整体逻辑。
- 91: `}` -> 结束当前语句或代码块。
- 92: `(空行)` -> 空行，用于提升代码结构可读性。
- 93: `type CreateProductRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 94: `Type         string             \`json:"type" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 95: `Name         string             \`json:"name" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 96: `Description  string             \`json:"description"\`` -> 执行当前语句，参与该文件整体逻辑。
- 97: `Price        float64            \`json:"price" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 98: `OriginalPrice *float64           \`json:"original_price"\`` -> 执行当前语句，参与该文件整体逻辑。
- 99: `Inventory    int                \`json:"inventory" binding:"required"\`` -> 执行当前语句，参与该文件整体逻辑。
- 100: `Tags         []string           \`json:"tags"\`` -> 执行当前语句，参与该文件整体逻辑。
- 101: `Metadata     ProductMetadata    \`json:"metadata"\`` -> 执行当前语句，参与该文件整体逻辑。
- 102: `Status       string             \`json:"status"\`` -> 执行当前语句，参与该文件整体逻辑。
- 103: `Images       []string           \`json:"images"\`` -> 执行当前语句，参与该文件整体逻辑。
- 104: `Location     Location           \`json:"location"\`` -> 执行当前语句，参与该文件整体逻辑。
- 105: `}` -> 结束当前语句或代码块。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `type UpdateProductRequest struct {` -> 定义类型或类结构，约束数据与行为。
- 108: `Type         *string            \`json:"type"\`` -> 执行当前语句，参与该文件整体逻辑。
- 109: `Name         *string            \`json:"name"\`` -> 执行当前语句，参与该文件整体逻辑。
- 110: `Description  *string            \`json:"description"\`` -> 执行当前语句，参与该文件整体逻辑。
- 111: `Price        *float64           \`json:"price"\`` -> 执行当前语句，参与该文件整体逻辑。
- 112: `OriginalPrice *float64           \`json:"original_price"\`` -> 执行当前语句，参与该文件整体逻辑。
- 113: `Inventory    *int               \`json:"inventory"\`` -> 执行当前语句，参与该文件整体逻辑。
- 114: `Tags         []string           \`json:"tags"\`` -> 执行当前语句，参与该文件整体逻辑。
- 115: `Metadata     *ProductMetadata   \`json:"metadata"\`` -> 执行当前语句，参与该文件整体逻辑。
- 116: `Status       *string            \`json:"status"\`` -> 执行当前语句，参与该文件整体逻辑。
- 117: `Images       []string           \`json:"images"\`` -> 执行当前语句，参与该文件整体逻辑。
- 118: `Location     *Location         \`json:"location"\`` -> 执行当前语句，参与该文件整体逻辑。
- 119: `}` -> 结束当前语句或代码块。
- 120: `(空行)` -> 空行，用于提升代码结构可读性。
- 121: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {` -> 定义函数或方法，实现具体业务逻辑。
- 122: `return &Service{` -> 返回结果或提前结束当前流程。
- 123: `db:     db,` -> 执行当前语句，参与该文件整体逻辑。
- 124: `logger: logger,` -> 执行当前语句，参与该文件整体逻辑。
- 125: `}` -> 结束当前语句或代码块。
- 126: `}` -> 结束当前语句或代码块。
- 127: `(空行)` -> 空行，用于提升代码结构可读性。
- 128: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {` -> 定义函数或方法，实现具体业务逻辑。
- 129: `products := router.Group("/products")` -> 执行当前语句，参与该文件整体逻辑。
- 130: `{` -> 执行当前语句，参与该文件整体逻辑。
- 131: `// Search products` -> 注释行，用于解释设计意图或使用说明。
- 132: `products.GET("/search", s.SearchProducts)` -> 执行当前语句，参与该文件整体逻辑。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `// CRUD operations` -> 注释行，用于解释设计意图或使用说明。
- 135: `products.POST("", s.CreateProduct)` -> 执行当前语句，参与该文件整体逻辑。
- 136: `products.GET("", s.ListProducts)` -> 执行当前语句，参与该文件整体逻辑。
- 137: `products.GET("/:id", s.GetProduct)` -> 执行当前语句，参与该文件整体逻辑。
- 138: `products.PUT("/:id", s.UpdateProduct)` -> 执行当前语句，参与该文件整体逻辑。
- 139: `products.DELETE("/:id", s.DeleteProduct)` -> 执行当前语句，参与该文件整体逻辑。
- 140: `(空行)` -> 空行，用于提升代码结构可读性。
- 141: `// Specific endpoints` -> 注释行，用于解释设计意图或使用说明。
- 142: `products.GET("/popular", s.GetPopularProducts)` -> 执行当前语句，参与该文件整体逻辑。
- 143: `products.GET("/recommendations/:user_id", s.GetRecommendations)` -> 执行当前语句，参与该文件整体逻辑。
- 144: `products.POST("/:id/increment-views", s.IncrementViews)` -> 执行当前语句，参与该文件整体逻辑。
- 145: `}` -> 结束当前语句或代码块。
- 146: `}` -> 结束当前语句或代码块。
- 147: `(空行)` -> 空行，用于提升代码结构可读性。
- 148: `func (s *Service) SearchProducts(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 149: `var filters SearchFilters` -> 声明变量或常量，保存运行时数据。
- 150: `(空行)` -> 空行，用于提升代码结构可读性。
- 151: `// Parse query parameters` -> 注释行，用于解释设计意图或使用说明。
- 152: `if destination := c.Query("destination"); destination != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 153: `filters.Destination = &destination` -> 执行当前语句，参与该文件整体逻辑。
- 154: `}` -> 结束当前语句或代码块。
- 155: `if startDate := c.Query("start_date"); startDate != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 156: `filters.StartDate = &startDate` -> 执行当前语句，参与该文件整体逻辑。
- 157: `}` -> 结束当前语句或代码块。
- 158: `if endDate := c.Query("end_date"); endDate != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 159: `filters.EndDate = &endDate` -> 执行当前语句，参与该文件整体逻辑。
- 160: `}` -> 结束当前语句或代码块。
- 161: `if budgetMin := c.Query("budget_min"); budgetMin != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 162: `if min, err := fmt.Sscanf(budgetMin, "%f", &filters.BudgetMin); err == nil && min == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 163: `// BudgetMin is set` -> 注释行，用于解释设计意图或使用说明。
- 164: `}` -> 结束当前语句或代码块。
- 165: `}` -> 结束当前语句或代码块。
- 166: `if budgetMax := c.Query("budget_max"); budgetMax != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 167: `if max, err := fmt.Sscanf(budgetMax, "%f", &filters.BudgetMax); err == nil && max == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 168: `// BudgetMax is set` -> 注释行，用于解释设计意图或使用说明。
- 169: `}` -> 结束当前语句或代码块。
- 170: `}` -> 结束当前语句或代码块。
- 171: `if travelStyle := c.Query("travel_style"); travelStyle != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 172: `filters.TravelStyle = &travelStyle` -> 执行当前语句，参与该文件整体逻辑。
- 173: `}` -> 结束当前语句或代码块。
- 174: `if groupSize := c.Query("group_size"); groupSize != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 175: `if size, err := fmt.Sscanf(groupSize, "%d", &filters.GroupSize); err == nil && size == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 176: `// GroupSize is set` -> 注释行，用于解释设计意图或使用说明。
- 177: `}` -> 结束当前语句或代码块。
- 178: `}` -> 结束当前语句或代码块。
- 179: `if ratingMin := c.Query("rating_min"); ratingMin != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 180: `if rating, err := fmt.Sscanf(ratingMin, "%f", &filters.RatingMin); err == nil && rating == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 181: `// RatingMin is set` -> 注释行，用于解释设计意图或使用说明。
- 182: `}` -> 结束当前语句或代码块。
- 183: `}` -> 结束当前语句或代码块。
- 184: `if productType := c.Query("type"); productType != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 185: `filters.Type = &productType` -> 执行当前语句，参与该文件整体逻辑。
- 186: `}` -> 结束当前语句或代码块。
- 187: `if limit := c.DefaultQuery("limit", "20"); limit != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 188: `if l, err := fmt.Sscanf(limit, "%d", &filters.Limit); err == nil && l == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 189: `// Limit is set` -> 注释行，用于解释设计意图或使用说明。
- 190: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 191: `filters.Limit = 20` -> 执行当前语句，参与该文件整体逻辑。
- 192: `}` -> 结束当前语句或代码块。
- 193: `}` -> 结束当前语句或代码块。
- 194: `if offset := c.DefaultQuery("offset", "0"); offset != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 195: `if o, err := fmt.Sscanf(offset, "%d", &filters.Offset); err == nil && o == 1 {` -> 条件判断分支，根据场景执行不同逻辑。
- 196: `// Offset is set` -> 注释行，用于解释设计意图或使用说明。
- 197: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 198: `filters.Offset = 0` -> 执行当前语句，参与该文件整体逻辑。
- 199: `}` -> 结束当前语句或代码块。
- 200: `}` -> 结束当前语句或代码块。
- 201: `(空行)` -> 空行，用于提升代码结构可读性。
- 202: `var products []Product` -> 声明变量或常量，保存运行时数据。
- 203: `query := s.db.Model(&Product{})` -> 执行当前语句，参与该文件整体逻辑。
- 204: `(空行)` -> 空行，用于提升代码结构可读性。
- 205: `// Apply filters` -> 注释行，用于解释设计意图或使用说明。
- 206: `if filters.Destination != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 207: `query = query.Where("location->>'city' = ?", *filters.Destination)` -> 执行当前语句，参与该文件整体逻辑。
- 208: `}` -> 结束当前语句或代码块。
- 209: `if filters.Type != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 210: `query = query.Where("type = ?", *filters.Type)` -> 执行当前语句，参与该文件整体逻辑。
- 211: `}` -> 结束当前语句或代码块。
- 212: `if filters.BudgetMin != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 213: `query = query.Where("price >= ?", *filters.BudgetMin)` -> 执行当前语句，参与该文件整体逻辑。
- 214: `}` -> 结束当前语句或代码块。
- 215: `if filters.BudgetMax != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 216: `query = query.Where("price <= ?", *filters.BudgetMax)` -> 执行当前语句，参与该文件整体逻辑。
- 217: `}` -> 结束当前语句或代码块。
- 218: `if filters.RatingMin != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 219: `query = query.Where("rating >= ?", *filters.RatingMin)` -> 执行当前语句，参与该文件整体逻辑。
- 220: `}` -> 结束当前语句或代码块。
- 221: `if len(filters.Tags) > 0 {` -> 条件判断分支，根据场景执行不同逻辑。
- 222: `// This is a simplified tag search - in production you'd want more complex logic` -> 注释行，用于解释设计意图或使用说明。
- 223: `query = query.Where("tags @> ?", fmt.Sprintf("[%s]", strings.Join(filters.Tags, ",")))` -> 执行当前语句，参与该文件整体逻辑。
- 224: `}` -> 结束当前语句或代码块。
- 225: `if filters.Status != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 226: `query = query.Where("status = ?", *filters.Status)` -> 执行当前语句，参与该文件整体逻辑。
- 227: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 228: `query = query.Where("status = ?", "active")` -> 执行当前语句，参与该文件整体逻辑。
- 229: `}` -> 结束当前语句或代码块。
- 230: `(空行)` -> 空行，用于提升代码结构可读性。
- 231: `// Apply pagination` -> 注释行，用于解释设计意图或使用说明。
- 232: `query = query.Offset(filters.Offset).Limit(filters.Limit)` -> 执行当前语句，参与该文件整体逻辑。
- 233: `(空行)` -> 空行，用于提升代码结构可读性。
- 234: `// Order by rating and review count` -> 注释行，用于解释设计意图或使用说明。
- 235: `query = query.Order("rating DESC, review_count DESC")` -> 执行当前语句，参与该文件整体逻辑。
- 236: `(空行)` -> 空行，用于提升代码结构可读性。
- 237: `if err := query.Find(&products).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 238: `s.logger.WithError(err).Error("Failed to search products")` -> 执行当前语句，参与该文件整体逻辑。
- 239: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to search products"})` -> 构造并返回接口响应数据。
- 240: `return` -> 返回结果或提前结束当前流程。
- 241: `}` -> 结束当前语句或代码块。
- 242: `(空行)` -> 空行，用于提升代码结构可读性。
- 243: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 244: `"products": products,` -> 执行当前语句，参与该文件整体逻辑。
- 245: `"total":    len(products),` -> 执行当前语句，参与该文件整体逻辑。
- 246: `"filters": filters,` -> 执行当前语句，参与该文件整体逻辑。
- 247: `})` -> 执行当前语句，参与该文件整体逻辑。
- 248: `}` -> 结束当前语句或代码块。
- 249: `(空行)` -> 空行，用于提升代码结构可读性。
- 250: `func (s *Service) CreateProduct(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 251: `var req CreateProductRequest` -> 声明变量或常量，保存运行时数据。
- 252: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 253: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 254: `return` -> 返回结果或提前结束当前流程。
- 255: `}` -> 结束当前语句或代码块。
- 256: `(空行)` -> 空行，用于提升代码结构可读性。
- 257: `product := Product{` -> 执行当前语句，参与该文件整体逻辑。
- 258: `Type:         req.Type,` -> 执行当前语句，参与该文件整体逻辑。
- 259: `Name:         req.Name,` -> 执行当前语句，参与该文件整体逻辑。
- 260: `Description:  req.Description,` -> 执行当前语句，参与该文件整体逻辑。
- 261: `Price:        req.Price,` -> 执行当前语句，参与该文件整体逻辑。
- 262: `OriginalPrice: req.OriginalPrice,` -> 执行当前语句，参与该文件整体逻辑。
- 263: `Inventory:    req.Inventory,` -> 执行当前语句，参与该文件整体逻辑。
- 264: `Tags:         req.Tags,` -> 执行当前语句，参与该文件整体逻辑。
- 265: `Metadata:     req.Metadata,` -> 执行当前语句，参与该文件整体逻辑。
- 266: `Status:       req.Status,` -> 执行当前语句，参与该文件整体逻辑。
- 267: `Images:       req.Images,` -> 执行当前语句，参与该文件整体逻辑。
- 268: `Location:     req.Location,` -> 执行当前语句，参与该文件整体逻辑。
- 269: `Rating:       0,` -> 执行当前语句，参与该文件整体逻辑。
- 270: `ReviewCount:  0,` -> 执行当前语句，参与该文件整体逻辑。
- 271: `CreatedAt:    time.Now(),` -> 执行当前语句，参与该文件整体逻辑。
- 272: `UpdatedAt:    time.Now(),` -> 执行当前语句，参与该文件整体逻辑。
- 273: `}` -> 结束当前语句或代码块。
- 274: `(空行)` -> 空行，用于提升代码结构可读性。
- 275: `if err := s.db.Create(&product).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 276: `s.logger.WithError(err).Error("Failed to create product")` -> 执行当前语句，参与该文件整体逻辑。
- 277: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create product"})` -> 构造并返回接口响应数据。
- 278: `return` -> 返回结果或提前结束当前流程。
- 279: `}` -> 结束当前语句或代码块。
- 280: `(空行)` -> 空行，用于提升代码结构可读性。
- 281: `s.logger.WithField("product_id", product.ID).Info("Product created successfully")` -> 执行当前语句，参与该文件整体逻辑。
- 282: `c.JSON(http.StatusCreated, gin.H{` -> 构造并返回接口响应数据。
- 283: `"message": "Product created successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 284: `"product": product,` -> 执行当前语句，参与该文件整体逻辑。
- 285: `})` -> 执行当前语句，参与该文件整体逻辑。
- 286: `}` -> 结束当前语句或代码块。
- 287: `(空行)` -> 空行，用于提升代码结构可读性。
- 288: `func (s *Service) ListProducts(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 289: `var products []Product` -> 声明变量或常量，保存运行时数据。
- 290: `query := s.db.Model(&Product{}).Where("status = ?", "active")` -> 执行当前语句，参与该文件整体逻辑。
- 291: `(空行)` -> 空行，用于提升代码结构可读性。
- 292: `if err := query.Find(&products).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 293: `s.logger.WithError(err).Error("Failed to list products")` -> 执行当前语句，参与该文件整体逻辑。
- 294: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to list products"})` -> 构造并返回接口响应数据。
- 295: `return` -> 返回结果或提前结束当前流程。
- 296: `}` -> 结束当前语句或代码块。
- 297: `(空行)` -> 空行，用于提升代码结构可读性。
- 298: `// 填充Images和Location（兼容旧数据）` -> 注释行，用于解释设计意图或使用说明。
- 299: `for i := range products {` -> 循环处理集合或重复执行逻辑。
- 300: `// 如果Images为空，用CoverImage填充` -> 注释行，用于解释设计意图或使用说明。
- 301: `if len(products[i].Images) == 0 && products[i].CoverImage != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 302: `products[i].Images = []string{products[i].CoverImage}` -> 执行当前语句，参与该文件整体逻辑。
- 303: `}` -> 结束当前语句或代码块。
- 304: `// 如果Location为空但有Subtitle，尝试提取城市名` -> 注释行，用于解释设计意图或使用说明。
- 305: `if products[i].Location.City == "" && products[i].Subtitle != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 306: `city := strings.TrimSuffix(products[i].Subtitle, "推荐")` -> 执行当前语句，参与该文件整体逻辑。
- 307: `products[i].Location = Location{City: city}` -> 执行当前语句，参与该文件整体逻辑。
- 308: `}` -> 结束当前语句或代码块。
- 309: `}` -> 结束当前语句或代码块。
- 310: `(空行)` -> 空行，用于提升代码结构可读性。
- 311: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 312: `"products": products,` -> 执行当前语句，参与该文件整体逻辑。
- 313: `"total":    len(products),` -> 执行当前语句，参与该文件整体逻辑。
- 314: `})` -> 执行当前语句，参与该文件整体逻辑。
- 315: `}` -> 结束当前语句或代码块。
- 316: `(空行)` -> 空行，用于提升代码结构可读性。
- 317: `func (s *Service) GetProduct(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 318: `id := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 319: `(空行)` -> 空行，用于提升代码结构可读性。
- 320: `var product Product` -> 声明变量或常量，保存运行时数据。
- 321: `if err := s.db.First(&product, id).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 322: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})` -> 构造并返回接口响应数据。
- 323: `return` -> 返回结果或提前结束当前流程。
- 324: `}` -> 结束当前语句或代码块。
- 325: `(空行)` -> 空行，用于提升代码结构可读性。
- 326: `// 填充Images（兼容旧数据）` -> 注释行，用于解释设计意图或使用说明。
- 327: `if len(product.Images) == 0 && product.CoverImage != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 328: `product.Images = []string{product.CoverImage}` -> 执行当前语句，参与该文件整体逻辑。
- 329: `}` -> 结束当前语句或代码块。
- 330: `// 填充Location` -> 注释行，用于解释设计意图或使用说明。
- 331: `if product.Location.City == "" && product.Subtitle != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 332: `city := strings.TrimSuffix(product.Subtitle, "推荐")` -> 执行当前语句，参与该文件整体逻辑。
- 333: `product.Location = Location{City: city}` -> 执行当前语句，参与该文件整体逻辑。
- 334: `}` -> 结束当前语句或代码块。
- 335: `(空行)` -> 空行，用于提升代码结构可读性。
- 336: `c.JSON(http.StatusOK, product)` -> 构造并返回接口响应数据。
- 337: `}` -> 结束当前语句或代码块。
- 338: `(空行)` -> 空行，用于提升代码结构可读性。
- 339: `func (s *Service) UpdateProduct(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 340: `id := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 341: `(空行)` -> 空行，用于提升代码结构可读性。
- 342: `var req UpdateProductRequest` -> 声明变量或常量，保存运行时数据。
- 343: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 344: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 构造并返回接口响应数据。
- 345: `return` -> 返回结果或提前结束当前流程。
- 346: `}` -> 结束当前语句或代码块。
- 347: `(空行)` -> 空行，用于提升代码结构可读性。
- 348: `var product Product` -> 声明变量或常量，保存运行时数据。
- 349: `if err := s.db.First(&product, id).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 350: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})` -> 构造并返回接口响应数据。
- 351: `return` -> 返回结果或提前结束当前流程。
- 352: `}` -> 结束当前语句或代码块。
- 353: `(空行)` -> 空行，用于提升代码结构可读性。
- 354: `// Update fields` -> 注释行，用于解释设计意图或使用说明。
- 355: `if req.Type != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 356: `product.Type = *req.Type` -> 执行当前语句，参与该文件整体逻辑。
- 357: `}` -> 结束当前语句或代码块。
- 358: `if req.Name != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 359: `product.Name = *req.Name` -> 执行当前语句，参与该文件整体逻辑。
- 360: `}` -> 结束当前语句或代码块。
- 361: `if req.Description != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 362: `product.Description = *req.Description` -> 执行当前语句，参与该文件整体逻辑。
- 363: `}` -> 结束当前语句或代码块。
- 364: `if req.Price != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 365: `product.Price = *req.Price` -> 执行当前语句，参与该文件整体逻辑。
- 366: `}` -> 结束当前语句或代码块。
- 367: `if req.OriginalPrice != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 368: `product.OriginalPrice = req.OriginalPrice` -> 执行当前语句，参与该文件整体逻辑。
- 369: `}` -> 结束当前语句或代码块。
- 370: `if req.Inventory != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 371: `product.Inventory = *req.Inventory` -> 执行当前语句，参与该文件整体逻辑。
- 372: `}` -> 结束当前语句或代码块。
- 373: `if len(req.Tags) > 0 {` -> 条件判断分支，根据场景执行不同逻辑。
- 374: `product.Tags = req.Tags` -> 执行当前语句，参与该文件整体逻辑。
- 375: `}` -> 结束当前语句或代码块。
- 376: `if req.Metadata != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 377: `product.Metadata = *req.Metadata` -> 执行当前语句，参与该文件整体逻辑。
- 378: `}` -> 结束当前语句或代码块。
- 379: `if req.Status != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 380: `product.Status = *req.Status` -> 执行当前语句，参与该文件整体逻辑。
- 381: `}` -> 结束当前语句或代码块。
- 382: `if len(req.Images) > 0 {` -> 条件判断分支，根据场景执行不同逻辑。
- 383: `product.Images = req.Images` -> 执行当前语句，参与该文件整体逻辑。
- 384: `}` -> 结束当前语句或代码块。
- 385: `if req.Location != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 386: `product.Location = *req.Location` -> 执行当前语句，参与该文件整体逻辑。
- 387: `}` -> 结束当前语句或代码块。
- 388: `(空行)` -> 空行，用于提升代码结构可读性。
- 389: `product.UpdatedAt = time.Now()` -> 执行当前语句，参与该文件整体逻辑。
- 390: `(空行)` -> 空行，用于提升代码结构可读性。
- 391: `if err := s.db.Save(&product).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 392: `s.logger.WithError(err).Error("Failed to update product")` -> 执行当前语句，参与该文件整体逻辑。
- 393: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update product"})` -> 构造并返回接口响应数据。
- 394: `return` -> 返回结果或提前结束当前流程。
- 395: `}` -> 结束当前语句或代码块。
- 396: `(空行)` -> 空行，用于提升代码结构可读性。
- 397: `s.logger.WithField("product_id", product.ID).Info("Product updated successfully")` -> 执行当前语句，参与该文件整体逻辑。
- 398: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 399: `"message": "Product updated successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 400: `"product": product,` -> 执行当前语句，参与该文件整体逻辑。
- 401: `})` -> 执行当前语句，参与该文件整体逻辑。
- 402: `}` -> 结束当前语句或代码块。
- 403: `(空行)` -> 空行，用于提升代码结构可读性。
- 404: `func (s *Service) DeleteProduct(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 405: `id := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 406: `(空行)` -> 空行，用于提升代码结构可读性。
- 407: `var product Product` -> 声明变量或常量，保存运行时数据。
- 408: `if err := s.db.First(&product, id).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 409: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})` -> 构造并返回接口响应数据。
- 410: `return` -> 返回结果或提前结束当前流程。
- 411: `}` -> 结束当前语句或代码块。
- 412: `(空行)` -> 空行，用于提升代码结构可读性。
- 413: `// Soft delete by setting status to inactive` -> 注释行，用于解释设计意图或使用说明。
- 414: `product.Status = "inactive"` -> 执行当前语句，参与该文件整体逻辑。
- 415: `product.UpdatedAt = time.Now()` -> 执行当前语句，参与该文件整体逻辑。
- 416: `(空行)` -> 空行，用于提升代码结构可读性。
- 417: `if err := s.db.Save(&product).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 418: `s.logger.WithError(err).Error("Failed to delete product")` -> 执行当前语句，参与该文件整体逻辑。
- 419: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to delete product"})` -> 构造并返回接口响应数据。
- 420: `return` -> 返回结果或提前结束当前流程。
- 421: `}` -> 结束当前语句或代码块。
- 422: `(空行)` -> 空行，用于提升代码结构可读性。
- 423: `s.logger.WithField("product_id", product.ID).Info("Product deleted successfully")` -> 执行当前语句，参与该文件整体逻辑。
- 424: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 425: `"message": "Product deleted successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 426: `})` -> 执行当前语句，参与该文件整体逻辑。
- 427: `}` -> 结束当前语句或代码块。
- 428: `(空行)` -> 空行，用于提升代码结构可读性。
- 429: `func (s *Service) GetPopularProducts(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 430: `var products []Product` -> 声明变量或常量，保存运行时数据。
- 431: `limit := 20` -> 执行当前语句，参与该文件整体逻辑。
- 432: `(空行)` -> 空行，用于提升代码结构可读性。
- 433: `if l := c.DefaultQuery("limit", "20"); l != "" {` -> 条件判断分支，根据场景执行不同逻辑。
- 434: `fmt.Sscanf(l, "%d", &limit)` -> 执行当前语句，参与该文件整体逻辑。
- 435: `}` -> 结束当前语句或代码块。
- 436: `(空行)` -> 空行，用于提升代码结构可读性。
- 437: `query := s.db.Model(&Product{}).` -> 执行当前语句，参与该文件整体逻辑。
- 438: `Where("status = ?", "active").` -> 执行当前语句，参与该文件整体逻辑。
- 439: `Order("rating DESC, review_count DESC").` -> 执行当前语句，参与该文件整体逻辑。
- 440: `Limit(limit)` -> 执行当前语句，参与该文件整体逻辑。
- 441: `(空行)` -> 空行，用于提升代码结构可读性。
- 442: `if err := query.Find(&products).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 443: `s.logger.WithError(err).Error("Failed to get popular products")` -> 执行当前语句，参与该文件整体逻辑。
- 444: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get popular products"})` -> 构造并返回接口响应数据。
- 445: `return` -> 返回结果或提前结束当前流程。
- 446: `}` -> 结束当前语句或代码块。
- 447: `(空行)` -> 空行，用于提升代码结构可读性。
- 448: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 449: `"products": products,` -> 执行当前语句，参与该文件整体逻辑。
- 450: `"total":    len(products),` -> 执行当前语句，参与该文件整体逻辑。
- 451: `})` -> 执行当前语句，参与该文件整体逻辑。
- 452: `}` -> 结束当前语句或代码块。
- 453: `(空行)` -> 空行，用于提升代码结构可读性。
- 454: `func (s *Service) GetRecommendations(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 455: `userID := c.Param("user_id")` -> 执行当前语句，参与该文件整体逻辑。
- 456: `(空行)` -> 空行，用于提升代码结构可读性。
- 457: `// TODO: Implement recommendation logic based on user preferences and behavior` -> 注释行，用于解释设计意图或使用说明。
- 458: `// For now, return popular products` -> 注释行，用于解释设计意图或使用说明。
- 459: `var products []Product` -> 声明变量或常量，保存运行时数据。
- 460: `(空行)` -> 空行，用于提升代码结构可读性。
- 461: `query := s.db.Model(&Product{}).` -> 执行当前语句，参与该文件整体逻辑。
- 462: `Where("status = ?", "active").` -> 执行当前语句，参与该文件整体逻辑。
- 463: `Order("rating DESC, review_count DESC").` -> 执行当前语句，参与该文件整体逻辑。
- 464: `Limit(10)` -> 执行当前语句，参与该文件整体逻辑。
- 465: `(空行)` -> 空行，用于提升代码结构可读性。
- 466: `if err := query.Find(&products).Error; err != nil {` -> 条件判断分支，根据场景执行不同逻辑。
- 467: `s.logger.WithError(err).Error("Failed to get recommendations")` -> 执行当前语句，参与该文件整体逻辑。
- 468: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get recommendations"})` -> 构造并返回接口响应数据。
- 469: `return` -> 返回结果或提前结束当前流程。
- 470: `}` -> 结束当前语句或代码块。
- 471: `(空行)` -> 空行，用于提升代码结构可读性。
- 472: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 473: `"products": products,` -> 执行当前语句，参与该文件整体逻辑。
- 474: `"user_id":   userID,` -> 执行当前语句，参与该文件整体逻辑。
- 475: `})` -> 执行当前语句，参与该文件整体逻辑。
- 476: `}` -> 结束当前语句或代码块。
- 477: `(空行)` -> 空行，用于提升代码结构可读性。
- 478: `func (s *Service) IncrementViews(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 479: `id := c.Param("id")` -> 执行当前语句，参与该文件整体逻辑。
- 480: `(空行)` -> 空行，用于提升代码结构可读性。
- 481: `// TODO: Implement view counting logic` -> 注释行，用于解释设计意图或使用说明。
- 482: `// For now, just return success` -> 注释行，用于解释设计意图或使用说明。
- 483: `c.JSON(http.StatusOK, gin.H{` -> 构造并返回接口响应数据。
- 484: `"message": "Views incremented successfully",` -> 执行当前语句，参与该文件整体逻辑。
- 485: `"product_id": id,` -> 执行当前语句，参与该文件整体逻辑。
- 486: `})` -> 执行当前语句，参与该文件整体逻辑。
- 487: `}` -> 结束当前语句或代码块。
