# `frontend/user-web/src/lib/media.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* ============================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 媒体资源处理工具` -> 注释行，用于解释设计意图或使用说明。
- 4: `* ============================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能说明】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 提供图片URL处理` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 处理占位图逻辑` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 统一媒体资源访问路径` -> 注释行，用于解释设计意图或使用说明。
- 10: `*/` -> 注释行，用于解释设计意图或使用说明。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `/** 占位图路径 - 当图片加载失败时显示的默认图片 */` -> 注释行，用于解释设计意图或使用说明。
- 13: `export const FALLBACK_MEDIA_PATH = 'scenic_images/__auto__/placeholder.png'` -> 导出当前声明，供其他模块复用。
- 14: `(空行)` -> 空行，用于提升代码结构可读性。
- 15: `/**` -> 注释行，用于解释设计意图或使用说明。
- 16: `* 生成媒体API访问URL` -> 注释行，用于解释设计意图或使用说明。
- 17: `*` -> 注释行，用于解释设计意图或使用说明。
- 18: `* @param path - 媒体文件路径` -> 注释行，用于解释设计意图或使用说明。
- 19: `* @returns 完整的API访问URL` -> 注释行，用于解释设计意图或使用说明。
- 20: `*` -> 注释行，用于解释设计意图或使用说明。
- 21: `* @example` -> 注释行，用于解释设计意图或使用说明。
- 22: `* apiMediaUrl('scenic_images/beijing.jpg')` -> 注释行，用于解释设计意图或使用说明。
- 23: `* // 返回: '/api/media?path=scenic_images%2Fbeijing.jpg'` -> 注释行，用于解释设计意图或使用说明。
- 24: `*/` -> 注释行，用于解释设计意图或使用说明。
- 25: `export function apiMediaUrl(path: string) {` -> 导出当前声明，供其他模块复用。
- 26: `// 始终使用完整 URL，避免 Next.js SSR 和客户端 hydration 不一致` -> 注释行，用于解释设计意图或使用说明。
- 27: `const base = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:5001'` -> 声明变量或常量，保存运行时数据。
- 28: `return \`${base}/api/media?path=${encodeURIComponent(path)}\`` -> 返回结果或提前结束当前流程。
- 29: `}` -> 结束当前语句或代码块。
- 30: `(空行)` -> 空行，用于提升代码结构可读性。
- 31: `/**` -> 注释行，用于解释设计意图或使用说明。
- 32: `* 解析封面图片URL` -> 注释行，用于解释设计意图或使用说明。
- 33: `*` -> 注释行，用于解释设计意图或使用说明。
- 34: `* @param coverImage - 封面图片路径（可能为空）` -> 注释行，用于解释设计意图或使用说明。
- 35: `* @returns 可访问的图片URL` -> 注释行，用于解释设计意图或使用说明。
- 36: `*` -> 注释行，用于解释设计意图或使用说明。
- 37: `* @处理逻辑】` -> 注释行，用于解释设计意图或使用说明。
- 38: `* 1. 空值 → 返回占位图` -> 注释行，用于解释设计意图或使用说明。
- 39: `* 2. 外部URL（http/https开头）→ 直接返回` -> 注释行，用于解释设计意图或使用说明。
- 40: `* 3. scenic_images/开头 → 通过API代理` -> 注释行，用于解释设计意图或使用说明。
- 41: `* 4. 其他情况 → 返回占位图` -> 注释行，用于解释设计意图或使用说明。
- 42: `*/` -> 注释行，用于解释设计意图或使用说明。
- 43: `export function resolveCoverSrc(coverImage?: string | null) {` -> 导出当前声明，供其他模块复用。
- 44: `const v = (coverImage || '').trim()` -> 声明变量或常量，保存运行时数据。
- 45: `(空行)` -> 空行，用于提升代码结构可读性。
- 46: `// 空值或空白 → 使用占位图` -> 注释行，用于解释设计意图或使用说明。
- 47: `if (!v) return apiMediaUrl(FALLBACK_MEDIA_PATH)` -> 条件判断分支，根据场景执行不同逻辑。
- 48: `(空行)` -> 空行，用于提升代码结构可读性。
- 49: `// 外部URL（如CDN图片）→ 直接返回` -> 注释行，用于解释设计意图或使用说明。
- 50: `if (v.startsWith('http://') || v.startsWith('https://')) return v` -> 条件判断分支，根据场景执行不同逻辑。
- 51: `(空行)` -> 空行，用于提升代码结构可读性。
- 52: `// 本地scenic_images目录 → 通过API代理` -> 注释行，用于解释设计意图或使用说明。
- 53: `if (v.startsWith('scenic_images/')) return apiMediaUrl(v)` -> 条件判断分支，根据场景执行不同逻辑。
- 54: `(空行)` -> 空行，用于提升代码结构可读性。
- 55: `// 其他情况 → 降级到占位图` -> 注释行，用于解释设计意图或使用说明。
- 56: `return apiMediaUrl(FALLBACK_MEDIA_PATH)` -> 返回结果或提前结束当前流程。
- 57: `}` -> 结束当前语句或代码块。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `/**` -> 注释行，用于解释设计意图或使用说明。
- 60: `* 图片加载失败时使用的回调函数` -> 注释行，用于解释设计意图或使用说明。
- 61: `*` -> 注释行，用于解释设计意图或使用说明。
- 62: `* @param e - React的img元素error事件` -> 注释行，用于解释设计意图或使用说明。
- 63: `*` -> 注释行，用于解释设计意图或使用说明。
- 64: `* @description` -> 注释行，用于解释设计意图或使用说明。
- 65: `* 将图片src替换为占位图，防止死链显示` -> 注释行，用于解释设计意图或使用说明。
- 66: `* 如果已经是占位图则不再替换，避免循环` -> 注释行，用于解释设计意图或使用说明。
- 67: `*/` -> 注释行，用于解释设计意图或使用说明。
- 68: `import type { SyntheticEvent } from 'react'` -> 导入依赖模块，供当前文件使用。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `export function onImgErrorUseFallback(e: SyntheticEvent<HTMLImageElement>) {` -> 导出当前声明，供其他模块复用。
- 71: `const img = e.currentTarget` -> 声明变量或常量，保存运行时数据。
- 72: `if (!img) return` -> 条件判断分支，根据场景执行不同逻辑。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `const fallback = apiMediaUrl(FALLBACK_MEDIA_PATH)` -> 声明变量或常量，保存运行时数据。
- 75: `(空行)` -> 空行，用于提升代码结构可读性。
- 76: `// 如果当前src已经是占位图，跳过避免循环` -> 注释行，用于解释设计意图或使用说明。
- 77: `// 使用 encodeURIComponent 后的路径来匹配，因为 URL 是编码的` -> 注释行，用于解释设计意图或使用说明。
- 78: `const encodedFallback = encodeURIComponent(FALLBACK_MEDIA_PATH)` -> 声明变量或常量，保存运行时数据。
- 79: `if (img.src && img.src.includes(encodedFallback)) return` -> 条件判断分支，根据场景执行不同逻辑。
- 80: `(空行)` -> 空行，用于提升代码结构可读性。
- 81: `// 替换为占位图` -> 注释行，用于解释设计意图或使用说明。
- 82: `img.src = fallback` -> 执行当前语句，参与该文件整体逻辑。
- 83: `}` -> 结束当前语句或代码块。
