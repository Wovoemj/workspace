# `monitoring/prometheus.yml` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# Prometheus 配置文件` -> 注释行，用于解释设计意图或使用说明。
- 2: `global:` -> 执行当前语句，参与该文件整体逻辑。
- 3: `scrape_interval: 15s` -> 执行当前语句，参与该文件整体逻辑。
- 4: `evaluation_interval: 15s` -> 执行当前语句，参与该文件整体逻辑。
- 5: `(空行)` -> 空行，用于提升代码结构可读性。
- 6: `# 告警规则文件` -> 注释行，用于解释设计意图或使用说明。
- 7: `rule_files:` -> 执行当前语句，参与该文件整体逻辑。
- 8: `- "alert_rules.yml"` -> 执行当前语句，参与该文件整体逻辑。
- 9: `(空行)` -> 空行，用于提升代码结构可读性。
- 10: `# 告警管理器配置` -> 注释行，用于解释设计意图或使用说明。
- 11: `alerting:` -> 执行当前语句，参与该文件整体逻辑。
- 12: `alertmanagers:` -> 执行当前语句，参与该文件整体逻辑。
- 13: `- static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 14: `- targets:` -> 执行当前语句，参与该文件整体逻辑。
- 15: `- alertmanager:9093` -> 执行当前语句，参与该文件整体逻辑。
- 16: `(空行)` -> 空行，用于提升代码结构可读性。
- 17: `# 抓取配置` -> 注释行，用于解释设计意图或使用说明。
- 18: `scrape_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 19: `# Prometheus 自监控` -> 注释行，用于解释设计意图或使用说明。
- 20: `- job_name: 'prometheus'` -> 执行当前语句，参与该文件整体逻辑。
- 21: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 22: `- targets: ['localhost:9090']` -> 执行当前语句，参与该文件整体逻辑。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `# 应用监控` -> 注释行，用于解释设计意图或使用说明。
- 25: `- job_name: 'travel-assistant'` -> 执行当前语句，参与该文件整体逻辑。
- 26: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 27: `- targets: ['app:5000']` -> 执行当前语句，参与该文件整体逻辑。
- 28: `metrics_path: /metrics` -> 执行当前语句，参与该文件整体逻辑。
- 29: `scrape_interval: 15s` -> 执行当前语句，参与该文件整体逻辑。
- 30: `scrape_timeout: 10s` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `# Nginx 监控` -> 注释行，用于解释设计意图或使用说明。
- 33: `- job_name: 'nginx'` -> 执行当前语句，参与该文件整体逻辑。
- 34: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 35: `- targets: ['nginx:80']` -> 执行当前语句，参与该文件整体逻辑。
- 36: `metrics_path: /nginx_status` -> 执行当前语句，参与该文件整体逻辑。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `# Redis 监控` -> 注释行，用于解释设计意图或使用说明。
- 39: `- job_name: 'redis'` -> 执行当前语句，参与该文件整体逻辑。
- 40: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 41: `- targets: ['redis:6379']` -> 执行当前语句，参与该文件整体逻辑。
- 42: `metrics_path: /metrics` -> 执行当前语句，参与该文件整体逻辑。
- 43: `(空行)` -> 空行，用于提升代码结构可读性。
- 44: `# MySQL 监控` -> 注释行，用于解释设计意图或使用说明。
- 45: `- job_name: 'mysql'` -> 执行当前语句，参与该文件整体逻辑。
- 46: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 47: `- targets: ['db:3306']` -> 执行当前语句，参与该文件整体逻辑。
- 48: `metrics_path: /metrics` -> 执行当前语句，参与该文件整体逻辑。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `# 系统监控` -> 注释行，用于解释设计意图或使用说明。
- 51: `- job_name: 'node-exporter'` -> 执行当前语句，参与该文件整体逻辑。
- 52: `static_configs:` -> 执行当前语句，参与该文件整体逻辑。
- 53: `- targets: ['node-exporter:9100']` -> 执行当前语句，参与该文件整体逻辑。
