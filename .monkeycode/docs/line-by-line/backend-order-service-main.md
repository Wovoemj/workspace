# `backend/order-service/main.go` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `package order` -> 声明当前文件所属的 Go 包。
- 2: `import (` -> 开始导入依赖包列表。
- 3: `"context"` -> 执行当前语句，参与该函数的业务流程。
- 4: `"database/sql"` -> 执行当前语句，参与该函数的业务流程。
- 5: `"encoding/json"` -> 执行当前语句，参与该函数的业务流程。
- 6: `"fmt"` -> 执行当前语句，参与该函数的业务流程。
- 7: `"net/http"` -> 执行当前语句，参与该函数的业务流程。
- 8: `"time"` -> 执行当前语句，参与该函数的业务流程。
- 9: `"github.com/gin-gonic/gin"` -> 执行当前语句，参与该函数的业务流程。
- 10: `"github.com/sirupsen/logrus"` -> 执行当前语句，参与该函数的业务流程。
- 11: `"golang.org/x/crypto/bcrypt"` -> 执行当前语句，参与该函数的业务流程。
- 12: `"gorm.io/gorm"` -> 执行当前语句，参与该函数的业务流程。
- 13: `)` -> 结束当前代码块或导入块。
- 14: `type Service struct {` -> 定义类型结构，用于组织数据或行为。
- 15: `db     *gorm.DB` -> 执行当前语句，参与该函数的业务流程。
- 16: `logger *logrus.Logger` -> 记录日志，便于运行观测与故障排查。
- 17: `}` -> 代码块边界，标记作用域开始或结束。
- 18: `type Order struct {` -> 定义类型结构，用于组织数据或行为。
- 19: `ID           uint      \`json:"id" gorm:"primaryKey"\`` -> 执行当前语句，参与该函数的业务流程。
- 20: `UserID       uint      \`json:"user_id" gorm:"not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 21: `OrderNo      string    \`json:"order_no" gorm:"unique;not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 22: `TotalAmount  float64   \`json:"total_amount" gorm:"not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 23: `Status       string    \`json:"status" gorm:"not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 24: `PaymentMethod string   \`json:"payment_method"\`` -> 执行当前语句，参与该函数的业务流程。
- 25: `PaymentTime  *time.Time \`json:"payment_time"\`` -> 执行当前语句，参与该函数的业务流程。
- 26: `CancelledReason *string \`json:"cancelled_reason"\`` -> 执行当前语句，参与该函数的业务流程。
- 27: `RefundAmount *float64  \`json:"refund_amount"\`` -> 执行当前语句，参与该函数的业务流程。
- 28: `RefundTime   *time.Time \`json:"refund_time"\`` -> 执行当前语句，参与该函数的业务流程。
- 29: `RefundReason *string   \`json:"refund_reason"\`` -> 执行当前语句，参与该函数的业务流程。
- 30: `CreatedAt    time.Time \`json:"created_at"\`` -> 执行当前语句，参与该函数的业务流程。
- 31: `UpdatedAt    time.Time \`json:"updated_at"\`` -> 执行当前语句，参与该函数的业务流程。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `// 关联` -> 注释行，用于说明后续逻辑意图。
- 34: `User         User       \`json:"user" gorm:"foreignKey:UserID"\`` -> 执行当前语句，参与该函数的业务流程。
- 35: `Items        []OrderItem \`json:"items" gorm:"foreignKey:OrderID"\`` -> 执行当前语句，参与该函数的业务流程。
- 36: `}` -> 代码块边界，标记作用域开始或结束。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `type User struct {` -> 定义类型结构，用于组织数据或行为。
- 39: `gorm.Model` -> 执行当前语句，参与该函数的业务流程。
- 40: `Phone         string \`json:"phone" gorm:"unique;not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 41: `Email         string \`json:"email"\`` -> 执行当前语句，参与该函数的业务流程。
- 42: `Nickname      string \`json:"nickname"\`` -> 执行当前语句，参与该函数的业务流程。
- 43: `AvatarURL     string \`json:"avatar_url"\`` -> 执行当前语句，参与该函数的业务流程。
- 44: `MembershipLevel int  \`json:"membership_level"\`` -> 执行当前语句，参与该函数的业务流程。
- 45: `}` -> 代码块边界，标记作用域开始或结束。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `type OrderItem struct {` -> 定义类型结构，用于组织数据或行为。
- 48: `ID           uint    \`json:"id" gorm:"primaryKey"\`` -> 执行当前语句，参与该函数的业务流程。
- 49: `OrderID      uint    \`json:"order_id" gorm:"not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 50: `ProductID    uint    \`json:"product_id" gorm:"not null"\`` -> 执行当前语句，参与该函数的业务流程。
- 51: `ProductName  string  \`json:"product_name"\`` -> 执行当前语句，参与该函数的业务流程。
- 52: `ProductType  string  \`json:"product_type"\`` -> 执行当前语句，参与该函数的业务流程。
- 53: `Quantity     int     \`json:"quantity"\`` -> 执行当前语句，参与该函数的业务流程。
- 54: `UnitPrice    float64 \`json:"unit_price"\`` -> 执行当前语句，参与该函数的业务流程。
- 55: `TotalPrice   float64 \`json:"total_price"\`` -> 执行当前语句，参与该函数的业务流程。
- 56: `BookingDetails json.RawMessage \`json:"booking_details" gorm:"type:json"\`` -> 执行当前语句，参与该函数的业务流程。
- 57: `Status       string  \`json:"status"\`` -> 执行当前语句，参与该函数的业务流程。
- 58: `CreatedAt    time.Time \`json:"created_at"\`` -> 执行当前语句，参与该函数的业务流程。
- 59: `UpdatedAt    time.Time \`json:"updated_at"\`` -> 执行当前语句，参与该函数的业务流程。
- 60: `}` -> 代码块边界，标记作用域开始或结束。
- 61: `(空行)` -> 空行，用于提升代码结构可读性。
- 62: `type CreateOrderRequest struct {` -> 定义类型结构，用于组织数据或行为。
- 63: `UserID       uint                 \`json:"user_id" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 64: `Items        []CreateOrderItem    \`json:"items" binding:"required,min=1"\`` -> 执行当前语句，参与该函数的业务流程。
- 65: `PaymentMethod string              \`json:"payment_method"\`` -> 执行当前语句，参与该函数的业务流程。
- 66: `}` -> 代码块边界，标记作用域开始或结束。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `type CreateOrderItem struct {` -> 定义类型结构，用于组织数据或行为。
- 69: `ProductID    uint                 \`json:"product_id" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 70: `Quantity     int                  \`json:"quantity" binding:"required,min=1"\`` -> 执行当前语句，参与该函数的业务流程。
- 71: `BookingDetails json.RawMessage     \`json:"booking_details"\`` -> 执行当前语句，参与该函数的业务流程。
- 72: `}` -> 代码块边界，标记作用域开始或结束。
- 73: `(空行)` -> 空行，用于提升代码结构可读性。
- 74: `type PaymentRequest struct {` -> 定义类型结构，用于组织数据或行为。
- 75: `OrderID      uint    \`json:"order_id" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 76: `PaymentMethod string \`json:"payment_method" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 77: `}` -> 代码块边界，标记作用域开始或结束。
- 78: `(空行)` -> 空行，用于提升代码结构可读性。
- 79: `type CancelRequest struct {` -> 定义类型结构，用于组织数据或行为。
- 80: `OrderID        uint   \`json:"order_id" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 81: `CancelledReason string \`json:"cancelled_reason" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 82: `}` -> 代码块边界，标记作用域开始或结束。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `type RefundRequest struct {` -> 定义类型结构，用于组织数据或行为。
- 85: `OrderID        uint    \`json:"order_id" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 86: `RefundAmount   float64 \`json:"refund_amount"\`` -> 执行当前语句，参与该函数的业务流程。
- 87: `RefundReason   string  \`json:"refund_reason" binding:"required"\`` -> 执行当前语句，参与该函数的业务流程。
- 88: `}` -> 代码块边界，标记作用域开始或结束。
- 89: `(空行)` -> 空行，用于提升代码结构可读性。
- 90: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {` -> 定义函数或方法，实现具体业务逻辑。
- 91: `return &Service{` -> 返回函数结果或提前结束当前流程。
- 92: `db:     db,` -> 执行当前语句，参与该函数的业务流程。
- 93: `logger: logger,` -> 记录日志，便于运行观测与故障排查。
- 94: `}` -> 代码块边界，标记作用域开始或结束。
- 95: `}` -> 代码块边界，标记作用域开始或结束。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {` -> 定义函数或方法，实现具体业务逻辑。
- 98: `orders := router.Group("/orders")` -> 注册路由或组织接口分组。
- 99: `{` -> 代码块边界，标记作用域开始或结束。
- 100: `orders.POST("", s.CreateOrder)` -> 注册路由或组织接口分组。
- 101: `orders.GET("/user/:user_id", s.GetUserOrders)` -> 注册路由或组织接口分组。
- 102: `orders.GET("/:id", s.GetOrder)` -> 注册路由或组织接口分组。
- 103: `orders.PUT("/:id/pay", s.PayOrder)` -> 注册路由或组织接口分组。
- 104: `orders.PUT("/:id/cancel", s.CancelOrder)` -> 注册路由或组织接口分组。
- 105: `orders.PUT("/:id/refund", s.RefundOrder)` -> 注册路由或组织接口分组。
- 106: `orders.GET("/:id/status", s.GetOrderStatus)` -> 注册路由或组织接口分组。
- 107: `}` -> 代码块边界，标记作用域开始或结束。
- 108: `}` -> 代码块边界，标记作用域开始或结束。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `func (s *Service) CreateOrder(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 111: `var req CreateOrderRequest` -> 声明局部变量，为后续逻辑准备数据。
- 112: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断，根据分支处理不同情况。
- 113: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 向客户端返回 JSON 响应。
- 114: `return` -> 返回函数结果或提前结束当前流程。
- 115: `}` -> 代码块边界，标记作用域开始或结束。
- 116: `(空行)` -> 空行，用于提升代码结构可读性。
- 117: `// 生成订单号` -> 注释行，用于说明后续逻辑意图。
- 118: `orderNo := fmt.Sprintf("TR%s%d", time.Now().Format("20060102"), time.Now().UnixNano()%10000)` -> 获取当前时间，用于时间字段或时序控制。
- 119: `(空行)` -> 空行，用于提升代码结构可读性。
- 120: `// 计算总金额` -> 注释行，用于说明后续逻辑意图。
- 121: `var totalAmount float64` -> 声明局部变量，为后续逻辑准备数据。
- 122: `orderItems := make([]OrderItem, len(req.Items))` -> 执行当前语句，参与该函数的业务流程。
- 123: `(空行)` -> 空行，用于提升代码结构可读性。
- 124: `for i, item := range req.Items {` -> 循环遍历数据集合并执行处理。
- 125: `// 获取产品信息` -> 注释行，用于说明后续逻辑意图。
- 126: `var product struct {` -> 声明局部变量，为后续逻辑准备数据。
- 127: `ID       uint` -> 执行当前语句，参与该函数的业务流程。
- 128: `Name     string` -> 执行当前语句，参与该函数的业务流程。
- 129: `Type     string` -> 执行当前语句，参与该函数的业务流程。
- 130: `Price    float64` -> 执行当前语句，参与该函数的业务流程。
- 131: `Inventory int` -> 执行当前语句，参与该函数的业务流程。
- 132: `}` -> 代码块边界，标记作用域开始或结束。
- 133: `(空行)` -> 空行，用于提升代码结构可读性。
- 134: `if err := s.db.First(&product, item.ProductID).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 135: `c.JSON(http.StatusNotFound, gin.H{"error": fmt.Sprintf("Product %d not found", item.ProductID)})` -> 向客户端返回 JSON 响应。
- 136: `return` -> 返回函数结果或提前结束当前流程。
- 137: `}` -> 代码块边界，标记作用域开始或结束。
- 138: `(空行)` -> 空行，用于提升代码结构可读性。
- 139: `// 检查库存` -> 注释行，用于说明后续逻辑意图。
- 140: `if product.Inventory < item.Quantity {` -> 条件判断，根据分支处理不同情况。
- 141: `c.JSON(http.StatusBadRequest, gin.H{"error": fmt.Sprintf("Product %d inventory not enough", item.ProductID)})` -> 向客户端返回 JSON 响应。
- 142: `return` -> 返回函数结果或提前结束当前流程。
- 143: `}` -> 代码块边界，标记作用域开始或结束。
- 144: `(空行)` -> 空行，用于提升代码结构可读性。
- 145: `// 创建订单项` -> 注释行，用于说明后续逻辑意图。
- 146: `totalPrice := float64(item.Quantity) * product.Price` -> 执行当前语句，参与该函数的业务流程。
- 147: `orderItems[i] = OrderItem{` -> 执行当前语句，参与该函数的业务流程。
- 148: `ProductID:    item.ProductID,` -> 执行当前语句，参与该函数的业务流程。
- 149: `ProductName:  product.Name,` -> 执行当前语句，参与该函数的业务流程。
- 150: `ProductType:  product.Type,` -> 执行当前语句，参与该函数的业务流程。
- 151: `Quantity:     item.Quantity,` -> 执行当前语句，参与该函数的业务流程。
- 152: `UnitPrice:    product.Price,` -> 执行当前语句，参与该函数的业务流程。
- 153: `TotalPrice:   totalPrice,` -> 执行当前语句，参与该函数的业务流程。
- 154: `BookingDetails: item.BookingDetails,` -> 执行当前语句，参与该函数的业务流程。
- 155: `Status:       "pending",` -> 执行当前语句，参与该函数的业务流程。
- 156: `}` -> 代码块边界，标记作用域开始或结束。
- 157: `(空行)` -> 空行，用于提升代码结构可读性。
- 158: `totalAmount += totalPrice` -> 执行当前语句，参与该函数的业务流程。
- 159: `}` -> 代码块边界，标记作用域开始或结束。
- 160: `(空行)` -> 空行，用于提升代码结构可读性。
- 161: `// 创建订单` -> 注释行，用于说明后续逻辑意图。
- 162: `order := Order{` -> 执行当前语句，参与该函数的业务流程。
- 163: `UserID:       req.UserID,` -> 执行当前语句，参与该函数的业务流程。
- 164: `OrderNo:      orderNo,` -> 执行当前语句，参与该函数的业务流程。
- 165: `TotalAmount:  totalAmount,` -> 执行当前语句，参与该函数的业务流程。
- 166: `Status:       "pending",` -> 执行当前语句，参与该函数的业务流程。
- 167: `PaymentMethod: req.PaymentMethod,` -> 执行当前语句，参与该函数的业务流程。
- 168: `CreatedAt:    time.Now(),` -> 获取当前时间，用于时间字段或时序控制。
- 169: `UpdatedAt:    time.Now(),` -> 获取当前时间，用于时间字段或时序控制。
- 170: `}` -> 代码块边界，标记作用域开始或结束。
- 171: `(空行)` -> 空行，用于提升代码结构可读性。
- 172: `// 开始事务` -> 注释行，用于说明后续逻辑意图。
- 173: `tx := s.db.Begin()` -> 执行当前语句，参与该函数的业务流程。
- 174: `(空行)` -> 空行，用于提升代码结构可读性。
- 175: `if err := tx.Create(&order).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 176: `tx.Rollback()` -> 执行当前语句，参与该函数的业务流程。
- 177: `s.logger.WithError(err).Error("Failed to create order")` -> 记录日志，便于运行观测与故障排查。
- 178: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order"})` -> 向客户端返回 JSON 响应。
- 179: `return` -> 返回函数结果或提前结束当前流程。
- 180: `}` -> 代码块边界，标记作用域开始或结束。
- 181: `(空行)` -> 空行，用于提升代码结构可读性。
- 182: `// 创建订单项` -> 注释行，用于说明后续逻辑意图。
- 183: `for i := range orderItems {` -> 循环遍历数据集合并执行处理。
- 184: `orderItems[i].OrderID = order.ID` -> 执行当前语句，参与该函数的业务流程。
- 185: `}` -> 代码块边界，标记作用域开始或结束。
- 186: `(空行)` -> 空行，用于提升代码结构可读性。
- 187: `if err := tx.Create(&orderItems).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 188: `tx.Rollback()` -> 执行当前语句，参与该函数的业务流程。
- 189: `s.logger.WithError(err).Error("Failed to create order items")` -> 记录日志，便于运行观测与故障排查。
- 190: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order items"})` -> 向客户端返回 JSON 响应。
- 191: `return` -> 返回函数结果或提前结束当前流程。
- 192: `}` -> 代码块边界，标记作用域开始或结束。
- 193: `(空行)` -> 空行，用于提升代码结构可读性。
- 194: `// 减少库存` -> 注释行，用于说明后续逻辑意图。
- 195: `for _, item := range orderItems {` -> 循环遍历数据集合并执行处理。
- 196: `var product struct {` -> 声明局部变量，为后续逻辑准备数据。
- 197: `Inventory int` -> 执行当前语句，参与该函数的业务流程。
- 198: `}` -> 代码块边界，标记作用域开始或结束。
- 199: `if err := tx.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory - ?", item.Quantity)).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 200: `tx.Rollback()` -> 执行当前语句，参与该函数的业务流程。
- 201: `s.logger.WithError(err).Error("Failed to update inventory")` -> 记录日志，便于运行观测与故障排查。
- 202: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update inventory"})` -> 向客户端返回 JSON 响应。
- 203: `return` -> 返回函数结果或提前结束当前流程。
- 204: `}` -> 代码块边界，标记作用域开始或结束。
- 205: `}` -> 代码块边界，标记作用域开始或结束。
- 206: `(空行)` -> 空行，用于提升代码结构可读性。
- 207: `// 提交事务` -> 注释行，用于说明后续逻辑意图。
- 208: `if err := tx.Commit().Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 209: `s.logger.WithError(err).Error("Failed to commit transaction")` -> 记录日志，便于运行观测与故障排查。
- 210: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create order"})` -> 向客户端返回 JSON 响应。
- 211: `return` -> 返回函数结果或提前结束当前流程。
- 212: `}` -> 代码块边界，标记作用域开始或结束。
- 213: `(空行)` -> 空行，用于提升代码结构可读性。
- 214: `s.logger.WithFields(logrus.Fields{` -> 记录日志，便于运行观测与故障排查。
- 215: `"order_id":   order.ID,` -> 执行当前语句，参与该函数的业务流程。
- 216: `"order_no":   orderNo,` -> 执行当前语句，参与该函数的业务流程。
- 217: `"total_amount": totalAmount,` -> 执行当前语句，参与该函数的业务流程。
- 218: `}).Info("Order created successfully")` -> 执行当前语句，参与该函数的业务流程。
- 219: `(空行)` -> 空行，用于提升代码结构可读性。
- 220: `c.JSON(http.StatusCreated, gin.H{` -> 向客户端返回 JSON 响应。
- 221: `"message": "Order created successfully",` -> 执行当前语句，参与该函数的业务流程。
- 222: `"order":   order,` -> 执行当前语句，参与该函数的业务流程。
- 223: `})` -> 执行当前语句，参与该函数的业务流程。
- 224: `}` -> 代码块边界，标记作用域开始或结束。
- 225: `(空行)` -> 空行，用于提升代码结构可读性。
- 226: `func (s *Service) GetUserOrders(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 227: `userID := c.Param("user_id")` -> 执行当前语句，参与该函数的业务流程。
- 228: `(空行)` -> 空行，用于提升代码结构可读性。
- 229: `var orders []Order` -> 声明局部变量，为后续逻辑准备数据。
- 230: `if err := s.db.Where("user_id = ?", userID).Preload("Items").Order("created_at DESC").Find(&orders).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 231: `s.logger.WithError(err).Error("Failed to get user orders")` -> 记录日志，便于运行观测与故障排查。
- 232: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get user orders"})` -> 向客户端返回 JSON 响应。
- 233: `return` -> 返回函数结果或提前结束当前流程。
- 234: `}` -> 代码块边界，标记作用域开始或结束。
- 235: `(空行)` -> 空行，用于提升代码结构可读性。
- 236: `c.JSON(http.StatusOK, gin.H{` -> 向客户端返回 JSON 响应。
- 237: `"orders": orders,` -> 执行当前语句，参与该函数的业务流程。
- 238: `"total":  len(orders),` -> 执行当前语句，参与该函数的业务流程。
- 239: `})` -> 执行当前语句，参与该函数的业务流程。
- 240: `}` -> 代码块边界，标记作用域开始或结束。
- 241: `(空行)` -> 空行，用于提升代码结构可读性。
- 242: `func (s *Service) GetOrder(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 243: `id := c.Param("id")` -> 执行当前语句，参与该函数的业务流程。
- 244: `(空行)` -> 空行，用于提升代码结构可读性。
- 245: `var order Order` -> 声明局部变量，为后续逻辑准备数据。
- 246: `if err := s.db.Preload("Items").First(&order, id).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 247: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})` -> 向客户端返回 JSON 响应。
- 248: `return` -> 返回函数结果或提前结束当前流程。
- 249: `}` -> 代码块边界，标记作用域开始或结束。
- 250: `(空行)` -> 空行，用于提升代码结构可读性。
- 251: `c.JSON(http.StatusOK, order)` -> 向客户端返回 JSON 响应。
- 252: `}` -> 代码块边界，标记作用域开始或结束。
- 253: `(空行)` -> 空行，用于提升代码结构可读性。
- 254: `func (s *Service) PayOrder(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 255: `var req PaymentRequest` -> 声明局部变量，为后续逻辑准备数据。
- 256: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断，根据分支处理不同情况。
- 257: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 向客户端返回 JSON 响应。
- 258: `return` -> 返回函数结果或提前结束当前流程。
- 259: `}` -> 代码块边界，标记作用域开始或结束。
- 260: `(空行)` -> 空行，用于提升代码结构可读性。
- 261: `// 查找订单` -> 注释行，用于说明后续逻辑意图。
- 262: `var order Order` -> 声明局部变量，为后续逻辑准备数据。
- 263: `if err := s.db.First(&order, req.OrderID).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 264: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})` -> 向客户端返回 JSON 响应。
- 265: `return` -> 返回函数结果或提前结束当前流程。
- 266: `}` -> 代码块边界，标记作用域开始或结束。
- 267: `(空行)` -> 空行，用于提升代码结构可读性。
- 268: `// 检查订单状态` -> 注释行，用于说明后续逻辑意图。
- 269: `if order.Status != "pending" {` -> 条件判断，根据分支处理不同情况。
- 270: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order is not in pending status"})` -> 向客户端返回 JSON 响应。
- 271: `return` -> 返回函数结果或提前结束当前流程。
- 272: `}` -> 代码块边界，标记作用域开始或结束。
- 273: `(空行)` -> 空行，用于提升代码结构可读性。
- 274: `// 更新订单状态` -> 注释行，用于说明后续逻辑意图。
- 275: `now := time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 276: `order.Status = "paid"` -> 执行当前语句，参与该函数的业务流程。
- 277: `order.PaymentMethod = req.PaymentMethod` -> 执行当前语句，参与该函数的业务流程。
- 278: `order.PaymentTime = &now` -> 执行当前语句，参与该函数的业务流程。
- 279: `order.UpdatedAt = time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 280: `(空行)` -> 空行，用于提升代码结构可读性。
- 281: `if err := s.db.Save(&order).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 282: `s.logger.WithError(err).Error("Failed to update order")` -> 记录日志，便于运行观测与故障排查。
- 283: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update order"})` -> 向客户端返回 JSON 响应。
- 284: `return` -> 返回函数结果或提前结束当前流程。
- 285: `}` -> 代码块边界，标记作用域开始或结束。
- 286: `(空行)` -> 空行，用于提升代码结构可读性。
- 287: `// 更新订单项状态` -> 注释行，用于说明后续逻辑意图。
- 288: `if err := s.db.Model(&OrderItem{}).Where("order_id = ?", req.OrderID).Update("status", "confirmed").Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 289: `s.logger.WithError(err).Error("Failed to update order items")` -> 记录日志，便于运行观测与故障排查。
- 290: `}` -> 代码块边界，标记作用域开始或结束。
- 291: `(空行)` -> 空行，用于提升代码结构可读性。
- 292: `s.logger.WithFields(logrus.Fields{` -> 记录日志，便于运行观测与故障排查。
- 293: `"order_id": req.OrderID,` -> 执行当前语句，参与该函数的业务流程。
- 294: `"status":   "paid",` -> 执行当前语句，参与该函数的业务流程。
- 295: `}).Info("Order paid successfully")` -> 执行当前语句，参与该函数的业务流程。
- 296: `(空行)` -> 空行，用于提升代码结构可读性。
- 297: `c.JSON(http.StatusOK, gin.H{` -> 向客户端返回 JSON 响应。
- 298: `"message": "Order paid successfully",` -> 执行当前语句，参与该函数的业务流程。
- 299: `"order":   order,` -> 执行当前语句，参与该函数的业务流程。
- 300: `})` -> 执行当前语句，参与该函数的业务流程。
- 301: `}` -> 代码块边界，标记作用域开始或结束。
- 302: `(空行)` -> 空行，用于提升代码结构可读性。
- 303: `func (s *Service) CancelOrder(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 304: `var req CancelRequest` -> 声明局部变量，为后续逻辑准备数据。
- 305: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断，根据分支处理不同情况。
- 306: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 向客户端返回 JSON 响应。
- 307: `return` -> 返回函数结果或提前结束当前流程。
- 308: `}` -> 代码块边界，标记作用域开始或结束。
- 309: `(空行)` -> 空行，用于提升代码结构可读性。
- 310: `// 查找订单` -> 注释行，用于说明后续逻辑意图。
- 311: `var order Order` -> 声明局部变量，为后续逻辑准备数据。
- 312: `if err := s.db.First(&order, req.OrderID).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 313: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})` -> 向客户端返回 JSON 响应。
- 314: `return` -> 返回函数结果或提前结束当前流程。
- 315: `}` -> 代码块边界，标记作用域开始或结束。
- 316: `(空行)` -> 空行，用于提升代码结构可读性。
- 317: `// 检查订单状态` -> 注释行，用于说明后续逻辑意图。
- 318: `if order.Status != "pending" && order.Status != "paid" {` -> 条件判断，根据分支处理不同情况。
- 319: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order cannot be cancelled"})` -> 向客户端返回 JSON 响应。
- 320: `return` -> 返回函数结果或提前结束当前流程。
- 321: `}` -> 代码块边界，标记作用域开始或结束。
- 322: `(空行)` -> 空行，用于提升代码结构可读性。
- 323: `// 更新订单状态` -> 注释行，用于说明后续逻辑意图。
- 324: `order.Status = "cancelled"` -> 执行当前语句，参与该函数的业务流程。
- 325: `order.CancelledReason = &req.CancelledReason` -> 执行当前语句，参与该函数的业务流程。
- 326: `order.UpdatedAt = time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 327: `(空行)` -> 空行，用于提升代码结构可读性。
- 328: `// 如果已支付，处理退款逻辑` -> 注释行，用于说明后续逻辑意图。
- 329: `if order.Status == "paid" {` -> 条件判断，根据分支处理不同情况。
- 330: `order.RefundAmount = &order.TotalAmount` -> 执行当前语句，参与该函数的业务流程。
- 331: `order.RefundTime = &time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 332: `order.RefundReason = &req.CancelledReason` -> 执行当前语句，参与该函数的业务流程。
- 333: `(空行)` -> 空行，用于提升代码结构可读性。
- 334: `// 恢复库存` -> 注释行，用于说明后续逻辑意图。
- 335: `var items []OrderItem` -> 声明局部变量，为后续逻辑准备数据。
- 336: `if err := s.db.Where("order_id = ?", req.OrderID).Find(&items).Error; err == nil {` -> 条件判断，根据分支处理不同情况。
- 337: `for _, item := range items {` -> 循环遍历数据集合并执行处理。
- 338: `s.db.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory + ?", item.Quantity))` -> 执行数据库查询或持久化操作。
- 339: `}` -> 代码块边界，标记作用域开始或结束。
- 340: `}` -> 代码块边界，标记作用域开始或结束。
- 341: `}` -> 代码块边界，标记作用域开始或结束。
- 342: `(空行)` -> 空行，用于提升代码结构可读性。
- 343: `if err := s.db.Save(&order).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 344: `s.logger.WithError(err).Error("Failed to cancel order")` -> 记录日志，便于运行观测与故障排查。
- 345: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to cancel order"})` -> 向客户端返回 JSON 响应。
- 346: `return` -> 返回函数结果或提前结束当前流程。
- 347: `}` -> 代码块边界，标记作用域开始或结束。
- 348: `(空行)` -> 空行，用于提升代码结构可读性。
- 349: `s.logger.WithFields(logrus.Fields{` -> 记录日志，便于运行观测与故障排查。
- 350: `"order_id": req.OrderID,` -> 执行当前语句，参与该函数的业务流程。
- 351: `"status":   "cancelled",` -> 执行当前语句，参与该函数的业务流程。
- 352: `}).Info("Order cancelled successfully")` -> 执行当前语句，参与该函数的业务流程。
- 353: `(空行)` -> 空行，用于提升代码结构可读性。
- 354: `c.JSON(http.StatusOK, gin.H{` -> 向客户端返回 JSON 响应。
- 355: `"message": "Order cancelled successfully",` -> 执行当前语句，参与该函数的业务流程。
- 356: `"order":   order,` -> 执行当前语句，参与该函数的业务流程。
- 357: `})` -> 执行当前语句，参与该函数的业务流程。
- 358: `}` -> 代码块边界，标记作用域开始或结束。
- 359: `(空行)` -> 空行，用于提升代码结构可读性。
- 360: `func (s *Service) RefundOrder(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 361: `var req RefundRequest` -> 声明局部变量，为后续逻辑准备数据。
- 362: `if err := c.ShouldBindJSON(&req); err != nil {` -> 条件判断，根据分支处理不同情况。
- 363: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})` -> 向客户端返回 JSON 响应。
- 364: `return` -> 返回函数结果或提前结束当前流程。
- 365: `}` -> 代码块边界，标记作用域开始或结束。
- 366: `(空行)` -> 空行，用于提升代码结构可读性。
- 367: `// 查找订单` -> 注释行，用于说明后续逻辑意图。
- 368: `var order Order` -> 声明局部变量，为后续逻辑准备数据。
- 369: `if err := s.db.First(&order, req.OrderID).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 370: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})` -> 向客户端返回 JSON 响应。
- 371: `return` -> 返回函数结果或提前结束当前流程。
- 372: `}` -> 代码块边界，标记作用域开始或结束。
- 373: `(空行)` -> 空行，用于提升代码结构可读性。
- 374: `// 检查订单状态` -> 注释行，用于说明后续逻辑意图。
- 375: `if order.Status != "paid" {` -> 条件判断，根据分支处理不同情况。
- 376: `c.JSON(http.StatusBadRequest, gin.H{"error": "Order is not in paid status"})` -> 向客户端返回 JSON 响应。
- 377: `return` -> 返回函数结果或提前结束当前流程。
- 378: `}` -> 代码块边界，标记作用域开始或结束。
- 379: `(空行)` -> 空行，用于提升代码结构可读性。
- 380: `// 更新订单状态` -> 注释行，用于说明后续逻辑意图。
- 381: `order.Status = "refunded"` -> 执行当前语句，参与该函数的业务流程。
- 382: `order.RefundAmount = &req.RefundAmount` -> 执行当前语句，参与该函数的业务流程。
- 383: `order.RefundReason = &req.RefundReason` -> 执行当前语句，参与该函数的业务流程。
- 384: `order.RefundTime = &time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 385: `order.UpdatedAt = time.Now()` -> 获取当前时间，用于时间字段或时序控制。
- 386: `(空行)` -> 空行，用于提升代码结构可读性。
- 387: `if err := s.db.Save(&order).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 388: `s.logger.WithError(err).Error("Failed to refund order")` -> 记录日志，便于运行观测与故障排查。
- 389: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to refund order"})` -> 向客户端返回 JSON 响应。
- 390: `return` -> 返回函数结果或提前结束当前流程。
- 391: `}` -> 代码块边界，标记作用域开始或结束。
- 392: `(空行)` -> 空行，用于提升代码结构可读性。
- 393: `// 恢复库存` -> 注释行，用于说明后续逻辑意图。
- 394: `var items []OrderItem` -> 声明局部变量，为后续逻辑准备数据。
- 395: `if err := s.db.Where("order_id = ?", req.OrderID).Find(&items).Error; err == nil {` -> 条件判断，根据分支处理不同情况。
- 396: `for _, item := range items {` -> 循环遍历数据集合并执行处理。
- 397: `s.db.Model(&struct{}{}).Table("products").Where("id = ?", item.ProductID).Update("inventory", gorm.Expr("inventory + ?", item.Quantity))` -> 执行数据库查询或持久化操作。
- 398: `}` -> 代码块边界，标记作用域开始或结束。
- 399: `}` -> 代码块边界，标记作用域开始或结束。
- 400: `(空行)` -> 空行，用于提升代码结构可读性。
- 401: `s.logger.WithFields(logrus.Fields{` -> 记录日志，便于运行观测与故障排查。
- 402: `"order_id": req.OrderID,` -> 执行当前语句，参与该函数的业务流程。
- 403: `"status":   "refunded",` -> 执行当前语句，参与该函数的业务流程。
- 404: `}).Info("Order refunded successfully")` -> 执行当前语句，参与该函数的业务流程。
- 405: `(空行)` -> 空行，用于提升代码结构可读性。
- 406: `c.JSON(http.StatusOK, gin.H{` -> 向客户端返回 JSON 响应。
- 407: `"message": "Order refunded successfully",` -> 执行当前语句，参与该函数的业务流程。
- 408: `"order":   order,` -> 执行当前语句，参与该函数的业务流程。
- 409: `})` -> 执行当前语句，参与该函数的业务流程。
- 410: `}` -> 代码块边界，标记作用域开始或结束。
- 411: `(空行)` -> 空行，用于提升代码结构可读性。
- 412: `func (s *Service) GetOrderStatus(c *gin.Context) {` -> 定义函数或方法，实现具体业务逻辑。
- 413: `id := c.Param("id")` -> 执行当前语句，参与该函数的业务流程。
- 414: `(空行)` -> 空行，用于提升代码结构可读性。
- 415: `var order struct {` -> 声明局部变量，为后续逻辑准备数据。
- 416: `ID     string    \`json:"id"\`` -> 执行当前语句，参与该函数的业务流程。
- 417: `Status string    \`json:"status"\`` -> 执行当前语句，参与该函数的业务流程。
- 418: `UpdatedAt time.Time \`json:"updated_at"\`` -> 执行当前语句，参与该函数的业务流程。
- 419: `}` -> 代码块边界，标记作用域开始或结束。
- 420: `(空行)` -> 空行，用于提升代码结构可读性。
- 421: `if err := s.db.Model(&Order{}).Select("id, status, updated_at").First(&order, id).Error; err != nil {` -> 条件判断，根据分支处理不同情况。
- 422: `c.JSON(http.StatusNotFound, gin.H{"error": "Order not found"})` -> 向客户端返回 JSON 响应。
- 423: `return` -> 返回函数结果或提前结束当前流程。
- 424: `}` -> 代码块边界，标记作用域开始或结束。
- 425: `(空行)` -> 空行，用于提升代码结构可读性。
- 426: `c.JSON(http.StatusOK, order)` -> 向客户端返回 JSON 响应。
- 427: `}` -> 代码块边界，标记作用域开始或结束。
