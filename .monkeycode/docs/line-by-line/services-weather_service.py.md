# `services/weather_service.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# -*- coding: utf-8 -*-` -> 注释行，用于解释设计意图或使用说明。
- 2: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 3: `============================================` -> 执行当前语句，参与该文件整体逻辑。
- 4: `天气服务模块` -> 执行当前语句，参与该文件整体逻辑。
- 5: `============================================` -> 执行当前语句，参与该文件整体逻辑。
- 6: `(空行)` -> 空行，用于提升代码结构可读性。
- 7: `【模块说明】` -> 执行当前语句，参与该文件整体逻辑。
- 8: `- 封装心知天气API，提供实时天气和预报功能` -> 执行当前语句，参与该文件整体逻辑。
- 9: `- 支持真实API和Mock数据切换` -> 执行当前语句，参与该文件整体逻辑。
- 10: `- 提供单例模式的全局服务实例` -> 执行当前语句，参与该文件整体逻辑。
- 11: `(空行)` -> 空行，用于提升代码结构可读性。
- 12: `【API文档】` -> 执行当前语句，参与该文件整体逻辑。
- 13: `- 心知天气API: https://api.seniverse.com/` -> 执行当前语句，参与该文件整体逻辑。
- 14: `- 需要申请API Key并设置环境变量 SENIVERSE_API_KEY` -> 执行当前语句，参与该文件整体逻辑。
- 15: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 16: `(空行)` -> 空行，用于提升代码结构可读性。
- 17: `import os` -> 导入依赖模块，供当前文件使用。
- 18: `import json` -> 导入依赖模块，供当前文件使用。
- 19: `import requests` -> 导入依赖模块，供当前文件使用。
- 20: `from datetime import datetime` -> 执行当前语句，参与该文件整体逻辑。
- 21: `from typing import Dict, List, Optional` -> 执行当前语句，参与该文件整体逻辑。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `(空行)` -> 空行，用于提升代码结构可读性。
- 24: `class WeatherService:` -> 定义类型或类结构，约束数据与行为。
- 25: `"""天气服务类` -> 执行当前语句，参与该文件整体逻辑。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `【功能】` -> 执行当前语句，参与该文件整体逻辑。
- 28: `- 获取实时天气（温度、湿度、风力等）` -> 执行当前语句，参与该文件整体逻辑。
- 29: `- 获取天气预报（3-7天）` -> 执行当前语句，参与该文件整体逻辑。
- 30: `- API不可用时自动降级到模拟数据` -> 执行当前语句，参与该文件整体逻辑。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `【使用方式】` -> 执行当前语句，参与该文件整体逻辑。
- 33: `service = WeatherService()` -> 执行当前语句，参与该文件整体逻辑。
- 34: `weather = service.get_current_weather('北京')` -> 执行当前语句，参与该文件整体逻辑。
- 35: `forecast = service.get_forecast('北京', 3)` -> 执行当前语句，参与该文件整体逻辑。
- 36: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 37: `(空行)` -> 空行，用于提升代码结构可读性。
- 38: `def __init__(self, api_key: str = None):` -> 定义 Python 函数，封装可复用逻辑。
- 39: `"""初始化天气服务` -> 执行当前语句，参与该文件整体逻辑。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 42: `api_key: API密钥，优先使用参数，否则从环境变量读取` -> 执行当前语句，参与该文件整体逻辑。
- 43: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 44: `self.api_key = api_key or os.getenv('SENIVERSE_API_KEY', '')` -> 执行当前语句，参与该文件整体逻辑。
- 45: `self.base_url = 'https://api.seniverse.com/v3'` -> 执行当前语句，参与该文件整体逻辑。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `def get_current_weather(self, city: str) -> Optional[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 48: `"""获取实时天气` -> 执行当前语句，参与该文件整体逻辑。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 51: `city: 城市名称（支持中文、拼音、坐标等）` -> 执行当前语句，参与该文件整体逻辑。
- 52: `(空行)` -> 空行，用于提升代码结构可读性。
- 53: `Returns:` -> 执行当前语句，参与该文件整体逻辑。
- 54: `天气数据字典，包含：` -> 执行当前语句，参与该文件整体逻辑。
- 55: `- city: 城市名` -> 执行当前语句，参与该文件整体逻辑。
- 56: `- country: 国家` -> 执行当前语句，参与该文件整体逻辑。
- 57: `- temperature: 温度(℃)` -> 执行当前语句，参与该文件整体逻辑。
- 58: `- weather: 天气状况` -> 执行当前语句，参与该文件整体逻辑。
- 59: `- wind_direction: 风向` -> 执行当前语句，参与该文件整体逻辑。
- 60: `- wind_scale: 风力等级` -> 执行当前语句，参与该文件整体逻辑。
- 61: `- humidity: 湿度` -> 执行当前语句，参与该文件整体逻辑。
- 62: `- feels_like: 体感温度` -> 执行当前语句，参与该文件整体逻辑。
- 63: `- last_update: 更新时间` -> 执行当前语句，参与该文件整体逻辑。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `【API调用】` -> 执行当前语句，参与该文件整体逻辑。
- 66: `GET https://api.seniverse.com/v3/weather/now.json` -> 执行当前语句，参与该文件整体逻辑。
- 67: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 68: `# 无API Key时返回模拟数据` -> 注释行，用于解释设计意图或使用说明。
- 69: `if not self.api_key:` -> 条件判断分支，根据场景执行不同逻辑。
- 70: `return self._get_mock_weather(city)` -> 返回结果或提前结束当前流程。
- 71: `(空行)` -> 空行，用于提升代码结构可读性。
- 72: `try:` -> 异常处理逻辑，提升程序健壮性。
- 73: `url = f"{self.base_url}/weather/now.json"` -> 执行当前语句，参与该文件整体逻辑。
- 74: `params = {` -> 执行当前语句，参与该文件整体逻辑。
- 75: `'key': self.api_key,` -> 执行当前语句，参与该文件整体逻辑。
- 76: `'location': city,` -> 执行当前语句，参与该文件整体逻辑。
- 77: `'language': 'zh-Hans',` -> 执行当前语句，参与该文件整体逻辑。
- 78: `'unit': 'c'` -> 执行当前语句，参与该文件整体逻辑。
- 79: `}` -> 结束当前语句或代码块。
- 80: `resp = requests.get(url, params=params, timeout=5)` -> 执行当前语句，参与该文件整体逻辑。
- 81: `data = resp.json()` -> 执行当前语句，参与该文件整体逻辑。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `if data.get('results'):` -> 条件判断分支，根据场景执行不同逻辑。
- 84: `result = data['results'][0]` -> 执行当前语句，参与该文件整体逻辑。
- 85: `now = result.get('now', {})` -> 执行当前语句，参与该文件整体逻辑。
- 86: `return {` -> 返回结果或提前结束当前流程。
- 87: `'city': result['location']['name'],` -> 执行当前语句，参与该文件整体逻辑。
- 88: `'country': result['location']['country'],` -> 执行当前语句，参与该文件整体逻辑。
- 89: `'temperature': now.get('temperature'),` -> 执行当前语句，参与该文件整体逻辑。
- 90: `'weather': now.get('text'),` -> 执行当前语句，参与该文件整体逻辑。
- 91: `'wind_direction': now.get('wind_direction'),` -> 执行当前语句，参与该文件整体逻辑。
- 92: `'wind_scale': now.get('wind_scale'),` -> 执行当前语句，参与该文件整体逻辑。
- 93: `'humidity': now.get('humidity'),` -> 执行当前语句，参与该文件整体逻辑。
- 94: `'feels_like': now.get('feels_like'),` -> 执行当前语句，参与该文件整体逻辑。
- 95: `'last_update': result.get('last_update'),` -> 执行当前语句，参与该文件整体逻辑。
- 96: `}` -> 结束当前语句或代码块。
- 97: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 98: `print(f"获取天气失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 99: `(空行)` -> 空行，用于提升代码结构可读性。
- 100: `# 失败时降级到模拟数据` -> 注释行，用于解释设计意图或使用说明。
- 101: `return self._get_mock_weather(city)` -> 返回结果或提前结束当前流程。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `def get_forecast(self, city: str, days: int = 3) -> List[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 104: `"""获取天气预报` -> 执行当前语句，参与该文件整体逻辑。
- 105: `(空行)` -> 空行，用于提升代码结构可读性。
- 106: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 107: `city: 城市名称` -> 执行当前语句，参与该文件整体逻辑。
- 108: `days: 预报天数（1-7天）` -> 执行当前语句，参与该文件整体逻辑。
- 109: `(空行)` -> 空行，用于提升代码结构可读性。
- 110: `Returns:` -> 执行当前语句，参与该文件整体逻辑。
- 111: `预报数据列表，每项包含：` -> 执行当前语句，参与该文件整体逻辑。
- 112: `- date: 日期` -> 执行当前语句，参与该文件整体逻辑。
- 113: `- text_day: 白天天气` -> 执行当前语句，参与该文件整体逻辑。
- 114: `- text_night: 夜间天气` -> 执行当前语句，参与该文件整体逻辑。
- 115: `- high: 最高温度` -> 执行当前语句，参与该文件整体逻辑。
- 116: `- low: 最低温度` -> 执行当前语句，参与该文件整体逻辑。
- 117: `- wind_direction: 风向` -> 执行当前语句，参与该文件整体逻辑。
- 118: `- wind_scale: 风力等级` -> 执行当前语句，参与该文件整体逻辑。
- 119: `- rainfall: 降水量` -> 执行当前语句，参与该文件整体逻辑。
- 120: `- humidity: 湿度` -> 执行当前语句，参与该文件整体逻辑。
- 121: `(空行)` -> 空行，用于提升代码结构可读性。
- 122: `【API调用】` -> 执行当前语句，参与该文件整体逻辑。
- 123: `GET https://api.seniverse.com/v3/weather/daily.json` -> 执行当前语句，参与该文件整体逻辑。
- 124: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 125: `# 无API Key时返回模拟数据` -> 注释行，用于解释设计意图或使用说明。
- 126: `if not self.api_key:` -> 条件判断分支，根据场景执行不同逻辑。
- 127: `return self._get_mock_forecast(city, days)` -> 返回结果或提前结束当前流程。
- 128: `(空行)` -> 空行，用于提升代码结构可读性。
- 129: `try:` -> 异常处理逻辑，提升程序健壮性。
- 130: `url = f"{self.base_url}/weather/daily.json"` -> 执行当前语句，参与该文件整体逻辑。
- 131: `params = {` -> 执行当前语句，参与该文件整体逻辑。
- 132: `'key': self.api_key,` -> 执行当前语句，参与该文件整体逻辑。
- 133: `'location': city,` -> 执行当前语句，参与该文件整体逻辑。
- 134: `'language': 'zh-Hans',` -> 执行当前语句，参与该文件整体逻辑。
- 135: `'unit': 'c',` -> 执行当前语句，参与该文件整体逻辑。
- 136: `'start': 0,` -> 执行当前语句，参与该文件整体逻辑。
- 137: `'days': days` -> 执行当前语句，参与该文件整体逻辑。
- 138: `}` -> 结束当前语句或代码块。
- 139: `resp = requests.get(url, params=params, timeout=5)` -> 执行当前语句，参与该文件整体逻辑。
- 140: `data = resp.json()` -> 执行当前语句，参与该文件整体逻辑。
- 141: `(空行)` -> 空行，用于提升代码结构可读性。
- 142: `if data.get('results'):` -> 条件判断分支，根据场景执行不同逻辑。
- 143: `result = data['results'][0]` -> 执行当前语句，参与该文件整体逻辑。
- 144: `daily = result.get('daily', [])` -> 执行当前语句，参与该文件整体逻辑。
- 145: `return [` -> 返回结果或提前结束当前流程。
- 146: `{` -> 执行当前语句，参与该文件整体逻辑。
- 147: `'date': d.get('date'),` -> 执行当前语句，参与该文件整体逻辑。
- 148: `'text_day': d.get('text_day'),` -> 执行当前语句，参与该文件整体逻辑。
- 149: `'text_night': d.get('text_night'),` -> 执行当前语句，参与该文件整体逻辑。
- 150: `'high': d.get('high'),` -> 执行当前语句，参与该文件整体逻辑。
- 151: `'low': d.get('low'),` -> 执行当前语句，参与该文件整体逻辑。
- 152: `'wind_direction': d.get('wind_direction'),` -> 执行当前语句，参与该文件整体逻辑。
- 153: `'wind_scale': d.get('wind_scale'),` -> 执行当前语句，参与该文件整体逻辑。
- 154: `'rainfall': d.get('rainfall'),` -> 执行当前语句，参与该文件整体逻辑。
- 155: `'humidity': d.get('humidity'),` -> 执行当前语句，参与该文件整体逻辑。
- 156: `}` -> 结束当前语句或代码块。
- 157: `for d in daily` -> 循环处理集合或重复执行逻辑。
- 158: `]` -> 执行当前语句，参与该文件整体逻辑。
- 159: `except Exception as e:` -> 异常处理逻辑，提升程序健壮性。
- 160: `print(f"获取预报失败: {e}")` -> 执行当前语句，参与该文件整体逻辑。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `# 失败时降级到模拟数据` -> 注释行，用于解释设计意图或使用说明。
- 163: `return self._get_mock_forecast(city, days)` -> 返回结果或提前结束当前流程。
- 164: `(空行)` -> 空行，用于提升代码结构可读性。
- 165: `def _get_mock_weather(self, city: str) -> Dict:` -> 定义 Python 函数，封装可复用逻辑。
- 166: `"""返回模拟实时天气` -> 执行当前语句，参与该文件整体逻辑。
- 167: `(空行)` -> 空行，用于提升代码结构可读性。
- 168: `【生成规则】` -> 执行当前语句，参与该文件整体逻辑。
- 169: `- 天气：随机选择晴/多云/阴/小雨/晴转多云` -> 执行当前语句，参与该文件整体逻辑。
- 170: `- 温度：15-30℃随机` -> 执行当前语句，参与该文件整体逻辑。
- 171: `- 湿度：40-80%随机` -> 执行当前语句，参与该文件整体逻辑。
- 172: `- 风向：随机选择东西南北风` -> 执行当前语句，参与该文件整体逻辑。
- 173: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 174: `import random` -> 导入依赖模块，供当前文件使用。
- 175: `weather_types = ['晴', '多云', '阴', '小雨', '晴转多云']` -> 执行当前语句，参与该文件整体逻辑。
- 176: `return {` -> 返回结果或提前结束当前流程。
- 177: `'city': city,` -> 执行当前语句，参与该文件整体逻辑。
- 178: `'country': '中国',` -> 执行当前语句，参与该文件整体逻辑。
- 179: `'temperature': str(random.randint(15, 30)),` -> 执行当前语句，参与该文件整体逻辑。
- 180: `'weather': random.choice(weather_types),` -> 执行当前语句，参与该文件整体逻辑。
- 181: `'wind_direction': random.choice(['北风', '南风', '东风', '西风']),` -> 执行当前语句，参与该文件整体逻辑。
- 182: `'wind_scale': str(random.randint(1, 5)),` -> 执行当前语句，参与该文件整体逻辑。
- 183: `'humidity': str(random.randint(40, 80)),` -> 执行当前语句，参与该文件整体逻辑。
- 184: `'feels_like': str(random.randint(14, 28)),` -> 执行当前语句，参与该文件整体逻辑。
- 185: `'last_update': datetime.now().isoformat(),` -> 执行当前语句，参与该文件整体逻辑。
- 186: `}` -> 结束当前语句或代码块。
- 187: `(空行)` -> 空行，用于提升代码结构可读性。
- 188: `def _get_mock_forecast(self, city: str, days: int) -> List[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 189: `"""返回模拟预报数据` -> 执行当前语句，参与该文件整体逻辑。
- 190: `(空行)` -> 空行，用于提升代码结构可读性。
- 191: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 192: `city: 城市名` -> 执行当前语句，参与该文件整体逻辑。
- 193: `days: 预报天数` -> 执行当前语句，参与该文件整体逻辑。
- 194: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 195: `import random` -> 导入依赖模块，供当前文件使用。
- 196: `weather_types = ['晴', '多云', '阴', '小雨', '晴转多云']` -> 执行当前语句，参与该文件整体逻辑。
- 197: `forecasts = []` -> 执行当前语句，参与该文件整体逻辑。
- 198: `for i in range(days):` -> 循环处理集合或重复执行逻辑。
- 199: `date = datetime.now().date()` -> 执行当前语句，参与该文件整体逻辑。
- 200: `forecasts.append({` -> 执行当前语句，参与该文件整体逻辑。
- 201: `'date': str(date),` -> 执行当前语句，参与该文件整体逻辑。
- 202: `'text_day': random.choice(weather_types),` -> 执行当前语句，参与该文件整体逻辑。
- 203: `'text_night': random.choice(weather_types),` -> 执行当前语句，参与该文件整体逻辑。
- 204: `'high': str(random.randint(20, 32)),` -> 执行当前语句，参与该文件整体逻辑。
- 205: `'low': str(random.randint(10, 20)),` -> 执行当前语句，参与该文件整体逻辑。
- 206: `'wind_direction': random.choice(['北风', '南风', '东风', '西风']),` -> 执行当前语句，参与该文件整体逻辑。
- 207: `'wind_scale': str(random.randint(1, 4)),` -> 执行当前语句，参与该文件整体逻辑。
- 208: `'rainfall': str(round(random.uniform(0, 10), 1)),` -> 执行当前语句，参与该文件整体逻辑。
- 209: `'humidity': str(random.randint(40, 80)),` -> 执行当前语句，参与该文件整体逻辑。
- 210: `})` -> 执行当前语句，参与该文件整体逻辑。
- 211: `return forecasts` -> 返回结果或提前结束当前流程。
- 212: `(空行)` -> 空行，用于提升代码结构可读性。
- 213: `(空行)` -> 空行，用于提升代码结构可读性。
- 214: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 215: `# 全局实例（单例模式）` -> 注释行，用于解释设计意图或使用说明。
- 216: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 217: `(空行)` -> 空行，用于提升代码结构可读性。
- 218: `# 全局天气服务实例，延迟初始化` -> 注释行，用于解释设计意图或使用说明。
- 219: `_weather_service = None` -> 执行当前语句，参与该文件整体逻辑。
- 220: `(空行)` -> 空行，用于提升代码结构可读性。
- 221: `def get_weather_service() -> WeatherService:` -> 定义 Python 函数，封装可复用逻辑。
- 222: `"""获取天气服务实例（单例）` -> 执行当前语句，参与该文件整体逻辑。
- 223: `(空行)` -> 空行，用于提升代码结构可读性。
- 224: `【好处】` -> 执行当前语句，参与该文件整体逻辑。
- 225: `- 全局复用，避免重复创建实例` -> 执行当前语句，参与该文件整体逻辑。
- 226: `- 节省资源` -> 执行当前语句，参与该文件整体逻辑。
- 227: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 228: `global _weather_service` -> 执行当前语句，参与该文件整体逻辑。
- 229: `if _weather_service is None:` -> 条件判断分支，根据场景执行不同逻辑。
- 230: `_weather_service = WeatherService()` -> 执行当前语句，参与该文件整体逻辑。
- 231: `return _weather_service` -> 返回结果或提前结束当前流程。
- 232: `(空行)` -> 空行，用于提升代码结构可读性。
- 233: `(空行)` -> 空行，用于提升代码结构可读性。
- 234: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 235: `# 便捷函数` -> 注释行，用于解释设计意图或使用说明。
- 236: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 237: `# 提供直接调用的简化接口` -> 注释行，用于解释设计意图或使用说明。
- 238: `(空行)` -> 空行，用于提升代码结构可读性。
- 239: `def get_current_weather(city: str) -> Optional[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 240: `"""获取实时天气"""` -> 执行当前语句，参与该文件整体逻辑。
- 241: `service = get_weather_service()` -> 执行当前语句，参与该文件整体逻辑。
- 242: `return service.get_current_weather(city)` -> 返回结果或提前结束当前流程。
- 243: `(空行)` -> 空行，用于提升代码结构可读性。
- 244: `(空行)` -> 空行，用于提升代码结构可读性。
- 245: `def get_forecast(city: str, days: int = 3) -> List[Dict]:` -> 定义 Python 函数，封装可复用逻辑。
- 246: `"""获取天气预报` -> 执行当前语句，参与该文件整体逻辑。
- 247: `(空行)` -> 空行，用于提升代码结构可读性。
- 248: `Args:` -> 执行当前语句，参与该文件整体逻辑。
- 249: `city: 城市名称` -> 执行当前语句，参与该文件整体逻辑。
- 250: `days: 预报天数（默认3天）` -> 执行当前语句，参与该文件整体逻辑。
- 251: `"""` -> 执行当前语句，参与该文件整体逻辑。
- 252: `service = get_weather_service()` -> 执行当前语句，参与该文件整体逻辑。
- 253: `return service.get_forecast(city, days)` -> 返回结果或提前结束当前流程。
