# `backend/product-service/main.go` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `package product`
  - 作用：声明包名。
  - 原因：通过包边界组织可见性与编译单元。
- 2: `import (`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 3: `"context"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 4: `"database/sql"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 5: `"encoding/json"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 6: `"fmt"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 7: `"net/http"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 8: `"strings"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 9: `"time"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 10: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 11: `"github.com/gin-gonic/gin"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 12: `"github.com/sirupsen/logrus"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 13: `"gorm.io/gorm"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 14: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 15: `type Service struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 16: `db     *gorm.DB`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 17: `logger *logrus.Logger`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 18: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 19: `type Product struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 20: `ID           uint      \`json:"id" gorm:"primaryKey"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 21: `Type         string    \`json:"type" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 22: `Name         string    \`json:"name" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 23: `Description  string    \`json:"description"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 24: `Price        float64   \`json:"price" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 25: `OriginalPrice *float64  \`json:"original_price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 26: `Inventory    int       \`json:"inventory" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 27: `Tags         []string  \`json:"tags" gorm:"type:jsonb"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 28: `Metadata     ProductMetadata \`json:"metadata" gorm:"type:jsonb"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 29: `Status       string    \`json:"status" gorm:"not null"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 30: `Images       []string  \`json:"images" gorm:"type:jsonb"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `Location     Location  \`json:"location" gorm:"type:jsonb"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `Rating       float64   \`json:"rating"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `ReviewCount  int       \`json:"review_count"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `CreatedAt    time.Time \`json:"created_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `UpdatedAt    time.Time \`json:"updated_at"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `CoverImage   string    \`json:"cover_image"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `Subtitle     string    \`json:"subtitle"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 38: `City         string    \`json:"city" gorm:"column:subtitle"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 39: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 40: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 41: `type Location struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 42: `City       string  \`json:"city"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 43: `Country    string  \`json:"country"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `Latitude   float64 \`json:"latitude"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 45: `Longitude  float64 \`json:"longitude"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `type ProductMetadata struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 49: `// Flight specific`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 50: `Airline         string  \`json:"airline"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `FlightNumber    string  \`json:"flight_number"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 52: `DepartureAirport string \`json:"departure_airport"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 53: `ArrivalAirport   string \`json:"arrival_airport"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 54: `DepartureTime   string \`json:"departure_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 55: `ArrivalTime     string \`json:"arrival_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `Duration        string \`json:"duration"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 58: `// Hotel specific`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 59: `StarRating      int      \`json:"star_rating"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `Amenities       []string \`json:"amenities"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `RoomType        string   \`json:"room_type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 62: `CheckInTime     string   \`json:"check_in_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 63: `CheckOutTime    string   \`json:"check_out_time"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 65: `// Ticket specific`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 66: `AttractionName  string \`json:"attraction_name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 67: `OpeningHours   string \`json:"opening_hours"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 68: `ValidDays       int    \`json:"valid_days"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 69: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 70: `// Experience specific`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 71: `Duration        string \`json:"duration"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `Difficulty      string \`json:"difficulty"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `GroupSizeMin    int    \`json:"group_size_min"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `GroupSizeMax    int    \`json:"group_size_max"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 75: `Includes        []string \`json:"includes"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 76: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 77: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 78: `type SearchFilters struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 79: `Destination    *string  \`json:"destination"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 80: `StartDate      *string  \`json:"start_date"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 81: `EndDate        *string  \`json:"end_date"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `BudgetMin      *float64 \`json:"budget_min"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `BudgetMax      *float64 \`json:"budget_max"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 84: `TravelStyle    *string  \`json:"travel_style"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `GroupSize      *int     \`json:"group_size"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `Tags           []string \`json:"tags"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `RatingMin      *float64 \`json:"rating_min"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `Type           *string  \`json:"type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `Limit          int      \`json:"limit"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 90: `Offset         int      \`json:"offset"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 91: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 92: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 93: `type CreateProductRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 94: `Type         string             \`json:"type" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 95: `Name         string             \`json:"name" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `Description  string             \`json:"description"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `Price        float64            \`json:"price" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 98: `OriginalPrice *float64           \`json:"original_price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `Inventory    int                \`json:"inventory" binding:"required"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `Tags         []string           \`json:"tags"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 101: `Metadata     ProductMetadata    \`json:"metadata"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `Status       string             \`json:"status"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `Images       []string           \`json:"images"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 104: `Location     Location           \`json:"location"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 106: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 107: `type UpdateProductRequest struct {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 108: `Type         *string            \`json:"type"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `Name         *string            \`json:"name"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 110: `Description  *string            \`json:"description"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 111: `Price        *float64           \`json:"price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 112: `OriginalPrice *float64           \`json:"original_price"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 113: `Inventory    *int               \`json:"inventory"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 114: `Tags         []string           \`json:"tags"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 115: `Metadata     *ProductMetadata   \`json:"metadata"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 116: `Status       *string            \`json:"status"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `Images       []string           \`json:"images"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `Location     *Location         \`json:"location"\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 120: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 121: `func NewService(db *gorm.DB, logger *logrus.Logger) *Service {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 122: `return &Service{`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 123: `db:     db,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `logger: logger,`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 125: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 126: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 127: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 128: `func (s *Service) SetupRoutes(router *gin.RouterGroup) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 129: `products := router.Group("/products")`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 130: `{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `// Search products`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 132: `products.GET("/search", s.SearchProducts)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 133: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 134: `// CRUD operations`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 135: `products.POST("", s.CreateProduct)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 136: `products.GET("", s.ListProducts)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 137: `products.GET("/:id", s.GetProduct)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 138: `products.PUT("/:id", s.UpdateProduct)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 139: `products.DELETE("/:id", s.DeleteProduct)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 140: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 141: `// Specific endpoints`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 142: `products.GET("/popular", s.GetPopularProducts)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 143: `products.GET("/recommendations/:user_id", s.GetRecommendations)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 144: `products.POST("/:id/increment-views", s.IncrementViews)`
  - 作用：注册路由或分组。
  - 原因：按业务域分组可降低冲突并提升可维护性。
- 145: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 146: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 147: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 148: `func (s *Service) SearchProducts(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 149: `var filters SearchFilters`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 150: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 151: `// Parse query parameters`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 152: `if destination := c.Query("destination"); destination != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 153: `filters.Destination = &destination`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 154: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 155: `if startDate := c.Query("start_date"); startDate != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 156: `filters.StartDate = &startDate`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 157: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 158: `if endDate := c.Query("end_date"); endDate != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 159: `filters.EndDate = &endDate`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 160: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 161: `if budgetMin := c.Query("budget_min"); budgetMin != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 162: `if min, err := fmt.Sscanf(budgetMin, "%f", &filters.BudgetMin); err == nil && min == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 163: `// BudgetMin is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 164: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 165: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 166: `if budgetMax := c.Query("budget_max"); budgetMax != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 167: `if max, err := fmt.Sscanf(budgetMax, "%f", &filters.BudgetMax); err == nil && max == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 168: `// BudgetMax is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 169: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 170: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 171: `if travelStyle := c.Query("travel_style"); travelStyle != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 172: `filters.TravelStyle = &travelStyle`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 174: `if groupSize := c.Query("group_size"); groupSize != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 175: `if size, err := fmt.Sscanf(groupSize, "%d", &filters.GroupSize); err == nil && size == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 176: `// GroupSize is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 177: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 178: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 179: `if ratingMin := c.Query("rating_min"); ratingMin != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 180: `if rating, err := fmt.Sscanf(ratingMin, "%f", &filters.RatingMin); err == nil && rating == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 181: `// RatingMin is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 182: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 183: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 184: `if productType := c.Query("type"); productType != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 185: `filters.Type = &productType`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 186: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 187: `if limit := c.DefaultQuery("limit", "20"); limit != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 188: `if l, err := fmt.Sscanf(limit, "%d", &filters.Limit); err == nil && l == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 189: `// Limit is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 190: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 191: `filters.Limit = 20`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 192: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 193: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 194: `if offset := c.DefaultQuery("offset", "0"); offset != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 195: `if o, err := fmt.Sscanf(offset, "%d", &filters.Offset); err == nil && o == 1 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 196: `// Offset is set`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 197: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 198: `filters.Offset = 0`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 199: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 200: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 201: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 202: `var products []Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 203: `query := s.db.Model(&Product{})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 204: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 205: `// Apply filters`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 206: `if filters.Destination != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 207: `query = query.Where("location->>'city' = ?", *filters.Destination)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 208: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 209: `if filters.Type != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 210: `query = query.Where("type = ?", *filters.Type)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 211: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 212: `if filters.BudgetMin != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 213: `query = query.Where("price >= ?", *filters.BudgetMin)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 214: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 215: `if filters.BudgetMax != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 216: `query = query.Where("price <= ?", *filters.BudgetMax)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 217: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 218: `if filters.RatingMin != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 219: `query = query.Where("rating >= ?", *filters.RatingMin)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 220: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 221: `if len(filters.Tags) > 0 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 222: `// This is a simplified tag search - in production you'd want more complex logic`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 223: `query = query.Where("tags @> ?", fmt.Sprintf("[%s]", strings.Join(filters.Tags, ",")))`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 224: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 225: `if filters.Status != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 226: `query = query.Where("status = ?", *filters.Status)`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 227: `} else {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 228: `query = query.Where("status = ?", "active")`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 229: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 230: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 231: `// Apply pagination`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 232: `query = query.Offset(filters.Offset).Limit(filters.Limit)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 233: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 234: `// Order by rating and review count`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 235: `query = query.Order("rating DESC, review_count DESC")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 236: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 237: `if err := query.Find(&products).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 238: `s.logger.WithError(err).Error("Failed to search products")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 239: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to search products"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 240: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 241: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 242: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 243: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 244: `"products": products,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 245: `"total":    len(products),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 246: `"filters": filters,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 247: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 248: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 249: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 250: `func (s *Service) CreateProduct(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 251: `var req CreateProductRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 252: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 253: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 254: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 255: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 256: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 257: `product := Product{`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 258: `Type:         req.Type,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 259: `Name:         req.Name,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 260: `Description:  req.Description,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 261: `Price:        req.Price,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 262: `OriginalPrice: req.OriginalPrice,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 263: `Inventory:    req.Inventory,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 264: `Tags:         req.Tags,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 265: `Metadata:     req.Metadata,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 266: `Status:       req.Status,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 267: `Images:       req.Images,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 268: `Location:     req.Location,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 269: `Rating:       0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 270: `ReviewCount:  0,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 271: `CreatedAt:    time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 272: `UpdatedAt:    time.Now(),`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 273: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 274: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 275: `if err := s.db.Create(&product).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 276: `s.logger.WithError(err).Error("Failed to create product")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 277: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to create product"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 278: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 279: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 280: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 281: `s.logger.WithField("product_id", product.ID).Info("Product created successfully")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 282: `c.JSON(http.StatusCreated, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 283: `"message": "Product created successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 284: `"product": product,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 285: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 286: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 287: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 288: `func (s *Service) ListProducts(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 289: `var products []Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 290: `query := s.db.Model(&Product{}).Where("status = ?", "active")`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 291: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 292: `if err := query.Find(&products).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 293: `s.logger.WithError(err).Error("Failed to list products")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 294: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to list products"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 295: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 296: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 297: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 298: `// 填充Images和Location（兼容旧数据）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 299: `for i := range products {`
  - 作用：循环处理集合数据。
  - 原因：批量处理逻辑时保持代码集中。
- 300: `// 如果Images为空，用CoverImage填充`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 301: `if len(products[i].Images) == 0 && products[i].CoverImage != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 302: `products[i].Images = []string{products[i].CoverImage}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 303: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 304: `// 如果Location为空但有Subtitle，尝试提取城市名`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 305: `if products[i].Location.City == "" && products[i].Subtitle != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 306: `city := strings.TrimSuffix(products[i].Subtitle, "推荐")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 307: `products[i].Location = Location{City: city}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 308: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 309: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 310: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 311: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 312: `"products": products,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 313: `"total":    len(products),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 314: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 315: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 316: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 317: `func (s *Service) GetProduct(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 318: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 319: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 320: `var product Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 321: `if err := s.db.First(&product, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 322: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 323: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 324: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 325: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 326: `// 填充Images（兼容旧数据）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 327: `if len(product.Images) == 0 && product.CoverImage != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 328: `product.Images = []string{product.CoverImage}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 329: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 330: `// 填充Location`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 331: `if product.Location.City == "" && product.Subtitle != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 332: `city := strings.TrimSuffix(product.Subtitle, "推荐")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 333: `product.Location = Location{City: city}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 334: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 335: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 336: `c.JSON(http.StatusOK, product)`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 337: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 338: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 339: `func (s *Service) UpdateProduct(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 340: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 341: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 342: `var req UpdateProductRequest`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 343: `if err := c.ShouldBindJSON(&req); err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 344: `c.JSON(http.StatusBadRequest, gin.H{"error": err.Error()})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 345: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 346: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 347: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 348: `var product Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 349: `if err := s.db.First(&product, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 350: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 351: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 352: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 353: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 354: `// Update fields`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 355: `if req.Type != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 356: `product.Type = *req.Type`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 357: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 358: `if req.Name != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 359: `product.Name = *req.Name`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 360: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 361: `if req.Description != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 362: `product.Description = *req.Description`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 363: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 364: `if req.Price != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 365: `product.Price = *req.Price`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 366: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 367: `if req.OriginalPrice != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 368: `product.OriginalPrice = req.OriginalPrice`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 369: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 370: `if req.Inventory != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 371: `product.Inventory = *req.Inventory`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 372: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 373: `if len(req.Tags) > 0 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 374: `product.Tags = req.Tags`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 375: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 376: `if req.Metadata != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 377: `product.Metadata = *req.Metadata`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 378: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 379: `if req.Status != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 380: `product.Status = *req.Status`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 381: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 382: `if len(req.Images) > 0 {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 383: `product.Images = req.Images`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 384: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 385: `if req.Location != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 386: `product.Location = *req.Location`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 387: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 388: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 389: `product.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 390: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 391: `if err := s.db.Save(&product).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 392: `s.logger.WithError(err).Error("Failed to update product")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 393: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to update product"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 394: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 395: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 396: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 397: `s.logger.WithField("product_id", product.ID).Info("Product updated successfully")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 398: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 399: `"message": "Product updated successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 400: `"product": product,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 401: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 402: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 403: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 404: `func (s *Service) DeleteProduct(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 405: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 406: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 407: `var product Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 408: `if err := s.db.First(&product, id).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 409: `c.JSON(http.StatusNotFound, gin.H{"error": "Product not found"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 410: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 411: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 412: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 413: `// Soft delete by setting status to inactive`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 414: `product.Status = "inactive"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 415: `product.UpdatedAt = time.Now()`
  - 作用：获取当前时间。
  - 原因：时间戳用于审计、排序和幂等控制。
- 416: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 417: `if err := s.db.Save(&product).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 418: `s.logger.WithError(err).Error("Failed to delete product")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 419: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to delete product"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 420: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 421: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 422: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 423: `s.logger.WithField("product_id", product.ID).Info("Product deleted successfully")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 424: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 425: `"message": "Product deleted successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 426: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 427: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 428: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 429: `func (s *Service) GetPopularProducts(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 430: `var products []Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 431: `limit := 20`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 432: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 433: `if l := c.DefaultQuery("limit", "20"); l != "" {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 434: `fmt.Sscanf(l, "%d", &limit)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 435: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 436: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 437: `query := s.db.Model(&Product{}).`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 438: `Where("status = ?", "active").`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 439: `Order("rating DESC, review_count DESC").`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 440: `Limit(limit)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 441: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 442: `if err := query.Find(&products).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 443: `s.logger.WithError(err).Error("Failed to get popular products")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 444: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get popular products"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 445: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 446: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 447: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 448: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 449: `"products": products,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 450: `"total":    len(products),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 451: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 452: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 453: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 454: `func (s *Service) GetRecommendations(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 455: `userID := c.Param("user_id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 456: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 457: `// TODO: Implement recommendation logic based on user preferences and behavior`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 458: `// For now, return popular products`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 459: `var products []Product`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 460: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 461: `query := s.db.Model(&Product{}).`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 462: `Where("status = ?", "active").`
  - 作用：执行数据库读写。
  - 原因：数据访问集中有助于后续优化索引与事务。
- 463: `Order("rating DESC, review_count DESC").`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 464: `Limit(10)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 465: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 466: `if err := query.Find(&products).Error; err != nil {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 467: `s.logger.WithError(err).Error("Failed to get recommendations")`
  - 作用：记录结构化日志。
  - 原因：线上问题可按字段检索，缩短定位时间。
- 468: `c.JSON(http.StatusInternalServerError, gin.H{"error": "Failed to get recommendations"})`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 469: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 470: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 471: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 472: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 473: `"products": products,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 474: `"user_id":   userID,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 475: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 476: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 477: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 478: `func (s *Service) IncrementViews(c *gin.Context) {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 479: `id := c.Param("id")`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 480: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 481: `// TODO: Implement view counting logic`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 482: `// For now, just return success`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 483: `c.JSON(http.StatusOK, gin.H{`
  - 作用：返回接口响应。
  - 原因：统一响应格式便于前端消费与错误兜底。
- 484: `"message": "Views incremented successfully",`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 485: `"product_id": id,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 486: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 487: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
