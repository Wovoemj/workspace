# `frontend/user-web/src/lib/aiAgentSettings.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `export type KbFileStatus = 'processing' | 'ready' | 'failed'` -> 导出当前声明，供其他模块复用。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `export type KbFile = {` -> 导出当前声明，供其他模块复用。
- 4: `id: string` -> 执行当前语句，参与该文件整体逻辑。
- 5: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 6: `size: number` -> 执行当前语句，参与该文件整体逻辑。
- 7: `status: KbFileStatus` -> 执行当前语句，参与该文件整体逻辑。
- 8: `uploadedAt: number` -> 执行当前语句，参与该文件整体逻辑。
- 9: `}` -> 结束当前语句或代码块。
- 10: `(空行)` -> 空行，用于提升代码结构可读性。
- 11: `export type WebToolKey = 'brave' | 'news' | 'weather'` -> 导出当前声明，供其他模块复用。
- 12: `(空行)` -> 空行，用于提升代码结构可读性。
- 13: `export type AiAgentSettingsState = {` -> 导出当前声明，供其他模块复用。
- 14: `systemPrompt: string` -> 执行当前语句，参与该文件整体逻辑。
- 15: `welcomeMessage: string` -> 执行当前语句，参与该文件整体逻辑。
- 16: `modelId: string` -> 执行当前语句，参与该文件整体逻辑。
- 17: `filterRules: string` -> 执行当前语句，参与该文件整体逻辑。
- 18: `webTools: Record<WebToolKey, boolean>` -> 执行当前语句，参与该文件整体逻辑。
- 19: `digitalHumanConfigured: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 20: `voiceCloneName: string` -> 执行当前语句，参与该文件整体逻辑。
- 21: `stats: { calls: number; tokens: number; estCost: number }` -> 执行当前语句，参与该文件整体逻辑。
- 22: `kbFiles: KbFile[]` -> 执行当前语句，参与该文件整体逻辑。
- 23: `}` -> 结束当前语句或代码块。
- 24: `(空行)` -> 空行，用于提升代码结构可读性。
- 25: `const STORAGE_KEY = 'ai_agent_settings_v1'` -> 声明变量或常量，保存运行时数据。
- 26: `(空行)` -> 空行，用于提升代码结构可读性。
- 27: `export const MODEL_OPTIONS: Array<{ id: string; label: string }> = [` -> 导出当前声明，供其他模块复用。
- 28: `{ id: 'mimo-v2-flash', label: 'Xiaomi MiMo V2 Flash' },` -> 执行当前语句，参与该文件整体逻辑。
- 29: `{ id: 'mimo-v2-pro', label: 'Xiaomi MiMo V2 Pro' },` -> 执行当前语句，参与该文件整体逻辑。
- 30: `{ id: 'mimo-v2-omni', label: 'Xiaomi MiMo V2 Omni' },` -> 执行当前语句，参与该文件整体逻辑。
- 31: `{ id: 'gpt-4o', label: 'GPT-4o（需后端配置）' },` -> 执行当前语句，参与该文件整体逻辑。
- 32: `]` -> 执行当前语句，参与该文件整体逻辑。
- 33: `(空行)` -> 空行，用于提升代码结构可读性。
- 34: `export const DEFAULT_SETTINGS: AiAgentSettingsState = {` -> 导出当前声明，供其他模块复用。
- 35: `systemPrompt:` -> 执行当前语句，参与该文件整体逻辑。
- 36: `'你是「智旅助手」的旅行助手：语气友好、条理清晰；不确定时主动说明假设；涉及价格政策时提醒以官方为准。【重要】你拥有知识库功能，优先根据已上传的文档作答；文档未覆盖的内容再使用你的通用知识，并在回答时说明。',` -> 执行当前语句，参与该文件整体逻辑。
- 37: `welcomeMessage: '你好！我是你的旅行助手，可以帮你规划行程、推荐目的地与估算预算，知识库已默认开启，随时可以向我提问！',` -> 执行当前语句，参与该文件整体逻辑。
- 38: `modelId: 'mimo-v2-flash',` -> 执行当前语句，参与该文件整体逻辑。
- 39: `filterRules: '',` -> 执行当前语句，参与该文件整体逻辑。
- 40: `webTools: { brave: true, news: true, weather: true },` -> 执行当前语句，参与该文件整体逻辑。
- 41: `digitalHumanConfigured: false,` -> 执行当前语句，参与该文件整体逻辑。
- 42: `voiceCloneName: '',` -> 执行当前语句，参与该文件整体逻辑。
- 43: `stats: { calls: 0, tokens: 0, estCost: 0 },` -> 执行当前语句，参与该文件整体逻辑。
- 44: `kbFiles: [],` -> 执行当前语句，参与该文件整体逻辑。
- 45: `}` -> 结束当前语句或代码块。
- 46: `(空行)` -> 空行，用于提升代码结构可读性。
- 47: `export function loadAiAgentSettings(): AiAgentSettingsState {` -> 导出当前声明，供其他模块复用。
- 48: `if (typeof window === 'undefined') return DEFAULT_SETTINGS` -> 条件判断分支，根据场景执行不同逻辑。
- 49: `try {` -> 异常处理逻辑，提升程序健壮性。
- 50: `const raw = localStorage.getItem(STORAGE_KEY)` -> 声明变量或常量，保存运行时数据。
- 51: `if (!raw) return { ...DEFAULT_SETTINGS }` -> 条件判断分支，根据场景执行不同逻辑。
- 52: `const p = JSON.parse(raw) as Partial<AiAgentSettingsState>` -> 声明变量或常量，保存运行时数据。
- 53: `return {` -> 返回结果或提前结束当前流程。
- 54: `...DEFAULT_SETTINGS,` -> 执行当前语句，参与该文件整体逻辑。
- 55: `...p,` -> 执行当前语句，参与该文件整体逻辑。
- 56: `webTools: { ...DEFAULT_SETTINGS.webTools, ...(p.webTools || {}) },` -> 执行当前语句，参与该文件整体逻辑。
- 57: `stats: { ...DEFAULT_SETTINGS.stats, ...(p.stats || {}) },` -> 执行当前语句，参与该文件整体逻辑。
- 58: `kbFiles: Array.isArray(p.kbFiles) ? p.kbFiles : [],` -> 执行当前语句，参与该文件整体逻辑。
- 59: `}` -> 结束当前语句或代码块。
- 60: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 61: `return { ...DEFAULT_SETTINGS }` -> 返回结果或提前结束当前流程。
- 62: `}` -> 结束当前语句或代码块。
- 63: `}` -> 结束当前语句或代码块。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `export function saveAiAgentSettings(s: AiAgentSettingsState) {` -> 导出当前声明，供其他模块复用。
- 66: `if (typeof window === 'undefined') return` -> 条件判断分支，根据场景执行不同逻辑。
- 67: `localStorage.setItem(STORAGE_KEY, JSON.stringify(s))` -> 执行当前语句，参与该文件整体逻辑。
- 68: `}` -> 结束当前语句或代码块。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `export function incrementStats(s: AiAgentSettingsState, tokensApprox: number): AiAgentSettingsState {` -> 导出当前声明，供其他模块复用。
- 71: `return {` -> 返回结果或提前结束当前流程。
- 72: `...s,` -> 执行当前语句，参与该文件整体逻辑。
- 73: `stats: {` -> 执行当前语句，参与该文件整体逻辑。
- 74: `calls: s.stats.calls + 1,` -> 执行当前语句，参与该文件整体逻辑。
- 75: `tokens: s.stats.tokens + tokensApprox,` -> 执行当前语句，参与该文件整体逻辑。
- 76: `estCost: Math.round((s.stats.estCost + tokensApprox * 0.00002) * 10000) / 10000,` -> 执行当前语句，参与该文件整体逻辑。
- 77: `},` -> 执行当前语句，参与该文件整体逻辑。
- 78: `}` -> 结束当前语句或代码块。
- 79: `}` -> 结束当前语句或代码块。
