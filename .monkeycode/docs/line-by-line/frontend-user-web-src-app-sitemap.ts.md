# `frontend/user-web/src/app/sitemap.ts` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `import { MetadataRoute } from 'next'` -> 导入依赖模块，供当前文件使用。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `// 静态页面路?` -> 注释行，用于解释设计意图或使用说明。
- 4: `const staticPaths = [` -> 声明变量或常量，保存运行时数据。
- 5: `'/',` -> 执行当前语句，参与该文件整体逻辑。
- 6: `'/about',` -> 执行当前语句，参与该文件整体逻辑。
- 7: `'/contact',` -> 执行当前语句，参与该文件整体逻辑。
- 8: `'/privacy',` -> 执行当前语句，参与该文件整体逻辑。
- 9: `'/terms',` -> 执行当前语句，参与该文件整体逻辑。
- 10: `'/careers',` -> 执行当前语句，参与该文件整体逻辑。
- 11: `'/partners',` -> 执行当前语句，参与该文件整体逻辑。
- 12: `'/services',` -> 执行当前语句，参与该文件整体逻辑。
- 13: `'/help',` -> 执行当前语句，参与该文件整体逻辑。
- 14: `'/feedback',` -> 执行当前语句，参与该文件整体逻辑。
- 15: `'/news',` -> 执行当前语句，参与该文件整体逻辑。
- 16: `'/deals',` -> 执行当前语句，参与该文件整体逻辑。
- 17: `'/destinations',` -> 执行当前语句，参与该文件整体逻辑。
- 18: `'/planner',` -> 执行当前语句，参与该文件整体逻辑。
- 19: `'/assistant'` -> 执行当前语句，参与该文件整体逻辑。
- 20: `]` -> 执行当前语句，参与该文件整体逻辑。
- 21: `(空行)` -> 空行，用于提升代码结构可读性。
- 22: `// 获取动态路?` -> 注释行，用于解释设计意图或使用说明。
- 23: `async function getDynamicPaths() {` -> 定义函数或方法，实现具体业务逻辑。
- 24: `try {` -> 异常处理逻辑，提升程序健壮性。
- 25: `// 获取所有目的地` -> 注释行，用于解释设计意图或使用说明。
- 26: `const destinationsRes = await fetch(` -> 声明变量或常量，保存运行时数据。
- 27: `\`${process.env.NEXT_PUBLIC_API_BASE_URL || 'http://127.0.0.1:5001'}/api/destinations?per_page=100\`,` -> 执行当前语句，参与该文件整体逻辑。
- 28: `{ cache: 'force-cache' }` -> 执行当前语句，参与该文件整体逻辑。
- 29: `)` -> 结束当前语句或代码块。
- 30: `const destinationsData = await destinationsRes.json()` -> 声明变量或常量，保存运行时数据。
- 31: `const destinations = destinationsData?.destinations || destinationsData?.items || []` -> 声明变量或常量，保存运行时数据。
- 32: `(空行)` -> 空行，用于提升代码结构可读性。
- 33: `const destinationPaths = destinations.map((destination: any) => ({` -> 声明变量或常量，保存运行时数据。
- 34: `url: \`/destinations/${destination.id}\`,` -> 执行当前语句，参与该文件整体逻辑。
- 35: `lastModified: new Date(),` -> 执行当前语句，参与该文件整体逻辑。
- 36: `changeFrequency: 'weekly' as const,` -> 执行当前语句，参与该文件整体逻辑。
- 37: `priority: 0.8` -> 执行当前语句，参与该文件整体逻辑。
- 38: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 39: `(空行)` -> 空行，用于提升代码结构可读性。
- 40: `// 获取所有产?` -> 注释行，用于解释设计意图或使用说明。
- 41: `const productsRes = await fetch(` -> 声明变量或常量，保存运行时数据。
- 42: `\`${process.env.NEXT_PUBLIC_API_BASE_URL || 'http://127.0.0.1:5001'}/api/products?status=active&limit=100\`,` -> 执行当前语句，参与该文件整体逻辑。
- 43: `{ cache: 'force-cache' }` -> 执行当前语句，参与该文件整体逻辑。
- 44: `)` -> 结束当前语句或代码块。
- 45: `const productsData = await productsRes.json()` -> 声明变量或常量，保存运行时数据。
- 46: `const products = productsData?.products || productsData?.items || []` -> 声明变量或常量，保存运行时数据。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `const productPaths = products.map((product: any) => ({` -> 声明变量或常量，保存运行时数据。
- 49: `url: \`/products/${product.id}\`,` -> 执行当前语句，参与该文件整体逻辑。
- 50: `lastModified: new Date(),` -> 执行当前语句，参与该文件整体逻辑。
- 51: `changeFrequency: 'weekly' as const,` -> 执行当前语句，参与该文件整体逻辑。
- 52: `priority: 0.7` -> 执行当前语句，参与该文件整体逻辑。
- 53: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 54: `(空行)` -> 空行，用于提升代码结构可读性。
- 55: `return [...destinationPaths, ...productPaths]` -> 返回结果或提前结束当前流程。
- 56: `} catch (error) {` -> 执行当前语句，参与该文件整体逻辑。
- 57: `console.warn('Failed to fetch dynamic paths for sitemap:', error)` -> 执行当前语句，参与该文件整体逻辑。
- 58: `// 返回空数组而不是抛出错误，这样sitemap仍能生成` -> 注释行，用于解释设计意图或使用说明。
- 59: `return []` -> 返回结果或提前结束当前流程。
- 60: `}` -> 结束当前语句或代码块。
- 61: `}` -> 结束当前语句或代码块。
- 62: `(空行)` -> 空行，用于提升代码结构可读性。
- 63: `export default async function sitemap(): Promise<MetadataRoute.Sitemap> {` -> 导出当前声明，供其他模块复用。
- 64: `const dynamicPaths = await getDynamicPaths()` -> 声明变量或常量，保存运行时数据。
- 65: `(空行)` -> 空行，用于提升代码结构可读性。
- 66: `const staticSitemap = staticPaths.map(path => ({` -> 声明变量或常量，保存运行时数据。
- 67: `url: \`${process.env.NEXT_PUBLIC_SITE_URL}${path}\`,` -> 执行当前语句，参与该文件整体逻辑。
- 68: `lastModified: new Date(),` -> 执行当前语句，参与该文件整体逻辑。
- 69: `changeFrequency: 'monthly' as const,` -> 执行当前语句，参与该文件整体逻辑。
- 70: `priority: path === '/' ? 1 : 0.5` -> 执行当前语句，参与该文件整体逻辑。
- 71: `}))` -> 执行当前语句，参与该文件整体逻辑。
- 72: `(空行)` -> 空行，用于提升代码结构可读性。
- 73: `return [...staticSitemap, ...dynamicPaths]` -> 返回结果或提前结束当前流程。
- 74: `}` -> 结束当前语句或代码块。
