# `frontend/user-web/src/app/products/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 产品列表页模块 - 旅行产品浏览与筛选` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 6: `*   - 展示所有旅行产品（门票、酒店、体验等）` -> 注释行，用于解释设计意图或使用说明。
- 7: `*   - 多维度筛选：类别、价格区间、时长、适合人群` -> 注释行，用于解释设计意图或使用说明。
- 8: `*   - 排序功能：智能推荐、人气、评分、价格` -> 注释行，用于解释设计意图或使用说明。
- 9: `*   - 移动端适配的筛选面板` -> 注释行，用于解释设计意图或使用说明。
- 10: `*` -> 注释行，用于解释设计意图或使用说明。
- 11: `* 依赖项：` -> 注释行，用于解释设计意图或使用说明。
- 12: `*   - components/Navbar：顶部导航栏` -> 注释行，用于解释设计意图或使用说明。
- 13: `*   - components/Footer：底部页脚` -> 注释行，用于解释设计意图或使用说明。
- 14: `*   - components/ProductCard：产品卡片` -> 注释行，用于解释设计意图或使用说明。
- 15: `*   - lucide-react：图标库` -> 注释行，用于解释设计意图或使用说明。
- 16: `*` -> 注释行，用于解释设计意图或使用说明。
- 17: `* 数据来源：` -> 注释行，用于解释设计意图或使用说明。
- 18: `*   - GET /api/products：产品列表` -> 注释行，用于解释设计意图或使用说明。
- 19: `*     - 参数：page, per_page, status, category, sort_by, min_price, max_price` -> 注释行，用于解释设计意图或使用说明。
- 20: `*` -> 注释行，用于解释设计意图或使用说明。
- 21: `* 筛选选项：` -> 注释行，用于解释设计意图或使用说明。
- 22: `*   - CATEGORIES：8种产品类别` -> 注释行，用于解释设计意图或使用说明。
- 23: `*   - PRICE_RANGES：4档价格区间` -> 注释行，用于解释设计意图或使用说明。
- 24: `*   - TAG_OPTIONS：8种适合人群标签` -> 注释行，用于解释设计意图或使用说明。
- 25: `*/` -> 注释行，用于解释设计意图或使用说明。
- 26: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 27: `(空行)` -> 空行，用于提升代码结构可读性。
- 28: `import { useState, useEffect, useCallback } from 'react'` -> 导入依赖模块，供当前文件使用。
- 29: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 30: `import { useSearchParams, useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 31: `import {` -> 导入依赖模块，供当前文件使用。
- 32: `Search, Filter, SlidersHorizontal, ChevronLeft, ChevronRight,` -> 执行当前语句，参与该文件整体逻辑。
- 33: `Star, ShoppingCart, MapPin, Calendar, Clock, Users,` -> 执行当前语句，参与该文件整体逻辑。
- 34: `ChevronDown, Loader2, X, Tag, DollarSign, TrendingUp,` -> 执行当前语句，参与该文件整体逻辑。
- 35: `Sparkles, Award, Shield, Heart, Zap, Globe` -> 执行当前语句，参与该文件整体逻辑。
- 36: `} from 'lucide-react'` -> 执行当前语句，参与该文件整体逻辑。
- 37: `import { ProductCard } from '@/components/ProductCard'` -> 导入依赖模块，供当前文件使用。
- 38: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 39: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 40: `import { Product } from '@/types'` -> 导入依赖模块，供当前文件使用。
- 41: `(空行)` -> 空行，用于提升代码结构可读性。
- 42: `// API 返回的产品类型（可能有额外字段）` -> 注释行，用于解释设计意图或使用说明。
- 43: `interface ApiProduct {` -> 定义类型或类结构，约束数据与行为。
- 44: `id: number | string` -> 执行当前语句，参与该文件整体逻辑。
- 45: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 46: `subtitle?: string` -> 执行当前语句，参与该文件整体逻辑。
- 47: `description?: string` -> 执行当前语句，参与该文件整体逻辑。
- 48: `category?: string` -> 执行当前语句，参与该文件整体逻辑。
- 49: `base_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 50: `discount_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 51: `cover_image?: string` -> 执行当前语句，参与该文件整体逻辑。
- 52: `images?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 53: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 54: `review_count?: number` -> 执行当前语句，参与该文件整体逻辑。
- 55: `sold_count?: number` -> 执行当前语句，参与该文件整体逻辑。
- 56: `location?: string | { city: string; country: string }` -> 执行当前语句，参与该文件整体逻辑。
- 57: `duration?: string` -> 执行当前语句，参与该文件整体逻辑。
- 58: `tags?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 59: `inventory_total?: number` -> 执行当前语句，参与该文件整体逻辑。
- 60: `inventory_sold?: number` -> 执行当前语句，参与该文件整体逻辑。
- 61: `status?: string` -> 执行当前语句，参与该文件整体逻辑。
- 62: `created_at?: string` -> 执行当前语句，参与该文件整体逻辑。
- 63: `updated_at?: string` -> 执行当前语句，参与该文件整体逻辑。
- 64: `type?: Product['type']` -> 执行当前语句，参与该文件整体逻辑。
- 65: `destination_id?: number` -> 执行当前语句，参与该文件整体逻辑。
- 66: `booking_type?: string` -> 执行当前语句，参与该文件整体逻辑。
- 67: `need_date?: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 68: `need_time?: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 69: `price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 70: `original_price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 71: `inventory?: number` -> 执行当前语句，参与该文件整体逻辑。
- 72: `metadata?: Product['metadata']` -> 执行当前语句，参与该文件整体逻辑。
- 73: `}` -> 结束当前语句或代码块。
- 74: `(空行)` -> 空行，用于提升代码结构可读性。
- 75: `interface ApiResponse {` -> 定义类型或类结构，约束数据与行为。
- 76: `success: boolean` -> 执行当前语句，参与该文件整体逻辑。
- 77: `products: ApiProduct[]` -> 执行当前语句，参与该文件整体逻辑。
- 78: `total: number` -> 执行当前语句，参与该文件整体逻辑。
- 79: `page: number` -> 执行当前语句，参与该文件整体逻辑。
- 80: `per_page: number` -> 执行当前语句，参与该文件整体逻辑。
- 81: `total_pages: number` -> 执行当前语句，参与该文件整体逻辑。
- 82: `}` -> 结束当前语句或代码块。
- 83: `(空行)` -> 空行，用于提升代码结构可读性。
- 84: `// 将 API 产品转换为 ProductCard 需要的格式` -> 注释行，用于解释设计意图或使用说明。
- 85: `function toProductCardProduct(p: ApiProduct): Product {` -> 定义函数或方法，实现具体业务逻辑。
- 86: `return {` -> 返回结果或提前结束当前流程。
- 87: `id: String(p.id),` -> 执行当前语句，参与该文件整体逻辑。
- 88: `type: p.type || 'ticket',` -> 执行当前语句，参与该文件整体逻辑。
- 89: `name: p.name,` -> 执行当前语句，参与该文件整体逻辑。
- 90: `description: p.description || p.subtitle || '',` -> 执行当前语句，参与该文件整体逻辑。
- 91: `price: p.price || p.base_price || p.discount_price || 0,` -> 执行当前语句，参与该文件整体逻辑。
- 92: `original_price: p.original_price || p.base_price,` -> 执行当前语句，参与该文件整体逻辑。
- 93: `inventory: p.inventory ?? p.inventory_total ?? 0,` -> 执行当前语句，参与该文件整体逻辑。
- 94: `tags: p.tags || [],` -> 执行当前语句，参与该文件整体逻辑。
- 95: `metadata: p.metadata || {},` -> 执行当前语句，参与该文件整体逻辑。
- 96: `status: (p.status === 'active' || p.status === 'inactive' || p.status === 'sold_out')` -> 执行当前语句，参与该文件整体逻辑。
- 97: `? p.status` -> 执行当前语句，参与该文件整体逻辑。
- 98: `: p.status === 'active' ? 'active' : 'inactive',` -> 执行当前语句，参与该文件整体逻辑。
- 99: `images: (p.images && p.images.length > 0) ? p.images : [p.cover_image].filter(Boolean) as string[],` -> 执行当前语句，参与该文件整体逻辑。
- 100: `cover_image: p.cover_image,` -> 执行当前语句，参与该文件整体逻辑。
- 101: `location: typeof p.location === 'string'` -> 执行当前语句，参与该文件整体逻辑。
- 102: `? { city: p.location, country: '中国', coordinates: { lat: 0, lng: 0 } }` -> 执行当前语句，参与该文件整体逻辑。
- 103: `: p.location` -> 执行当前语句，参与该文件整体逻辑。
- 104: `? { city: p.location.city || '', country: p.location.country || '中国', coordinates: { lat: 0, lng: 0 } }` -> 执行当前语句，参与该文件整体逻辑。
- 105: `: { city: '', country: '中国', coordinates: { lat: 0, lng: 0 } },` -> 执行当前语句，参与该文件整体逻辑。
- 106: `rating: p.rating || 0,` -> 执行当前语句，参与该文件整体逻辑。
- 107: `review_count: p.review_count || 0,` -> 执行当前语句，参与该文件整体逻辑。
- 108: `}` -> 结束当前语句或代码块。
- 109: `}` -> 结束当前语句或代码块。
- 110: `(空行)` -> 空行，用于提升代码结构可读性。
- 111: `// 图标组件` -> 注释行，用于解释设计意图或使用说明。
- 112: `const TicketIcon = () => <div className="w-5 h-5">🎫</div>` -> 声明变量或常量，保存运行时数据。
- 113: `const HotelIcon = () => <div className="w-5 h-5">🏨</div>` -> 声明变量或常量，保存运行时数据。
- 114: `const TrainIcon = () => <div className="w-5 h-5">🚄</div>` -> 声明变量或常量，保存运行时数据。
- 115: `const FoodIcon = () => <div className="w-5 h-5">🍜</div>` -> 声明变量或常量，保存运行时数据。
- 116: `const ShoppingIcon = () => <div className="w-5 h-5">🛍️</div>` -> 声明变量或常量，保存运行时数据。
- 117: `const ExperienceIcon = () => <div className="w-5 h-5">🎯</div>` -> 声明变量或常量，保存运行时数据。
- 118: `const TourIcon = () => <div className="w-5 h-5">👥</div>` -> 声明变量或常量，保存运行时数据。
- 119: `(空行)` -> 空行，用于提升代码结构可读性。
- 120: `// 筛选选项` -> 注释行，用于解释设计意图或使用说明。
- 121: `const CATEGORIES = [` -> 声明变量或常量，保存运行时数据。
- 122: `{ id: 'all', label: '全部类别', icon: Tag },` -> 执行当前语句，参与该文件整体逻辑。
- 123: `{ id: 'ticket', label: '景点门票', icon: TicketIcon },` -> 执行当前语句，参与该文件整体逻辑。
- 124: `{ id: 'experience', label: '体验活动', icon: Zap },` -> 执行当前语句，参与该文件整体逻辑。
- 125: `{ id: 'tour', label: '跟团游', icon: Users },` -> 执行当前语句，参与该文件整体逻辑。
- 126: `{ id: 'hotel', label: '酒店住宿', icon: HotelIcon },` -> 执行当前语句，参与该文件整体逻辑。
- 127: `{ id: 'transport', label: '交通套票', icon: TrainIcon },` -> 执行当前语句，参与该文件整体逻辑。
- 128: `{ id: 'food', label: '美食套餐', icon: FoodIcon },` -> 执行当前语句，参与该文件整体逻辑。
- 129: `{ id: 'shopping', label: '购物优惠', icon: ShoppingCart }` -> 执行当前语句，参与该文件整体逻辑。
- 130: `]` -> 执行当前语句，参与该文件整体逻辑。
- 131: `(空行)` -> 空行，用于提升代码结构可读性。
- 132: `// 价格区间` -> 注释行，用于解释设计意图或使用说明。
- 133: `const PRICE_RANGES = [` -> 声明变量或常量，保存运行时数据。
- 134: `{ id: 'all', label: '所有价格', min: null, max: null },` -> 执行当前语句，参与该文件整体逻辑。
- 135: `{ id: 'budget', label: '经济实惠 (¥0-200)', min: 0, max: 200 },` -> 执行当前语句，参与该文件整体逻辑。
- 136: `{ id: 'midrange', label: '中等价位 (¥200-500)', min: 200, max: 500 },` -> 执行当前语句，参与该文件整体逻辑。
- 137: `{ id: 'premium', label: '高端体验 (¥500-1000)', min: 500, max: 1000 },` -> 执行当前语句，参与该文件整体逻辑。
- 138: `{ id: 'luxury', label: '奢华享受 (¥1000+)', min: 1000, max: null }` -> 执行当前语句，参与该文件整体逻辑。
- 139: `]` -> 执行当前语句，参与该文件整体逻辑。
- 140: `(空行)` -> 空行，用于提升代码结构可读性。
- 141: `// 排序选项` -> 注释行，用于解释设计意图或使用说明。
- 142: `const SORT_OPTIONS = [` -> 声明变量或常量，保存运行时数据。
- 143: `{ id: 'recommended', label: '智能推荐', icon: Sparkles },` -> 执行当前语句，参与该文件整体逻辑。
- 144: `{ id: 'popular', label: '人气最高', icon: TrendingUp },` -> 执行当前语句，参与该文件整体逻辑。
- 145: `{ id: 'rating', label: '评分最高', icon: Star },` -> 执行当前语句，参与该文件整体逻辑。
- 146: `{ id: 'newest', label: '最新发布', icon: Award },` -> 执行当前语句，参与该文件整体逻辑。
- 147: `{ id: 'price_asc', label: '价格从低到高', icon: DollarSign },` -> 执行当前语句，参与该文件整体逻辑。
- 148: `{ id: 'price_desc', label: '价格从高到低', icon: DollarSign }` -> 执行当前语句，参与该文件整体逻辑。
- 149: `]` -> 执行当前语句，参与该文件整体逻辑。
- 150: `(空行)` -> 空行，用于提升代码结构可读性。
- 151: `// 时长选项` -> 注释行，用于解释设计意图或使用说明。
- 152: `const DURATION_OPTIONS = [` -> 声明变量或常量，保存运行时数据。
- 153: `{ id: 'all', label: '所有时长' },` -> 执行当前语句，参与该文件整体逻辑。
- 154: `{ id: 'short', label: '短时体验 (<2小时)' },` -> 执行当前语句，参与该文件整体逻辑。
- 155: `{ id: 'half_day', label: '半日游 (2-4小时)' },` -> 执行当前语句，参与该文件整体逻辑。
- 156: `{ id: 'full_day', label: '一日游 (4-8小时)' },` -> 执行当前语句，参与该文件整体逻辑。
- 157: `{ id: 'multi_day', label: '多日游 (>8小时)' }` -> 执行当前语句，参与该文件整体逻辑。
- 158: `]` -> 执行当前语句，参与该文件整体逻辑。
- 159: `(空行)` -> 空行，用于提升代码结构可读性。
- 160: `// 标签选项` -> 注释行，用于解释设计意图或使用说明。
- 161: `const TAG_OPTIONS = [` -> 声明变量或常量，保存运行时数据。
- 162: `{ id: 'family', label: '亲子友好', color: 'bg-pink-100 text-pink-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 163: `{ id: 'couple', label: '情侣浪漫', color: 'bg-red-100 text-red-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 164: `{ id: 'group', label: '团队活动', color: 'bg-blue-100 text-blue-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 165: `{ id: 'solo', label: '独自旅行', color: 'bg-green-100 text-green-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 166: `{ id: 'photography', label: '摄影打卡', color: 'bg-purple-100 text-purple-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 167: `{ id: 'adventure', label: '冒险刺激', color: 'bg-orange-100 text-orange-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 168: `{ id: 'relax', label: '休闲放松', color: 'bg-teal-100 text-teal-800' },` -> 执行当前语句，参与该文件整体逻辑。
- 169: `{ id: 'cultural', label: '文化体验', color: 'bg-amber-100 text-amber-800' }` -> 执行当前语句，参与该文件整体逻辑。
- 170: `]` -> 执行当前语句，参与该文件整体逻辑。
- 171: `(空行)` -> 空行，用于提升代码结构可读性。
- 172: `export default function ProductsPage() {` -> 导出当前声明，供其他模块复用。
- 173: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 174: `const searchParams = useSearchParams()` -> 声明变量或常量，保存运行时数据。
- 175: `const [products, setProducts] = useState<ApiProduct[]>([])` -> 声明变量或常量，保存运行时数据。
- 176: `const [loading, setLoading] = useState(true)` -> 声明变量或常量，保存运行时数据。
- 177: `const [total, setTotal] = useState(0)` -> 声明变量或常量，保存运行时数据。
- 178: `const [currentPage, setCurrentPage] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 179: `const [totalPages, setTotalPages] = useState(1)` -> 声明变量或常量，保存运行时数据。
- 180: `const [showFilters, setShowFilters] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 181: `const [showMobileFilters, setShowMobileFilters] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 182: `const [sortOpen, setSortOpen] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 183: `(空行)` -> 空行，用于提升代码结构可读性。
- 184: `// 筛选状态` -> 注释行，用于解释设计意图或使用说明。
- 185: `const [filters, setFilters] = useState({` -> 声明变量或常量，保存运行时数据。
- 186: `category: searchParams.get('category') || 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 187: `priceRange: searchParams.get('price') || 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 188: `sortBy: searchParams.get('sort') || 'recommended',` -> 执行当前语句，参与该文件整体逻辑。
- 189: `duration: searchParams.get('duration') || 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 190: `keyword: searchParams.get('keyword') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 191: `minPrice: searchParams.get('minPrice') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 192: `maxPrice: searchParams.get('maxPrice') || '',` -> 执行当前语句，参与该文件整体逻辑。
- 193: `tags: searchParams.get('tags')?.split(',') || []` -> 执行当前语句，参与该文件整体逻辑。
- 194: `})` -> 执行当前语句，参与该文件整体逻辑。
- 195: `(空行)` -> 空行，用于提升代码结构可读性。
- 196: `// 获取产品数据` -> 注释行，用于解释设计意图或使用说明。
- 197: `const fetchProducts = useCallback(async () => {` -> 声明变量或常量，保存运行时数据。
- 198: `setLoading(true)` -> 执行当前语句，参与该文件整体逻辑。
- 199: `try {` -> 异常处理逻辑，提升程序健壮性。
- 200: `// 构建查询参数` -> 注释行，用于解释设计意图或使用说明。
- 201: `const params = new URLSearchParams({` -> 声明变量或常量，保存运行时数据。
- 202: `page: currentPage.toString(),` -> 执行当前语句，参与该文件整体逻辑。
- 203: `per_page: '12',` -> 执行当前语句，参与该文件整体逻辑。
- 204: `status: 'active'` -> 执行当前语句，参与该文件整体逻辑。
- 205: `})` -> 执行当前语句，参与该文件整体逻辑。
- 206: `(空行)` -> 空行，用于提升代码结构可读性。
- 207: `// 添加筛选参数` -> 注释行，用于解释设计意图或使用说明。
- 208: `if (filters.category !== 'all') params.append('type', filters.category)` -> 条件判断分支，根据场景执行不同逻辑。
- 209: `if (filters.sortBy !== 'recommended') params.append('sort_by', filters.sortBy)` -> 条件判断分支，根据场景执行不同逻辑。
- 210: `if (filters.duration !== 'all') params.append('duration', filters.duration)` -> 条件判断分支，根据场景执行不同逻辑。
- 211: `if (filters.keyword) params.append('keyword', filters.keyword)` -> 条件判断分支，根据场景执行不同逻辑。
- 212: `if (filters.minPrice) params.append('min_price', filters.minPrice)` -> 条件判断分支，根据场景执行不同逻辑。
- 213: `if (filters.maxPrice) params.append('max_price', filters.maxPrice)` -> 条件判断分支，根据场景执行不同逻辑。
- 214: `if (filters.tags.length > 0) params.append('tags', filters.tags.join(','))` -> 条件判断分支，根据场景执行不同逻辑。
- 215: `(空行)` -> 空行，用于提升代码结构可读性。
- 216: `// 添加价格区间筛选` -> 注释行，用于解释设计意图或使用说明。
- 217: `const priceRange = PRICE_RANGES.find(r => r.id === filters.priceRange)` -> 声明变量或常量，保存运行时数据。
- 218: `if (priceRange && priceRange.id !== 'all') {` -> 条件判断分支，根据场景执行不同逻辑。
- 219: `if (priceRange.min !== null) params.append('min_price', priceRange.min.toString())` -> 条件判断分支，根据场景执行不同逻辑。
- 220: `if (priceRange.max !== null) params.append('max_price', priceRange.max.toString())` -> 条件判断分支，根据场景执行不同逻辑。
- 221: `}` -> 结束当前语句或代码块。
- 222: `(空行)` -> 空行，用于提升代码结构可读性。
- 223: `// 加时间戳强制绕过后端缓存` -> 注释行，用于解释设计意图或使用说明。
- 224: `params.append('_t', String(Date.now()))` -> 执行当前语句，参与该文件整体逻辑。
- 225: `const response = await fetch(\`/api/products?${params}\`)` -> 声明变量或常量，保存运行时数据。
- 226: `(空行)` -> 空行，用于提升代码结构可读性。
- 227: `if (!response.ok) {` -> 条件判断分支，根据场景执行不同逻辑。
- 228: `console.error('获取产品列表失败:', response.status)` -> 执行当前语句，参与该文件整体逻辑。
- 229: `setProducts([])` -> 执行当前语句，参与该文件整体逻辑。
- 230: `setTotal(0)` -> 执行当前语句，参与该文件整体逻辑。
- 231: `setTotalPages(0)` -> 执行当前语句，参与该文件整体逻辑。
- 232: `return` -> 返回结果或提前结束当前流程。
- 233: `}` -> 结束当前语句或代码块。
- 234: `(空行)` -> 空行，用于提升代码结构可读性。
- 235: `const data: ApiResponse = await response.json()` -> 声明变量或常量，保存运行时数据。
- 236: `(空行)` -> 空行，用于提升代码结构可读性。
- 237: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 238: `setProducts(data.products || [])` -> 执行当前语句，参与该文件整体逻辑。
- 239: `setTotal(data.total || 0)` -> 执行当前语句，参与该文件整体逻辑。
- 240: `setTotalPages(Math.ceil((data.total || 0) / 12))` -> 执行当前语句，参与该文件整体逻辑。
- 241: `}` -> 结束当前语句或代码块。
- 242: `} catch (error) {` -> 执行当前语句，参与该文件整体逻辑。
- 243: `console.error('获取产品列表失败:', error)` -> 执行当前语句，参与该文件整体逻辑。
- 244: `setProducts([])` -> 执行当前语句，参与该文件整体逻辑。
- 245: `setTotal(0)` -> 执行当前语句，参与该文件整体逻辑。
- 246: `setTotalPages(0)` -> 执行当前语句，参与该文件整体逻辑。
- 247: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 248: `setLoading(false)` -> 执行当前语句，参与该文件整体逻辑。
- 249: `}` -> 结束当前语句或代码块。
- 250: `}, [filters, currentPage])` -> 执行当前语句，参与该文件整体逻辑。
- 251: `(空行)` -> 空行，用于提升代码结构可读性。
- 252: `// 初始化时获取数据` -> 注释行，用于解释设计意图或使用说明。
- 253: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 254: `fetchProducts()` -> 执行当前语句，参与该文件整体逻辑。
- 255: `}, [filters, currentPage, fetchProducts])` -> 执行当前语句，参与该文件整体逻辑。
- 256: `(空行)` -> 空行，用于提升代码结构可读性。
- 257: `// 处理筛选变化` -> 注释行，用于解释设计意图或使用说明。
- 258: `const handleFilterChange = (key: string, value: any) => {` -> 声明变量或常量，保存运行时数据。
- 259: `setFilters(prev => ({ ...prev, [key]: value }))` -> 执行当前语句，参与该文件整体逻辑。
- 260: `setCurrentPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 261: `}` -> 结束当前语句或代码块。
- 262: `(空行)` -> 空行，用于提升代码结构可读性。
- 263: `// 处理标签切换` -> 注释行，用于解释设计意图或使用说明。
- 264: `const handleTagToggle = (tagId: string) => {` -> 声明变量或常量，保存运行时数据。
- 265: `const newTags = filters.tags.includes(tagId)` -> 声明变量或常量，保存运行时数据。
- 266: `? filters.tags.filter(t => t !== tagId)` -> 执行当前语句，参与该文件整体逻辑。
- 267: `: [...filters.tags, tagId]` -> 执行当前语句，参与该文件整体逻辑。
- 268: `handleFilterChange('tags', newTags)` -> 执行当前语句，参与该文件整体逻辑。
- 269: `}` -> 结束当前语句或代码块。
- 270: `(空行)` -> 空行，用于提升代码结构可读性。
- 271: `// 重置筛选` -> 注释行，用于解释设计意图或使用说明。
- 272: `const resetFilters = () => {` -> 声明变量或常量，保存运行时数据。
- 273: `setFilters({` -> 执行当前语句，参与该文件整体逻辑。
- 274: `category: 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 275: `priceRange: 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 276: `sortBy: 'recommended',` -> 执行当前语句，参与该文件整体逻辑。
- 277: `duration: 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 278: `keyword: '',` -> 执行当前语句，参与该文件整体逻辑。
- 279: `minPrice: '',` -> 执行当前语句，参与该文件整体逻辑。
- 280: `maxPrice: '',` -> 执行当前语句，参与该文件整体逻辑。
- 281: `tags: []` -> 执行当前语句，参与该文件整体逻辑。
- 282: `})` -> 执行当前语句，参与该文件整体逻辑。
- 283: `setCurrentPage(1)` -> 执行当前语句，参与该文件整体逻辑。
- 284: `}` -> 结束当前语句或代码块。
- 285: `(空行)` -> 空行，用于提升代码结构可读性。
- 286: `// 清空关键词` -> 注释行，用于解释设计意图或使用说明。
- 287: `const clearKeyword = () => {` -> 声明变量或常量，保存运行时数据。
- 288: `handleFilterChange('keyword', '')` -> 执行当前语句，参与该文件整体逻辑。
- 289: `}` -> 结束当前语句或代码块。
- 290: `(空行)` -> 空行，用于提升代码结构可读性。
- 291: `// 获取活动筛选数量` -> 注释行，用于解释设计意图或使用说明。
- 292: `const activeFilterCount = [` -> 声明变量或常量，保存运行时数据。
- 293: `filters.category !== 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 294: `filters.priceRange !== 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 295: `filters.sortBy !== 'recommended',` -> 执行当前语句，参与该文件整体逻辑。
- 296: `filters.duration !== 'all',` -> 执行当前语句，参与该文件整体逻辑。
- 297: `filters.keyword !== '',` -> 执行当前语句，参与该文件整体逻辑。
- 298: `filters.minPrice !== '',` -> 执行当前语句，参与该文件整体逻辑。
- 299: `filters.maxPrice !== '',` -> 执行当前语句，参与该文件整体逻辑。
- 300: `filters.tags.length > 0` -> 执行当前语句，参与该文件整体逻辑。
- 301: `].filter(Boolean).length` -> 执行当前语句，参与该文件整体逻辑。
- 302: `(空行)` -> 空行，用于提升代码结构可读性。
- 303: `// 渲染筛选器` -> 注释行，用于解释设计意图或使用说明。
- 304: `const renderFilters = () => (` -> 声明变量或常量，保存运行时数据。
- 305: `<div className="bg-white rounded-xl border border-gray-200 p-6 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 306: `{/* 搜索框 */}` -> 执行当前语句，参与该文件整体逻辑。
- 307: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 308: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 309: `<Search className="absolute left-4 top-1/2 -translate-y-1/2 h-5 w-5 text-gray-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 310: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 311: `type="text"` -> 执行当前语句，参与该文件整体逻辑。
- 312: `value={filters.keyword}` -> 执行当前语句，参与该文件整体逻辑。
- 313: `onChange={(e) => handleFilterChange('keyword', e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 314: `placeholder="搜索产品、目的地、关键词..."` -> 执行当前语句，参与该文件整体逻辑。
- 315: `className="w-full pl-12 pr-10 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 316: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 317: `{filters.keyword && (` -> 执行当前语句，参与该文件整体逻辑。
- 318: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 319: `onClick={clearKeyword}` -> 执行当前语句，参与该文件整体逻辑。
- 320: `className="absolute right-3 top-1/2 -translate-y-1/2"` -> 执行当前语句，参与该文件整体逻辑。
- 321: `>` -> 执行当前语句，参与该文件整体逻辑。
- 322: `<X className="h-5 w-5 text-gray-400 hover:text-gray-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 323: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 324: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 325: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 326: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 327: `(空行)` -> 空行，用于提升代码结构可读性。
- 328: `{/* 分类筛选 */}` -> 执行当前语句，参与该文件整体逻辑。
- 329: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 330: `<h3 className="font-semibold text-gray-900 mb-3">产品类别</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 331: `<div className="grid grid-cols-2 sm:grid-cols-4 gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 332: `{CATEGORIES.map(cat => {` -> 执行当前语句，参与该文件整体逻辑。
- 333: `const Icon = cat.icon` -> 声明变量或常量，保存运行时数据。
- 334: `return (` -> 返回结果或提前结束当前流程。
- 335: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 336: `key={cat.id}` -> 执行当前语句，参与该文件整体逻辑。
- 337: `onClick={() => handleFilterChange('category', cat.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 338: `className={\`flex flex-col items-center justify-center p-3 rounded-lg border transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 339: `filters.category === cat.id` -> 执行当前语句，参与该文件整体逻辑。
- 340: `? 'border-blue-500 bg-blue-50 text-blue-700'` -> 执行当前语句，参与该文件整体逻辑。
- 341: `: 'border-gray-200 hover:border-gray-300 hover:bg-gray-50'` -> 执行当前语句，参与该文件整体逻辑。
- 342: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 343: `>` -> 执行当前语句，参与该文件整体逻辑。
- 344: `<Icon />` -> JSX/HTML 结构行，用于描述页面元素。
- 345: `<span className="mt-2 text-sm font-medium">{cat.label}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 346: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 347: `)` -> 结束当前语句或代码块。
- 348: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 349: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 350: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 351: `(空行)` -> 空行，用于提升代码结构可读性。
- 352: `{/* 价格区间 */}` -> 执行当前语句，参与该文件整体逻辑。
- 353: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 354: `<h3 className="font-semibold text-gray-900 mb-3">价格区间</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 355: `<div className="space-y-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 356: `{PRICE_RANGES.map(range => (` -> 执行当前语句，参与该文件整体逻辑。
- 357: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 358: `key={range.id}` -> 执行当前语句，参与该文件整体逻辑。
- 359: `onClick={() => handleFilterChange('priceRange', range.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 360: `className={\`flex items-center justify-between w-full px-4 py-3 rounded-lg border transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 361: `filters.priceRange === range.id` -> 执行当前语句，参与该文件整体逻辑。
- 362: `? 'border-blue-500 bg-blue-50 text-blue-700'` -> 执行当前语句，参与该文件整体逻辑。
- 363: `: 'border-gray-200 hover:border-gray-300 hover:bg-gray-50'` -> 执行当前语句，参与该文件整体逻辑。
- 364: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 365: `>` -> 执行当前语句，参与该文件整体逻辑。
- 366: `<span>{range.label}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 367: `<DollarSign className="h-4 w-4" />` -> JSX/HTML 结构行，用于描述页面元素。
- 368: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 369: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 370: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 371: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 372: `(空行)` -> 空行，用于提升代码结构可读性。
- 373: `{/* 自定义价格 */}` -> 执行当前语句，参与该文件整体逻辑。
- 374: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 375: `<h3 className="font-semibold text-gray-900 mb-3">自定义价格</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 376: `<div className="flex gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 377: `<div className="flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 378: `<label className="block text-sm text-gray-600 mb-1">最低价</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 379: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 380: `<span className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500">¥</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 381: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 382: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 383: `value={filters.minPrice}` -> 执行当前语句，参与该文件整体逻辑。
- 384: `onChange={(e) => handleFilterChange('minPrice', e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 385: `placeholder="0"` -> 执行当前语句，参与该文件整体逻辑。
- 386: `className="w-full pl-8 pr-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 387: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 388: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 389: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 390: `<div className="flex-1">` -> JSX/HTML 结构行，用于描述页面元素。
- 391: `<label className="block text-sm text-gray-600 mb-1">最高价</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 392: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 393: `<span className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500">¥</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 394: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 395: `type="number"` -> 执行当前语句，参与该文件整体逻辑。
- 396: `value={filters.maxPrice}` -> 执行当前语句，参与该文件整体逻辑。
- 397: `onChange={(e) => handleFilterChange('maxPrice', e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 398: `placeholder="不限"` -> 执行当前语句，参与该文件整体逻辑。
- 399: `className="w-full pl-8 pr-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 400: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 401: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 402: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 403: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 404: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 405: `(空行)` -> 空行，用于提升代码结构可读性。
- 406: `{/* 标签筛选 */}` -> 执行当前语句，参与该文件整体逻辑。
- 407: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 408: `<h3 className="font-semibold text-gray-900 mb-3">适合人群</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 409: `<div className="flex flex-wrap gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 410: `{TAG_OPTIONS.map(tag => (` -> 执行当前语句，参与该文件整体逻辑。
- 411: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 412: `key={tag.id}` -> 执行当前语句，参与该文件整体逻辑。
- 413: `onClick={() => handleTagToggle(tag.id)}` -> 执行当前语句，参与该文件整体逻辑。
- 414: `className={\`px-3 py-1.5 rounded-full text-sm font-medium transition-all ${` -> 执行当前语句，参与该文件整体逻辑。
- 415: `filters.tags.includes(tag.id)` -> 执行当前语句，参与该文件整体逻辑。
- 416: `? tag.color` -> 执行当前语句，参与该文件整体逻辑。
- 417: `: 'bg-gray-100 text-gray-700 hover:bg-gray-200'` -> 执行当前语句，参与该文件整体逻辑。
- 418: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 419: `>` -> 执行当前语句，参与该文件整体逻辑。
- 420: `{tag.label}` -> 执行当前语句，参与该文件整体逻辑。
- 421: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 422: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 423: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 424: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 425: `(空行)` -> 空行，用于提升代码结构可读性。
- 426: `{/* 操作按钮 */}` -> 执行当前语句，参与该文件整体逻辑。
- 427: `<div className="flex gap-3 pt-4 border-t border-gray-200">` -> JSX/HTML 结构行，用于描述页面元素。
- 428: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 429: `onClick={resetFilters}` -> 执行当前语句，参与该文件整体逻辑。
- 430: `className="flex-1 px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 431: `>` -> 执行当前语句，参与该文件整体逻辑。
- 432: `重置筛选` -> 执行当前语句，参与该文件整体逻辑。
- 433: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 434: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 435: `onClick={() => setShowFilters(false)}` -> 执行当前语句，参与该文件整体逻辑。
- 436: `className="flex-1 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 437: `>` -> 执行当前语句，参与该文件整体逻辑。
- 438: `应用筛选` -> 执行当前语句，参与该文件整体逻辑。
- 439: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 440: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 441: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 442: `)` -> 结束当前语句或代码块。
- 443: `(空行)` -> 空行，用于提升代码结构可读性。
- 444: `// 渲染移动端筛选按钮` -> 注释行，用于解释设计意图或使用说明。
- 445: `const renderMobileFilterButton = () => (` -> 声明变量或常量，保存运行时数据。
- 446: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 447: `onClick={() => setShowMobileFilters(true)}` -> 执行当前语句，参与该文件整体逻辑。
- 448: `className="lg:hidden fixed bottom-6 right-6 z-40 flex items-center gap-2 px-4 py-3 bg-blue-600 text-white rounded-full shadow-lg hover:bg-blue-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 449: `>` -> 执行当前语句，参与该文件整体逻辑。
- 450: `<Filter className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 451: `筛选` -> 执行当前语句，参与该文件整体逻辑。
- 452: `{activeFilterCount > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 453: `<span className="ml-1 px-2 py-0.5 bg-white text-blue-600 text-xs rounded-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 454: `{activeFilterCount}` -> 执行当前语句，参与该文件整体逻辑。
- 455: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 456: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 457: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 458: `)` -> 结束当前语句或代码块。
- 459: `(空行)` -> 空行，用于提升代码结构可读性。
- 460: `// 渲染移动端筛选面板` -> 注释行，用于解释设计意图或使用说明。
- 461: `const renderMobileFilterPanel = () => (` -> 声明变量或常量，保存运行时数据。
- 462: `showMobileFilters && (` -> 执行当前语句，参与该文件整体逻辑。
- 463: `<div className="lg:hidden fixed inset-0 z-50 bg-white">` -> JSX/HTML 结构行，用于描述页面元素。
- 464: `<div className="h-full overflow-y-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 465: `<div className="sticky top-0 bg-white border-b border-gray-200 p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 466: `<div className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 467: `<h2 className="text-lg font-semibold text-gray-900">筛选条件</h2>` -> JSX/HTML 结构行，用于描述页面元素。
- 468: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 469: `onClick={() => setShowMobileFilters(false)}` -> 执行当前语句，参与该文件整体逻辑。
- 470: `className="p-2"` -> 执行当前语句，参与该文件整体逻辑。
- 471: `>` -> 执行当前语句，参与该文件整体逻辑。
- 472: `<X className="h-6 w-6" />` -> JSX/HTML 结构行，用于描述页面元素。
- 473: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 474: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 475: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 476: `<div className="p-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 477: `{renderFilters()}` -> 执行当前语句，参与该文件整体逻辑。
- 478: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 479: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 480: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 481: `)` -> 结束当前语句或代码块。
- 482: `)` -> 结束当前语句或代码块。
- 483: `(空行)` -> 空行，用于提升代码结构可读性。
- 484: `return (` -> 返回结果或提前结束当前流程。
- 485: `<div className="min-h-screen bg-gray-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 486: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 487: `(空行)` -> 空行，用于提升代码结构可读性。
- 488: `<main className="container mx-auto px-4 pt-24 pb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 489: `<div className="flex flex-col lg:flex-row gap-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 490: `{/* 左侧筛选栏 */}` -> 执行当前语句，参与该文件整体逻辑。
- 491: `<div className="lg:w-1/4">` -> JSX/HTML 结构行，用于描述页面元素。
- 492: `<div className="hidden lg:block">` -> JSX/HTML 结构行，用于描述页面元素。
- 493: `{renderFilters()}` -> 执行当前语句，参与该文件整体逻辑。
- 494: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 495: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 496: `(空行)` -> 空行，用于提升代码结构可读性。
- 497: `{/* 右侧内容 */}` -> 执行当前语句，参与该文件整体逻辑。
- 498: `<div className="lg:w-3/4">` -> JSX/HTML 结构行，用于描述页面元素。
- 499: `{/* 头部 */}` -> 执行当前语句，参与该文件整体逻辑。
- 500: `<div className="mb-8">` -> JSX/HTML 结构行，用于描述页面元素。
- 501: `<div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 502: `<div>` -> JSX/HTML 结构行，用于描述页面元素。
- 503: `<h1 className="text-2xl font-bold text-gray-900">产品列表</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 504: `<p className="mt-2 text-gray-600">` -> JSX/HTML 结构行，用于描述页面元素。
- 505: `发现 {total} 个精彩旅行体验` -> 执行当前语句，参与该文件整体逻辑。
- 506: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 507: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 508: `(空行)` -> 空行，用于提升代码结构可读性。
- 509: `{/* 排序 */}` -> 执行当前语句，参与该文件整体逻辑。
- 510: `<div className="relative" style={{ zIndex: 100 }} ref={(el) => {` -> 执行当前语句，参与该文件整体逻辑。
- 511: `if (el) {` -> 条件判断分支，根据场景执行不同逻辑。
- 512: `const handleClickOutside = (e: MouseEvent) => {` -> 声明变量或常量，保存运行时数据。
- 513: `if (!el.contains(e.target as Node)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 514: `setSortOpen(false)` -> 执行当前语句，参与该文件整体逻辑。
- 515: `}` -> 结束当前语句或代码块。
- 516: `}` -> 结束当前语句或代码块。
- 517: `if (sortOpen) {` -> 条件判断分支，根据场景执行不同逻辑。
- 518: `document.addEventListener('mousedown', handleClickOutside)` -> 执行当前语句，参与该文件整体逻辑。
- 519: `}` -> 结束当前语句或代码块。
- 520: `return () => document.removeEventListener('mousedown', handleClickOutside)` -> 返回结果或提前结束当前流程。
- 521: `}` -> 结束当前语句或代码块。
- 522: `}}>` -> 执行当前语句，参与该文件整体逻辑。
- 523: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 524: `onClick={() => setSortOpen(!sortOpen)}` -> 执行当前语句，参与该文件整体逻辑。
- 525: `className="w-full sm:w-auto flex items-center gap-2 pl-4 pr-10 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:outline-none bg-white relative"` -> 执行当前语句，参与该文件整体逻辑。
- 526: `>` -> 执行当前语句，参与该文件整体逻辑。
- 527: `{(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 528: `const opt = SORT_OPTIONS.find(o => o.id === filters.sortBy)` -> 声明变量或常量，保存运行时数据。
- 529: `const Icon = opt?.icon || Sparkles` -> 声明变量或常量，保存运行时数据。
- 530: `return (` -> 返回结果或提前结束当前流程。
- 531: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 532: `<Icon className="h-4 w-4 text-gray-500" />` -> JSX/HTML 结构行，用于描述页面元素。
- 533: `<span>{opt?.label}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 534: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 535: `)` -> 结束当前语句或代码块。
- 536: `})()}` -> 执行当前语句，参与该文件整体逻辑。
- 537: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 538: `<ChevronDown className={\`absolute right-3 top-1/2 -translate-y-1/2 pointer-events-none transition-transform ${sortOpen ? 'rotate-180' : ''}\`} />` -> JSX/HTML 结构行，用于描述页面元素。
- 539: `(空行)` -> 空行，用于提升代码结构可读性。
- 540: `{sortOpen && (` -> 执行当前语句，参与该文件整体逻辑。
- 541: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 542: `className="absolute right-0 top-full mt-2 w-48 bg-white border border-gray-200 rounded-lg shadow-lg overflow-hidden"` -> 执行当前语句，参与该文件整体逻辑。
- 543: `style={{ zIndex: 9999 }}` -> 执行当前语句，参与该文件整体逻辑。
- 544: `>` -> 执行当前语句，参与该文件整体逻辑。
- 545: `{SORT_OPTIONS.map(option => {` -> 执行当前语句，参与该文件整体逻辑。
- 546: `const Icon = option.icon` -> 声明变量或常量，保存运行时数据。
- 547: `const active = filters.sortBy === option.id` -> 声明变量或常量，保存运行时数据。
- 548: `return (` -> 返回结果或提前结束当前流程。
- 549: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 550: `key={option.id}` -> 执行当前语句，参与该文件整体逻辑。
- 551: `onClick={() => {` -> 执行当前语句，参与该文件整体逻辑。
- 552: `handleFilterChange('sortBy', option.id)` -> 执行当前语句，参与该文件整体逻辑。
- 553: `setSortOpen(false)` -> 执行当前语句，参与该文件整体逻辑。
- 554: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 555: `className={\`w-full flex items-center gap-2 px-4 py-2.5 text-left hover:bg-gray-50 transition-colors ${active ? 'bg-blue-50 text-blue-700 font-medium' : 'text-gray-700'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 556: `>` -> 执行当前语句，参与该文件整体逻辑。
- 557: `<Icon className={\`h-4 w-4 ${active ? 'text-blue-600' : 'text-gray-400'}\`} />` -> JSX/HTML 结构行，用于描述页面元素。
- 558: `{option.label}` -> 执行当前语句，参与该文件整体逻辑。
- 559: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 560: `)` -> 结束当前语句或代码块。
- 561: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 562: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 563: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 564: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 565: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 566: `(空行)` -> 空行，用于提升代码结构可读性。
- 567: `{/* 活动筛选标签 */}` -> 执行当前语句，参与该文件整体逻辑。
- 568: `{activeFilterCount > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 569: `<div className="mt-4 flex flex-wrap gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 570: `<span className="text-sm text-gray-600">已选条件：</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 571: `{filters.category !== 'all' && (` -> 执行当前语句，参与该文件整体逻辑。
- 572: `<span className="inline-flex items-center gap-1 px-3 py-1 bg-blue-100 text-blue-800 text-sm rounded-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 573: `{CATEGORIES.find(c => c.id === filters.category)?.label}` -> 执行当前语句，参与该文件整体逻辑。
- 574: `<button onClick={() => handleFilterChange('category', 'all')}>` -> JSX/HTML 结构行，用于描述页面元素。
- 575: `<X className="h-3 w-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 576: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 577: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 578: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 579: `{filters.tags.map(tagId => {` -> 执行当前语句，参与该文件整体逻辑。
- 580: `const tag = TAG_OPTIONS.find(t => t.id === tagId)` -> 声明变量或常量，保存运行时数据。
- 581: `return tag ? (` -> 返回结果或提前结束当前流程。
- 582: `<span key={tagId} className={\`inline-flex items-center gap-1 px-3 py-1 ${tag.color} text-sm rounded-full\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 583: `{tag.label}` -> 执行当前语句，参与该文件整体逻辑。
- 584: `<button onClick={() => handleTagToggle(tagId)}>` -> JSX/HTML 结构行，用于描述页面元素。
- 585: `<X className="h-3 w-3" />` -> JSX/HTML 结构行，用于描述页面元素。
- 586: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 587: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 588: `) : null` -> 执行当前语句，参与该文件整体逻辑。
- 589: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 590: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 591: `onClick={resetFilters}` -> 执行当前语句，参与该文件整体逻辑。
- 592: `className="text-sm text-gray-600 hover:text-gray-900"` -> 执行当前语句，参与该文件整体逻辑。
- 593: `>` -> 执行当前语句，参与该文件整体逻辑。
- 594: `清空所有` -> 执行当前语句，参与该文件整体逻辑。
- 595: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 596: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 597: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 598: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 599: `(空行)` -> 空行，用于提升代码结构可读性。
- 600: `{/* 产品网格 */}` -> 执行当前语句，参与该文件整体逻辑。
- 601: `{loading ? (` -> 执行当前语句，参与该文件整体逻辑。
- 602: `<div className="flex justify-center items-center h-64">` -> JSX/HTML 结构行，用于描述页面元素。
- 603: `<Loader2 className="h-8 w-8 animate-spin text-blue-600" />` -> JSX/HTML 结构行，用于描述页面元素。
- 604: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 605: `) : products.length > 0 ? (` -> 执行当前语句，参与该文件整体逻辑。
- 606: `<>` -> JSX/HTML 结构行，用于描述页面元素。
- 607: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 608: `{products.map(product => (` -> 执行当前语句，参与该文件整体逻辑。
- 609: `<ProductCard` -> 执行当前语句，参与该文件整体逻辑。
- 610: `key={product.id}` -> 执行当前语句，参与该文件整体逻辑。
- 611: `product={toProductCardProduct(product)}` -> 执行当前语句，参与该文件整体逻辑。
- 612: `onClick={() => router.push(\`/products/${product.id}\`)}` -> 执行当前语句，参与该文件整体逻辑。
- 613: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 614: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 615: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 616: `(空行)` -> 空行，用于提升代码结构可读性。
- 617: `{/* 分页 */}` -> 执行当前语句，参与该文件整体逻辑。
- 618: `{totalPages > 1 && (` -> 执行当前语句，参与该文件整体逻辑。
- 619: `<div className="mt-12 flex justify-center">` -> JSX/HTML 结构行，用于描述页面元素。
- 620: `<nav className="flex items-center gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 621: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 622: `onClick={() => setCurrentPage(p => Math.max(1, p - 1))}` -> 执行当前语句，参与该文件整体逻辑。
- 623: `disabled={currentPage === 1}` -> 执行当前语句，参与该文件整体逻辑。
- 624: `className="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 625: `>` -> 执行当前语句，参与该文件整体逻辑。
- 626: `上一页` -> 执行当前语句，参与该文件整体逻辑。
- 627: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 628: `(空行)` -> 空行，用于提升代码结构可读性。
- 629: `{Array.from({length: Math.min(5, totalPages)}, (_, i) => {` -> 执行当前语句，参与该文件整体逻辑。
- 630: `const pageNum = i + 1` -> 声明变量或常量，保存运行时数据。
- 631: `const isCurrent = pageNum === currentPage` -> 声明变量或常量，保存运行时数据。
- 632: `return (` -> 返回结果或提前结束当前流程。
- 633: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 634: `key={pageNum}` -> 执行当前语句，参与该文件整体逻辑。
- 635: `onClick={() => setCurrentPage(pageNum)}` -> 执行当前语句，参与该文件整体逻辑。
- 636: `className={\`w-10 h-10 rounded-lg transition-colors ${` -> 执行当前语句，参与该文件整体逻辑。
- 637: `isCurrent` -> 执行当前语句，参与该文件整体逻辑。
- 638: `? 'bg-blue-600 text-white'` -> 执行当前语句，参与该文件整体逻辑。
- 639: `: 'border border-gray-300 hover:bg-gray-100'` -> 执行当前语句，参与该文件整体逻辑。
- 640: `}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 641: `>` -> 执行当前语句，参与该文件整体逻辑。
- 642: `{pageNum}` -> 执行当前语句，参与该文件整体逻辑。
- 643: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 644: `)` -> 结束当前语句或代码块。
- 645: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 646: `(空行)` -> 空行，用于提升代码结构可读性。
- 647: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 648: `onClick={() => setCurrentPage(p => p + 1)}` -> 执行当前语句，参与该文件整体逻辑。
- 649: `disabled={currentPage === totalPages}` -> 执行当前语句，参与该文件整体逻辑。
- 650: `className="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 651: `>` -> 执行当前语句，参与该文件整体逻辑。
- 652: `下一页` -> 执行当前语句，参与该文件整体逻辑。
- 653: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 654: `</nav>` -> JSX/HTML 结构行，用于描述页面元素。
- 655: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 656: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 657: `</>` -> JSX/HTML 结构行，用于描述页面元素。
- 658: `) : (` -> 执行当前语句，参与该文件整体逻辑。
- 659: `<div className="text-center py-12">` -> JSX/HTML 结构行，用于描述页面元素。
- 660: `<div className="text-gray-400 mb-4">暂无产品</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 661: `<p className="text-gray-600 mb-6">尝试调整筛选条件或搜索关键词</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 662: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 663: `onClick={resetFilters}` -> 执行当前语句，参与该文件整体逻辑。
- 664: `className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"` -> 执行当前语句，参与该文件整体逻辑。
- 665: `>` -> 执行当前语句，参与该文件整体逻辑。
- 666: `重置筛选条件` -> 执行当前语句，参与该文件整体逻辑。
- 667: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 668: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 669: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 670: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 671: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 672: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 673: `(空行)` -> 空行，用于提升代码结构可读性。
- 674: `{/* 移动端筛选按钮 */}` -> 执行当前语句，参与该文件整体逻辑。
- 675: `{renderMobileFilterButton()}` -> 执行当前语句，参与该文件整体逻辑。
- 676: `{renderMobileFilterPanel()}` -> 执行当前语句，参与该文件整体逻辑。
- 677: `(空行)` -> 空行，用于提升代码结构可读性。
- 678: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 679: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 680: `)` -> 结束当前语句或代码块。
- 681: `}` -> 结束当前语句或代码块。
