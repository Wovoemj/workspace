# `frontend/user-web/src/components/DestinationCard.tsx` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `'use client'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 2: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 3: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 5: `* 目的地卡片组件 (DestinationCard)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 7: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `* 功能说明：`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 9: `* - 显示目的地卡片，包含图片、名称、城市、评分等信息`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 10: `* - 支持响应式图片加载（srcset）和懒加载`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 11: `* - 自动生成目的地标签（如"5A景区"、"热门"等）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 13: `* 使用场景：`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 14: `* - 首页目的地推荐列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `* - 目的地搜索结果列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 16: `* - 收藏目的地展示`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 18: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 19: `import { useState, useMemo } from 'react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 20: `import Link from 'next/link'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 21: `import { MapPin, Star } from 'lucide-react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 22: `import { formatRating, shouldShowRating } from '@/lib/display'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 23: `import { resolveCoverSrc } from '@/lib/media'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 24: `import { deriveDestinationTags } from '@/lib/destinationTags'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 25: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 26: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 27: `* 目的地卡片数据项类型`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 28: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 29: `export type DestinationCardItem = {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 30: `id: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `name: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 32: `city: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 33: `province: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 34: `description?: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 35: `cover_image?: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 36: `rating?: number`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 37: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 38: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 39: `interface DestinationCardProps {`
  - 作用：定义数据结构或接口。
  - 原因：先约束数据形态，后续逻辑更稳定可测。
- 40: `destination: DestinationCardItem`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 41: `className?: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 42: `/** 移动端压缩图片高度（tailwind class */`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 43: `imageHeightClass?: string`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 44: `/** 是否首屏优先加载 */`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 45: `priority?: boolean`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 46: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 49: `* 生成响应式图片 srcset`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 50: `* @description 根据基础图片URL生成多尺寸的 srcset，支持响应式图片加载`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 51: `* @param baseSrc - 基础图片URL`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 52: `* @returns srcset 字符串，用于 <img> 标签`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 53: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 54: `function generateSrcSet(baseSrc: string): string {`
  - 作用：定义函数/方法。
  - 原因：将复杂流程拆分为可复用单元，降低耦合。
- 55: `if (!baseSrc || baseSrc.startsWith('data:') || baseSrc.includes('placeholder')) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 56: `return ''`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 57: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 58: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 59: `// 支持的尺寸`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 60: `const widths = [320, 480, 640, 800, 1200]`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 61: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 62: `return widths`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 63: `.map(w => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `// 如果图片路径包含查询参数，尝试修改它`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 65: `if (baseSrc.includes('?')) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 66: `return \`${baseSrc}&w=${w} ${w}w\``
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 67: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 68: `// 否则添加参数`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 69: `return \`${baseSrc}?w=${w} ${w}w\``
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 70: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `.join(', ')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 73: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 74: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 75: `* 目的地卡片主组件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 76: `* @description 展示目的地信息的卡片组件，包含图片、名称、城市、评分和标签`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 77: `* @param props - 组件属性`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 78: `* @param props.destination - 目的地数据`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 79: `* @param props.className - 自定义样式类`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 80: `* @param props.imageHeightClass - 移动端图片高度`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 81: `* @param props.priority - 是否优先加载（用于首屏）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 82: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 83: `export function DestinationCard({`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 84: `destination: d,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `className = '',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 86: `imageHeightClass,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 87: `priority = false`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 88: `}: DestinationCardProps) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 89: `const [imgLoaded, setImgLoaded] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 90: `const [hasError, setHasError] = useState(false)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 91: `const src = resolveCoverSrc(d.cover_image)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 92: `const tags = deriveDestinationTags(d.name, d.description)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 93: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 94: `const imgWrap = imageHeightClass`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 95: `? \`relative w-full ${imageHeightClass} sm:h-auto sm:aspect-[4/3]\``
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `: 'relative w-full aspect-[4/3]'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 98: `// 生成响应式图片配置`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 99: `const { optimizedSrc, srcSet } = useMemo(() => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 100: `return {`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 101: `optimizedSrc: hasError ? '/images/placeholder.jpg' : src,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 102: `srcSet: generateSrcSet(src)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 103: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 104: `}, [src, hasError])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 106: `const handleError = () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 107: `if (!hasError) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 108: `setHasError(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 109: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 110: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 111: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 112: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 113: `<Link href={\`/destinations/${d.id}\`} className={\`group block h-full ${className}\`}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 114: `<div className="h-full rounded-2xl border border-border/60 bg-card shadow-sm overflow-hidden transition-all hover:shadow-md hover:-translate-y-0.5">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 115: `<div className={imgWrap}>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 116: `{/* 骨架屏占位 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 117: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 118: `className={\`absolute inset-0 bg-gradient-to-br from-muted to-muted/60 dark:from-slate-800 dark:to-slate-900 animate-pulse transition-opacity duration-300 ${`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 119: `imgLoaded ? 'opacity-0 pointer-events-none' : 'opacity-100'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 120: `}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `aria-hidden`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 122: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 124: `{/* 优化后的图片 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 125: `<img`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `src={optimizedSrc}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 127: `srcSet={srcSet || undefined}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 128: `sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 129: `alt={\`${d.name}风景\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `className={\`absolute inset-0 h-full w-full object-cover transition-all duration-300 group-hover:scale-105 ${imgLoaded ? 'opacity-100' : 'opacity-0'}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 131: `loading={priority ? 'eager' : 'lazy'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `decoding={priority ? 'sync' : 'async'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 133: `onLoad={() => setImgLoaded(true)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 134: `onError={handleError}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 135: `/>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 137: `{/* 渐变遮罩 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 138: `<div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 139: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 140: `{/* 信息覆盖层 */}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 141: `<div className="absolute bottom-2.5 left-3 right-3 sm:bottom-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 142: `<div className="flex items-end justify-between gap-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 143: `<div className="min-w-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 144: `<div className="text-white font-bold text-base leading-snug line-clamp-2">{d.name}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 145: `<div className="mt-0.5 flex items-center gap-1 text-white/85 text-xs">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 146: `<MapPin className="h-3.5 w-3.5 shrink-0" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 147: `<span className="truncate">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 148: `{d.city} · {d.province}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 150: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 151: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 152: `{shouldShowRating(d.rating) ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 153: `<div className="inline-flex items-center gap-1 bg-white/12 border border-white/20 px-2 py-1 rounded-full text-white shrink-0">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 154: `<Star className="h-3.5 w-3.5 fill-amber-400 text-amber-400" />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 155: `<span className="text-[11px] font-semibold">{formatRating(d.rating)}</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 156: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 157: `) : null}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 158: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 159: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 160: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 161: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 162: `<div className="p-4">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 163: `{tags.length > 0 ? (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `<div className="flex flex-wrap gap-1.5 mb-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 165: `{tags.map((t) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `<span`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 167: `key={t}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `className="px-2 py-0.5 rounded-full bg-primary/10 text-primary text-[11px] font-medium border border-primary/15"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 169: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 170: `{t}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 172: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 174: `) : null}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 175: `<p className="text-sm text-muted-foreground line-clamp-2 min-h-[2.5rem]">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 176: `{d.description?.replace(/\[citation:\d+\]/g, '').trim() || '发现这座城市的独特风景与旅行灵感'}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 177: `</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 178: `<div className="mt-3 flex items-center justify-between">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 179: `<span className="text-xs font-semibold text-primary group-hover:underline">探索这座城市</span>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 180: `<Link`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 181: `href={\`/destinations/map?city=${encodeURIComponent(d.city)}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 182: `onClick={(e) => e.stopPropagation()}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 183: `className="text-xs font-semibold text-sky-600 hover:text-sky-700"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 184: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 185: `地图查看`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 186: `</Link>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 187: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 188: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 189: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 190: `</Link>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 191: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 192: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
