# `monitoring/alert_rules.yml` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# 告警规则配置` -> 注释行，用于解释设计意图或使用说明。
- 2: `groups:` -> 执行当前语句，参与该文件整体逻辑。
- 3: `- name: travel-assistant-alerts` -> 执行当前语句，参与该文件整体逻辑。
- 4: `rules:` -> 执行当前语句，参与该文件整体逻辑。
- 5: `# API 响应时间告警` -> 注释行，用于解释设计意图或使用说明。
- 6: `- alert: HighApiResponseTime` -> 执行当前语句，参与该文件整体逻辑。
- 7: `expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 2` -> 执行当前语句，参与该文件整体逻辑。
- 8: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 9: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 10: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 11: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 12: `summary: "API 响应时间过高"` -> 执行当前语句，参与该文件整体逻辑。
- 13: `description: "API 响应时间在最近5分钟内超过2秒: {{ $value }}s"` -> 执行当前语句，参与该文件整体逻辑。
- 14: `(空行)` -> 空行，用于提升代码结构可读性。
- 15: `# API 错误率告警` -> 注释行，用于解释设计意图或使用说明。
- 16: `- alert: HighApiErrorRate` -> 执行当前语句，参与该文件整体逻辑。
- 17: `expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.05` -> 执行当前语句，参与该文件整体逻辑。
- 18: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 19: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 20: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 21: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 22: `summary: "API 错误率过高"` -> 执行当前语句，参与该文件整体逻辑。
- 23: `description: "API 错误率在最近5分钟内超过5%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 24: `(空行)` -> 空行，用于提升代码结构可读性。
- 25: `# 数据库连接数告警` -> 注释行，用于解释设计意图或使用说明。
- 26: `- alert: HighDatabaseConnections` -> 执行当前语句，参与该文件整体逻辑。
- 27: `expr: mysql_global_status_threads_connected / mysql_global_variables_max_connections > 0.8` -> 执行当前语句，参与该文件整体逻辑。
- 28: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 29: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 30: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 31: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 32: `summary: "数据库连接数过高"` -> 执行当前语句，参与该文件整体逻辑。
- 33: `description: "数据库连接数使用率超过80%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 34: `(空行)` -> 空行，用于提升代码结构可读性。
- 35: `# Redis 内存使用率告警` -> 注释行，用于解释设计意图或使用说明。
- 36: `- alert: HighRedisMemoryUsage` -> 执行当前语句，参与该文件整体逻辑。
- 37: `expr: redis_memory_used_bytes / redis_memory_max_bytes > 0.8` -> 执行当前语句，参与该文件整体逻辑。
- 38: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 39: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 40: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 41: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 42: `summary: "Redis 内存使用率过高"` -> 执行当前语句，参与该文件整体逻辑。
- 43: `description: "Redis 内存使用率超过80%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 44: `(空行)` -> 空行，用于提升代码结构可读性。
- 45: `# 应用无响应告警` -> 注释行，用于解释设计意图或使用说明。
- 46: `- alert: ApplicationDown` -> 执行当前语句，参与该文件整体逻辑。
- 47: `expr: up == 0` -> 执行当前语句，参与该文件整体逻辑。
- 48: `for: 1m` -> 执行当前语句，参与该文件整体逻辑。
- 49: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 50: `severity: critical` -> 执行当前语句，参与该文件整体逻辑。
- 51: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 52: `summary: "应用服务无响应"` -> 执行当前语句，参与该文件整体逻辑。
- 53: `description: "应用服务 {{ $labels.instance }} 在过去1分钟内无响应"` -> 执行当前语句，参与该文件整体逻辑。
- 54: `(空行)` -> 空行，用于提升代码结构可读性。
- 55: `# 高CPU使用率告警` -> 注释行，用于解释设计意图或使用说明。
- 56: `- alert: HighCPUUsage` -> 执行当前语句，参与该文件整体逻辑。
- 57: `expr: 100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 80` -> 执行当前语句，参与该文件整体逻辑。
- 58: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 59: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 60: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 61: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 62: `summary: "CPU使用率过高"` -> 执行当前语句，参与该文件整体逻辑。
- 63: `description: "CPU使用率在最近5分钟内超过80%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 64: `(空行)` -> 空行，用于提升代码结构可读性。
- 65: `# 高内存使用率告警` -> 注释行，用于解释设计意图或使用说明。
- 66: `- alert: HighMemoryUsage` -> 执行当前语句，参与该文件整体逻辑。
- 67: `expr: (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100 > 85` -> 执行当前语句，参与该文件整体逻辑。
- 68: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 69: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 70: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 71: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 72: `summary: "内存使用率过高"` -> 执行当前语句，参与该文件整体逻辑。
- 73: `description: "内存使用率在最近5分钟内超过85%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 74: `(空行)` -> 空行，用于提升代码结构可读性。
- 75: `- name: travel-assistant-business-alerts` -> 执行当前语句，参与该文件整体逻辑。
- 76: `rules:` -> 执行当前语句，参与该文件整体逻辑。
- 77: `# 订单量异常告警` -> 注释行，用于解释设计意图或使用说明。
- 78: `- alert: LowOrderRate` -> 执行当前语句，参与该文件整体逻辑。
- 79: `expr: rate(orders_created_total[1h]) < 1` -> 执行当前语句，参与该文件整体逻辑。
- 80: `for: 30m` -> 执行当前语句，参与该文件整体逻辑。
- 81: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 82: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 83: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 84: `summary: "订单创建率过低"` -> 执行当前语句，参与该文件整体逻辑。
- 85: `description: "过去1小时订单创建率低于1个/小时: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 86: `(空行)` -> 空行，用于提升代码结构可读性。
- 87: `# 用户活跃度异常告警` -> 注释行，用于解释设计意图或使用说明。
- 88: `- alert: LowUserActivity` -> 执行当前语句，参与该文件整体逻辑。
- 89: `expr: rate(user_logins_total[1h]) < 5` -> 执行当前语句，参与该文件整体逻辑。
- 90: `for: 30m` -> 执行当前语句，参与该文件整体逻辑。
- 91: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 92: `severity: warning` -> 执行当前语句，参与该文件整体逻辑。
- 93: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 94: `summary: "用户活跃度过低"` -> 执行当前语句，参与该文件整体逻辑。
- 95: `description: "过去1小时用户登录数少于5次: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
- 96: `(空行)` -> 空行，用于提升代码结构可读性。
- 97: `# 支付失败率告警` -> 注释行，用于解释设计意图或使用说明。
- 98: `- alert: HighPaymentFailureRate` -> 执行当前语句，参与该文件整体逻辑。
- 99: `expr: rate(payment_failed_total[5m]) / rate(payment_attempts_total[5m]) > 0.1` -> 执行当前语句，参与该文件整体逻辑。
- 100: `for: 5m` -> 执行当前语句，参与该文件整体逻辑。
- 101: `labels:` -> 执行当前语句，参与该文件整体逻辑。
- 102: `severity: critical` -> 执行当前语句，参与该文件整体逻辑。
- 103: `annotations:` -> 执行当前语句，参与该文件整体逻辑。
- 104: `summary: "支付失败率过高"` -> 执行当前语句，参与该文件整体逻辑。
- 105: `description: "支付失败率在最近5分钟内超过10%: {{ $value }}"` -> 执行当前语句，参与该文件整体逻辑。
