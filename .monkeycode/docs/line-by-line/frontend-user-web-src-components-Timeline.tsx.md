# `frontend/user-web/src/components/Timeline.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `/**` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* 时间轴组件 (Timeline)` -> 注释行，用于解释设计意图或使用说明。
- 6: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 7: `*` -> 注释行，用于解释设计意图或使用说明。
- 8: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 一日游/行程时间轴展示组件` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 按时间段（上午、下午、晚上等）展示活动安排` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 每个时间段显示图标、时间标签、标题、描述和标签` -> 注释行，用于解释设计意图或使用说明。
- 12: `*` -> 注释行，用于解释设计意图或使用说明。
- 13: `* 时间段图标映射：` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - 上午/清晨 → Sun` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 中午 → Clock` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 下午 → Clock` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 傍晚 → Sunset` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - 晚上/夜间 → Moon` -> 注释行，用于解释设计意图或使用说明。
- 19: `*/` -> 注释行，用于解释设计意图或使用说明。
- 20: `(空行)` -> 空行，用于提升代码结构可读性。
- 21: `import { Clock, Sun, Sunset, Moon } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `/** 时间轴单个条目类型 */` -> 注释行，用于解释设计意图或使用说明。
- 24: `interface TimelineItem {` -> 定义类型或类结构，约束数据与行为。
- 25: `id: string | number` -> 执行当前语句，参与该文件整体逻辑。
- 26: `timeLabel: string` -> 执行当前语句，参与该文件整体逻辑。
- 27: `title: string` -> 执行当前语句，参与该文件整体逻辑。
- 28: `description: string` -> 执行当前语句，参与该文件整体逻辑。
- 29: `tags?: string[]` -> 执行当前语句，参与该文件整体逻辑。
- 30: `}` -> 结束当前语句或代码块。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `interface TimelineProps {` -> 定义类型或类结构，约束数据与行为。
- 33: `title?: string` -> 执行当前语句，参与该文件整体逻辑。
- 34: `items: TimelineItem[]` -> 执行当前语句，参与该文件整体逻辑。
- 35: `}` -> 结束当前语句或代码块。
- 36: `(空行)` -> 空行，用于提升代码结构可读性。
- 37: `const timeIcons: Record<string, string> = {` -> 声明变量或常量，保存运行时数据。
- 38: `'上午': 'from-amber-400 to-yellow-500',` -> 执行当前语句，参与该文件整体逻辑。
- 39: `'中午': 'from-orange-400 to-red-500',` -> 执行当前语句，参与该文件整体逻辑。
- 40: `'下午': 'from-blue-400 to-indigo-500',` -> 执行当前语句，参与该文件整体逻辑。
- 41: `'傍晚': 'from-purple-400 to-pink-500',` -> 执行当前语句，参与该文件整体逻辑。
- 42: `'晚上': 'from-indigo-400 to-purple-600',` -> 执行当前语句，参与该文件整体逻辑。
- 43: `}` -> 结束当前语句或代码块。
- 44: `(空行)` -> 空行，用于提升代码结构可读性。
- 45: `const getTimeIcon = (label: string) => {` -> 声明变量或常量，保存运行时数据。
- 46: `if (label.includes('上') || label.includes('晨')) return Sun` -> 条件判断分支，根据场景执行不同逻辑。
- 47: `if (label.includes('午') || label.includes('中')) return Clock` -> 条件判断分支，根据场景执行不同逻辑。
- 48: `if (label.includes('午') && label.includes('下')) return Clock` -> 条件判断分支，根据场景执行不同逻辑。
- 49: `if (label.includes('午') && !label.includes('上')) return Sunset` -> 条件判断分支，根据场景执行不同逻辑。
- 50: `if (label.includes('晚') || label.includes('夜')) return Moon` -> 条件判断分支，根据场景执行不同逻辑。
- 51: `return Clock` -> 返回结果或提前结束当前流程。
- 52: `}` -> 结束当前语句或代码块。
- 53: `(空行)` -> 空行，用于提升代码结构可读性。
- 54: `/**` -> 注释行，用于解释设计意图或使用说明。
- 55: `* 时间轴组件` -> 注释行，用于解释设计意图或使用说明。
- 56: `* @description 按时间段展示行程活动的时间轴组件` -> 注释行，用于解释设计意图或使用说明。
- 57: `* @param props - 组件属性` -> 注释行，用于解释设计意图或使用说明。
- 58: `* @param props.title - 标题（默认：一日游时间轴）` -> 注释行，用于解释设计意图或使用说明。
- 59: `* @param props.items - 时间轴条目列表` -> 注释行，用于解释设计意图或使用说明。
- 60: `*/` -> 注释行，用于解释设计意图或使用说明。
- 61: `export default function Timeline({ title = '一日游时间轴', items }: TimelineProps) {` -> 导出当前声明，供其他模块复用。
- 62: `return (` -> 返回结果或提前结束当前流程。
- 63: `<div className="card overflow-hidden mt-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 64: `{/* 标题栏 */}` -> 执行当前语句，参与该文件整体逻辑。
- 65: `<div className="bg-gradient-to-r from-blue-600 to-indigo-600 px-6 py-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 66: `<div className="flex items-center gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 67: `<Clock className="w-5 h-5 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 68: `<h3 className="text-lg font-bold text-white">{title}</h3>` -> JSX/HTML 结构行，用于描述页面元素。
- 69: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 70: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 71: `(空行)` -> 空行，用于提升代码结构可读性。
- 72: `{/* 时间轴内容 */}` -> 执行当前语句，参与该文件整体逻辑。
- 73: `<div className="p-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 74: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 75: `{/* 连接线 */}` -> 执行当前语句，参与该文件整体逻辑。
- 76: `<div className="absolute left-8 top-0 bottom-0 w-0.5 bg-gradient-to-b from-primary-300 via-primary-400 to-primary-300" />` -> JSX/HTML 结构行，用于描述页面元素。
- 77: `(空行)` -> 空行，用于提升代码结构可读性。
- 78: `{items.map((item, index) => {` -> 执行当前语句，参与该文件整体逻辑。
- 79: `const TimeIcon = getTimeIcon(item.timeLabel)` -> 声明变量或常量，保存运行时数据。
- 80: `const gradient = timeIcons[item.timeLabel] || 'from-gray-400 to-gray-500'` -> 声明变量或常量，保存运行时数据。
- 81: `const isLast = index === items.length - 1` -> 声明变量或常量，保存运行时数据。
- 82: `(空行)` -> 空行，用于提升代码结构可读性。
- 83: `return (` -> 返回结果或提前结束当前流程。
- 84: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 85: `key={item.id}` -> 执行当前语句，参与该文件整体逻辑。
- 86: `className={\`relative flex gap-6 ${isLast ? '' : 'pb-8'}\`}` -> 执行当前语句，参与该文件整体逻辑。
- 87: `>` -> 执行当前语句，参与该文件整体逻辑。
- 88: `{/* 时间节点 */}` -> 执行当前语句，参与该文件整体逻辑。
- 89: `<div className={\`relative z-10 w-16 h-16 rounded-2xl bg-gradient-to-br ${gradient} flex items-center justify-center shadow-lg flex-shrink-0\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 90: `<TimeIcon className="w-7 h-7 text-white" />` -> JSX/HTML 结构行，用于描述页面元素。
- 91: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 92: `(空行)` -> 空行，用于提升代码结构可读性。
- 93: `{/* 内容卡片 */}` -> 执行当前语句，参与该文件整体逻辑。
- 94: `<div className="flex-1 bg-white border border-gray-100 rounded-xl p-5 shadow-sm hover:shadow-md hover:border-primary-200 transition-all duration-300">` -> JSX/HTML 结构行，用于描述页面元素。
- 95: `<div className="flex flex-col sm:flex-row sm:items-center gap-3 mb-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 96: `{/* 时间标签 */}` -> 执行当前语句，参与该文件整体逻辑。
- 97: `<span className={\`inline-flex items-center px-3 py-1.5 rounded-full text-xs font-bold text-white bg-gradient-to-r ${gradient}\`}>` -> JSX/HTML 结构行，用于描述页面元素。
- 98: `{item.timeLabel}` -> 执行当前语句，参与该文件整体逻辑。
- 99: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 100: `{/* 标题 */}` -> 执行当前语句，参与该文件整体逻辑。
- 101: `<h4 className="font-bold text-gray-900 text-lg">{item.title}</h4>` -> JSX/HTML 结构行，用于描述页面元素。
- 102: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 103: `(空行)` -> 空行，用于提升代码结构可读性。
- 104: `{/* 描述 */}` -> 执行当前语句，参与该文件整体逻辑。
- 105: `<p className="text-sm text-gray-600 leading-relaxed">` -> JSX/HTML 结构行，用于描述页面元素。
- 106: `{item.description}` -> 执行当前语句，参与该文件整体逻辑。
- 107: `</p>` -> JSX/HTML 结构行，用于描述页面元素。
- 108: `(空行)` -> 空行，用于提升代码结构可读性。
- 109: `{/* 标签 */}` -> 执行当前语句，参与该文件整体逻辑。
- 110: `{item.tags && item.tags.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 111: `<div className="mt-4 flex flex-wrap gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 112: `{item.tags.map((tag, tagIdx) => (` -> 执行当前语句，参与该文件整体逻辑。
- 113: `<span` -> 执行当前语句，参与该文件整体逻辑。
- 114: `key={tagIdx}` -> 执行当前语句，参与该文件整体逻辑。
- 115: `className="px-2.5 py-1 rounded-full bg-primary-50 text-primary-600 text-xs font-medium border border-primary-100"` -> 执行当前语句，参与该文件整体逻辑。
- 116: `>` -> 执行当前语句，参与该文件整体逻辑。
- 117: `{tag}` -> 执行当前语句，参与该文件整体逻辑。
- 118: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 119: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 120: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 121: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 122: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 123: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 124: `)` -> 结束当前语句或代码块。
- 125: `})}` -> 执行当前语句，参与该文件整体逻辑。
- 126: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 127: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 128: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 129: `)` -> 结束当前语句或代码块。
- 130: `}` -> 结束当前语句或代码块。
