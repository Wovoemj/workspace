# `backend/gateway/main.go` 逐行说明

说明：本文件按“代码行号 -> 作用”解释 `backend/gateway/main.go` 的每一行。

- 1: 声明当前 Go 文件属于 `main` 包，表示它是可执行程序入口包。
- 2: 空行，用于分隔包声明与导入区域。
- 3: `import (` 开始导入依赖包列表。
- 4: 导入 `context`，用于优雅停机时的超时上下文控制。
- 5: 导入 `fmt`，用于字符串格式化（如端口地址、错误信息）。
- 6: 导入 `net/http`，用于 HTTP 状态码和服务器结构体。
- 7: 导入 `net/http/httputil`，用于反向代理能力。
- 8: 导入 `net/url`，用于解析服务目标 URL。
- 9: 导入 `strings`，用于路径前缀替换。
- 10: 导入 `time`，用于时间戳、耗时统计、超时控制。
- 11: 空行，分隔标准库与第三方库。
- 12: 导入 `gin` Web 框架。
- 13: 导入 `logrus` 日志库。
- 14: `import` 块结束。
- 15: 空行，分隔导入与类型定义。
- 16: 定义 `Config` 结构体，承载网关配置。
- 17: `Port int` 定义网关监听端口。
- 18: `ServiceURLs map[string]string` 定义服务名到地址映射。
- 19: `Config` 结构体结束。
- 20: 空行。
- 21: 定义 `Gateway` 结构体，封装网关运行时对象。
- 22: `config` 保存配置指针。
- 23: `logger` 保存日志实例。
- 24: `router` 保存 Gin 路由引擎。
- 25: `httpServer` 保存 HTTP 服务实例，用于启动和关闭。
- 26: `Gateway` 结构体结束。
- 27: 空行。
- 28: 定义 `NewGateway` 构造函数，接收配置并返回网关实例。
- 29: 创建新的 `logrus` logger。
- 30: 设置日志格式器为文本格式。
- 31: 开启完整时间戳输出。
- 32: 结束格式器配置。
- 33: 设置日志级别为 `Info`。
- 34: 空行。
- 35: 创建一个新的 Gin 引擎（不带默认中间件）。
- 36: 挂载 `Recovery` 中间件，防止 panic 导致进程崩溃。
- 37: 挂载自定义 CORS 中间件。
- 38: 挂载自定义请求日志中间件。
- 39: 空行。
- 40: 返回 `Gateway` 结构体实例。
- 41: 填充 `config` 字段。
- 42: 填充 `logger` 字段。
- 43: 填充 `router` 字段。
- 44: 结束结构体字面量。
- 45: 结束 `NewGateway` 函数。
- 46: 空行。
- 47: 定义 `SetupRoutes` 方法，用于注册所有路由。
- 48: 注释：以下是健康检查接口。
- 49: 注册 `GET /health` 路由。
- 50: 返回 HTTP 200 的 JSON 响应。
- 51: `status` 字段表示服务状态。
- 52: `message` 字段返回可读提示。
- 53: `time` 字段返回当前 RFC3339 格式时间。
- 54: 结束健康检查响应体。
- 55: 结束健康检查处理函数。
- 56: 空行。
- 57: 注释：以下是 API 代理路由。
- 58: 创建 `/api` 路由组。
- 59: 开始 `/api` 路由组代码块。
- 60: 注释：产品服务代理（景点等）。
- 61: 创建空子组（实质仍在 `/api` 下）。
- 62: 代理 `/api/destinations` 到 `product-service`。
- 63: 代理 `/api/destinations/:id` 到 `product-service`。
- 64: 代理 `/api/products` 到 `product-service`。
- 65: 代理 `/api/products/:id` 到 `product-service`。
- 66: 空行。
- 67: 注释：用户服务代理，说明要裁剪前缀。
- 68: 创建 `/api/users` 路由组。
- 69: 代理 `/api/users/*path` 到 `user-service` 并去掉 `/api/users` 前缀。
- 70: 空行。
- 71: 注释：收藏相关代理。
- 72: 代理 `/api/favorites` 到 `user-service` 并裁剪 `/api/favorites`。
- 73: 代理 `/api/favorites/*path` 到 `user-service` 并裁剪 `/api/favorites`。
- 74: 空行。
- 75: 注释：订单服务代理。
- 76: 创建 `/api/orders` 路由组。
- 77: 代理 `/api/orders/*path` 到 `order-service` 并去掉 `/api/orders`。
- 78: 空行。
- 79: 注释：AI 服务代理。
- 80: 创建 `/api/ai` 路由组。
- 81: 开始 AI 组代码块。
- 82: 注释：强调具体路由先于通配符注册。
- 83: 代理 `/api/ai/chat` 到 `ai-service` 并裁剪 `/api/ai`。
- 84: 空行。
- 85: 结束 AI 组代码块。
- 86: 空行。
- 87: 注释：推荐服务代理。
- 88: 创建 `/api/recommendations` 路由组。
- 89: 代理 `/api/recommendations/*path` 到推荐服务并裁剪前缀。
- 90: 空行。
- 91: 注释：通知服务代理。
- 92: 创建 `/api/notifications` 路由组。
- 93: 代理 `/api/notifications/*path` 到通知服务并裁剪前缀。
- 94: 空行。
- 95: 注释：统计接口（当前为临时实现）。
- 96: 注册 `GET /api/stats` 路由。
- 97: 返回 HTTP 200 的 JSON。
- 98: `success` 字段表示请求成功。
- 99: `stats` 字段嵌套统计对象。
- 100: `destinations` 统计值，当前固定为 0。
- 101: `users` 统计值，当前固定为 0。
- 102: `trips` 统计值，当前固定为 0。
- 103: `comments` 统计值，当前固定为 0。
- 104: `orders` 统计值，当前固定为 0。
- 105: `footprints` 统计值，当前固定为 0。
- 106: `pages` 统计值，当前固定为 0。
- 107: `notifications` 统计值，当前固定为 0。
- 108: 结束 `stats` 对象。
- 109: 结束响应对象。
- 110: 结束统计接口处理函数。
- 111: 结束 `/api` 路由组代码块。
- 112: 结束 `SetupRoutes` 方法。
- 113: 空行。
- 114: 定义 `proxyToService` 方法，返回一个 Gin 处理函数。
- 115: 返回闭包处理函数，处理实际代理逻辑。
- 116: 根据服务名从配置中读取目标 URL。
- 117: 判断服务名是否存在。
- 118: 若不存在，记录错误日志。
- 119: 返回 HTTP 404 JSON。
- 120: 错误信息中带上具体服务名。
- 121: 结束错误响应对象。
- 122: 提前返回，终止后续代理流程。
- 123: 结束 `if !exists` 分支。
- 124: 空行。
- 125: 解析服务 URL 字符串。
- 126: 判断 URL 解析是否失败。
- 127: 解析失败时记录错误日志。
- 128: 返回 HTTP 500 JSON。
- 129: 返回统一配置错误信息。
- 130: 结束错误响应对象。
- 131: 提前返回，终止后续流程。
- 132: 结束 URL 解析错误分支。
- 133: 空行。
- 134: 注释：准备创建反向代理。
- 135: 基于目标地址创建单主机反向代理。
- 136: 空行。
- 137: 保存原始请求路径，便于日志记录。
- 138: 空行。
- 139: 注释：准备替换请求路径前缀。
- 140: 仅在配置了前缀时执行替换。
- 141: 将请求路径中的前缀替换为空字符串（仅替换一次）。
- 142: 结束前缀替换条件分支。
- 143: 设置请求目标主机为服务主机。
- 144: 设置请求目标协议为服务协议。
- 145: 空行。
- 146: 打印代理日志，展示原路径与转发后路径。
- 147: 继续日志参数：方法、原路径、目标服务地址、新路径。
- 148: 结束日志调用。
- 149: 空行。
- 150: 将请求通过反向代理转发给目标服务。
- 151: 结束闭包处理函数。
- 152: 结束 `proxyToService` 方法。
- 153: 空行。
- 154: 定义 `Start` 方法，负责启动网关。
- 155: 启动前先注册路由。
- 156: 空行。
- 157: 根据配置端口拼接监听地址字符串。
- 158: 创建 `http.Server` 实例。
- 159: 设置监听地址。
- 160: 设置处理器为 Gin 路由引擎。
- 161: 结束服务实例初始化。
- 162: 空行。
- 163: 打印网关启动日志。
- 164: 调用 `ListenAndServe` 启动 HTTP 服务并返回其错误。
- 165: 结束 `Start` 方法。
- 166: 空行。
- 167: 定义 `Stop` 方法，负责优雅停机。
- 168: 创建 10 秒超时的上下文。
- 169: 函数结束前释放上下文资源。
- 170: 空行。
- 171: 调用 `Shutdown` 在超时窗口内平滑关闭服务器。
- 172: 结束 `Stop` 方法。
- 173: 空行。
- 174: 定义 CORS 中间件函数。
- 175: 返回实际处理中间件闭包。
- 176: 允许所有来源跨域访问。
- 177: 声明允许的 HTTP 方法。
- 178: 声明允许的请求头。
- 179: 预检请求结果可缓存 86400 秒。
- 180: 空行。
- 181: 判断当前请求是否为 `OPTIONS` 预检请求。
- 182: 预检请求直接返回 204 并中止链路。
- 183: 返回，避免继续执行后续处理。
- 184: 结束 `OPTIONS` 判断分支。
- 185: 空行。
- 186: 调用 `Next`，交给后续中间件或业务处理。
- 187: 结束中间件闭包。
- 188: 结束 `corsMiddleware` 函数。
- 189: 空行。
- 190: 定义日志中间件函数，接收 logger。
- 191: 返回实际处理中间件闭包。
- 192: 记录请求开始时间。
- 193: 空行。
- 194: 执行后续处理链。
- 195: 空行。
- 196: 计算请求总耗时。
- 197: 获取响应状态码。
- 198: 空行。
- 199: 以结构化字段方式写日志。
- 200: 记录请求方法字段。
- 201: 记录请求路径字段。
- 202: 记录响应状态字段。
- 203: 记录请求耗时字段。
- 204: 记录客户端 IP 字段。
- 205: 记录 User-Agent 字段。
- 206: 输出一条 `HTTP request` 信息级日志。
- 207: 结束日志中间件闭包。
- 208: 结束 `loggingMiddleware` 函数。
- 209: 空行。
- 210: 定义 `main` 函数，程序入口。
- 211: 初始化网关配置结构体。
- 212: 监听端口设置为 `8080`。
- 213: 初始化服务地址映射表。
- 214: 配置 `user-service` 地址。
- 215: 配置 `product-service` 地址。
- 216: 配置 `order-service` 地址。
- 217: 配置 `ai-service` 地址。
- 218: 配置 `recommend-service` 地址。
- 219: 配置 `notification-service` 地址。
- 220: 结束服务地址映射表。
- 221: 结束配置结构体初始化。
- 222: 空行。
- 223: 基于配置创建网关实例。
- 224: 启动网关并捕获错误。
- 225: 若启动失败，打印致命日志并退出进程。
- 226: 结束 `if` 分支。
- 227: 结束 `main` 函数。
