# `frontend/user-web/src/components/search/LeafletMap.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 2: `(空行)` -> 空行，用于提升代码结构可读性。
- 3: `/**` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `* Leaflet 地图组件 (LeafletMap)` -> 注释行，用于解释设计意图或使用说明。
- 6: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 7: `*` -> 注释行，用于解释设计意图或使用说明。
- 8: `* 功能说明：` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 基于 Leaflet 开源地图库的交互式地图组件` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 支持在地图上标注多个产品/目的地位置` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 点击标记显示详情弹窗` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 自动适应所有标记的显示范围` -> 注释行，用于解释设计意图或使用说明。
- 13: `*` -> 注释行，用于解释设计意图或使用说明。
- 14: `* 技术实现：` -> 注释行，用于解释设计意图或使用说明。
- 15: `* - 动态加载 Leaflet JS/CSS（通过 unpkg CDN）` -> 注释行，用于解释设计意图或使用说明。
- 16: `* - 使用 OpenStreetMap 瓦片图层` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - 支持选中状态联动（点击列表项定位到地图标记）` -> 注释行，用于解释设计意图或使用说明。
- 18: `*/` -> 注释行，用于解释设计意图或使用说明。
- 19: `(空行)` -> 空行，用于提升代码结构可读性。
- 20: `import { useEffect, useMemo, useRef } from 'react'` -> 导入依赖模块，供当前文件使用。
- 21: `import type { CSSProperties } from 'react'` -> 导入依赖模块，供当前文件使用。
- 22: `(空行)` -> 空行，用于提升代码结构可读性。
- 23: `/** 产品/地点数据结构 */` -> 注释行，用于解释设计意图或使用说明。
- 24: `type ProductLike = {` -> 定义类型或类结构，约束数据与行为。
- 25: `id: string | number` -> 执行当前语句，参与该文件整体逻辑。
- 26: `name?: string` -> 执行当前语句，参与该文件整体逻辑。
- 27: `price?: number` -> 执行当前语句，参与该文件整体逻辑。
- 28: `rating?: number` -> 执行当前语句，参与该文件整体逻辑。
- 29: `location?: any` -> 执行当前语句，参与该文件整体逻辑。
- 30: `}` -> 结束当前语句或代码块。
- 31: `(空行)` -> 空行，用于提升代码结构可读性。
- 32: `function getLatLng(p: ProductLike) {` -> 定义函数或方法，实现具体业务逻辑。
- 33: `const loc = p.location || {}` -> 声明变量或常量，保存运行时数据。
- 34: `const lat = loc?.coordinates?.lat ?? loc?.latitude ?? loc?.lat` -> 声明变量或常量，保存运行时数据。
- 35: `const lng = loc?.coordinates?.lng ?? loc?.longitude ?? loc?.lng` -> 声明变量或常量，保存运行时数据。
- 36: `const nlat = typeof lat === 'number' ? lat : Number(lat)` -> 声明变量或常量，保存运行时数据。
- 37: `const nlng = typeof lng === 'number' ? lng : Number(lng)` -> 声明变量或常量，保存运行时数据。
- 38: `if (!Number.isFinite(nlat) || !Number.isFinite(nlng)) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 39: `return { lat: nlat, lng: nlng }` -> 返回结果或提前结束当前流程。
- 40: `}` -> 结束当前语句或代码块。
- 41: `(空行)` -> 空行，用于提升代码结构可读性。
- 42: `declare global {` -> 执行当前语句，参与该文件整体逻辑。
- 43: `interface Window {` -> 定义类型或类结构，约束数据与行为。
- 44: `L?: any` -> 执行当前语句，参与该文件整体逻辑。
- 45: `}` -> 结束当前语句或代码块。
- 46: `}` -> 结束当前语句或代码块。
- 47: `(空行)` -> 空行，用于提升代码结构可读性。
- 48: `let leafletLoaderPromise: Promise<any> | null = null` -> 声明变量或常量，保存运行时数据。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `async function ensureLeaflet(): Promise<any> {` -> 定义函数或方法，实现具体业务逻辑。
- 51: `if (typeof window === 'undefined') return null` -> 条件判断分支，根据场景执行不同逻辑。
- 52: `if (window.L) return window.L` -> 条件判断分支，根据场景执行不同逻辑。
- 53: `if (leafletLoaderPromise) return leafletLoaderPromise` -> 条件判断分支，根据场景执行不同逻辑。
- 54: `(空行)` -> 空行，用于提升代码结构可读性。
- 55: `leafletLoaderPromise = new Promise((resolve, reject) => {` -> 执行当前语句，参与该文件整体逻辑。
- 56: `const linkHref = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css'` -> 声明变量或常量，保存运行时数据。
- 57: `const jsSrc = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js'` -> 声明变量或常量，保存运行时数据。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `const ensureLink = () => {` -> 声明变量或常量，保存运行时数据。
- 60: `const existing = document.querySelector(\`link[href="${linkHref}"]\`)` -> 声明变量或常量，保存运行时数据。
- 61: `if (existing) return` -> 条件判断分支，根据场景执行不同逻辑。
- 62: `const link = document.createElement('link')` -> 声明变量或常量，保存运行时数据。
- 63: `link.rel = 'stylesheet'` -> 执行当前语句，参与该文件整体逻辑。
- 64: `link.href = linkHref` -> 执行当前语句，参与该文件整体逻辑。
- 65: `document.head.appendChild(link)` -> 执行当前语句，参与该文件整体逻辑。
- 66: `}` -> 结束当前语句或代码块。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `ensureLink()` -> 执行当前语句，参与该文件整体逻辑。
- 69: `(空行)` -> 空行，用于提升代码结构可读性。
- 70: `const script = document.createElement('script')` -> 声明变量或常量，保存运行时数据。
- 71: `script.src = jsSrc` -> 执行当前语句，参与该文件整体逻辑。
- 72: `script.async = true` -> 执行当前语句，参与该文件整体逻辑。
- 73: `script.onload = () => {` -> 执行当前语句，参与该文件整体逻辑。
- 74: `const L = window.L` -> 声明变量或常量，保存运行时数据。
- 75: `if (!L) {` -> 条件判断分支，根据场景执行不同逻辑。
- 76: `reject(new Error('Leaflet loaded but window.L is missing'))` -> 执行当前语句，参与该文件整体逻辑。
- 77: `return` -> 返回结果或提前结束当前流程。
- 78: `}` -> 结束当前语句或代码块。
- 79: `(空行)` -> 空行，用于提升代码结构可读性。
- 80: `// Fix default icon urls (CDN).` -> 注释行，用于解释设计意图或使用说明。
- 81: `const iconBase = 'https://unpkg.com/leaflet@1.9.4/dist/images/'` -> 声明变量或常量，保存运行时数据。
- 82: `L.Icon.Default.mergeOptions({` -> 执行当前语句，参与该文件整体逻辑。
- 83: `iconRetinaUrl: \`${iconBase}marker-icon-2x.png\`,` -> 执行当前语句，参与该文件整体逻辑。
- 84: `iconUrl: \`${iconBase}marker-icon.png\`,` -> 执行当前语句，参与该文件整体逻辑。
- 85: `shadowUrl: \`${iconBase}marker-shadow.png\`,` -> 执行当前语句，参与该文件整体逻辑。
- 86: `})` -> 执行当前语句，参与该文件整体逻辑。
- 87: `resolve(L)` -> 执行当前语句，参与该文件整体逻辑。
- 88: `}` -> 结束当前语句或代码块。
- 89: `script.onerror = () => reject(new Error('Failed to load Leaflet JS'))` -> 执行当前语句，参与该文件整体逻辑。
- 90: `document.body.appendChild(script)` -> 执行当前语句，参与该文件整体逻辑。
- 91: `})` -> 执行当前语句，参与该文件整体逻辑。
- 92: `(空行)` -> 空行，用于提升代码结构可读性。
- 93: `return leafletLoaderPromise` -> 返回结果或提前结束当前流程。
- 94: `}` -> 结束当前语句或代码块。
- 95: `(空行)` -> 空行，用于提升代码结构可读性。
- 96: `type LeafletMapProps = {` -> 定义类型或类结构，约束数据与行为。
- 97: `products: ProductLike[]` -> 执行当前语句，参与该文件整体逻辑。
- 98: `selectedId: string | null` -> 执行当前语句，参与该文件整体逻辑。
- 99: `onSelect: (id: string) => void` -> 执行当前语句，参与该文件整体逻辑。
- 100: `heightClass?: string` -> 执行当前语句，参与该文件整体逻辑。
- 101: `style?: CSSProperties` -> 执行当前语句，参与该文件整体逻辑。
- 102: `}` -> 结束当前语句或代码块。
- 103: `(空行)` -> 空行，用于提升代码结构可读性。
- 104: `/**` -> 注释行，用于解释设计意图或使用说明。
- 105: `* Leaflet 地图组件` -> 注释行，用于解释设计意图或使用说明。
- 106: `* @description 展示产品/目的地位置的交互式地图` -> 注释行，用于解释设计意图或使用说明。
- 107: `* @param props - 组件属性` -> 注释行，用于解释设计意图或使用说明。
- 108: `* @param props.products - 产品列表（含经纬度）` -> 注释行，用于解释设计意图或使用说明。
- 109: `* @param props.selectedId - 选中的产品ID` -> 注释行，用于解释设计意图或使用说明。
- 110: `* @param props.onSelect - 选中回调` -> 注释行，用于解释设计意图或使用说明。
- 111: `* @param props.heightClass - 地图高度样式` -> 注释行，用于解释设计意图或使用说明。
- 112: `* @param props.style - 自定义样式` -> 注释行，用于解释设计意图或使用说明。
- 113: `*/` -> 注释行，用于解释设计意图或使用说明。
- 114: `export function LeafletMap({ products, selectedId, onSelect, heightClass = 'h-[560px]', style }: LeafletMapProps) {` -> 导出当前声明，供其他模块复用。
- 115: `const containerRef = useRef<HTMLDivElement | null>(null)` -> 声明变量或常量，保存运行时数据。
- 116: `const mapRef = useRef<any>(null)` -> 声明变量或常量，保存运行时数据。
- 117: `const markersRef = useRef<Map<string, any>>(new Map())` -> 声明变量或常量，保存运行时数据。
- 118: `(空行)` -> 空行，用于提升代码结构可读性。
- 119: `const markers = useMemo(() => {` -> 声明变量或常量，保存运行时数据。
- 120: `return products` -> 返回结果或提前结束当前流程。
- 121: `.map((p) => {` -> 执行当前语句，参与该文件整体逻辑。
- 122: `const coords = getLatLng(p)` -> 声明变量或常量，保存运行时数据。
- 123: `if (!coords) return null` -> 条件判断分支，根据场景执行不同逻辑。
- 124: `return { product: p, coords }` -> 返回结果或提前结束当前流程。
- 125: `})` -> 执行当前语句，参与该文件整体逻辑。
- 126: `.filter(Boolean) as Array<{ product: ProductLike; coords: { lat: number; lng: number } }>` -> 执行当前语句，参与该文件整体逻辑。
- 127: `}, [products])` -> 执行当前语句，参与该文件整体逻辑。
- 128: `(空行)` -> 空行，用于提升代码结构可读性。
- 129: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 130: `let cancelled = false` -> 声明变量或常量，保存运行时数据。
- 131: `async function init() {` -> 定义函数或方法，实现具体业务逻辑。
- 132: `const L = await ensureLeaflet()` -> 声明变量或常量，保存运行时数据。
- 133: `if (cancelled || !L) return` -> 条件判断分支，根据场景执行不同逻辑。
- 134: `if (!containerRef.current) return` -> 条件判断分支，根据场景执行不同逻辑。
- 135: `(空行)` -> 空行，用于提升代码结构可读性。
- 136: `if (!mapRef.current) {` -> 条件判断分支，根据场景执行不同逻辑。
- 137: `mapRef.current = L.map(containerRef.current, {` -> 执行当前语句，参与该文件整体逻辑。
- 138: `zoomControl: true,` -> 执行当前语句，参与该文件整体逻辑。
- 139: `scrollWheelZoom: true,` -> 执行当前语句，参与该文件整体逻辑。
- 140: `})` -> 执行当前语句，参与该文件整体逻辑。
- 141: `L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {` -> 执行当前语句，参与该文件整体逻辑。
- 142: `attribution: '&copy; OpenStreetMap contributors',` -> 执行当前语句，参与该文件整体逻辑。
- 143: `}).addTo(mapRef.current)` -> 执行当前语句，参与该文件整体逻辑。
- 144: `}` -> 结束当前语句或代码块。
- 145: `}` -> 结束当前语句或代码块。
- 146: `init()` -> 执行当前语句，参与该文件整体逻辑。
- 147: `return () => {` -> 返回结果或提前结束当前流程。
- 148: `cancelled = true` -> 执行当前语句，参与该文件整体逻辑。
- 149: `}` -> 结束当前语句或代码块。
- 150: `}, [])` -> 执行当前语句，参与该文件整体逻辑。
- 151: `(空行)` -> 空行，用于提升代码结构可读性。
- 152: `// 1) 产品变化时：重建 marker 并自动适配地图范围` -> 注释行，用于解释设计意图或使用说明。
- 153: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 154: `const L = typeof window !== 'undefined' ? window.L : null` -> 声明变量或常量，保存运行时数据。
- 155: `const map = mapRef.current` -> 声明变量或常量，保存运行时数据。
- 156: `if (!L || !map) return` -> 条件判断分支，根据场景执行不同逻辑。
- 157: `(空行)` -> 空行，用于提升代码结构可读性。
- 158: `// Remove old markers.` -> 注释行，用于解释设计意图或使用说明。
- 159: `for (const marker of markersRef.current.values()) {` -> 循环处理集合或重复执行逻辑。
- 160: `try {` -> 异常处理逻辑，提升程序健壮性。
- 161: `marker.remove()` -> 执行当前语句，参与该文件整体逻辑。
- 162: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 163: `// ignore` -> 注释行，用于解释设计意图或使用说明。
- 164: `}` -> 结束当前语句或代码块。
- 165: `}` -> 结束当前语句或代码块。
- 166: `markersRef.current.clear()` -> 执行当前语句，参与该文件整体逻辑。
- 167: `(空行)` -> 空行，用于提升代码结构可读性。
- 168: `const bounds: any[] = []` -> 声明变量或常量，保存运行时数据。
- 169: `(空行)` -> 空行，用于提升代码结构可读性。
- 170: `for (const m of markers) {` -> 循环处理集合或重复执行逻辑。
- 171: `const id = String(m.product.id)` -> 声明变量或常量，保存运行时数据。
- 172: `bounds.push([m.coords.lat, m.coords.lng])` -> 执行当前语句，参与该文件整体逻辑。
- 173: `(空行)` -> 空行，用于提升代码结构可读性。
- 174: `const popupHtml = \`` -> 声明变量或常量，保存运行时数据。
- 175: `<div style="min-width:140px">` -> JSX/HTML 结构行，用于描述页面元素。
- 176: `<div style="font-weight:700; margin-bottom:4px">${m.product.name ? String(m.product.name) : '产品'}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `<div style="font-size:12px; opacity:0.9">${m.product.location?.city ? String(m.product.location.city) : ''}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 178: `<div style="font-size:12px; margin-top:4px; color:#0ea5e9; font-weight:700">¥${Number(m.product.price || 0).toLocaleString()}</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 179: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `\`` -> 执行当前语句，参与该文件整体逻辑。
- 181: `(空行)` -> 空行，用于提升代码结构可读性。
- 182: `const marker = L.marker([m.coords.lat, m.coords.lng])` -> 声明变量或常量，保存运行时数据。
- 183: `marker.addTo(map)` -> 执行当前语句，参与该文件整体逻辑。
- 184: `marker.bindPopup(popupHtml)` -> 执行当前语句，参与该文件整体逻辑。
- 185: `marker.on('click', () => onSelect(id))` -> 执行当前语句，参与该文件整体逻辑。
- 186: `markersRef.current.set(id, marker)` -> 执行当前语句，参与该文件整体逻辑。
- 187: `}` -> 结束当前语句或代码块。
- 188: `(空行)` -> 空行，用于提升代码结构可读性。
- 189: `if (bounds.length) {` -> 条件判断分支，根据场景执行不同逻辑。
- 190: `map.fitBounds(bounds, { padding: [40, 40], animate: true })` -> 执行当前语句，参与该文件整体逻辑。
- 191: `}` -> 结束当前语句或代码块。
- 192: `}, [markers, onSelect])` -> 执行当前语句，参与该文件整体逻辑。
- 193: `(空行)` -> 空行，用于提升代码结构可读性。
- 194: `// 2) 选中变化时：只定位到对应 marker` -> 注释行，用于解释设计意图或使用说明。
- 195: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 196: `const map = mapRef.current` -> 声明变量或常量，保存运行时数据。
- 197: `if (!map || !selectedId) return` -> 条件判断分支，根据场景执行不同逻辑。
- 198: `if (!markersRef.current.has(selectedId)) return` -> 条件判断分支，根据场景执行不同逻辑。
- 199: `(空行)` -> 空行，用于提升代码结构可读性。
- 200: `const marker = markersRef.current.get(selectedId)` -> 声明变量或常量，保存运行时数据。
- 201: `try {` -> 异常处理逻辑，提升程序健壮性。
- 202: `const ll = marker.getLatLng()` -> 声明变量或常量，保存运行时数据。
- 203: `map.setView([ll.lat, ll.lng], 14, { animate: true })` -> 执行当前语句，参与该文件整体逻辑。
- 204: `marker.openPopup()` -> 执行当前语句，参与该文件整体逻辑。
- 205: `} catch {` -> 执行当前语句，参与该文件整体逻辑。
- 206: `// ignore` -> 注释行，用于解释设计意图或使用说明。
- 207: `}` -> 结束当前语句或代码块。
- 208: `}, [selectedId])` -> 执行当前语句，参与该文件整体逻辑。
- 209: `(空行)` -> 空行，用于提升代码结构可读性。
- 210: `return (` -> 返回结果或提前结束当前流程。
- 211: `<div` -> 执行当前语句，参与该文件整体逻辑。
- 212: `ref={containerRef}` -> 执行当前语句，参与该文件整体逻辑。
- 213: `className={heightClass}` -> 执行当前语句，参与该文件整体逻辑。
- 214: `style={{` -> 执行当前语句，参与该文件整体逻辑。
- 215: `width: '100%',` -> 执行当前语句，参与该文件整体逻辑。
- 216: `borderRadius: '14px',` -> 执行当前语句，参与该文件整体逻辑。
- 217: `overflow: 'hidden',` -> 执行当前语句，参与该文件整体逻辑。
- 218: `...style,` -> 执行当前语句，参与该文件整体逻辑。
- 219: `}}` -> 执行当前语句，参与该文件整体逻辑。
- 220: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 221: `)` -> 结束当前语句或代码块。
- 222: `}` -> 结束当前语句或代码块。
- 223: `(空行)` -> 空行，用于提升代码结构可读性。
