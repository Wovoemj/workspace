# `frontend/user-web/src/app/travel-notes/new/page.tsx` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `/**` -> 注释行，用于解释设计意图或使用说明。
- 2: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 3: `* 创建游记模块 - 游记发布编辑` -> 注释行，用于解释设计意图或使用说明。
- 4: `* =====================================================` -> 注释行，用于解释设计意图或使用说明。
- 5: `*` -> 注释行，用于解释设计意图或使用说明。
- 6: `* 【功能列表】` -> 注释行，用于解释设计意图或使用说明。
- 7: `* - 游记标题输入` -> 注释行，用于解释设计意图或使用说明。
- 8: `* - 游记内容编辑（多行文本框）` -> 注释行，用于解释设计意图或使用说明。
- 9: `* - 封面图片选择` -> 注释行，用于解释设计意图或使用说明。
- 10: `* - 目的地关联搜索与选择` -> 注释行，用于解释设计意图或使用说明。
- 11: `* - 标签添加与删除` -> 注释行，用于解释设计意图或使用说明。
- 12: `* - 发布状态选择（草稿/发布）` -> 注释行，用于解释设计意图或使用说明。
- 13: `* - 表单验证与提交` -> 注释行，用于解释设计意图或使用说明。
- 14: `* - 登录状态校验（未登录重定向到登录页）` -> 注释行，用于解释设计意图或使用说明。
- 15: `*` -> 注释行，用于解释设计意图或使用说明。
- 16: `* 【组件依赖】` -> 注释行，用于解释设计意图或使用说明。
- 17: `* - Navbar, Footer: 布局组件` -> 注释行，用于解释设计意图或使用说明。
- 18: `* - useUserStore: 用户状态（Zustand）` -> 注释行，用于解释设计意图或使用说明。
- 19: `*` -> 注释行，用于解释设计意图或使用说明。
- 20: `* 【API 接口】` -> 注释行，用于解释设计意图或使用说明。
- 21: `* - GET /api/destinations?q=xxx&limit=10: 搜索目的地` -> 注释行，用于解释设计意图或使用说明。
- 22: `* - POST /api/travel-notes: 创建游记（需登录）` -> 注释行，用于解释设计意图或使用说明。
- 23: `*` -> 注释行，用于解释设计意图或使用说明。
- 24: `* 【状态管理】` -> 注释行，用于解释设计意图或使用说明。
- 25: `* - useState: title, content, destinationId, coverImage, tags, tagInput, status, submitting, destinations, destSearch, showDestDropdown` -> 注释行，用于解释设计意图或使用说明。
- 26: `* - useEffect: 登录状态检测` -> 注释行，用于解释设计意图或使用说明。
- 27: `*/` -> 注释行，用于解释设计意图或使用说明。
- 28: `'use client'` -> 执行当前语句，参与该文件整体逻辑。
- 29: `(空行)` -> 空行，用于提升代码结构可读性。
- 30: `import { useState, useEffect } from 'react'` -> 导入依赖模块，供当前文件使用。
- 31: `import { useRouter } from 'next/navigation'` -> 导入依赖模块，供当前文件使用。
- 32: `import { toast } from 'react-hot-toast'` -> 导入依赖模块，供当前文件使用。
- 33: `import { ArrowLeft, Loader2, MapPin, X } from 'lucide-react'` -> 导入依赖模块，供当前文件使用。
- 34: `import Link from 'next/link'` -> 导入依赖模块，供当前文件使用。
- 35: `import { Navbar } from '@/components/Navbar'` -> 导入依赖模块，供当前文件使用。
- 36: `import { Footer } from '@/components/Footer'` -> 导入依赖模块，供当前文件使用。
- 37: `import { useUserStore } from '@/store'` -> 导入依赖模块，供当前文件使用。
- 38: `(空行)` -> 空行，用于提升代码结构可读性。
- 39: `type Destination = {` -> 定义类型或类结构，约束数据与行为。
- 40: `id: number` -> 执行当前语句，参与该文件整体逻辑。
- 41: `name: string` -> 执行当前语句，参与该文件整体逻辑。
- 42: `city: string` -> 执行当前语句，参与该文件整体逻辑。
- 43: `province: string` -> 执行当前语句，参与该文件整体逻辑。
- 44: `}` -> 结束当前语句或代码块。
- 45: `(空行)` -> 空行，用于提升代码结构可读性。
- 46: `export default function NewTravelNotePage() {` -> 导出当前声明，供其他模块复用。
- 47: `const router = useRouter()` -> 声明变量或常量，保存运行时数据。
- 48: `const { isAuthenticated, user } = useUserStore()` -> 声明变量或常量，保存运行时数据。
- 49: `(空行)` -> 空行，用于提升代码结构可读性。
- 50: `const [title, setTitle] = useState('')` -> 声明变量或常量，保存运行时数据。
- 51: `const [content, setContent] = useState('')` -> 声明变量或常量，保存运行时数据。
- 52: `const [destinationId, setDestinationId] = useState<number | null>(null)` -> 声明变量或常量，保存运行时数据。
- 53: `const [coverImage, setCoverImage] = useState('')` -> 声明变量或常量，保存运行时数据。
- 54: `const [tags, setTags] = useState<string[]>([])` -> 声明变量或常量，保存运行时数据。
- 55: `const [tagInput, setTagInput] = useState('')` -> 声明变量或常量，保存运行时数据。
- 56: `const [status, setStatus] = useState('draft')` -> 声明变量或常量，保存运行时数据。
- 57: `const [submitting, setSubmitting] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 58: `const [destinations, setDestinations] = useState<Destination[]>([])` -> 声明变量或常量，保存运行时数据。
- 59: `const [destSearch, setDestSearch] = useState('')` -> 声明变量或常量，保存运行时数据。
- 60: `const [showDestDropdown, setShowDestDropdown] = useState(false)` -> 声明变量或常量，保存运行时数据。
- 61: `(空行)` -> 空行，用于提升代码结构可读性。
- 62: `useEffect(() => {` -> 执行当前语句，参与该文件整体逻辑。
- 63: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 64: `router.push('/login?redirect=/travel-notes/new')` -> 执行当前语句，参与该文件整体逻辑。
- 65: `}` -> 结束当前语句或代码块。
- 66: `}, [isAuthenticated, router])` -> 执行当前语句，参与该文件整体逻辑。
- 67: `(空行)` -> 空行，用于提升代码结构可读性。
- 68: `const searchDestinations = async (q: string) => {` -> 声明变量或常量，保存运行时数据。
- 69: `if (!q) {` -> 条件判断分支，根据场景执行不同逻辑。
- 70: `setDestinations([])` -> 执行当前语句，参与该文件整体逻辑。
- 71: `return` -> 返回结果或提前结束当前流程。
- 72: `}` -> 结束当前语句或代码块。
- 73: `try {` -> 异常处理逻辑，提升程序健壮性。
- 74: `const res = await fetch(\`/api/destinations?q=${encodeURIComponent(q)}&limit=10\`)` -> 声明变量或常量，保存运行时数据。
- 75: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 76: `setDestinations(data.destinations || [])` -> 执行当前语句，参与该文件整体逻辑。
- 77: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 78: `console.error(e)` -> 执行当前语句，参与该文件整体逻辑。
- 79: `}` -> 结束当前语句或代码块。
- 80: `}` -> 结束当前语句或代码块。
- 81: `(空行)` -> 空行，用于提升代码结构可读性。
- 82: `const handleDestSearch = (e: React.ChangeEvent<HTMLInputElement>) => {` -> 声明变量或常量，保存运行时数据。
- 83: `const v = e.target.value` -> 声明变量或常量，保存运行时数据。
- 84: `setDestSearch(v)` -> 执行当前语句，参与该文件整体逻辑。
- 85: `searchDestinations(v)` -> 执行当前语句，参与该文件整体逻辑。
- 86: `setShowDestDropdown(true)` -> 执行当前语句，参与该文件整体逻辑。
- 87: `}` -> 结束当前语句或代码块。
- 88: `(空行)` -> 空行，用于提升代码结构可读性。
- 89: `const selectDestination = (d: Destination) => {` -> 声明变量或常量，保存运行时数据。
- 90: `setDestinationId(d.id)` -> 执行当前语句，参与该文件整体逻辑。
- 91: `setDestSearch(\`${d.city} · ${d.name}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 92: `setShowDestDropdown(false)` -> 执行当前语句，参与该文件整体逻辑。
- 93: `}` -> 结束当前语句或代码块。
- 94: `(空行)` -> 空行，用于提升代码结构可读性。
- 95: `const addTag = () => {` -> 声明变量或常量，保存运行时数据。
- 96: `const tag = tagInput.trim()` -> 声明变量或常量，保存运行时数据。
- 97: `if (tag && !tags.includes(tag)) {` -> 条件判断分支，根据场景执行不同逻辑。
- 98: `setTags([...tags, tag])` -> 执行当前语句，参与该文件整体逻辑。
- 99: `setTagInput('')` -> 执行当前语句，参与该文件整体逻辑。
- 100: `}` -> 结束当前语句或代码块。
- 101: `}` -> 结束当前语句或代码块。
- 102: `(空行)` -> 空行，用于提升代码结构可读性。
- 103: `const removeTag = (tag: string) => {` -> 声明变量或常量，保存运行时数据。
- 104: `setTags(tags.filter(t => t !== tag))` -> 执行当前语句，参与该文件整体逻辑。
- 105: `}` -> 结束当前语句或代码块。
- 106: `(空行)` -> 空行，用于提升代码结构可读性。
- 107: `const handleSubmit = async (publish: boolean = false) => {` -> 声明变量或常量，保存运行时数据。
- 108: `if (!title.trim()) {` -> 条件判断分支，根据场景执行不同逻辑。
- 109: `toast.error('请输入标题')` -> 执行当前语句，参与该文件整体逻辑。
- 110: `return` -> 返回结果或提前结束当前流程。
- 111: `}` -> 结束当前语句或代码块。
- 112: `if (!content.trim()) {` -> 条件判断分支，根据场景执行不同逻辑。
- 113: `toast.error('请输入内容')` -> 执行当前语句，参与该文件整体逻辑。
- 114: `return` -> 返回结果或提前结束当前流程。
- 115: `}` -> 结束当前语句或代码块。
- 116: `(空行)` -> 空行，用于提升代码结构可读性。
- 117: `setSubmitting(true)` -> 执行当前语句，参与该文件整体逻辑。
- 118: `try {` -> 异常处理逻辑，提升程序健壮性。
- 119: `const res = await fetch('/api/travel-notes', {` -> 声明变量或常量，保存运行时数据。
- 120: `method: 'POST',` -> 执行当前语句，参与该文件整体逻辑。
- 121: `headers: { 'Content-Type': 'application/json' },` -> 执行当前语句，参与该文件整体逻辑。
- 122: `body: JSON.stringify({` -> 执行当前语句，参与该文件整体逻辑。
- 123: `title: title.trim(),` -> 执行当前语句，参与该文件整体逻辑。
- 124: `content: content.trim(),` -> 执行当前语句，参与该文件整体逻辑。
- 125: `destination_id: destinationId,` -> 执行当前语句，参与该文件整体逻辑。
- 126: `cover_image: coverImage || undefined,` -> 执行当前语句，参与该文件整体逻辑。
- 127: `tags,` -> 执行当前语句，参与该文件整体逻辑。
- 128: `status: publish ? 'published' : status` -> 执行当前语句，参与该文件整体逻辑。
- 129: `})` -> 执行当前语句，参与该文件整体逻辑。
- 130: `})` -> 执行当前语句，参与该文件整体逻辑。
- 131: `const data = await res.json()` -> 声明变量或常量，保存运行时数据。
- 132: `(空行)` -> 空行，用于提升代码结构可读性。
- 133: `if (data.success) {` -> 条件判断分支，根据场景执行不同逻辑。
- 134: `toast.success(publish ? '发布成功' : '保存成功')` -> 执行当前语句，参与该文件整体逻辑。
- 135: `router.push(\`/travel-notes/${data.travel_note.id}\`)` -> 执行当前语句，参与该文件整体逻辑。
- 136: `} else {` -> 执行当前语句，参与该文件整体逻辑。
- 137: `toast.error(data.error || '提交失败')` -> 执行当前语句，参与该文件整体逻辑。
- 138: `}` -> 结束当前语句或代码块。
- 139: `} catch (e) {` -> 执行当前语句，参与该文件整体逻辑。
- 140: `console.error(e)` -> 执行当前语句，参与该文件整体逻辑。
- 141: `toast.error('提交失败')` -> 执行当前语句，参与该文件整体逻辑。
- 142: `} finally {` -> 执行当前语句，参与该文件整体逻辑。
- 143: `setSubmitting(false)` -> 执行当前语句，参与该文件整体逻辑。
- 144: `}` -> 结束当前语句或代码块。
- 145: `}` -> 结束当前语句或代码块。
- 146: `(空行)` -> 空行，用于提升代码结构可读性。
- 147: `if (!isAuthenticated) {` -> 条件判断分支，根据场景执行不同逻辑。
- 148: `return null` -> 返回结果或提前结束当前流程。
- 149: `}` -> 结束当前语句或代码块。
- 150: `(空行)` -> 空行，用于提升代码结构可读性。
- 151: `return (` -> 返回结果或提前结束当前流程。
- 152: `<div className="min-h-screen bg-gradient-to-br from-amber-50 via-orange-50 to-sky-50">` -> JSX/HTML 结构行，用于描述页面元素。
- 153: `<Navbar />` -> JSX/HTML 结构行，用于描述页面元素。
- 154: `<main className="pt-20 pb-16">` -> JSX/HTML 结构行，用于描述页面元素。
- 155: `<div className="max-w-3xl mx-auto px-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 156: `{/* Back */}` -> 执行当前语句，参与该文件整体逻辑。
- 157: `<Link href="/travel-notes" className="inline-flex items-center gap-2 text-sky-600 hover:text-sky-700 mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 158: `<ArrowLeft className="h-5 w-5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 159: `返回攻略` -> 执行当前语句，参与该文件整体逻辑。
- 160: `</Link>` -> JSX/HTML 结构行，用于描述页面元素。
- 161: `(空行)` -> 空行，用于提升代码结构可读性。
- 162: `<div className="bg-white rounded-2xl p-6 shadow-sm">` -> JSX/HTML 结构行，用于描述页面元素。
- 163: `<h1 className="text-2xl font-black text-gray-900 mb-6">写游记</h1>` -> JSX/HTML 结构行，用于描述页面元素。
- 164: `(空行)` -> 空行，用于提升代码结构可读性。
- 165: `{/* Title */}` -> 执行当前语句，参与该文件整体逻辑。
- 166: `<div className="mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 167: `<label className="block text-sm font-medium text-gray-700 mb-1">标题 *</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 168: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 169: `type="text"` -> 执行当前语句，参与该文件整体逻辑。
- 170: `value={title}` -> 执行当前语句，参与该文件整体逻辑。
- 171: `onChange={(e) => setTitle(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 172: `placeholder="给你的游记起个标题"` -> 执行当前语句，参与该文件整体逻辑。
- 173: `className="w-full px-4 py-2.5 rounded-xl border border-gray-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-100 outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 174: `maxLength={200}` -> 执行当前语句，参与该文件整体逻辑。
- 175: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 176: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 177: `(空行)` -> 空行，用于提升代码结构可读性。
- 178: `{/* Destination */}` -> 执行当前语句，参与该文件整体逻辑。
- 179: `<div className="mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 180: `<label className="block text-sm font-medium text-gray-700 mb-1">关联目的地</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 181: `<div className="relative">` -> JSX/HTML 结构行，用于描述页面元素。
- 182: `<MapPin className="absolute left-3 top-1/2 -translate-y-1/2 h-5 w-5 text-gray-400" />` -> JSX/HTML 结构行，用于描述页面元素。
- 183: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 184: `type="text"` -> 执行当前语句，参与该文件整体逻辑。
- 185: `value={destSearch}` -> 执行当前语句，参与该文件整体逻辑。
- 186: `onChange={handleDestSearch}` -> 执行当前语句，参与该文件整体逻辑。
- 187: `onFocus={() => setShowDestDropdown(true)}` -> 执行当前语句，参与该文件整体逻辑。
- 188: `placeholder="搜索目的地（可选）"` -> 执行当前语句，参与该文件整体逻辑。
- 189: `className="w-full pl-10 pr-4 py-2.5 rounded-xl border border-gray-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-100 outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 190: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 191: `{showDestDropdown && destinations.length > 0 && (` -> 执行当前语句，参与该文件整体逻辑。
- 192: `<div className="absolute z-10 w-full mt-1 bg-white border border-gray-200 rounded-xl shadow-lg max-h-60 overflow-auto">` -> JSX/HTML 结构行，用于描述页面元素。
- 193: `{destinations.map((d) => (` -> 执行当前语句，参与该文件整体逻辑。
- 194: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 195: `key={d.id}` -> 执行当前语句，参与该文件整体逻辑。
- 196: `onClick={() => selectDestination(d)}` -> 执行当前语句，参与该文件整体逻辑。
- 197: `className="w-full text-left px-4 py-2 hover:bg-gray-50"` -> 执行当前语句，参与该文件整体逻辑。
- 198: `>` -> 执行当前语句，参与该文件整体逻辑。
- 199: `<span className="font-medium">{d.name}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 200: `<span className="text-gray-500 text-sm ml-2">{d.city} · {d.province}</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 201: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 202: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 203: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 204: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 205: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 206: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 207: `(空行)` -> 空行，用于提升代码结构可读性。
- 208: `{/* Cover Image */}` -> 执行当前语句，参与该文件整体逻辑。
- 209: `<div className="mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 210: `<label className="block text-sm font-medium text-gray-700 mb-1">封面图片 URL</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 211: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 212: `type="text"` -> 执行当前语句，参与该文件整体逻辑。
- 213: `value={coverImage}` -> 执行当前语句，参与该文件整体逻辑。
- 214: `onChange={(e) => setCoverImage(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 215: `placeholder="https://..."` -> 执行当前语句，参与该文件整体逻辑。
- 216: `className="w-full px-4 py-2.5 rounded-xl border border-gray-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-100 outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 217: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 218: `{coverImage && (` -> 执行当前语句，参与该文件整体逻辑。
- 219: `<div className="mt-2 rounded-lg overflow-hidden h-32">` -> JSX/HTML 结构行，用于描述页面元素。
- 220: `<img src={coverImage} alt="封面预览" className="w-full h-full object-cover" />` -> JSX/HTML 结构行，用于描述页面元素。
- 221: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 222: `)}` -> 执行当前语句，参与该文件整体逻辑。
- 223: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 224: `(空行)` -> 空行，用于提升代码结构可读性。
- 225: `{/* Tags */}` -> 执行当前语句，参与该文件整体逻辑。
- 226: `<div className="mb-4">` -> JSX/HTML 结构行，用于描述页面元素。
- 227: `<label className="block text-sm font-medium text-gray-700 mb-1">标签</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 228: `<div className="flex flex-wrap gap-2 mb-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 229: `{tags.map((tag) => (` -> 执行当前语句，参与该文件整体逻辑。
- 230: `<span key={tag} className="inline-flex items-center gap-1 px-3 py-1 bg-sky-50 text-sky-600 text-sm rounded-full">` -> JSX/HTML 结构行，用于描述页面元素。
- 231: `{tag}` -> 执行当前语句，参与该文件整体逻辑。
- 232: `<button onClick={() => removeTag(tag)} className="hover:text-red-500">` -> JSX/HTML 结构行，用于描述页面元素。
- 233: `<X className="h-3.5 w-3.5" />` -> JSX/HTML 结构行，用于描述页面元素。
- 234: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 235: `</span>` -> JSX/HTML 结构行，用于描述页面元素。
- 236: `))}` -> 执行当前语句，参与该文件整体逻辑。
- 237: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 238: `<div className="flex gap-2">` -> JSX/HTML 结构行，用于描述页面元素。
- 239: `<input` -> 执行当前语句，参与该文件整体逻辑。
- 240: `type="text"` -> 执行当前语句，参与该文件整体逻辑。
- 241: `value={tagInput}` -> 执行当前语句，参与该文件整体逻辑。
- 242: `onChange={(e) => setTagInput(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 243: `onKeyDown={(e) => e.key === 'Enter' && (e.preventDefault(), addTag())}` -> 执行当前语句，参与该文件整体逻辑。
- 244: `placeholder="添加标签，回车确认"` -> 执行当前语句，参与该文件整体逻辑。
- 245: `className="flex-1 px-4 py-2 rounded-xl border border-gray-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-100 outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 246: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 247: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 248: `onClick={addTag}` -> 执行当前语句，参与该文件整体逻辑。
- 249: `className="px-4 py-2 border border-gray-200 rounded-xl hover:bg-gray-50"` -> 执行当前语句，参与该文件整体逻辑。
- 250: `>` -> 执行当前语句，参与该文件整体逻辑。
- 251: `添加` -> 执行当前语句，参与该文件整体逻辑。
- 252: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 253: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 254: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 255: `(空行)` -> 空行，用于提升代码结构可读性。
- 256: `{/* Content */}` -> 执行当前语句，参与该文件整体逻辑。
- 257: `<div className="mb-6">` -> JSX/HTML 结构行，用于描述页面元素。
- 258: `<label className="block text-sm font-medium text-gray-700 mb-1">内容 *</label>` -> JSX/HTML 结构行，用于描述页面元素。
- 259: `<textarea` -> 执行当前语句，参与该文件整体逻辑。
- 260: `value={content}` -> 执行当前语句，参与该文件整体逻辑。
- 261: `onChange={(e) => setContent(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 262: `placeholder="记录你的旅行故事..."` -> 执行当前语句，参与该文件整体逻辑。
- 263: `rows={12}` -> 执行当前语句，参与该文件整体逻辑。
- 264: `className="w-full px-4 py-2.5 rounded-xl border border-gray-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-100 outline-none resize-none"` -> 执行当前语句，参与该文件整体逻辑。
- 265: `/>` -> 执行当前语句，参与该文件整体逻辑。
- 266: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 267: `(空行)` -> 空行，用于提升代码结构可读性。
- 268: `{/* Actions */}` -> 执行当前语句，参与该文件整体逻辑。
- 269: `<div className="flex items-center justify-between">` -> JSX/HTML 结构行，用于描述页面元素。
- 270: `<select` -> 执行当前语句，参与该文件整体逻辑。
- 271: `value={status}` -> 执行当前语句，参与该文件整体逻辑。
- 272: `onChange={(e) => setStatus(e.target.value)}` -> 执行当前语句，参与该文件整体逻辑。
- 273: `className="px-4 py-2 rounded-xl border border-gray-200 outline-none"` -> 执行当前语句，参与该文件整体逻辑。
- 274: `>` -> 执行当前语句，参与该文件整体逻辑。
- 275: `<option value="draft">存为草稿</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 276: `<option value="published">直接发布</option>` -> JSX/HTML 结构行，用于描述页面元素。
- 277: `</select>` -> JSX/HTML 结构行，用于描述页面元素。
- 278: `(空行)` -> 空行，用于提升代码结构可读性。
- 279: `<div className="flex gap-3">` -> JSX/HTML 结构行，用于描述页面元素。
- 280: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 281: `onClick={() => handleSubmit(false)}` -> 执行当前语句，参与该文件整体逻辑。
- 282: `disabled={submitting}` -> 执行当前语句，参与该文件整体逻辑。
- 283: `className="px-6 py-2.5 border border-gray-200 rounded-xl font-medium hover:bg-gray-50 disabled:opacity-50"` -> 执行当前语句，参与该文件整体逻辑。
- 284: `>` -> 执行当前语句，参与该文件整体逻辑。
- 285: `保存草稿` -> 执行当前语句，参与该文件整体逻辑。
- 286: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 287: `<button` -> 执行当前语句，参与该文件整体逻辑。
- 288: `onClick={() => handleSubmit(true)}` -> 执行当前语句，参与该文件整体逻辑。
- 289: `disabled={submitting}` -> 执行当前语句，参与该文件整体逻辑。
- 290: `className="flex items-center gap-2 px-6 py-2.5 bg-gradient-to-r from-sky-600 to-indigo-600 text-white font-semibold rounded-xl hover:from-sky-700 hover:to-indigo-700 disabled:opacity-50"` -> 执行当前语句，参与该文件整体逻辑。
- 291: `>` -> 执行当前语句，参与该文件整体逻辑。
- 292: `{submitting && <Loader2 className="h-4 w-4 animate-spin" />}` -> 执行当前语句，参与该文件整体逻辑。
- 293: `发布游记` -> 执行当前语句，参与该文件整体逻辑。
- 294: `</button>` -> JSX/HTML 结构行，用于描述页面元素。
- 295: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 296: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 297: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 298: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 299: `</main>` -> JSX/HTML 结构行，用于描述页面元素。
- 300: `<Footer />` -> JSX/HTML 结构行，用于描述页面元素。
- 301: `</div>` -> JSX/HTML 结构行，用于描述页面元素。
- 302: `)` -> 结束当前语句或代码块。
- 303: `}` -> 结束当前语句或代码块。
