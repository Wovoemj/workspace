# `frontend/user-web/src/app/notifications/page.tsx` 业务语义增强版逐行说明

说明：每行包含“作用”和“原因”，用于把实现细节映射到业务意图。

- 1: `/**`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 2: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 3: `* 通知中心模块 - 用户消息通知管理`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 4: `* =====================================================`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 5: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 6: `* 【功能列表】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 7: `* - 未读通知计数展示`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 8: `* - 通知列表展示（标题、内容、创建时间）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 9: `* - 单条标记已读功能`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 10: `* - 全部标记已读功能`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 11: `* - 已读/未读状态区分样式（未读：靛蓝色背景；已读：灰色背景）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 12: `* - 分页加载（每页 20 条）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 13: `* - 登录状态校验`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 14: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 15: `* 【组件依赖】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 16: `* - Navbar, Footer: 布局组件`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 17: `* - useUserStore: 用户状态（Zustand）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 18: `* - AppNotification 类型定义`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 19: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 20: `* 【API 接口】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 21: `* - GET /api/notifications?user_id=xxx&page=1&per_page=20: 获取通知列表`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 22: `* - PUT /api/notifications/${id}/read: 标记单条通知已读`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 23: `* - PUT /api/notifications/read-all: 标记全部已读`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 24: `*`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 25: `* 【状态管理】`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 26: `* - useState: notifications(通知列表), loading, error, unreadCount(计算属性)`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 27: `* - useMemo: unreadCount 计算（过滤未读通知数量）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 28: `* - 依赖: isAuthenticated, user?.id（登录状态变化时重新加载）`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 29: `*/`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 30: `'use client'`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 31: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 32: `import { useEffect, useMemo, useState } from 'react'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 33: `import { Navbar } from '@/components/Navbar'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 34: `import { Footer } from '@/components/Footer'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 35: `import { useUserStore } from '@/store'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 36: `import { AppNotification } from '@/types'`
  - 作用：导入依赖。
  - 原因：显式依赖便于构建、重构和静态分析。
- 37: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 38: `const PAGE_SIZE = 20`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 39: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 40: `export default function NotificationsPage() {`
  - 作用：导出声明供其他模块复用。
  - 原因：减少重复实现，提升模块化程度。
- 41: `const { user, isAuthenticated } = useUserStore()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 42: `const [notifications, setNotifications] = useState<AppNotification[]>([])`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 43: `const [loading, setLoading] = useState(true)`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 44: `const [error, setError] = useState('')`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 45: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 46: `const backendUrl = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5001'`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 47: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 48: `const unreadCount = useMemo(`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 49: `() => notifications.filter((n) => !n.is_read).length,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 50: `[notifications]`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 51: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 52: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 53: `const fetchNotifications = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 54: `if (!isAuthenticated || !user?.id) {`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 55: `setNotifications([])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 56: `setLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 57: `return`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 58: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 59: `setLoading(true)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 60: `setError('')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 61: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 62: `const res = await fetch(`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 63: `\`${backendUrl}/api/notifications?user_id=${encodeURIComponent(user.id)}&page=1&per_page=${PAGE_SIZE}\`,`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 64: `{ cache: 'no-store' }`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 65: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 66: `if (!res.ok) throw new Error('获取通知失败')`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 67: `const data = await res.json()`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 68: `if (!data?.success) throw new Error(data?.error || '获取通知失败')`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 69: `setNotifications(data.notifications || [])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 70: `} catch (e: any) {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 71: `setError(e?.message || '获取通知失败')`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 72: `} finally {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 73: `setLoading(false)`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 74: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 75: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 76: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 77: `const markRead = async (id: number) => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 78: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 79: `const res = await fetch(\`${backendUrl}/api/notifications/${id}/read\`, { method: 'PUT' })`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 80: `if (!res.ok) throw new Error('操作失败')`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 81: `setNotifications((prev) =>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 82: `prev.map((n) => (n.id === id ? { ...n, is_read: true, read_at: new Date().toISOString() } : n))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 83: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 84: `} catch {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 85: `// 静默失败，避免打断阅?`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 86: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 87: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 88: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 89: `const markAllRead = async () => {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 90: `if (!user?.id) return`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 91: `try {`
  - 作用：异常处理逻辑。
  - 原因：避免异常直接中断主流程，提高健壮性。
- 92: `const res = await fetch(\`${backendUrl}/api/notifications/read-all\`, {`
  - 作用：声明变量或常量。
  - 原因：显式命名中间状态，提升可读性和调试性。
- 93: `method: 'PUT',`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 94: `headers: { 'Content-Type': 'application/json' },`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 95: `body: JSON.stringify({ user_id: user.id }),`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 96: `})`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 97: `if (!res.ok) throw new Error('操作失败')`
  - 作用：条件分支处理。
  - 原因：提前处理异常路径可减少主流程嵌套。
- 98: `setNotifications((prev) => prev.map((n) => ({ ...n, is_read: true })))`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 99: `} catch {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 100: `// 静默失败`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 101: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 102: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 103: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 104: `useEffect(() => {`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 105: `fetchNotifications()`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 106: `// eslint-disable-next-line react-hooks/exhaustive-deps`
  - 作用：注释说明设计意图或上下文。
  - 原因：让维护者在不追踪历史的情况下理解作者决策。
- 107: `}, [isAuthenticated, user?.id])`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 108: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 109: `return (`
  - 作用：返回结果或视图。
  - 原因：早返回减少分支深度并提升可读性。
- 110: `<div className="min-h-screen page-bg">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 111: `<Navbar />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 112: `<main className="pt-20 pb-10">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 113: `<div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 114: `<div className="bg-white rounded-2xl shadow p-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 115: `<div className="flex items-center justify-between mb-6">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 116: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 117: `<h1 className="text-2xl font-bold text-gray-900">通知中心</h1>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 118: `<p className="text-sm text-gray-500 mt-1">未读 ({unreadCount})</p>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 119: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 120: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 121: `onClick={markAllRead}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 122: `className="px-4 py-2 rounded-lg bg-indigo-600 text-white hover:bg-indigo-700 transition-colors"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 123: `disabled={!notifications.length}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 124: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 125: `全部标记已读`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 126: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 127: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 128: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 129: `{!isAuthenticated && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 130: `<div className="text-gray-600">请先登录后查看通知</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 131: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 132: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 133: `{isAuthenticated && loading && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 134: `<div className="text-gray-500">加载?..</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 135: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 136: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 137: `{isAuthenticated && !loading && error && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 138: `<div className="text-red-500">{error}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 139: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 140: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 141: `{isAuthenticated && !loading && !error && notifications.length === 0 && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 142: `<div className="text-gray-500">暂无通知</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 143: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 144: `(空行)`
  - 作用：空行分隔逻辑块。
  - 原因：减少视觉噪音，便于快速理解结构。
- 145: `{isAuthenticated && !loading && !error && notifications.length > 0 && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 146: `<div className="space-y-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 147: `{notifications.map((n) => (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 148: `<div`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 149: `key={n.id}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 150: `className={\`border rounded-xl p-4 ${n.is_read ? 'bg-gray-50 border-gray-200' : 'bg-indigo-50 border-indigo-200'}\`}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 151: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 152: `<div className="flex items-start justify-between gap-3">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 153: `<div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 154: `<div className="font-semibold text-gray-900">{n.title}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 155: `<div className="text-sm text-gray-600 mt-1">{n.content}</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 156: `<div className="text-xs text-gray-400 mt-2">`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 157: `{new Date(n.created_at).toLocaleString()}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 158: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 159: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 160: `{!n.is_read && (`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 161: `<button`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 162: `onClick={() => markRead(n.id)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 163: `className="text-sm px-3 py-1 rounded-md border border-indigo-300 text-indigo-700 hover:bg-indigo-100"`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 164: `>`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 165: `标记已读`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 166: `</button>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 167: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 168: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 169: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 170: `))}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 171: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 172: `)}`
  - 作用：执行当前语句。
  - 原因：参与本文件主流程，完成具体业务步骤。
- 173: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 174: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 175: `</main>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 176: `<Footer />`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 177: `</div>`
  - 作用：渲染 JSX/HTML 结构。
  - 原因：声明式视图让状态与界面映射更清晰。
- 178: `)`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
- 179: `}`
  - 作用：结束当前语句块。
  - 原因：明确作用域边界，避免逻辑泄漏。
