# `models.py` 逐行说明

说明：本文件按“代码行号 -> 代码 -> 作用”解释每一行。

- 1: `# ============================================` -> 注释行，用于解释设计意图或使用说明。
- 2: `# 数据模型定义 - 智能旅游助手` -> 注释行，用于解释设计意图或使用说明。
- 3: `# ============================================` -> 注释行，用于解释设计意图或使用说明。
- 4: `#` -> 注释行，用于解释设计意图或使用说明。
- 5: `# 【模块说明】` -> 注释行，用于解释设计意图或使用说明。
- 6: `# - 定义项目所有数据库表结构` -> 注释行，用于解释设计意图或使用说明。
- 7: `# - 使用 SQLAlchemy ORM 进行数据库操作` -> 注释行，用于解释设计意图或使用说明。
- 8: `# - 每个模型类对应一张数据库表` -> 注释行，用于解释设计意图或使用说明。
- 9: `#` -> 注释行，用于解释设计意图或使用说明。
- 10: `# 【数据表清单】` -> 注释行，用于解释设计意图或使用说明。
- 11: `# 1. Destination - 景点表` -> 注释行，用于解释设计意图或使用说明。
- 12: `# 2. User - 用户表` -> 注释行，用于解释设计意图或使用说明。
- 13: `# 3. Trip - 行程表` -> 注释行，用于解释设计意图或使用说明。
- 14: `# 4. TripItem - 行程项目表` -> 注释行，用于解释设计意图或使用说明。
- 15: `# 5. UserLike - 点赞表` -> 注释行，用于解释设计意图或使用说明。
- 16: `# 6. Favorite - 收藏表` -> 注释行，用于解释设计意图或使用说明。
- 17: `# 7. UserFootprint - 用户足迹表` -> 注释行，用于解释设计意图或使用说明。
- 18: `# 8. DestinationComment - 景点评论表` -> 注释行，用于解释设计意图或使用说明。
- 19: `# 9. Notification - 通知表` -> 注释行，用于解释设计意图或使用说明。
- 20: `# 10. Page - 动态页面表` -> 注释行，用于解释设计意图或使用说明。
- 21: `# 11. SiteConfig - 网站配置表` -> 注释行，用于解释设计意图或使用说明。
- 22: `# 12. Menu - 菜单表` -> 注释行，用于解释设计意图或使用说明。
- 23: `# 13. Order - 订单表` -> 注释行，用于解释设计意图或使用说明。
- 24: `# 14. OrderItem - 订单明细表` -> 注释行，用于解释设计意图或使用说明。
- 25: `#` -> 注释行，用于解释设计意图或使用说明。
- 26: `# 【索引设计】` -> 注释行，用于解释设计意图或使用说明。
- 27: `# - 所有外键字段都添加了索引` -> 注释行，用于解释设计意图或使用说明。
- 28: `# - 常用查询字段（name, city, rating等）添加了索引` -> 注释行，用于解释设计意图或使用说明。
- 29: `# - 定义了复合索引优化多条件查询` -> 注释行，用于解释设计意图或使用说明。
- 30: `(空行)` -> 空行，用于提升代码结构可读性。
- 31: `# 导入JSON处理模块，用于数据序列化` -> 注释行，用于解释设计意图或使用说明。
- 32: `import json` -> 导入依赖模块，供当前文件使用。
- 33: `# 导入日期时间处理模块` -> 注释行，用于解释设计意图或使用说明。
- 34: `from datetime import datetime, time, date` -> 执行当前语句，参与该文件整体逻辑。
- 35: `(空行)` -> 空行，用于提升代码结构可读性。
- 36: `# 导入SQLAlchemy原生SQL执行支持` -> 注释行，用于解释设计意图或使用说明。
- 37: `from sqlalchemy import text, or_` -> 执行当前语句，参与该文件整体逻辑。
- 38: `# 导入密码哈希工具` -> 注释行，用于解释设计意图或使用说明。
- 39: `from werkzeug.security import generate_password_hash, check_password_hash` -> 执行当前语句，参与该文件整体逻辑。
- 40: `(空行)` -> 空行，用于提升代码结构可读性。
- 41: `# 导入全局数据库实例` -> 注释行，用于解释设计意图或使用说明。
- 42: `from extensions import db` -> 执行当前语句，参与该文件整体逻辑。
- 43: `(空行)` -> 空行，用于提升代码结构可读性。
- 44: `(空行)` -> 空行，用于提升代码结构可读性。
- 45: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 46: `# Destination - 景点数据模型` -> 注释行，用于解释设计意图或使用说明。
- 47: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 48: `#` -> 注释行，用于解释设计意图或使用说明。
- 49: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 50: `# - 存储景点基本信息` -> 注释行，用于解释设计意图或使用说明。
- 51: `# - 支持按城市、省份、评分筛选` -> 注释行，用于解释设计意图或使用说明。
- 52: `# - 支持地图定位（经纬度）` -> 注释行，用于解释设计意图或使用说明。
- 53: `#` -> 注释行，用于解释设计意图或使用说明。
- 54: `# 【索引】` -> 注释行，用于解释设计意图或使用说明。
- 55: `# - name, city, province, rating, ticket_price, created_at, updated_at 单字段索引` -> 注释行，用于解释设计意图或使用说明。
- 56: `# - idx_destination_name_city 复合索引` -> 注释行，用于解释设计意图或使用说明。
- 57: `# - idx_destination_rating_price 复合索引` -> 注释行，用于解释设计意图或使用说明。
- 58: `(空行)` -> 空行，用于提升代码结构可读性。
- 59: `class Destination(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 60: `"""景点数据模型 - 包含性能优化索引"""` -> 执行当前语句，参与该文件整体逻辑。
- 61: `# 指定数据库表名为'destinations'` -> 注释行，用于解释设计意图或使用说明。
- 62: `__tablename__ = 'destinations'` -> 执行当前语句，参与该文件整体逻辑。
- 63: `(空行)` -> 空行，用于提升代码结构可读性。
- 64: `# 景点ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 65: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 66: `# 景点名称，必填，最大200字符，添加索引以加速查询` -> 注释行，用于解释设计意图或使用说明。
- 67: `name = db.Column(db.String(200), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 68: `# 所在城市，必填，最大100字符，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 69: `city = db.Column(db.String(100), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 70: `# 所在省份，必填，最大50字符，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 71: `province = db.Column(db.String(50), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 72: `# 景点描述，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 73: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 74: `# 封面图片URL，可选，最大500字符` -> 注释行，用于解释设计意图或使用说明。
- 75: `cover_image = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 76: `# 评分，浮点数，默认5.0，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 77: `rating = db.Column(db.Float, default=5.0, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 78: `# 门票价格，浮点数，默认0（免费），添加索引` -> 注释行，用于解释设计意图或使用说明。
- 79: `ticket_price = db.Column(db.Float, default=0, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 80: `# 开放时间，字符串格式` -> 注释行，用于解释设计意图或使用说明。
- 81: `open_time = db.Column(db.String(100))` -> 执行当前语句，参与该文件整体逻辑。
- 82: `# 经度，可选，用于地图展示和周边查询` -> 注释行，用于解释设计意图或使用说明。
- 83: `lng = db.Column(db.Float)` -> 执行当前语句，参与该文件整体逻辑。
- 84: `# 纬度，可选，用于地图展示和周边查询` -> 注释行，用于解释设计意图或使用说明。
- 85: `lat = db.Column(db.Float)` -> 执行当前语句，参与该文件整体逻辑。
- 86: `# 创建时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 87: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 88: `# 更新时间，默认当前时间，更新时自动刷新，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 89: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 90: `(空行)` -> 空行，用于提升代码结构可读性。
- 91: `# 定义复合索引以优化多条件查询` -> 注释行，用于解释设计意图或使用说明。
- 92: `__table_args__ = (` -> 执行当前语句，参与该文件整体逻辑。
- 93: `# 名称和城市的复合索引，用于按名称和城市搜索` -> 注释行，用于解释设计意图或使用说明。
- 94: `db.Index('idx_destination_name_city', 'name', 'city'),` -> 执行当前语句，参与该文件整体逻辑。
- 95: `# 评分和价格的复合索引，用于按评分和价格排序筛选` -> 注释行，用于解释设计意图或使用说明。
- 96: `db.Index('idx_destination_rating_price', 'rating', 'ticket_price'),` -> 执行当前语句，参与该文件整体逻辑。
- 97: `)` -> 结束当前语句或代码块。
- 98: `(空行)` -> 空行，用于提升代码结构可读性。
- 99: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 100: `"""将模型对象转换为字典格式，便于JSON序列化"""` -> 执行当前语句，参与该文件整体逻辑。
- 101: `return {` -> 返回结果或提前结束当前流程。
- 102: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 103: `'name': self.name,` -> 执行当前语句，参与该文件整体逻辑。
- 104: `'city': self.city,` -> 执行当前语句，参与该文件整体逻辑。
- 105: `'province': self.province,` -> 执行当前语句，参与该文件整体逻辑。
- 106: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 107: `'cover_image': self.cover_image,` -> 执行当前语句，参与该文件整体逻辑。
- 108: `'rating': self.rating,` -> 执行当前语句，参与该文件整体逻辑。
- 109: `'ticket_price': self.ticket_price,` -> 执行当前语句，参与该文件整体逻辑。
- 110: `'open_time': self.open_time,` -> 执行当前语句，参与该文件整体逻辑。
- 111: `'lat': self.lat,` -> 执行当前语句，参与该文件整体逻辑。
- 112: `'lng': self.lng,` -> 执行当前语句，参与该文件整体逻辑。
- 113: `# 将datetime对象转换为ISO格式字符串` -> 注释行，用于解释设计意图或使用说明。
- 114: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 115: `'updated_at': self.updated_at.isoformat() if self.updated_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 116: `}` -> 结束当前语句或代码块。
- 117: `(空行)` -> 空行，用于提升代码结构可读性。
- 118: `(空行)` -> 空行，用于提升代码结构可读性。
- 119: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 120: `# User - 用户数据模型` -> 注释行，用于解释设计意图或使用说明。
- 121: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 122: `#` -> 注释行，用于解释设计意图或使用说明。
- 123: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 124: `# - 存储用户基本信息` -> 注释行，用于解释设计意图或使用说明。
- 125: `# - 支持手机号/邮箱/用户名登录` -> 注释行，用于解释设计意图或使用说明。
- 126: `# - 支持会员等级和积分系统` -> 注释行，用于解释设计意图或使用说明。
- 127: `# - 支持邀请码和邀请关系` -> 注释行，用于解释设计意图或使用说明。
- 128: `#` -> 注释行，用于解释设计意图或使用说明。
- 129: `# 【认证方式】` -> 注释行，用于解释设计意图或使用说明。
- 130: `# - 手机号登录` -> 注释行，用于解释设计意图或使用说明。
- 131: `# - 邮箱登录` -> 注释行，用于解释设计意图或使用说明。
- 132: `# - 用户名登录` -> 注释行，用于解释设计意图或使用说明。
- 133: `# - 密码使用 PBKDF2+SHA256 加密存储` -> 注释行，用于解释设计意图或使用说明。
- 134: `(空行)` -> 空行，用于提升代码结构可读性。
- 135: `class User(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 136: `"""用户数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 137: `__tablename__ = 'users'` -> 执行当前语句，参与该文件整体逻辑。
- 138: `(空行)` -> 空行，用于提升代码结构可读性。
- 139: `# 用户ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 140: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 141: `# 用户名，唯一，必填，最大80字符，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 142: `username = db.Column(db.String(80), unique=True, nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 143: `# 昵称，用于展示，可选，最大80字符` -> 注释行，用于解释设计意图或使用说明。
- 144: `nickname = db.Column(db.String(80))` -> 执行当前语句，参与该文件整体逻辑。
- 145: `# 邮箱地址，唯一，最大120字符，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 146: `email = db.Column(db.String(120), unique=True, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 147: `# 手机号码，唯一，最大20字符，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 148: `phone = db.Column(db.String(20), unique=True, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 149: `# 密码哈希值，存储加密后的密码` -> 注释行，用于解释设计意图或使用说明。
- 150: `password_hash = db.Column(db.String(255))` -> 执行当前语句，参与该文件整体逻辑。
- 151: `# 是否为管理员，布尔值，默认False` -> 注释行，用于解释设计意图或使用说明。
- 152: `is_admin = db.Column(db.Boolean, default=False)` -> 执行当前语句，参与该文件整体逻辑。
- 153: `# 用户头像URL，最大500字符` -> 注释行，用于解释设计意图或使用说明。
- 154: `avatar = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 155: `# 会员等级，整数，默认1级，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 156: `membership_level = db.Column(db.Integer, default=1, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 157: `# 积分，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 158: `points = db.Column(db.Integer, default=0, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 159: `# 邀请码（唯一）` -> 注释行，用于解释设计意图或使用说明。
- 160: `invite_code = db.Column(db.String(20), unique=True, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 161: `# 邀请人ID` -> 注释行，用于解释设计意图或使用说明。
- 162: `invited_by = db.Column(db.Integer, db.ForeignKey('users.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 163: `# 用户偏好设置，JSON格式存储` -> 注释行，用于解释设计意图或使用说明。
- 164: `preferences = db.Column(db.JSON)` -> 执行当前语句，参与该文件整体逻辑。
- 165: `# 账户创建时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 166: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 167: `# 最后登录时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 168: `last_login = db.Column(db.DateTime, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 169: `(空行)` -> 空行，用于提升代码结构可读性。
- 170: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 171: `"""将用户模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 172: `return {` -> 返回结果或提前结束当前流程。
- 173: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 174: `'username': self.username,` -> 执行当前语句，参与该文件整体逻辑。
- 175: `'nickname': self.nickname,` -> 执行当前语句，参与该文件整体逻辑。
- 176: `'email': self.email,` -> 执行当前语句，参与该文件整体逻辑。
- 177: `'phone': self.phone,` -> 执行当前语句，参与该文件整体逻辑。
- 178: `'is_admin': self.is_admin,` -> 执行当前语句，参与该文件整体逻辑。
- 179: `'avatar': self.avatar,` -> 执行当前语句，参与该文件整体逻辑。
- 180: `'membership_level': self.membership_level,` -> 执行当前语句，参与该文件整体逻辑。
- 181: `'points': self.points or 0,` -> 执行当前语句，参与该文件整体逻辑。
- 182: `'preferences': self.preferences,` -> 执行当前语句，参与该文件整体逻辑。
- 183: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 184: `'last_login': self.last_login.isoformat() if self.last_login else None` -> 执行当前语句，参与该文件整体逻辑。
- 185: `}` -> 结束当前语句或代码块。
- 186: `(空行)` -> 空行，用于提升代码结构可读性。
- 187: `(空行)` -> 空行，用于提升代码结构可读性。
- 188: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 189: `# Trip - 行程数据模型` -> 注释行，用于解释设计意图或使用说明。
- 190: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 191: `#` -> 注释行，用于解释设计意图或使用说明。
- 192: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 193: `# - 存储用户创建的旅行行程` -> 注释行，用于解释设计意图或使用说明。
- 194: `# - 支持多个 TripItem（行程项目）` -> 注释行，用于解释设计意图或使用说明。
- 195: `# - 行程状态：planning(规划中)/confirmed(已确认)/completed(已完成)/cancelled(已取消)` -> 注释行，用于解释设计意图或使用说明。
- 196: `#` -> 注释行，用于解释设计意图或使用说明。
- 197: `# 【关联】` -> 注释行，用于解释设计意图或使用说明。
- 198: `# - 一对多：User → Trip（一个用户多个行程）` -> 注释行，用于解释设计意图或使用说明。
- 199: `# - 一对多：Trip → TripItem（一个行程多个项目）` -> 注释行，用于解释设计意图或使用说明。
- 200: `(空行)` -> 空行，用于提升代码结构可读性。
- 201: `class Trip(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 202: `"""行程数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 203: `__tablename__ = 'trips'` -> 执行当前语句，参与该文件整体逻辑。
- 204: `(空行)` -> 空行，用于提升代码结构可读性。
- 205: `# 行程ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 206: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 207: `# 关联的用户ID，外键引用users表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 208: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 209: `# 行程标题，必填，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 210: `title = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 211: `# 行程描述，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 212: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 213: `# 行程开始日期，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 214: `start_date = db.Column(db.Date, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 215: `# 行程结束日期，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 216: `end_date = db.Column(db.Date, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 217: `# 行程状态，字符串，默认'planning'（规划中），添加索引` -> 注释行，用于解释设计意图或使用说明。
- 218: `status = db.Column(db.String(20), default='planning', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 219: `# 创建时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 220: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 221: `# 更新时间，默认当前时间，更新时自动刷新，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 222: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 223: `(空行)` -> 空行，用于提升代码结构可读性。
- 224: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 225: `"""将行程模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 226: `return {` -> 返回结果或提前结束当前流程。
- 227: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 228: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 229: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 230: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 231: `# 将date对象转换为ISO格式字符串` -> 注释行，用于解释设计意图或使用说明。
- 232: `'start_date': self.start_date.isoformat() if self.start_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 233: `'end_date': self.end_date.isoformat() if self.end_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 234: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 235: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 236: `'updated_at': self.updated_at.isoformat() if self.updated_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 237: `}` -> 结束当前语句或代码块。
- 238: `(空行)` -> 空行，用于提升代码结构可读性。
- 239: `(空行)` -> 空行，用于提升代码结构可读性。
- 240: `class TripItem(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 241: `"""行程项目数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 242: `__tablename__ = 'trip_items'` -> 执行当前语句，参与该文件整体逻辑。
- 243: `(空行)` -> 空行，用于提升代码结构可读性。
- 244: `# 行程项目ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 245: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 246: `# 关联的行程ID，外键引用trips表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 247: `trip_id = db.Column(db.Integer, db.ForeignKey('trips.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 248: `# 关联的景点ID，外键引用destinations表的id字段，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 249: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 250: `# 第几天的行程，整数，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 251: `day_number = db.Column(db.Integer, nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 252: `# 项目标题，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 253: `title = db.Column(db.String(200))` -> 执行当前语句，参与该文件整体逻辑。
- 254: `# 项目描述，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 255: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 256: `# 项目地点，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 257: `location = db.Column(db.String(200))` -> 执行当前语句，参与该文件整体逻辑。
- 258: `# 开始时间，time类型` -> 注释行，用于解释设计意图或使用说明。
- 259: `start_time = db.Column(db.Time)` -> 执行当前语句，参与该文件整体逻辑。
- 260: `# 结束时间，time类型` -> 注释行，用于解释设计意图或使用说明。
- 261: `end_time = db.Column(db.Time)` -> 执行当前语句，参与该文件整体逻辑。
- 262: `# 排序顺序，整数，默认0，数值越小越靠前` -> 注释行，用于解释设计意图或使用说明。
- 263: `sort_order = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 264: `# 创建时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 265: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 266: `(空行)` -> 空行，用于提升代码结构可读性。
- 267: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 268: `"""将行程项目模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 269: `return {` -> 返回结果或提前结束当前流程。
- 270: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 271: `'trip_id': self.trip_id,` -> 执行当前语句，参与该文件整体逻辑。
- 272: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 273: `'day_number': self.day_number,` -> 执行当前语句，参与该文件整体逻辑。
- 274: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 275: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 276: `'location': self.location,` -> 执行当前语句，参与该文件整体逻辑。
- 277: `# 将time对象转换为ISO格式字符串` -> 注释行，用于解释设计意图或使用说明。
- 278: `'start_time': self.start_time.isoformat() if self.start_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 279: `'end_time': self.end_time.isoformat() if self.end_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 280: `'sort_order': self.sort_order` -> 执行当前语句，参与该文件整体逻辑。
- 281: `}` -> 结束当前语句或代码块。
- 282: `(空行)` -> 空行，用于提升代码结构可读性。
- 283: `(空行)` -> 空行，用于提升代码结构可读性。
- 284: `class UserLike(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 285: `"""用户点赞数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 286: `__tablename__ = 'user_likes'` -> 执行当前语句，参与该文件整体逻辑。
- 287: `(空行)` -> 空行，用于提升代码结构可读性。
- 288: `# 点赞记录ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 289: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 290: `# 关联的用户ID，外键引用users表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 291: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 292: `# 关联的景点ID，外键引用destinations表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 293: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 294: `# 点赞时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 295: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 296: `(空行)` -> 空行，用于提升代码结构可读性。
- 297: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 298: `"""将点赞模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 299: `return {` -> 返回结果或提前结束当前流程。
- 300: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 301: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 302: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 303: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 304: `}` -> 结束当前语句或代码块。
- 305: `(空行)` -> 空行，用于提升代码结构可读性。
- 306: `(空行)` -> 空行，用于提升代码结构可读性。
- 307: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 308: `# Favorite - 用户收藏数据模型` -> 注释行，用于解释设计意图或使用说明。
- 309: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 310: `#` -> 注释行，用于解释设计意图或使用说明。
- 311: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 312: `# - 记录用户收藏的景点` -> 注释行，用于解释设计意图或使用说明。
- 313: `# - 便于用户快速访问喜欢的景点` -> 注释行，用于解释设计意图或使用说明。
- 314: `#` -> 注释行，用于解释设计意图或使用说明。
- 315: `# 【关联】` -> 注释行，用于解释设计意图或使用说明。
- 316: `# - 多对一：User（多个收藏属于一个用户）` -> 注释行，用于解释设计意图或使用说明。
- 317: `# - 多对一：Destination（多个收藏可以针对同一景点）` -> 注释行，用于解释设计意图或使用说明。
- 318: `(空行)` -> 空行，用于提升代码结构可读性。
- 319: `class Favorite(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 320: `"""用户收藏数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 321: `__tablename__ = 'favorite'` -> 执行当前语句，参与该文件整体逻辑。
- 322: `# 收藏记录ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 323: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 324: `# 关联的用户ID，外键引用user表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 325: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 326: `# 关联的景点ID，外键引用destinations表的id字段，必填，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 327: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 328: `# 收藏时间，默认当前时间，添加索引` -> 注释行，用于解释设计意图或使用说明。
- 329: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 330: `(空行)` -> 空行，用于提升代码结构可读性。
- 331: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 332: `"""将收藏模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 333: `return {` -> 返回结果或提前结束当前流程。
- 334: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 335: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 336: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 337: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 338: `}` -> 结束当前语句或代码块。
- 339: `(空行)` -> 空行，用于提升代码结构可读性。
- 340: `(空行)` -> 空行，用于提升代码结构可读性。
- 341: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 342: `# UserFootprint - 用户足迹数据模型` -> 注释行，用于解释设计意图或使用说明。
- 343: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 344: `#` -> 注释行，用于解释设计意图或使用说明。
- 345: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 346: `# - 记录用户浏览过的景点` -> 注释行，用于解释设计意图或使用说明。
- 347: `# - 用于个性化推荐` -> 注释行，用于解释设计意图或使用说明。
- 348: `# - 记录浏览时间` -> 注释行，用于解释设计意图或使用说明。
- 349: `(空行)` -> 空行，用于提升代码结构可读性。
- 350: `class UserFootprint(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 351: `"""用户足迹数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 352: `__tablename__ = 'user_footprint'` -> 执行当前语句，参与该文件整体逻辑。
- 353: `(空行)` -> 空行，用于提升代码结构可读性。
- 354: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 355: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 356: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 357: `view_time = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 358: `view_duration = db.Column(db.Integer, default=0)  # 浏览时长（秒）` -> 执行当前语句，参与该文件整体逻辑。
- 359: `(空行)` -> 空行，用于提升代码结构可读性。
- 360: `# 关联的景点对象，使用joined加载策略立即加载` -> 注释行，用于解释设计意图或使用说明。
- 361: `destination = db.relationship('Destination', foreign_keys=[destination_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 362: `(空行)` -> 空行，用于提升代码结构可读性。
- 363: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 364: `d = self.destination` -> 执行当前语句，参与该文件整体逻辑。
- 365: `return {` -> 返回结果或提前结束当前流程。
- 366: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 367: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 368: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 369: `'view_time': self.view_time.isoformat() if self.view_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 370: `'view_duration': self.view_duration,` -> 执行当前语句，参与该文件整体逻辑。
- 371: `'destination': d.to_dict() if d else None,` -> 执行当前语句，参与该文件整体逻辑。
- 372: `}` -> 结束当前语句或代码块。
- 373: `(空行)` -> 空行，用于提升代码结构可读性。
- 374: `(空行)` -> 空行，用于提升代码结构可读性。
- 375: `class DestinationComment(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 376: `"""景点评论模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 377: `__tablename__ = 'destination_comment'` -> 执行当前语句，参与该文件整体逻辑。
- 378: `(空行)` -> 空行，用于提升代码结构可读性。
- 379: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 380: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 381: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 382: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 383: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 384: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 385: `(空行)` -> 空行，用于提升代码结构可读性。
- 386: `# 关联关系` -> 注释行，用于解释设计意图或使用说明。
- 387: `author = db.relationship('User', foreign_keys=[user_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 388: `(空行)` -> 空行，用于提升代码结构可读性。
- 389: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 390: `author = self.author` -> 执行当前语句，参与该文件整体逻辑。
- 391: `return {` -> 返回结果或提前结束当前流程。
- 392: `"id": self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 393: `"destination_id": self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 394: `"user_id": self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 395: `"content": self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 396: `"created_at": self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 397: `"updated_at": self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 398: `"user": {` -> 执行当前语句，参与该文件整体逻辑。
- 399: `"id": str(author.id) if author else str(self.user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 400: `"nickname": getattr(author, "nickname", "") if author else "",` -> 执行当前语句，参与该文件整体逻辑。
- 401: `"avatar_url": getattr(author, "avatar", None) if author else None,` -> 执行当前语句，参与该文件整体逻辑。
- 402: `},` -> 执行当前语句，参与该文件整体逻辑。
- 403: `}` -> 结束当前语句或代码块。
- 404: `(空行)` -> 空行，用于提升代码结构可读性。
- 405: `(空行)` -> 空行，用于提升代码结构可读性。
- 406: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 407: `# Notification - 通知数据模型` -> 注释行，用于解释设计意图或使用说明。
- 408: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 409: `#` -> 注释行，用于解释设计意图或使用说明。
- 410: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 411: `# - 存储系统通知、订单通知、活动通知等` -> 注释行，用于解释设计意图或使用说明。
- 412: `# - 支持已读/未读状态` -> 注释行，用于解释设计意图或使用说明。
- 413: `# - 支持多种通知类型：system(系统)/booking(订单)/payment(支付)/promotion(活动)/service(服务)` -> 注释行，用于解释设计意图或使用说明。
- 414: `(空行)` -> 空行，用于提升代码结构可读性。
- 415: `class Notification(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 416: `"""通知数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 417: `__tablename__ = 'notification'` -> 执行当前语句，参与该文件整体逻辑。
- 418: `(空行)` -> 空行，用于提升代码结构可读性。
- 419: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 420: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 421: `type = db.Column(db.String(20), default='system', index=True)  # system, booking, payment, promotion, service` -> 定义类型或类结构，约束数据与行为。
- 422: `title = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 423: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 424: `is_read = db.Column(db.Boolean, default=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 425: `read_at = db.Column(db.DateTime)` -> 执行当前语句，参与该文件整体逻辑。
- 426: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 427: `(空行)` -> 空行，用于提升代码结构可读性。
- 428: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 429: `return {` -> 返回结果或提前结束当前流程。
- 430: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 431: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 432: `'type': self.type,` -> 执行当前语句，参与该文件整体逻辑。
- 433: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 434: `'content': self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 435: `'is_read': self.is_read,` -> 执行当前语句，参与该文件整体逻辑。
- 436: `'read_at': self.read_at.isoformat() if self.read_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 437: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 438: `}` -> 结束当前语句或代码块。
- 439: `(空行)` -> 空行，用于提升代码结构可读性。
- 440: `(空行)` -> 空行，用于提升代码结构可读性。
- 441: `class Page(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 442: `"""页面模型 - 存储动态页面内容"""` -> 执行当前语句，参与该文件整体逻辑。
- 443: `# 页面ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 444: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 445: `# 页面名称，唯一，必填，最大100字符` -> 注释行，用于解释设计意图或使用说明。
- 446: `name = db.Column(db.String(100), unique=True, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 447: `# 页面标题，必填，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 448: `title = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 449: `# 页面描述，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 450: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 451: `# HTML内容，必填，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 452: `html_content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 453: `# CSS样式内容，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 454: `css_content = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 455: `# JavaScript脚本内容，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 456: `js_content = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 457: `# 是否激活，布尔值，默认True` -> 注释行，用于解释设计意图或使用说明。
- 458: `is_active = db.Column(db.Boolean, default=True)` -> 执行当前语句，参与该文件整体逻辑。
- 459: `# 排序顺序，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 460: `sort_order = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 461: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 462: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 463: `# 更新时间，默认当前时间，更新时自动刷新` -> 注释行，用于解释设计意图或使用说明。
- 464: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 465: `(空行)` -> 空行，用于提升代码结构可读性。
- 466: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 467: `"""将页面模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 468: `return {` -> 返回结果或提前结束当前流程。
- 469: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 470: `'name': self.name,` -> 执行当前语句，参与该文件整体逻辑。
- 471: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 472: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 473: `'html_content': self.html_content,` -> 执行当前语句，参与该文件整体逻辑。
- 474: `'css_content': self.css_content,` -> 执行当前语句，参与该文件整体逻辑。
- 475: `'js_content': self.js_content,` -> 执行当前语句，参与该文件整体逻辑。
- 476: `'is_active': self.is_active,` -> 执行当前语句，参与该文件整体逻辑。
- 477: `'sort_order': self.sort_order` -> 执行当前语句，参与该文件整体逻辑。
- 478: `}` -> 结束当前语句或代码块。
- 479: `(空行)` -> 空行，用于提升代码结构可读性。
- 480: `(空行)` -> 空行，用于提升代码结构可读性。
- 481: `class SiteConfig(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 482: `"""网站配置模型 - 存储系统配置参数"""` -> 执行当前语句，参与该文件整体逻辑。
- 483: `# 配置ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 484: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 485: `# 配置键，唯一，必填，最大100字符` -> 注释行，用于解释设计意图或使用说明。
- 486: `key = db.Column(db.String(100), unique=True, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 487: `# 配置值，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 488: `value = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 489: `# 配置值类型，字符串，默认'string'` -> 注释行，用于解释设计意图或使用说明。
- 490: `value_type = db.Column(db.String(20), default='string')` -> 执行当前语句，参与该文件整体逻辑。
- 491: `# 配置分类，最大50字符` -> 注释行，用于解释设计意图或使用说明。
- 492: `category = db.Column(db.String(50))` -> 执行当前语句，参与该文件整体逻辑。
- 493: `# 配置描述，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 494: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 495: `# 是否公开，布尔值，默认False` -> 注释行，用于解释设计意图或使用说明。
- 496: `is_public = db.Column(db.Boolean, default=False)` -> 执行当前语句，参与该文件整体逻辑。
- 497: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 498: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 499: `# 更新时间，默认当前时间，更新时自动刷新` -> 注释行，用于解释设计意图或使用说明。
- 500: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 501: `(空行)` -> 空行，用于提升代码结构可读性。
- 502: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 503: `"""将配置模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 504: `return {` -> 返回结果或提前结束当前流程。
- 505: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 506: `'key': self.key,` -> 执行当前语句，参与该文件整体逻辑。
- 507: `'value': self.value,` -> 执行当前语句，参与该文件整体逻辑。
- 508: `'value_type': self.value_type,` -> 执行当前语句，参与该文件整体逻辑。
- 509: `'category': self.category,` -> 执行当前语句，参与该文件整体逻辑。
- 510: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 511: `'is_public': self.is_public` -> 执行当前语句，参与该文件整体逻辑。
- 512: `}` -> 结束当前语句或代码块。
- 513: `(空行)` -> 空行，用于提升代码结构可读性。
- 514: `def get_typed_value(self):` -> 定义 Python 函数，封装可复用逻辑。
- 515: `"""获取类型化后的配置值，根据value_type转换为对应类型"""` -> 执行当前语句，参与该文件整体逻辑。
- 516: `# 如果类型是整数` -> 注释行，用于解释设计意图或使用说明。
- 517: `if self.value_type == 'int':` -> 条件判断分支，根据场景执行不同逻辑。
- 518: `return int(self.value) if self.value else 0` -> 返回结果或提前结束当前流程。
- 519: `# 如果类型是浮点数` -> 注释行，用于解释设计意图或使用说明。
- 520: `elif self.value_type == 'float':` -> 条件判断分支，根据场景执行不同逻辑。
- 521: `return float(self.value) if self.value else 0.0` -> 返回结果或提前结束当前流程。
- 522: `# 如果类型是布尔值` -> 注释行，用于解释设计意图或使用说明。
- 523: `elif self.value_type == 'bool':` -> 条件判断分支，根据场景执行不同逻辑。
- 524: `return self.value.lower() in ('true', '1', 'yes') if self.value else False` -> 返回结果或提前结束当前流程。
- 525: `# 如果类型是JSON` -> 注释行，用于解释设计意图或使用说明。
- 526: `elif self.value_type == 'json':` -> 条件判断分支，根据场景执行不同逻辑。
- 527: `import json` -> 导入依赖模块，供当前文件使用。
- 528: `return json.loads(self.value) if self.value else {}` -> 返回结果或提前结束当前流程。
- 529: `# 否则返回原始字符串值` -> 注释行，用于解释设计意图或使用说明。
- 530: `else:` -> 条件判断分支，根据场景执行不同逻辑。
- 531: `return self.value` -> 返回结果或提前结束当前流程。
- 532: `(空行)` -> 空行，用于提升代码结构可读性。
- 533: `(空行)` -> 空行，用于提升代码结构可读性。
- 534: `class Menu(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 535: `"""菜单模型 - 存储导航菜单信息"""` -> 执行当前语句，参与该文件整体逻辑。
- 536: `# 菜单ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 537: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 538: `# 菜单名称，必填，最大100字符` -> 注释行，用于解释设计意图或使用说明。
- 539: `name = db.Column(db.String(100), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 540: `# 菜单URL，最大500字符` -> 注释行，用于解释设计意图或使用说明。
- 541: `url = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 542: `# 菜单图标，最大100字符` -> 注释行，用于解释设计意图或使用说明。
- 543: `icon = db.Column(db.String(100))` -> 执行当前语句，参与该文件整体逻辑。
- 544: `# 父菜单ID，外键引用menu表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 545: `parent_id = db.Column(db.Integer, db.ForeignKey('menu.id'))` -> 执行当前语句，参与该文件整体逻辑。
- 546: `# 排序顺序，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 547: `sort_order = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 548: `# 是否激活，布尔值，默认True` -> 注释行，用于解释设计意图或使用说明。
- 549: `is_active = db.Column(db.Boolean, default=True)` -> 执行当前语句，参与该文件整体逻辑。
- 550: `# 菜单类型，字符串，默认'main'（主菜单）` -> 注释行，用于解释设计意图或使用说明。
- 551: `menu_type = db.Column(db.String(50), default='main')` -> 执行当前语句，参与该文件整体逻辑。
- 552: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 553: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 554: `(空行)` -> 空行，用于提升代码结构可读性。
- 555: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 556: `"""将菜单模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 557: `return {` -> 返回结果或提前结束当前流程。
- 558: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 559: `'name': self.name,` -> 执行当前语句，参与该文件整体逻辑。
- 560: `'url': self.url,` -> 执行当前语句，参与该文件整体逻辑。
- 561: `'icon': self.icon,` -> 执行当前语句，参与该文件整体逻辑。
- 562: `'parent_id': self.parent_id,` -> 执行当前语句，参与该文件整体逻辑。
- 563: `'sort_order': self.sort_order,` -> 执行当前语句，参与该文件整体逻辑。
- 564: `'is_active': self.is_active,` -> 执行当前语句，参与该文件整体逻辑。
- 565: `'menu_type': self.menu_type` -> 执行当前语句，参与该文件整体逻辑。
- 566: `}` -> 结束当前语句或代码块。
- 567: `(空行)` -> 空行，用于提升代码结构可读性。
- 568: `(空行)` -> 空行，用于提升代码结构可读性。
- 569: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 570: `# Order - 订单数据模型` -> 注释行，用于解释设计意图或使用说明。
- 571: `# =============================================` -> 注释行，用于解释设计意图或使用说明。
- 572: `#` -> 注释行，用于解释设计意图或使用说明。
- 573: `# 【功能】` -> 注释行，用于解释设计意图或使用说明。
- 574: `# - 存储用户订单信息` -> 注释行，用于解释设计意图或使用说明。
- 575: `# - 记录订单状态：pending(待支付)/paid(已支付)/cancelled(已取消)/refunded(已退款)` -> 注释行，用于解释设计意图或使用说明。
- 576: `# - 支持多种支付方式` -> 注释行，用于解释设计意图或使用说明。
- 577: `#` -> 注释行，用于解释设计意图或使用说明。
- 578: `# 【关联】` -> 注释行，用于解释设计意图或使用说明。
- 579: `# - 一对多：User → Order` -> 注释行，用于解释设计意图或使用说明。
- 580: `# - 一对多：Order → OrderItem` -> 注释行，用于解释设计意图或使用说明。
- 581: `(空行)` -> 空行，用于提升代码结构可读性。
- 582: `class Order(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 583: `"""订单模型 - 存储用户订单信息"""` -> 执行当前语句，参与该文件整体逻辑。
- 584: `# 订单ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 585: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 586: `# 关联的用户ID，外键引用user表的id字段，必填` -> 注释行，用于解释设计意图或使用说明。
- 587: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 588: `# 订单号，唯一，必填，最大80字符` -> 注释行，用于解释设计意图或使用说明。
- 589: `order_no = db.Column(db.String(80), unique=True, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 590: `# 订单总金额，浮点数，默认0.0` -> 注释行，用于解释设计意图或使用说明。
- 591: `total_amount = db.Column(db.Float, default=0.0)` -> 执行当前语句，参与该文件整体逻辑。
- 592: `# 订单状态，字符串，默认'pending'（待支付）` -> 注释行，用于解释设计意图或使用说明。
- 593: `status = db.Column(db.String(30), default='pending')` -> 执行当前语句，参与该文件整体逻辑。
- 594: `# 支付方式，字符串，默认'alipay'（支付宝）` -> 注释行，用于解释设计意图或使用说明。
- 595: `payment_method = db.Column(db.String(30), default='alipay')` -> 执行当前语句，参与该文件整体逻辑。
- 596: `# 支付时间` -> 注释行，用于解释设计意图或使用说明。
- 597: `payment_time = db.Column(db.DateTime, nullable=True)` -> 执行当前语句，参与该文件整体逻辑。
- 598: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 599: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 600: `# 更新时间，默认当前时间，更新时自动刷新` -> 注释行，用于解释设计意图或使用说明。
- 601: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 602: `(空行)` -> 空行，用于提升代码结构可读性。
- 603: `# 关联的订单项列表，级联删除` -> 注释行，用于解释设计意图或使用说明。
- 604: `items = db.relationship('OrderItem', backref='order', cascade='all, delete-orphan')` -> 执行当前语句，参与该文件整体逻辑。
- 605: `(空行)` -> 空行，用于提升代码结构可读性。
- 606: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 607: `"""将订单模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 608: `return {` -> 返回结果或提前结束当前流程。
- 609: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 610: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 611: `'order_no': self.order_no,` -> 执行当前语句，参与该文件整体逻辑。
- 612: `'total_amount': self.total_amount,` -> 执行当前语句，参与该文件整体逻辑。
- 613: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 614: `'payment_method': self.payment_method,` -> 执行当前语句，参与该文件整体逻辑。
- 615: `'payment_time': self.payment_time.isoformat() if self.payment_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 616: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 617: `'updated_at': self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 618: `'items': [item.to_dict() for item in self.items] if self.items else []` -> 执行当前语句，参与该文件整体逻辑。
- 619: `}` -> 结束当前语句或代码块。
- 620: `(空行)` -> 空行，用于提升代码结构可读性。
- 621: `(空行)` -> 空行，用于提升代码结构可读性。
- 622: `class OrderItem(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 623: `"""订单明细模型 - 存储订单中的具体商品项"""` -> 执行当前语句，参与该文件整体逻辑。
- 624: `# 订单项ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 625: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 626: `# 关联的订单ID，外键引用order表的id字段，必填` -> 注释行，用于解释设计意图或使用说明。
- 627: `order_id = db.Column(db.Integer, db.ForeignKey('order.id'), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 628: `# 产品ID，必填，最大120字符` -> 注释行，用于解释设计意图或使用说明。
- 629: `product_id = db.Column(db.String(120), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 630: `# 产品名称，必填，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 631: `product_name = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 632: `# 产品类型，必填，最大50字符` -> 注释行，用于解释设计意图或使用说明。
- 633: `product_type = db.Column(db.String(50), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 634: `# 数量，整数，默认1` -> 注释行，用于解释设计意图或使用说明。
- 635: `quantity = db.Column(db.Integer, default=1)` -> 执行当前语句，参与该文件整体逻辑。
- 636: `# 单价，浮点数，默认0.0` -> 注释行，用于解释设计意图或使用说明。
- 637: `unit_price = db.Column(db.Float, default=0.0)` -> 执行当前语句，参与该文件整体逻辑。
- 638: `# 总价，浮点数，默认0.0` -> 注释行，用于解释设计意图或使用说明。
- 639: `total_price = db.Column(db.Float, default=0.0)` -> 执行当前语句，参与该文件整体逻辑。
- 640: `# 预订详情，JSON格式字符串` -> 注释行，用于解释设计意图或使用说明。
- 641: `booking_details = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 642: `(空行)` -> 空行，用于提升代码结构可读性。
- 643: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 644: `"""将订单项模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 645: `return {` -> 返回结果或提前结束当前流程。
- 646: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 647: `'order_id': self.order_id,` -> 执行当前语句，参与该文件整体逻辑。
- 648: `'product_id': self.product_id,` -> 执行当前语句，参与该文件整体逻辑。
- 649: `'product_name': self.product_name,` -> 执行当前语句，参与该文件整体逻辑。
- 650: `'product_type': self.product_type,` -> 执行当前语句，参与该文件整体逻辑。
- 651: `'quantity': self.quantity,` -> 执行当前语句，参与该文件整体逻辑。
- 652: `'unit_price': self.unit_price,` -> 执行当前语句，参与该文件整体逻辑。
- 653: `'total_price': self.total_price,` -> 执行当前语句，参与该文件整体逻辑。
- 654: `'booking_details': self.booking_details` -> 执行当前语句，参与该文件整体逻辑。
- 655: `}` -> 结束当前语句或代码块。
- 656: `(空行)` -> 空行，用于提升代码结构可读性。
- 657: `(空行)` -> 空行，用于提升代码结构可读性。
- 658: `class Product(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 659: `"""门票产品模型 - 存储景点门票产品信息"""` -> 执行当前语句，参与该文件整体逻辑。
- 660: `__tablename__ = 'product'` -> 执行当前语句，参与该文件整体逻辑。
- 661: `(空行)` -> 空行，用于提升代码结构可读性。
- 662: `# 产品ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 663: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 664: `# 产品名称，必填，最大200字符` -> 注释行，用于解释设计意图或使用说明。
- 665: `name = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 666: `# 产品副标题，最大500字符` -> 注释行，用于解释设计意图或使用说明。
- 667: `subtitle = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 668: `# 产品描述，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 669: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 670: `# 关联的景点ID，外键引用destinations表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 671: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=True, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 672: `# 产品类别，字符串，默认'ticket'（门票）` -> 注释行，用于解释设计意图或使用说明。
- 673: `category = db.Column(db.String(50), default='ticket', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 674: `# 产品类型（兼容前端），与 category 保持一致` -> 注释行，用于解释设计意图或使用说明。
- 675: `type = db.Column(db.String(20), default='ticket')` -> 定义类型或类结构，约束数据与行为。
- 676: `# 基础价格，浮点数，默认0.0` -> 注释行，用于解释设计意图或使用说明。
- 677: `base_price = db.Column(db.Float, default=0.0)` -> 执行当前语句，参与该文件整体逻辑。
- 678: `# 折扣价格，浮点数` -> 注释行，用于解释设计意图或使用说明。
- 679: `discount_price = db.Column(db.Float, nullable=True)` -> 执行当前语句，参与该文件整体逻辑。
- 680: `# 售价（兼容直接定价场景）` -> 注释行，用于解释设计意图或使用说明。
- 681: `price = db.Column(db.Float, default=0.0)` -> 执行当前语句，参与该文件整体逻辑。
- 682: `# 位置信息（JSON格式）` -> 注释行，用于解释设计意图或使用说明。
- 683: `location = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 684: `# 总库存，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 685: `inventory_total = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 686: `# 已售数量，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 687: `inventory_sold = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 688: `# 预订类型，字符串，默认'date'（按日期预订）` -> 注释行，用于解释设计意图或使用说明。
- 689: `booking_type = db.Column(db.String(20), default='date')` -> 执行当前语句，参与该文件整体逻辑。
- 690: `# 是否需要日期，布尔值，默认True` -> 注释行，用于解释设计意图或使用说明。
- 691: `need_date = db.Column(db.Boolean, default=True)` -> 执行当前语句，参与该文件整体逻辑。
- 692: `# 是否需要时间，布尔值，默认False` -> 注释行，用于解释设计意图或使用说明。
- 693: `need_time = db.Column(db.Boolean, default=False)` -> 执行当前语句，参与该文件整体逻辑。
- 694: `# 封面图片URL，最大500字符` -> 注释行，用于解释设计意图或使用说明。
- 695: `cover_image = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 696: `# 图片列表，JSON格式字符串` -> 注释行，用于解释设计意图或使用说明。
- 697: `images = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 698: `# 产品状态，字符串，默认'active'（上架）` -> 注释行，用于解释设计意图或使用说明。
- 699: `status = db.Column(db.String(20), default='active', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 700: `# 评分，浮点数，默认5.0` -> 注释行，用于解释设计意图或使用说明。
- 701: `rating = db.Column(db.Float, default=5.0, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 702: `# 销售数量，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 703: `sold_count = db.Column(db.Integer, default=0, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 704: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 705: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 706: `# 更新时间，默认当前时间，更新时自动刷新` -> 注释行，用于解释设计意图或使用说明。
- 707: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 708: `(空行)` -> 空行，用于提升代码结构可读性。
- 709: `# 关联的景点对象，使用joined加载策略立即加载` -> 注释行，用于解释设计意图或使用说明。
- 710: `destination = db.relationship('Destination', foreign_keys=[destination_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 711: `(空行)` -> 空行，用于提升代码结构可读性。
- 712: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 713: `"""将产品模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 714: `# 优先使用 price 字段，其次折扣价，最后基础价` -> 注释行，用于解释设计意图或使用说明。
- 715: `display_price = self.price if self.price else (self.discount_price if self.discount_price else self.base_price)` -> 执行当前语句，参与该文件整体逻辑。
- 716: `d = self.destination` -> 执行当前语句，参与该文件整体逻辑。
- 717: `return {` -> 返回结果或提前结束当前流程。
- 718: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 719: `'name': self.name,` -> 执行当前语句，参与该文件整体逻辑。
- 720: `'subtitle': self.subtitle,` -> 执行当前语句，参与该文件整体逻辑。
- 721: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 722: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 723: `'category': self.category,` -> 执行当前语句，参与该文件整体逻辑。
- 724: `'type': self.type or self.category,` -> 执行当前语句，参与该文件整体逻辑。
- 725: `# 优先显示 price，其次折扣价，最后基础价` -> 注释行，用于解释设计意图或使用说明。
- 726: `'price': display_price,` -> 执行当前语句，参与该文件整体逻辑。
- 727: `'base_price': self.base_price,` -> 执行当前语句，参与该文件整体逻辑。
- 728: `'discount_price': self.discount_price,` -> 执行当前语句，参与该文件整体逻辑。
- 729: `# 计算可用库存` -> 注释行，用于解释设计意图或使用说明。
- 730: `'inventory_available': max(0, self.inventory_total - self.inventory_sold),` -> 执行当前语句，参与该文件整体逻辑。
- 731: `'booking_type': self.booking_type,` -> 执行当前语句，参与该文件整体逻辑。
- 732: `'need_date': self.need_date,` -> 执行当前语句，参与该文件整体逻辑。
- 733: `'need_time': self.need_time,` -> 执行当前语句，参与该文件整体逻辑。
- 734: `'cover_image': self.cover_image,` -> 执行当前语句，参与该文件整体逻辑。
- 735: `# 解析图片列表` -> 注释行，用于解释设计意图或使用说明。
- 736: `'images': json.loads(self.images) if self.images else [],` -> 执行当前语句，参与该文件整体逻辑。
- 737: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 738: `'rating': self.rating,` -> 执行当前语句，参与该文件整体逻辑。
- 739: `'sold_count': self.sold_count,` -> 执行当前语句，参与该文件整体逻辑。
- 740: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 741: `# 解析位置信息` -> 注释行，用于解释设计意图或使用说明。
- 742: `'location': json.loads(self.location) if self.location else None,` -> 执行当前语句，参与该文件整体逻辑。
- 743: `# 关联景点信息（用于行程展示）` -> 注释行，用于解释设计意图或使用说明。
- 744: `'destination': {` -> 执行当前语句，参与该文件整体逻辑。
- 745: `'id': d.id,` -> 执行当前语句，参与该文件整体逻辑。
- 746: `'name': d.name,` -> 执行当前语句，参与该文件整体逻辑。
- 747: `'city': d.city,` -> 执行当前语句，参与该文件整体逻辑。
- 748: `'open_time': d.open_time,` -> 执行当前语句，参与该文件整体逻辑。
- 749: `'description': d.description,` -> 执行当前语句，参与该文件整体逻辑。
- 750: `'ticket_price': d.ticket_price,` -> 执行当前语句，参与该文件整体逻辑。
- 751: `} if d else None,` -> 执行当前语句，参与该文件整体逻辑。
- 752: `}` -> 结束当前语句或代码块。
- 753: `(空行)` -> 空行，用于提升代码结构可读性。
- 754: `(空行)` -> 空行，用于提升代码结构可读性。
- 755: `class ProductReview(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 756: `"""产品评价模型 - 存储用户对产品的评价"""` -> 执行当前语句，参与该文件整体逻辑。
- 757: `__tablename__ = 'product_review'` -> 执行当前语句，参与该文件整体逻辑。
- 758: `(空行)` -> 空行，用于提升代码结构可读性。
- 759: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 760: `product_id = db.Column(db.Integer, db.ForeignKey('product.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 761: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 762: `rating = db.Column(db.Integer, default=5, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 763: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 764: `images = db.Column(db.Text)  # JSON 格式存储图片 URL 列表` -> 执行当前语句，参与该文件整体逻辑。
- 765: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 766: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 767: `(空行)` -> 空行，用于提升代码结构可读性。
- 768: `# 关联关系` -> 注释行，用于解释设计意图或使用说明。
- 769: `author = db.relationship('User', foreign_keys=[user_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 770: `product = db.relationship('Product', foreign_keys=[product_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 771: `(空行)` -> 空行，用于提升代码结构可读性。
- 772: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 773: `author = self.author` -> 执行当前语句，参与该文件整体逻辑。
- 774: `return {` -> 返回结果或提前结束当前流程。
- 775: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 776: `'product_id': self.product_id,` -> 执行当前语句，参与该文件整体逻辑。
- 777: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 778: `'rating': self.rating,` -> 执行当前语句，参与该文件整体逻辑。
- 779: `'content': self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 780: `'images': json.loads(self.images) if self.images else [],` -> 执行当前语句，参与该文件整体逻辑。
- 781: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 782: `'updated_at': self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 783: `'user': {` -> 执行当前语句，参与该文件整体逻辑。
- 784: `'id': str(author.id) if author else str(self.user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 785: `'nickname': getattr(author, 'nickname', '') if author else '',` -> 执行当前语句，参与该文件整体逻辑。
- 786: `'avatar_url': getattr(author, 'avatar', None) if author else None,` -> 执行当前语句，参与该文件整体逻辑。
- 787: `},` -> 执行当前语句，参与该文件整体逻辑。
- 788: `}` -> 结束当前语句或代码块。
- 789: `(空行)` -> 空行，用于提升代码结构可读性。
- 790: `(空行)` -> 空行，用于提升代码结构可读性。
- 791: `class ProductQA(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 792: `"""产品问答模型 - 存储用户对产品的问题和答案"""` -> 执行当前语句，参与该文件整体逻辑。
- 793: `# 问答ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 794: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 795: `# 关联的产品ID，外键引用product表的id字段，必填` -> 注释行，用于解释设计意图或使用说明。
- 796: `product_id = db.Column(db.Integer, db.ForeignKey('product.id'), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 797: `# 关联的景点ID，外键引用destinations表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 798: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), nullable=True)` -> 执行当前语句，参与该文件整体逻辑。
- 799: `# 关联的用户ID，外键引用user表的id字段，必填` -> 注释行，用于解释设计意图或使用说明。
- 800: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 801: `# 问题内容，必填，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 802: `question = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 803: `# 答案内容，可选，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 804: `answer = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 805: `# 状态，字符串，默认'pending'（待回答）` -> 注释行，用于解释设计意图或使用说明。
- 806: `status = db.Column(db.String(20), default='pending')` -> 执行当前语句，参与该文件整体逻辑。
- 807: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 808: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 809: `# 回答时间` -> 注释行，用于解释设计意图或使用说明。
- 810: `answered_at = db.Column(db.DateTime)` -> 执行当前语句，参与该文件整体逻辑。
- 811: `# 关联的景点对象，使用joined加载策略立即加载` -> 注释行，用于解释设计意图或使用说明。
- 812: `destination = db.relationship('Destination', foreign_keys=[destination_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 813: `(空行)` -> 空行，用于提升代码结构可读性。
- 814: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 815: `"""将问答模型转换为字典格式"""` -> 执行当前语句，参与该文件整体逻辑。
- 816: `return {` -> 返回结果或提前结束当前流程。
- 817: `"id": self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 818: `"product_id": self.product_id,` -> 执行当前语句，参与该文件整体逻辑。
- 819: `"destination_id": self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 820: `"user_id": self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 821: `"question": self.question,` -> 执行当前语句，参与该文件整体逻辑。
- 822: `"answer": self.answer,` -> 执行当前语句，参与该文件整体逻辑。
- 823: `"status": self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 824: `"created_at": self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 825: `"answered_at": self.answered_at.isoformat() if self.answered_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 826: `}` -> 结束当前语句或代码块。
- 827: `(空行)` -> 空行，用于提升代码结构可读性。
- 828: `(空行)` -> 空行，用于提升代码结构可读性。
- 829: `class TravelNote(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 830: `"""游记/攻略模型 - 用户发布的旅行笔记"""` -> 执行当前语句，参与该文件整体逻辑。
- 831: `__tablename__ = 'travel_notes'` -> 执行当前语句，参与该文件整体逻辑。
- 832: `(空行)` -> 空行，用于提升代码结构可读性。
- 833: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 834: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 835: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 836: `title = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 837: `cover_image = db.Column(db.String(500))` -> 执行当前语句，参与该文件整体逻辑。
- 838: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 839: `tags = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 840: `view_count = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 841: `like_count = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 842: `status = db.Column(db.String(20), default='draft')` -> 执行当前语句，参与该文件整体逻辑。
- 843: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 844: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 845: `(空行)` -> 空行，用于提升代码结构可读性。
- 846: `user = db.relationship('User', backref='travel_notes', lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 847: `destination = db.relationship('Destination', backref='travel_notes', lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 848: `(空行)` -> 空行，用于提升代码结构可读性。
- 849: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 850: `return {` -> 返回结果或提前结束当前流程。
- 851: `"id": self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 852: `"user_id": self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 853: `"destination_id": self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 854: `"title": self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 855: `"cover_image": self.cover_image,` -> 执行当前语句，参与该文件整体逻辑。
- 856: `"content": self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 857: `"tags": json.loads(self.tags) if self.tags else [],` -> 执行当前语句，参与该文件整体逻辑。
- 858: `"view_count": self.view_count,` -> 执行当前语句，参与该文件整体逻辑。
- 859: `"like_count": self.like_count,` -> 执行当前语句，参与该文件整体逻辑。
- 860: `"status": self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 861: `"created_at": self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 862: `"updated_at": self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 863: `"user": {"id": self.user.id, "nickname": self.user.nickname, "avatar_url": self.user.avatar} if self.user else None,` -> 执行当前语句，参与该文件整体逻辑。
- 864: `"destination": {"id": self.destination.id, "name": self.destination.name, "city": self.destination.city} if self.destination else None,` -> 执行当前语句，参与该文件整体逻辑。
- 865: `}` -> 结束当前语句或代码块。
- 866: `(空行)` -> 空行，用于提升代码结构可读性。
- 867: `(空行)` -> 空行，用于提升代码结构可读性。
- 868: `class TravelNoteLike(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 869: `"""游记点赞模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 870: `__tablename__ = 'travel_note_likes'` -> 执行当前语句，参与该文件整体逻辑。
- 871: `(空行)` -> 空行，用于提升代码结构可读性。
- 872: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 873: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 874: `travel_note_id = db.Column(db.Integer, db.ForeignKey('travel_notes.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 875: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 876: `(空行)` -> 空行，用于提升代码结构可读性。
- 877: `__table_args__ = (db.UniqueConstraint('user_id', 'travel_note_id', name='uix_user_note_like'),)` -> 执行当前语句，参与该文件整体逻辑。
- 878: `(空行)` -> 空行，用于提升代码结构可读性。
- 879: `travel_note = db.relationship('TravelNote', backref='likes')` -> 执行当前语句，参与该文件整体逻辑。
- 880: `(空行)` -> 空行，用于提升代码结构可读性。
- 881: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 882: `return {` -> 返回结果或提前结束当前流程。
- 883: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 884: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 885: `'travel_note_id': self.travel_note_id,` -> 执行当前语句，参与该文件整体逻辑。
- 886: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 887: `}` -> 结束当前语句或代码块。
- 888: `(空行)` -> 空行，用于提升代码结构可读性。
- 889: `(空行)` -> 空行，用于提升代码结构可读性。
- 890: `class Coupon(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 891: `"""优惠券数据模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 892: `__tablename__ = 'coupons'` -> 执行当前语句，参与该文件整体逻辑。
- 893: `(空行)` -> 空行，用于提升代码结构可读性。
- 894: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 895: `code = db.Column(db.String(50), unique=True, nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 896: `name = db.Column(db.String(100), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 897: `type = db.Column(db.String(20), nullable=False)  # fixed/percent` -> 定义类型或类结构，约束数据与行为。
- 898: `value = db.Column(db.Float, nullable=False)  # 金额或折扣百分比` -> 执行当前语句，参与该文件整体逻辑。
- 899: `min_order = db.Column(db.Float, default=0)  # 最低订单金额` -> 执行当前语句，参与该文件整体逻辑。
- 900: `max_uses = db.Column(db.Integer, default=0)  # 0=无限` -> 执行当前语句，参与该文件整体逻辑。
- 901: `used_count = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 902: `start_date = db.Column(db.DateTime)` -> 执行当前语句，参与该文件整体逻辑。
- 903: `end_date = db.Column(db.DateTime)` -> 执行当前语句，参与该文件整体逻辑。
- 904: `status = db.Column(db.String(20), default='active')  # active/expired/disabled` -> 执行当前语句，参与该文件整体逻辑。
- 905: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 906: `(空行)` -> 空行，用于提升代码结构可读性。
- 907: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 908: `return {` -> 返回结果或提前结束当前流程。
- 909: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 910: `'code': self.code,` -> 执行当前语句，参与该文件整体逻辑。
- 911: `'name': self.name,` -> 执行当前语句，参与该文件整体逻辑。
- 912: `'type': self.type,` -> 执行当前语句，参与该文件整体逻辑。
- 913: `'value': self.value,` -> 执行当前语句，参与该文件整体逻辑。
- 914: `'min_order': self.min_order,` -> 执行当前语句，参与该文件整体逻辑。
- 915: `'max_uses': self.max_uses,` -> 执行当前语句，参与该文件整体逻辑。
- 916: `'used_count': self.used_count,` -> 执行当前语句，参与该文件整体逻辑。
- 917: `'start_date': self.start_date.isoformat() if self.start_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 918: `'end_date': self.end_date.isoformat() if self.end_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 919: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 920: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 921: `}` -> 结束当前语句或代码块。
- 922: `(空行)` -> 空行，用于提升代码结构可读性。
- 923: `(空行)` -> 空行，用于提升代码结构可读性。
- 924: `class UserCoupon(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 925: `"""用户优惠券领取记录模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 926: `__tablename__ = 'user_coupons'` -> 执行当前语句，参与该文件整体逻辑。
- 927: `(空行)` -> 空行，用于提升代码结构可读性。
- 928: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 929: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 930: `coupon_id = db.Column(db.Integer, db.ForeignKey('coupons.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 931: `is_used = db.Column(db.Boolean, default=False)` -> 执行当前语句，参与该文件整体逻辑。
- 932: `used_at = db.Column(db.DateTime)` -> 执行当前语句，参与该文件整体逻辑。
- 933: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 934: `(空行)` -> 空行，用于提升代码结构可读性。
- 935: `coupon = db.relationship('Coupon', backref='user_coupons')` -> 执行当前语句，参与该文件整体逻辑。
- 936: `(空行)` -> 空行，用于提升代码结构可读性。
- 937: `__table_args__ = (db.UniqueConstraint('user_id', 'coupon_id', name='uix_user_coupon'),)` -> 执行当前语句，参与该文件整体逻辑。
- 938: `(空行)` -> 空行，用于提升代码结构可读性。
- 939: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 940: `return {` -> 返回结果或提前结束当前流程。
- 941: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 942: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 943: `'coupon_id': self.coupon_id,` -> 执行当前语句，参与该文件整体逻辑。
- 944: `'is_used': self.is_used,` -> 执行当前语句，参与该文件整体逻辑。
- 945: `'used_at': self.used_at.isoformat() if self.used_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 946: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 947: `'coupon': self.coupon.to_dict() if self.coupon else None` -> 执行当前语句，参与该文件整体逻辑。
- 948: `}` -> 结束当前语句或代码块。
- 949: `(空行)` -> 空行，用于提升代码结构可读性。
- 950: `(空行)` -> 空行，用于提升代码结构可读性。
- 951: `class SupportTicket(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 952: `"""客服工单模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 953: `__tablename__ = 'support_tickets'` -> 执行当前语句，参与该文件整体逻辑。
- 954: `(空行)` -> 空行，用于提升代码结构可读性。
- 955: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 956: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 957: `title = db.Column(db.String(200), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 958: `description = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 959: `ticket_type = db.Column(db.String(20), default='other')` -> 执行当前语句，参与该文件整体逻辑。
- 960: `status = db.Column(db.String(20), default='open', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 961: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 962: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 963: `(空行)` -> 空行，用于提升代码结构可读性。
- 964: `# 关联` -> 注释行，用于解释设计意图或使用说明。
- 965: `user = db.relationship('User', foreign_keys=[user_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 966: `replies = db.relationship('TicketReply', backref='ticket', cascade='all, delete-orphan')` -> 执行当前语句，参与该文件整体逻辑。
- 967: `(空行)` -> 空行，用于提升代码结构可读性。
- 968: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 969: `return {` -> 返回结果或提前结束当前流程。
- 970: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 971: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 972: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 973: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 974: `'ticket_type': self.ticket_type,` -> 执行当前语句，参与该文件整体逻辑。
- 975: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 976: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 977: `'updated_at': self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 978: `}` -> 结束当前语句或代码块。
- 979: `(空行)` -> 空行，用于提升代码结构可读性。
- 980: `(空行)` -> 空行，用于提升代码结构可读性。
- 981: `class TicketReply(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 982: `"""工单回复模型"""` -> 执行当前语句，参与该文件整体逻辑。
- 983: `__tablename__ = 'ticket_replies'` -> 执行当前语句，参与该文件整体逻辑。
- 984: `(空行)` -> 空行，用于提升代码结构可读性。
- 985: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 986: `ticket_id = db.Column(db.Integer, db.ForeignKey('support_tickets.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 987: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 988: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 989: `is_admin = db.Column(db.Boolean, default=False)` -> 执行当前语句，参与该文件整体逻辑。
- 990: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 991: `(空行)` -> 空行，用于提升代码结构可读性。
- 992: `# 关联` -> 注释行，用于解释设计意图或使用说明。
- 993: `user = db.relationship('User', foreign_keys=[user_id], lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 994: `(空行)` -> 空行，用于提升代码结构可读性。
- 995: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 996: `user = self.user` -> 执行当前语句，参与该文件整体逻辑。
- 997: `return {` -> 返回结果或提前结束当前流程。
- 998: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 999: `'ticket_id': self.ticket_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1000: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1001: `'content': self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 1002: `'is_admin': self.is_admin,` -> 执行当前语句，参与该文件整体逻辑。
- 1003: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1004: `'user': {` -> 执行当前语句，参与该文件整体逻辑。
- 1005: `'id': str(user.id) if user else str(self.user_id),` -> 执行当前语句，参与该文件整体逻辑。
- 1006: `'nickname': getattr(user, 'nickname', '') if user else '',` -> 执行当前语句，参与该文件整体逻辑。
- 1007: `'avatar_url': getattr(user, 'avatar', None) if user else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1008: `} if user else None` -> 执行当前语句，参与该文件整体逻辑。
- 1009: `}` -> 结束当前语句或代码块。
- 1010: `(空行)` -> 空行，用于提升代码结构可读性。
- 1011: `(空行)` -> 空行，用于提升代码结构可读性。
- 1012: `class AIConversation(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 1013: `"""AI对话记录模型 - 存储用户与AI助手的完整对话历史"""` -> 执行当前语句，参与该文件整体逻辑。
- 1014: `__tablename__ = 'ai_conversations'` -> 执行当前语句，参与该文件整体逻辑。
- 1015: `(空行)` -> 空行，用于提升代码结构可读性。
- 1016: `# 对话记录ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 1017: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1018: `# 关联的用户ID，外键引用users表` -> 注释行，用于解释设计意图或使用说明。
- 1019: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=True, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1020: `# 会话ID，用于将多条对话关联到同一个会话` -> 注释行，用于解释设计意图或使用说明。
- 1021: `session_id = db.Column(db.String(100), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1022: `# 发言角色：user（用户）/ assistant（AI助手）/ system（系统）` -> 注释行，用于解释设计意图或使用说明。
- 1023: `role = db.Column(db.String(20), nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 1024: `# 对话内容` -> 注释行，用于解释设计意图或使用说明。
- 1025: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 1026: `# 意图类型：行程规划、景点咨询、酒店推荐等` -> 注释行，用于解释设计意图或使用说明。
- 1027: `intent = db.Column(db.String(50), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1028: `# 额外元数据，JSON格式存储（使用meta_data避免与SQLAlchemy保留字段冲突）` -> 注释行，用于解释设计意图或使用说明。
- 1029: `meta_data = db.Column(db.JSON)` -> 执行当前语句，参与该文件整体逻辑。
- 1030: `# 创建时间` -> 注释行，用于解释设计意图或使用说明。
- 1031: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1032: `(空行)` -> 空行，用于提升代码结构可读性。
- 1033: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 1034: `return {` -> 返回结果或提前结束当前流程。
- 1035: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 1036: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1037: `'session_id': self.session_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1038: `'role': self.role,` -> 执行当前语句，参与该文件整体逻辑。
- 1039: `'content': self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 1040: `'intent': self.intent,` -> 执行当前语句，参与该文件整体逻辑。
- 1041: `'metadata': self.meta_data,` -> 执行当前语句，参与该文件整体逻辑。
- 1042: `'created_at': self.created_at.isoformat() if self.created_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 1043: `}` -> 结束当前语句或代码块。
- 1044: `(空行)` -> 空行，用于提升代码结构可读性。
- 1045: `(空行)` -> 空行，用于提升代码结构可读性。
- 1046: `class TravelPlan(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 1047: `"""行程规划模型 - 存储由AI生成的完整行程规划"""` -> 执行当前语句，参与该文件整体逻辑。
- 1048: `__tablename__ = 'travel_plans'` -> 执行当前语句，参与该文件整体逻辑。
- 1049: `(空行)` -> 空行，用于提升代码结构可读性。
- 1050: `# 规划ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 1051: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1052: `# 关联的对话ID，外键引用ai_conversations表` -> 注释行，用于解释设计意图或使用说明。
- 1053: `conversation_id = db.Column(db.Integer, db.ForeignKey('ai_conversations.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1054: `# 关联的用户ID，外键引用users表` -> 注释行，用于解释设计意图或使用说明。
- 1055: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1056: `# 会话ID` -> 注释行，用于解释设计意图或使用说明。
- 1057: `session_id = db.Column(db.String(100), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1058: `# 规划名称` -> 注释行，用于解释设计意图或使用说明。
- 1059: `plan_name = db.Column(db.String(200))` -> 执行当前语句，参与该文件整体逻辑。
- 1060: `# 目的地` -> 注释行，用于解释设计意图或使用说明。
- 1061: `destination = db.Column(db.String(100), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1062: `# 开始日期` -> 注释行，用于解释设计意图或使用说明。
- 1063: `start_date = db.Column(db.Date)` -> 执行当前语句，参与该文件整体逻辑。
- 1064: `# 结束日期` -> 注释行，用于解释设计意图或使用说明。
- 1065: `end_date = db.Column(db.Date)` -> 执行当前语句，参与该文件整体逻辑。
- 1066: `# 预算` -> 注释行，用于解释设计意图或使用说明。
- 1067: `budget = db.Column(db.Float)` -> 执行当前语句，参与该文件整体逻辑。
- 1068: `# 偏好设置，JSON格式` -> 注释行，用于解释设计意图或使用说明。
- 1069: `preferences = db.Column(db.JSON)` -> 执行当前语句，参与该文件整体逻辑。
- 1070: `# 详细行程，JSON格式存储每日行程安排` -> 注释行，用于解释设计意图或使用说明。
- 1071: `plan_details = db.Column(db.JSON)` -> 执行当前语句，参与该文件整体逻辑。
- 1072: `# 状态：draft（草稿）/ confirmed（已确认）/ completed（已完成）/ cancelled（已取消）` -> 注释行，用于解释设计意图或使用说明。
- 1073: `status = db.Column(db.String(20), default='draft', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1074: `# 创建时间` -> 注释行，用于解释设计意图或使用说明。
- 1075: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1076: `# 更新时间` -> 注释行，用于解释设计意图或使用说明。
- 1077: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 1078: `(空行)` -> 空行，用于提升代码结构可读性。
- 1079: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 1080: `return {` -> 返回结果或提前结束当前流程。
- 1081: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 1082: `'conversation_id': self.conversation_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1083: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1084: `'session_id': self.session_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1085: `'plan_name': self.plan_name,` -> 执行当前语句，参与该文件整体逻辑。
- 1086: `'destination': self.destination,` -> 执行当前语句，参与该文件整体逻辑。
- 1087: `'start_date': self.start_date.isoformat() if self.start_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1088: `'end_date': self.end_date.isoformat() if self.end_date else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1089: `'budget': self.budget,` -> 执行当前语句，参与该文件整体逻辑。
- 1090: `'preferences': self.preferences,` -> 执行当前语句，参与该文件整体逻辑。
- 1091: `'plan_details': self.plan_details,` -> 执行当前语句，参与该文件整体逻辑。
- 1092: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 1093: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1094: `'updated_at': self.updated_at.isoformat() if self.updated_at else None` -> 执行当前语句，参与该文件整体逻辑。
- 1095: `}` -> 结束当前语句或代码块。
- 1096: `(空行)` -> 空行，用于提升代码结构可读性。
- 1097: `(空行)` -> 空行，用于提升代码结构可读性。
- 1098: `class PlanItem(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 1099: `"""行程项目模型 - 存储行程中的具体活动项目"""` -> 执行当前语句，参与该文件整体逻辑。
- 1100: `__tablename__ = 'plan_items'` -> 执行当前语句，参与该文件整体逻辑。
- 1101: `(空行)` -> 空行，用于提升代码结构可读性。
- 1102: `# 项目ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 1103: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1104: `# 关联的行程ID，外键引用travel_plans表` -> 注释行，用于解释设计意图或使用说明。
- 1105: `plan_id = db.Column(db.Integer, db.ForeignKey('travel_plans.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1106: `# 关联的景点ID，外键引用destinations表` -> 注释行，用于解释设计意图或使用说明。
- 1107: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1108: `# 第几天的行程` -> 注释行，用于解释设计意图或使用说明。
- 1109: `day_number = db.Column(db.Integer, nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1110: `# 排序顺序` -> 注释行，用于解释设计意图或使用说明。
- 1111: `sort_order = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 1112: `# 时间段：morning/afternoon/evening` -> 注释行，用于解释设计意图或使用说明。
- 1113: `time_slot = db.Column(db.String(20))` -> 执行当前语句，参与该文件整体逻辑。
- 1114: `# 活动类型：景点/餐厅/交通/酒店` -> 注释行，用于解释设计意图或使用说明。
- 1115: `activity_type = db.Column(db.String(50))` -> 执行当前语句，参与该文件整体逻辑。
- 1116: `# 标题` -> 注释行，用于解释设计意图或使用说明。
- 1117: `title = db.Column(db.String(200))` -> 执行当前语句，参与该文件整体逻辑。
- 1118: `# 描述` -> 注释行，用于解释设计意图或使用说明。
- 1119: `description = db.Column(db.Text)` -> 执行当前语句，参与该文件整体逻辑。
- 1120: `# 地点` -> 注释行，用于解释设计意图或使用说明。
- 1121: `location = db.Column(db.String(200))` -> 执行当前语句，参与该文件整体逻辑。
- 1122: `# 开始时间` -> 注释行，用于解释设计意图或使用说明。
- 1123: `start_time = db.Column(db.Time)` -> 执行当前语句，参与该文件整体逻辑。
- 1124: `# 结束时间` -> 注释行，用于解释设计意图或使用说明。
- 1125: `end_time = db.Column(db.Time)` -> 执行当前语句，参与该文件整体逻辑。
- 1126: `# 持续时间（分钟）` -> 注释行，用于解释设计意图或使用说明。
- 1127: `duration_minutes = db.Column(db.Integer)` -> 执行当前语句，参与该文件整体逻辑。
- 1128: `# 费用` -> 注释行，用于解释设计意图或使用说明。
- 1129: `cost = db.Column(db.Float)` -> 执行当前语句，参与该文件整体逻辑。
- 1130: `# 预订信息，JSON格式` -> 注释行，用于解释设计意图或使用说明。
- 1131: `booking_info = db.Column(db.JSON)` -> 执行当前语句，参与该文件整体逻辑。
- 1132: `# 创建时间` -> 注释行，用于解释设计意图或使用说明。
- 1133: `created_at = db.Column(db.DateTime, default=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 1134: `(空行)` -> 空行，用于提升代码结构可读性。
- 1135: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 1136: `return {` -> 返回结果或提前结束当前流程。
- 1137: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 1138: `'plan_id': self.plan_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1139: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1140: `'day_number': self.day_number,` -> 执行当前语句，参与该文件整体逻辑。
- 1141: `'sort_order': self.sort_order,` -> 执行当前语句，参与该文件整体逻辑。
- 1142: `'time_slot': self.time_slot,` -> 执行当前语句，参与该文件整体逻辑。
- 1143: `'activity_type': self.activity_type,` -> 执行当前语句，参与该文件整体逻辑。
- 1144: `'title': self.title,` -> 执行当前语句，参与该文件整体逻辑。
- 1145: `'description': self.description,` -> 执行当前语句，参与该文件整体逻辑。
- 1146: `'location': self.location,` -> 执行当前语句，参与该文件整体逻辑。
- 1147: `'start_time': self.start_time.isoformat() if self.start_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1148: `'end_time': self.end_time.isoformat() if self.end_time else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1149: `'duration_minutes': self.duration_minutes,` -> 执行当前语句，参与该文件整体逻辑。
- 1150: `'cost': self.cost,` -> 执行当前语句，参与该文件整体逻辑。
- 1151: `'booking_info': self.booking_info` -> 执行当前语句，参与该文件整体逻辑。
- 1152: `}` -> 结束当前语句或代码块。
- 1153: `(空行)` -> 空行，用于提升代码结构可读性。
- 1154: `(空行)` -> 空行，用于提升代码结构可读性。
- 1155: `class Comment(db.Model):` -> 定义类型或类结构，约束数据与行为。
- 1156: `"""评论模型 - 存储用户对景点/目的地的评论"""` -> 执行当前语句，参与该文件整体逻辑。
- 1157: `__tablename__ = 'comments'` -> 执行当前语句，参与该文件整体逻辑。
- 1158: `(空行)` -> 空行，用于提升代码结构可读性。
- 1159: `# 评论ID，主键，自增长` -> 注释行，用于解释设计意图或使用说明。
- 1160: `id = db.Column(db.Integer, primary_key=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1161: `# 关联的用户ID，外键引用users表的id字段，必填` -> 注释行，用于解释设计意图或使用说明。
- 1162: `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1163: `# 关联的目的地/景点ID，外键引用destinations表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 1164: `destination_id = db.Column(db.Integer, db.ForeignKey('destinations.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1165: `# 关联的产品ID，外键引用product表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 1166: `product_id = db.Column(db.Integer, db.ForeignKey('product.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1167: `# 关联的游记ID，外键引用travel_notes表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 1168: `travel_note_id = db.Column(db.Integer, db.ForeignKey('travel_notes.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1169: `# 父评论ID，用于回复功能，外键引用comments表的id字段` -> 注释行，用于解释设计意图或使用说明。
- 1170: `parent_id = db.Column(db.Integer, db.ForeignKey('comments.id'), index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1171: `# 评论内容，必填，文本类型` -> 注释行，用于解释设计意图或使用说明。
- 1172: `content = db.Column(db.Text, nullable=False)` -> 执行当前语句，参与该文件整体逻辑。
- 1173: `# 评分，浮点数，1-5分` -> 注释行，用于解释设计意图或使用说明。
- 1174: `rating = db.Column(db.Float, default=5.0)` -> 执行当前语句，参与该文件整体逻辑。
- 1175: `# 评论状态，字符串，默认'approved'（已通过）` -> 注释行，用于解释设计意图或使用说明。
- 1176: `status = db.Column(db.String(20), default='approved', index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1177: `# 点赞数量，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 1178: `like_count = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 1179: `# 回复数量，整数，默认0` -> 注释行，用于解释设计意图或使用说明。
- 1180: `reply_count = db.Column(db.Integer, default=0)` -> 执行当前语句，参与该文件整体逻辑。
- 1181: `# 创建时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 1182: `created_at = db.Column(db.DateTime, default=datetime.now, index=True)` -> 执行当前语句，参与该文件整体逻辑。
- 1183: `# 更新时间，默认当前时间` -> 注释行，用于解释设计意图或使用说明。
- 1184: `updated_at = db.Column(db.DateTime, default=datetime.now, onupdate=datetime.now)` -> 执行当前语句，参与该文件整体逻辑。
- 1185: `(空行)` -> 空行，用于提升代码结构可读性。
- 1186: `# 关联关系` -> 注释行，用于解释设计意图或使用说明。
- 1187: `user = db.relationship('User', backref='comments', lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 1188: `destination = db.relationship('Destination', backref='comments', lazy='joined')` -> 执行当前语句，参与该文件整体逻辑。
- 1189: `replies = db.relationship('Comment', backref=db.backref('parent', remote_side=[id]), lazy='dynamic')` -> 执行当前语句，参与该文件整体逻辑。
- 1190: `(空行)` -> 空行，用于提升代码结构可读性。
- 1191: `def to_dict(self):` -> 定义 Python 函数，封装可复用逻辑。
- 1192: `return {` -> 返回结果或提前结束当前流程。
- 1193: `'id': self.id,` -> 执行当前语句，参与该文件整体逻辑。
- 1194: `'user_id': self.user_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1195: `'destination_id': self.destination_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1196: `'product_id': self.product_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1197: `'travel_note_id': self.travel_note_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1198: `'parent_id': self.parent_id,` -> 执行当前语句，参与该文件整体逻辑。
- 1199: `'content': self.content,` -> 执行当前语句，参与该文件整体逻辑。
- 1200: `'rating': self.rating,` -> 执行当前语句，参与该文件整体逻辑。
- 1201: `'status': self.status,` -> 执行当前语句，参与该文件整体逻辑。
- 1202: `'like_count': self.like_count,` -> 执行当前语句，参与该文件整体逻辑。
- 1203: `'reply_count': self.reply_count,` -> 执行当前语句，参与该文件整体逻辑。
- 1204: `'created_at': self.created_at.isoformat() if self.created_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1205: `'updated_at': self.updated_at.isoformat() if self.updated_at else None,` -> 执行当前语句，参与该文件整体逻辑。
- 1206: `'user': {` -> 执行当前语句，参与该文件整体逻辑。
- 1207: `'id': self.user.id,` -> 执行当前语句，参与该文件整体逻辑。
- 1208: `'nickname': getattr(self.user, 'nickname', ''),` -> 执行当前语句，参与该文件整体逻辑。
- 1209: `'avatar_url': getattr(self.user, 'avatar', None),` -> 执行当前语句，参与该文件整体逻辑。
- 1210: `} if self.user else None` -> 执行当前语句，参与该文件整体逻辑。
- 1211: `}` -> 结束当前语句或代码块。
